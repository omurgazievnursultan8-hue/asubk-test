# Карта: сверка модуля анализа и проверка на сухость (очередь, пункт `A3`)

## Destination

Волна 7 модуля анализа — пункт **`A3`** очереди модулей (`docs/ochered-modulei.md:220`,
«Анализ: волна, проверка на сухость») — закрыта. Это значит:

- у модуля **есть раздел «Что модуль требует от соседей»** в
  `mockups/analysis/ASUBK-status-razrabotki.md`, которого до сих пор не было вовсе
  (`docs/ochered-modulei.md:262`, канон §14 последним абзацем). Форма — по образцу
  отчётности (`mockups/reports/ASUBK-status-razrabotki.md:454`), со столбцом **«состояние»**:
  выполнено · выполнено иначе · условное · открыто · не тронуто (у «не тронуто» в скобках —
  **жив ли адресат**);
- **входящий ящик разобран**: единственная строка, адресованная анализу соседями, —
  «убрать из модели обзора хранение чисел, оставить ссылку на выпуск» (`ADR-0157` §4) —
  принята в канон;
- канон и макет **сведены с двумя внешними углами**: Порядком №41 и канонами соседей;
- вынесен **вердикт о сухости** — с явно названным углом, с которого он вынесен.

## Notes

**Домен.** `mockups/analysis/`: канон `ASUBK-analiz-logika.md` (на конец волны — **v4**,
15 разделов, инварианты `ИА-1`…`ИА-20`; при черчении карты был v3 и `ИА-19`), макет
`analysis.html` (**2012 строк**, было 1877; single-file, раздел финанализа),
смоук `node scripts/inspect/analysis-check.mjs` (**52/52 PASS**, было 46/46; штамп в шапке макета), статус
`ASUBK-status-razrabotki.md`, постановка `docs/tasks/p21-analiz-tasks.html` (пакет `P21`,
22 карточки, 7 этапов, редакция 1). Волны 1–6 закрыты, открытых дефектов макета нет.

**Карта переносит исполнение внутрь себя** (override wayfinder-дефолта «plan, don't do»):
тикеты правят канон, макет, смоук, статус и постановку, а не только решают. Так шли все шесть
предыдущих волн модуля — волна, кончающаяся списком решений, по домашнему счёту не закрыта.

**Постановка `P21` разработке не выдана** (`docs/ochered-modulei.md` §3: выдана одна
классификация `P19`) — значит правится **точечно в редакции 1**, прецедент `АН-35`
(«исправлена в двух местах, а не переиздана»). ~~Редакция 2 зарезервирована за разделом
обзоров (`АН-29`, `АН-35`)~~ — **бронь отменена тикетом 02** (`АН-45`): редакция считается
**от выдачи**, а пакет не выдан, поэтому и раздел обзоров придёт в ту же редакцию 1.

**Два угла сверки, и оба названы заранее:**

1. **Порядок №41** — `docs/normativ/poryadok-2026-07-06-N41.txt`, редакция в силе от 06.07.2026.
   Проверяет, **правильно ли модуль устроен**.
2. **Каноны и постановки соседей.** Проверяют, **знает ли кто-нибудь, что модуль есть**.

Урок, ради которого углов два, записан отчётностью вслух
(`mockups/reports/ASUBK-status-razrabotki.md:485`): сухими у неё оказались три волны подряд,
шедшие **по своему макету**, а каждая сверка с внешним источником добавляла строку —
«сухость мерялась не исчерпанностью модуля, а исчерпанностью того угла, с которого на него
смотрели».

**Нумерация.** Решения волны — `АН-37`+ (волна 6 кончилась на `АН-36`). Дефекты макета —
`АН-Д7`+ (`АН-Д1`…`АН-Д6` закрыты). Инварианты — `ИА-20`+. Номер ADR берётся
`git ls-tree` **по всем ссылкам**, а не по рабочему дереву, и **пересчитывается перед
вливанием в `main`**: ветка чужого worktree держит номер, которого в `main` ещё нет
(`docs/adr/README.md`, §«Правило номера»).

**Домашний стиль, которому волна обязана следовать:**

- всякое утверждение о поведении макета — **прогоном** (`node:vm`), а не пересказом по
  памяти (`АН-26`: сверка вскрыла три собственных расхождения до сдачи; у классификации
  пересказ разошёлся с макетом шестью пунктами);
- где машина выбрала бы одно из двух правдоподобных — **отказ вместо молчания** (`АН-23`);
- правило живёт в одной двери, второй текст той же причины расходится с первым молча
  (`АН-20`, `АН-32`).

**Проверки перед закрытием тикетов.** `node scripts/inspect/analysis-check.mjs` (смоук),
`python3 scripts/check_tasks.py docs/tasks/p21-analiz-tasks.html` (постановка),
`python3 scripts/check_points.py` (цитаты «п. NN» против Порядка №41),
`python3 scripts/find.py <ID>` вместо grep по дереву.

**Ручной прогон в браузере (`АН-25`) остаётся долгом и этой волной не берётся** — среда его
не даёт: `/opt/google/chrome/chrome` нет, `node_modules` в репозитории нет, кэша
`~/.cache/ms-playwright` нет (перепроверено 29.08.2026). Смоук — единственная проверка,
вёрстку и живой клик он не ловит.

**Навыки.** `/grilling` + `/domain-modeling` на тикетах 01–04: решения принимает человек,
агент за него не отвечает.

## Decisions so far

<!-- индекс: строка на закрытый тикет -->

- [01 · Порядок №41 против раздела финанализа: чем норма ставит обязательность и срок](issues/01-poryadok-finanaliz.md): расхождение подтверждено и оформлено **решениями волны** `АН-37`…`АН-41`, а не дефектом — макет соответствует канону §7, с нормой расходится сам канон. Обязательность финанализа переходит на пару **(тип лица × категория риска)** плюс **порог суммы задолженности**, обе величины ведомыми полями строки расписания и обе читаются **на конец отчётного периода**. Во входящей ячейке §12 «Классификация» снимается оговорка «как величину среза»; `ИА-10` при этом **не трогается** — он про исходящее направление (поправка тикета 04). Две строки требований (категория — классификация, сумма — ядро) заводятся со состоянием **«выполнено»**, и волна 7 тем самым **сухой не будет**. Следующий свободный номер решения — `АН-42`.

- [02 · Порядок №41 против раздела обзоров и три открытых вопроса §15](issues/02-poryadok-obzor-i-otkrytye-voprosy.md): у обзора **норма есть** — п. 6 п.п. 7 «анализ кредитного портфеля» и есть аналитический обзор (`АН-42`), значит посылка §15 «б» ложна; но **срока норма не даёт**, и обязательства не возникает (`АН-43`) — по двум независимым причинам. Объект обзора не сдвинулся: портфель — значение разрезов, а не новый объект, так что к статистике модуль не уходит и тикет 04 разблокирован мягко. Цитаты нормы ставятся **по месту** (`АН-44`), `check_points.py` становится обязательным. Открылось, что три вопроса постановки — **не те три**, что в §15: «б» подменён вопросом о справочнике расписания и до разработки не доехал → **`АН-Д7`**. Редакция постановки считается **от выдачи** (`АН-45`), поэтому `P21` правится как угодно глубоко внутри редакции 1, а бронь «редакция 2 за разделом обзоров» отменена. «а» и «в» остаются умолчаниями (`АН-46`) — норма о групповом заёмщике и о филиалах молчит, проверено нулевыми вхождениями. Следующие свободные номера — `АН-47`, `АН-Д8`.
- [03 · Каноны соседей: швы наружу объявлены в одну сторону](issues/03-sosedi-shvy-naruzhu.md): посылка тикета **опровергнута** — отдел `analytics` в `DEPT_DIR` заявки заведён (`loan-application.html:5962`); верно лишь, что провода к нему нет. Введён критерий `АН-47`: односторонний шов — требование **только там, где исполнение требует работы на чужой стороне**; публичный шов (ядро `ИЯ-1`, классификация §9) требованием не является. По нему девять строк разложились на четыре **выполнено**, одну **открыто** (статистика: объявляет анализу только `statSeries`, а нужны три) и четыре **не тронуто** без живого адресата. Вердикт заявки подставляется **ссылкой-основанием**, а не заключением (`АН-48`, по глоссарию: «материал, а не решение»). Сопровождение признано **пропущенным модулем** (`АН-49`) — в него независимо упёрлись анализ и отчётность, значит это заявка в очередь, а не правка §7. Попутно `АН-Д8`: §4 называет два шва статистики из трёх, и пропущен тот, которым приходит список объектов среза. Следующие свободные номера — `АН-50`, `АН-Д9`.
- [04 · Входящий ящик: `ADR-0157` §4 в модель обзора](issues/04-adr-0157-v-model-obzora.md): единственная строка соседей исполнена **буквально** — из модели обзора уходит хранение чисел, состояние **выполнено**. Заодно ушёл и **состав**: список объектов среза становится **рабочим списком** отчётности по ссылке (`АН-50`), потому что ADR прямо называет его «отдельной вещью с другой судьбой», а свой завёл бы ту же ошибку на объектах вместо чисел. `ИА-8` **остаётся** с переписанной причиной — инвариант с чужим механизмом всё равно наш, прецедент `ИА-13` (`АН-51`). У обзора нет ни корректировки, ни переиздания: метку носит, не пересчитывается, а исправление — корректирующий выпуск плюс **новый** обзор (`АН-52`); `ИА-16` на него не распространяется — следствие `АН-43`. В `CONTEXT.md` заводится третий вид получателя выпуска — обзор (`АН-53`). Новый инвариант `ИА-20`. **Поправка к тикету 01:** `ИА-10` не переписывается — он про исходящее направление; правится одна оговорка в §12. Следующие свободные номера — `АН-54`, `АН-Д9`, `ИА-21`.
- [05 · Макет, смоук и постановка догоняют канон](issues/05-maket-smouk-postanovka.md): три файла сведены с каноном за один тикет; ворота зелены — смоук **52/52** (было 46/46), `check_tasks.py` 0/0, `check_points.py` 0 мёртвых цитат и в `--strict`. `SCHEDULE` стал парным: **8 строк вместо 4**, порог и пункт — поля строки, строк низкого риска нет ни одной (п. 11.1 исполнен отсутствием строки). При исполнении принято **`АН-54`**: признак «только при действующих кредитах» из строки убран как **вторая дверь** одного правила — категория живёт на кредите, и отказ приходит от классификационного шва раньше. Затравка сделана несущей: у `b-1` долг падает ниже порога **после** конца периода, и чтение «сегодня» дало бы обратный ответ. `P21-R17` переписана по существу, `P21-R18` догнала точечно, свод и словарь пополнены; числа примеров — прогон `node:vm`, цитаты отказов посимвольны. Попутно: канон дописан по таблицам 01–04 (версию **v4** ставит тикет 06), цитаты приведены к домовому виду «п. 6.5» — прежняя запись «п. 6 п.п. 5» разбиралась чекером как «п. 5», и пять файлов анализа заведены в `check_points.py` (97/97 → **102/102**). **По проверке на каждый дефект не сошлось и это названо:** у `АН-Д7` и `АН-Д8` поверхности в макете нет — оба про раздел обзоров, которого в макете не заводится. Следующие свободные номера — `АН-55`, `АН-Д9`, `ИА-21`.
- [06 · Таблица требований, вердикт о сухости, запись волны 7](issues/06-tablica-verdikt-zapis-volny.md): раздел **«Что модуль требует от соседей» заведён** — 10 строк (5 выполнено · 1 открыта · 4 не тронуты, и у каждой из четырёх в скобках названо главное: **адресата нет**). Две строки тикета 01 слились с ячейками тикета 03, но **с сохранением обоих применений**, и заведены, хотя требование исполнено: неполна была наша сторона. Вердикт — **волна сухой не была**, шесть строк из десяти дали внешние углы (две — Порядок №41, четыре — каноны соседей); третий угол, легаси, проверен и пуст, ТЗ внешним не считается; оговорка про «новый источник даст новую строку» записана там же. Волна записана в пяти местах: раздел «Волна 7» статуса (`АН-37`…`АН-54`, `АН-Д7`/`АН-Д8`, вердикт, «Чего волна не сделала»), канон **v4**, `CONTEXT.md` (`АН-53` — тикет 04 его решил, но в файл не внёс), очередь (`A3` закрыт, строка §2 обновлена, два пункта §7 сняты, **заявка на модуль сопровождения** заведена в «Вне дорожек»), `TODO.md` (строка «Волна 7» и шапка раздела). Правка «редакция 2 → та же редакция 1» разнесена по трём файлам сразу — макет, постановка, `TODO.md`. Ворота: смоук **52/52**, `check_tasks.py` 0/0, `check_points.py` 2560 ссылок и 0 мёртвых, `INDEX.md` пересобран (803 ID). Не сделано и названо: `STATUS.md` не тронут (вне рамок тикета), Google Sheet не синхронизирован — `TODO.md` правился скриптом, хук не сработал, а ключа `service-account.json` в дереве нет. Новых ADR волна не завела ни одного, пересчитывать перед `main` нечего.

**Карта пройдена: все шесть тикетов закрыты, пункт `A3` очереди закрыт.** Оставшееся ниже —
не остаток маршрута, а фог следующих волн: он ждёт **внешнего** (модуля отчётности, ответа
заказчика, браузера в среде), а не решения, которое можно принять здесь.

## Not yet specified

- **Раздел обзоров в макете.** Ждёт модуль отчётности: шаблона, на который обзор обязан
  ссылаться, в системе нет, а заводить конструктор «временно у себя» запрещено (`ADR-0154`).
  Станет специфицируемым, когда у отчётности появится пометка «пригоден для обзора»
  (`ADR-0157` §4). Вместе с ним снимаются `ИА-6`…`ИА-8` из «в ожидании». Бронь «редакция 2»
  за этим разделом **отменена** (`АН-45`): до выдачи пакета всё идёт в редакцию 1.
- **Раздел ТЗ по анализу.** Свободный номер — `requirements/tz/20`, пишется не раньше раздела
  по отчётности (очередь, «Выдача», пункт 5).
- **Что делать с долгом `АН-25`,** если среда когда-нибудь даст браузер: девять дефектов,
  которых смоук не видел, дал браузерный прогон на классификации — ждать того же и здесь.
  Не решение, а ожидание среды.

## Out of scope

- **Сверка с легаси-обследованием как третий угол.** Угол проверен и пуст: в
  `requirements/legacy/` «финанс…» встречается трижды и всё не о том (метод дней в
  `02-loans.md:195`, финансовая разбивка фазы взыскания в `05-collection.md:140`, справочник
  в `12-dictionaries.md:64`). Анализа как модуля в старой системе нет — сверять не с чем.
- **Сверка с ТЗ как «внешний» угол.** ТЗ не внешний источник, а тот самый, из которого модуль
  выведен: `requirements/tz/original/TZ-ASUBK-3.md:770` даёт модулю восемь строк, и волна 1
  вывела из них `АН-1`…`АН-8`, включая `ADR-0155`. Смотреть оттуда — то же, что смотреть со
  своего макета.
- **Выдача постановки разработке.** Очередь ставит анализ пятым, после отчётности;
  `ADR-0154` блокирует **выдачу**, а не проработку. Этой картой не трогается.
