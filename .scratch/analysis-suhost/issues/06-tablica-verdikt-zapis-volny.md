# 06 · Таблица требований, вердикт о сухости, запись волны 7

Type: task
Status: resolved
Blocked by: 01, 02, 03, 04, 05

## Question

Закрыть волну: собрать то, ради чего пункт `A3` очереди существует.

### 1. Завести раздел «Что модуль требует от соседей»

В `mockups/analysis/ASUBK-status-razrabotki.md`, по образцу
`mockups/reports/ASUBK-status-razrabotki.md:454`. Столбцы: **от кого · что · где записано ·
состояние на дату**. Значения состояния: **выполнено** · **выполнено иначе** (и переписана
наша сторона, а не их) · **условное** (ждёт ответа заказчика) · **открыто** · **не тронуто**;
у «не тронуто» в скобках — главное: **жив ли адресат**, то есть есть ли у модуля рабочая ветка
и владелец, которому требование вообще можно предъявить.

Строки приходят из тикетов 01, 02, 03, 04. Требование без адресата **не снимается**:
«требование без адресата — это адресат, которого ещё не завели, а не требование, которого не
было» (отчётность, тот же раздел).

Заодно снять последний абзац §14 канона («Раздела „Что модуль требует от соседей“ у анализа
нет вовсе») — он перестаёт быть правдой.

### 2. Вынести вердикт о сухости

Критерий очереди, решение 7: **проработка потребителя закрыта, когда волна не добавила ни
строки** в эту таблицу. Но вердикт обязан назвать **угол**, с которого вынесен, — иначе
повторяется ошибка отчётности, объявившей себя сухой по трём волнам, шедшим по своему макету.

Сформулировать честно, одним из двух способов:

- **не сухо** — столько-то строк добавлено, вот они; или
- **сухо с двух углов** — Порядка №41 и канонов соседей — с явной оговоркой, что новая сверка
  с новым источником даст новую строку, и от этого не защищаются.

### 3. Записать волну

- `mockups/analysis/ASUBK-status-razrabotki.md` — раздел «Волна 7» по форме предыдущих шести:
  таблица решений `АН-37`+ со столбцом «почему», таблица дефектов (если завелись `АН-Д7`+),
  счётчик смоука, версия канона. Переписать «Что дальше» — он сейчас утверждает, что своей
  работы у модуля нет.
- `mockups/analysis/ASUBK-analiz-logika.md` — версия **v4** в шапке, новые инварианты `ИА-20`+
  в §11 **с причиной**.
- `docs/ochered-modulei.md` — **завести заявку на модуль сопровождения** (`АН-49`): в него
  независимо упёрлись анализ (§7, §12) и отчётность (§16), модуля, канона и ветки у него нет —
  оформить, как оформлена подсистема заданий в «Вне дорожек» (:231). Закрыть строку `A3` в
  §6 («Дорожка A»), поправить строку модуля в
  §2 (там до сих пор «`ИА-1`…`ИА-18` (v2)», «Открыт `АН-Д6`» — устарело волнами 4 и 6) и снять
  пункт из §7 «Что не проверено».
- `TODO.md` — реестровые строки, если постановка тронута тикетом 05. Хук `scripts/todo_hook.py`
  синхронизирует Google Sheet сам при правке через Claude Code; при правке извне — вручную
  `python3 scripts/sync_todos.py`.
- `python3 scripts/find.py --build` — пересобрать `INDEX.md`, если появились новые ID.
- `STATUS.md` — «Last updated» правится, если файл вообще трогается (он устарел с 12.07.2026 и
  ни одного из шести модулей не упоминает; чинить его целиком — не задача этой волны).

Перед вливанием в `main`: номера ADR **пересчитать** `git ls-tree` по всем ссылкам — между
написанием и выводом номер могут занять (`docs/adr/README.md`, §«Правило номера»).

## Answer

Волна 7 записана, таблица требований заведена, вердикт вынесен с названным углом, пункт `A3`
очереди закрыт. Что сделано по трём частям тикета.

**1. Раздел «Что модуль требует от соседей» заведён** — `mockups/analysis/ASUBK-status-razrabotki.md`,
столбцы «от кого · что · где записано · состояние на 29.08.2026», по образцу отчётности.
**Десять строк**: пять **выполнено** (отчётность — паспорт выпуска и пометка «пригоден для
обзора»; классификация — `riskCategory` на дату, теперь и как условие расписания, и как
величина среза; ядро — `calcPortfolio`/`calc*`, теперь и как порог сравнения, и как значение
объекта; кураторство — ведущий куратор), одна **открыта** (статистика: объявлен `statSeries`,
нужны ещё `statSlice` и `statRows` — `АН-Д8`), четыре **не тронуты**, и у каждой из четырёх в
скобках сказано главное: **адресата нет**. У заявки и карточки заёмщика ветки не заведено, у
сопровождения и подсистемы заданий нет и модуля. Ни одна из четырёх не снята: требование без
адресата — это адресат, которого ещё не завели.

Две строки тикета 01 не стали отдельными: они попали ровно в те же ячейки, что строки тикета
03 (классификация и ядро), и слиты **с сохранением обоих применений** — в ячейке названы и
условие расписания, и величина среза, и порог сравнения, и значение объекта. Строка заведена,
хотя требование уже исполнено: неполна была **наша** сторона, а не их.

Последний абзац §14 канона снят — вместо «раздела у анализа нет вовсе» там теперь состав
таблицы и ссылка на неё.

**2. Вердикт: волна сухой не была**, и угол назван вслух. Шесть строк из десяти дали именно
внешние углы: две — Порядок №41 (категория риска и порог задолженности как условия
обязательности), четыре — каноны соседей. Третий угол, легаси-обследование, проверен и **пуст**
(анализа как модуля в старой системе нет); ТЗ внешним углом не считается — модуль из него и
выведен. Оговорка записана там же: новая сверка с новым источником даст новую строку, и от
этого не защищаются — сухость мерится углом, а не самим модулем.

**3. Волна записана в пяти местах.**

- `ASUBK-status-razrabotki.md` — раздел «Волна 7»: таблица решений `АН-37`…`АН-54` со столбцом
  «почему», таблица дефектов `АН-Д7`/`АН-Д8` (оба заведены и закрыты внутри волны), счётчик
  смоука **52/52**, версия канона v4, вердикт о сухости, таблица требований. Абзац «Чего волна
  не сделала» назван честно: **проверки смоука ни у одного из двух дефектов нет** — поверхности
  в макете нет ни у того, ни у другого, и выдуманная проверка проверяла бы не то. «Что дальше»
  переписан: прежнее «своей работы у модуля больше нет» **опровергнуто** — правдой оно было
  ровно в одном смысле, «по своему макету»; точная формулировка — «работа по разделу финанализа
  исчерпана изнутри».
- `ASUBK-analiz-logika.md` — **v4**: нормативная база переписана (норма приходит обязательностью
  и сроком, ключ строки расписания — пара «тип лица × категория риска», п. 11.1 / п. 11.2 /
  п. 6.5), абзац «Что добавила v4», §14 пересобран. `ИА-20` и переписанный `ИА-19` стояли уже
  тикетом 05.
- `CONTEXT.md` — `АН-53` **исполнен здесь**: тикет 04 его решил, но в файл не внёс (проверено
  `git status` — файл был не тронут). Третий вид получателя выпуска — сам обзор.
- `docs/ochered-modulei.md` — строка `A3` закрыта, строка модуля в §2 обновлена (было
  «`ИА-1`…`ИА-18` (v2)», «Открыт `АН-Д6`»), два пункта §7 «Что не проверено» сняты
  зачёркиванием с датой, и **заведена заявка на модуль сопровождения** в «Вне дорожек»
  (`АН-49`): в отсутствующего соседа независимо упёрлись два модуля, и это пропущенный модуль,
  а не пропущенная строка у существующего — переписать §7 под живого соседа значило бы
  назначить владельца обязательства из чужой ветки (прецедент `B3`).
- `TODO.md` — строка «Волна 7» в разделе «Анализ (целевая модель)», шапка раздела приведена к
  v4 / 52/52 / 20 инвариантам, и хвост строки постановки про «редакцию 2» зачёркнут с
  указанием `АН-45`. Та же поправка внесена в преамбулу макета и в двух местах постановки
  (`docs/tasks/p21-analiz-tasks.html`) — иначе три файла говорили бы о редакции разное.

**Ворота зелены после всех правок:** смоук **52/52 PASS** (штамп в шапке макета — `SMOKE
2026-08-29 · 52/52 PASS`), `check_tasks.py` — 9 файлов, 0 ошибок, 0 предупреждений,
`check_points.py` — 2560 ссылок, 0 мёртвых, 102/102 файла на новой нумерации. `find.py --build`
пересобран: 803 ID, 11 без определения — все из чужих модулей (`P14-*`, `P18-*`, `Г-37`), новых
не добавилось. Ряды `АН-*` в индекс не входят по устройству `find.py` — там же нет ни `КФ-*`,
ни `ФО-*`; это не пробел анализа.

**Чего не сделано, и это названо.** `STATUS.md` не тронут — он устарел с 12.07.2026 и не
упоминает ни одного из шести модулей; чинить его целиком тикет прямо вывел за рамки.
**Google Sheet не синхронизирован**: `TODO.md` правился скриптом, а не инструментом Claude
Code, поэтому хук `todo_hook.py` не сработал, а ручной `sync_todos.py` без
`service-account.json` (его в этом дереве нет) не пройдёт. `--dry-run` разбирает файл верно —
все 22 строки `P21-R*` на месте. Синхронизацию делает тот, у кого есть ключ.

**Перед вливанием в `main`** номера ADR в этой волне пересчитывать нечего — **новых ADR волна
не завела ни одного**, все ссылки (`ADR-0153`…`ADR-0157`, `ADR-0165`, `ADR-0112`, `ADR-0124`,
`ADR-0135`) на уже существующие решения. Пересчёт `git ls-tree` по всем ссылкам остаётся
обязательным для номеров пакетов и ADR, если следующая волна их заведёт.
