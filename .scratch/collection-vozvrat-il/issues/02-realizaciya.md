Type: task
Status: resolved
Blocked by: —

## Plan

1. **Новый вид меры «Повторное предъявление ИЛ»** (рядом с «Постановление о возврате ИЛ»,
   :1513): `source:'наш документ'`, `resultIsDocument:false`, `outcomes:[{value:'предъявлен
   повторно'}]` (без `setsPhase`/`setsTerminal` — фаза «На исполнении» не регрессирует и не
   двигается заново, `ADR-0031`/«фаза — максимум»), `needsDelivery:false`,
   `basisKinds:['Постановление о возврате ИЛ']`. Секция (:1631) — `'Исполнительное'`, тот же
   раздел, что сосед.
2. **Трек переоткрывается.** `ISP_OPENERS` (:1923) получает второй элемент — «Повторное
   предъявление ИЛ» — тем же приёмом, что `SUD_OPENERS` уже переоткрывает судебный трек
   «Определением об отмене судебного приказа» (МП5-17, наряд предыдущей волны).
3. **Два новых шаблона срока** (`DEADLINE_TEMPLATES`, после строки 45): `n:46` «Открытие
   окна повторного предъявления ИЛ» — КАТАЛОЖНАЯ строка (не материализуется билдером,
   тот же статус, что у строки 40/СК-11 — день считывается предусловием напрямую по
   номеру шаблона, в `DEADLINE_DERIVED` не входит); `n:47` «Предельный срок повторного
   предъявления ИЛ» — материализуется (`DEADLINE_DERIVED`), это и есть видимое
   обязательство «определить судьбу требования» (обе идеи схлопнуты в одну очередь-запись,
   не три — заводить отдельный, невидимый в очереди «флаг обязательства» рядом с уже
   видимым предельным сроком значило бы две записи там, где одна уже сообщает то же самое).
   Оба несут `origin:'закон'` (только эти две строки — 45 существующих не трогаем, решение
   Q1). `term` — конкретное число, парсящееся `termToCalendarDays` (регэксп требует старта
   строки с цифры — формат «≤ N…» строки 40 сюда не годится, это КАТАЛОЖНАЯ-НЕ-билдерская
   строка ровно потому что не проходит парсер; мои же строки ОБЯЗАНЫ парситься — `n:47` идёт
   в `DEADLINE_DERIVED`, `assertDeadlineTemplatesParse` бросит на непарсящемся term; `n:46`
   парсится тем же парсером напрямую из предусловия, см. п.5). Приём Q3 (текстовый маркер
   неуверенности) сохраняется, но БЕЗ ведущего «≤» — `term:'30 к.д. (по умолч., настройка)'`
   (n:46) / `'90 к.д. (по умолч., настройка)'` (n:47) — парсер читает ведущее число, хвост
   игнорирует. `point:'ADR-0051 §2'` (не выдумываем номер статьи закона — та же дисциплина,
   что П-9 у строки 45, «не выдумывать номер»), `resp:'куратор ОД'`, `base:'постановление о
   возврате ИЛ'`.
4. **`DEADLINE_DERIVED`/`DEADLINE_CLOSERS`** — `{ n:47, base:{ kind:'Постановление о
   возврате ИЛ' } }`; `DEADLINE_CLOSERS[47] = ['Повторное предъявление ИЛ', 'Извещение об
   обращении на залог', 'Заключение комиссии о безнадёжности']` — три исхода МП5-14, два уже
   существуют (не трогаем их собственные `outcomes`/`basisKinds`/precondition — уже
   регистрируемы на фазе «На исполнении», ESCALATION_LEVEL 3, проверено чтением).
5. **Предусловие-окно.** `PRECONDITIONS['Повторное предъявление ИЛ'] = { basis: true,
   windowTpl: 46 }`. `preconditionReason()` (:2483) — существующая ветка `pc.basis` перестаёт
   ранним `return null` обрывать функцию при успехе (падать в конец при неуспехе — как и
   раньше), плюс новая ветка `pc.windowTpl`: находит живую меру-основание
   (`liveMeasuresOf(r).find(...)`), считает `opens = ruAdd(basisM.dates.event,
   termToCalendarDays(TPL_BY_N[46].term))`, блокирует текстом с датой, если `daysLeft({due:
   opens}) > 0`. Существующие `basis:true`-виды (без `windowTpl`) ведут себя побайтово как
   раньше — новая ветка у них не читается.
6. **Видимость происхождения.** Новая функция `dlOriginPill(d)` рядом с `dlTpl`/`dlAction`
   (:2755): если `!dlTpl(d)` — старая пилюля «вне Порядка» (текст не меняем); если
   `tpl.origin==='закон'` — пилюля «по закону» (title со ссылкой на `ADR-0051`); иначе пусто.
   Оба места рендера («Сроки на контроле» вкладки требования :7309 и общий реестр :9423)
   переключаются на неё вместо инлайн-тернарника — один источник вместо двух копий той же
   развилки. `dlTitle()` (:7140) — «срок по Порядку» становится «срок по закону», когда
   `t.origin==='закон'` (мелкая, но фактическая правка текста подсказки).
7. **Затравка — новых кейсов не нужно.** Существующие K4-ВОЗВРАТ дела 354/355
   (`ilReturned:true`, :5344/:5350) уже несут живую «Постановление о возврате ИЛ» без
   единого закрывателя в журнале — ровно сценарий МП5-14. После правки оба дела должны САМИ
   предъявить срок №47 в очереди («Сроки на контроле»), без затравки.
8. **Регрессия** — throwaway jsdom-смоук: затравка строится без throw (в т.ч.
   `assertDeadlineTemplatesParse` над новыми строками 46/47) · дела 354/355 предъявляют
   живой срок tpl 47 с корректным due (дата постановления + 90 к.д.) · «Повторное
   предъявление ИЛ» на этих требованиях недоступно до окна (`preconditionReason` возвращает
   причину) и доступно после (дата вручную сдвинута/тест мутирует TODAY-эквивалент через
   прямой вызов с гипотетической датой основания в прошлом относительно окна) · после
   регистрации меры трек ИСП (`ispTrackOf`) реоткрывается, а срок 47 пропадает из очереди
   (`deadlinesOf`) · `dlOriginPill` даёт «по закону» на tpl 46/47 и не трогает остальные 45 ·
   0 ошибок консоли. Полный тройной прогон проверок — в резерве на конец наряда п.7 (после
   7б/7в).

## Answer

Реализовано по плану, без отклонений.

**Новый вид меры.** «Повторное предъявление ИЛ» (:1519 приблизительно, рядом с
«Постановление о возврате ИЛ») — `наш документ`, `outcomes:[{value:'предъявлен
повторно'}]`, без `setsPhase`/`setsTerminal`, `basisKinds:['Постановление о возврате
ИЛ']`. Секция `'Исполнительное'`.

**Трек.** `ISP_OPENERS` несёт вторым элементом «Повторное предъявление ИЛ» — реоткрывает
исполнительный трек тем же приёмом, что отмена приказа у `SUD_OPENERS`.

**Два новых шаблона срока.** `n:46` «Открытие окна повторного предъявления ИЛ» —
КАТАЛОЖНАЯ строка (в `DEADLINE_DERIVED` не входит, статус строки 40); `n:47` «Предельный
срок повторного предъявления ИЛ» — материализуется, и есть видимое обязательство
«определить судьбу требования» (одна запись очереди на обе идеи ADR, не три — тонкий хвост,
см. ниже). Оба несут `origin:'закон'`; term — «N к.д. (по умолч., настройка)», без
ведущего «≤» (иначе не парсится `termToCalendarDays` — 45 существующих строк не тронуты).
`point:'ADR-0051 §2'` (не выдумываем номер статьи закона).

**`DEADLINE_DERIVED`/`DEADLINE_CLOSERS`.** `{n:47, base:{kind:'Постановление о возврате
ИЛ'}}`; `DEADLINE_CLOSERS[47]` — все три исхода (два уже существовали и уже были
регистрируемы на фазе «На исполнении» безо всяких изменений — проверено чтением
`ESCALATION_LEVEL`/их `PRECONDITIONS`).

**Предусловие-окно.** `PRECONDITIONS['Повторное предъявление ИЛ'] = {basis:true,
windowTpl:46}`. `preconditionReason()` — ветка `pc.basis` перестала обрывать функцию
ранним `return null` при успехе (существующие `basis:true`-виды без `windowTpl` ведут себя
побайтово как раньше — смоук подтверждает); новая ветка `pc.windowTpl` считает дату
открытия окна по номеру каталожного шаблона и блокирует текстом с датой, пока она не
наступила.

**Видимость происхождения.** `dlOriginPill(d)` — общая функция вместо двух копий
тернарника «вне Порядка» на местах рендера (вкладка требования + общий реестр); для
`origin==='закон'` — пилюля «по закону». `dlTitle()` — подсказка тоже говорит «срок по
закону» для этих строк.

**Затравка.** Без изменений — существующие K4-ВОЗВРАТ дела 354/355 (`ilReturned:true`)
уже несли ровно нужный сценарий (ИСП-трек закрыт «Постановление о возврате ИЛ» без
единого закрывателя).

**Смоук (throwaway, `.mjs`, удалён после прогона)** — 36 проверок, все зелёные с первого
прогона (без фикс-раунда): каталог/`DEADLINE_DERIVED`/`DEADLINE_CLOSERS`/`ISP_OPENERS`
несут новые записи · оба дела 354/355 сами предъявляют срок №47 с верным `due` (дата
постановления + 90 к.д.) · гейт окна согласован с датой окна на ОБОИХ делах — 354 всё ещё
заблокировано, 355 уже открыто (естественный разброс реальных дат затравки, ничего не
подгонялось) · существующий `basis:true`-вид без `windowTpl` не задет рефакторингом ·
`dlOriginPill` даёt «по закону» ровно на 46/47 и не трогает остальные 45 · регистрация
«Повторное предъявление ИЛ» снимает срок №47 из очереди и переоткрывает трек ИСП · 0
ошибок страницы. Полный тройной прогон проверок — в резерве на конец наряда п.7 (после
7б/7в, по нарядy §30).

**Тонкий хвост, оставленный вне карты** (по аналогии с card 6б — записать в
`ASUBK-status-razrabotki.md`, не здесь): ADR-0051 §2 называет ОБЯЗАТЕЛЬСТВО «определить
судьбу требования» и «предельный срок предъявления» как два разных понятия; реализация
схлопывает их в ОДНУ очередь-запись (срок №47) — видимость обязательства = видимость
предельного срока. Если будущая карта (7б/7в или отдельный МП) потребует показывать
обязательство РАНЬШE, чем наступит формальный предельный срок (т.е. как «есть решение
ждёт» независимо от даты) — понадобится третья, отдельная от даты запись; сегодня второй
именованный потребитель для такого разделения не назван.

**Готово, когда:** ✅ «Повторное предъявление ИЛ» — новый вид, гейтится живой мерой-
основанием и окном ожидания · ✅ окно/предельный срок — каталожные строки `origin:'закон'`,
45 существующих строк не тронуты · ✅ обязательство видимо как срок №47 в «Сроки на
контроле» на обоих делах-донорах сценария без единой правки затравки · ✅ любой из трёх
исходов (повторное предъявление / внесудебное обращение на залог / комиссия безнадёжности)
снимает срок и трек ИСП реоткрывается только повторным предъявлением · ✅ происхождение
срока видимо пилюлей «по закону» в обоих местах рендера. Наряд п.7а закрыт.
