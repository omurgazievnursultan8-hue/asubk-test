Type: task
Status: open
Blocked by: 02, 03

## Question

`scripts/inspect/restructuring-check.mjs` — привести к новой матрице. Харнесс не трогаем:
`node:vm`, DOM недоступен, render-функции напрямую не зовутся, сценарии читают производные
функции и мутируют состояние (образец — прежние #1/#2/#9/#10).

**Сценарий 16 перевернуть.** Сейчас (:168–181) он сторожит **существование дыры**:
`nullDefaults === 2 && dotReads === 2` — то есть падает, как только `periods` начинают
писать. После работы он обязан сторожить обратное: живой путь пишет `periods`, и `distribute`
их читает. Старую формулировку не оставлять закомментированной — она станет ложью в файле.

**Новые сценарии** (номера согласовать с §14 спеки, тикет 01):

1. **РС-38, сумма.** `setDispAmount` на `accInterest/over` меняет сумму; значение больше
   позиции отбивается, состояние не меняется.
2. **РС-38, сроки.** `setDispPeriods` кладёт `{mode:'range',from,to}`; переключение обратно
   на `all` стирает интервал, а не хранит его тенью.
3. **РС-39, статья.** `spread` на `interest/over` уводит сумму в `accInterest`; на строке,
   уже бывшей `accInterest`, статья не меняется — прогон дважды даёт тот же результат.
4. **Решение 8, просрочка.** Частичное распределение оставляет `remainsOverdue = true`,
   полное — `false`.
5. **Решение 2, ставка.** Сумма процентов графика не зависит от безставочных колонок:
   два прогона с одинаковым ОД и разными накопленными дают одинаковую колонку процентов.
6. **ИР-2′.** Сумма всех статейных колонок = `transferSum` в копейку.
7. **ИР-15.** Интервал длиннее плана добавляет причину в `validateDS` и закрывает
   регистрацию; укорачивание срока после задания интервала ловится тем же.
8. **Решение 7, округление.** Сумма, не делящаяся нацело, сходится в копейку, и остаток
   лежит в **последней** позиции интервала, а не в первой.
9. **Решение 3, граница двери.** Сроки существуют у `spread` и у `cap`-на-пеню; у
   `cap`-на-проценты и `forgive` их нет — проверка по состоянию, не по тексту файла.

**Мёртвые счётчики.** Прежний #16 ловил дыру по тексту файла; той же техникой (`js.match`)
проверить, что `capitalizedPenalty` теперь читается, а не только пишется — это и был признак
неисполненной РС-27.

Проверка на выходе: `node scripts/inspect/restructuring-check.mjs` — все PASS, 0 ошибок
консоли; стамп в шапке `restructuring.html` обновлён; `ASUBK-status-razrabotki.md` — РС-11
из ❌ в ✅, строка РС-27 приведена в соответствие с фактом, счётчик сценариев обновлён,
§«Открытые вопросы» теряет пункт про `disposition.periods`.

## Answer

_(пусто — тикет открыт)_
