Type: task
Status: open
Blocked by: 01, 02, 03, 05

## Question

Реализовать в `mockups/restructuring/restructuring.html` то, что решили тикеты 01, 02, 03, 05:
модель демо (транши, семь статей, датированные операции, версии графика с периодом действия),
финансовое ядро (состав базы, срез ↔ факт, четыре распоряжения, накопленные, расчёт на любую
дату), дверь `restructureApplied` с валидацией строк, вкладка расчёта по прототипу.

**Объём демо-набора** — здесь же (патч тумана, снятый с карты решением тикета
[Финансовое ядро](02-finansovoe-yadro.md)): состав данных больше не требует своего решения, он
выводится из решений 01/02 и сценариев §14 — минимум субъект с псевдонимом и правопреемником,
кредит с двумя траншами, три последовательных ДС (чтобы ИР-3 читался на цепочке переносов) и
дата между первой и второй реструктуризацией, на которой долг выводится. Что пришлось завести
сверх этого — записать в ответе тикета.

Правится **на месте**, тем же путём, без второго файла — ссылки из спеки, дом-статуса, смоука
и `TODO.md` не рвутся.

Сносится: `amortize` (:458), `termCap`/`rateFloor` (:504/:512), `firstOverdueMode` (:516),
`calcRestructure` (:520), `versionFrom` (:676), `PARAM_KEYS`/`OP_KEYS` (:360/:367),
`pTerms`/`termsPanels` (:991/:1067), `setParam`/`setInput`/`setCutoff`/`addGrace`/`calcGraph`
(:1424–1443), `applyEntryOps` (:1339).

Оболочка (реестр, карточка, конвейер, гейты, роли, справочник, журнал) в этом тикете **не
трогается** — она в 07. Если решения 01–03 вынуждают править оболочку раньше, это сигнал, что
граница между 06 и 07 проведена неверно: зафиксировать в ответе, а не растворять молча.

Проверка на выходе: страница открывается без ошибок консоли, инварианты ИР-2…ИР-7 показуемы
руками на демо-данных. Полный смоук — тикет 08, здесь его ещё нет по чему писать.
