Type: task
Status: resolved
Blocked by: —

## Plan

1. **«Дополнительное решение суда»** (`ADR-0046`) — новая запись `MEASURE_KINDS` сразу
   после «Решение суда» (:1456-1460, `collection.html`): `source:'внешний акт'`,
   `resultIsDocument:false` (не `true`, как первично предложено в грилинге — исправлено
   по факту сверки с кодом: у *внешний акт* с собственным эффектом `resultIsDocument`
   всегда `false`, это поле только про «наш документ», ожидающий ответа дочерней мерой),
   `outcomes:[{value:'вынесено',setsPhase:'Решение суда'}]`, `needsDelivery:false`,
   `deliveryChannels:null`, `basisKinds:['Решение суда']`. Комментарий — ссылка на
   `ADR-0046`/наряд п.1 и явная оговорка, что слоевую арифметику достроит наряд п.2.
2. **«Определение о повороте исполнения»** (`ADR-0049`) — новая запись после
   «Определение суда (прочее)» (:1485-1487), перед «Мировое соглашение»:
   `source:'внешний акт'`, `resultIsDocument:false`, `outcomes:null`,
   `needsDelivery:false`, `deliveryChannels:null`, `basisKinds:['Определение об отмене
   судебного приказа','Решение суда']`.
3. **`collection-check.mjs`** — два жёстко зашитых числа поправить: `MEASURE_KINDS.length`
   60→62 (:2948), `MEASURE_KINDS.filter(k=>kindPhase(k.name)).length` 21→22 (:2954,
   «Дополнительное решение суда» — единственный из двух новых видов с `setsPhase`).
   Пояснительные комментарии над обеими проверками дополнить историей (по образцу
   прежних волн).
4. **Регрессия** — `collection-check.mjs` целиком (сравнить FAIL-список до/после правки
   через git stash — 0 новых провалов), `collection-data-audit.mjs` (0 находок),
   `collection-seed-coverage.mjs` (не трогается — SEED для новых видов не заводится,
   вне периметра карты).
5. **Не входит в эту карту**: SEED-примеры новых видов (пересев затравки — отдельно,
   в резерве до конца волны), `МП-11` (мировое соглашение — отдельная карта, вопрос 0
   грилинга), слоевая арифметика «Дополнительного решения суда» (наряд п.2), собственные
   виды для прекращения обеспечения/утверждения реструктуризации (решено — их нет,
   вопрос 3 грилинга).

## Answer

Реализовано по плану, с одним исправлением по ходу (см. п.1 плана — `resultIsDocument`
скорректирован с `true` на `false` при сверке с реальной семантикой поля в коде,
подтверждено пользователем).

**Справочник.** `MEASURE_KINDS` 60 → **62**. «Дополнительное решение суда» —
`внешний акт`, ставит фазу «Решение суда» (единственным исходом), `basisKinds:['Решение
суда']`. «Определение о повороте исполнения» — `внешний акт`, без `outcomes` (регистрация
факта, не развилка), `basisKinds` — два акта, гасящих слой при уже произведённом
взыскании (отмена приказа, решение суда — в это же поле попадают отмены по вновь
открывшимся/новым обстоятельствам и кассации/надзору, поскольку все они оформляются
через «Решение суда» либо «Определение суда (прочее)»; последнее сознательно не
включено — оно catch-all без системного следствия, ADR-0028 п.3, и не гасит слой само
по себе).

**Тесты.** `collection-check.mjs`: 664 проверки, 32 провала — **тот же набор**, что и до
правки (сверено построчным diff FAIL-списков на коммите до и после через `git stash`).
0 новых провалов, 0 ошибок консоли. `collection-data-audit.mjs`: 0 находок, 97 дел, 106
требований — без изменений. `collection-seed-coverage.mjs`: не тронут (SEED для новых
видов не заводился), тот же остаток 3 неизвестных кода, что и до этой карты.

**Готово, когда:** ✅ оба вида заведены с полным набором полей ✅ `collection-check.mjs`
жёстко зашитые числа поправлены и объяснены ✅ регрессия нулевая (diff FAIL-списков)
✅ `resultIsDocument` сверен с реальной семантикой поля в коде, а не только с грилингом.
Наряд п.1 (в периметре этой карты — без `МП-11`) закрыт.
