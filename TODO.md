# Бэклог / Задачи

> Группировка по приоритету. Переносите начатое в STATUS.md «In progress»,
> отмечайте галочкой (или удаляйте) выполненное.

## 🔴 Сейчас — Сквозная проверка кредитного модуля (QA + документация)
Область: только основная кредитная цепочка. По каждой фазе → документ в
`requirements/features/` + дефекты в `notes/qa-findings.md`. Работаем
пофазно, с проверкой после каждой фазы.

- [x] Фаза 1 — Решение правительства — документация и QA готовы 2026-06-17
- [ ] Фаза 2 — Кредитные программы — документация и QA готовы, на ревью
- [ ] Фаза 3 — Заявки и комиссии по заявкам — документация и QA готовы, на ревью
- [ ] Фаза 4 — Заемщики — документация и QA готовы, на ревью
- [ ] Фаза 5 — Выдача кредитов — документация и QA готовы, на ревью (2026-06-20)
- [ ] Фаза 6 — Освоение и транши — документация и QA готовы, на ревью (2026-06-20)
- [ ] Фаза 7 — Обслуживание: платежи, реестр (LoanLedger), резервы — документация и QA готовы, на ревью (2026-06-20)
- [ ] Фаза 8 — Мониторинг залога
- [ ] Фаза 9 — Взыскание задолженности
- [ ] Фаза 0 — Справочные данные (документируются по мере необходимости)

## 🟡 Далее (скоро)
### Фаза 1 — Решение правительства: предложения по улучшению (для команды разработки)
_Полное обоснование в `requirements/features/01-government-decision.md` → Recommendations._
- [ ] R1 🔴 Вложение документа решения
  - сейчас: поля для файла нет, есть только текстовое «Примечание»;
  - добавить загрузку файлов отдельно от «Примечания»;
  - формат только **PDF**, лимит размера (≈10–20 МБ), валидация при загрузке;
  - можно прикрепить **несколько** файлов (основное решение + приложения/изменения), ровно один помечается как «основной»;
  - на стадии «На рассмотрении» файл необязателен; для перехода в «**Действует**» (бывш. «Одобрен», см. R7) наличие основного документа **обязательно**;
  - замена файла — новой версией с сохранением истории (без перезаписи); у одобренного решения удаление файла запрещено;
  - кто и когда загрузил/заменил файл — фиксируется в журнале (см. R2).
- [ ] R2 🔴 Отклонение решения: подтверждение, причина, аудит, обратимость
  - сейчас: «Отклонить» срабатывает мгновенно, без причины и следа, необратимо (P1-03);
  - перед отклонением — диалог подтверждения с явными кнопками (защита от случайного клика);
  - обязательное поле «Причина отклонения» — без него «Отклонить» недоступна; причина видна в просмотре записи и в списке;
  - журнал аудита по каждому переходу статуса (одобрение/отклонение/реактивация): кто, когда, действие, причина — это же закрывает аудит загрузки файла из R1;
  - обратимость: дать администратору (по праву/роли) вернуть запись из «Закрыт» **и «Отклонён»** обратно «На рассмотрении» с обязательной записью в журнал — мис-клик не должен навсегда хоронить юр. основание (✅ согласовано с R7 2026-06-22: выбрана эта модель админ-возврата, «Дублировать» не делаем).
- [ ] R3 🔴 Поле «Код» — системная генерация вместо ручного ввода
  - сейчас: код вводится **вручную** (поле редактируемо, обязательно при создании), без проверки уникальности, есть пустые legacy-записи (P1-02);
  - код должен генерироваться **системой** при создании, ручной ввод убрать, поле сделать read-only (заодно убирает проблему «серого» стиля поля — P1-01);
  - формат `{ТИП}-{ГГГГ}-{NNNN}`: ТИП — короткий код вида решения (ПОСТ/РАСП/ОСН/РКМКР/РПКР/ПКМКР), ГГГГ — год из «Даты решения», NNNN — сквозной порядковый номер внутри пары тип+год с ведущими нулями (пример: ПОСТ-2026-0007);
  - уникальность гарантируется счётчиком;
  - старые пустые коды оставляем как legacy (предстоит полное обнуление данных, миграция не нужна).
- [ ] R4 🟠 Удаление решений: защита ссылок, статус, soft-delete
  - сейчас: «Удалить» активна даже на одобренных/закрытых; редактирование уже корректно заблокировано, удаление — нет (P1-05);
  - серверная проверка ссылок: запретить удаление, если на решение ссылается хотя бы одна кредитная программа (или иная downstream-сущность) — проверка на сервере, не только скрытием кнопки;
  - при попытке удалить используемое решение — внятное сообщение («Нельзя удалить: на решение ссылаются N программ»), а не молчаливый отказ;
  - выровнять с редактированием: для одобренных/закрытых записей удаление недоступно (кнопка неактивна);
  - soft-delete вместо физического удаления — запись помечается удалённой, остаётся в БД, действие пишется в журнал аудита (см. R2);
  - удаление необратимо → **обязательная причина в модалке подтверждения** (как при отклонении, по образцу P1-07), а не голый confirm(); причина пишется в журнал аудита (R2) — проверено в макете 2026-06-22, `scripts/inspect/decisions-tasks-test.mjs`;
  - уточнить полный список downstream-ссылок на решение при реализации фаз 2+.
- [ ] R5 🟠 Валидация «Даты решения»
  - сейчас: календарь/поле принимают будущие даты (P1-04);
  - запретить будущие даты: «Дата решения» не может быть позже сегодняшнего дня;
  - валидация и на клиенте (календарь не даёт выбрать будущее), и на сервере (чтобы не обойти через API);
  - при попытке ввести будущую дату — внятное сообщение («Дата решения не может быть в будущем»), а не молчаливый сброс;
  - нижний порог даты пока не вводим.
- [ ] R6 🟠 Страница деталей решения — переработать по мокапу
  - сейчас: страница — это набор неактивных форм без единого режима просмотра;
  - сделать страницу в режиме **«Просмотр + действия»** по готовому мокапу: **`mockups/decision/decisions.html`** (вкладки деталей решения; передать команде разработки как образец вёрстки и дизайн-системы);
  - компоненты страницы:
    - **слева** — дерево-навигация модуля (Кредиты → Программы и решения → Решение правительства);
    - **шапка** — хлебные крошки + номер решения;
    - **вкладки**: «Общая информация», «Кредитные программы», «Документы», «История»;
    - **Общая информация** — режим просмотра (подпись → значение, без редактируемых полей): Вид решения, Номер, Дата, Орган, Наименование, Примечание, «Действующее решение» (бейдж Да/Нет);
    - **Кредитные программы** — таблица связанных программ;
    - **Документы** — таблица вложений (увязать с R1);
    - **История** — таймлайн действий с иконками по типу (создание/изменение/загрузка/смена статуса), увязать с журналом аудита R2;
    - **футер** — «Изменить» (вход в режим редактирования) + «Назад»; в режиме редактирования поля становятся инпутами дизайн-системы, футер `OK / Отмена`;
  - мокап самодостаточный (HTML/CSS/JS, дизайн-система ASUBK gov-blue), отрисовку в продукте делать на стеке проекта по образцу мокапа.
- [ ] R7 🔴 Воркфлоу статусов решения — модель жизненного цикла
  - спецификация: **`mockups/decision/decision-workflow.html`** (статусы, переходы, роли, матрица прав, поведение тулбара) — передать команде разработки;
  - сейчас в системе 3 статуса («На рассмотрении», «Одобрен», «Закрыт»); модель добавляет недостающие и переименовывает:
    - статусы (6): Черновик → На рассмотрении → На доработке → **Действует** → Отклонён → Закрыт;
    - переходы (11): Создать/Отправить/Отозвать/Одобрить(→Действует)/Отклонить(→Отклонён)/Вернуть(→На доработке)/Отправить повторно/Закрыть(Действует→Закрыт)/Удалить + **2 админ-возврата**: Закрыт→На рассмотрении, Отклонён→На рассмотрении;
    - роли: Автор, Согласующий (+ опц. Наблюдатель);
    - матрица прав роль×статус; тулбар: **скрывать по роли, гасить по статусу** (с подсказкой на неактивных);
  - правила: редактирование только в «Черновик»/«На доработке»; удаление только «Черновик»; «Действует» только закрывается; «Закрыт» достижим только из «Действует»; «Отклонён» — только из «На рассмотрении»; «Закрыт» и «Отклонён» — не терминальные: админ возвращает их в «На рассмотрении» с обязательной причиной в журнал;
  - **решения заказчика приняты (2026-06-22), снято при реализации (увязать с R1, R2):**
    - терминология: «Одобрен» → «Действует» — синхронизировать с R1 (обязательность файла теперь на переходе в «Действует»);
    - обратимость: **выбрана модель R2 — админ-возврат** «Закрыт»/«Отклонён» → «На рассмотрении» (обязательная причина, запись в журнал). «Дублировать» НЕ делаем — возврат покрывает оживление записи;
    - **«Отклонить» ведёт в отдельный статус «Отклонён»** (не в «Закрыт»); «Закрыт» — только отмена действовавшего решения (из «Действует»). **Нужна миграция данных**: записи живого стенда со статусом «Закрыт», означающие отказ в согласовании, перевести в «Отклонён»;
    - именование: статус «Действует» и булев флаг «Действующее решение (Да/Нет)» — **разные сущности**, флаг хранится отдельно, НЕ выводится из статуса; определить семантику флага при статусе «Действует» (кто ставит, значение по умолчанию, что значит «Нет» при «Действует»);
    - журнал переходов (кто/когда/комментарий) — это тот же аудит, что в R2 и таймлайн «История» в R6; не дублировать, сделать единый журнал;
  - **decisions.html апгрейдится до модели R7** (6 статусов, новые переходы, демо-переключатель ролей Автор/Согласующий для показа поведения тулбара);
  - открытые вопросы (из спеки, требуют решения заказчика): обязательность комментария-причины при «Отклонить»/«Вернуть»; нужна ли роль «Наблюдатель»; кто уполномочен закрывать решение.

### Фаза 2 — Кредитные программы: предложения по улучшению (для команды разработки)
_Полное обоснование в `requirements/features/02-loan-program.md` → Recommendations.
Нумерация `P2-R*` — отдельный счётчик фазы (как `P2-xx` у дефектов); счётчик
Фазы 1 (`R1–R7`) остаётся независимым. Дефекты — `notes/qa-findings.md` (P2-xx)._
- [ ] P2-R1 🟡 Визуальная семантика полей и ошибок формы-мастера
  - сейчас: ~40 полей на 9 вкладках в одинаковом сером «disabled»-стиле; обязательные/опциональные/readonly визуально неотличимы; обязательные видно только после неудачного сохранения (P2-01); тост «Заполните все обязательные поля!» обобщённый, не называет вкладку/поле (P2-06);
  - ввести **3 состояния поля** с разным видом:
    - **обязательное-редактируемое** → белый фон + рамка + красный `*` у подписи;
    - **опциональное-редактируемое** → белый фон + рамка, без `*`;
    - **только для чтения / вычисляемое** → серый фон, без рамки (напр. «Валюта» на вкл. 2, «Выбранные шаблоны», вся вкл. «Предпросмотр»);
  - красный `*` у подписи показывать **сразу при открытии формы**, а не только после ошибки валидации;
  - **бейдж/счётчик ошибок на вкладке** — вкладки с невалидными полями помечаются (красный индикатор + число), чтобы было видно, где искать на 9-вкладочной форме (из P2-R6);
  - **inline-сообщение у поля** — под каждым невалидным полем текст («Обязательное поле»), а не только красная рамка (из P2-R6);
  - не берём (по решению): автопереход на вкладку + фокус, общий перечень незаполненных полей;
  - **единый стиль с R6** (Фаза 1): один компонент-стиль полей дизайн-системы ASUBK, применяемый в обоих модулях;
  - применить ко всем 9 вкладкам мастера;
  - тот же корень, что P1-01/P1-06 (отображение; набор обязательных полей и логика гейта — P2-R10).
- [ ] P2-R2 🟠 Валидация «Дата с» / «Дата по»
  - сейчас: у пикеров нет min/max и нет проверки порядка — «Дата по» раньше «Даты с» не блокируется (P2-02);
  - **«Дата по» ≥ «Дата с»** (равенство допускается — программа на 1 день) — на клиенте (пикер «Дата по» не даёт выбрать раньше «Даты с») и на сервере;
  - **«Дата с» ≥ «Дата решения»** связанного решения правительства — нижняя граница (у решения нет периода действия, только дата принятия, проверено 2026-06-17, поэтому она и есть нижний порог);
  - будущие даты для программы **допустимы** (в отличие от решения, R5) — это проверка порядка/согласованности, а не запрет будущего;
  - при нарушении — внятное сообщение, а не молчаливый сброс.
  - уточнено 2026-06-25 (владелец): **«Дата по» опциональна** (бессрочные программы) — верхнюю проверку «по ≥ с» применять только если «Дата по» заполнена; пусто = бессрочно (показывать «бессрочно» в Просмотре/гриде); при смене решения — перепроверка обеих дат с подсветкой; см. P2-R12.
- [ ] P2-R3 🟡 Семантика и подписи кнопок сохранения
  - факт (проверено 2026-06-17 на тест-записи): «OK» = сохранить **и закрыть** редактор; «Сохранить» = сохранить **и остаться**; обе сохраняют данные — это штатный паттерн Jmix, не потеря данных (P2-03);
  - проблема: разница не очевидна, подписи непоследовательны (англ. «OK» рядом с рус. «Сохранить»/«Отмена»);
  - сделать: единый язык подписей и понятная семантика — напр. «Сохранить» (остаться) + «Сохранить и закрыть» + «Отмена»; либо тултипы на кнопках;
  - мелочь: после «OK» переход идёт на `/` (главную), а не в список программ — вернуть в список.
- [ ] P2-R4 🟡 Отдельная страница деталей программы («Просмотр»)
  - сейчас: в тулбаре только Создать/Изменить/Удалить — нет режима просмотра, инспекция вынуждает входить в редактирование (риск случайной правки) (P2-04); расходится с UX решений правительства, где «Просмотр» есть;
  - сделать: **отдельную страницу деталей** программы (не вкладку «Предпросмотр» и не read-only мастер) — единый читаемый обзор всех данных программы;
  - открытие из списка: **кнопка «Просмотр»** в тулбаре (выбрать строку → Просмотр) **и двойной клик по строке**;
  - со страницы просмотра — кнопка **«Изменить»** (переход в редактирование), выровнять с моделью Фазы 1 (R6 «Просмотр + действия»).
- [ ] P2-R5 🟠 Жизненный цикл статусов программы (формализация + унификация с Фазой 1)
  - сейчас: в списке только «Черновик», нет Одобрить/Отклонить; единственный переход — «Отправить на подтверждение» на вкладке «Предпросмотр»; полный набор статусов, переходы после отправки и роль подтверждающего в UI не определены (P2-05); словарь статусов расходится с решением Фазы 1 (анализ 2026-06-24, владелец);
  - **набор статусов — единый с решением (Фаза 1, R7)**: `Черновик → На рассмотрении → Действует / Отклонена → Закрыта`, плюс `На доработке`; «одобрено и в силе» = **«Действует»** (не «Одобрена»); «Черновик» — рабочее состояние до отправки;
  - **роли разделены (принцип 4-eyes)**: автор/редактор отправляет; **другой** пользователь — **«Согласующий»** (единое имя в обоих модулях, не «Подтверждающий») — одобряет/отклоняет; самоподтверждение запрещено;
  - **переходы и подписи (единые с решением)**: Черновик → На рассмотрении = **«Отправить на согласование»** (правит и Фазу 1: было «Отправить»; и программу: было «Отправить на подтверждение»); «Вернуть на доработку» (Согласующий) / «Отправить повторно» (Автор) — для статуса «На доработке»;
  - **обратимость терминальных статусов + роль Администратор**: «Отклонена» / «Закрыта» → «На рассмотрении» с причиной в журнал (как у решения; закрывает откр. вопросы §10.1/§10.5 раздела 02);
  - **переходы доступны и в списке, и на странице деталей** (P2-R4): Одобрить/Отклонить/Отправить — как у решений;
  - отклонение/возврат/закрытие — с подтверждением и причиной + запись в аудит (по образцу Фазы 1 R2);
  - правки словаря/бейджа: §1/§6.1/§7/§8 раздела 02 + мокап `mockups/loan-program/loan-program.html` («Одобрена» → «Действует»); статус виден бейджем (см. P1-08);
  - решено **НЕ** вводить запрет самосогласования («4 глаза») в решение — расхождение принято осознанно (у программы остаётся);
  - увязать с Фазой 1 (R7) — единый паттерн жизненного цикла в обоих модулях.
- [ ] P2-R6 🔵 → **объединено в P2-R1** (UX ошибок большой формы — бейдж/счётчик ошибок на вкладке + inline-сообщение у поля — теперь часть визуальной семантики полей и ошибок P2-R1).
- [ ] P2-R7 🟠 Обработка ошибки удаления (локализация сообщения)
  - факт (проверено 2026-06-17): удаление программы со ссылкой даёт диалог «Непредвиденная ошибка» с сырым `DeletePolicyException: Unable to delete LoanProgram because there are references from GovDecision` (англ., техническое) (P2-07);
  - серверная защита ссылок есть (это хорошо, как и просили в R4 для решений) — плохая только подача;
  - сделать: ловить `DeletePolicyException` → **общее** локализованное сообщение («Нельзя удалить: на программу есть ссылки»), без перечня связанных сущностей; согласовать формулировку с R4 (Фаза 1);
  - прим.: из-за этой защиты тест-запись ID 24 осталась в среде (обнуление данных всё равно предстоит).
- [ ] P2-R8 🟠 Баг: направление delete-policy для программы
  - факт (проверено 2026-06-17): только что созданная программа (ID 24) **без** downstream-ссылок не удаляется, ошибка винит «references from GovDecision» — хотя это программа ссылается на решение, а не наоборот (P2-07);
  - похоже на инвертированную политику удаления / back-reference в модели;
  - сделать: разработчику проверить направление связи LoanProgram↔GovDecision и delete-policy; программа без реальных downstream-ссылок (заявки/кредиты) должна удаляться;
  - отдельно от P2-R7 (там только подача ошибки, здесь — корректность самой политики).
- [ ] P2-R9 🔴 Выбор «Решения правительства» только из одобренных — серверный enforcement
  - факт (проверено 2026-06-19): фильтр «Статус = Одобрен» в picker'е — **только дефолтный UI-фильтр и снимается** одним кликом по чипу; после снятия в выборе появляются все решения, включая «На стадии рассмотрения» и «Закрыт» (7 → 13 строк), их можно выбрать и **программа сохраняется без серверной проверки** (P2-08);
  - сделать: серверную валидацию при сохранении — связанное решение должно быть **одобренным/действующим**, иначе сохранение отклоняется с внятным сообщением (не полагаться на UI-фильтр);
  - в picker'е неодобренные решения **скрывать** (жёсткий фильтр загрузчика), а не просто ставить съёмный дефолт; либо блокировать выбор;
  - picker открывает **полный экран-справочник** с действиями Создать/Изменить/Удалить/Одобрить/Отклонить — это слишком широкий доступ из формы программы; ограничить до режима выбора;
  - увязать со статусами решения (Фаза 1 R7: «Одобрен» → «Действует») — ссылаться можно только на «Действует».
- [ ] P2-R10 🔴 Прогрессивная валидация мастера + набор обязательных полей
  - сейчас: обязательны только **7 полей вкладки 1**; всё финансовое ядро (сумма, срок, ставка, штрафы, метод/периодичность погашения) — `required=false` → можно сохранить и отправить **«пустую» программу**; обязательность видна только после неудачного сохранения (ключевой системный риск, подтверждён владельцем 2026-06-18; P2-01/P2-06);
  - P2-R1 покрывает только **отображение** (`*`, счётчик ошибок на вкладке, inline-текст; вобрала бывш. P2-R6); эта задача — **какие поля обязательны** и **логика гейта** (дополняет, не дублирует её);
  - **набор обязательных по вкладкам** (решения владельца 2026-06-18/19):
    - вкл.1 «Основные» (уточнено 2026-06-25, владелец): к текущим 7 добавить наименование на **кыргызском**, **источник финансирования**, **валюта**, **отрасль**, **ответственные сотрудники** (multi, кормит маршрутизацию — P2-10/P2-R12); **«Дата по» из обязательных исключена** — теперь опциональна (бессрочные программы, P2-R12/P2-R2). Обязательные (11): рус/кырг наименование, краткое описание, решение, источник, назначение, вид кредита, валюта, отрасль, дата с, ответственные сотрудники;
    - вкл.2 «Сумма и срок»: тип суммы **и** тип срока + ≥1 значение каждого (или мин+макс при «Диапазон»);
    - вкл.3 «Ставки»: тип ставки + значение (≥1 из справочника / мин+макс); плавающая — опциональна (флаг-заготовка);
    - вкл.4 «Штрафы»: тип штрафа осн. + значение, макс. размер (% от суммы), тип штрафа проц. + значение; маржа плавающей — опциональна;
    - вкл.5 «Льготный период»: **условный гейт** — при включённом блоке требовать тип + продолжительность; иначе свободно;
    - вкл.6 «Платежи»: периодичность, день платежа, метод расчёта дней, метод погашения, тип расчёта процентов, очередь погашения (6) + «конкретные месяцы» при Ежекв./Ежегодно; выходные и окно мин/макс — опциональны;
    - вкл.7/8 **залоговый гейт**: чекбокс «Требуется залоговое обеспечение» (вкл.8) выкл → залоговые поля скрыты; вкл → ≥1 вид залога + требования ру/кырг обязательны (eng опц.);
  - **двухуровневая валидация**: черновик **«Сохранить»** проверяет только вкладки 0..текущая (можно сохранять незаполненным дальше); **«Отправить на подтверждение»** валидирует **все** вкладки (увязать с воркфлоу P2-R5);
  - **прогрессивный гейт навигации**: «Далее»/клик по вкладке вперёд — только если обязательные текущей и предыдущих вкладок заполнены (иначе прыжок на первую незаполненную + подсветка); назад — свободно;
  - валидация на клиенте **и** сервере (не полагаться на UI-гейт); серверная — обязательное условие отправки/одобрения;
  - спецификация и эталон поведения: **`mockups/loan-program/loan-program.html`** (реализованы все 9 вкладок, гейт, двухуровневая валидация, залоговый гейт) — передать команде разработки; полное пополевое обоснование — `requirements/features/02-loan-program-field-analysis.md`.
- [ ] P2-R11 🟡 → **объединено в P2-R5** (унификация статусной модели и словаря с Фазой 1 — «Действует», «Согласующий», «Отправить на согласование», «На доработке», обратимость терминальных статусов — теперь часть жизненного цикла статусов программы P2-R5).
- [ ] P2-R12 🟡 Вкладка 1 «Основные» — пополевые правила полей и валидации
  - повод: пополевой разбор мокапа с владельцем 2026-06-25 (17 компонентов вкл.1); эталон — `mockups/loan-program/loan-program.html`;
  - **Наименования (рус/кырг/англ):** рус — обяз., `maxlen 200`, trim + схлопывание пробелов, **мягкий warning уникальности** при совпадении с активной программой (Черновик/На рассмотрении/Действует) на blur, сохранять не мешает; кырг — обяз. (политика), warning при точном совпадении с рус (страховка от копипасты-заглушки); англ — опц., без проверок; для всех трёх — правило отображения по локали с **fallback на рус** при пустом переводе (нужно для старых записей под будущую i18n);
  - **Краткое описание:** обяз., `maxlen 500` + счётчик, показ **только в Просмотре**; роль — публичная суть программы (отделить от «Комментарий»);
  - **Даты:** правила «Дата с»/«Дата по» — авторитетно в **P2-R2** (здесь не дублируются): «Дата с» обяз. ≥ даты решения; «Дата по» опц. (пусто = бессрочно, метка «бессрочно» в Просмотре/гриде), если задана — ≥ «Дата с»;
  - **Заявители (физ/юр):** опц. multi, whitelist — **жёсткое** ограничение (подают только из списка), **пусто = открыто всем**; пустой показывать явно как «Без ограничений — доступно всем»; enforcement в модуле заявок (проверка `заявитель ∈ whitelist`); отдельного признака типа заявителя НЕ вводим;
  - **Ответственные сотрудники:** исправить на **multi** (сейчас одиночное при названии во множ. — P2-10) и сделать **обязательным** (кормит маршрутизацию заявок); признак «куратор» здесь не задаём — логика в будущем **модуле кураторства** (он потребитель списка);
  - **Комментарий:** опц., `maxlen 1000`, внутренняя заметка (hint «Внутренняя заметка для команды»), в Просмотре — отдельный блок «Служебные заметки»;
  - сквозной приём: опц. поля с особым смыслом пустого значения показывать явной меткой («бессрочно», «Без ограничений»), а не пустотой.
- [ ] P2-R13 🟠 Справочники вкладки 1 — контрол выбора и ведение
  - повод: пополевой разбор 2026-06-25 (владелец); дефекты данных — P2-09, термины — P2-11;
  - **малые справочники** (источник, назначение, вид кредита, валюта, кредитная линия, вид счёта погашения) → ••• модалку заменить на **combobox** (выпадающий список + поиск); модалка остаётся для больших (Отрасль — 45 строк, коды/статусы/поиск);
  - **ведение справочников — только администратор**; inline-добавления значений оператором из формы программы **нет** (перекрывает источник мусора P2-09);
  - **семантика полей** (решения владельца 2026-06-25): источник и вид кредита — **независимы** (противоречия допускаются, каскада нет); назначение — самостоятельное; кредитная линия — **плоский список без каскада** от источника, опциональна; вид счёта погашения — атрибут программы (дефолт для займов);
  - **фильтры выбора:** Отрасль — только «Активный»; Решение — только «Действует» (см. P2-R9/P2-R11); неактивные/недействующие в пикерах **скрывать**, старым ссылкам не ломать;
  - **термин «Назначение кредита» — единый**: справочник «Цели кредита» переименовать (P2-11);
  - чистка/бэкфилл содержимого справочников — дефект **P2-09**.
- [ ] P2-R14 🔴 Каскад недействительности «Решение → Программа → Заявки» (межмодульный)
  - повод: разбор компонента «Решение правительства» с владельцем 2026-06-25;
  - **наследование:** программа берёт от решения **только дату** (нижний порог валидации «Дата с»); источник/валюта/лимит программа задаёт **независимо**;
  - **каскад:** при переходе решения в недействительный статус — **все его программы зеркалят статус решения** (тихий автокаскад, без диалога подтверждения);
  - **аудит:** запись в **хронологию каждой программы** — «основание <решение №> → статус <X>» (иначе тихий каскад нечитаем);
  - **эффект:** новые заявки/займы по программе **блокируются**; уже выданные займы и прочее — не трогаются (обязательства заёмщика остаются);
  - **предусловие:** статусы решения и программы — в одном словаре (после P2-R11); если у решения есть статус, которого нет у программы, — явная карта соответствия;
  - увязать с P2-R9 (ссылка только на действующее решение) и P2-R5/P2-R11 (модель статусов).
- [ ] P2-R15 🟠 Вкладка 2 «Сумма и срок» — пополевые правила (пополевой разбор с владельцем 2026-06-25)
  - **режимы (Тип суммы/срока):** оба режима «Фиксированная»/«Диапазон» оставить; дефолт «Фиксированная»; **переименовать** «Тип суммы кредита» → **«Ограничение суммы»**, «Тип срока кредита» → **«Ограничение срока»**; на сохранении брать только активный режим (данные скрытого не сохранять);
  - **Фиксированная (сумма и срок):** значения выбираются **из справочника** (модалка-пикер сохраняется — справочники переиспользуются в будущих отчётах/аналитике); **запрет дублей** в гриде; **авто-сортировка по возрастанию**;
  - **Диапазон (сумма и срок):** numeric-поля (сумма — маска: разряды + 2 знака + код валюты; срок — целое); правило `0 < мин ≤ макс`; **мин == макс — мягкий warning** «рассмотрите режим Фиксированная»; верхнего потолка нет;
  - **срок — в месяцах, единицу писать явно** («Ограничение срока», суффикс «мес.» в полях и гриде); срок — целые положительные; сумма — 2 знака, положительная;
  - **Валюта** под колонкой «Сумма» — read-only из вкладки 1, оставить; если валюта пуста — плейсхолдер «—»;
  - **ведение справочников «Суммы кредита» / «Сроки кредита» — только администратор**, с валидацией (суммы > 0; сроки — целые > 0); чистка мусора (негативы) — дефект **P2-12**; правила валидации полей — дефект **P2-13**.
- [ ] P2-R16 🟠 Вкладка 3 «Процентные ставки» — пополевые правила (пополевой разбор с владельцем 2026-06-25)
  - **Тип основной ставки:** оба режима «Фиксированная»/«Диапазон» оставить, дефолт «Фиксированная», термин без изменений («основная» противопоставлена «плавающей»); на сохранении брать только активный режим;
  - **Значение фиксированной ставки:** мульти-выбор оставить, **только из справочника** ставок (свободного ввода нет — справочник переиспользуется в аналитике); суффикс «%»; запрет дублей-чипов; открытый вопрос на будущий модуль — при нескольких фикс-ставках нужен критерий «какая применяется» на уровне заявки;
  - **Диапазон ставки (с/по):** numeric + 2 знака + «%»; `0 < с ≤ по`; **с == по — мягкий warning** «рассмотрите режим Фиксированная»; верхнего потолка нет;
  - **Плавающая ставка — надбавка к основной** (не альтернативный тип): итоговая = основная (база) + бенчмарк; чекбокс-модель оставить (радио не нужно); косметика — «плавающую» со строчной;
  - **Тип плавающей ставки:** контрол-пикер **из справочника бенчмарков оставить** (combobox не делать); хранится тип/индекс, значение бенчмарка подтягивается на дату расчёта; справочник админ-ведомый + проверить мусор (дефект **P2-15**);
  - **«Маржа к плавающей ставке» = read-only зеркало основной ставки** (НЕ отдельный ввод; разработчики продублировали — дефект **P2-14**): показывать все фикс-значения (если мульти) либо диапазон + бенчмарк, формат-пример «от 1 до 5 + ЛИБОР 6м»;
  - **справочники ставок и бенчмарков — админ-ведомые, наполнить, проверить мусор** (справочник ставок сейчас 1 строка «10,00») — дефект **P2-15**.
- [ ] P2-R17 🟠 Вкладка 4 «Штрафы» — пополевые правила (пополевой разбор с владельцем 2026-06-25)
  - **переименовать опцию «Диапазон» → «Плавающая»** по всему табу (и осн. сумма, и проценты): ветка показывает бенчмарк+маржу, а не с/по — дефект **P2-16**; оба варианта (фикс/плавающая) нужны, дефолт «Фиксированная»;
  - **штрафная ставка — из справочника** (не свободный ввод), единообразно со ставками таба 3, для аналитики; единица — **«% годовых»**; справочник штрафных ставок админ-ведомый (возможно, ещё не заведён);
  - **плавающая штрафная:** пикер бенчмарка из справочника оставить (админ-ведомый + чистка мусора); **«Маржа к плавающей штрафной ставке» = read-only зеркало** фикс. штрафной ставки (НЕ отдельный ввод, как P2-14); итоговый штраф = бенчмарк + фикс. штрафная ставка;
  - **«Максимальный размер штрафа»:** ограничивает **весь штраф суммарно** (тело + проценты), «% от первоначальной суммы кредита»; **вынести в отдельную полосу «Ограничения штрафа»** (вне колонок); обязателен, **дефолт 20**;
  - **компоновка / дефект B (P2-17):** убрать дубль-заголовок «Штраф за просрочку процентов» — после выноса потолка остаётся структура: две колонки [Осн. сумма | Проценты] + полоса [Ограничения штрафа];
  - блок «по процентам» (тип/фикс-значение/плавающая+маржа) — **зеркало блока «по основной сумме»**;
  - дефекты: ярлык «Диапазон» + дубль маржи + свободный ввод — **P2-16**; область потолка + дубль-заголовок — **P2-17**.

### Фаза 3 — Заявки и комиссии по заявкам: предложения по улучшению (для команды разработки)
_Полное обоснование в `requirements/features/03-application-commission.md` → Recommendations.
Нумерация `P3-R*` — отдельный счётчик фазы (как `P3-xx` у дефектов). Дефекты —
`notes/qa-findings.md` (P3-xx). Модуль зрелее предыдущих: уже есть статус-степпер,
4-eyes-голосование комиссии, inline-валидация, режим просмотра у комиссии._
- [ ] P3-R1 🟡 Визуальная семантика полей формы заявки
  - сейчас: в диалоге «Новая заявка» редактируемые поля (Адрес, Номер телефона, Источник финансирования) в сером «disabled»-стиле; обязательные/опциональные/readonly визуально неотличимы (P3-01);
  - **область — только форма создания** («Новая заявка»), где серый стиль подтверждён визуально; страница деталей и экран комиссии — вне этой задачи;
  - применить **тот же единый стиль полей дизайн-системы**, что вводится в P1-R6/P2-R1: обязательное-редактируемое → белый фон + рамка + красный `*`; опциональное → белый фон + рамка; readonly/вычисляемое → серый;
  - переиспользовать **общий компонент** из P1-R6/P2-R1 (не плодить новый), задача остаётся отдельной по фазе для трекинга;
  - прим.: обязательные поля здесь уже показывают `*` и inline «Поле является обязательным» — остаётся только серый базовый стиль (тот же корень, что P1-01/P2-01).
- [ ] P3-R2 🟡 Отдельная страница деталей заявки («Просмотр»)
  - сейчас: в тулбаре заявок Создать/Изменить/Удалить/Отправить в комиссию/Отправить в залоговую комиссию — нет «Просмотр», инспекция вынуждает входить в редактирование (`/loan-applications/{id}`, без `?mode=readonly`) (P3-02);
  - **расхождение внутри фазы**: соседний экран «Комиссии по заявкам» «Просмотр» и readonly-режим **имеет**;
  - сделать: **отдельную страницу деталей** заявки (как решили для программ в P2-R4 и для решений в Фазе 1 R6) — единый читаемый обзор всех данных заявки;
  - открытие из списка: **кнопка «Просмотр»** в тулбаре (выбрать строку → Просмотр) **и двойной клик по строке → Просмотр (readonly)**;
  - со страницы просмотра — кнопка **«Изменить»** (переход в редактирование);
  - выровнять с P2-R4 и Фазой 1 (R6 «Просмотр + действия»).
- [ ] P3-R3 🟠 Валидация дат и чисел заявки
  - факт (проверено 2026-06-18): у полей «Запрашиваемая сумма» и «Запрашиваемый срок» нет min/max/step, у пикера даты комиссии нет min — клиент не блокирует 0/отрицательное/прошлые даты (P3-03);
  - **числа заявки**: «Запрашиваемая сумма» > 0 и «Запрашиваемый срок» > 0;
  - **границы программы**: запрашиваемая сумма и срок должны укладываться в min/max суммы и срока, заданные в связанной кредитной программе (Фаза 2) — кросс-проверка с правилами программы;
  - **даты комиссии**: «Крайний срок» голосования ≥ сегодня; «Дата заседания» не раньше создания комиссии (порядок дат);
  - валидация на клиенте и на сервере, с внятными сообщениями (не молчаливый сброс);
  - прим.: у самой заявки своих дат нет (только срок-число), поэтому проверка завязана на границы программы, а не на окно её действия.
- [ ] P3-R4 🟠 Правила работы комиссии (финальное решение, 4-eyes)
  - голосование реализовано (Прогресс голосования, Члены/Председатель, Решение + Крайний срок, Финальное решение, Протокол), но правила в UI не видны (P3-04);
  - позиция заказчика согласована 2026-06-19 + проверено на стенде 2026-06-19 (`scripts/inspect/verify-p3r4*.mjs`); scope разработки сузился до двух пунктов:
  - **#1 — «Финальное решение» ручное + аудит** 🟠 (разработка):
    - UI-поле уже ручное (отдельная секция «Финальное решение (Председатель комиссии)», голосование — отдельная кнопка «Проголовать»); править UI не нужно;
    - добавить **аудит-запись** при сохранении финала: кто/когда/какое решение + причина, особенно когда финал расходится с тэлли голосов (через UI не виден — вероятно нужен backend);
  - **#2 — 4-eyes предупреждение** 🟠 (разработка):
    - если автор/податель заявки оказался членом/председателем комиссии по своей заявке — не блокировать, показать предупреждение + пометить в аудите;
    - проверено: такого предупреждения в UI сейчас нет → новая разработка;
  - **#3 — кворум: без порога** ✅ no-op (подтверждено заказчиком + стендом): голосование закрывает председатель в любой момент, голоса справочно; текста про кворум/порог в UI нет, авто-блокировки по тэлли нет — только задокументировать;
  - **#4 — порядок комиссий: параллельно, независимо** ✅ no-op (подтверждено заказчиком + стендом): обе кнопки «Отправить в комиссию»/«Отправить в залоговую комиссию» активны независимо, гейтинга нет; кредитная и залоговая параллельны (2 статуса заявки) — только задокументировать;
  - **#5 — «Крайний срок» члена: напоминание/эскалация** 🟠 (разработка, согласовано 2026-06-19):
    - дедлайн принуждается **уведомлением**: при приближении/истечении срока — нотификация члену + председателю;
    - голос **не меняется автоматически** (без авто-«воздержался» — иначе конфликт с «председатель закрывает голосование в любой момент»); голос открыт пока не подан или председатель не закрыл;
    - новая разработка: таймер/планировщик + механизм нотификаций;
  - ✅ готово к разработке; реальный код — #1 (аудит) + #2 (warning) + #5 (напоминание/эскалация).
- [ ] P3-R5 🔴 «Субъект» и «Кредитная программа» — обязательные (форма «Новая заявка»)
  - факт (проверено 2026-06-19, `scripts/inspect/app-create-fields.mjs`): оба поля ядро заявки, но `required=false` на сервере — заявку можно провести без заёмщика и без программы (P3-05);
  - сделать: `*` + inline «Поле является обязательным» (переиспользовать паттерн суммы/срока) **и** серверную валидацию;
  - блокировать «Далее», пока оба не заполнены;
  - проблема целостности данных, не только UI: заявка без субъекта/программы бессмысленна и засоряет список.
- [ ] P3-R6 🟡 Отложить показ ошибки обязательности (форма «Новая заявка»)
  - факт (проверено 2026-06-19): «Запрашиваемая сумма»/«Запрашиваемый срок» показывают красный фон + «Поле является обязательным» **сразу при открытии**, до взаимодействия (P3-06);
  - сделать: ленивая валидация — ошибка по blur или по «Далее», не на рендере;
  - eager-ошибки на нетронутой форме читаются как «вы что-то сделали не так» до начала ввода — портят в остальном эталонный UX валидации.
- [ ] P3-R7 🟠 Гард действий тулбара по статусу (список заявок)
  - факт (проверено 2026-06-19, `scripts/inspect/verify-p3r5r8.mjs`): «Отправить в комиссию»/«Отправить в залоговую комиссию»/«Удалить» активны для любой выделенной строки **независимо от статуса** (проверено на Одобрено/Отклонено/На рассмотрении/Требуется доп. информация) — гард только по выделению (P3-07);
  - сделать: блокировать «Отправить…» на терминальных статусах (Одобрено, Отклонено); блокировать «Удалить» на Одобрено и на строках со ссылками вниз (кредит/комиссия); диалог подтверждения + тост-результат по строкам для bulk-отправки; клиент + сервер;
  - тот же класс, что Фаза 1 R4/R5 (P1-03/P1-05): можно переотправить Одобренную или удалить её, отправить Отклонённую.
- [ ] P3-R8 🟡 Колонки списка: показать результат, убрать балласт
  - факт (проверено 2026-06-19, тот же запуск): нет колонки «Одобренная сумма» (`hasApprovedAmount=false`) — одобренная комиссией сумма/срок не видны в списке, виден только запрос; «Дополнительная информация» (свободный текст, чаще пустой) — колонка низкой ценности (P3-08);
  - сделать: добавить **«Одобренная сумма»**; опционально счётчик голосов комиссии (`n/4`, уже есть на соседнем экране) и **Дата создания**; убрать/понизить «Дополнительная информация».
- [ ] P3-R9 🟡 (док) Полный набор статусов заявки + переходы
  - факт (проверено 2026-06-19): обнаружен 4-й статус заявки «Требуется доп. информация» (Заявка-74); спек знал только 3 (На рассмотрении/Одобрено/Отклонено) (P3-09);
  - сделать: выписать полный набор статусов (Статус заявки + Статус залога) и переходы из модели, обновить спек.

_Страница деталей заявки — пообъектный обход 9 табов (проверено 2026-06-19, id 28 →
«Заявка - 60», статус Одобрено; `scripts/inspect/app-detail-tabs.mjs`, `app-detail-geo.mjs`;
полный разбор `notes/app-detail-analysis.md`). Дефекты P3-10..P3-17._
- [ ] P3-R10 🟠 Гард редактирования полей по статусу (страница деталей)
  - факт (проверено 2026-06-19): на одобренной заявке «Запрашиваемая сумма» и «Номер телефона» остаются редактируемыми, остальное readonly (P3-10);
  - сделать: блокировать поля по статусу — у одобренной заявки условия полностью read-only (или все редактируемы по явному праву).
- [ ] P3-R11 🟠 График: проценты не считаются
  - факт (проверено 2026-06-19): в графике платежей «Проценты» = 0,00 во всех строках при ставке 12 % годовых; основной долг амортизируется (16 666,67 ×12 = 200k) (P3-11);
  - сделать: рассчитывать проценты по ставке за период; проверить генератор графика.
- [ ] P3-R12 🟠 Обязательные документы не гейтят одобрение
  - факт (проверено 2026-06-19): обязательные документы (Паспорт лиц./обор., Подписанное согласие, Паспорт на кредит./залог.) пусты на одобренной заявке, «Загрузить» активен после одобрения (P3-12);
  - сделать: обязательные документы — условие одобрения; блокировать загрузку после прохождения документного этапа.
- [ ] P3-R13 🟡 График редактируем после одобрения
  - факт (проверено 2026-06-19): «Построить график»/«Добавить строку» активны на одобренной заявке (P3-13);
  - сделать: подтвердить, должен ли график зафиксированного кредита меняться и кем; гейт по статусу/роли.
- [ ] P3-R14 🟡 Дубль колонки «Сотрудник» в гриде комиссии
  - факт (проверено 2026-06-19): в гриде решений комиссии две колонки с заголовком «Сотрудник» (логин `[admin]` / ФИО) (P3-14);
  - сделать: разные заголовки — «Логин» / «ФИО» (или «Должность»).
- [ ] P3-R15 🟡 Пустые селекты одобренных условий
  - факт (проверено 2026-06-19): «Периодичность платежей»/«Метод погашения»/«Обработка выходных» в блоке «одобренные комиссией» пусты при наличии графика (P3-15);
  - сделать: проверить сохранение значений; если есть — починить read-only рендер селектов.
- [ ] P3-R16 🟡 Пустая История (нет аудит-трейла)
  - факт (проверено 2026-06-19): таб «История» (Дата/Заголовок/Детали/Автор) пуст для заявки Подача→Одобрено (P3-16);
  - сделать: логировать переходы статусов и решения комиссии в Историю.
- [ ] P3-R17 🔵 Шапка: id≠номер, повтор на всех табах
  - факт (проверено 2026-06-19): «Заявка - 60» ≠ id в URL (28); 5 полей шапки повторяются на всех 9 табах (P3-17);
  - сделать: косметика — свернуть повтор шапки / прояснить id vs отображаемый номер.

### Фаза 4 — Заёмщики: предложения по улучшению (для команды разработки)
_Полное обоснование в `requirements/features/04-borrower.md` → Recommendations.
Нумерация `P4-R*` — отдельный счётчик фазы (как `P4-xx` у дефектов). Дефекты —
`notes/qa-findings.md` (P4-xx). Модуль зрелый: вкладочная карточка + панель
«Результаты проверок» (риск/комплаенс)._
- [ ] P4-R1 🟡 «Просмотр» и «Создать» для заёмщиков
  - сейчас: в тулбаре только Изменить/Удалить — нет «Просмотр» (инспекция вынуждает редактирование) и нет «Создать» (P4-01);
  - **«Просмотр» — отдельной страницей деталей** (как P2-R4/P3-R2, Фаза 1 R6): кнопка «Просмотр» в тулбаре + двойной клик по строке → readonly; со страницы — «Изменить»;
  - **добавить «Создать»**: ручная регистрация заёмщика (привязка к стороне/реестру), форма по образцу вкладки «Основная информация»; помимо авто-появления из «Субъекта» заявки;
  - выровнять тулбар с остальными модулями (Создать/Просмотр/Изменить/Удалить).
- [ ] P4-R2 🟡 Визуальная семантика полей карточки заёмщика
  - сейчас: поля в сером «disabled»-стиле, обязательные/опциональные/readonly неотличимы (P4-02);
  - **область — только карточка** (вкладка «Основная информация» редактирования/деталей), где серый стиль подтверждён; новая форма «Создать» — вне этой задачи;
  - переиспользовать общий компонент полей из P1-R6/P2-R1 (обязательное → белый+рамка+`*`; опциональное → белый+рамка; readonly → серый);
  - задача остаётся отдельной по фазе для трекинга.
- [ ] P4-R3 🟠 Баг: противоречие по активным кредитам (согласовано 2026-06-19)
  - факт (проверено 2026-06-18): шапка показывает «1 активных кредитов на сумму 188 000», а проверка «Действующие кредиты» — «Нет активных кредитов» (при «Количество кредитов: 1») (P4-03);
  - **единый источник**: завести один расчёт «активных кредитов» (по статусу кредита — открытые/непогашенные) и питать из него и шапку, и панель «Результаты проверок» — расхождение станет невозможным технически;
  - **развести термины явно**: «активные / действующие» = открытые сейчас (непогашенные); «всего кредитов» = вся история; разные подписи в шапке и панели, чтобы «1» (всего) не путалось с числом активных.
- [ ] P4-R4 🟠 Баг: скрытое обязательное поле «ID в системе» (согласовано 2026-06-19)
  - факт (проверено 2026-06-18, запись 11): при открытии заёмщика — тост «Заполните поле "ID в системе"», но самого поля на форме нет — сохранение будет блокироваться (P4-04);
  - **трактовка**: «ID в системе» — технический id (скоринг / внешняя интеграция), не для ручного ввода;
  - **фикс**: заполнять системой/интеграцией автоматически, из формы убрать, валидацию required не вешать на пользователя — тост на открытии не должен появляться;
  - задокументировать назначение поля (что за система/интеграция выдаёт id).
- [ ] P4-R5 🟠 Семантика панели «Результаты проверок» (согласовано 2026-06-19: полностью открытый вопрос — дефолт не навязываем)
  - проверки: Чёрный список, Кредитная история, Действующие кредиты, Просрочки, Скоринговая система, Лимит задолженности (P4-05);
  - по каждой проверке уточнить у заказчика: источник данных (живая интеграция / расчёт / вручную), частота обновления, блокирует выдачу или справочная;
  - дефолтное разделение блокирующих/справочных НЕ предлагать — собрать ответы заказчика;
  - ❓ требует подтверждения заказчика.

### Фаза 5 — Выдача кредитов: предложения по улучшению (для команды разработки)
_Полное обоснование в `requirements/features/05-loan-issuance.md` → Recommendations.
Нумерация `P5-R*` — отдельный счётчик фазы (как `P5-xx` у дефектов). Дефекты —
`notes/qa-findings.md` (P5-xx). Кредит = материализованная одобренная заявка;
страница деталей — хаб обслуживания из 11 вкладок (`/loan-credits/{id}`)._
- [ ] P5-R1 🔴 Падение страницы деталей кредита (data-dependent)
  - факт (проверено 2026-06-20, `scripts/inspect/loans-crash.mjs`): записи 20 и 22 открываются с диалогом «Непредвиденная ошибка» — и в режиме редактирования (`loan-credits/20`), и в readonly (`loan-credits-read/20?mode=readonly`); записи 18/19/21 открываются нормально → зависит от данных (P5-01);
  - серверное необработанное исключение: Vaadin рендерит ошибку через UIDL (HTTP 200, в сети нет 4xx/5xx; в консоли только шум push-websocket) — клиентских следов нет;
  - сделать: снять **серверный лог** при открытии записи 20/22, найти первопричину (вероятно битые/неполные связанные данные кредита); починить; на уровне UI — заменить общий диалог на обработанное сообщение, а не «белый экран».
- [ ] P5-R2 🟠 Гард полей и статуса кредита по статусу
  - факт (проверено 2026-06-20): «Статус кредита» (combo), «Сумма по договору» и условия на вкладке «Условия кредита» редактируемы независимо от статуса; статус меняется прямым выбором значения, без управляемых переходов (P5-02);
  - сделать: блокировать финансовые условия после выдачи; статус вести через переходы воркфлоу с аудитом (тот же класс, что P3-R10/P3-R7 и Фаза 1 R7);
  - переиспользовать единую модель жизненного цикла Фазы 1 (R7).
- [ ] P5-R3 🟠 Валидация «Суммы по договору»
  - факт (проверено 2026-06-20): «Сумма по договору» = 250 000,00 больше «Одобренной суммы» = 150 000,00 (read-only); поле — обычный текстовый инпут без числовой/min/max валидации (P5-03);
  - сделать: «Сумма по договору» ≤ «Одобренной суммы» и > 0; числовое поле, не свободный текст; валидация на клиенте и на сервере, с внятным сообщением;
  - тот же класс, что P3-R3 (числа заявки в границах программы).
- [ ] P5-R4 🟡 Отдельная страница «Просмотр» кредита
  - факт (проверено 2026-06-20): в тулбаре `/loansCredit` единственный read-путь — «Просмотр» — ведёт на падающий readonly-экран (P5-01); список показывает «Статус заявки», а не собственный «Статус кредита» (P5-04);
  - сделать: отдельную читаемую страницу деталей кредита (переиспользовать паттерн P2-R4/P3-R2, Фаза 1 R6 «Просмотр + действия»);
  - согласовать, какой статус показывать в списке (заявки vs кредита).
- [ ] P5-R5 🟡 Чистка полей формы кредита (формат/даты/ставка/опечатки)
  - факты (проверено 2026-06-20): несогласованный формат суммы на одном экране (`150000.00` vs `250 000,00`, P5-05); дата разбита на 3 безымянных виджета — date-time + date + time (P5-06); опечатки «Измнен», «начисения»/«начсиления» (P5-07); «Процентная ставка» read-only и пустая, хотя график считает проценты (P5-08);
  - сделать: единый денежный формат везде; один подписанный date(-time)-инпут; показать действующую ставку (вероятно наследуется от программы); исправить опечатки;
  - переиспользовать компонент семантики полей дизайн-системы (P1-R6/P2-R1/P3-R1) — обязательное/опциональное/readonly визуально различимы.

### Фаза 6 — Освоение и транши: предложения по улучшению (для команды разработки)
_Полное обоснование в `requirements/features/06-disbursement-tranches.md`.
Нумерация `P6-R*` — отдельный счётчик фазы (как `P6-xx` у дефектов). Дефекты —
`notes/qa-findings.md` (P6-xx). Модель: Кредит → Транш (субкредит) → Освоение
(фактическая выборка средств, со своим графиком). Экраны под «Приложение»:
`/sub-loans` (Список Траншей) и `/disbursements` (Освоение)._
- [ ] P6-R1 🟠 Локализация: убрать сырые имена сущностей из UI
  - факт (проверено 2026-06-20, `scripts/inspect/p6.mjs`): заголовок первой колонки списка траншей — `SubLoan.amount` (должно быть «Сумма транша»); поле-пикер родительского транша на деталях освоения подписано `Disbursement.subLoan` (должно быть «Транш») (P6-01);
  - сделать: добавить недостающие ключи i18n; пройтись по всем экранам фазы и заменить `Entity.property` на русские подписи;
  - тот же класс, что чистка подписей в P5-R5 — но виднее (главный заголовок колонки).
- [ ] P6-R2 🟡 График транша не формируется автоматически
  - факт (проверено 2026-06-20): транш `/sub-loans/1` (статус Active, срок 24, ставка 10%), вкладка «График» пустая — Кол-во платежей 0, суммы 0.00, грид пуст, хотя под ним есть освоение (50 000) с полностью посчитанным графиком (P6-02);
  - сделать: формировать/агрегировать график транша автоматически (или не показывать вводящую в заблуждение нулевую сводку, пока не нажат «Пересчитать график»);
  - расчёт на уровне освоения корректен (50 000 @10%/24мес → платёж 2 307,25) — пробел только в сводке транша.
- [ ] P6-R3 🔵 Опечатка «шрафы» → «штрафы»
  - факт (проверено 2026-06-20): на вкладке «Условия» транша значение «Очередность погашения» = «Основная сумма, проценты, шрафы» (P6-03);
  - сделать: исправить на «… штрафы».

### Фаза 7 — Обслуживание (платежи, резервы, реестр): предложения по улучшению (для команды разработки)
_Полное обоснование в `requirements/features/07-servicing.md`. Нумерация `P7-R*` —
отдельный счётчик фазы (как `P7-xx` у дефектов). Дефекты — `notes/qa-findings.md`
(P7-xx). После освоения кредит обслуживается: Платеж (`/payments`), Резерв
(`/loan-reserves`), LoanLedger (`/loan-ledgers`) — журнал финансовых событий._
- [ ] P7-R1 🟠 Локализация всех экранов обслуживания
  - факт (проверено 2026-06-20, `scripts/inspect/p7.mjs`): в Резерве сырые заголовки `Loan`, `Loan amount`, `LoanReserve.disbursedAmount/.unusedAmount/.reserveRate/.reserveAmount`; в LoanLedger колонка «Событие» мешает сырой энум `EventType.PRINCIPAL_ACCRUAL`, английское `Interest accrual`/`Payment` и русское `Пеня`; заголовок страницы — `LoanLedger` (P7-01);
  - сделать: добавить ключи i18n для всех заголовков; единый русский набор подписей для энума событий; перевести заголовок страницы;
  - тот же класс, что P6-R1, но шире (3 экрана).
- [ ] P7-R2 🟠 Убрать служебные колонки из списка платежей
  - факт (проверено 2026-06-20): грид `/payments` показывает `Payment uuid` (UUID сущности) и `Payment version` (версия оптимистичной блокировки) (P7-02);
  - сделать: скрыть инфраструктурные колонки; в списке — только бизнес-поля.
- [ ] P7-R3 🟡 Дублирующая колонка статуса в платежах
  - факт (проверено 2026-06-20): первая колонка «Статус» показывает сырой код `1` во всех строках, дублируя «Статус платежа» с человекочитаемым значением («Оплачен») (P7-03);
  - сделать: оставить один столбец статуса с локализованным значением, сырой код убрать.
- [ ] P7-R4 🟡 Нет страницы просмотра резерва
  - факт (проверено 2026-06-20): в `/loan-reserves` тулбар = только «Обновить» + «Добавить условие поиска» (нет Создать/Изменить/Просмотр/Удалить), двойной клик по строке ничего не открывает — у записи резерва нет детальной страницы (P7-04);
  - сделать: добавить read-only страницу деталей/«Просмотр», чтобы видеть, как посчитан резерв (переиспользовать паттерн P2-R4/P3-R2).
- [ ] P7-R5 🟡 LoanLedger должен быть append-only
  - факт (проверено 2026-06-20): реестр — журнал финансовых событий, но в тулбаре есть «Удалить» (по выбору строки); удаление событий ломает аудируемость (P7-05);
  - сделать: запретить удаление строк реестра; корректировки — сторнирующими проводками; любое админ-удаление — только с аудитом.

### Фаза 8 — Справочники (классификаторы): предложения по улучшению (для команды разработки)
_Полное обоснование в `requirements/tz/08-spravochniki.md`. Нумерация `P8-R*` —
отдельный счётчик фазы (как `P8-xx` у дефектов). Дефекты — `notes/qa-findings.md`
(P8-xx). Сквозной слой справочников, на который ссылаются формы разделов 01–07;
проверено 2026-06-24, `scripts/inspect/tz/dump-dict.mjs` (47 экранов: данные + CSS).
Стенд тестовый — значения индикатор структуры, не норматив._
- [ ] P8-R1 🟠 Единый архетип справочника + режим «Просмотр»
  - факт (проверено 2026-06-24): из 47 экранов только 7 (`industryDirections`, `collateralTypes`, `commissions`, `creditTerms`, `calculationCoefficients`, `individuals`, `organizations`) имеют расширенный шаблон (Код + Статус + Дата создания + «Просмотр»); остальные 40 — голый CRUD без «Просмотр», инспекция вынуждает входить в редактирование (P8-06, P8-09);
  - сделать: распространить расширенный шаблон с режимом «Просмотр» на все простые словари; переиспользовать паттерн P2-R4 / Фаза 1 R6 («Просмотр + действия»);
  - также: 3 справочника формы программы («Цели кредита», «Уровни проверки документов», «Типы расчета процентов») не имеют пункта меню — открываются только из picker'а; завести управляющий экран (P8-09).
- [ ] P8-R2 🟠 Локализация заголовков справочников
  - факт (проверено 2026-06-24): `agreement-templates` и `agreement-template-codes` показывают сырые Java-пути в заголовках колонок («AgreementTemplate.templateType», «AgreementTemplateCode.code» …); экран `destination-accounts` имеет англоязычный заголовок «Destination accounts» (P8-04, P8-05);
  - сделать: ключи i18n для заголовков колонок и заголовка экрана; тот же класс, что P6-R1 / P7-R1.
- [ ] P8-R3 🟠 Наполнить пустые справочники-енумы
  - факт (проверено 2026-06-24): 14 справочников возвращают 0 строк — `order-document-types`, `entity-document-states`, `entity-document-registered-bies`, `document-package-states`, `document-package-types`, `applied-entity-list-states`, `applied-entity-list-types`, `applied-entity-states`, `order-term-frequency-types`, `order-term-rate-periods`, `order-term-accr-methods`, `work-sectors`, `loan-payment-capacity-groups`, `good-types`; часть — статус/тип-енумы воркфлоу, пустой справочник = поле без значений для выбора (P8-03);
  - сделать: наполнить нормативными значениями **до** включения серверной валидации обязательных полей (P2-R10); перечни задаёт владелец.
- [ ] P8-R4 🟡 Чистка тест-мусора + фикс «Статус кредита»
  - факт (проверено 2026-06-24): `loan-states` («Статус кредита») содержит единственную мусорную строку «Кредит компании» — не статус, валидных статусов жизненного цикла нет (P8-01, риск для раздела 05); тест-мусор в боевых-кандидатах: «соновное 1», «123/321», «hidshgiu/fgxdzxh», «шрафы» (опечатка), «Aibek rate» (P8-08);
  - сделать: задать нормативный набор статусов кредита; вычистить опечатки и заглушки; зафиксировать перечни с владельцем.
- [ ] P8-R5 🟡 Статусы отрисовывать бейджами
  - факт (проверено 2026-06-24): значения статус-справочников и колонка «Статус» расширенных классификаторов («Активный») выводятся простым текстом; дизайн-система мокапов предусматривает цветные бейджи статусов (P8-07);
  - сделать: рендерить статусы бейджами дизайн-системы (тинты `status-*-bg`); согласовать с бейджами статуса Фазы 1 / P2 (P1-08).
- [ ] P8-R6 🔵 Единая плотность/шрифт грида справочников
  - факт (проверено 2026-06-24): шрифт тела грида 14px vs 16px, высота строки 29/30/32/34px/auto вразнобой; остальные токены (заголовок 18px/#192434, primary #006AF5, рамка rgba(28,55,90,0.16), padding 16px) единообразны и совпадают с дизайн-системой мокапов (P8-10);
  - сделать: один размер шрифта и высоту строки на всех экранах справочников.

## 🟢 Позже / идеи
- [ ] Пока вне области: Поручительства, СУГС (субсидии)

## ✅ Недавно сделано
- [x] 2026-06-17 — Вход в тестовую среду, задокументирована полная карта модулей
- [x] 2026-06-17 — Создано рабочее пространство проекта
