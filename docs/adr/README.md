# ADR — как нумеруются

Нумерация **сквозная по всему репозиторию**, не по модулям: `NNNN-slug.md`, четыре цифры.
Модуль указывается в строке статуса внутри файла («принято · дата · модуль «…»»), а не
в имени.

## Правило номера

**Номер берётся по максимуму в `main`, а не в своей ветке.** Модули пишутся параллельно, в
разных worktree, и «следующий свободный» у каждой ветки свой. Перед заведением ADR:

```bash
git fetch --all --quiet
git ls-tree -r --name-only origin/main docs/adr/ | sed 's|.*/||' | cut -c1-4 | sort | tail -1
# и то же по живым веткам соседних worktree:
git branch -a --format='%(refname:short)' | grep worktree-
```

Если соседняя ветка уже заняла номер, но ещё не влита — брать следующий за **её** максимумом.
Занять блок вперёд (например четыре номера на волну) дешевле, чем разъезжаться потом.

## Что делать при столкновении

Уступает **тот, кто закоммитил номер позже** (по времени коммита, а не по дате в шапке).
Уступивший сдвигается в свободный блок целиком, сохраняя порядок и слаги. Правятся:
файлы ADR, перекрёстные ссылки внутри них, макет и спека модуля, журнал волн, смоук,
`STATUS.md`, `TODO.md`, ТЗ и пакет передачи, если номера туда дошли.

Диапазоны в тексте («ADR `0055`…`0065`») после сдвига становятся неверными молча — их
искать отдельно: `grep -rn "00[0-9][0-9].\{0,3\}…"`.

## Карта занятого (21.08.2026)

| номера | модуль |
|---|---|
| 0001 | общий («производные не хранятся») |
| 0002–0004 | Взыскание задолженности |
| 0006–0009, 0014–0016 | Залоговое обеспечение |
| 0010, 0042, 0104, 0105, 0108, 0111–0113, 0115, 0119 | Кредиты |
| 0020–0041, 0043–0047, 0049–0054 | Взыскание задолженности |
| 0055–0058 | Платежи |
| 0062–0072 | Взыскание задолженности |
| 0073–0076 | Платежи |
| 0077–0081 | Взыскание задолженности |
| 0087–0091 | Платежи |
| 0092–0099 | Реструктуризация |
| 0100–0103 | Взыскание задолженности |
| 0106–0107, 0109–0110, 0114 | Реструктуризация |
| 0111–0113 | Кредиты |
| 0116–0118, 0131 | Кураторство |
| 0120–0127, 0137 | Классификация |
| 0128–0130 | Кредиты |
| 0132–0134 | Кредиты |
| 0135–0136, 0173–0175, 0181–0182, 0191–0196 | Расчётное ядро (`worktree-calculations`) |
| 0145–0152, 0176–0180, 0183–0186, 0190, 0197–0201 | Статистика (`worktree-stats`) |
| 0153–0155 | Анализ |
| 0156–0168 | Отчётность |
| 0005, 0011–0013, 0017–0019, 0048, 0057, 0059–0061 | метки модуля в шапке нет — смотреть по содержанию |

Блок `0082–0086` пуст, а не занят: изначальная заявка реструктуризации на `0079–0086`
целиком уехала на `0092–0099` из-за столкновения с этой веткой — см. «Второе столкновение»
ниже. **`0105` был оставлен пустым намеренно** — отдан ветке кредита, которая вела волну тем
же днём; реструктуризация взяла блок через номер, чтобы не столкнуться в четвёртый раз (см.
«Третье столкновение»). Ставка подтвердилась в тот же вечер: кредит занял `0105`
(`0105-procenty-na-fakticheskoe-telo`, влит в `main` 10.08.2026 18:22).

**Следующий свободный — `0202`.** `0201` взят 28.08.2026 волной 12 статистики (три тонких
объекта): **тонкий объект доспрашивается или снимается** — объект без владельца, отдающего
множество, уходит из реестра строкой, а не релизом (§1); величина про подчинённое множество
собирается срезом потомка по разрезу-родителю, а полем родителя была бы вторым источником
(§2); объявленный источник, которого не существует, есть заявка владельцу, и объект без
единой меры строки законен (§3); многозначное поле владельца разрезом не становится (§4).
Номер проверен по `origin/main` и всем живым веткам `worktree-*` перед записью: везде
максимум `0200`.

**Прежняя запись (волна 10 статистики).** Следующий свободный — `0201`. Блок `0198`–`0200` взят 28.08.2026 волной 10 статистики
(дело и требование): `0198` — **требование есть объект статистики, дело остаётся тонким
отрезком работы**: многозначное на дату (фаза) переезжает вниз, у дела остаются филиал,
куратор, регион и расходы по взысканию — единственная денежная статья, которой оно владеет;
`0199` — **дедуп-ключ показателя**: у величины, чьи строки пересекаются по чужому ключу,
ключ объявлен записью, схлопывание идёт перед агрегатом на каждом узле дерева, сумма строк
итогу не равна и не должна, а паспорт ключ называет; `0200` — **удельная величина не
показатель, а пара «срез + счёт»**: знаменатель спрашивается у своего объекта, снятый со
среза он занижен систематически. Номера проверены по `origin/main` и всем живым веткам
`worktree-*` перед записью: везде максимум `0197`, столкновения нет — блок взят подряд.

**Прежняя запись (волна 9 статистики).** Следующий свободный — `0198`. `0197` взят 28.08.2026 волной 9 статистики (рождение
строки): **строки нет до рождения объекта** — отсутствие строки третье состояние, не «пусто» и
не «ноль»; рождение объявляется реквизитом записи объекта и читается тем же читателем, что
разрез, а движок его не угадывает ни первым звеном истории, ни датой первого прогона.
Это **восьмое столкновение** и второе, пойманное ДО записи: `0191` был свободен по
`origin/main`, но `origin/worktree-calculations` держит `0191`–`0196` (волна 6 расчётного ядра:
проекция, расходы взыскания, очередь прогона) — ни один из них в `main` ещё не влит и по
`origin/main` не виден. Правило, записанное волной 8 (максимум по `origin/main` **и** по всем живым
`worktree-*`), окупилось первым же применением: восемь команд, полминуты. Проверено:
`origin/main` — `0190`, расчётное ядро — `0196`, отчётность — `0168`, анализ — `0155`,
классификация — `0137`, кураторство и кредит — `0134`, реструктуризация — `0130`.

**Прежняя запись (волна 6 расчётного ядра).** Следующим свободным был `0197`. Блок `0191`–`0196` взяла волна 6 расчётного ядра
28.08.2026 — расчётная проекция: `0191` портфельный шов читает множеством, `0192` атом
проекции — критическая дата, `0193` в проекции лежит скорость, `0194` расходы взыскания
присоединяются итогом, `0195` проекция в валюте кредита, `0196` прогон берёт работу из
очереди. `0187`–`0189` свободны и заводить их можно: волна занимала их первым заходом и
освободила, переехав целым блоком, — разорванный блок сломал бы совпадение порядка чтения с
порядком зависимостей (`0192` обязан идти раньше `0194` и `0195`, которые на него опираются).

**Седьмое столкновение разрешено: уступило расчётное ядро.** Запись волны 8 статистики ниже
поймала его до слияния и оказалась права во всём. Волна 6 ядра взяла `0184`–`0189` и записала
в каждом файле «номеров `0184+` нет ни на одной ветке» — неправду уже в момент написания:
`0183`–`0186` статистика взяла волной 7 (`6475185`), `0190` — волной 8 (`a91f3b9`), оба влиты
в `origin/main` 27.08.2026, до того как ядро взяло блок. По правилу «уступает тот, кто в
`main` ещё не влит» уступает ядро, и здесь правило сработало однозначно.

**Чем именно ошиблась проверка — и почему это важнее самого переезда.** Сверка смотрела на
**рабочую копию** `main`, а та отставала от ссылки: `git fetch origin main:main` двигает
ссылку, не трогая рабочее дерево. В дереве `/home/azamat/projects/asubk-test` лежал `0180`, в
ссылке `main` уже был `0186` — пять занятых номеров проверка не увидела и написала об этом
уверенно. Второй способ, `git log --all --diff-filter=A` по маске файла, тоже вернул пусто:
он молча пропускает файлы, пришедшие слиянием.

**Правило проверки номера, отменяющее прежние формулировки.** Номер сверяется **только**
`git ls-tree` и **только** по ссылкам — `refs/heads` и `refs/remotes` целиком, обходом, а не
`origin/main` и парой веток на глаз (запись волны 8 ниже требует того же, но перечислением
веток: перечень устаревает молча, когда заводится новая ветка). Ни рабочее дерево, ни `log`
по маске файла для этого не годятся: первое отстаёт от ссылки, второе теряет слияния.
Обе ошибки дают один и тот же результат — уверенное «свободно» на занятом номере.

**Прежняя запись (волна 8 статистики).** Следующий свободный — `0191`. `0190` взят 27.08.2026 волной 8 статистики (полный состав
показателей залога): **портфельный шов принадлежит владельцу МНОЖЕСТВА** — если множество
выбирает чужой junction (`ОТНЕСЕНИЕ_ЗАЛОГА`), шов держит его владелец, а не расчётное ядро;
отношение двух величин показателем не бывает, в срез входят числитель и знаменатель.
Номер взят **через три**, и это **седьмое столкновение** — единственное, пойманное ДО записи,
а не при слиянии: `origin/worktree-calculations` держит `0184`–`0189` (волна 6 расчётного ядра,
проекция), причём её `0184`–`0186` сталкиваются с решениями статистики, уже влитыми в `main`
тем же днём. По обоим правилам разъезжаться ядру («уступает тот, кто в `main` ещё не влит»),
но статистика этого не ждёт и берёт номер, свободный у всех, — тем же движением, каким
реструктуризация обошла ветку кредита через `0105`. Проверено по `origin/main` (максимум
`0186`) и всем живым веткам `worktree-*`: `0189` (расчётное ядро), `0168` (отчётность),
`0155` (анализ), `0137` (классификация), `0134` (кураторство), `0130` (реструктуризация).
**Правило, которое стоит записать отдельно:** свободный номер ищется не «следующим после
своего», а максимумом по `origin/main` **и** по всем живым веткам — одна команда
(`git ls-tree -r --name-only origin/<ветка> -- docs/adr`) на ветку, шесть команд, полминуты.
Шесть переездов до этого стоили по половине дня каждый.

**Прежняя запись (волна 7 статистики).** Следующий свободный — `0187`. Блок `0184`–`0186` взят 27.08.2026 волной 7 статистики
(полный состав показателей заёмщика). Номера проверены по `origin/main` и всем живым веткам
`worktree-*`: максимум — `0183` (статистика, влита в `main` тем же днём), у соседей
`0182` (расчётное ядро), `0168` (отчётность), `0155` (анализ), `0137` (классификация).
Содержание: `0184` — показатели заёмщика приходят портфельным швом, клетка суммы несёт состав
по валютам; `0185` — подгруппа как двухуровневый разрез, свёрнутая величина носит своё имя,
агрегат обязателен числу и запрещён перечислению; `0186` — счёт объектов другого уровня
задаётся своему объекту, «количества различных» в модуле нет.

**Прежняя запись (волна 6 статистики).** Следующий свободный — `0184`. `0183` взят 27.08.2026 волной 6 статистики — правило
именования показателя (`0183-imenovannaya-kletka-shva`): **показателем становится величина,
которую спрашивают о множестве объектов**, а величина об одном объекте остаётся в ядре и
берётся живым запросом; именуется клетка шва, а не строка и не столбец матрицы величин.
Это **шестое столкновение**: волна брала номер `0181` по факту свободного на своей ветке, а к
слиянию его уже занимало расчётное ядро (`0181` — волна 4, `0182` — волна 5, обе влиты в
`main` раньше). Уступил тот, кто в `main` ещё не влит, — статистика переехала на `0183`,
содержание решения не менялось. Тем же слиянием статистика **лишилась номера постановки**:
`P20-R27` она завела ядру заявкой, а ядро тем же днём выпустило пакет редакции 2 и заняло
`P20-R27`…`P20-R29` карточками. Счётчик `P20-R*` ведёт пакет ядра, и строка реестра без
карточки роняет `scripts/check_tasks.py`, — заявка осталась строкой в разделе статистики
`TODO.md`, а номер ей присвоит ядро своей редакцией.
См. «Урок, записанный дважды» ниже — теперь он записан трижды.

**Прежняя запись (ветка расчётного ядра, волна 5).** Следующий свободный — `0183`. `0182` взят 27.08.2026 волной 5 расчётного ядра — сведение с
кредитом: **очередь погашения выводит ядро, предъявляет кредит**, платежи читают шов
`calcAllocation`, а не карточку. Решение **правит** `ADR-0060` §4 («очередь публикует кредит»):
довод §4 был фактическим — «сырьё целиком у кредита», — и перестал держаться, когда волна 1
вынесла движок из `credit.html`. Тем же решением порядок очереди закреплён четырьмя ключами
(слой → срочность → дата срока → статья), а порядок статей внутри ключа — датированным
нормативом (`ADR-0173`). Проверено по `main` и всем живым веткам: максимум — `0181` (эта же
ветка); `worktree-credit` влита в `main` целиком и своих решений не ведёт.

**Прежняя запись (ветка расчётного ядра).** Следующий свободный — `0182`. `0181` взят 27.08.2026 волной 4 расчётного ядра при разборе
входящего ящика `B1`: восьмой шов `objectRows` — **ядро держит форму, перечисляет владелец
объекта**, а исчислимый налету показатель в строке всегда ядра. Решение исполняет `ADR-0158`
отчётности §1 и разрешает его внутреннее расхождение (§4 отдаёт публичный перечень полей
владельцу объекта, «Последствия» возлагают перечни на ядро) в пользу §4 и §5. Проверено по
`main` и всем живым веткам `worktree-*`: максимум — `0180` (статистика, влита в `main`
слиянием `6cc2d06`), в `worktree-reports` — `0168`, в остальных ниже. Столкновения нет:
номер взят через один после последнего влитого.

**Прежняя запись (ветка статистики).** Следующий свободный — `0181`. Блок `0176`–`0180` взяла статистика 26.08.2026 **при вливании
`worktree-stats` в `main`** — это пятое столкновение и второй переезд одного и того же блока.
Волна 3 статистики выпустила решения как `0166`–`0169`, при слиянии с отчётностью двинула их на
`0168`–`0171` (четвёртое столкновение, см. ниже), волна 4 добавила `0172`. К моменту вливания
`0168` уже занимала отчётность (волна 12, влита в `main` 26.08.2026 15:40), а `0173`–`0175` —
расчётное ядро (волна 3, влито 16:22). Правило «уступает тот, кто в `main` ещё не влит» отдало
переезд статистике целиком: `0168`→`0176` разрез несёт уровни и корзины, `0169`→`0177` обратные
ссылки хранит владелец реестра, `0170`→`0178` `statRows` двоичен, `0171`→`0179` многозначность
разреза означает нижележащий объект, `0172`→`0180` форма фильтра — ДНФ без скобок. Содержание
решений не менялось ни разу; двигались только номера.

**Урок, записанный дважды.** Номер, взятый «по факту свободного» на своей ветке, свободен
только на своей ветке. Блок из пяти решений переехал два раза за один день, и оба раза git
не помог: имена файлов разные, конфликта нет — совпадают только номера, и ловится это
глазами при слиянии.

**Записан трижды — 27.08.2026.** В тот же день статистика взяла `0181`, расчётное ядро взяло
`0181` и `0182`, и обе ветки были правы на своей стороне. Git снова смолчал: файлы
`0181-imenovannaya-kletka-shva.md` и `0181-objectrows-…md` — разные имена, конфликта нет,
и слияние прошло бы с двумя решениями под одним номером, если бы не сверка глазами. Столкнулись
не только ADR: статистика завела ядру строку `P20-R27`, а ядро тем же днём заняло
`P20-R27`…`P20-R29` карточками пакета. Счётчики постановок сталкиваются ровно так же и в том
же месте, искать их надо тем же движением (`python3 scripts/find.py --list P20`), — но
разъезжаются они иначе: **номер постановки принадлежит пакету, а не просителю**. Модуль,
которому нужно чужое, кладёт заявку строкой в свой раздел `TODO.md`; номер ей присвоит
владелец пакета, когда примет. Иначе строка реестра остаётся без карточки и роняет
`scripts/check_tasks.py` — что и случилось при этом слиянии.

**Прежняя запись (ветка расчётного ядра).** Следующий свободный — `0176`. Блок
`0173`–`0175` взяло расчётное ядро 26.08.2026
(волна 3: `0173` — накопленное гасится раньше своей текущей статьи и порядок живёт данными,
`0174` — портфельный шов седьмой и свод равен сумме одиночных, `0175` — запись фиксации несёт
редакцию своей формы). Проверено по `main` и всем живым веткам `worktree-*`: максимум `0172`
(`worktree-stats`, статистика, блок `0156`–`0172`).

**Столкновение `0156`–`0168` между отчётностью и статистикой — исполнено 26.08.2026.**
Отчётность влила блок `0156`–`0168` в `main` 26.08.2026 (волна 12), статистика держит те же
номера `0156`–`0172` в `worktree-stats` под другими слагами. По правилу «уступает
закоммитивший позже» перенумеровать пришлось статистике (см. выше); ядро в этом споре
не участвует и свой блок берёт заведомо выше обоих.

**Прежняя запись (ветка отчётности).** Следующий свободный — `0169`. `0168` взят 26.08.2026 волной 12 макета отчётности (пять
незаведённых обязательных бланков — `ФО-31`, `ФО-33`, `ФО-36`, `ФО-37`, `ФО-39`): получатель
объявляется **видом и значением**, видов четыре (роль · подразделение · подразделение объекта ·
должность), и все перечни значений — чужие. Служебная записка внутрь организации остаётся
выпуском: тратит номер и морозит числа, но ключом «лицо» не отбирается. Заданы `ОЧ-62`
(`ФО-33` по норме — трём адресатам), `ОЧ-63` (кто печатает записку ДПО) и `ОЧ-64` (объект
`ФО-33` — предмет залога).

**Прежняя запись (ветка статистики).** Следующий свободный был `0173`; `0180` (тогда `0172`) взят 26.08.2026 при написании раздела ТЗ 18 по
статистике: **форма фильтра вопроса — ДНФ без скобок** (наборы через ИЛИ, сравнения через И,
операнд — разрез или мера строки, правая часть всегда константа). Форму взял у `ADR-0137`
(классификация), но словарь другой и намеренно: там *правило* и *предикат* — они принадлежат
опубликованной редакции и несут пункт нормы, здесь *набор* и *сравнение* — фильтр принадлежит
одному вопросу одного человека и ничего не публикует; слово «условие» в статистике не
употребляется вовсе (занято договорным условием кредита). Закрывает `СС-Д8` моделью 26.08.2026; макет
догнал 28.08.2026 (волна 11, часть первая) — расхождения прототипа с моделью не осталось.

**И прежде того.** Следующий свободный был `0172`. `0176`–`0179` (тогда `0168`–`0171`) взяты 26.08.2026 волной 3 статистики —
**с перенумерацией**: волна выпустила их как `0166`–`0169`, не зная, что отчётность в
`worktree-reports` заняла `0166` и `0167` тем же днём десятью минутами раньше. Столкновение
поймано при слиянии веток: git не конфликтовал (имена файлов разные), совпали только номера.
Двинулась статистика — четыре имени файлов и 141 цитата в семи файлах, содержание решений не
менялось. Заняты: `0168` разрез несёт уровни и корзины (родитель считается, а не складывается
из детей; `statSlice` отдаёт дерево), `0169` обратные ссылки хранит владелец реестра (вторая
хранимая вещь модуля), `0170` `statRows` двоичен — полный список или отказ с числом, сверка
целиком идёт выгрузкой-заданием, `0171` многозначность разреза означает нижележащий объект
(реестр вырос до восьми объектов).

**Прежняя запись.** Следующий свободный — `0168`. `0167` взят 26.08.2026 волной 11 макета отчётности (дефект
`ОЧ-Д1`, форма `ФО-32` «Требование поручителю»): получатель бланка — ссылка на субъект ядра, а
роль получателя объявляет редакция; у шва `reportIssues` появился второй ключ — лицо; охват
по-прежнему считает договоры, но расхождение со знаменателем нормы (п. 9 считает лиц)
печатается обеими величинами и задано вопросом заказчику (`ОЧ-61`).

**До этого.** Следующий свободный — `0167`. `0166` взят 26.08.2026 волной 10 отчётности при постановке
требования взысканию — **статус «предложено»**, потому что решение снимает строку из
справочника чужого модуля: обязательство по п. 9 заведено дважды (строка 38
`DEADLINE_TEMPLATES`), календарность срока признана необходимым, но не достаточным признаком,
а требование переписано в модели соседа (вид меры, мера, экземпляр шаблона — не «фаза»).
Исполняет ветка взыскания.

**И прежде того.** Следующий свободный — `0166`. `0165` взят 26.08.2026 волной 9 макета
отчётности (акт сверки `ФО-34`, Порядок №41 п. 9): полный охват меряется объектами из снимка
на дату состояния, бланк бывает «на дату» и читает `statRows`, отметки «подписан» в
отчётности нет.

**Прежде того.** Следующий свободный — `0165`. `0164` взят 26.08.2026 сверкой каталога форм
с Порядком №41: срок бланка, отсчитываемый от события объекта, принадлежит процессу
(взысканию, сопровождению), а не отчётности; расписание сдачи остаётся календарным.

**Ещё прежде.** Следующий свободный — `0164`. `0163` взят 26.08.2026 разбором «список
отчётов и страница формирования»: форма ведётся справочником, обязательная форма без шаблона
видна как неисполняемая норма.

**Прежде того.** Следующий свободный — `0163`. Блок `0156`–`0161` взяла отчётность 23.08.2026 (волна 1:
отчёт — объявленный состав · выпуск с получателем — вторая заморозка · четвёртый шов
`objectRows` · исчислимость налету · отчётность показывает обязательство, но не поручает ·
наружу уходит pdf). `0162` взят 24.08.2026 разбором «как формируется отчёт»: экран крутится
в границах, объявлённых редакцией, «Вид» — восьмая сущность модуля. Проверено `git ls-tree`
по `main` и всем живым веткам `worktree-*`: в `main` максимум `0155`, по веткам — `0161`
(эта ветка).

**Прежняя запись.** Следующий свободный — `0156`. Блок `0153`–`0155` взял анализ 21.08.2026 (волна 1:
`0153` — заключение хранит снимок основания, `0154` — состав среза объявляет шаблон
отчётности, `0155` — анализ отдаёт зеркало и факт, а в чужие карточки не пишет).
Проверено по `main` и всем живым веткам `worktree-*`: максимум был `0152` (статистика),
по веткам — `0137` (классификация); `0156` оставлен запасом волны и пока свободен.

**Прежняя запись.** Следующий свободный — `0153`. Блок `0145`–`0152` взяла статистика 21.08.2026 (см. ниже);
`0138`–`0144` — оставленный ею запас, дыра расчищена и пользоваться ею можно.
`0137` взяла классификация 21.08.2026 (волна 4:
`0137-pravilo-vmesto-usloviya-dnf-bez-skobok` — правило вместо условия, ДНФ без скобок,
«одно из списка» и строгие сравнения, область как список правил, константа в правой части,
отказ вместо молчания при неотданном показателе). Проверено `git ls-tree` по `main` и всем
живым веткам: максимум был `0136` (`worktree-calculations`).

**Прежняя запись.** Следующий свободный — `0135`. Блок `0132–0134` взяла ветка кредита 17.08.2026 (волна
«плата за неосвоенный остаток»:
`0132-plata-za-neosvoennyy-ostatok-transh-a-ne-statya` — плата живёт **выводимым** траншем,
вид транша расширен до трёх значений;
`0133-usloviya-urovnya-kredita-vtoroy-zhurnal` — ставка платы, дата начала, периодичность,
день платежа и пеня уезжают с транша на кредит;
`0134-plata-statya-procenty-razrez-neset-transh` — плата ложится статьёй «Проценты», разрез
несёт транш). Перед заведением живым `git ls-tree` проверены `main`, `origin/main`, все
ветки `worktree-*` локально и на `origin`, плюс `docs/tz-asubk-2025`,
`feat/credit-procenty-fakticheskoe-telo` и `origin/grafik-versii-kv27-kv28` — максимум
`0131` (`origin/main` и `worktree-curator`,
`0131-priznak-prohodit-geyt-gruppirovki-v-spravochnike`), на остальных `0130` и ниже;
номеров `0132+` нет ни на одной.

**Блок `0145`–`0152` взяла статистика 21.08.2026** (волна 1: `0145` — статистика это
датированные срезы, а не второй калькулятор; `0146` — порядок слоёв `ядро → классификация →
статистика` и переезд реестра показателей; `0147` — атом хранения; `0148` — одна защёлка,
месяц; `0149` — объект как параметр реестра; `0150` — показатель объявляет источник, а не
формулу; `0151` — валюта и потоки; `0152` — три шва и паспорт ответа). Проверено живым
`git ls-tree` по `main`, `origin/main` и всем веткам `worktree-*` плюс `p15-kredit-tasks-red2`:
максимум **`0136`** (`worktree-calculations`, волна расчётного ядра — `0135`/`0136`).

**`0137`–`0144` были пропущены сознательно, а не свободны.** `0137` был занят волной, которую
`git` тогда ещё не видел — она не была закоммичена; остальное оставлено запасом живым волнам.
Правило README («занять блок вперёд дешевле, чем разъезжаться потом») здесь применено на шаг
дальше: пропустить блок дешевле, чем столкнуться с невидимой веткой. **Ставка подтвердилась:**
`0137` действительно оказался занят классификацией и пришёл в `main` тем же днём. Дыра
**расчищена** слиянием 21.08.2026 — `0138`–`0144` свободны и заводить их можно.

**Запись ниже («следующий свободный — `0132`») устарела** и оставлена как история волны
кураторства. Заведение `0132–0134` шло по ней с проверкой живым `git ls-tree`, и проверка
понадобилась: карта, действовавшая до кураторства, называла свободным `0131` — по ней волна
кредита получила бы столкновение, пятое по счёту. Правило «перепроверять живым запросом
перед каждым заведением» сработало снова.

**Следующим свободным был `0132`.** `0131` взяла ветка кураторства 17.08.2026
(`0131-priznak-prohodit-geyt-gruppirovki-v-spravochnike` — гейт допуска признака
распределения из четырёх условий, объявленный источник, двенадцать признаков, группировки
в справочнике признака и со своими редакциями). Перед ней `0130` взяла ветка кредита 15.08.2026
(`0130-sila-osnovaniya-i-versiya-grafika` — ретроспективность как свойство основания: доп.
соглашение действует вперёд, суд и постановление правительства — задним числом; носитель
правки — версия графика). Проверено `git log --all --diff-filter=A -- docs/adr` — по всем
локальным и удалённым рефам максимум `0129`, номеров `0130+` не было ни на одной ветке.
Ниже — запись предыдущей волны.

**Следующим свободным был `0130`.** `0129` взяла та же ветка кредита 15.08.2026
(`0129-kriticheskaya-data-os-detalnogo-rascheta` — ось детального расчёта переехала с позиции
графика на критическую дату; §1 и §3 `0128` перекрыты своим же преемником через сутки).
Проверено `git log --all --diff-filter=A -- docs/adr` — по всем локальным и удалённым рефам
максимум `0128`, номеров `0129+` не было ни на одной ветке. Ниже — запись предыдущей волны.

**Следующим свободным был `0129`.** `0128` взяла ветка кредита 14.08.2026
(`0128-otrezok-nachisleniya-os-detalnogo-rascheta` — отрезок начисления как ось детального
расчёта). Прежняя запись называла свободным `0119`, и она устарела дважды: `0119` с тех пор
влит в `main` (`0119-vyvod-prognoza-lestnica-sostoyaniy-i-okno-poziciy`), а блок `0120–0127`
на момент заведения держала **локальная** ветка `worktree-classify` (волна классификатора,
восемь файлов `0120`…`0127`) — на `origin` её тогда не было, и проверка только по удалённым
веткам её не увидела бы; в `main` она влилась тем же днём (PR #46).
Перед заведением живым `git ls-tree` проверены `main`, `origin/main`, семь локальных и шесть
удалённых `worktree-*` и все четыре ветки задач (`feat/kv29-schedule-button`,
`grafik-versii-kv27-kv28`, `feat/credit-procenty-fakticheskoe-telo`,
`docs/tz-asubk-2025`) — максимумы:
`worktree-classify` — `0127`, `main`/`origin/main` — `0119`, остальные ниже; номеров `0128+`
нет ни на одной. **Локальные ветки проверять обязательно** — блок соседа может ни разу не
уехать на `origin`. Ниже — запись предыдущей волны.

**Следующим свободным был `0128`.** `0126–0127`
(`0126-versiya-obyavlyaet-oblast-poryadok-i-znachenie-po-umolchaniyu` ·
`0127-obyasnenie-nazyvaet-vse-prichiny-reestr-faktov-nezachyoty`) взяла та же ветка
`worktree-classify` тем же днём — то, что вскрыла сборка наброска
`mockups/classification/classification.html`; перед заведением `git ls-tree` по всем
удалённым веткам дал максимум `0119` (`origin/main`, `origin/worktree-credit`), номеров
`0120+` нет ни на одной. `0125`
(`0125-predikat-skalyaren-versiya-publikuetsya-znacheniya-eyo`) взяла та же ветка
`worktree-classify` тем же днём — уточнение конструктора после проверки модели на лестнице
подгруппы; `origin/main` и `origin/worktree-credit` на момент взятия — `0119`. Блок
`0120–0124` взяла ветка `worktree-classify` 14.08.2026
(волна «модуль классификации из ТЗ»:
`0120-klassifikator-konstruktor-predikatov-nad-reestrom` ·
`0121-obekt-klassifikacii-kredit-zaemshchik-svertkoy` ·
`0122-fakt-rukami-znachenie-mashinoy` ·
`0123-kategoriya-zhurnal-fiksaciy-zapiraemyy-periodom` ·
`0124-edinyy-vychislitel-vzyskanie-chitaet-ne-schitaet`); перед заведением живым
`git ls-tree` проверены `origin/main`, все шесть удалённых `worktree-*` и три ветки задач —
максимумы: `origin/main` и `worktree-credit` — `0119`, `worktree-restructure` и
`worktree-curator` — `0118`, `feat/kv29-schedule-button` и `grafik-versii-kv27-kv28` —
`0115`, `feat/credit-procenty-fakticheskoe-telo` — `0105`, `worktree-payments` и
`worktree-vzyskanie` — `0103`, `worktree-org-structure` — `0076`; номеров `0120+` нет ни на
одной. `0119` взяла ветка кредита 14.08.2026
(`0119-vyvod-prognoza-lestnica-sostoyaniy-i-okno-poziciy`) и уже влила в `main` — запись
ниже, объявлявшая `0119` свободным, к этому моменту устарела. Блок `0116–0118` взяла ветка
кураторства 13.08.2026
(`0116-kuratorstvo-izmerenie-a-ne-pole` · `0117-dve-stupeni-pravila-i-folbek` ·
`0118-otstranenie-otdelnoe-izmerenie`); перед заведением живым `git ls-tree` проверены
`origin/main`, все пять удалённых `worktree-*` и три ветки задач (`feat/kv29-schedule-button`,
`grafik-versii-kv27-kv28`, `restructuring-registry-grid`) — максимумы: `main` — `0115`,
`worktree-credit`, `worktree-restructure`, `feat/kv29-schedule-button` и
`grafik-versii-kv27-kv28` — `0115`, `restructuring-registry-grid` — `0114`,
`worktree-payments` и `worktree-vzyskanie` — `0103`, `worktree-org-structure` — `0076`;
номеров `0116+` нет ни на одной. Ниже — запись предыдущей волны.

`0115` взяла ветка кредита 12.08.2026 (волна КВ-26
«транши и график под реструктуризацию»:
`0115-proizvodnyy-transh-ne-rashoduet-summu-dogovora`); перед заведением живым `git ls-tree`
проверены `origin/main` и все пять удалённых `worktree-*` — максимумы: `main` и
`worktree-restructure` — `0114`, `worktree-credit` — `0113`, `worktree-payments` и
`worktree-vzyskanie` — `0103`, `worktree-org-structure` — `0076`; номеров `0115+` нет ни на
одной. `0114` взяла реструктуризация 11.08.2026 (`0114-raschet-zhivet-na-transhe`) и к
12.08.2026 уже влила в `main` — запись ниже, объявлявшая `0114` свободным, к этому моменту
устарела на сутки. Блок `0111–0113` взяла ветка кредита 11.08.2026 (волна
«изменение условий»: `0111-vid-ds-razvodit-marshruty-izmeneniya-usloviy` ·
`0112-korrektirovka-i-otmena-dvumya-mehanizmami` · `0113-retro-rashozhdenie-zapiraet-period`),
`0114` взяла реструктуризация тем же днём (`0114-raschet-zhivet-na-transhe`) — см. «Четвёртое
столкновение» ниже: `0111` планировался веткой реструктуризации, но `origin/main` успел занять
`0111–0113` кредитом раньше. Оба блока теперь влиты в `main`. `0110` взяла реструктуризация
11.08.2026 (`0110-restrukturizaciya-vedet-pyat-statey`); перед заведением живым `git ls-tree`
проверены `main` и все шесть веток локально и на `origin` — номеров `0110+` нет ни на одной.
`0108` занят ветвью кредита в тот же вечер
(`0108-vznos-prognoza-ot-ostatka-osnovnogo-dolga`) — блок `0106–0107` к тому моменту уже
влит реструктуризацией в `main`, и кредит взял следующий за ним, а не «свой» по волне.
`0109` взяла реструктуризация тем же вечером (`0109-stroka-grafika-neset-stati`) — перед
заведением живым `git ls-tree` проверены `origin/main` и все шесть удалённых веток, ни на
одной номеров `0109+` нет; ADR закоммичен сразу, по уроку трёх столкновений.
Ниже — состояние на момент влития предыдущей волны кредита:

Перепроверено 10.08.2026 после влития ветки кредита:
`origin/main` стоит на `0105` (`0104-prognoz-ozhidanie-po-kontraktu` +
`0105-procenty-na-fakticheskoe-telo`, оба — кредиты). Живые невлитые ветки на этот момент:
`worktree-credit` — `0104`, `worktree-vzyskanie` и `worktree-payments` — `0103`,
`worktree-org-structure` — `0076`, `worktree-restructure` — `0107` (**`0106–0107` не заняты
больше нигде**, проверено живым `git ls-tree` по всем удалённым веткам).
**Перепроверять `origin/main` живым запросом перед каждым
заведением**, а не по этой таблице — она за одни сутки 06.08.2026 расходилась с `main`
дважды (23 номера за день, плюс отдельно столкновение на `0079`).

## Четвёртое столкновение: 0111 занят кредитом раньше (разрешено 11.08.2026)

Эта карта (правка 11.08.2026 после `0110`) называла `0111` следующим свободным. К моменту
заведения ADR для задачи 8 (расчёт живёт на транше) `origin/main` уже нёс `0111–0113` за
модулем «Кредиты» (`0111-vid-ds-razvodit-marshruty-izmeneniya-usloviy`,
`0112-korrektirovka-i-otmena-dvumya-mehanizmami`,
`0113-retro-rashozhdenie-zapiraet-period`, все три датированы тем же 11.08.2026 и уже влиты).
Обнаружено перед заведением — по собственному предписанию карты «перепроверять `origin/main`
живым запросом перед каждым заведением», — а не после коммита: столкновения не случилось,
потому что номер не был закоммичен ни на одной стороне до проверки.

Файл заведён сразу под `0114` (проверено живым `git ls-tree` по `origin/main` и всем пяти
удалённым `worktree-*` веткам — свободен только он), без сдвига уже влитых номеров кредита.
Урок прежний, в четвёртый раз: карта репозитория живёт до первого нового коммита в `main` от
любой другой ветки того же дня — перепроверка обязательна, что и было сделано.

## Третье столкновение: 0104 занят дважды (разрешено 10.08.2026)

Номер `0104` заняли ветка кредита (`0104-prognoz-ozhidanie-po-kontraktu.md`, коммит
`8bf5aab6` от 10.08.2026 17:49, **уже влит в `main`**) и `worktree-restructure`
(`0104-penya-terminalnaya-statya.md`, на момент обнаружения **не закоммичен**). Обнаружено в
тот же вечер из ветки реструктуризации при сверке карты перед записью в `STATUS.md`.

Разрешено по правилу старшинства без спора: чужой номер влит, свой ещё не существовал в
истории. **Уступила реструктуризация**: `0104` → `0106`, `0105` → `0107`, слаги и порядок
сохранены; правки ссылок — спека, макет, смоук, `ASUBK-status-razrabotki.md`, `STATUS.md`,
`ADR-0099`. `0105` намеренно не взят: ветка кредита вела волну тем же днём, и следующий её
номер по правилу — как раз `0105`. **Через полчаса так и вышло** — кредит влил
`0105-procenty-na-fakticheskoe-telo.md` в `main` (18:22), то есть шаг через номер спас от
четвёртого столкновения ровно в тот вечер, когда был сделан.

Урок тот же, что и в двух предыдущих случаях, и он теперь стоит трижды: **номер, не
закоммиченный и не отправленный в `origin`, для соседней ветки не существует**. Заводя ADR
в невлитой ветке, коммитить его сразу, а не в конце волны.

## Второе столкновение: 0079 занят дважды (разрешено 06.08.2026)

Номер `0079` заняли одновременно две невлитые ветки: `worktree-vzyskanie`
(`0079-kassacionnaya-vetv.md`, коммит `0a43d562` от 06.08.2026 08:57) и
`worktree-restructure` (`0079-restrukturizaciya-delit-transh-perenos-datirovan.md`, коммит
`ffb413fc` от 05.08.2026 17:38) — накладка растянулась на три номера, `0079–0081`, обе ветки
заняли их независимо друг от друга. Обнаружено 06.08.2026 из ветки `worktree-payments` при
взятии блока под волну платежей — третьей стороной, не участниками; `main` конфликта не
показывает, слаги разные, файлы просто ложатся рядом.

По времени коммита реструктуризация старше почти на 15 часов — по строгой букве правила
старшинства уступать полагалось бы взысканию. Решили иначе: PR взыскания влился в `main`
раньше, чем накладка обнаружилась, а PR реструктуризации на тот момент ещё не был даже открыт
— переносить номера в уже влитой истории дороже и рискованнее, чем сдвинуть невлитую ветку.
**Уступила реструктуризация**: весь блок `0079–0086` → `0092–0099`, слаги и порядок сохранены.
Правки: 8 файлов ADR, макет (`restructuring.html`), спека
(`ASUBK-restrukturizatsiya-logika.md`), статус (`ASUBK-status-razrabotki.md`), карта и тикеты
пересборки (`.scratch/restructuring-pereborka/`).

## Первое столкновение

04.08.2026 номера **0062–0065** заняли обе ветки за один день: взыскание в 18:21
(шаг 2 наряда сверки), платежи в 21:06 (волна 3). Обнаружено 05.08.2026 при слиянии
`main` в `worktree-vzyskanie` — git конфликта не даёт, файлы просто ложатся рядом с
разными слагами.

Разрешено по правилу старшинства: **платежи сдвинуты `0062–0065` → `0073–0076`**, слаги
сохранены. Взыскание оставило номера — они старше на три часа и уже стояли в переданном
пакете `P14` (ТЗ 13 + задачи) и в сверке с редакцией №41.
