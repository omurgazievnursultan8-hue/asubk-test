# Реестр гейтов модуля «Кредиты» — Г-1…Г-22, Г-25, Г-34 (+ Г-23, Г-24 заявлены, Г-26…Г-33 в шапке макета, Г-35/Г-36 сняты)

**Дата сборки:** 2026-07-26
**Артефакт №1** из пакета передачи (см. `docs/superpowers/plans/2026-07-26-kredit-handoff.md`).
**Назначение:** восстановленный свод жёстких блокировок целевой модели. Входит основой
в раздел «Гейты» ТЗ `requirements/tz/05-kredit.html` (артефакт №2).

## Откуда восстановлено

Исходный канон `/home/azamat/Downloads/prompt-kredit-mockup.md` (раздел §6) утрачен —
файла на диске нет, он был вне репозитория. Реестр собран обратной инженерией:

| источник | что дал |
|---|---|
| `mockups/loan-credit/credit.html` — функция `gate()`, строки **2132–2225** | все 22 гейта в поставляемом виде, точные тексты отказов. **Высший авторитет** |
| `docs/superpowers/plans/2026-07-23-credit-module-mockup.md`, Task 3 (стр. 280–420) | Г-1…Г-17 в первоначальной редакции + матрица ролей + карта разделов канона §0…§11 |
| `docs/superpowers/plans/2026-07-25-credit-conditions-tab.md`, Task 6 (стр. 720–870) | Г-18…Г-21, переформулировка Г-9, расширение Г-10 |
| `scripts/inspect/credit-check.mjs` | покрытие смоуком, 43/43 PASS на 2026-07-26 |

Где план и код расходятся — **прав код**. Расхождения перечислены в разделе 6.

**Оговорка о полноте.** Гарантии, что в утраченном каноне не было гейтов сверх
реализованных, нет. Единственный аргумент в пользу полноты — нумерация **Г-1…Г-22
сплошная, без пропусков** (как и Р-1…Р-21). Если канон найдётся, реестр надо сверить.

---

## 1. Сводная таблица

Все ссылки — на `mockups/loan-credit/credit.html`, если не указано иное.
«План» = восстановлено по файлам superpowers, «Код» = только по мокапу.

| Гейт | Действие (`action`) | Правило | Текст отказа (дословно) | Где срабатывает | Источник |
|---|---|---|---|---|---|
| **Г-1** | `saveContractAmount` | Сумма договора не выше одобренной комиссией | `Сумма договора не может превышать одобренную (<сумма>)` | 2136 · мутация 2303 · ре-валидация модалки 4098 | План + Код |
| **Г-2** | `createCredit` | Кредит рождается только из одобренной заявки, и только один | `Кредит создаётся только из одобренной заявки` · `У заявки уже есть кредит` | 2139–2140 · подтверждение перед созданием 2896 | План + Код |
| **Г-3** | `addTranche` | Σ сумм траншей **происхождением «освоение»** ≤ сумма договора | `Σ сумм траншей не может превышать сумму договора (доступно <сумма>)` | 2143 · мутация 2315 | План + Код |
| **Г-4** | `addDisbursement` | Σ освоений транша ≤ сумма транша; **производный транш не осваивается вовсе** | `Σ освоений транша не может превышать сумму транша` · `Производный транш не осваивается: сумма пришла переносом по <ДС>` | 2149 · мутация 2332 | План + Код |
| **Г-5** | `addDisbursement` | Освоение только при живом договоре | `Освоение возможно только при ЖЦ «Зарегистрирован» или «Действует»` | 2150 | План + Код |
| **Г-6** | `addDisbursement` | Провал покрытия закрывает освоение; два штатных разблока | `Провал покрытия: нужны реквизиты решения КМ (Р-8) или waiver комиссии по залогу` | 2151 · разблок КМ 2344 · разблок waiver 2359 | План + Код |
| **Г-7** | `register` | Регистрация требует реквизитов, комплекта документов и пройденного покрытия | `Нужны номер, дата и скан договора` · `Не собран обязательный комплект документов` · `Не пройден гейт покрытия (или исключение Г-6)` | 2188–2191 · `requiredDocsComplete` 2117–2124 | План + Код |
| **Г-8** | `buildSchedule` | Транш происхождением «освоение» — график от факта освоения; транш происхождением «разделение по ДС» — от даты вступления ДС | `График строится только от фактической даты освоения транша` · `У производного транша нет основания ДС — график строить не от чего` | 2195 · мутация 2251 | План + Код |
| **Г-9** | `editConditions` | После «Проекта» условия правятся только зарегистрированным основанием | `Изменение условий — только зарегистрированным основанием: доп. соглашение · решение суда · постановление правительства` | 2198–2199 · блокировка полей вкладки 3189 | План + Код (переформулирован, см. §6.1) |
| **Г-10** | `addAgreement`, `addConditionRecords` | Документ-основание нельзя завести без реквизитов; чужие документы — только ссылкой | `Доп. соглашение требует номер и дату (Г-10)` · `Решение суда и постановление правительства указываются ссылкой на существующий документ (Г-10)` | 2158 · 2165–2168 · мутации 2398, 2412 | План + Код (расширен, см. §6.2) |
| **Г-11** | `savePayment` | Платёж: сумма > 0, дата не в будущем, транш обязателен | `Сумма платежа должна быть > 0` · `Дата платежа не может быть в будущем` · `Транш обязателен` | 2201–2204 · мутация 2375 | План + Код |
| **Г-12** | `writeOff` | Списание только по реквизитам решения | `Списание — только с реквизитами решения (вид · номер · дата)` | 2207 · `closeCredit` 2436 · модалка 4027–4041 | План + Код |
| **Г-13** | `linkPledge` | Привязывается только обеспечение того же ИНН | `Привязка — только договоры того же ИНН заёмщика` | 2210 · мутация 2451 | План + Код |
| **Г-14** | `repay` | «Погашен» — при нуле по всем слоям и без активного взыскания | `«Погашен» — только при нулевом остатке по всем слоям` · `Есть активный процесс взыскания` | 2212–2215 · `closeCredit` 2436 · кнопка 3662 | План + Код |
| **Г-15** | `holdAccrual` | Пауза начисления — только с основанием, документом и датой | `Приостановление — только с основанием, документом и датой начала` | 2218 · мутация 2275 · ре-валидация 4118 | План + Код |
| **Г-16** | `transferDebt` | Смена заёмщика — только по зарегистрированному соглашению | `Заёмщик не меняется без зарегистрированного соглашения` | 2221 · UI-кнопка 3405 (**заглушка**, см. §6.5) | План + Код |
| **Г-17** | — (структурный) | Физического удаления нет нигде; журнал append-only и заморожен | нет текста: гейт реализован отсутствием кнопок и заморозкой объекта | 2129–2130 · 2241 · `pushAudit` 2293 | План + Код |
| **Г-18** | `addConditionRecords` | Условие не вступает в силу раньше даты договора | `Дата вступления не может быть раньше даты договора <дата> (Г-18)` | 2177–2178 | План + Код |
| **Г-19** | `addConditionRecords` | Задним числом — только внешний акт (суд, ПП), не сделка сторон | `Задним числом условия меняют только решение суда и постановление правительства (Г-19)` | 2181 · пояснение 1035 · проверка ДС 1722 | План + Код |
| **Г-20** | `addConditionRecords` | Ретро-условие обязано объяснить, что пересчитывается | `Ретроспективное условие требует примечания: что пересчитывается (Г-20)` | 2183 | План + Код |
| **Г-21** | — (структурный) | Записи условий не удаляются: отмена — новая запись | нет текста: в `window.CR` отсутствуют `remove*`/`delete*` для условий | 2409 · текст в UI 3331 · проверка смоука #36 | План + Код |
| **Г-22** | `addConditionRecords` | Записи условий возможны только на живом договоре | `Условия меняются только при ЖЦ «Зарегистрирован» или «Действует» (Г-22); в «Проекте» условия правятся напрямую, без доп. соглашения` | 2161–2162 · подсказка кнопки 3264 | **Только Код** (планами не покрыт) |
| **Г-25** | `applyDs` | Приём строк из приложения к ДС: Σ колонок строки (основной долг + статьи) = сумме переноса; даты позиций строго возрастают; последняя не выходит за конец срока по ДС; интервалы распоряжений внутри длины графика; перенос ≤ остатка тела транша-источника; деление не возникает, если весь остаток идёт на новые условия и других действующих траншей нет (ADR-0092 §4) | `ИР-2′: Σ колонок графика <сумма> ≠ сумме переноса <сумма>` · `Даты позиций графика не строго возрастают (позиция №N)` · `Перенос <сумма> больше остатка тела транша №N на <дата>` · `Весь остаток идёт на новые условия, других действующих траншей нет — деления не возникает (ADR-0092 § 4)` | гейт 5081 · дверь `restructureApplied` 5289 · смоук #122, #127 | План + Код |
| **Г-34** | `editClassification` | Классификация правится напрямую в любом ЖЦ; вид, линия и источник обязательны и берутся из набора кредитной программы, цель — свободный текст | `<Поле> обязателен` · `«<значение>» вне набора программы <ПРГ-N> для «<поле>»: <набор>` | гейт 5291 · мутация `editClassification` · зеркало `PROGRAM_CLASSIFICATION` 1911 | КВ-30 · упрощён КВ-32 |
| ~~**Г-35**~~ | ~~`addClassificationRecords`~~ | **СНЯТ волной КВ-32** — изменение классификации по документу с датой вступления. Номер не переиспользуется | — | — | КВ-30 · снят КВ-32 |
| ~~**Г-36**~~ | ~~`correctClassification`~~ | **СНЯТ волной КВ-32** — корректировка неверно записанного значения (ADR-0112). Механизм остаётся заявленным за условиями (Г-24). Номер не переиспользуется | — | — | КВ-31 · снят КВ-32 |

### 1.1. Номера заняты вперёд: Г-23 и Г-24 заявлены, но не реализованы

Оба приняты 11.08.2026 разбором логики изменения условий и **в мокапе ещё не стоят**. Номера
занимаются здесь, чтобы следующая волна не выдала их второй раз.

| Гейт | Действие | Правило | Источник решения | Состояние |
|---|---|---|---|---|
| **Г-23** | `addAgreement`, `addConditionRecords` | Формы п. 89 меняются только допсоглашением вида «реструктуризация» — прямое ДС в кредите их не трогает. Исключение: вид «досрочное погашение», `term` вниз | [ADR-0111](../../adr/0111-vid-ds-razvodit-marshruty-izmeneniya-usloviy.md) · задача `P15-R26` | Заявлен |
| **Г-24** | `correctConditionRecord` | Корректировка не меняет основание и дату вступления, требует примечания с пунктом документа и блокируется, если исправляемая дата в закрытом периоде | [ADR-0112](../../adr/0112-korrektirovka-i-otmena-dvumya-mehanizmami.md) · задача `P15-R27` | Заявлен |

Оговорка о сплошной нумерации из шапки после этого читается так: **реализованы** Г-1…Г-22 и Г-25
(волна КВ-26 заняла следующий свободный номер, не трогая заявленных), **заявлены**
Г-23…Г-24. Реализуются — переезжают в таблицу выше со ссылками на строки кода и номерами
сценариев смоука.

### 1.2. Номера, выданные мимо реестра, и перенумерация 12.08.2026

Шапка-комментарий `credit.html` вела **свой** счёт: волна 27.07.2026 выдала там Г-23…Г-29, волна
01.08.2026 — Г-30 (`setPlan`, ADR-0042). Реестр об этом не знал и 11.08.2026 отдал Г-23 и Г-24
другим правилам (ADR-0111, ADR-0112), а КВ-26 заняла Г-25. Три номера стали означать по два
правила сразу.

Разведено волной КВ-26: **реестр — источник номеров**, макет уступает. Перенумерованы только
столкнувшиеся, и только в комментариях (номера этих правил не цитируются нигде, кроме шапки):

| Было в шапке | Стало | Действие |
|---|---|---|
| Г-23 | **Г-31** | `activate` — «Зарегистрирован» → «Действует» только при наличии освоения |
| Г-24 | **Г-32** | `closeDisbursement` — закрытие неосвоенного остатка: остаток, основание, рабочий ЖЦ |
| Г-25 | **Г-33** | `closeTranche` — soft-close с основанием; последний активный транш не закрыть |

Г-26 (`terminate`) · Г-27 (`editDocs`) · Г-28 (`addNote`) · Г-29 (`targetUse`) · Г-30 (`setPlan`)
коллизии не имеют и оставлены как есть. **В таблицу §1 они не развёрнуты** — правила, тексты
отказов и строки кода для Г-26…Г-33 держит пока только шапка макета; развернуть их в реестр —
отдельная работа.

Волна **КВ-30** (13.08.2026) заняла отсюда **Г-34** и **Г-35** — классификация кредита получила
дверь. Волна **КВ-31** (13.08.2026) заняла **Г-36**: изменение по документу и корректировка
разведены по ADR-0112. Волна **КВ-32** (13.08.2026) **сняла Г-35 и Г-36**: у классификации
осталась одна дверь — прямая правка Г-34. Снятые номера **не переиспользуются** — иначе
ссылка «Г-35» из старого текста указала бы на чужое правило. **Следующий свободный номер — Г-37.**

---

## 2. Развёрнуто по каждому гейту

### Г-1 · Сумма договора ≤ одобренной
Сравнивается вводимое значение с `credit.approvedAmount` — суммой, одобренной кредитной
комиссией и пришедшей из заявки (Р-18: происхождение кредита). Равенство проходит,
превышение блокируется. Проверяется дважды: в `saveContractAmount` и повторно в
обработчике модалки перед мутацией (4098) — чтобы значение нельзя было провести в обход
формы. Одобренная сумма в карточке — только для чтения.

### Г-2 · Кредит только из одобренной заявки
Две причины в одном действии: заявка не одобрена (`approved !== true`) и заявка уже
породила кредит (`application.creditId` заполнен). Гейт вызывается до создания записи
(2896), поэтому «Создать кредит» открывает подборщик, где показаны только одобренные
заявки — двойная защита: фильтр подборщика плюс гейт.

### Г-3 · Σ траншей ≤ сумма договора
Опирается на производную `derive(credit).allocatable` — свободный остаток договора.
В сообщение подставляется доступная сумма, чтобы пользователю не приходилось считать
самому. Транш — единица учёта (Р-1), поэтому гейт защищает главный инвариант агрегата.

**Волна КВ-26 (ADR-0115):** `allocated`/`allocatable` считаются только по траншам
происхождением «освоение». Производный транш рождён **переносом** уже розданного тела, а
не распределением суммы договора; складывать его в `allocated` значило бы объявлять
«Σ траншей > суммы договора» ровно на величину каждой реструктуризации. Отвергнутая
альтернатива — вычитать сумму переноса из `allocated`: арифметически сходится, но плитка
перестаёт отвечать на свой вопрос «сколько договора ещё не роздано».

### Г-4 · Σ освоений ≤ сумма транша
Считает уже освоенное (`disbursedSum(t)`) и прибавляет новое. Работает в паре с Г-5 и Г-6:
все три проверяются одним вызовом `gate('addDisbursement')` (2332), пользователь получает
сразу полный список причин, а не по одной за раз. Дополнительно там же — не-канонная
защита `Транш не найден` от неизвестного `trancheNo` (см. §6.7).

**Волна КВ-26:** к производному траншу гейт неприменим и заменяется отказом — деньги по
нему выданы на родителе и второй раз выданы не будут. Причина называет ДС, по которому
пришёл перенос, а не отсылает к абстрактному правилу.

### Г-5 · Освоение только при «Зарегистрирован» / «Действует»
Белый список ЖЦ. Отсекает и «Проект» (договор ещё не подписан), и терминалы
(«Погашен», «Списан», «Закрыт»). В UI терминальные состояния перехватываются раньше —
общей проверкой `isTerminal` (2948, 2958), которая даёт свою подсказку.

### Г-6 · Провал покрытия
Самый нагруженный гейт наравне с Г-1 (по 21 упоминанию в файле). Читает
`derive(credit).coverage.ok` — индекс покрытия против переменного порога
(120 % для ликвидного; 150 % для движимого неликвидного при доле ликвида ≥ 80 %).
База индекса — **залоговая стоимость, не оценочная**. Поручительство в индекс не входит
(решение по П2, вариант «б»), банковская гарантия входит.

Два штатных разблока, оба оставляют след:
1. **Решение КМ** (Р-8) — `credit.kmDecision` с реквизитами, вводится через `setKmDecision`.
2. **Waiver комиссии по залогу** — `credit.mirror.pledgeWaiver`; сам waiver под своим
   мини-гейтом: `Waiver требует обоснования (протокол комиссии по залогу)` (2155).

Тот же дизъюнкт «покрытие ИЛИ КМ ИЛИ waiver» повторён в Г-7 — регистрация закрыта теми же
условиями, что и освоение.

### Г-7 · Регистрация договора
Три независимые причины: реквизиты (`num`, `date`, `reg.scan`), обязательный комплект
документов (`requiredDocsComplete`, 2117–2124) и покрытие. Именно с регистрацией связан
переход ЖЦ, открывающий освоение по Г-5.
**Дыра:** мутации `register` и кнопки регистрации в мокапе нет — гейт вызывается только
из смоука. Подробнее §6.4.

### Г-8 · График от факта освоения — либо от даты вступления ДС
Блокирует построение графика, пока у транша нет ни одного освоения. Это ключевое отличие
целевой модели от легаси, где график набивался руками до всякого движения денег
(находка легаси-прогона). Повторное построение делает новую версию, прежняя остаётся
архивной (смоук #11).

**Волна КВ-26:** веток две, и вторая — следствие ADR-0092 §1, а не новое правило.
У транша происхождением «разделение по ДС» освоения нет и не будет; его график строится
**от даты вступления ДС**, а отказ у него другой — «нет основания ДС, график строить не
от чего». Ждать освоения на таком транше значило бы оставить его без контракта навсегда.
Перестроить версию, рождённую ДС, движком можно только с основанием под собой
(ретро-запись условия либо заявление на досрочку) — иначе кнопка стирала бы подписанное.

### Г-9 · Условия правятся только основанием
`editConditions` разрешён исключительно при ЖЦ «Проект». Во всех остальных состояниях
прямой ввод закрыт, и вкладка «Условия» переходит в режим «только через запись с
основанием» (блокировка полей — 3189). Формулировка была изменена на шаге 15: раньше
единственным основанием считалось доп. соглашение, теперь их три — ДС, решение суда,
постановление правительства.

### Г-10 · Реквизиты документа-основания
Живёт в двух действиях. В `addAgreement` — классический вид: ДС без номера или даты не
регистрируется. В `addConditionRecords` — расширенный: ДС требует номер и дату
(кредит владеет документом), а суд и постановление правительства **не заводятся в кредите
вовсе** — только ссылка `basis.ref` на существующий документ зеркала (Д-4, Р-7/Р-16).
Разделение задаётся реестром `BASIS_KINDS` через флаг `owned`.

### Г-11 · Валидность платежа
Три причины: сумма ≤ 0, дата в будущем, не указан транш. Платёж в кредите — зеркало
(Р-5): форма ручного ввода помечает запись осями «Ручной ввод» + «Ожидает ЦК», а платежи
со статусом «Подтверждён ЦК» не редактируются из кредита ни при какой роли (смоук #16).

### Г-12 · Списание только по решению
Требует все три реквизита документа: вид, номер, дата. Вызывается через `closeCredit`
с `reason = 'Списан'` (2436). Списание — терминальное состояние (Р-20), обратного хода нет.

### Г-13 · Обеспечение того же ИНН
Сравнивает `pledge.pledgorInn` с `credit.borrower.inn`. Подборщик показывает реестр
залоговых договоров как зеркало (Р-7): создавать залоговый договор из кредита нельзя
в принципе — во вкладке «Обеспечение» такой кнопки нет ни при какой роли (смоук #18).

### Г-14 · «Погашен» только при нуле
Две причины: ненулевой остаток хотя бы по одному слою (основной долг, проценты, пеня;
порог 0,005 — защита от копеечных хвостов округления) и наличие активного процесса
взыскания в зеркале. Прямой ответ на находку легаси-прогона E2E-13/14 — «осиротевшая
копейка» при ручном закрытии без проверки нуля.

### Г-15 · Пауза начисления
Требует три поля: основание, документ, дата начала. Приостановление даёт интервал, внутри
которого проценты не начисляются; интервал виден на вкладке «Расчёты» и оставляет запись
в журнале (смоук #24, #27b).

### Г-16 · Перевод долга
Смена заёмщика возможна только при зарегистрированном соглашении
(`ctx.agreement.registered`). Гейт написан, но действие в мокапе не доведено:
кнопка (3405) вызывает `CR.stubAction('Перевод долга')`, смоук-теста нет. См. §6.5.

### Г-17 · Нет физического удаления (структурный)
Не участвует в `gate()` вовсе. Реализован тремя способами сразу: в карточке нет ни одной
кнопки «Удалить» (проверяется структурно — смоук #28); все мутации пишут append-only
запись в `credit.audit`; сама запись журнала **заморожена** (`Object.freeze` в `pushAudit`,
2293), то есть её нельзя изменить и программно. Связан с Р-4 и Р-20.

### Г-18 · Не раньше даты договора
Первая из четырёх проверок даты вступления условия. Сравнение `effectiveFrom` с
`credit.date`. В сообщение подставляется дата договора — пользователь видит границу.

### Г-19 · Задним числом — только внешний акт
Если `effectiveFrom` раньше `TODAY`, проверяется флаг `retro` у вида основания:
разрешены `court` и `govAct`, запрещены `agreement` и `application`. Логика прямая:
внешний акт задним числом штатен (суд так и решает), сделка сторон задним числом — нет
(пояснение в коде, 1035). Отдельно проверяется, что и само ДС не ретроспективно (1722).

### Г-20 · Ретро требует примечания
Дополняет Г-19: даже разрешённое ретро-условие не сохраняется без непустого `note`.
Смысл примечания — зафиксировать, что именно подлежит пересчёту, поскольку
**автопересчёта графика нет** (Д-5): вместо него на вкладке «Условия» загорается красная
плашка, а на «Расчётах» — бейдж «график построен по прежним условиям» с переходом на
перегенерацию. Плашка гаснет, когда `generateSchedule` проштампует новую версию.

### Г-21 · Записи условий не удаляются (структурный)
Как и Г-17, вне `gate()`. Отмена условия оформляется новой записью с прежним значением,
а не удалением старой. Проверяется наличием: в экспорте `window.CR` не должно быть ни
одной функции, подходящей под `/^(remove|delete).*[Cc]ondition/` — это часть смоука #36.

### Г-22 · Условия только на живом договоре
Белый список ЖЦ для `addConditionRecords`: «Зарегистрирован» или «Действует». Отсекает
два случая с разным смыслом. В «Проекте» условия правятся напрямую через `editConditions`
(Г-9) — доп. соглашение к неподписанному договору бессмысленно; текст отказа прямо
подсказывает этот путь. В терминалах («Закрыт» и прочие) правка невозможна вообще; в UI
до Г-22 дело обычно не доходит — терминальная проверка перехватывает кнопку раньше и даёт
свою подсказку, но логика всё равно держит границу (смоук #39 проверяет именно это,
включая то, что запись в модель не попала).

### Валидации формы без номера гейта
В `addConditionRecords` есть ещё шесть проверок, не привязанных к номеру: отсутствие
основания, пустой набор записей, неизвестный параметр (вне реестра `PARAM_KEYS` из
10 ключей), не выбран ни один транш, несуществующий номер транша, пустая дата вступления.
Формально это не гейты, но в ТЗ их надо перечислить — разработчик обязан их воспроизвести.

---

### Г-25 · Приём строк из приложения к ДС
Единственный гейт, который проверяет не действие человека, а **форму пришедшего
документа**. Реструктуризация оформляется в своём модуле и приходит зеркалом (Р-16);
кредит её применяет одной дверью `restructureApplied` (ADR-0096), и Г-25 стоит до первой
записи — полуприменённого ДС не бывает.

Шесть причин показываются **вместе**, как у Г-6/Г-7: разбирать приложение по одной ошибке
за раз значит гонять человека в модуль реструктуризации шесть раз.

Почему кредит валидирует, а не пересчитывает: приложение — часть подписанного соглашения,
и пересчёт разошёлся бы с бумагой. Календари у модулей к тому же разные (у
реструктуризации месяц = 30 дней, у кредита — реальный календарь с `payDay`/`freq`),
поэтому строки и **приходят**, а не сверяются пересчётом.

Отдельно стоит проверка ADR-0092 §4: если весь остаток идёт на новые условия и других
действующих траншей нет, деления **не возникает** — такое ДС оформляется записями условий
и новой версией графика на существующем транше. Причина так и сформулирована, вместе
с указанием, чем ДС оформить вместо деления.

### Г-34 · Классификация правится напрямую, в любом ЖЦ
Единственная дверь классификации. Гейт держит **набор значений**: вид, линия и источник берутся
из справочного набора кредитной программы (`PROGRAM_CLASSIFICATION` — зеркало, третий экземпляр
идиомы `PROGRAM_DOCS`), и «свой» вариант тут означал бы, что кредит выдан не по той программе,
за которую себя выдаёт. **Цель не сверяется** — её формулирует договор, классификатора целей у
модуля нет. Причины показываются вместе, как у Г-6/Г-7, и отказ по набору называет и программу,
и сам набор: иначе пользователю нечем исправиться. Критерия по ЖЦ у гейта нет — как у
`editContractRequisites`, которому классификация теперь ровня.

**Упрощён волной КВ-32** (решение владельца). Первые две редакции разводили правку по режимам:
КВ-30 закрывала прямую дверь после регистрации и требовала доп. соглашение (Г-35), КВ-31
добавляла к ней корректировку по ADR-0112 (Г-36). Обе двери сняты, вместе со слоем записей
`classificationRecords` и срезом `classificationAt`. Основание: классификация — **паспортный
разряд договора, а не договорённость сторон**. Она не двигает ни начислений, ни графика, ни
остатка, поэтому «каким было значение на 01.05» никому не нужно, а вопрос «изменение это или
исправление» задавался пользователю там, где от ответа ничего не зависело. История правок
осталась там же, где у № и даты договора, — в журнале (`editClassification`, before/after).

**Что этим потеряно** (осознанно): срез классификации на дату · подпись основания под полем ·
отдельное надзорное действие журнала для корректировок. Если предмет когда-нибудь начнёт влиять
на деньги — восстанавливать придётся новым номером, Г-35 и Г-36 отозваны навсегда.

### ~~Г-35~~ · снят волной КВ-32
Изменение классификации по документу с датой вступления: вид основания из `BASIS_KINDS`,
Г-10/Г-18/Г-19-паттерны, запись на изменённый параметр. Снят вместе со слоем записей — см. Г-34.
Номер не переиспользуется.

### ~~Г-36~~ · снят волной КВ-32
Корректировка неверно записанного значения по
[ADR-0112](../../adr/0112-korrektirovka-i-otmena-dvumya-mehanizmami.md): ни основания, ни даты
вступления, обязательное примечание, отдельное действие журнала `classificationFix`. Снят
вместе с механизмом изменения: без слоя записей исправлять нечего — прямая правка перезаписывает
значение и пишет before/after в журнал. **Сам механизм ADR-0112 из модуля не уходит** — он
по-прежнему заявлен за условиями (Г-24), где предмет действительно двигает начисления и молча
переписать значение нельзя. Номер не переиспользуется.

## 3. Матрица ролей

Из `ROLE_ACTIONS`, `credit.html:2228–2235`. `canRole(role, action)` вызывается до `gate()`:
роль решает, показывать ли кнопку активной, гейт — можно ли выполнить действие.

| Действие | Кредитный специалист | Куратор | Бухгалтер | Начальник отдела | Наблюдатель |
|---|:--:|:--:|:--:|:--:|:--:|
| `createCredit` | ✔ | | | ✔ | |
| `saveContractAmount` | ✔ | | | ✔ | |
| `register` | ✔ | | | ✔ | |
| `addTranche` | ✔ | | | ✔ | |
| `addDisbursement` | ✔ | | | ✔ | |
| `buildSchedule` | ✔ | | | ✔ | |
| `applyDs` | ✔ | | | ✔ | |
| `addAgreement` | ✔ | | | ✔ | |
| `addConditionRecords` | ✔ | | | ✔ | |
| `editConditions` | ✔ | | | ✔ | |
| `editClassification` | ✔ | | | ✔ | |
| `editDocs` | ✔ | | | ✔ | |
| `targetUse` | | ✔ | | ✔ | |
| `inspection` | | ✔ | | ✔ | |
| `holdAccrual` | | ✔ | | ✔ | |
| `transferDebt` | | ✔ | | ✔ | |
| `linkPledge` | | ✔ | | ✔ | |
| `savePayment` | | | ✔ | ✔ | |
| `waiver` | | | | ✔ | |
| `setKmDecision` | | | | ✔ | |
| `writeOff` | | | | ✔ | |
| `repay` | | | | ✔ | |

«Наблюдатель» — пустое множество, все действия закрыты (смоук #26). Отказ по роли даёт
подсказку `Роль «<роль>» не имеет права на это действие` (3262).

---

## 4. Покрытие смоуком

`scripts/inspect/credit-check.mjs`, 43/43 PASS на 2026-07-26; **133/133 PASS на 2026-08-12**
(волна КВ-26).

| Гейт | Сценарии |
|---|---|
| Г-1 | #1, #2 (превышение блокируется, равенство проходит) |
| Г-2 | #3 |
| Г-3 | #4 (и блок, и проход в пределах), #113 (регресс: без реструктуризации цифры прежние), #128 (производные не расходуют сумму договора) |
| Г-4 | #5, #120 (отказ на производном транше называет ДС) |
| Г-5 | #6 |
| Г-6 | #7 (разблок КМ), #10 (покрытие 84 % → блок → реквизиты КМ → разрешено), #8b (waiver без обоснования) |
| Г-7 | #9 |
| Г-8 | #8, #119 (две ветки: производный строит от даты ДС, пустой отказан) |
| Г-9 | #12 |
| Г-10 | #13 (ДС), #36 (суд без ссылки) |
| Г-11 | #15 |
| Г-12 | #23 |
| Г-13 | #17 |
| Г-14 | #22 |
| Г-15 | #24 |
| **Г-16** | **нет** |
| Г-17 | #28 (структурная проверка отсутствия кнопок «Удалить»), #27/#27b (заморозка журнала) |
| Г-18 | #36 |
| Г-19 | #36 (и запрет ретро-ДС, и разрешение ретро-суда) |
| Г-20 | #36 |
| Г-21 | #36 (в `CR` нет `remove*`/`delete*` для условий) |
| Г-22 | #37 («Проект»), #39 («Закрыт» + запись не попала в модель) |
| Г-25 | #122 (все причины разом + атомарность), #127 (ADR-0092 §4 и перенос сверх остатка) |
| Г-34 | #139 (правка после регистрации проходит), #140 (та же дверь в «Проекте»), #141 (значение вне набора программы, отказ называет набор), #142 (пустое справочное значение / свободная цель), #143 (журнал: одна запись before/after), #144 (затравка КР-59 в наборе) |

**Без смоук-теста остаётся один гейт — Г-16.** Причина та же, что и дыра §6.5:
действие не доведено до мутации.

Тесты #37 и #39 пинят формулировку Г-22 целиком, а не фрагмент — намеренно, чтобы тест
не проходил на старом тексте. При правке текста отказа тест придётся править синхронно.

---

## 5. Карта разделов утраченного канона

Восстановлена из `2026-07-23-credit-module-mockup.md:695–707`. Нужна для замены ссылок
вида «§6» на якоря внутри ТЗ (артефакт №2).

| § | содержание | куда лёг в мокапе |
|---|---|---|
| §0 | абсолюты: без заглушек, единые `derive`/`gate`, только память, append-only журнал, дисциплина зеркал | глобальные ограничения |
| §0.2 | — | (упоминается в коде) |
| §0.3 | нет молчаливых отказов: `disabled` ⇒ подсказка + тост | `disabledBtn` 2941 |
| §0.5 | журнал append-only | `pushAudit` 2293 |
| §1 | стек и визуал | токены, оболочка |
| §2 | решения Р-1…Р-20 (в мокапе доросли до Р-21) | шапка файла |
| §3 | модель CREDIT / TRANCHE | `seedDb` |
| §4 | производные | `derive` |
| §5.1 | колонки реестра (Р-19) | реестр |
| §5.2 | шапка карточки | карточка |
| §5.3 | ровно 8 вкладок, пустых нет | карточка |
| §6 | **гейты Г-1…Г-17** (доросли до Г-22) | `gate` 2132 |
| §7 | роли | `ROLE_ACTIONS` 2228 |
| §8 | демо-цепочки К-1…К-6 | демо-данные |
| §9 | смоук 1–28 (дорос до 43) | `credit-check.mjs` |
| §10 | открытые вопросы | шапка файла |
| §11 | «не делать» — анти-паттерны легаси | глобальные ограничения |

---

## 6. Расхождения и дыры

### 6.1 Г-9 переформулирован (план → код)
План: `Изменение условий — только доп. соглашением`.
Код: `Изменение условий — только зарегистрированным основанием: доп. соглашение ·
решение суда · постановление правительства`.
Изменено осознанно на шаге 15 (шапка мокапа, строки 194–195). **В ТЗ идёт редакция кода.**

### 6.2 Г-10 расширен
В плане жил только внутри `addAgreement`. В коде добавлена вторая площадка —
`addConditionRecords` — и второй текст отказа про ссылку на существующий документ для
суда и ПП. **В ТЗ описывать обе площадки.**

### 6.3 Матрица ролей выросла
Против плана в коде добавлено: `addConditionRecords` — кредитному специалисту и
начальнику отдела; `repay` — начальнику отдела. В плане `repay` не был закреплён ни за
кем, то есть закрытие по погашению было недоступно всем ролям — явная недоделка плана,
исправленная в коде.

### 6.4 Г-7 — гейт без действия 🟠
`gate(credit,'register')` определён (2188–2191), роль `register` есть в матрице, смоук #9
его проверяет — но **мутации `register` в коде нет и кнопки регистрации в карточке нет**.
Гейт вызывается только из смоука. ЖЦ в демо-данных проставлен сидированием.
Для разработчика это означает: правило описано, а сценарий регистрации договора надо
проектировать с нуля. Кандидат в отдельную задачу P15.

### 6.5 Г-16 — гейт при заглушке 🟠
Та же картина, но хуже: мутации нет, смоук-теста нет, а кнопка есть и ведёт в заглушку
`CR.stubAction('Перевод долга')` (3405). Перевод долга (Р-14) в мокапе присутствует как
свёрнутый блок на вкладке «Договор», но не как работающий сценарий.
Кандидат в отдельную задачу P15.

### 6.6 Г-22 подтверждается только кодом
Ни один план superpowers его не описывает — он появился на шаге 15 и зафиксирован лишь
в шапке мокапа (строки 197–200) и в самом коде. Восстановлен полностью (текст, обе
перекрытые ветки ЖЦ, подсказка кнопки, два смоук-теста), но независимого подтверждения
у него нет.

### 6.7 Проверки сверх канона
- `Транш не найден` в `addDisbursement` (2147) — защита от неизвестного `trancheNo`;
  без неё гейт проходил, а мутация падала на `push` в `undefined`. Номера гейта не имеет.
- Шесть валидаций формы в `addConditionRecords` (перечислены в конце раздела 2).

### 6.8 Устаревший комментарий в коде
Заголовок секции 4 (строка 2127) гласит `единая точка блокировок Г-1…Г-16`, хотя в
`switch` живут и Г-18…Г-20, и Г-22. Комментарий отстал на два шага слияния.
Мелочь, но при передаче введёт в заблуждение. `credit.html` в рамках этой работы не
правится — фиксируется здесь и уходит в задачу P15 вместе с прочими хвостами.

### 6.9 Что восстановить не удалось
- **Первоначальные формулировки канона.** Реестр отражает поставляемое поведение мокапа,
  а не исходный текст §6. Где план и код разошлись, обе редакции показаны (§6.1, §6.2),
  но что стояло в самом каноне — неизвестно.
- **Гейты, задуманные, но не реализованные.** Если такие были, следов не осталось.
  Косвенное свидетельство против их существования — сплошная нумерация.
- **Содержание §0.2.** Ссылка в коде есть, расшифровки нет ни в одном источнике.
