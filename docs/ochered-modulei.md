# Очередь модулей: порядок проработки и порядок выдачи

> **Принято 26.08.2026.** Предмет — шесть модулей двух подсистем ТЗ: расчёты, классификация,
> анализ, статистика (подсистема обработки информации) и отчётность, шаблоны распечатки
> (подсистема предоставления информации).
>
> **Что этот документ решает:** в каком порядке доводить проработку модулей и в каком порядке
> отдавать постановки разработке. **Чего не решает:** содержание модулей — оно живёт в канонах
> логики рядом с макетами, в `requirements/tz/` и в `docs/tasks/`.
>
> Порядок исполнения слоёв (кто кого читает в рантайме) задан `ADR-0146` и здесь **не
> переигрывается** — он входит сюда как исходное данное.

---

## 1. Как шесть модулей легли на ТЗ

Официальное ТЗ (`ТЗ АСУБК-3.docx`, вне репозитория — см. `references.md`) описывает их в двух
подсистемах.

| ТЗ | у нас | расхождение |
|---|---|---|
| **Подсистема обработки информации** | | |
| Модуль расчетов | Расчётное ядро (`mockups/calc-core/`) | отдельного «модуля расчётов» в приложении нет: движок физически живёт в `credit.html` между маркерами `<<< mirror:calc-core >>>` и зеркалится в `restructuring.html` |
| Модуль классификации | Классификация (`mockups/classification/`) | нет |
| Модуль анализа | Анализ (`mockups/analysis/`) | нет |
| Модуль статистики | Статистика (`mockups/statistics/`) | нет |
| **Подсистема предоставления информации** | | |
| Модуль отчетности | Отчётность (`mockups/reports/`) | нет |
| Модуль управления шаблонами распечатки | **свёрнут в «бланк»** — вид шаблона внутри отчётности (`ADR-0161`, канон отчётности §2.2 и §8) | **есть, не оформлено письменно** |
| Модуль настройки интерфейса (меню · рабочий стол · дашборд) | **отсутствует полностью** | **есть**: ни макета, ни канона, ни ADR, ни строки в `TODO.md` |

Два расхождения требуют разной реакции и разобраны в решениях 3 и 10.

---

## 2. Состояние на 29.08.2026

| модуль | макет | смоук | канон | ТЗ | постановка | где лежит |
|---|---|---|---|---|---|---|
| Расчётное ядро | 6525 стр. | 2196 числовых сверок + 66 + 167 проверок | 12 разд., `ИЯ-1`…`ИЯ-22` (v6) | ✅ `tz/18` (ред. 4) | ✅ `P20`, 32 карточки, 9 этапов | `worktree-calculations` влит в `main` 28.08.2026, волны 3–6 закрыты по бумаге. **Волна 7 закрыта и влита в `main`** 28.08.2026: проекция с прогоном, `calcPortfolio`, конверт с семью статьями, восьмой шов `objectRows`, разнесение платежа в четырёх координатах (`РЯ-Д5`) и сторож чисел проекции (`РЯ-Д6`) — 66 проверок в 9 группах. Сторож поймал **два дефекта движка**: цена дня вперёд не замирала у закрытого кредита и списание снимало названное решением, а не числившееся (`КВ-63`); оба исправлены в каноне и зеркале. |
| Классификация | 2669 стр. | 83/83 | 13 разд., `ИК-1`…`ИК-22` | ✅ `tz/17` | ✅ `P19`, 23 карточки, 7 этапов | `main` |
| Статистика | 4677 стр. | 131/131 | 16 разд., `ИС-1`…`ИС-36` | ✅ `tz/19` (ред. 8) | ✅ `P22`, 22 карточки, 8 этапов, ред. 2 | `worktree-stats`, волны 5–14 закрыты: **десять** объектов, полный состав показателей кредита, заёмщика, залога, погашения и меры взыскания, дело и требование взыскания, дедуп-ключ (`ADR-0198`…`ADR-0200`), фильтр наборами (`ADR-0180`), погашение разделено на «Платёж» и «Поступление» (`СС-105`), тонкий объект доспрашивается или снимается (`ADR-0201`: «Задание кураторства» снято строкой реестра, у программы нет ни одной меры строки — и это законно), **«Залоговый договор» заведён десятым** (`СС-119`: разрез `d-zdate` пять волн лежал сиротой без объекта, а параметр ТЗ `#4` числился исполненным; сторож `#128` закрывает класс), и **спрашивается дата прогона, а не «сегодня»** (`ADR-0202`, `ИС-36`: список дат вопроса есть журнал прогонов, за последним прогоном — отказ с дорогой к реестру владельца, `ИС-12` сужена до дыр внутри истории). Открыты `СС-Д4` (чинится в ядре) и `СС-Д11` (охват ролей, с волны 13 накрывает три объекта); расхождений макета с моделью не осталось |
| Отчётность | 4466 стр. | 154/154 | 19 разд., `ИО-1`…`ИО-30` | ✅ `tz/20` (ред. 1) | — | `worktree-reports`, волны 12–14 закрыты. **Волна 14 от 29.08.2026** — раздел ТЗ `requirements/tz/20-otchetnost.html` (19 разд.): каталог `ФО-01`…`ФО-41` целиком, «долг соседей» шестнадцатью строками со «состоянием», «чего в модуле нет» с разведёнными причинами восьмёрки без владельца, открытые вопросы с ценой другого ответа. Находка сборки: у `ФО-38` (исковое заявление) адресат — судебные органы, ни один из четырёх видов получателя не подходит; закрывается, вероятно, ответом по `ОЧ-62`. Требований волна не добавила. **Волна 13 от 29.08.2026** — сверка шестнадцати требований с выданными канонами ядра и статистики: три контракта другие, переписана наша сторона (`ОЧ-65`…`ОЧ-67`), заведён `ИО-30`, у каталога появился столбец «данные», у таблицы требований — «состояние». Модуль **разблокирован**, раздел ТЗ можно писать. Открыты `ОЧ-68`…`ОЧ-70` — сужают область (8 форм из 40), не блокируют |
| Анализ | 1849 стр. | 44/44 | 15 разд., `ИА-1`…`ИА-18` (v2) | — | ✅ `P21`, 22 карточки, 7 этапов — **раздел финанализа**, редакция 1 | `worktree-analysis`, волна 5 закрыта 28.08.2026: постановка `docs/tasks/p21-analiz-tasks.html`. Раздел обзоров в редакцию 1 не вошёл (`ADR-0154`: нет модуля отчётности), придёт редакцией 2. Открыт `АН-Д6` |
| Дашборд | — | — | — | — | — | не начат |

**Открытые дефекты макетов:** `РЯ-Д1`, `РЯ-Д2`, `РЯ-Д7` (ядро) · `СС-Д4`, `СС-Д10` и `СС-Д11` (статистика;
`СС-Д5` и `СС-Д7` закрыты, `СС-Д1`/`Д2`/`Д3`/`Д6` собраны в макете частью второй волны 3,
`СС-Д9` закрыт волной 10, `СС-Д8` закрыт **с обоих концов** — моделью волной 4 (`ADR-0180`) и
макетом волной 11 частью первой 28.08.2026, `СС-Д12`/`СС-Д13`/`СС-Д14` заведены и закрыты
внутри волны 12).
У отчётности, классификации и анализа открытых нет: `ОЧ-Д1` закрыт волной 11.

`РЯ-Д1` (версия графика резолвится «на сегодня») и `РЯ-Д2` (курс не датирован) — дефекты
ядра, чьё последствие лежит в **статистике**: исторические срезы неточны, пока они не
починены.

---

## 3. Отправная точка разговора: порядок уже был нарушен

Единственная выданная разработке постановка — классификация `P19`. Она объявляет две внешние
зависимости, ни одна из которых не обеспечена:

- `P19-R1` → `extDeps`: «модуль статистики ведёт реестр показателей: заведение, домен, снятие,
  ответственность за состав». Постановка требует **читать реестр, который никому не поручено
  построить**.
- Показатели предикатов приходят от ядра, у которого нет ни ТЗ, ни постановки.

Там же, `traps` карточки `P19-R1`: «Реестр в макете правится изнутри классификации. Макет
писался до переезда реестра к статистике». То же самое случилось второй раз с другой стороны:
отчётность, не дождавшись статистики, сформулировала ей три требования (§5).

Вывод, из которого выросли решения 6 и 8: **потребитель, выданный раньше производителя,
заводит контракт у себя.**

---

## 4. Решения

**1. Предмет очереди — проработка и выдача, не реализация.** Порядок написания кода команда
определяет сама; мы управляем тем, на что у неё есть постановка.

**2. Критерий упорядочивания — зависимость по данным**, тай-брейк — неизвестность.
Дефицитный ресурс здесь не время разработчиков, а проработка. Дороже всего стоит не «поздно
начали», а «переиздали постановку целиком, потому что снизу поменялся контракт».

**3. «Распечатка» — не модуль очереди.** Бланк остаётся видом шаблона внутри отчётности
(`ADR-0161`). Расхождение с ТЗ реальное, и на приёмке его придётся объяснять — поэтому
**пишется отдельное письменное обоснование свёртки**. Очередь от этого не меняется.

**4. Расчётное ядро — отдельный пункт очереди** со своим разделом ТЗ и своей постановкой
(`P20-R*`, имя `docs/tasks/p20-raschetnoe-yadro-tasks.html` зарезервировано). Три довода:
ядро отдаёт пять швов и его читают все четыре остальных модуля — это определение общего
контракта, а общий контракт не живёт внутри одного из потребителей (`ADR-0146` §4);
`ADR-0135` перекрывает `ADR-0096` §2, а вещь, правящая чужой ADR, не является его частью;
`РЯ-Д1`/`РЯ-Д2` — дефекты ядра с последствием в статистике, и чинить их в бэклоге кредита
нельзя. Принятая цена: 76 объявлений уже описаны в `tz/05-kredit.html` и `P15-R*`, и два
описания придётся синхронизировать. **Цена заплачена в части очереди 27.08.2026** (волна 5
ядра, `ADR-0182`): порядок, владение и термины сведены, расхождение эталона `credit.html`
адресовано ветке кредита. Прочие части движка (свод, детальный расчёт, плата) сводятся по мере
волн. Цифра «пять швов» в доводе выше — на дату решения; сейчас их **восемь**.

**5. `P19` не отзывается.** Разработка за неё ещё не бралась, все 23 карточки `[ ]`. Команде
сообщается: этап 1 (`P19-R1`, `P19-R2`) заблокирован до выдачи реестра, начинать с этапа 2 —
конструктор редакции, он от ядра не зависит. Отзыв стоил бы доверия и ничего бы не починил:
`P19` написана правильно, пусто вокруг неё.

**6. Осей две, и они встречные.**

```
проработка  (макет → канон)      потребитель  ──────▶  производитель
выдача      (ТЗ → постановка)    производитель ─────▶  потребитель
```

Проработка потребителя ценна не макетом, а **строками требований к производителю**: `statSlice`
пакетной формой не вывелся бы из статистики — нужен был кто-то, кто попробовал сложить итог по
уровню разреза и упёрся в неаддитивность доли. Выдача обязана идти обратно, иначе повторяется
`P19-R1`. Цена решения: между «проработали» и «выдали» появляется обязательная **волна
сведения** у производителя, и она не быстрая.

**7. Проработка потребителя закрыта, когда волна не добавила ни строки в «Что модуль требует
от соседей».** Макет без открытых дефектов — гигиена, а не критерий: у отчётности `ОЧ-Д1`
открыт и шесть обязательных форм не заведены, но соседям она уже отдала всё, что могла.

**8. Реестр показателей выдаётся внутри статистики, этапом 1 постановки** — не отдельным
пакетом. Правило «не по половине» выведено из опыта отчётности и не разменивается на скорость
одной разблокировки; `ADR-0159` (признак «исчислим налету») — требование к записи реестра,
выведенное из работы витрины, и фиксировать запись до витрины нельзя. Принятая цена:
классификация — единственное, что у разработки на руках, — простоит до конца всей статистики.

**9. Дорожек проработки две** — производители и потребители; долг сводится по ходу.
Исключение, сделанное немедленно: `worktree-calculations` запушена 26.08.2026 (`99130ce`) —
3346 строк макета и `ADR-0135`/`0136` существовали в одном экземпляре на диске.

**10. Дашборд входит в дорожку потребителей разведочной волной, перед статистикой.** По
решению 6 потребитель формулирует контракт производителю; дашборд — непроработанный
потребитель статистики, и выдав статистику раньше него, мы повторяем ровно то, что чиним.
Волна урезанная: не полный макет, а одна итерация, кончающаяся строкой «что требует от
статистики». Риск принят осознанно: если строка окажется пустой, волна потрачена зря.

> **Исполнено 26.08.2026, иначе, чем задумано.** Волна `A2` проведена **внутри** волны 3
> статистики — восемью вопросами, а не отдельной веткой. Строк оказалось четыре, не одна
> (`Д-1`…`Д-4`; журнал `mockups/statistics/ASUBK-status-razrabotki.md`, волна 3): краткую
> форму паспорта объявляет производитель · пакетная форма `statSlice` подтверждена вторым
> независимым потребителем · сходимость с реестром считается на одну дату, и действий два ·
> кредитная программа — объект статистики. Пятое — не требование, а наблюдение: на дашборде
> **два времени на одном экране**, ночной срез и живая очередь. Риск не реализовался; способ
> дешевле задуманного, потому что вопросы потребителя задавались там же, где сразу правились
> решения производителя.

---

## 5. Что отчётность уже потребовала от соседей

Источник — `mockups/reports/ASUBK-status-razrabotki.md`, раздел «Что модуль требует от
соседей» (ветка `worktree-reports`).

| от кого | что | где |
|---|---|---|
| Ядро | публичные перечни полей по каждому объекту-основному; шов `objectRows` | `ADR-0158`; **исполнено 27.08.2026** — `ADR-0181`: форма шва у ядра, перечни объявляют владельцы объектов, ядро отвечает по своим |
| Статистика | признак «исчислим налету» у каждого показателя реестра | `ADR-0159` |
| Статистика | ограничение объёма ответа `statRows` при разрешённом углублении | `ADR-0162` §8 |
| Статистика | пакетная форма `statSlice` — итог по уровню разреза модуль не вправе складывать сам | `ОЧ-25`, `ОЧ-34` |
| Анализ | убрать из модели обзора хранение чисел, оставить ссылку на выпуск | `ADR-0157` §4 |
| Задачи | второй вид основания — обязательство по отчётности | `ADR-0160` §4 |
| Справочники | справочник адресатов сдачи | `ADR-0157` §3 |

Все семь строк датированы волнами 4–5. **Сверено 26.08.2026 — отчётность сухая по решению 7.**

| волна | что добавила соседям |
|---|---|
| 1 (`eae4dd6`) | признак исчислимости налету → статистика |
| 4 (`10101fa`) | пакетная форма `statSlice` → статистика |
| 5 (`609048a`) | `statRows` и `objectRows` → статистика и ядро |
| 6 (`930a332`, `38f4aee`) | **ничего** — только находки `ОЧ-38`…`ОЧ-51` |
| сверка с видением заказчика (`67885b0`) | **ничего** — `ОЧ-52`…`ОЧ-54`, все три предложения отклонены |
| 7 (`76a20e3`) | **ничего** — `ОЧ-55`, раскладка меню тремя столами |

Три сухие волны подряд; таблица требований стоит на семи строках с волны 5. Оговорка `ОЧ-44`
(«живой запрос к ядру» по кнопке) **снята**: запрос обслуживается уже объявленным швом
`objectRows` (`ADR-0158`) и новой строки не порождает — входящий ящик `B1` не меняется.

Проработка отчётности закрыта; отметка внесена в её собственную «Очередь»
(`mockups/reports/ASUBK-status-razrabotki.md`, ветка `worktree-reports`).

**Сверка 29.08.2026 (волна 13 отчётности): три контракта из шестнадцати выданы не такими, как
просились, и переписана сторона потребителя.** Ядро и статистика вышли из очереди, и требования
впервые стало с чем сверять — читались выданные каноны соседей, а не наши формулировки
требований к ним.

| требование | как выдано | что сделано у потребителя |
|---|---|---|
| пакетная форма `statSlice` | **дерево уровней**: один вопрос, ответ — дерево узлов, значение узла считает статистика по строкам узла (`ADR-0176` §6, `ИС-24`) | принято как выдано — дерево сильнее пакета; переписаны §3 п. 4, §11 и `ИО-17` канона отчётности, в макете две двери шва свёрнуты в одну (`ОЧ-65`) |
| ограничение объёма `statRows` | **бинарный ответ**: полный список или отказ; выше порога — асинхронная выгрузка статистики (`ADR-0178`) | принято; выгрузка **не берётся** — она у владельца строк. Граница «два фоновых механизма — не дубль» записана в канон §11 (`ОЧ-66`) |
| перечни полей + `objectRows` | принято, но в v1 **ядро отвечает по своим объектам** (`ADR-0181` §4) | заведён `ИО-30`: режим «сейчас» в v1 — только формы с основным объектом «кредит»; в каталоге появился столбец «данные» (`ОЧ-67`) |

Встречных требований соседям волна не выставила ни одного. **Правило, выведенное ею и годное
всей очереди: спор имеет смысл там, где выданный контракт слабее запрошенного; там, где он
другой или сильнее, дешевле переписать потребителя.** Иначе выдача превращается в
согласование, а производитель, знающий свой предмет лучше, правит его под формулировку,
написанную за полгода до выдачи.

Волна вскрыла и обратное — **требование без адресата**. Из шестнадцати строк таблицы
отчётности десять не тронуты, и у шести адресата нет вовсе: у оргструктуры, справочников и
взыскания нет заведённой ветки, у подсистемы заданий нет и модуля. Отсюда две новые строки
очереди — `B3` и раздел «Вне дорожек» (§6).

---

## 6. Очередь

### Дорожка A — потребители (проработка сверху вниз)

| # | что | состояние |
|---|---|---|
| A1 | **Отчётность**: сверить таблицу требований, объявить сухой или доделать волну | ✅ **закрыт 26.08.2026** — три сухие волны подряд (§5); модуль ждёт места в выдаче |
| A2 | **Дашборд**: разведочная волна → строка «что требует от статистики» | ✅ **закрыт 26.08.2026** — сложен внутрь `B2` вопросами 3–8, дал **четыре** строки требований (`Д-1`…`Д-4`, журнал статистики, волна 3). Проработкой модуля не является: своя волна остаётся впереди, перед выдачей |
| A3 | **Анализ**: волна, проверка на сухость | свободен. Вход: `ADR-0157` §4, три открытых вопроса §15 спеки, отсутствующий раздел «что требует от соседей». `ADR-0154` блокирует **выдачу**, а не проработку |

### Дорожка B — производители (сведение входящего)

| # | что | входящий ящик |
|---|---|---|
| B1 | **Ядро**, волна сведения | ✅ **`ADR-0158` разобран 27.08.2026** (волна 4): шов `objectRows` принят восьмым, противоречие самого `ADR-0158` (§4 отдаёт перечень полей владельцу объекта, «Последствия» — ядру) разрешено `ADR-0181` в пользу §4/§5 — **ядро держит форму, перечисляет владелец объекта**, показатель в строке всегда ядра. Бумага закрыта целиком: спека v4, постановка редакции 2 (`P20-R27`…`P20-R29`), раздел ТЗ 18 редакции 2, `ИЯ-19`. В ящике остаются `РЯ-Д1` и `РЯ-Д2`; ящик больше не растёт — отчётность сухая, `ОЧ-44` снят (§5) |
| B2 | **Статистика**, волна 3 сведения | ✅ **часть первая закрыта 26.08.2026** — ящик разобран весь: `СС-Д5` закрыт, `СС-Д1`/`Д2`/`Д3`/`Д6` стали требованиями ТЗ, `ADR-0159` и `ADR-0162` §8 сведены, `statSlice` отдаёт дерево, четыре строки дашборда легли. 13 решений, `ADR-0176`…`ADR-0179`. ✅ **часть вторая закрыта 26.08.2026** — `СС-Д7` снят: макет собрал все шесть механизмов, 63/63, ещё 10 решений (`СС-43`…`СС-52`). Открыт `СС-Д4` (чинится в ядре) и новый `СС-Д8` (форма условия фильтра — в ТЗ). Модуль готов к разделу ТЗ. ✅ **волна 4 закрыта 26.08.2026** — раздел ТЗ `requirements/tz/19-statistika.html` (18 разд.), `СС-Д8` закрыт моделью (`ADR-0180`: фильтр — ДНФ без скобок, наборы через ИЛИ, сравнения через И), 4 решения `СС-53`…`СС-56`. ✅ **редактор наборов собран 28.08.2026** волной 11 частью первой (8 решений `СС-97`…`СС-104`, смоук 110/110): `СС-Д8` закрыт и в макете, отставания прототипа от модели не осталось |

| B3 | **Взыскание**, ветка не заведена | входящий ящик открыт 29.08.2026 сверкой отчётности, четыре строки, все из `ADR-0164`/`ADR-0166`: **(1)** признак «печатается бланк такой-то» — свойство **вида меры**, мера читает `reportIssues`; **(2)** реквизиты исполнительного листа на мере вида «Исполнительный лист» (вид, индекс, второй индекс) — без них не собирается Вид «ОИП» формы `ФО-17`; **(3)** **расхождение**: `ADR-0166` §1 предлагает снять строку 38 `DEADLINE_TEMPLATES` («Акт сверки на 1 января») как дубль строки 37 — календарное обязательство по п. 9 одно на систему и живёт в календаре сдачи отчётности; ADR стоит «предложено» с 26.08.2026, **предъявить его некому**; **(4)** владелец показателя «погашено в период по листу» — спрашивается вместе с платежами (`ОЧ-60`). Правка чужого канона из ветки отчётности **не делается**: это перенос решения через границу модуля |

**Вне дорожек — подсистема управления заданиями: проработка не начата.** ТЗ объявляет её
отдельно (раздел «подсистема управления заданиями» плюс раздел расчётных показателей по
заданиям), но канона, ветки и владельца у неё нет. Статистика объект «задание кураторства»
**сняла** (`ADR-0201`): владельца, отдающего множество, не нашлось. Кураторство отказывается
от заданий дословно. Отчётность из-за этого несёт два хвоста: `ФО-20` помечена в каталоге
«модуль ТЗ не прорабатывался», а у просроченного обязательства **нет продолжения внутри
системы** — задачу ставят вне её (`ADR-0160` §4 остаётся неисполненным требованием без
адресата). Ни один модуль её на себя не берёт, и это состояние надо либо признать за v1 вслух
перед заказчиком, либо завести владельца. В дорожки не кладётся: ни потребитель, ни
производитель — модуля пока нет.

### Выдача — строго последовательно, снизу вверх

```
1. Ядро           ✅ ТЗ 18 (ред. 2) + постановка P20-R1…R29 готовы 27.08.2026
2. Статистика     ✅ ТЗ 19 (ред. 8) + постановка P22-R1…R22 (ред. 2) готовы 29.08.2026 ──▶ разблокирует P19 этап 1
3. Классификация  ✅ P19 выдана 21.08.2026
4. Отчётность     ✅ ТЗ 20 (ред. 1) 29.08.2026; постановка P23 — следующий шаг
5. Анализ         ✅ постановка P21-R1…R22 (финанализ) 28.08.2026; ТЗ и обзоры — после отчётности
6. Дашборд        ТЗ + постановка, последним
```

Отдельным документом, вне очереди: **обоснование свёртки «модуля управления шаблонами
распечатки» в бланк** (решение 3).

---

## 7. Что не проверено

- ~~Таблица требований отчётности после волны 6 не сверена~~ — **сверено 26.08.2026** (§5):
  сухая, `ОЧ-44` снят, ящик `B1` не изменился.
- **У анализа раздела «что требует от соседей» нет вовсе** — проверено 26.08.2026. §5 спеки
  перечисляет только то, что модуль **берёт** через уже объявленные швы. Значит, сухость по
  решению 7 у анализа пока не проверяема: таблицы, в которую волна либо добавляет строку,
  либо нет, не существует. Первое дело волны `A3` — завести её.
- ~~**Номера не выбраны:** разделы ТЗ заняты до 17, следующий свободный — 18~~ — **устарело.**
  На 29.08.2026 занято до 20: `tz/18` — расчётное ядро (редакция 4), `tz/19` — статистика
  (редакция 7), **`tz/20` — отчётность** (редакция 1, волна 14); следующий свободный —
  **21**. Постановки: `P20` — ядро (редакция 3, 32 карточки), **`P21` —
  анализ** (редакция 1, 22 карточки), **`P22` — статистика** (редакция 1, 22 карточки);
  следующий свободный — **`P23`** — за ним и пойдёт постановка отчётности, номер берётся при
  её написании. Для дашборда номер по-прежнему не взят. Оба номера после `P20` достались перенумерацией, в один день 28.08.2026: анализ
  писался в своём worktree и тоже взял `P20` — уступил ядру (номер старше по `main`) и стал
  `P21`; статистика писалась под `P21`, свободный по всем ссылкам на тот час, — уступила
  анализу, влившемуся раньше, и стала `P22`. **Правило после двух столкновений за день:**
  номер постановки берётся `git ls-tree` **по всем ссылкам** (а не по рабочему дереву — ветка
  чужого worktree держит номер, которого в `main` ещё нет) и **пересчитывается перед
  вливанием**: между написанием пакета и его выводом в `main` номер могут занять. Уступает
  всегда невлитое — перенумеровать влитое дороже.
- ~~**Терминологическое столкновение:** «дашборд» в `_Avoid_` у «Обзора»~~ — **разведено
  26.08.2026**: в `CONTEXT.md` заведён термин **Дашборд** (рабочий стол роли: плитки-вопросы
  с краткой формой паспорта + живая очередь, два времени на одном экране), а `_Avoid_`
  «Аналитического обзора» назвал границу вместо запрета — «дашборд ставит вопросы плитками,
  обзор отвечает на них суждением».
- **Долг сведения веток** (по решению 9 сводится по ходу, не блокирует). ~~`ADR-0135`/`0136` и
  `ADR-0156`…`0163` не в `main` — в `main` дыра `0134→0137`~~ — **закрыто:** проверено
  27.08.2026, в `main` лежат все ADR подряд до `0180`, дыры нет; ветка
  `worktree-calculations` сошлась с `origin/main` ровно (0/0). Остаётся: `CONTEXT.md` разошёлся
  на три версии (main / reports +225 строк / calculations +28 строк); у анализа ноль строк в
  `TODO.md`. Столкновение номеров ADR регламентировано — `docs/adr/README.md`, §«Правило
  номера» и §«Что делать при столкновении».

---

## 8. Чего этот документ не заменяет

- `ADR-0146` — порядок слоёв исполнения. Здесь он исходное данное, а не предмет.
- Каноны логики модулей — содержание.
- `TODO.md` — реестровые строки задач. Пункты очереди сюда не дублируются как задачи.
- `STATUS.md` — снимок состояния. На 26.08.2026 он устарел и ни одного из шести модулей не
  упоминает.

Если какое-то из решений §4 понадобится цитировать из чужих модулей — оно повышается до ADR
по правилам `docs/adr/README.md`. Пока ни одно не повышено.
