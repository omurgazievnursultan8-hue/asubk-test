# Current State

> Last updated: 2026-08-16 (**кураторство: макет переписан целиком — волна 1, ядро и
> «Политика распределения»**). Прототип от 12.07.2026 был старше модели и правке не подлежал
> (`КУ-Д1`, `КУ-Д4`, `КУ-Д5` задевали раскладку данных): от `POLICIES`, `COI` и
> `computeRecalcDemo` не осталось ничего, вкладок стало пять. Новый
> `mockups/kuratorstvo/kuratorstvo.html` **исполняет логику**, а не иллюстрирует её: журнал
> закреплений append-only (атрибута куратора нет ни на одном объекте), рождение объекта
> запускает правила по всем ролям своего вида, обе ступени собраны из данных (5 правил
> холдинга со своими ключами + 7 правил подразделений, в том числе составной ключ «область и
> форма»), фолбэк-лестница с порогами 2·3·5·5 рабочих дней из справочника, редакции с датой
> вступления (действующая не переписывается, в прошлое дата не ставится), шов
> `curatorOn(объект, роль, дата)` с двумя ответами и единственным законным пустым. Рабочий
> экран один — «Политика распределения» (этапы 1–2, `P18-R1`…`R8`); «Пересчёт», «Передачи»,
> «Витрины», «Отстранения» стоят заглушками с адресом задач. Заведён смоук
> `scripts/inspect/kuratorstvo-check.mjs` — **41/41**, инварианты нумерованы `ИУ-1`…`ИУ-14`,
> отчёт скрипт вставляет в шапку макета сам. Демо-данные подобраны под дефекты: 6 фолбэков,
> 3 просрочены, 1 ровно на пороге; смена редакции даёт разные ответы до и после 01.03.2026.
> Журнал волн и границы — `mockups/kuratorstvo/ASUBK-status-razrabotki.md`, `ЗАПИСКА.md`
> переведена в археологию.
>
> Ранее 2026-08-15 (**кураторство: постановка P18 доведена — ключ верхней ступени и
> пять расхождений закрыты**). Модуль административный, greenfield: отвечает на один вопрос
> «кто вёл этот объект на эту дату», дефектов живого стенда по нему нет. Верхняя
> (холдинговая) ступень правила стала ключеваться парой **«объект и роль»** вместо одного
> объекта: предмет залога рождает сразу два закрепления в разных подразделениях — куратор
> залога в залоговом управлении, региональный исполнитель в областном отделе, — и одним
> подразделением на объект их не развести. Правил холдинга поэтому ровно **пять**, по числу
> назначаемых ролей, у каждого свой ключ признаков; общей таблицы «объект → отдел» не
> существует, экран верхней ступени открывается выбором роли (`ADR-0117`). Тем же заходом
> закрыты пять расхождений постановки: **пересчёт живёт только у нижней ступени** — у верхней
> его нет вовсе и кнопки тоже, ступень родильная и предпросмотр был бы вечно нулевым;
> **замещающий получил шов наружу** — `resolveActor(работник, дата)` у модуля сотрудников
> (`P12-R12`), молчание контракта отдаёт объект заведующему; **роль администратора не бывает
> вакантной** — снятие только вместе с назначением преемника, гейт в модуле сотрудников;
> **лестница ведущего куратора достроена вниз** — нет действующих кредитов → последний
> закрытый → специалист последней заявки → объектов нет ни одного, тогда ведущего нет;
> **пустой ответ назван ровно один** — вопрос на дату раньше рождения объекта, и значит он
> «объекта тогда не было», а не «куратор неизвестен». Редакция постановки осталась первой:
> пакет разработчику не передавался. Заведён `mockups/kuratorstvo/ASUBK-status-razrabotki.md`
> — `КУ-Д1`…`КУ-Д10`, что обязан закрыть переписанный прототип. Проверки: `check_tasks.py`
> 4 файла 0/0, `check_points.py` 2060 ссылок ❌ 0.
>
> Ранее 2026-08-12 (кредит: **«Транши» и «График» пересобраны под реструктуризацию —
> волна КВ-26**). Обе вкладки были написаны до того, как реструктуризация приняла свои решения,
> и о них не знали. Транш теперь рождается освоением **или** разделением по ДС (`ADR-0092`):
> происхождение выводится по входящему переносу, производный транш не осваивается (Г-4) и
> **не расходует сумму договора** (`ADR-0115` — Г-3 меряет только «освоение»), «Освоено» у него
> прочерк, а не 0. Единственное новое хранилище — `t.transfers[]`; остаток тела считается по
> четырём слагаемым (ИР-3), опустошённый транш закрывается **переносом**, и причину выводит
> операция (ИР-5); появилась секция «Движение по траншу». Строка графика несёт **безставочные
> статьи** (`ADR-0109`: ставку несёт только основной долг), пустая колонка не рисуется, под
> ДС-версией стоит сверка ИР-2′. Версия графика получила **период действия и основание** вместо
> флага «активная» — действующая выводится по дате среза. Строки приходят приложением к ДС,
> кредит их валидирует новым гейтом **Г-25** и применяет одной дверью `restructureApplied`,
> которую нажимает человек кнопкой «Применить ДС» в хвосте журнала «Условий». Заведены задачи
> **P15-R29** и **P15-R30**; разведена коллизия номеров гейтов — правила шапки макета уехали в
> Г-31…Г-33, следующий свободный номер Г-34. Смоук — 133/133, реструктуризация 57/57 без
> регресса.
>
> Запись того же дня: 2026-08-12 (реструктуризация: **многокредитная заявка — расчёт живёт на
> транше, план из 8 задач слит в `main`**). Реализована спека
> `docs/superpowers/specs/2026-08-11-restructuring-multicredit-design.md`: охват заявки
> называет траншы (не кредиты), `Calc` заводится один на каждый транш охвата (не один на
> заявку) — ключ строки базы и распоряжения стал тройкой (транш, статья, срочность), <!-- check_points:ignore -->
> `creditIds` выводится из расчётов, а не хранится. Дверь совместимости `requireCalc(app,
> calcId)` отказывает вместо угадывания, когда расчётов 2+ и `calcId` не передан явно.
> Решение — `docs/adr/0114-raschet-zhivet-na-transhe.md`. Проведено через
> `subagent-driven-development` (8 задач, свежий исполнитель + ревью на каждую); финальное
> сквозное ревью нашло 3 Critical/8 Important/1 Minor, не пойманных ни одним из восьми
> точечных ревью (корень — нулевое покрытие тестами уровня рендера, только regex по
> исходнику; закрыто новым сценарием #68, который реально красный на до-фиксовом коде).
> Один раунд фиксов снял всё, кроме **C3** (нет экрана для многокредитных операций —
> охват/регистрация ДС по расчёту) — сознательно отложено человеком, задокументировано
> `TODO.md:P17-R2`. Смоук `restructuring-check.mjs` **68/68 PASS** (было 42/42). Слито PR
> `#25` → `main`, коммит `9569cba`. Пакет передачи по-прежнему не начат (ни ТЗ, ни
> `docs/tasks/p17-*`) — `P17-R1` из реестра `TODO.md` закрыт кодом, `P17-R2` — новый открытый
> пункт, экраны ещё не построены.
>
> Предыдущая запись: 2026-08-11 (кредит: **разобрана логика изменения условий — три ADR,
> `0111`…`0113`, и три задачи, `P15-R26`…`P15-R28`**). Найдена дыра: формы п. 89 (ставка, <!-- check_points:ignore -->
> срок, льготный период, капитализация, отсрочка) сегодня меняются двумя путями — конвейером
> реструктуризации с согласиями, пределами и постановлением Кабмина либо прямым ДС в карточке
> кредита за две минуты; `agreements[].source` — свободная строка, гейта нет. Решения:
> **Г-23** — вид ДС из закрытого справочника (реструктуризация · досрочное погашение · перевод
> долга · прочее · без изменения условий) решает, какие параметры документ вправе тронуть;
> граница п. 89 проведена по эффекту на поток погашения, а не по букве нормы (`ADR-0111`). <!-- check_points:ignore -->
> **Г-24** — опечатка лечится новым типом записи «корректировка» (наследует основание и дату,
> меняет значение, прежняя запись зачёркнута), расторжение ДС — обратными записями датой
> расторжения, прошлое не переписывается (`ADR-0112`). Ретро-расхождение «условия изменены,
> график не перестроен» перестаёт быть баннером и **запирает закрытие периода** через
> именованный шов `creditPeriodBlockers(период)`; начисление и платежи не блокируются
> (`ADR-0113`). В глоссарий добавлены «Вид допсоглашения» и «Корректировка»; реестр гейтов
> получил §1.1 с заявленными Г-23/Г-24; спека вкладки условий поправлена — `dayMethod` в
> реестре параметров и в коде, исходные «десять параметров» были ошибкой. **Открытый вопрос
> заказчику:** есть ли у бюджетного кредита мораторий на досрочное погашение и комиссия за
> него — глава 9 молчит; модуль вопрос не держит.
>
> Ранее 2026-08-10 (реструктуризация: **найдено Положение о работе с бюджетными
> кредитами — приложение к ПКМ КР от 19.01.2026 № 14, ред. 19.01.2026; все восемь вопросов
> письма 05.08 закрыты, открытых вопросов к заказчику по модулю нет**. Извлечение гл. 9
> (пп. 81–96) и п. 34 — `docs/normativ/polozhenie-2026-01-19-N14-gl9.txt`. Семь вопросов <!-- check_points:ignore -->
> закрыты собственными решениями (**РС-26…РС-32**): порог расхождения «срез ↔ факт» в две
> ступени (10 %/500 тыс. → виза куратора; 25 %/5 млн → возврат на комитет) · **пеня —
> терминальная статья** (`ADR-0106`: ни на неё, ни ею; капитализация разводит проценты и пеню
> по разным счётчикам) · прощение только по сумме, названной в акте (ИР-13) · гейт покрытия
> снимает **комитет по администрированию бюджетных кредитов**, снятие раскрывается в пакете к
> письму (п. 86), «залоговой комиссии» в макете не осталось · производственный календарь — <!-- check_points:ignore -->
> справочник дат, общий с платежами, пустой год виден отметкой, а не откатом к «сб/вс» ·
> пять нормативных видов п. 89 замкнуты, прочее — «иное» с обязательным обоснованием (ИР-14) · <!-- check_points:ignore -->
> повторность с третьей сделки — сигнал, не гейт. Из самой нормы пришли **РС-33…РС-37**:
> главное — **решение принимает Кабинет Министров по согласованию с профильным комитетом
> Жогорку Кенеша** (п. 82), уполномоченный орган даёт лишь заключение (пп. 87/88), поэтому <!-- check_points:ignore -->
> конвейер развёрнут с пяти стадий до **девяти** (`ADR-0107`), а ДС регистрируется под
> номером постановления КМ (ИР-12); полов ставки два, применяется больший — `max(50 % от
> первоначальной, минимум по виду проекта п. 34)`, вид не указан ⇒ флаг «п. 34 не проверен»,
> не блокировка; пакет документов нормативен (пп. 85–86) с условными строками пп. 91/95; <!-- check_points:ignore -->
> предел срока меряется от **остатка задолженности** (п. 90) — прежнее чтение «от суммы <!-- check_points:ignore -->
> переноса» исправлено, `ADR-0099` уточнён в трёх местах. Макет и смоук доведены: сценариев
> 29 → **42, прогон 42/42 PASS**; §14 спеки, `ASUBK-status-razrabotki.md` и шапка
> `restructuring.html` переписаны. Письмо заказчику переиздано **редакцией 2** — не вопросы, а
> принятые умолчания с просьбой возразить; к Холдингу остались две просьбы, ни одна модуль не
> блокирует: более поздняя редакция Положения, если есть, и **вид проекта (п. 34) по
> действующим кредитам**.)
>
> Тем же днём 2026-08-10 (платежи: **постановка переписана с нуля, редакция 1** —
> `docs/tasks/p16-platezhi-tasks.html`, 37 карточек в 8 этапах, номера строго по сборке;
> прежний файл в `docs/tasks/archive/`, таблицы соответствия ID нет намеренно. Волна 4
> (`ADR-0087…0091`) внесена в карточки; ТЗ `14-platezhi.html` про автосторно ещё устарело —
> в шапке ТЗ стоит предупреждение. Подробности — в changelog ниже.)
>
> Ранее 2026-08-10 (взыскание: **постановка переписана с нуля, редакция 1** —
> `docs/tasks/p14-collection-tasks.html`, 32 карточки в 8 этапах, номера строго по сборке;
> прежний файл в `docs/tasks/archive/`, таблицы соответствия ID нет намеренно.)
>
> Ранее 2026-08-07: (реструктуризация: **тикет 08 закрыт — макет и смоук переборены под
> РС-1…РС-25 целиком**. §14 спеки — 29-пунктовая матрица приёмки заменила прозу; смоук
> `restructuring-check.mjs` переписан с нуля (458 строк/41 сценарий под отменённой моделью →
> новый файл, 29 сценариев под РС-1…РС-25), прогон **29/29 PASS, 0 ошибок консоли**. Шапка
> `restructuring.html` переписана (Р-1…Р-19/6 стадий → РС-1…РС-25/7 стадий с гейтами и §14).
> `ASUBK-status-razrabotki.md` пересобран: таблица «отменено/показывает» снята, «Готово»
> ключуется по РС-номерам, единственный незакрытый пункт — распределение начисленного «по
> периодам» (`disposition.periods`) задокументировано как честный пробел без UI-двери, не
> подделано тестом.) — _(update this date every time you edit)_
>
> Строки под РС-1…РС-25 выше остаются верны по устройству; числа сценариев и стадий заменены
> записью 10.08.2026.
>
> Ранее 2026-08-06: платежи: **пакет передачи готов** — `requirements/tz/14-platezhi.html`
> (13 разделов) + `docs/tasks/p16-platezhi-tasks.html` (28 карточек, 6 этапов, JSON-остров по
> `docs/tasks/FORMAT.md`, `check_tasks.py` чист) + `TODO.md` раздел «Платежи (целевая модель)»
> (`P16-R1…R28`, флоский список без вложенной прозы); `requirements/tz/07-obsluzhivanie.md`
> переставлен на новое ТЗ вместо разделов `05-kredit.html`. Все восемь открытых вопросов спеки
> несут явный дефолт (ни одного «уточняется»). Граница с кредитом (`P15-R15/17/22` — платёж-
> зеркало, леджер, гейт Г-17) зафиксирована явно, не задублирована. Модуль готов к постройке;
> коммит `d18b11c`.
>
> Ранее в тот же день: **платежи, волна 4 — разбор восьми открытых вопросов §11**. Пять
> оказались решаемыми: один закрыт ответом Холдинга (очерёдность погашения —
> `расходы → комиссия → ОД → проценты → пеня`, владеет программа, снимок берётся решением по
> заявке, после подписания неподвижна), четыре — собственным решением. **Автосторно по таймауту
> упразднено**: машина сама производила работу для комиссии, значительная часть которой
> отменялась поздним подтверждением; ожидание стало бессрочным, давление даёт закрытие периода,
> сторно — решение бухгалтера по сводке. **Замка стало два**: спорную пеню решает орган (шестая
> строка справочника Р-6 с источником `внутренний акт`, справочник перестал быть закрытым),
> разморозку и открытие периода — главный бухгалтер (журнал и распоряжение с вложением).
> **Курсы** берутся у самого НБ КР, файл — резерв, на дату без публикации действует последний
> установленный. **Порог копейки** перенесён в разбивку и поглощается в обе стороны — кредит
> закрывается строгим нулём, легаси-дефект `E2E-13/14` лечится по построению. Побочно:
> «обратная очередь» упразднена (совпала с прямой), у `ADR-0073` два режима. Пять ADR —
> `0087`…`0091`, врезки-пересмотры в `0058`, `0059`, `0061`, `0073`, `0076`; `CONTEXT.md`
> (новый термин «Комиссия по спорным начислениям» + семь переписанных статей), спека (§2.4,
> §4.2–4.4, §5, §7.2, §8.1, §8.5, §11, журнал волны 4), макет и смоук —
> **214 проверок, 0 провалено** (было 191). Письмо ужато с восьми вопросов до трёх коротких +
> запрос двух документов: `docs/voprosy-zakazchiku/2026-08-06-platezhi.md` (редакция от 05.08
> помечена заменённой). Находка не по теме: **`ADR-0079` занят дважды** — `worktree-vzyskanie`
> и `worktree-restructure`, обе ветки не влиты, конфликт описан в `docs/adr/README.md` и
> разрешается владельцами веток, не платежами.
>
> _Позже 06.08.2026, тот же день:_ довешена перепроверка §11 письма 06.08 — оба перенесённых из
> 02.08 вопроса (момент исполнения обязательства, ликвидация) подтверждены: собственным решением
> не закрываются. Норма ни одну из трёх дат платежа (внесение · зачисление · ввод) исполнением не
> называет (грep по тексту №41); ликвидация в норме не упомянута вовсе, а `ADR-0048` уже отклонил
> аналогию с банкротством. Найдена и вписана в спеку (§11) сопутствующая опора — межмодульная
> согласованность дефолта (М-6 §8.1 и платежи §2.1/§4 одинаково берут дату внесения, не
> зачисления) и точное соответствие `в.2`/`в.5` наряда сверки; два коммита. Оставшиеся три
> вопроса письма (орган по спорной пене, запрещённые статьи банкротства, доступ к nbkr.kg)
> перепроверены без правок — в репозитории нет ничего сверх того, что уже сказано в `ADR-0089` и
> в самом письме. Волна 4 закрыта целиком, все шесть вопросов спеки корректно висят на ответе
> снаружи. Пакет передачи по платежам на тот момент ещё не начат — собран позже в тот же день, коммит `d18b11c` (см. выше).
>
> Ранее в тот же день: взыскание: **reqId-коллизия (МП-О1) закрыта — двое обеспечителей
> одной роли на одном кредите больше не схлопываются в один `reqId`**. Правка
> (`mkReq`, `collection.html:7224`): третий сегмент id — `personId` вместо кода роли
> (`role==='заёмщик' ? 'з' : personId`); заёмщик, у которого коллизии не было, не тронут. Под
> уже принятым **`ADR-0024`**, без нового ADR. Баг был латентным — текущая затравка не несёт
> двух обеспечителей одной роли на одном кредите (три дела, по одному обеспечителю). Смоук
> пересобран под новый формат: формула-тест переписана, 25 хардкод-id `307/307/п` →
> `307/307/п-307`. Прогон: `collection-check.mjs` 810/0/0 (19 провалов сразу после кодовой
> правки — все от старой формулы в тестах, не баг — исправлены);
> `collection-data-audit.mjs` 0 находок/107 дел/116 требований; `collection-seed-coverage.mjs`
> 48 ситуаций/0 тонких. Новый `TODO.md`-пункт не заведён — латентный баг вне бэклога. Журнал —
> `mockups/collection/ASUBK-status-razrabotki.md`, запись «Правка reqId-коллизии (МП-О1)».
> Ранее в тот же день: **ревизия статуса МП — таблица §17 и план
> `2026-08-02-collection-model-gaps.md` сверены построчно с кодом, 18/20 решений сделано**.
> Без ADR — сверка факта, не новое решение модели; последний пункт согласованного порядка
> волн. Карантин закрытия дела (**`ADR-0044`**) нигде не реализован — собственная врезка §17
> утверждала обратное («живёт в макете»), исправлено на месте. Правопреемство (**`ADR-0052`**)
> — частично: терминал заведён, разветвление требования на нескольких преемников отложено
> (`TODO.md:P14-R14`). Подписант по подразделению кредита (МП-О5) сделан
> (`respFor`, `collection.html:4239`). Оба открытых гэпа уже покрыты `TODO.md` (`P14-R4`,
> `P14-R14`) — новых пунктов и нового ADR не заведено. Журнал —
> `mockups/collection/ASUBK-status-razrabotki.md`, запись «Ревизия статуса МП»; коммит
> `a3f6394`.
> Ранее в тот же день: **старый долг смоука закрыт целиком — 32 провала → 0**.
> Без ADR — обе части чистая починка ожиданий под уже принятые решения, не новое решение
> модели. Часть 1 (`6c5ced9`) сняла 22 провала: держались на слое, который **`ADR-0045`**
> (RULES_SCHEMA 3) уже снял — лестница контуров, `sequenceReason`, `movePhase`, вкладки
> «Стадии»/«Фазы»; ожидания смоука остались на старой оси. Заодно починена регрессия макета:
> фильтр по разделу на вкладке «Порог МП5-11» молча вычищал таблицу. Часть 2 (эта волна) сняла
> оставшиеся 10: дрейф счётчиков затравки от волн **`ADR-0077…0081`**/`Task 7 fix-round2`/
> `МП5-17` — шаги таймлайна К6 (3 → 7), `measureGate(...).kind` (`sequence` → `precondition`),
> закрытые требования/дела (14/14 → 6/6), снимок после restore (100 → 116 требований),
> `SCOPE_KIND_DEFAULT` (8 → 9 видов), горизонт «всё» на сроках (112 → 237), рамка счётчиков
> сроков, расхождение подразделения шаблона/требования (22 → 114 строк, новый доминант — тпл
> 48 «Исковая давность»), вкладка «Гейты» (4 → 5, `МП5-17`, проверка через `gateOrgans()`).
> Плюс переформулирована одна заноза: **«нулевой остаток закрывает требование сам»** держалась
> только на совпадении затравки и молчала бы, доведись гейту треков (**`ADR-0053`**/МП-О4)
> когда-нибудь сработать по-настоящему — расщеплена на гейт-осознанный ассерт и новый
> синтетический регрессионный тест (открывает судебный трек без закрывателя на живом
> требовании 208/208/з, проверяет, что нулевой остаток при открытом треке НЕ закрывает, откат
> меры в конце). Прогон: `collection-check.mjs` **810/0** (0 провалов, 0 ошибок консоли),
> остальные линии как прежде — `collection-data-audit.mjs`/`collection-seed-coverage.mjs` 0
> находок, `check_tasks.py` 0 ошибок. Журнал — `mockups/collection/ASUBK-status-razrabotki.md`,
> запись «Старый долг смоука 32 → 0»; сверка с редакцией №41 закрыла тот же пункт остатка.
> Ранее в тот же день: **четыре акта-ответа МП-3 получили затравку, снят дубль
> срока по п. 37**. Решение — **`ADR-0081`**. `ADR-0072` завёл четыре вида внешних актов —
> ответов суда на наши процессуальные обращения, — но ни одно дело затравки их не поднимало:
> слоевое следствие двух отмен было **объявлено** (вид стоял в `AWARD_DEATH_KINDS`), а не
> показано на данных. Форма у всех одна — «наше обращение → внешний акт», исход несёт **акт**;
> расходятся они тем, что делают с деньгами. Отмена решения (по вновь открывшимся или новым
> обстоятельствам) слой **гасит, а не правит** — сам акт отмены ничего не присуждает, и правая
> колонка сверки после него **пуста, а не нулевая**; восстановление срока и изменение способа
> исполнения денег не трогают вовсе. Границы, взятые из справочника, а не из головы:
> изменение способа стоит на **исполнительном листе**, а не на решении (менять способ можно у
> того, что уже исполняется), а заявление о восстановлении по **п. 30.6** подаётся
> одновременно с самим процессуальным действием — билдер требует нашей апелляционной жалобы и
> датирует заявление её днём. Коды **`K3-ПЕРЕСМОТР`** (дела **368** — отмена гасит слой в
> 250 000; **369** — заявление подано, суд не ответил, слой стоит) и **`K3-ХОДАТАЙСТВА`**
> (**374** — восстановление срока; **381** — изменение способа). Отдельным ассертом закреплено
> то, чего модель НЕ делает: ни один из четырёх актов не несёт `setsPhase` — **откат фазы
> после отмены решения не построен** (осознанное состояние `ADR-0072`), и теперь он не может
> появиться молча. Вторая половина волны — **дубль срока**: на п. 37 стояло ДВЕ строки
> шаблонов (один орган под двумя именами, та же величина 3 р.д.), расходившиеся базой отсчёта;
> слиты в одну с нормативной базой, шаблонов **48** вместо 49. Довод шага 4 наряда, которым
> вторая строка и была заведена («срок норма назначает ровно одному переходу»), её же и
> опровергает. Прогон: `collection-check.mjs` **809/32** (10 новых проверок, список провалов
> побитово равен baseline), `collection-data-audit.mjs` 0 находок · **107 дел · 116
> требований**, `collection-seed-coverage.mjs` **48 ситуаций** · 0 тонких, `check_points.py`
> **1890 ссылок ❌ 0**. Пакет передачи: ТЗ и задача **P14-R28**.
> Ещё раньше в тот же день: **судебная ветка засеяна с обеих сторон — жалоба
> ЗАЁМЩИКА, наше возражение и требуемое по статьям**. Решение — **`ADR-0080`**. Шаг 6 наряда
> завёл виды пп. 30.4/30.5 в справочник, но затравки под них не было ни одной: билдер не клал
> ни одной жалобы заёмщика, обязанность возразить не поднималась ни разу, а таблица
> «требовали → присудили» показывала заглушку на всех 101 деле. Ветка зеркальна нашей, но не
> копия: **повод обратный** (п. 30.4 даёт заёмщику повод только против решения, которым иск
> удовлетворён — граница `REJECTED_VERDICT` читается из справочника исходов, а не пишется
> строкой), между жалобой и актом стоит **наше возражение** — единственное, что норма
> требует от нас по чужой жалобе, и срока у него нет вовсе («в соответствии с процессуальным
> законодательством», `ADR-0068`), а акт инстанции ссылается `basedOn` **на жалобу, а не на
> возражение**. Затравка получила код **`K3-ЖАЛОБА-ЗАЁМЩИКА`**: дело **366** (решение
> полностью в нашу пользу, апелляция по жалобе заёмщика оставила в силе — слой держится актом
> ПЕРВОЙ инстанции) и **367** (кассация заёмщика по п. 30.5 отменила и отказала в иске — слой
> гаснет, фаза откатывается на «Иск»). Вторая половина волны — **`m.claims`**: опция
> `claimed` кладёт требуемое по пяти статьям на корень цепочки (иск), пять дел (332 · 346 ·
> 347 · 366 · 367), и проверка согласия свёрток `claimChainsFor`/`layerOf` **переехала с
> подставленного объекта на живые данные**. Находка волны: после отмены цепочка сверки
> заканчивается актом отмены и правая колонка **пуста, а не нулевая** — «статья не
> присуждалась» ≠ «присуждено 0». Прогон: `collection-check.mjs` **797/32** (16 новых
> проверок, список провалов побитово равен baseline), `collection-data-audit.mjs` 0 находок ·
> **103 дела · 112 требований**, `collection-seed-coverage.mjs` **46 ситуаций** · 0 тонких.
> Пакет передачи: ТЗ получило подразделы 9.6 и 12.4, задачи — **P14-R26** и **P14-R27**.
> И ещё раньше в тот же день: **кассационная ветвь разобрана — хвост справочника
> видов, оставленный шагом 6 наряда**. Решение — **`ADR-0079`**. Справочник противоречил
> собственному шаблону срока: основанием «Кассационной жалобы» стояли акты ПЕРВОЙ инстанции
> (кассация минуя апелляцию), а шаблон 28 отсчитывал три месяца от «даты вынесения акта
> апелляционной инстанции» — как и требует **п. 30.3**. Основание сужено до
> **«Постановление апелляционной инстанции»**; зеркало у жалобы заёмщика (п. 30.5) было
> записано верно ещё шагом 6. Заведён **81-й вид меры — «Постановление кассационной
> инстанции»** с пятью исходами. Содержательная находка: у кассации есть исход, которого
> нет у апелляции — **«апелляция отменена, решение первой инстанции в силе»**: умирает не
> слой, а ОДИН акт, и денежный слой возвращается к решению первой инстанции. Выражено
> свёрткой `dropRestoredAppeals`, общей у `layerOf` и `claimChainsFor`; формулировка исхода
> намеренно не начинается на «отменено» (префикс читает предикат отмены) и это закреплено
> ассертом, а не вкусом. Затравка получила код **`K3-КАССАЦИЯ`**, дела **346 · 347** (оба
> полюса) — и вместе с ними **впервые за всю волну проехал денежный слой на seed-данных**:
> до сих пор `awards` носил только акт утверждения мирового. Попутно вскрыт **настоящий
> дефект групп 4 и 5.2** (`ADR-0078` называл причину неверно): терминальные кандидаты есть
> у восьми дел, все восемь глушит `tracksClosedFor === false` — дело с актом Кабмина о
> списании читается как «2.2. Проводится работа по исполнительным листам». Решение, чинить
> ли это затравкой или самим списанием (п. 65 за второе), волной **не принято** — заменена
> только неверная причина на верную, тремя ассертами. Прогон: `collection-check.mjs`
> **781/32** (19 новых проверок, список провалов побитово равен baseline),
> `collection-data-audit.mjs` 0 находок · **101 дело · 110 требований**,
> `collection-seed-coverage.mjs` **45 ситуаций** · 0 тонких, `check_points.py` **1749 ссылок
> ❌ 0**. Пакет передачи: **P14-R25** новая, **P14-R12** переиздана и переименована.
> Ранее 05.08.2026: **§4.14 сверки закрыт — «мелочи с ценой», семь точек;
> расхождений редакции №41, ждущих кода, не осталось**. Решение — **`ADR-0078`**.
> Точки: тпл 38 «до 1 июля» → **«до 1 сентября»** (п. 9, величина была нашей) · заведены
> **телефонные переговоры с заёмщиком** (п. 16.2) — вид меры, первая ступень маршрута и
> предусловие первичной претензии, требующее ФАКТА, а не успеха («связь не установлена»
> его удовлетворяет) · **доказательство доставки стало реквизитом канала** (п. 16):
> справочник `DELIVERY_PROOF`, поле документа было свободным текстом — стало выбором из
> набора канала, СЭД бумажного уведомления не требует · **«направлено» разведено с
> «вручено»** · **группы платёжеспособности заведены закрытым справочником нормы** — девять
> значений с кодами и пунктами перевода вместо восьми наших строк, среди которых были
> сквозные имена СТАДИЙ · **у паузы п. 17 стало два основания**, предел 70 к.д. привязан
> дословно к гарантийному письму, у рисков срыва программ предела норма не назвала.
> **Главная находка волны:** ось вручения в шести местах ссылалась на **п. 19.2**, где о
> вручении нет ни слова (там состав пакета в ДПО) — настоящий адрес **п. 20.1**, и
> последствие другое: срок должника течёт от НАПРАВЛЕНИЯ и невручением не отменяется, а
> вручением живёт ПАКЕТ В ДПО. Выражено новым полем предусловия `servedKinds` на
> «Служебной записке в ДПО» — единственным, читающим пометку доставки. Затравка получила
> код `K1-ПАУЗА-ПРОГ`, дела **328 · 329** (оба полюса комитета, отсрочки длиннее 70 к.д.).
> Прогон: `collection-check.mjs` **762/32** (36 новых проверок, список провалов побитово
> равен baseline), `collection-data-audit.mjs` 0 находок · **99 дел · 108 требований**,
> `collection-seed-coverage.mjs` **44 ситуации** · 0 тонких, `check_points.py` **1664 ссылки
> ❌ 0**. Письмо ГФХ выросло до **19 вопросов** — **18** (между первичным вводом и иском
> Порядок перевода в другую группу не называет ни разу: заёмщик с просрочкой и запущенным
> безакцептом формально «производит погашение по графику») и **19** (предел отсрочки по
> второму основанию п. 17); по-прежнему **не отправлено**.
> Ранее в тот же день: **§4.8 сверки закрыт**. П. 16.5 разводит безакцепт на два комитета
> по типу проекта, и гейт, привязанный к одному органу, развилку не выражал. Заведён
> **обязательный факт кредита `projectType`** («ДАК» / «региональный») — не выводимый из
> `subdiv`: ведущее подразделение едет по эстафете (ОД → ОПК → ДПО → САК), и у 57 кредитов
> затравки из 104 происхождение из него уже не восстанавливается. Гейт несёт `organByProject`
> вместо `organ`, органа по умолчанию нет вовсе (неизвестный тип → орган не выбран, а не
> ORGANS[0]); один вопрос — один орган, смешанный набор кредитов отклоняется на обеих границах.
> Решение — **`ADR-0077`**. Попутная находка: шаблон срока 13 держал **выдуманные «5 р.д. от
> получения документов от РП»** — норма даёт куратору 10 р.д. от той же базы, что и ветви А.
> Затравка получила дело **327** (региональный проект с ожидающим гейтом) — без него развилка
> жила бы только в коде: все пять кредитов с `bezakcept:true` были проектами ДАК. Прогон:
> `collection-check.mjs` **726/32** (15 новых проверок, список провалов побитово равен baseline),
> `collection-data-audit.mjs` 0 находок · **97 дел · 106 требований**, `collection-seed-coverage.mjs`
> 43 ситуации · 0 тонких, `check_points.py` **1318 ссылок ❌ 0**. Умолчание «не-ДАК = региональный»
> вынесено наружу **вопросом 17** письма ГФХ (тогда — 17 вопросов; текущее состояние письма
> см. выше).
> Ранее в тот же день: **наряд сверки с редакцией №41 закрыт целиком — семь шагов из семи**. Шаг 7 — письмо ГФХ: редакцию 2 писали после шага 4, и находок шагов 5–6 она
> не знала. Добавлены четыре вопроса — **13** (величина порога ст. 9-1: предусловие
> банкротства готово, а числа нет, Порядком не устанавливается) и **14–16** (три расхождения
> глоссария §8 сверки: «фаза взыскания», «куратор», «комиссии»). Находка шага: расхождение по
> комиссиям **не словарное** — «сборы» кредитного модуля сворачиваются в «Расходы», которые
> п. 48 ставит первыми, а модуль платежей ведёт комиссию своей категорией с очередью из
> договора; вопрос 16 объявлен единственным, меняющим устройство, и итоговая формула письма
> переписана с явным исключением. Добавлено **приложение** — все двенадцать величин без
> нормативной опоры: девять закрыты вопросами, три названы вместе с причиной, почему вопроса
> нет. Письмо — 16 вопросов + запрос шести документов, **не отправлено** (17-й добавлен
> позже, тем же днём). Модель шагом 7 не тронута: 711/32 · 0 находок · 43 ситуации;
> `check_points.py` **1273 ссылки ❌ 0**. Ранее 05.08.2026:
> шаг 6 (справочник видов 73 → **79** и пересев затравки, `ADR-0071`, `ADR-0072`), шаг 5
> (новые ветви и предусловия, `ADR-0069`, `ADR-0070`), шаг 4 (маршруты,
> `ADR-0066`…`ADR-0068`). Ранее 04.08.2026: шаг 3 (пороги и паузы, `ADR-0063`…`ADR-0065`),
> шаг 2 (1-й день просрочки, `ADR-0062`), шаг 1 (перенумерация ссылок, ❌ 0), получена сама
> редакция №41, сделана сверка `docs/normativ/2026-08-04-sverka-red-N41.md`. Осталось вне
> наряда: отправить письмо (19 вопросов) и два открытых подраздела §4 — 4.9 и 4.10, оба
> ждут ответа ГФХ; список — §9 сверки).
>
> _Тот же день, трек платежей:_ **Платежи:** журнал догнан за 02.08…04.08 — две волны разбора
> (`ПТ-1`…`ПТ-31`, ADR `0055`…`0061` и `0073`…`0076`), макет `mockups/payments/payments.html` переписан тремя
> этапами (5211 строк, **восемь экранов** вместо пяти, `ПТ-Д1`…`ПТ-Д27` закрыты), смоук
> `scripts/inspect/payments-check.mjs` — 191 проверка, 0 провалено (`npm run test:payments`,
> команда заведена этой же правкой). Восемь вопросов §11 спеки собраны письмом
> `docs/voprosy-zakazchiku/2026-08-05-platezhi.md` — пять новых, три повторяют письмо 02.08
> ссылкой; учёт ожидания — в «Blocked / waiting» и «Known issues». Пакет передачи по платежам
> не начат. Ветка `worktree-payments` влита в `main` 05.08.2026 — сайдбар в три подгруппы
> (`ПТ-Д27`), словарь экрана отделён от словаря модели (разнесение → разбивка платежа), поиск
> и фильтры в лентах, пять лент переведены на грид реестра кредитов, снят пояснительный слой
> (5768 → 5211 строк). Слияние перевело ссылки ветки на новые номера `ADR-0073…0076`.
>
> _06.08.2026, трек оргструктура:_ **Оргструктура:** разработчики отчитались о завершении
> модуля `/org-structure-mgmt` — QA-прогон против мокапа `mockups/org-structure/org-structure.html`
> и P11-R1…R13, `scripts/inspect/orgstruct-*.mjs`. Найден **P11-01** (🔴 blocker) — гейт
> закрытия/ликвидации узла блокирует бездетный узел с 0 штатных единиц и 0 назначений тем же
> сообщением, что требует их наличия («есть действующие... штатные единицы; ...назначения либо
> и.о.») — противоречие подтверждено дважды (`orgstruct-leaf.mjs`, `orgstruct-verify-contradiction.mjs`);
> ни один узел за прогон не удалось ликвидировать. Плюс два косметических: **P11-02** — 4-я
> вкладка карточки узла названа «Закрытие», в спеке — «Ликвидация»; **P11-03** — живая версия
> имеет 4 верхних вкладки вместо 3-х спеки (лишняя — «Типы подразделений», рабочий CRUD, не
> дефект). Подтверждено спеке: Ставок-степпер гасится на 1 (целое ≥1), Экспорт CSV
> (UTF-8 BOM + `;` + 7 колонок, побайтово), Паспорт на дату — оба через реальные
> `download`-события, вкладка «Обзор» (6 KPI-плиток + 6 списков «Требует внимания» с
> реальными данными, пустые — «Нарушений нет», вакансия корня — отдельный баннер). Не
> проверено: 183-дневный потолок и.о. (сломался селектор дважды; попытка открыть модалку
> «И.о.: назначить» через `orgstruct-modals.mjs` упала на зависшем диалоге «Назначить» —
> похоже на баг скрипта, не факт приложения, не заносится как дефект без чистого повтора),
> один-руководитель/одно-основное-место/дубль-названия/непересечение-территорий, каскад
> резолва руководителя «передано наверх» (видна только тривиальная вакансия корня, не
> цепочка через промежуточный узел), CRUD справочника «Должности» (список отрисован,
> создание/защита удаления не нажаты), операции над узлом переименование/переподчинение/
> территория/тип (кнопки есть, ни одна не нажата), роль Наблюдатель (нет второго логина),
> P11-R12 (события — не видны из UI). `notes/qa-findings.md`
> дополнен разделом «Phase 11», `TODO.md` P11-R1/R3/R4/R7/R13 отмечены `[x]` с оговорками.
>
> _Предыдущая запись (2026-08-04, два потока за день):_ **Взыскание:** пакет передачи разработчикам
> переиздан редакцией 2 — `docs/tasks/p14-collection-tasks.html` (23 задачи, 7 этапов, JSON-остров
> по `docs/tasks/FORMAT.md`, первый файл в этом формате) + `requirements/tz/13-collection.html`
> (20 разделов, реконсилиация против ADR `0043…0054`) + реестр `TODO.md` P14-R1…R23;
> `scripts/check_tasks.py` 0 ошибок. **Кредит:** журнал догнан за 27.07…04.08 — волна приведения
> карточки к ADR-модели, подача карточки, волна ADR-0042 «план · прогноз · исполнение», реестр и
> фильтр, единый переключатель области КВ-17; смоук `credit-check.mjs` 98/98, jsdom-тесты
> `mockups/loan-credit/tests/` 38/38.

## One-line summary
АСУБК — a large, mature back-office system for a Kyrgyz state Financial-Credit
Fund, managing the full lifecycle of state-backed lending and subsidies. In
active development (extending/maintaining an existing multi-module app).

## Overall status
- **Phase:** in development (extending an existing system)
- **Health:** 🟢 on track _(provisional — refine once current work is logged)_

## What's working / done
The app is live on the test env with ~150 screens across these domains
(see [requirements/overview.md](requirements/overview.md) for the full map):
- **Приложение** — users, departments, disbursements, reserves, payments, tranches, ledger
- **Система кредитования** — gov decisions, loan programs, applications, borrowers, loans, collateral monitoring
- **Поручительства** — guarantees / sureties
- **Корпоративное управление** — company registry, KPI, financial reports, shareholders
- **СУГС** — subsidies (projects, requests, payments, refunds, reconciliations, reports)
- **Взыскание задолженности** — debt collection (pre-trial → judicial → enforcement → asset alienation)
- **Справочники / Сервисы / Администрирование / Отчёты / Безопасность / Инструменты данных**

## In progress
| Item | Owner | Started | Notes |
|---|---|---|---|
| Credit module end-to-end check (QA + docs) | admin | 2026-06-17 | Phases 1–10 docs+QA done. See TODO.md |
| Пакеты передачи по модулям (мокап + ТЗ + задачи) | admin | 2026-07-19 | Актуальны: залог (P13), заёмщик (P4-R6…R25, 2026-07-27), взыскание (P14, редакция 2, 2026-08-04), **платежи (P16, редакция 1, 2026-08-06)** — `requirements/tz/14-platezhi.html` + `docs/tasks/p16-platezhi-tasks.html`, 28 карточек / 6 этапов, `check_tasks.py` чист. Кредит (P15, 2026-07-26) отстал от макета — см. строку ниже |
| Взыскание — волна «МП» (модельные пробелы) + наряд §30 | admin | 2026-07-27 | Реестровые волны ТР…МТ, модельные ПМ/РМ/ПЕ/РР/ДС/ЗС, 5 кругов разбора МП (18 решений, ADR `0043…0054`), наряд §30 п.1–п.7 + МП-11 + «выручка/очередь» + МП-2 — все реализованы в код. `collection-check.mjs` 660/32 (32 — старый долг карточки дела/`ПЕ`, не новый), `collection-data-audit.mjs` 0 находок, `collection-seed-coverage.mjs` 43/43 0 находок. Журнал волн — `mockups/collection/ASUBK-status-razrabotki.md`. Пакет передачи P14 переиздан (см. ниже) — модуль готов к постройке |
| Кредит: доработка макета после передачи | admin | 2026-07-27 | Волны 27.07…04.08 (КВ-1…КВ-17, КР-1…КР-57) — макет ушёл вперёд пакета от 26.07. Учёт волн — `mockups/loan-credit/ASUBK-status-razrabotki.md`. Требует переиздания: `docs/tasks/p15-kredit-tasks.html` (частично догнан 03.08, legacy-формат) и `requirements/tz/05-kredit.html` |
| Реструктуризация: канон, макет, смоук | admin | 2026-08-05 | Спека `mockups/restructuring/ASUBK-restrukturizatsiya-logika.md` пересобрана с чистого листа (**РС-1…РС-42**, ADR `0092`…`0099`, `0106`, `0107`, `0109`, `0110`, `0114`), макет и смоук доведены — **68/68 PASS** (`node scripts/inspect/restructuring-check.mjs`), матрица приёмки §14. Норма получена 10.08.2026: Положение, прил. к ПКМ КР от 19.01.2026 № 14 — конвейер развёрнут до девяти стадий, решение принимает Кабинет Министров (`ADR-0107`). 12.08.2026 реализована многокредитная заявка — расчёт живёт на транше (`ADR-0114`, план `docs/superpowers/plans/2026-08-11-restructuring-multicredit.md`, PR `#25` → `main`); экраны под неё не построены (`TODO.md:P17-R2`). Открытых вопросов к заказчику нет; письмо — редакция 2. Учёт волн — `mockups/restructuring/ASUBK-status-razrabotki.md`. **Пакет передачи не начат** — ни ТЗ, ни `docs/tasks/p17-*` |
| Кураторство (P18): постановка и прототип | admin | 2026-08-13 | Административный модуль связки «Сотрудники · Задания · Кураторство», проектируется с нуля — дефектов живого стенда нет. Постановка `docs/tasks/p18-kuratorstvo-tasks.html` (редакция 1, 5 этапов, `P18-R1…R20`); **своего ТЗ у модуля нет намеренно** — источник истины постановка плюс `CONTEXT.md` и `ADR-0116…0118` поверх `0023`. Правки 15.08.2026: ключ верхней ступени — пара «объект и роль», затем пять расхождений (пересчёт, замещающий, вакансия администратора, ведущий куратор, пустой ответ). **Макет `mockups/kuratorstvo/kuratorstvo.html` переписан целиком 16.08.2026** (волна 1: ядро плюс рабочий экран «Политика распределения», этапы 1–2; четыре экрана следующих этапов — заглушки), логика исполняется, смоук `scripts/inspect/kuratorstvo-check.mjs` 41/41, инварианты `ИУ-1…ИУ-14`. Прежний прототип от 12.07.2026 был старше модели и уничтожен; археология расхождений `КУ-Д1…Д10` — в `ЗАПИСКА.md` и `ASUBK-status-razrabotki.md`, спека, на которую он ссылался, утрачена. **Пакет разработчику не передавался** |
| Платежи: волны 2–3 и перепись макета | admin | 2026-07-12 | Спека `mockups/payments/ASUBK-platezhi-logika.md` переписана двумя волнами (02.08 — 21 решение, ADR `0055…0061`; 04.08 — ре-деривация вслепую, 10 решений, ADR `0073…0076`). Макет пересобран 04.08 тремя этапами: **восемь экранов**, 5157 строк, роли куратор/бухгалтер/наблюдатель, все `ПТ-Д1…ПТ-Д26` закрыты. Смоук `scripts/inspect/payments-check.mjs` — 179/179, 0 ошибок консоли (`npm run test:payments`). Учёт волн — `mockups/payments/ASUBK-status-razrabotki.md`; «Порядок работ» закрыт целиком 05.08. **Пакет передачи готов 06.08.2026**, постановка **переписана заново 10.08.2026** — `requirements/tz/14-platezhi.html`, `docs/tasks/p16-platezhi-tasks.html` (редакция 1: 37 карточек `P16-R1…R37`, 8 этапов, номера строго по сборке; прежний файл в `docs/tasks/archive/`), `TODO.md` раздел «Платежи (целевая модель)»; `07-obsluzhivanie.md` переставлен на новое ТЗ. Модуль готов к постройке; долг — ТЗ ещё не впитало волну 4 (автосторно, раздел 4.2) |

## Blocked / waiting
| Item | Blocked by | Since | Notes |
|---|---|---|---|
| ~~МП-9 (расходы по взысканию в очереди при обычном платеже)~~ ✅ | — | 2026-08-02 → **закрыто 06.08.2026** | Очерёдность назвал Холдинг: `расходы → комиссия → ОД → проценты → пеня`, расходы первые и при обычном платеже, и при распределении выручки (п. 49). `ADR-0087`. Вопрос 3 письма 02.08 снят письмом 06.08 |
| ~~Приведение модуля взыскания к редакции Порядка №41~~ ✅ | — | 2026-08-04 → **закрыто 05.08.2026** | Все семь шагов наряда `docs/normativ/2026-08-04-sverka-red-N41.md` сделаны: 1 перенумерация · 2 1-й день просрочки · 3 пороги и паузы · 4 маршруты · 5 новые ветви и предусловия · 6 справочник видов и пересев затравки · 7 письмо ГФХ. Решения — `ADR-0062`…`ADR-0072`. Пакет передачи P14 разморожен. **§4.8 и §4.14 закрыты вне наряда 05.08.2026** (`ADR-0077` — тип проекта разводит безакцепт на два комитета; `ADR-0078` — семь «мелочей с ценой», включая разведение «направлено»/«вручено» и справочник групп платёжеспособности). Осталось **вне наряда**: отправить письмо (19 вопросов, не отправлено) и два открытых подраздела §4 — 4.9 и 4.10, оба ждут ответа ГФХ. **06.08.2026 разобран хвост справочника — кассационная ветвь** (`ADR-0079`: жалоба только на акт апелляции — п. 30.3; новый вид меры «Постановление кассационной инстанции»; восстановление решения первой инстанции гасит слой апелляции); там же найдена верная причина немых групп 4 и 5.2 — гейт `tracksClosedFor`, решение не принято. **В тот же день засеяна судебная ветка с обеих сторон** (`ADR-0080` — жалоба заёмщика, наше возражение, требуемое по статьям) и **закрыты два хвоста МП** (`ADR-0081` — затравка под четыре акта-ответа МП-3 и снятие дубля срока по п. 37, шаблонов 48 вместо 49). Полный список — §9 сверки |
| Нумерация Порядка в трёх других модулях | Таблицы соответствия для их пунктов нет | 2026-08-04 | Заёмщик (ТЗ 04 + мокап), платежи, реструктуризация цитируют тот же Порядок по прежней нумерации — перечень в §6.1 сверки. Пункты 89, 90, 93, 97 в таблице §3 отсутствуют: аналоги в №41 не установлены. `CONTEXT.md` из списка вышел — переведён и включён в охват проверки 04.08.2026 |
| Категория риска в модуле заёмщика | Отдельная волна по модулю заёмщика | 2026-08-04 | Три расхождения по одному адресу: `catByDays` живёт вторым экземпляром в `mockups/borrower/*` (+ ТЗ 04 и смоук `borrower-check.mjs` с границами 5/6/181) и начинает средний риск с 6-го дня; перевод на 181-й день описан **послаблением** (оверлей `suppress181` с дедлайном), а не отложенным событием; уровень фактора комитета задаётся записью, а не справочником п. 11. Норма (пп. 11/16.1/19.1) та же, что во взыскании; правка — не хвост шагов 2–3, а своя волна. Тот же оборот «подавление 181-го дня» стоит в постановке модуля кредита (оверлеи P15) |
| ~~Порядок статей, банкротная очередь и состав исключённых~~ ✅ (кроме «да/нет») | Ответ ГФХ, вопрос 2 в `docs/voprosy-zakazchiku/2026-08-06-platezhi.md` | 2026-08-05 → **сужено 06.08.2026** | Порядок назван Холдингом, банкротная доля идёт им же, исключённых статей нет: их роль играет заморозка начисления (`ADR-0048`). `ADR-0087`. К ГФХ остался один вопрос «да/нет» — есть ли запрещённые к погашению статьи; модуль работоспособен при любом ответе |
| ~~`N` дней таймаута и `M` дней предупреждения до автосторно~~ ✅ | — | 2026-08-05 → **закрыто 06.08.2026** | Спрашивать нечего: **автосторно упразднено** (`ADR-0088`). Ожидание бессрочно, давление даёт закрытие периода, сторно — решение бухгалтера по сводке неподтверждённых. Цена: окно ожидания выросло до месяца, компенсируется оговоркой «в т.ч. не подтверждено ЦК» во всём, что выдаётся наружу |
| ~~Справочник курсов НБ КР — интеграция или загрузка файлом~~ ✅ (остался контур) | Ответ ИТ Холдинга, вопрос 3 письма 06.08 | 2026-08-05 → **сужено 06.08.2026** | Источник — сам НБ КР (курсы публичны и первичны), файл — штатный резерв; на дату без публикации действует последний установленный (`ADR-0090`). Модуль перестал быть неработоспособным без ответа: открыт только сетевой контур |
| Имя органа по спорной пене и его учредительный акт | Ответ заказчика, вопрос 1 письма 06.08 | 2026-08-06 | Устройство решено (`ADR-0089`): орган — шестая строка справочника Р-6 с источником `внутренний акт`, рабочее имя «Комиссия по спорным начислениям». Разморозка и открытие периода из вопроса вышли — там распоряжается главный бухгалтер |
| Образец кредитного договора и карточка типовой программы | Запрос в письме 06.08 | 2026-08-06 | Сверить, что написанная в них очерёдность совпадает с названной Холдингом. На решение не влияет — подтверждение, а не источник |
| Вид проекта (п. 34) по действующим кредитам | Данные Холдинга, письмо-реструктуризация ред. 2 | 2026-08-10 | Второй пол ставки считается по виду проекта; у мигрированных кредитов атрибута нет. Модуль **не блокирован**: гейт работает по первому полу и показывает флаг «п. 34 не проверен» (`ADR-0099` §2). Жёсткая блокировка отвергнута сознательно — остановила бы работу по всей унаследованной базе |
| Более поздняя редакция Положения о бюджетных кредитах, если есть | Ответ ГФХ, письмо-реструктуризация ред. 2 | 2026-08-10 | Работаем по прил. к ПКМ КР от 19.01.2026 № 14, ред. 19.01.2026 (`docs/normativ/polozhenie-2026-01-19-N14-gl9.txt`). Смена редакции — заведение новой записи справочника, а не правка старой; прошлые сделки продолжат объясняться прежней нормой (ИР-10) |
| Возражения по семи умолчаниям модуля реструктуризации | Ответ заказчика/ГФХ, письмо `2026-08-05-restrukturizatsiya.md` ред. 2 | 2026-08-10 | Письмо переиздано умолчаниями с просьбой возразить; молчание читаем как согласие. Ни одно из семи модуль не блокирует. **Не отправлено** |
| Статья ГК КР об очерёдности при недостаточности платежа | Наша работа, не вопрос | 2026-08-06 | Норма диспозитивная (издержки → проценты → основная сумма); названный порядок ставит пеню последней и с ней не спорит. В репозитории ГК цитируется только по исковой давности — статью надо найти по тексту |

## Known issues / risks
- Workspace knowledge is freshly reconstructed from the running app, not yet
  validated against real project docs — treat the module map as a working draft.
- ⚠️ **Номера `ADR-0062…0065` у платежей сдвинуты в `0073…0076` (05.08.2026)** — те же четыре
  номера 04.08.2026 заняли обе ветки за один день (взыскание в 18:21, платежи в 21:06); git
  такое конфликтом не считает, файлы легли рядом с разными слагами. Уступили платежи по
  старшинству коммита. Ветка `worktree-payments` влита в `main` 05.08.2026 — её ссылки
  переведены на новые номера в том же слиянии (16 в `payments.html`, 3 в
  `payments-check.mjs`), смоук после перевода 191/0. Правило нумерации и карта занятых
  диапазонов — новый `docs/adr/README.md`.
- ⚠️ **`ADR-0104` занят дважды (10.08.2026, третье столкновение подряд)** — кредит
  (`0104-prognoz-ozhidanie-po-kontraktu`, коммит `8bf5aab6` 17:49, **влит в `main`**) и
  реструктуризация (`0104-penya-terminalnaya-statya`, на момент обнаружения не закоммичен).
  Уступила реструктуризация: `0104` → **`0106`**, `0105` → **`0107`**, ссылки переведены в
  спеке, макете, смоуке, `ASUBK-status-razrabotki.md`, `STATUS.md` и `ADR-0099`; смоук после
  перевода 42/42. **`0105` намеренно оставлен пустым** — ветка кредита вела волну тем же днём
  и следующий её номер по правилу как раз `0105`; **так и вышло** — `0105-procenty-na-
  fakticheskoe-telo` влит в `main` в 18:22, шаг через номер спас от четвёртого столкновения.
  Перепроверено 10.08.2026 после этого влития: `origin/main` на `0105`, `0106–0107` не заняты
  ни одной веткой, наши. Следующий свободный — **`0108`**. Причина
  повторяется третий раз: номер, не закоммиченный и не отправленный в `origin`, для соседней
  ветки не существует — заводя ADR в невлитой ветке, коммитить сразу.
- 🟢 **Модуль взыскания приведён к редакции Порядка №41 по всем содержательным расхождениям,
  которые не ждут заказчика.** Действующая —
  №41 от 06.07.2026 (`docs/normativ/poryadok-2026-07-06-N41.pdf`). Нумерация приведена
  04.08.2026 (проверка `python3 scripts/check_points.py` зелёная), тогда же
  закрыты пять содержательных расхождений из двенадцати: просрочка считается с **1-го** дня
  (вместе с автооткрытием, окном ожидания и базой половины сроков), пауза приостанавливает
  **предмет**, а не журнал, перевод на 181-й день стал **отложенным событием**, фактор
  категории работает только через комитет. 05.08.2026 шаг 4 закрыл ещё три: маршрут после
  возврата ИЛ идёт по п. 36 (пять выходов с названным сортом основания, два новых вида меры),
  ускорение стало **следствием извещения**, а не решением и не основанием открытия,
  рукопожатие при передаче признано **проектным** с единственным нормативным сроком (п. 37,
  3 р.д.). 05.08.2026 шаг 5 закрыл ещё два: смерть заёмщика стала цепочкой главы 10 из пяти
  видов мер с условным требованием запросов, банкротная ветвь получила предусловия пп. 55–57
  и порог ст. 9-1 настройкой. 05.08.2026 шаг 6 закрыл §4.15: дыра пп. 30.4/30.5 была не в
  возражении, а в жалобе заёмщика (заведены два входящих вида), вид п. 30.6 переименован по
  норме; попутно реализован `МП-3` и найден пятый вид без отвечающего акта, справочник
  73 → 79; затравка пересеяна и догнала предусловия шага 5. Шаг 7 (письмо ГФХ) закрыл наряд:
  вопросов стало 16, добавлено приложение со всеми величинами без нормативной опоры.
  05.08.2026 **вне наряда закрыт §4.8** — последнее расхождение, не ждавшее заказчика: тип
  проекта заведён фактом кредита и разводит безакцепт на комитет по администрированию
  бюджетных кредитов либо по региональному развитию (`ADR-0077`), органа по умолчанию у
  развилочного гейта нет; попутно снята выдуманная величина шаблона 13 («5 р.д. от получения
  документов от РП» → 10 р.д. от повторного направления требований), а умолчание «не-ДАК =
  региональный» вынесено вопросом 17 письма. В тот же день **закрыт §4.14** — семь «мелочей с
  ценой» (`ADR-0078`): срок акта сверки, телефонные переговоры п. 16.2 отдельным видом и
  предусловием претензии, состав доказательства доставки по каналу, разведение «направлено» и
  «вручено» (ось вручения ссылалась на п. 19.2, где о вручении нет ни слова, — настоящий адрес
  п. 20.1), закрытый справочник групп платёжеспособности из девяти значений нормы, базовая
  группа 1.1 п. 14 и второе основание паузы п. 17 со своим (отсутствующим) пределом.
  06.08.2026 разобран **хвост справочника — кассационная ветвь** (`ADR-0079`): жалоба подаётся
  только на акт апелляции (п. 30.3 — справочник противоречил собственному шаблону 28), заведён
  81-й вид «Постановление кассационной инстанции», и у него есть исход, которого нет у
  апелляции — восстановление решения первой инстанции убивает **один акт**, а не слой.
  В тот же день снят **дубль срока по п. 37** (`ADR-0081`): две строки шаблонов несли один
  абзац нормы — одна с верной нормативной базой отсчёта, другая с базой первого абзаца
  (действие куратора); слиты в одну, шаблонов 48 вместо 49, и строка остаётся каталожной,
  пока подтверждение принимающей стороны не привязано к эстафете (`ADR-0023`).
  **Кода больше не ждёт ничего** из расхождений редакции №41: остались только §4.9 и §4.10,
  оба ждут ответа ГФХ. Собственный дефект модели — **гейт треков у групп 4 и 5.2** (см. §9
  сверки), причина найдена 06.08.2026, решение не принято.
  Нумерация проверяется тем же `check_points.py` — 1890 ссылок, ❌ 0. Разбор — `docs/normativ/2026-08-04-sverka-red-N41.md`. Решения МП/МП5 и ADR
  `0043…0054` в силе; **таблица §17 спеки ревизована построчно 06.08.2026** — 18/20 решений
  сделано, карантин закрытия дела (`ADR-0044`) не реализован вовсе (врезка ранее утверждала
  обратное — исправлено), правопреемство (`ADR-0052`) частично. План
  `docs/superpowers/plans/2026-08-02-collection-model-gaps.md` несёт шапку-статус вместо
  чекбоксов по той же причине — построчные `[x]` дали бы такой же ложный сигнал.
- **19 нормативных вопросов ГФХ по взысканию не отвечены, письмо не отправлено** —
  `docs/voprosy-zakazchiku/2026-08-04-vzyskanie.md` (карантин закрытия дела, дата исполнения
  платежа, очередь расходов при обычном платеже, очередь при реализации через судисполнителя,
  ликвидация ЮЛ и начисление, порог подачи повторного иска, сроки повторного предъявления ИЛ,
  1-й день просрочки и окно ожидания, подтверждение передачи вместо п. 98, ускорение долга <!-- check_points:ignore -->
  без залога, приостановка по гарантийному письму, конфликт интересов, **величина порога
  ст. 9-1**, **«фаза взыскания»**, **«куратор»**, **«комиссии»**, **тип проекта вне двух
  ветвей п. 16.5**, **группа платёжеспособности в досудебной работе**, **предел отсрочки по
  второму основанию п. 17**) + запрос шести документов,
  на которые Порядок ссылается (в т.ч. Инструкция по группам платёжеспособности). Приложение
  письма — все двенадцать величин модуля без нормативной опоры; пустая среди них одна (порог
  ст. 9-1). **Вопрос 16 — единственный, меняющий устройство** (комиссия шестой статьёй
  требования): сегодня «сборы» свёрнуты в «Расходы», которым п. 48 даёт первую позицию.
  (карантин закрытия дела, дата исполнения платежа, очередь расходов при обычном платеже,
  ликвидация ЮЛ и начисление, порог подачи иска по непредъявленной просрочке, срок предъявления ИЛ).
- **3 вопроса по платежам не отвечены** — `docs/voprosy-zakazchiku/2026-08-06-platezhi.md`
  (редакция 2, заменила письмо 05.08 из восьми вопросов): **имя органа по спорной пене и его
  учредительный акт** (заказчик) · **есть ли статьи, запрещённые к погашению в процедуре
  банкротства** — да/нет (ГФХ) · **разрешён ли исходящий доступ сервера к `nbkr.kg`** (ИТ).
  Плюс запрос двух документов — образец кредитного договора и карточка типовой программы.
  **Ни один из трёх модуль не блокирует**, включая справочник курсов: он перестал быть внешней
  зависимостью без ответа (`ADR-0090` — источник назван, файловый путь резервный). Из письма
  02.08 живут два: момент исполнения обязательства и ликвидация ЮЛ; место расходов снято.

## Recent changes (changelog)
_Newest first._
- 2026-08-11 — **Кредит: разобрана логика изменения условий — три ADR и три задачи.**
  Разбор начался с вопроса «нужна ли кнопка „Изменить условия“» и вскрыл дыру: формы п. 89 <!-- check_points:ignore -->
  меняются двумя путями — конвейером реструктуризации (согласия обеспечителей, регуляторные
  пределы, постановление Кабмина) либо прямым ДС в карточке кредита за две минуты. Различить
  пути нечем: `agreements[].source` — свободная строка, которую никто не читает.
  - **`ADR-0111`** — у ДС появляется **вид** из закрытого справочника (реструктуризация ·
    досрочное погашение · перевод долга · прочее изменение условий · без изменения условий),
    и вид решает, какие параметры документ вправе тронуть. **Г-23**: прямое ДС не трогает зону
    п. 89. Гейт стоит на виде, а не на наборе параметров, — иначе он сломал бы досрочку, <!-- check_points:ignore -->
    которая законно правит `term` мимо конвейера (`ADR-0074`). Граница зоны проведена по
    эффекту на поток погашения: `payDay` и `freq` в норме не названы, но переносят срок и
    переписывают график; ставка пени, наоборот, названа только как предмет капитализации и
    потоком погашения не является. В реестр `PARAMS` добавляются `earlyRepayMoratorium` и
    `earlyRepayFee` — условия транша по `ADR-0074`, так и не заведённые.
  - **`ADR-0112`** — Г-21 («отмена — новая запись») разведён на два механизма. Опечатка
    лечится **корректировкой**: новый тип записи, наследует основание и дату вступления,
    меняет только значение, прежняя запись остаётся зачёркнутой, журнал читает «исправление».
    **Г-24** сторожит: основание неизменно, примечание обязательно и называет пункт документа,
    дата в закрытом периоде блокирует до открытия распоряжением главного бухгалтера.
    Расторжение ДС — **обратные записи датой расторжения**; прошлое не переписывается, срез на
    дату действия отдаёт условия документа. Слабое место названо прямо: внутри открытого
    периода единственный сторож — примечание, машинного нет и быть не может.
  - **`ADR-0113`** — ретро-расхождение «условия изменены, график не перестроен» перестаёт быть
    баннером и **запирает закрытие периода** наравне с невыясненными поступлениями и платежами
    в ожидании ЦК (`ADR-0088`: период — единственное давление на неподтверждённое). Начисление
    и приём платежей не блокируются. Шов назван — `creditPeriodBlockers(период)`, кредит
    публикует, защёлка читает (`ADR-0014`).
  - Правки: `CONTEXT.md` — два термина («Вид допсоглашения», «Корректировка») плюс дополнение
    «Основания изменения условий» и «Закрытия периода»; `ASUBK-kredit-logika.md` — §5.1…§5.4 и
    инварианты **И-19…И-22**; реестр гейтов — §1.1 с заявленными Г-23/Г-24; спека вкладки
    условий — поправка §3.2: `dayMethod` в реестре параметров и в коде, исходные «десять
    параметров» разошлись с поставленными шестнадцатью, и прав код. `TODO.md` и
    `docs/tasks/p15-kredit-tasks.html` — `P15-R26`…`P15-R28` (28 карточек, файл legacy-формата
    догнан вручную).
  - **Открытый вопрос заказчику:** есть ли у бюджетного кредита мораторий на досрочное
    погашение и комиссия за него — глава 9 Положения молчит. Идёт письмом, модуль не держит.
- 2026-08-10 — **Реструктуризация: найдена норма, все восемь вопросов заказчику закрыты,
  конвейер развёрнут до девяти стадий.** Получено **Положение о работе с бюджетными кредитами**
  — приложение к постановлению Кабинета Министров КР от 19.01.2026 № 14, редакция 19.01.2026;
  извлечение гл. 9 (пп. 81–96) и п. 34 — `docs/normativ/polozhenie-2026-01-19-N14-gl9.txt`. <!-- check_points:ignore -->
  Это закрыло вопрос 8 письма 05.08, на котором висели два жёстких гейта: значения совпали с
  зашитыми, номера пунктов (90 — срок, 92 — ставка) тоже, пометка «пункт не подтверждён»
  снята. Семь остальных вопросов закрыты **собственными решениями** — **РС-26…РС-32**:
  порог расхождения «срез ↔ факт» в две ступени по проценту **либо** абсолюту (10 %/500 тыс. →
  виза куратора с обоснованием; 25 %/5 млн → возврат на комитет, регистрация ДС заблокирована);
  **пеня — терминальная статья** (`ADR-0106`) — ни проценты на неё, ни пеня на неё, режим
  датирован справочником, а капитализация разводит проценты и пеню по разным счётчикам, как
  требует «отдельной суммой» п. 89 пп. 4; **прощение недоступно, пока сумма не названа в акте** <!-- check_points:ignore -->
  (ИР-13) — отдельного акта комитета не заводим, но акт, разрешающий прощение, обязан содержать
  прощаемое; гейт покрытия снимает **комитет по администрированию бюджетных кредитов** (КД-10 —
  роли «залоговая комиссия» в системе нет, из макета вычищена), и снятие **раскрывается в
  пакете к письму**, потому что п. 86 даёт уполномоченному органу право требовать залоговые <!-- check_points:ignore -->
  предложения; **производственный календарь** — справочник дат, общий с платежами, незаведённый
  год даёт видимую отметку, а не молчаливый откат к «сб/вс»; **пять нормативных видов п. 89 <!-- check_points:ignore -->
  замкнуты** от правки и удаления, прочее заводится как «иное обоснованное предложение
  (п. 89 пп. 5)» с обязательным обоснованием (ИР-14); **повторность — сигнал с третьей сделки, <!-- check_points:ignore -->
  не гейт**: маршрут установлен п. 82, надстраивать над ним свою ступень Холдинг не вправе, но <!-- check_points:ignore -->
  и невидимой многократная реструктуризация быть не должна. Из самой нормы пришли
  **РС-33…РС-37**, которых до 10.08 некому было задать. Главное — **`ADR-0107`**: решение
  принимает **Кабинет Министров по согласованию с профильным комитетом Жогорку Кенеша** (п. 82), <!-- check_points:ignore -->
  а уполномоченный орган даёт лишь заключение о возможности (п. 87) либо отрицательное <!-- check_points:ignore -->
  заключение-отказ (п. 88); конвейер развёрнут с пяти стадий до **девяти**, ДС регистрируется <!-- check_points:ignore -->
  под номером и датой постановления КМ (ИР-12), отрицательное заключение закрывает заявку до
  Кабмина, инициатор проекта решения (отраслевой орган · ОМСУ через полпреда Президента ·
  уполномоченный орган, п. 83) стал реквизитом заявки, а срок 70 к.д. остаётся **один на всю <!-- check_points:ignore -->
  цепь** — своих сроков новым стадиям норма не даёт, и выдумывать их значило бы зажечь флаг
  просрочки по несуществующему правилу. `ADR-0099` уточнён в трёх местах: полов ставки **два**,
  применяется больший — `max(первоначальная × 0,5; минимум по виду проекта п. 34)`, вид не
  указан или ставка внешняя ⇒ флаг «п. 34 не проверен», не блокировка (у мигрированных кредитов
  атрибута нет); пакет документов нормативен (пп. 85–86) и несёт условные строки пп. 91/95 при <!-- check_points:ignore -->
  снижении ставки; предел срока меряется от **остатка задолженности** (п. 90 дословно), а не от <!-- check_points:ignore -->
  суммы переноса — прежнее чтение было нашим и исправлено по тексту. Правки: два новых ADR
  (`0106`, `0107`) + уточнение `0099`; спека — §3, §4, §5, §6.7, §8.2, §9, §10 (ИР-11…ИР-14),
  §11 (таблица закрытия восьми вопросов), §12, §13, §14 (**сценарии 30–42**) и журнал решений;
  макет `restructuring.html` (девять стадий, вкладка «Комитет и решение» вместо «Минфин», три
  акта с реквизитами, две ступени дрейфа, `forgiveGate`, `accrualModeOf`, происхождение видов,
  производственный календарь, признак повторности); смоук **29 → 42 сценариев, 42/42 PASS**;
  `ASUBK-status-razrabotki.md` и `STATUS.md`. Письмо `2026-08-05-restrukturizatsiya.md`
  переиздано **редакцией 2**: не восемь вопросов, а семь принятых умолчаний с просьбой
  возразить (молчание = согласие) + четыре решения из нормы к сведению; к Холдингу остались две
  просьбы, ни одна модуль не блокирует — более поздняя редакция Положения, если есть, и **вид
  проекта (п. 34) по действующим кредитам**. Пакет передачи по модулю (ТЗ + `docs/tasks/p17-*`)
  **не начат**.
- 2026-08-10 — **Платежи: постановка переписана с нуля, редакция 1 (P16).**
  `docs/tasks/p16-platezhi-tasks.html` — **37 карточек `P16-R1…R37` в 8 этапах** (Каркас: три
  сущности денег → Внешний факт: реестр ЦК и сверка → Опознание плательщика и очередь погашения →
  Разнесение по матрице → Слои долга → Остаток, возврат, списание → Ожидание казначейства и
  спорная пеня → Заморозка, период, акты). Номера идут **строго по сборке**: зависимость каждой
  карточки указывает только назад, ID возрастают в порядке файла — прежняя редакция шла по этапам,
  а ID в реестре скакали (R26→R29→R27→R28). Прежний файл убран в
  `docs/tasks/archive/p16-platezhi-tasks-red-2026-08-06.html`; таблицы соответствия старых и
  новых ID **нет намеренно** — редакция самостоятельная.
  **Внесена волна 4** (`ADR-0087…0091`), которой прежняя редакция не знала: автосторно по
  таймауту упразднено вместе с параметром `payment.gateway.confirmTimeoutDays` (было в старых
  `R9`/`R10`/`R24`), ожидание бессрочно, сторно — решение бухгалтера при закрытии периода, каждая
  выдаваемая наружу цифра несёт строку «в т.ч. не подтверждено казначейством»; два замка
  (разморозка — запись журнала главного бухгалтера, открытие периода — распоряжение с номером,
  датой и вложением); курс НБ КР — сайт как первичный источник, файл как штатный резерв; порог
  0,01 живёт внутри разнесения и поглощается в обе стороны, а кредит закрывается строгим нулём
  (закрывает `E2E-13/14`).
  Снято: путеводитель ужат до четырёх голов, `changelog` при редакции 1 (бессмыслен), имена
  функций макета из текста, одинаковый `look.mock` без маршрута. Добавлено: **12 «Ловушек»**
  (в прежней редакции их не было ни одной) и **8 открытых вопросов** во врезках, у каждого
  назван умолчательный ответ и цена другого. Блокирующих вопросов у модуля не осталось.
  `check_tasks.py` — 0 ошибок/0 предупреждений; `check_points.py` — 1906 ссылок «п. NN», 0 мёртвых
  в редакции №41. `TODO.md` получил новый реестр из 37 строк; диапазоны задач в
  `requirements/tz/14-platezhi.html` и `requirements/tz/07-obsluzhivanie.md` переведены на
  `P16-R1…R37`. **Долг:** само ТЗ волну 4 ещё не впитало — автосторно живёт в разделе 4.2 и в
  шести местах рядом; в шапку ТЗ добавлено предупреждение с перечнем затронутых разделов, до
  выпуска редакции 2 действует постановка.
- 2026-08-10 — **Взыскание: постановка переписана с нуля, редакция 1 (P14).**
  `docs/tasks/p14-collection-tasks.html` — **32 карточки `P14-R1…R32` в 8 этапах** (Модель и
  каркас → Открытие и рабочие экраны → Журнал мер → Коллегиальные органы → Суд и слой долга →
  Деньги и закрытие → Особые состояния → Сроки, передача, настройки). Номера идут **строго по
  сборке**: зависимость каждой карточки указывает только назад, ID возрастают в порядке файла —
  прежняя редакция шла по этапам, а ID в реестре скакали (R8→R29→R9), из-за чего сборочный
  порядок читался только по графу. Прежний файл убран в
  `docs/tasks/archive/p14-collection-tasks-red-2026-08-09.html`; таблицы соответствия старых и
  новых ID **нет намеренно** — редакция самостоятельная. Снято: дубль путеводителя (пересказывал
  правила карточек), 33 имени функций макета в тексте постановки, одинаковый `look.mock` на всех
  карточках, дублирующиеся якоря ТЗ (`#s8-1`, `#s18` стояли по два раза), хроники ревью в «Ловушках»
  и идентификаторы ADR (внешнему подрядчику нечитаемы). Пять размазанных по разным карточкам
  правил свёртки судебной цепочки собраны в одну опорную `P14-R18`. **15 открытых вопросов**
  вынесены из прозы в отдельные врезки, у каждого назван умолчательный ответ и цена другого
  ответа. Тело карточек: медиана 219 слов, потолок 360 при лимите 400 формата.
  `check_tasks.py` — 0 ошибок/0 предупреждений, `check_points.py` — 70 ссылок «п. NN» из файла
  живы в редакции №41. `TODO.md` получил новый реестр из 32 строк; 42 ссылки на задачи в
  `requirements/tz/13-collection.html` переведены на новую нумерацию (отображение не
  механическое — часть старых ID расходится на два новых).
- 2026-08-06 — **Платежи: пакет передачи (P16).** `requirements/tz/14-platezhi.html` (13 разделов —
  три сущности денег, сверка/шлюз, разнесение, слои долга, невыясненные/переплата/возврат,
  заморозка/акт сверки/права, счета/курс, восемь экранов, открытые вопросы, влияние на макеты)
  и `docs/tasks/p16-platezhi-tasks.html` (28 карточек `P16-R1…R28` в 6 этапах — Каркас и три
  сущности → Сверка и шлюз → Разнесение и слои долга → Невыясненные/переплата/возврат →
  Заморозка/акты/права/счета → Целостность и границы), JSON-остров по `docs/tasks/FORMAT.md`,
  `check_tasks.py` 0 ошибок/0 предупреждений на обоих проверяемых файлах пакета. `TODO.md`
  получил раздел «Платежи (целевая модель)» между «Кредит» и «Заёмщик» — флоский реестр,
  прозы нет, тело задач живёт только в HTML. `07-obsluzhivanie.md` переставлен на новое ТЗ.
  Все восемь вопросов §11 спеки несут дефолт, ни одного «уточняется». Граница с зеркалом
  платежа в кредите (`P15-R15/17/22`) зафиксирована в `notIn` карточек, не задублирована.
  Коммит `d18b11c`.
- 2026-08-06 — **Платежи, волна 4: разобраны восемь открытых вопросов §11, письмо ужато до трёх.**
  Один вопрос закрыт **ответом Холдинга** — очерёдность погашения `расходы → комиссия → ОД →
  проценты → пеня`; владеет ею кредитная программа, при рассмотрении заявки меняется реквизитом
  решения, после подписания **неподвижна** (иначе смена задним числом молча переписала бы
  разбивку всех незамороженных платежей — `ADR-0055`). Четыре закрыты **своими решениями**:
  **автосторно упразднено** (`ADR-0088`) — таймер сам производил работу для комиссии, значительная
  часть которой отменялась поздним подтверждением; ожидание стало бессрочным, давление даёт
  закрытие периода, сторно — решение бухгалтера по сводке, а цена (окно до месяца) закрыта
  оговоркой «в т.ч. не подтверждено ЦК» во всём, что выдаётся наружу. **Замка стало два**
  (`ADR-0089`): спорную пеню решает орган — шестая строка справочника Р-6 с источником
  `внутренний акт` (справочник взыскания перестал быть закрытым и получил реквизит источника),
  разморозку и открытие периода разрешает главный бухгалтер — журналом и распоряжением с
  вложением. **Курсы** (`ADR-0090`) берутся у самого НБ КР, файл — штатный резерв, на дату без
  публикации действует последний установленный. **Порог копейки** (`ADR-0091`) перенесён из
  закрытия кредита в разбивку и поглощается в обе стороны — кредит закрывается строгим нулём,
  `E2E-13/14` лечится по построению, а `ADR-0076` сужен до настоящих списаний. Побочно: **термин
  «обратная очередь» упразднён** — он совпал с прямой, у `ADR-0073` осталось два режима; исключённая
  статья в банкротстве не нужна, её роль играет заморозка начисления (`ADR-0048`), и `ADR-0059`
  сужен. Правки: пять ADR `0087`…`0091` + врезки в `0058`, `0059`, `0061`, `0073`, `0076`;
  `CONTEXT.md` (новый термин + семь переписанных статей); спека §2.4, §4.2–4.4, §5, §7.2, §8.1,
  §8.5, §11 и журнал волны 4 (решения 58–68); `payments.html`; смоук **214/0** (было 191);
  `ASUBK-status-razrabotki.md` — `ПТ-32`…`ПТ-42`. Письмо: `2026-08-06-platezhi.md` — три коротких
  вопроса + запрос двух документов, редакция 05.08 помечена заменённой. `check_points.py`
  **1283 ссылки ❌ 0**. **Находка не по теме:** `ADR-0079` занят дважды — `worktree-vzyskanie`
  (`kassacionnaya-vetv`) и `worktree-restructure` (`restrukturizaciya-delit-transh`), обе ветки
  не влиты; описано в `docs/adr/README.md`, разрешают владельцы веток.
- 2026-08-06 — **reqId-коллизия (МП-О1) закрыта.** `mkReq()` (`collection.html:7224`) строила
  id требования из `ROLE_CODE[role]` — буквы роли, а не лица; двое обеспечителей одной роли
  (напр. два `'поручитель'`) на одном кредите получали одинаковый id и второй молча затирал
  первого в `REQ_INDEX` (объект в `p.requirements` оставался, но был недостижим по id).
  `ADR-0024`/`ADR-0002` (М-1) требуют «дело × кредит × **обязанное лицо**». Правка: третий
  сегмент id — `personId` (уже уникален, уже передавался в функцию, просто не читался в
  формуле id) для всех ролей кроме `'заёмщик'`, который остаётся с кодом `'з'` — заёмщик на
  кредите один, коллизии там не было и формула его не тронута (совместимость с ad hoc
  шаблоном `reqIdBorrower`, `collection.html:4800`). Латентный баг — в текущей затравке нет
  дел с двумя обеспечителями одной роли на одном кредите, живых данных не портил. Смоук
  пересобран: формула-тест переписана под новое правило, 25 хардкод-id `307/307/п` →
  `307/307/п-307`, самоконтрольный regex файла расширен под новый хвост. Прогон:
  `collection-check.mjs` 810/0/0 (после кодовой правки временно 19 провалов — все от старой
  формулы в тестах, не от бага, — исправлены правкой смоука);
  `collection-data-audit.mjs` 0 находок/107 дел/116 требований;
  `collection-seed-coverage.mjs` 48 ситуаций/0 тонких. Без ADR — починка под уже принятым
  `ADR-0024`. Новый `TODO.md`-пункт не заведён. Журнал —
  `mockups/collection/ASUBK-status-razrabotki.md`, запись «Правка reqId-коллизии (МП-О1)».
- 2026-08-06 — **Ревизия статуса МП: таблица §17 и план `2026-08-02-collection-model-gaps.md`
  сверены построчно с кодом, 18/20 решений сделано.** Последний пункт согласованного порядка
  волн. Метод — `grep` на характерные идентификаторы плюс прямое чтение функций, которые
  решение должно реализовывать (`measureGate`, `PRECONDITIONS`, `respFor`, справочник видов
  мер); код не менялся, регрессия не перезапускалась. Карантин закрытия дела (`ADR-0044`)
  нигде не реализован — ни слова «карантин», ни `RULES.quarantineDays` в `collection.html`;
  собственная врезка §17 утверждала обратное («живёт в макете»), исправлено на месте.
  Правопреемство (`ADR-0052`) — частично: терминал «Правопреемство» заведён
  (`measureSetsTerminal`), а разветвление требования на нескольких преемников осознанно
  отложено (комментарий у справочника: «наряд п.6, отдельная будущая карта»). Вне таблицы
  решений: формула `reqId` для двух поручителей одной роли на одном кредите (план, Task 8
  Step 1/`ADR-0024`, МП-О1) по-прежнему не исправлена, коллизия жива
  (`` id: `${p.id}/${c.id}/${ROLE_CODE[role]||'з'}` ``, `collection.html:7224`); соседний
  пункт того же шага — подписант по подразделению кредита (МП-О5) — рядом уже сделан
  (`respFor`, `collection.html:4239`). Оба открытых гэпа уже покрыты `TODO.md` (`P14-R4`,
  `P14-R14`) — новых пунктов не заведено, нового ADR не заведено (сверка факта, не решение).
  Изменения: §17 мокапа (`ASUBK-vzyskanie-logika.md:2062-2100`), журнал (запись «Ревизия
  статуса МП»), шапка-статус плана `docs/superpowers/plans/2026-08-02-collection-model-gaps.md`.
  Коммит `a3f6394`.
- 2026-08-06 — **Четыре акта-ответа МП-3 получили затравку, снят дубль срока по п. 37
  (`ADR-0081`).** `ADR-0072` завёл четыре вида внешних актов — ответов суда на наши
  процессуальные обращения, — но ни одно дело затравки их не поднимало: слоевое следствие двух
  отмен было объявлено (вид стоял в `AWARD_DEATH_KINDS`), а не показано на живых данных.
  Форма пары одинакова у всех — «наше обращение → внешний акт», исход несёт акт; расходятся
  они тем, что делают с деньгами: обе отмены решения слой **гасят, а не правят** (акт отмены
  ничего не присуждает, и правая колонка сверки после него **пуста, а не нулевая** — та же
  находка, что у отмены кассацией в `ADR-0080`), восстановление срока и изменение способа
  исполнения денег не трогают вовсе. Границы взяты из справочника: изменение способа стоит на
  **исполнительном листе**, а не на решении, а заявление о восстановлении по **п. 30.6**
  подаётся одновременно с процессуальным действием — билдер требует нашей апелляционной жалобы
  и датирует заявление её днём; обе опции бросают ошибку затравки, если основания в цепочке
  нет. Новые коды каталога — **`K3-ПЕРЕСМОТР`** (дело **368**: решение присудило 250 000,
  определение об отмене гасит слой; дело **369**: заявление подано, суд не ответил — слой
  стоит на решении, обращение висит без акта) и **`K3-ХОДАТАЙСТВА`** (**374** — заявление о
  восстановлении срока в один день с апелляционной жалобой, срок восстановлен; **381** —
  заявление об изменении способа на исполнительном листе, способ изменён). Отдельным ассертом
  закреплено то, чего модель НЕ делает: ни один из четырёх актов не несёт `setsPhase` — откат
  фазы после отмены решения не построен (осознанное состояние `ADR-0072`), маршрут после
  отмены назначает куратор, и молча это состояние теперь не изменится. Вторая половина волны —
  **дубль срока по п. 37**: строки шаблонов 29 и 49 несли один абзац нормы (один орган под
  двумя именами, та же величина 3 р.д.) и расходились только базой отсчёта, причём верной была
  база младшей строки. Слиты в строку 29 с нормативной базой, шаблонов **48** вместо 49;
  строка остаётся каталожной — вида меры под подтверждение принимающего департамента нет, и
  привязка к рукопожатию эстафеты (`ADR-0023`) названа отдельным решением. Довод шага 4
  наряда, которым вторая строка и была заведена («срок норма назначает ровно одному
  переходу»), её же и опровергает. Прогон: `collection-check.mjs` **809/32** (10 новых
  проверок, список провалов побитово равен baseline), `collection-data-audit.mjs` 0 находок ·
  107 дел · 116 требований, `collection-seed-coverage.mjs` **48 ситуаций** · 0 тонких,
  `check_points.py` 1890 ссылок ❌ 0, `check_tasks.py` 0 ошибок. Пакет передачи: ТЗ получило
  подраздел про акты пересмотра, задачи — **P14-R28**.
- 2026-08-06 — **Судебная ветка засеяна с обеих сторон: жалоба заёмщика, возражение и
  требуемое по статьям (`ADR-0080`).** Шаг 6 наряда (`ADR-0071`) завёл в справочник обе жалобы
  заёмщика (пп. 30.4/30.5) и оба «Возражения», но затравки под них не было ни одной: билдер не
  клал ни одной жалобы заёмщика, обязанность возразить не поднималась ни разу. Волна `ADR-0079`
  этого закрыть не могла — она разбирала АКТ инстанции, а не подателя жалобы. Ветка зеркальна
  нашей и в трёх местах ей не равна: **повод обратный** — наша жалоба требует неблагоприятного
  исхода, а п. 30.4 даёт заёмщику повод только против решения, «в котором были удовлетворены
  исковые требования» (граница заведена константой `REJECTED_VERDICT`, читаемой из справочника
  исходов «Решения суда», с падением при загрузке, если такого исхода в справочнике не станет);
  между жалобой и актом стоит **наше возражение** — единственное, что норма требует от нас по
  чужой жалобе, и **срока у него нет вовсе** («в соответствии с процессуальным
  законодательством», `ADR-0068`), поэтому шаблона не заведено и это закреплено ассертом, чтобы
  срок не появился «для полноты»; акт инстанции ссылается `basedOn` **на жалобу, а не на
  возражение** — акт выносится по жалобе, возражение довод в том же деле. Три `throw` билдера
  держат полярность: жалобы обеих сторон на одном решении, жалоба заёмщика против отказа в иске,
  кассация заёмщика без акта апелляции. Затравка получила код **`K3-ЖАЛОБА-ЗАЁМЩИКА`** — дело
  **366** (решение полностью в нашу пользу, апелляция по жалобе заёмщика оставила его в силе:
  слой держится актом ПЕРВОЙ инстанции, `РС-366` = 240 000, акт пересмотра своего слоя не
  заводит) и **367** (заёмщик проиграл апелляцию и пошёл в кассацию по п. 30.5, кассация
  «отменено, в иске отказано» — слой гаснет тем же общим правилом отмены, фаза откатывается на
  «Иск»). Вторая половина волны — **`m.claims`**: до неё требуемое по статьям не нёс НИ ОДИН
  из 101 дела, таблица «требовали → присудили» показывала заглушку «сверять пока нечего»
  везде, а согласие свёрток `claimChainsFor` и `layerOf` снималось подставленным объектом с
  откатом. Опция `claimed` кладёт разбивку на корень цепочки (иск) пяти дел — 332 (требовали,
  присуждения ещё нет), 346, 347, 366, 367, — и проверка переехала на живые данные, подстановка
  снята. Находка волны: после отмены цепочка заканчивается АКТОМ ОТМЕНЫ, и правая колонка
  **пуста, а не нулевая** — «статья не присуждалась» ≠ «присуждено 0»; закреплено ассертом.
  Прогон: `collection-check.mjs` **797/32** (16 новых проверок, список провалов побитово равен
  baseline), `collection-data-audit.mjs` 0 находок · **103 дела · 112 требований**,
  `collection-seed-coverage.mjs` **46 ситуаций** · 0 тонких, `check_tasks.py` 0 ошибок.
  Переписаны девять счётчиков (сроки 91→92 и 62→63 просроченных, глубокий край очереди
  −155→−160, «сейчас …» 116→120 из 194 строк, комитет 76→78 и 69→71 положительных, иски
  53→55). Пакет передачи: ТЗ получило подразделы 9.6 и 12.4, задачи — **P14-R26** и **P14-R27**.
- 2026-08-06 — **Кассационная ветвь разобрана: хвост справочника видов (`ADR-0079`).** Шаг 6
  наряда завёл жалобы (пп. 30.3/30.5), но акт кассационной инстанции сознательно отложил —
  его исходы и действие на слой были неизвестны. Разбор нашёл **противоречие справочника
  самому себе**: основанием «Кассационной жалобы» стояли акты ПЕРВОЙ инстанции, тогда как
  шаблон срока 28 отсчитывает три месяца от «даты вынесения акта апелляционной инстанции» —
  ровно как велит **п. 30.3** («судебный акт апелляционной инстанции»). Основание сужено до
  единственного — «Постановление апелляционной инстанции»; зеркало у жалобы заёмщика (п. 30.5)
  было записано верно ещё шагом 6, ошибка жила только у нашей. Заведён **81-й вид меры —
  «Постановление кассационной инстанции»**, пять исходов. Содержательное отличие от апелляции:
  исход **«апелляция отменена, решение первой инстанции в силе»** убивает не слой, а **один
  акт** — денежный слой возвращается к решению первой инстанции, чего у апелляции произойти
  не может. Выражено свёрткой `dropRestoredAppeals`, общей у `layerOf` и `claimChainsFor`
  (одно место, а не две копии); формулировка исхода намеренно **не** начинается на «отменено»,
  потому что этот префикс читает предикат отмены — закреплено ассертом, а не соглашением.
  Исходы обеих инстанций — **проектное решение, не норма**: Порядок обязывает подать (п. 30.3)
  и о результатах молчит; вопроса ГФХ не заводили — исходы инстанций процессуальные, не
  ведение Холдинга. Затравка получила код **`K3-КАССАЦИЯ`** и дела **346** (восстановление)
  и **347** (отмена, на новое рассмотрение) — с ними **впервые проехал денежный слой на
  seed-данных**: до сих пор `awards` носил только акт утверждения мирового, и машинерия
  `corrects` из МП-2 жила лишь в коде. Попутно вскрыт **настоящий дефект групп 4 и 5.2**
  (`ADR-0078` называл причину неверно, поправлено): терминальные кандидаты есть у восьми дел,
  все восемь глушит `tracksClosedFor === false` — дело с актом Кабмина о списании читается
  как «2.2. Проводится работа по исполнительным листам». Два способа починки названы (закрыть
  треки в затравке либо заставить списание/безнадёжность закрывать их самим; п. 65 за второе),
  **выбор не сделан** — волна заменила неверную причину на верную и заперла её тремя
  ассертами. Прогон: `collection-check.mjs` **781/32** (19 новых проверок, список провалов
  побитово равен baseline), `collection-data-audit.mjs` 0 находок · 101 дело · 110 требований,
  `collection-seed-coverage.mjs` 45 ситуаций · 0 тонких, `check_points.py` 1749 ссылок ❌ 0,
  `check_tasks.py` 0 ошибок. Пакет передачи: ТЗ получило подраздел 9.5, **P14-R25** новая,
  **P14-R12** переиздана и переименована («два акта пересмотра: правка и отмена слоя»).
- 2026-08-05 — **§4.14 сверки закрыт: «мелочи с ценой», семь точек (`ADR-0078`). Расхождений
  редакции №41, ждущих кода, не осталось.** Точки: **тпл 38** — «до 1 июля» было нашей
  величиной, п. 9 даёт «до 1 сентября текущего календарного года». **Телефонные переговоры
  (п. 16.2)** — норма велит куратору провести их и лишь потом, в срок до 10 р.д., направить
  претензию; в модели они упоминались ровно один раз, в тексте закрытия окна ожидания, то
  есть в чужом сюжете и без даты. Заведён вид меры, стал первой ступенью маршрута (`ТЛФ-…`,
  1-й день просрочки, `gap:0` — даты затравки не сдвинулись), у первичной претензии появилось
  предусловие; **вехой вид не является** и требует ФАКТА, а не успеха: исход «связь с
  заёмщиком не установлена» предусловие удовлетворяет, иначе недоступный заёмщик закрыл бы
  взыскание сам себе. **Доказательство доставки (п. 16)** — реквизит канала, а не одна строка
  на все: справочник `DELIVERY_PROOF` (бумага — уведомление либо отметка о получении; СЭД —
  электронный отчёт, бумажных уведомлений не требуется), поле документа было свободным текстом,
  стало выбором из набора канала, добавлены два гарда на сохранении. Прежние значения
  («квитанция СЭД», «акт вручения нарочно») нормой не назывались вовсе — «акта вручения» она
  не знает. **Главная находка:** ось вручения в шести местах ссылалась на **п. 19.2**, где о
  вручении **нет ни слова** (там состав пакета в ДПО, претензия в нём — «заверенная копия»).
  Настоящий адрес — **п. 20.1**, и последствие другое: срок должника 14 к.д. течёт от
  НАПРАВЛЕНИЯ и невручением не отменяется, а вручением живёт ПАКЕТ В ДПО («возвращает
  материалы на доработку… в случае отсутствия документа, подтверждающего её вручение»).
  Выражено новым полем предусловия `servedKinds` — единственным, читающим пометку доставки, —
  на «Служебной записке в ДПО»; порога эскалации у неё намеренно нет, она бывает и по
  залоговому треку, и по судебному. **Группы платёжеспособности** — было восемь наших строк,
  среди них сквозные имена СТАДИЙ, и справочника не существовало вовсе; заведён `PAY_GROUPS`
  из девяти значений нормы с кодами и пунктами перевода (1.1 п. 14 · 2.1 п. 21 · 2.2 п. 29 ·
  2.3 п. 58 · 3.1 п. 59 · 3.2 п. 61 · 4 п. 63 · 5.1 п. 64 · 5.2 п. 65). По существу:
  завершённое банкротство больше не «безнадёжное», списание — своя группа, банкротство
  разведено на инициирование и процедуру по виду открывающей меры. **Досудебная стадия и
  наблюдение читаются как 1.1** — буквально: п. 14 присвоил, а следующий перевод норма
  называет только на иске, и потому заёмщик с просрочкой и запущенным безакцептом формально
  «производит погашение по графику»; расхождение внутри самой нормы, вынесено **вопросом 18**
  письма. **Второе основание паузы п. 17** — риски срыва государственных, национальных и
  целевых программ; тот же предмет и тот же комитет, но предел 70 к.д. норма привязала
  дословно к гарантийному письму, а для рисков не назвала ничего (**вопрос 19**). Отсюда два
  типа паузы, а не поле «основание»: предел — реквизит типа. Затравка получила код
  `K1-ПАУЗА-ПРОГ`, дела **328** (региональный проект, 111 к.д.) и **329** (проект ДАК,
  133 к.д.) — обе отсрочки длиннее семидесяти намеренно. Прогон: `collection-check.mjs`
  **762/32** (36 новых проверок; список провалов побитово равен baseline 726/32 — старый долг
  карточки дела/`ПЕ` не сдвинулся; девять ожиданий переписаны под новое поведение, ни одно не
  отключено), `collection-data-audit.mjs` 0 находок · 99 дел · 108 требований,
  `collection-seed-coverage.mjs` 44 ситуации · 0 тонких, `check_points.py` 1664 ссылки · ❌ 0,
  `check_tasks.py` 0 ошибок. **Известная дыра, названная ассертом:** группы «4. Безнадёжные
  долги» и «5.2. Списанные» затравкой не покрыты — в ней нет ни одного дела с этими
  терминалами; дыра затравки, не справочника.
- 2026-08-05 — **§4.8 сверки закрыт вне наряда: безакцепт разведён на два комитета
  (`ADR-0077`).** П. 16.5 назначает орган по типу проекта — «проекты Департамента
  администрирования кредитов» идут на комитет по администрированию кредитов (куратор ДАК,
  10 р.д. со дня повторного направления требований), «региональные» — на комитет по
  региональному развитию (РП собирает документы на месте за 3 р.д. и передаёт куратору
  отраслевого департамента, **либо** куратор сам выносит вопрос за 10 р.д.). Гейт стоял на
  одном органе. **Разведён не тем полем, каким казалось.** Готовый на вид признак
  `credit.subdiv` не подошёл: ведущее подразделение едет по эстафете передачи дела, и в
  затравке у 55 кредитов оно уже «ОПК», у одного «ДПО», у одного «САК» — происхождение из
  него не восстанавливается, а вывод дал бы этим 57 записям не «неизвестно», а молча неверный
  комитет. Заведён отдельный обязательный факт кредита `projectType` («ДАК» / «региональный»)
  с гардом в билдере: кредит без него дело не собирает, потому что умолчание здесь — не
  удобство, а тихий выбор органа. Гейт несёт `organByProject` вместо `organ`; неизвестный тип
  органа **не получает вовсе** (`gateOrgan` → null), дело-уровневый вызов называет оба через
  слэш — как это делает сама норма ещё в девяти пунктах. Один вопрос — один орган: смешанный
  набор кредитов форма не подставляет и не сохраняет, проверка стоит на обеих границах. Тип
  проекта виден плашкой прямо в строке выбора кредита. **Попутная находка — величина без
  опоры:** шаблон срока 13 держал «5 р.д., база — получение документов от РП»; ни срока, ни
  базы в п. 16.5 нет, обе ветви считаются от дня повторного направления требований, куратору
  дано 10 р.д. Строки 11 и 13 заодно названы своими комитетами. **Затравка получила дело 327**
  (`K1-БА-ГЕЙТ`, региональный проект, ожидающий гейт) — второй полюс развилки на живых данных:
  все пять прежних кредитов с `bezakcept:true` были проектами ДАК, и любой прогон открывал бы
  ровно один орган. Дел 96 → 97, требований 105 → 106, вопросов на органы 73 → 74. Значений
  типа **два** — третьей ветви норма не даёт; не-ДАК читается региональным (буквальное чтение
  ветви Б), умолчание объявлено вслух и вынесено наружу **вопросом 17** письма ГФХ (письмо —
  17 вопросов, не отправлено). Прогон: `collection-check.mjs` **726/32** (15 новых проверок;
  список провалов побитово равен baseline 711/32 — старый долг карточки дела/`ПЕ` не сдвинулся;
  шесть ожиданий с жёсткими числами переписаны под дело 327, не отключены),
  `collection-data-audit.mjs` 0 находок · 97 дел · 106 требований, `collection-seed-coverage.mjs`
  43 ситуации · 97 дел · 0 тонких, `check_points.py` 1318 ссылок · ❌ 0.
- 2026-08-05 — **Наряд сверки с редакцией №41 закрыт целиком: семь шагов из семи.** После
  шага 4 (запись ниже) прошли ещё три. **Шаг 5** — новые ветви и предусловия (`ADR-0069`,
  `ADR-0070`): глава 10 стала цепочкой из пяти видов мер, банкротство получило три жёстких
  факта пп. 55–57 с ИЛИ-ветвью внутри п. 56 и порогом ст. 9-1 настройкой. **Шаг 6** —
  справочник видов и пересев затравки (`ADR-0071`, `ADR-0072`): дыра пп. 30.4/30.5 оказалась
  в жалобе заёмщика, а не в возражении; попутно закрыт `МП-3`, не сделанный ни одной волной;
  справочник 73 → **79**; проверка ведётся инвариантом «нет вида с `resultIsDocument` без
  отвечающего акта», который тут же нашёл пятого сироту. **Шаг 7** — письмо ГФХ: четыре новых
  вопроса (13 — величина порога ст. 9-1; 14–16 — «фаза взыскания», «куратор», «комиссии») и
  приложение со всеми двенадцатью величинами без нормативной опоры. Расхождение по комиссиям
  оказалось не словарным: «сборы» свёрнуты в «Расходы», а те стоят первыми по п. 48 — вопрос
  16 единственный, меняющий устройство. Итог: `collection-check.mjs` **711/32** (список
  провалов побитово равен baseline), `collection-data-audit.mjs` 0 находок · 96 дел ·
  105 требований, `collection-seed-coverage.mjs` 43 ситуации · 0 тонких, `check_points.py`
  **1273 ссылки ❌ 0**, `check_tasks.py` 0/0. Подробности каждой волны — журнал
  `mockups/collection/ASUBK-status-razrabotki.md`; остатки — §9 сверки.
- 2026-08-05 — **Платежи: открытые вопросы §11 собраны письмом наружу, «Порядок работ» закрыт.**
  `docs/voprosy-zakazchiku/2026-08-05-platezhi.md` — **пять новых** вопросов (какой орган решает
  судьбу спорной пени и он же ли разрешает разморозку платежа и открытие закрытого периода ·
  нормативный порядок статей, очерёдность в процедуре банкротства и состав исключённых статей ·
  `N` дней таймаута и `M` дней предупреждения до автосторно в рабочих днях · справочник курсов
  НБ КР: интеграция или файл, суточные значения и полная история · порог полного погашения —
  строгий ноль или копеечный допуск). **Три вопроса повторно не задаются:** момент исполнения
  обязательства, место расходов в очереди и ликвидация юрлица уже ушли в ГФХ письмом
  `docs/voprosy-zakazchiku/2026-08-02-vzyskanie.md` (вопросы 2, 3, 4) — в новом письме они собраны
  отдельным разделом со ссылкой и указанием, что ответ меняет именно в платежах (дата исполнения
  задаёт дату курса НБ; в замороженный слой переплата не уходит). **Неденежные погашения вопросом
  не ставятся** — это граница модулей (вынесены в модуль по имуществам, здесь только точка
  стыковки), а не норма. Попутно **снято обещание** письма 02.08 прислать по платежам отдельный
  вопрос про движение остатка платежом со статусом «Расхождение»: `ОВ-ПЛ-1` закрыт нашим решением
  (`ADR-0058` — после сопоставления действует сумма ЦК). Пункт 4 «Вопросы наружу» в
  `mockups/payments/ASUBK-status-razrabotki.md` переведён ⏳ → ✅: открытых пунктов «Порядка работ»
  по треку не осталось. В `package.json` заведена команда `test:payments`; прогон 05.08.2026 —
  179 проверок, 0 провалено, 0 ошибок консоли. **Не сделано:** пакет передачи разработчикам
  (ТЗ и задачи) по платежам не начат.
- 2026-08-05 — **Шаг 4 наряда сверки закрыт: маршруты.** Три развилки, которые сверка
  оставила нерешёнными, развязаны и записаны решениями. (1) **Выходы после возврата ИЛ**
  (`ADR-0066`): заведены оба вида п. 36 — «Жалоба на акты судебного исполнителя» (взята
  вперёд из списка §4.15) и «Направление материалов в САК на банкротство»; выходов стало
  пять, и у каждого назван сорт основания — два по норме, один по закону об исполнительном
  производстве (повторное предъявление, предмет шаблонов 46/47), два под оговоркой п. 36
  «в том числе дальнейшие мероприятия», которой список нормой не закрыт. Признание
  безнадёжной осталось ещё и технически: без него срок 47 у безнадёжного требования снять
  нечем. Справочник видов мер 64 → 66. (2) **Ускорение — следствие извещения** (`ADR-0067`):
  основание открытия «решение об ускорении п. 20.1» ушло из словаря (осталось четыре — оно и
  не могло быть основанием *открытия*: извещение регистрируется на уже открытом требовании),
  а диалог «Отмена / Зарегистрировать и пересчитать» заменён сообщением с одной кнопкой —
  п. 20.1 согласия не спрашивает. Дело 582 затравки, сеявшее ушедшее основание, снято.
  (3) **Рукопожатие при передаче осталось общим и проектным** (`ADR-0068`): сужать до четырёх
  нормативных точек значило бы сделать прочие передачи односторонними, а молчание нормы — не
  запрет; цена названа вслух — 5 р.д. ничем не подтверждены. Нормативный срок заведён ровно
  один: шаблон 49, «Подтверждение полного погашения по судебному акту принимающим
  департаментом», 3 р.д., п. 37. Шаблонов сроков 48 → 49. Проверки: `collection-check.mjs`
  674/32 (список провалов побитово равен baseline 664/32; 10 проверок добавлено, 3 переписаны
  под новое поведение), `collection-data-audit.mjs` 0 находок · 96 дел · 105 требований,
  `collection-seed-coverage.mjs` 43 ситуации · 96 дел · 0 тонких, `check_points.py` 980 ссылок
  ❌ 0, `check_tasks.py` 0/0. Спека, ТЗ 13, пакет передачи P14, `CONTEXT.md`, `ADR-0022/0023/0050`
  и §9 сверки обновлены; следующий шаг — 5 «Новые ветви» (§4.12 смерть заёмщика, §4.13
  предусловия банкротства).
- 2026-08-02…04 — **Учёт и сверка платежей: две волны разбора, перепись макета, первый смоук.**
  Журнал по треку не вёлся с 12.07 — запись закрывает разрыв. **Волна 2 (02.08)**, гриллинг после
  волны взыскания: 21 решение (`ПТ-1…ПТ-21`), семь ADR `0055…0061` — разнесение **выводится**, а
  заморозка делает снимок · поступление · платёж · возврат суть **три сущности** (невыясненное —
  поступление с нулём платежей, а не отдельный буфер) · матрица разносит **только наступившее** на
  дату, просрочка — величина слоя, категория риска — худшее по слоям · сторно лечит поступление, а
  после сопоставления действует **сумма ЦК** · банкротная доля приходит обычным поступлением, но
  порядок статей задаёт норма, и матрица обязана уметь **исключённую статью** · счёт опознаёт
  объект (кредит либо транш), транш — область разнесения, очередь публикует кредит · курс —
  справочник НБ на дату поступления, полем не хранится. Отменены три счёта шлюза, гибрид «чистого
  долга» и «каскад пересчёта» как механизм; `ОВ-ПЛ-1` закрыт `ADR-0058`, `ОВ-ПЛ-2` — `ADR-0048`.
  **Волна 3 (04.08)** — ре-деривация вслепую: модуль пересобран с нуля по вопросам и затем сведён
  со спекой (из 35 решений 11 совпали дословно, одно снято в пользу спеки, четыре разошлись, шесть
  новых), решения `ПТ-22…ПТ-31`, четыре ADR `0073…0076` — три режима остатка сверх наступившего
  (обратная очередь · аванс · адресный резерв), досрочка по заявлению перестраивает график,
  заморозка **двухуровневая** (крыжение морозит платёж, закрытие морозит период), списание
  уменьшает начисленное, а не увеличивает погашенное. **Макет переписан 04.08** тремя этапами:
  спина (модель трёх сущностей, транш в каждой строке разнесения, курс с датой, очередь погашения
  от кредита, периоды учёта с гейтом, инвариант «сумма поступления = Σ платежей + возврат +
  нераспределённое»), экраны 2A (поступления · платежи · возвраты · закрытие периода) и 2B (реестр
  ЦК · сопоставление · акты сверки). `mockups/payments/payments.html` — 5157 строк, **восемь
  экранов** вместо пяти, роли куратор / бухгалтер / наблюдатель; все `ПТ-Д1…ПТ-Д26` закрыты,
  отменённое из редакции 27.07 изъято. Демо-набор доведён до случаев волн: сводная строка ЦК на
  трёх заёмщиков · кредит с траншами · валютное поступление · слой мирового с живым графиком ·
  возврат третьему лицу · адресный резерв · снимок, разошедшийся с выводом · закрытый и открытый
  периоды. **Смоук** `scripts/inspect/payments-check.mjs` — **179 проверок в 18 разделах, 0
  провалено, 0 ошибок консоли**; мутационный тест роняет 12 проверок, то есть они не холостые.
  Журнал волн и дефектов — `mockups/payments/ASUBK-status-razrabotki.md`, спека —
  `mockups/payments/ASUBK-platezhi-logika.md`.
- 2026-08-04 — **Шаг 3 наряда сверки закрыт: пороги и паузы.** Три механизма приведены к
  редакции №41. (1) **У паузы появился предмет приостановки** (`ADR-0063`): раньше обе паузы
  блокировали регистрацию любой меры, теперь п. 17 останавливает только процедуру
  безакцептного списания, п. 18 — только меры, «запущенные в рамках пункта 16»; судебный и
  залоговый треки не приостанавливает ни одна. Паузу по п. 17 ставит **решение комитета** —
  его реквизиты стали обязательной частью записи; предел 70 к.д. распространён на обе паузы.
  (2) **181-й день стал отложенным событием** (`ADR-0064`): хранимый `suppress181` снят,
  отложение выводится из живого рассмотрения реструктуризации, отказ фиксируется датой, и
  «высокий риск» наступает на следующий календарный день после неё — категория при живом
  рассмотрении **средняя**, а не «высокая с послаблением». (3) **Фактор категории работает
  только через комитет** (`ADR-0065`), а его уровень задан справочником нормы (восемь строк
  пп. 11.2/11.3) — поле уровня с записи снято: затравка, объявившая «уклонение от акта
  сверки» высоким риском, была бы не данными, а правкой Порядка. Попутно переписано
  обоснование §8.3 (пеня идёт всегда — вывод устоял, довод про «никем не одобряемое
  обещание» нормой снят) и объявлено вслух соответствие «возврату на прежнюю фазу» (п. 18):
  фаза есть свёртка журнала, механизма возврата нет и быть не должно. Проверки:
  `collection-check.mjs` 664/32 (список провалов побитово равен baseline; 6 проверок
  переписаны, 4 добавлены), аудит 0 находок, покрытие 43/97/0, `check_points.py` 892 ссылки
  ❌ 0, `check_tasks.py` 0/0. Разбор — `mockups/collection/ASUBK-status-razrabotki.md`,
  запись «Наряд сверки, шаг 3».
- 2026-08-04 — **Шаг 2 наряда сверки закрыт: модуль считает просрочку с 1-го дня.** П. 16.1
  редакции №41 переводит заёмщика в средний риск на 1-й календарный день, п. 11.2 определяет
  средний риск как «от 1 до 180 дней»; модель считала с 6-го. Переведены `catByDays`,
  автооткрытие Р-4, шаблоны сроков 4/5/40, база `opened`, оба счётчика просрочки и окно
  ожидания; в билдере затравки исчез сдвиг «открытие = просрочка + 5 дней» — это одна дата.
  Категории «низкий риск» по дням **не существует вовсе** (п. 11.1 — качественные признаки),
  поэтому бакет «Норма» означает «просрочки нет». Основание открытия переименовано
  «просрочка 6 дней» → «просрочка»: число дней есть свойство триггера, а не признак
  основания, и зашитая в название цифра уже один раз разошлась с нормой.
  Побочно вскрылся **дефект Н-1**: окно ожидания и срок первичной претензии теперь считаются
  от одной даты, и при прежнем пределе окна (14 к.д.) гейт открывался бы ровно в день
  истечения срока — обязательство невыполнимо по построению. `ADR-0062` снижает предел до
  7 к.д. правилом «окно не длиннее половины срока претензии» (отношением, не числом);
  умолчание 5 к.д. не тронуто, величина остаётся вопросом 8.2 письма ГФХ.
  Прогон: `collection-check.mjs` **660/32**, список провалов побитово идентичен baseline;
  четыре проверки очереди сроков переписаны под новые числа (горизонт 79 → 86, глубина дела
  391 −155/−131/−117). `collection-data-audit.mjs` 0 находок, `collection-seed-coverage.mjs`
  43 ситуации / 97 дел / тонких 0, `check_tasks.py` 0/0. `CONTEXT.md` переведён и добавлен
  в охват `check_points.py` (731 ссылка, ❌ 0). Пакет передачи P14 разморожен.
- 2026-08-04 — **Шаг 1 наряда сверки закрыт: весь модуль взыскания переведён на нумерацию
  редакции №41.** 687 ссылок в 35 артефактах (макет 341, журнал волн 83, спека 72, ADR 74 в
  27 файлах, смоук 38, ТЗ 25, задачи 7, STATUS/TODO 9), `python3 scripts/check_points.py`
  из красной стала зелёной и держит результат. Правка механическая, но три места решены
  по существу: шаблоны сроков 33/44 (конфликт интересов) и 34 (приём передачи) нормы под
  собой не имеют — им дано **проектное** основание `Р-12` и `ADR-0023`, а не новый номер;
  ADR-0023 получил раздел «основание рукопожатия нормой не подтверждено»; ADR-0017 —
  пометку, что перечень оснований безнадёжности ушёл в отдельный Порядок Минфина.
  Заодно исправлено: «п. 114» в МС-1 (такого пункта не было и в прежней редакции — речь <!-- check_points:ignore -->
  об ускорении долга, п. 20.1) и глава конфликта интересов, всюду названная 16-й, — в №41
  это 15-я. Инструмент: `scripts/migrate_points.py` (исполняющая половина проверки, делит
  с ней разбор) + `Scanner` в `check_points.py`, читающий перечисления через запятую и
  косую, голые номера в колонке «пункт» таблиц и блоки `check_points:ignore`.
  Смоук вёлся вместе с макетом — он держит номера литералами, в т.ч. в регексах с
  экранированной точкой; `collection-check.mjs` 660/32 без изменений. §6.1 сверки называет
  четыре модуля вне взыскания, которые цитируют тот же Порядок и не тронуты.
- 2026-08-04 — **Получена действующая редакция Порядка (№41 от 06.07.2026) и проведена полная
  сверка.** Модуль взыскания строился по предыдущей редакции («99 пунктов, 17 глав»);
  действующая — 16 глав, 69 пунктов. Источник положен в репозиторий:
  `docs/normativ/poryadok-2026-07-06-N41.pdf` (+ текстовая копия для поиска). Сверка —
  `docs/normativ/2026-08-04-sverka-red-N41.md`: таблица соответствия по всем цитируемым
  модулем пунктам, 12 содержательных расхождений (1-й день просрочки вместо 6-го · гарантийное
  письмо ставит паузу только решением комитета и только на безакцепт · отложенный переход на
  181-й день вместо послабления · другой маршрут после возврата ИЛ · ускорение долга
  извещением, а не решением · факторы риска под решением комитета · два комитета по безакцепту ·
  п. 48 без очерёдности залогодержателей · конфликт интересов без процедуры · п. 98 не <!-- check_points:ignore -->
  существует · глава о смерти заёмщика без ветви в модели · предусловия банкротства), список
  недостающих видов мер и наряд из 7 шагов. Разборы МП/МП5 и ADR `0043…0054` сверкой не
  отменены — расхождение в слое нормативных значений, не в устройстве модели. Письмо ГФХ
  переиздано редакцией 2 — `docs/voprosy-zakazchiku/2026-08-04-vzyskanie.md` (11 вопросов +
  запрос 6 документов); письмо от 02.08.2026 помечено отменённым. Шапки спеки, макета и ТЗ
  несут предупреждение о нумерации.
- 2026-08-04 — **Взыскание задолженности: пакет передачи разработчикам переиздан редакцией 2**,
  закрывая разрыв, отмеченный предыдущей записью. Три файла: `docs/tasks/p14-collection-tasks.html`
  (23 задачи `P14-R1…R23` в 7 этапах — Каркас и модель · Реестр и карточка · Открытие и журнал мер ·
  Суд/соглашения/правопреемство/гейты · Долг и залог · Особые состояния · Передача/сроки/настройки/
  история; JSON-остров по контракту `docs/tasks/FORMAT.md` §8 — **первый файл в этом формате**,
  рендер написан с нуля, старого образца не было), `requirements/tz/13-collection.html` (18 → 20
  разделов — вставлены «14. Мировое соглашение» и «17. Правопреемство», плюс подразделы карантина
  (5.3), основания открытия (8.1), давности/два счётчика (10.1–10.3), слоя долга/апелляции/поворота
  исполнения (12.1–12.3); гейт «пересечение судебных процессов» вычищен как противоречащий М-1),
  `TODO.md` (реестр `### Взыскание задолженности` — 20 полнотекстовых карточек заменены 23 строками
  реестра, приоритет+заголовок+ссылка на HTML — источник постановки теперь только там, по
  `FORMAT.md` §5). Переиздание — двойной скачок модели: старый файл (22.07) был на v2 («процесс»),
  `TODO.md` уже была на v3 (26.07, «требование»), новый пакет донёс v3 + все 12 ADR волны «МП»
  (слой долга и апелляция, карантин, предусловия вместо контурной лестницы, семь терминалов,
  основание открытия, правопреемство, давность и два счётчика, заморозка начисления, мировое
  соглашение актами журнала, делёж выручки между кредитами). ID `P14-R1…R11` не сдвинулись
  (совпадение нумерации со старым файлом), `R12…R20` разъехались — таблица «старый → новый» в
  шапке HTML. `scripts/check_tasks.py docs/tasks/p14-collection-tasks.html` — 0 ошибок; полный
  прогон `check_tasks.py` — 1 файл в новом формате + 7 legacy (не проверяются, как задумано).
  `collection.html` и смоук-скрипты не тронуты — `collection-check.mjs` 660/32 (тот же старый
  долг), `collection-data-audit.mjs`/`collection-seed-coverage.mjs` 0 находок, без изменений.
  **Не сделано:** сама постройка модуля (это задача дев-команды, не этого репозитория); 6 вопросов
  ГФХ по-прежнему без ответа (см. «Blocked/waiting», «Known issues» ниже) — их умолчания уже внутри
  карточек постановки врезками «Решения нет».
- 2026-08-04 — **Взыскание задолженности: волна «МП» (модельные пробелы) + наряд §30 закрыты в
  коде; журнал не вёлся здесь по ходу — восстановлен задним числом.** 2026-07-27…08-04, восемь
  дней подряд. Поэкранный обход мокапа `collection.html` закрыт шестью экранными волнами —
  **ТР** (реестр требований, ТР-1…ТР-11) · **КД** (карточка дела, КД-1…КД-15) · **СК** (сроки на
  контроле, СК-1…СК-15) · **КР** (три кросс-процессных реестра, КР-1…КР-16) · **НП** (настройки
  правил, НП-1…НП-16) · **ПЛ** (плотность реестра, второй проход, ПЛ-1…ПЛ-7) — плюс модельные
  волны **МТ** (много требований в одном деле), **ПМ/РМ** (справочник мер 51→56, три оси
  результата → один `outcome`), **ПЕ** (передача требования вместо процедуры дела, `ADR-0023`),
  **РР** (ревизия реестра требований) и **ДС/ЗС** (достоверность и полный пересев затравки —
  40 ситуаций, ~92 дела). Следом — **5 кругов разбора МП** (02.08, код не тронут): 18 решений,
  6 новых ADR `0043…0054` (слой долга публикует взыскание · предусловия вида вместо контурной
  лестницы · карантин закрытия дела · ключ слоя по кредиту, не по требованию · солидарность ·
  семь терминалов · фаза — максимум по маршруту · правопреемники разветвляют требование).
  **Наряд §30** перевёл решения в код семью пунктами (п.1 справочник видов 56→64 · п.2/3 ключ
  слоя + разбивка присуждённого/требуемого по 5 статьям · п.4 универсальная проверка треков ·
  п.5а/б фаза-максимум + предусловия вместо лестницы, экран настроек · п.6а/б основание
  требования словарём из пяти + разветвление правопреемников · п.7а/б/в возврат ИЛ рождает два
  срока, исковая давность с перерывом, второй счётчик непредъявленной просрочки), и следом ещё
  три карты сверх наряда: **МП-11** (мировое соглашение — акты журнала вместо сущности с
  флагами) · **«выручка/очередь»** (МП5-8+МП5-10, смерть/рост слоя) · **МП-2** (апелляция ревизует
  слой решения суда, не образует новый). Итог: `collection-check.mjs` 660 проверок/32 провала
  (старый долг карточки дела, не новый — сверено побитово по FAIL-спискам до/после), `collection-
  data-audit.mjs` 0 находок, 97 дел, 106 требований. Полный журнал волна-за-волной —
  `mockups/collection/ASUBK-status-razrabotki.md`; спека — `mockups/collection/ASUBK-vzyskanie-
  logika.md`. Письмо в ГФХ (`docs/voprosy-zakazchiku/2026-08-02-vzyskanie.md`) — 6 нормативных
  вопросов, без ответа МП-9 не программируется. **Не сделано:** пакет передачи разработчикам
  `docs/tasks/p14-collection-tasks.html` (22.07, legacy-формат) предшествует всей этой волне —
  кандидат на реисью, не начат; `STATUS.md` не обновлялся весь период (этот коммит закрывает
  разрыв); попутно почищен `scripts/inspect/collection-seed-coverage.mjs` — три кода, рождённых
  нарядом (`K0-ОСНОВАНИЕ`, `K0-ПРАВОПРЕЕМСТВО`, `K3-НЕПРЕДЪЯВЛЕНО`), не значились в каталоге
  §ЗС-1 design doc; занесены туда явно (43 позиции, ~97 дел; `K3-НЕПРЕДЪЯВЛЕНО` — задокументированное
  исключение с минимумом 1 дело, не 2).
- 2026-08-04 — **Кредит: единый переключатель области карточки (КВ-17) и правило Р-30.**
  Три контрола области — `condScope` на «Условиях», `detailTrancheNo` на Траншах/Графике/
  Прогнозе/Расчётах и чекбокс `calcConsolidated` на «Расчётах», — плюс два слова для одного
  понятия («агрегат» и «консолидировано») схлопнуты в одну переменную **`cardScope`** и один
  переключатель «Область» в шапке, рядом с датой среза: тот же перенос, что КВ-10 сделал для
  `cardAsOf` по дефекту КР-29. «График» и «Прогноз» при области «по кредиту» получили слитую
  таблицу с колонкой «Транш» — идиому консолидированного расчёта, а не вторую трактовку;
  шапка за областью не следует (плитки — агрегаты по кредиту) и говорит об этом подписью.
  Спека `docs/superpowers/specs/2026-08-04-credit-scope-switcher-design.md`, план из шести
  задач `docs/superpowers/plans/2026-08-04-credit-scope-switcher.md` (порядок такой, что
  промежуточного сломанного состояния нет). Заведена **обвязка jsdom** `mockups/loan-credit/tests/`
  (`harness.mjs` + `scope.test.mjs` через шов `window.CR`) — первые в модуле тесты слоя рендера,
  которого смоук не покрывал (КР-23); **38/38 PASS**. Ревью ветки дало восемь правок:
  хвост прошлого в «Прогнозе» распределён по траншам (подпись называла кредитную сумму,
  превышавшую сам платёж); **«по кредиту» = ВСЕ транши, закрытые включительно** — фильтр
  `activeTranche` снят со `scheduleTotalsAll` и обоих `srcTranches`, один контрол не может
  означать на соседних вкладках разное; журнал и сводная матрица «Условий» стартуют от области
  карточки; «(консолидировано)» → «(по кредиту)». **Р-30 ОБЛАСТЬ ПРОСМОТРА** внесено в блок
  решений и затем переписано: адресность действия решает **ярлык кнопки** — «Закрыть транш №N»
  именует транш, которого при области «по кредиту» у неё нет, и гасится с причиной;
  «Внести освоение» транша не именует и живёт, транш спрашивает её модалка (хелпер
  `actionTranche` — первый **активный** транш, по закрытому освоение запрещено Г-17;
  тем же хелпером выровнены гейт кнопки и предвыбор её формы). Смоук **98/98 PASS**.
- 2026-08-03 — **Кредит: реестр, фильтр и подача карточки — три волны за день.**
  (1) **Реестр 12 колонок → 8**: реквизит без своего «хозяина» переехал во вторую строку
  хозяйской ячейки — № + дата → «Договор», программа + куратор → «Программа», сумма + валюта →
  «Сумма договора» (KGS-эквивалент снизу), категория + подгруппа → «Классификация».
  (2) **Фильтр в два яруса** (спека `docs/superpowers/specs/2026-08-03-credit-registry-filter-design.md`):
  быстрая строка (единый поиск по заёмщику/ИНН/номеру · ЖЦ · куратор) + панель на трёх колонках
  (привязка · риск · проблемность). Слияния `fQ+fNum` и `fRegion+fDistrict`, счётчик условий
  переведён на таблицу `CONDS` (одно условие на паре «от/до» и на каскаде). Снято: фильтр
  «Состояние освоения» (отбирал по невидимой в реестре величине), дефолт ЖЦ «Действует»
  (молча прятал проектные и закрытые), тулбар с дублирующими кнопками и счётчик `#regCount`.
  Проверено `credit-filter-shot.mjs` / `credit-list-merge-shot.mjs` на 1680×1000.
  (3) **Waiver залоговой комиссии — КР-56/КР-57**: `saveWaiver` писал в модель, а читал поле
  только `gate()` (Г-6, Г-7) — освоение проходило при непройденном пороге, экран показывал
  красное «недостаточно» и молчал о причине; плюс `pledgeWaiver` лежал в `mirror.*`, объявленный
  зеркалом модуля залога, который его не отдаёт и не знает (waiver выдаёт комиссия кредитора).
  Поле переехало в `mkCredit` встык к `kmDecision`, карточка «Освобождение от порога обеспечения»
  во вкладке «Договор» + read-only раздел-ссылка во вкладке «Обеспечение».
  (4) **Вкладка «Договор»** переложена: «Основание выдачи» на всю ширину, три редких карточки
  («Предмет кредита», «Освобождение от порога», «Перевод долга») сведены в одну карточку-таб.
  (5) **«Условия»**: разворот журнала, где документ правил больше одного транша, разводится
  вкладками (транш на вкладку), сводная матрица разделена на зоны «Ставки и льготы» / «Погашение»
  со своими наборами дат. (6) **Вкладки «График» и «Прогноз»**: из «Расчётов» выделен план
  возврата, из него — ожидание по тому же плану (Р-29, ADR-0042); «Транши и освоение» → «Транши»,
  «План и исполнение» → «План», `DTABS` **9 → 11**; плитки «Графика» переведены на выбранный
  транш, плитка платежа зависит от метода погашения. Внешние документы догнаны — приёмка
  «ровно 11 вкладок» в `TODO.md` (P15-R1) и `docs/tasks/p15-kredit-tasks.html`, таблица вкладок
  в `requirements/tz/05-kredit.html` §4.3. Смоук **97/97 → 98/98 PASS**, добавлена проверка #53a
  (список вкладок смоука сверяется с `DTABS` в исходнике — иначе вкладка тихо выпадает
  из-под проверки рендера, как «План и исполнение» в волну 01.08).
- 2026-08-01 — **Кредит: волна ADR-0042 «план · прогноз · исполнение плана» + КР-55.**
  `ADR-0042` (принят 31.07) фиксирует, почему **план — вводимая величина**, снимок прогноза при
  заведении, а не производная от графика: три ориентира на будущее (график — контракт транша,
  прогноз — тот же план от факта, план — целевая сумма на месяц) друг на друга не ссылаются.
  Спека и план волны — `docs/superpowers/`; в модель добавлены **Р-29, Г-30, Д-9, И-18**,
  в `CONTEXT.md` — три термина. Сделано: модель плана и помощники месяцев, прогноз транша и
  агрегат `forecastByMonth`, мутация `setPlan` под Г-30, вкладка «План и исполнение», модалка
  «Поставить план» (пачка месяцев с предзаполнением прогнозом), блок прогноза в «Расчётах»
  и плитка исполнения в шапке. Финальное ревью закрыло четыре находки: дефолт формы был
  невалиден на 58 демо-кредитах из 59 (галка ставилась на месяц без плана и позиции);
  ненаступивший месяц показывал 0 % и тянул вниз квартал/год — введён признак `future`,
  `planExecOf` разводит `plan` (вся цель) и `planDone` (наступившее); снятие плана было
  недостижимо из интерфейса, хотя гейт и сид его знали; девятая вкладка не входила в `TABS`
  смоука и под проверку рендера не попадала вовсе. Смоук **79 → 96 PASS**.
  Отдельно **КР-55**: «Предмет кредита» рендерился read-only таблицей, хотя ТЗ держит `subject[]`
  как owned-поле — до регистрации свободный add/remove (`editSubject`), после только доп.
  соглашением (append-only `addSubjectRecords`, паттерн Р-21), роль «Кредитный специалист» +
  «Начальник отдела».
- 2026-07-30…31 — **Кредит: подача карточки, шаги 12–20.** Разгружены шапка (шаг 12) и вкладки
  «Договор»/«Условия» (13–15, 17): карточная раскладка вместо внешней `panel-card`, объединена
  карточка «Основание выдачи», убраны info-plate, дублирующие замочки, и пояснения-заглушки;
  поля льготного периода показываются только при реальной льготе; перевод долга ушёл в «Договор»
  (Р-21 — меняет субъекта, не параметр); закреплено правило show/hide «карточка без действия
  прячется, карточка с действием остаётся». Роутинг переведён на hash, даты — на нативные
  пикеры, реквизиты договора стали редактируемыми под ролью «Начальник отдела». Шаг 18 —
  сверка с `loan-program.html`: в `PARAMS` добавлены `payDay`, `lastPaymentAnchor`, `payMonths`,
  `graceIntDistFrom/To`, движок `buildSchedule` их читает, «плавающая» обобщена с ставки на любой
  денежный параметр; шаги 19–20 — `dayMethod` наконец подключён к начислению процентов
  (хранился, но не читался). Смоук **75 → 79 PASS**.
- 2026-07-28 — **ТЗ раздела 04 «Заёмщик» синхронизировано с макетом.** Документ был снят
  27.07 до постройки карточки и за сутки отстал по фактам, не по решениям (`СП-1…СП-24`
  и `КЗ-1…КЗ-41` в нём уже были). Догнано: инварианты **И-1…И-9 → И-1…И-13** (лента одна ·
  состав витрины постоянный · итог считает показанное множество · класс достижим типом лица);
  §4 получил ограничение **И-13/ADR-0017** — банкротные классы `2.3`/`3.1`/`3.2` только юрлицу
  и ИП, до этого ТЗ описывало лестницу, по которой макет и смоук дают FAIL; ступень 1
  исправлена на `3.1 · 3.2 · 4 · 5` (`isTerminalGroup` — весь третий этаж). Дефекты
  **Б-1…Б-15 → Б-1…Б-24**, открыт один — `Б-16`; открытые вопросы **+ОВ-19, +ОВ-20**,
  `ОВ-13` возвращён в открытые (шов с модулем пользователей не сделан). Пометки «решение,
  кода нет» в §12–§13 сняты — все одиннадцать вкладок в статусе «макет», колонка «Эталон»
  в §18 переписана; добавлены **ADR-0012** (направление зеркала кредит ↔ заёмщик),
  **ADR-0016**, **ADR-0017**; счётчики смоука 165/165 и 147/147 → **309/309**. В шапку
  добавлена явная граница ролей: постановка на постройку (эпики `Э0…Э14`) — комплект
  разработчика и **при расхождении права она**, ТЗ отвечает на вопрос «почему модель такая»;
  §18 получил таблицу соответствия блоков/эпиков задачам `P4-R*`. Счётчики `P4-R*`,
  `СП-*`, `КЗ-*` не менялись.
- 2026-07-27 — **Кредит: карточка приведена к ADR-модели — КВ-1…КВ-16, КР-1…КР-53.**
  Макет строился по канону `prompt-kredit-mockup.md` (23.07) и остался единственным модулем,
  живущим по доADR-правилам: 26.07 проект принял ADR, переопределившие модель долга, единицу
  взыскания, поручительство и метрику обеспеченности. Ревизия против `docs/adr/` + `CONTEXT.md`
  дала **16 решений волны (КВ-1…КВ-16)** и **53 дефекта (КР-1…КР-53)**; учёт волны — в
  `mockups/loan-credit/ASUBK-status-razrabotki.md` (не в `TODO.md` — это задачи по макету).
  Модель: три новых ADR — **0010** (долг: «начислено» выводится из графика × условий × приостановок,
  «погашено» и разнесение — зеркало платежей, поле `ledger` удалено; четыре статьи + расходы
  взыскания зеркалом), **0011** (обеспеченность: числитель и порог состава — зеркало залога,
  кредиту принадлежат база = сумма под риском и порог КМ; поручительство из числителя исключено),
  **0012** (категория owned кредитом и выводится, группа риска A–D удалена как фантом, подгруппа —
  зеркало заёмщика, послабление отделено от признака состояния); ADR-0007 дополнен явной формулой
  индекса; по итогам сверки с платежами принят **ADR-0013** (зачёт читает две оси, слой решения
  суда делится между кредитом и платежами). Ядро: `buildLedger` строит детальный расчёт при
  рендере, `debtOf` развёл тело кредита и просроченную часть — этим закрыты КР-7/8/9 (кредит с
  освоенными 100 000 показывал остаток 0). Этап 3 (спека `ASUBK-kredit-logika.md`) нашёл шесть
  дефектов **сборки** обеспеченности, которых не искал смоук — КР-38…КР-43 (у гарантии отобран
  её собственный порог: K-C6 134 % вместо 161 %; вторая подпроверка §2.4 не собиралась вовсе —
  залог блокировал, кредит выдавал). Этап 4 (сверка со спекой платежей) — КР-44…КР-53: у платежа
  три независимые оси, кредит читал одну (платёж по шлюзу не двигал долг; ось сопоставления знала
  2 значения из 5; спорной пени не существовало — на К-1 5 дней просрочки и 10,57 пени порождены
  отменённым платежом; начисление шло по кредиту, а не по слоям решения суда). Макет 4143 → 5511
  строк, смоук **43 → 75 PASS**.
- 2026-07-27 — **Модуль заёмщика подготовлен к передаче разработчикам.** Пакет из трёх артефактов:
  ТЗ целевой модели `requirements/tz/04-zaemshchik.html` (19 разделов — субъект и роль, лестница
  подгрупп, категория worst-of, инварианты И-1…И-9 с причиной, семь швов, реестр, шапка, витрина,
  вкладки 2–11, страница субъекта, «было → стало», открытые вопросы); секция «Заёмщик (целевая
  модель)» в `TODO.md` с задачами **P4-R6…P4-R25** в строгом порядке зависимостей; постановка на
  постройку `docs/tasks/p4-zaemshchik-tasks.html` (28.07.2026 переписана в формате залоговой:
  эпики **Э0…Э14** в шести блоках, порядок карточек = порядок сборки; комплект разработчика —
  она плюс макет, без ссылок на спеку, ADR и внутренние шифры решений; при расхождении с бэклогом
  по логике модуля права она).
  Нарезка: **сквозное вперёд** (модель · производные · дата · контракты чтения · приёмы вкладок —
  R6–R10), затем экраны (R11–R13), затем **одна вкладка = одна задача** (R14–R23), страница субъекта
  (R24), миграция (R25) — чтобы одиннадцать вкладок не договаривались с чужими модулями по отдельности.
  В задачи разнесены все 65 решений (`СП-1…СП-24`, `КЗ-1…КЗ-41`), 13 открытых дефектов макета
  (`Б-1…Б-15`, два закрыты шапкой) и открытые вопросы `ОВ-13…ОВ-18`; отдельными задачами они не
  выделяются. Эталон — `mockups/borrower/borrower.html` (смоук 165/165), макет не правился: эталон
  есть у реестра и шапки, вкладки 2–11 идут «только постановкой». Ревизия старых задач фазы 4: из 5
  четыре поглощены (R1 → P4-R11, R3 → P4-R8+R13, R4 → P4-R25, R5 → P4-R20), R2 отпадает вместе с
  формой; отдельно не остаётся ни одной. Закрыт **ОВ-13** (модель пользователя уже построена в
  `mockups/users/users.html` — нужен только шов, P4-R18); вопрос заказчику по панели «Результаты
  проверок» сужен с шести проверок до одной — «Лимит задолженности». `04-zaemshchik.md` помечен
  архивом обследования, `requirements/tz/index.md` обновлён (04 → to-be).
  **Sheet не синхронизирован:** правки сделаны в worktree, хук `todo_hook.py` реагирует только на
  `TODO.md` основного рабочего дерева — синхронизировать после влития ветки.
- 2026-07-26 — **Кредитный модуль подготовлен к передаче разработчикам.** Собран пакет из трёх
  артефактов: ТЗ целевой модели `requirements/tz/05-kredit.html` (19 разделов — решения Р-1…Р-21,
  гейты Г-1…Г-22 с дословными текстами отказов, модель данных, матрица ролей 20×5, отдельный
  раздел «Дыры, допущения и техдолг», 6 открытых вопросов); секция «Кредит (целевая модель)» в
  `TODO.md` с задачами **P15-R1…P15-R23** в строгом порядке зависимостей (ядро R1–R9); читаемый
  бэклог `docs/tasks/p15-kredit-tasks.html` (23 карточки «в чём боль → что сделать», 8 групп-шагов,
  раскрывашки «Для разработчика»). Эталон — `mockups/loan-credit/credit.html` (смоук 43/43,
  `scripts/inspect/credit-check.mjs`), макет не правился. Реестр гейтов восстановлен обратной
  инженерией по коду (`docs/superpowers/specs/2026-07-26-kredit-gates.md`) — исходный канон утрачен.
  Ревизия старых задач: из 13 задач фаз 5/6/7 семь поглощены P15-R\*, пять отпадают вместе с
  экранами, остаётся одна — **P5-R1** (падение живого экрана на записях 20/22). Разделы ТЗ
  `05-kredit.md`, `06-transhi-osvoenie.md`, `07-obsluzhivanie.md` помечены как архив обследования;
  `requirements/tz/index.md` обновлён (05 → to-be, 06/07 → поглощены; добавлены отсутствовавшие
  разделы 11/12/13). Закрывает легаси-дефекты **E2E-09** (остаток — расчёт по леджеру, не
  батч-снапшот) и **E2E-13/14** (закрытие при нуле с порогом 0,005).
- _(пробел в журнале: работы 2026-07-13…2026-07-25 — обследование легаси `requirements/legacy/`,
  модули залога P13 и взыскания P14 — в changelog не занесены; см. git log)_
- 2026-07-12 — Собран to-be мокап модуля **«Учёт и сверка платежей»** (`mockups/payments/payments.html`) по
  спеке `mockups/payments/ASUBK-platezhi-logika.md`. 5 экранов: Платежи (список + карточка), Реестр ЦК,
  Сопоставление (сверка), Невыясненные, Акты сверки. Демонстрирует 7 архитектурных швов: платёж vs запись
  ЦК как две сущности, три независимые оси состояния (регистрация · сопоставление · заморозка), 4 исхода
  сверки, шлюз с действием до подтверждения ЦК + сторно/восстановление, спорная пеня, матрица разнесения
  слой→категория→срочность, N слоёв решений суда с разными режимами, крыжение/акт сверки. Роли
  Куратор/Бухгалтер/Наблюдатель меняют доступные действия. Проверка (playwright-core headless): 5 экранов
  рендерятся, карточка/табы/смена роли/сторно работают, 0 ошибок консоли.
- 2026-07-10 — Макет заявки, список: **переключатель вида «Таблица / Карточки»** (`P3-R44`) — сегмент как в
  мокапе кредитных программ; карточка несёт заёмщика, номер+дату, бейджи статуса и стадии, сумму, программу,
  область/район и специалиста. Фильтры, сортировка, счётчик и гейты тулбара общие — `renderGrid()` считает
  выборку один раз и выбирает рендер; выбор вида хранится в `localStorage` (`la.listView`).
  Проверка (Chrome DevTools MCP): 0 ошибок консоли, выбор карточки включает «Просмотр»/«Удалить»,
  вид переживает перезагрузку.
- 2026-07-10 — Собран to-be мокап модуля «Взыскание задолженности» (`mockups/collection/collection.html`):
  список процессов взыскания (не кредитов), карточка на 7 вкладок, шапка-индикатор четырёх измерений,
  живой контроль пересечения охвата, 6 демо-процессов включая пару «два процесса на один кредит» и
  два терминальных исхода. Проверка: `scripts/inspect/collection-check.mjs` — 169 ассертов, 0 провалов,
  0 ошибок консоли. Рекомендации команде: `TODO.md`, «Фаза 9» (P9-R1…R9).
- 2026-07-10 — Макет заявки, список: **фильтры по территории и исполнителю** (`P3-R43`) — четыре селекта
  (область · район · филиал/отдел · кредитный специалист) с двумя каскадами (область сужает районы и
  подразделения; подразделение сужает специалистов) + три сортируемые колонки. Справочники `REGION_DIR`
  (9 областей → 45 районов) / `ORG_UNITS` / `SPEC_DIR`; плоский `APPLICANT_REGIONS` заменён структурными
  полями `oblast`/`rayon`/`spec`/`unit`. Проверка (Playwright + system Chrome): 0 ошибок консоли, каскады
  пересобираются, специалист заявки всегда из подразделения, обслуживающего её область (31/31).
- 2026-07-10 — Макет заявки: вкладка «Заключения» переработана (`P3-R41`) — отделы назначает специалист,
  ЖЦ «черновик → внесено» с отзывом, отрицательный вердикт блокирует комиссию.
  Проверка: `node scripts/inspect/conclusions-check.mjs` — 87 ассертов, 0 провалов, 0 ошибок консоли.
- 2026-07-10 — **Групповой кредит: документы по каждому члену группы** (`loan-application.html`,
  задача **P3-R37**). Модель — зонтичный batch (1 кредит / N траншей, лидера нет), поэтому личные
  документы принадлежат **члену**, а не заявке: при `isGroup` секции «Идентификация» и
  «Платёжеспособность» уходят на уровень члена (по заявке остаются «Печатные формы», «Целевые»,
  «Договорные»). На вкладке «Документы» — селектор «Документы по: Вся группа / член» (зеркало
  вкладки «График»); режим «Вся группа» — сводка с чипом комплектации, `N/M` и замечаниями по
  каждому члену, строка кликабельна. Ключ дока члена `mem:<pin>::<docId>`, плоский стор
  `app._memberDocs` (по образцу `_collDocs`); перечень не дублируется — берётся из `DOC_SECTIONS`.
  Гейт «Отправить в комиссию» суммирует обязательные доки всех членов и **называет их поимённо**
  (`_memberGateList`). Попутно: (1) источник интеграции ищется через **`_docSrc`**, а не по сырому
  ключу — иначе у залогового ТС (`::reg` = «Свидетельство о регистрации ТС») всплывала бы кнопка
  «Запросить из «Минюст»» (`DOC_SOURCES.reg` = выписка Минюста); (2) демо-члену «ОРОЗБЕКОВА А.М.»
  исправлен ПИН — стоял ПИН юрлица МП «Ноокат Тазалык», и она получила бы устав с ЕГРЮЛ;
  (3) `_applyConfirmedSeed` доводит по-членные доки до `confirmed`, иначе демо-заявка
  `З-2026-000105` упиралась бы гейтом в документы вместо заключений отделов.
  Проверено в браузере (Playwright, headless): сводка/селектор/комплект члена, цикл интеграции
  `requested → uploaded`, add/delete члена (стор чистится, вид откатывается), регресс залоговых
  доков и индивидуальной заявки — JS-ошибок нет.
- 2026-07-10 — **Мокап «Комиссии по заявкам» переведён as-is → to-be**
  (`mockups/loan-application-commission/commission.html`; as-is версия остаётся в git, коммит `c50077b`).
  UX/UI-разбор экрана дал 8 новых дефектов **P3-26…P3-33** (`notes/qa-findings.md`) и
  3 задачи **P3-R38 / P3-R39 / P3-R40** (`TODO.md`); попутно закрыты P3-R32/R33/R34.
  Главная находка — **P3-26 🔴**: председатель мог вынести решение при **нуле голосов**
  (кнопки не связаны с голосованием, прогресс нарисован под ними, протокол не обязателен).
  В to-be: кворум-гейт (`quorumOf`/`tally`/`decisionGateReason`) + прогресс над решением;
  голоса и комментарии членов — в гриде состава (пустая секция-дубль удалена);
  «Проголосовать» — в строке своего члена (`can(rec)`, демо-свитчер ролей);
  единая карточка вместо разных «Просмотр»/«Изменить»; список — плитка «Ждут моего голоса»,
  колонка «Крайний срок» с чипом просрочки, статус-чипы, «Голоса N из M», № → `К-2026-000138`;
  прогресс — стек 4 сегментов (`rgb(255,123,123)` контраст 2.3:1 → палитра);
  диалоги — Escape/подложка, защита несохранённого ввода, подтверждение называет действие
  и запись, деструктив красный; empty states вместо 150px пустоты, «Протокол» — не тупик.
  Проверено в браузере: `scripts/inspect/commission-tobe-check.mjs` — **38 assert'ов, 0 ошибок консоли**.
- 2026-07-09 — **Вкладка «Документы» — статусы комплектации + ярусы кнопок** (`loan-application.html`,
  `renderDocs`/`renderDocSection`/`docRow`). Спек: `docs/superpowers/specs/2026-07-09-loan-app-docs-completeness-design.md`.
  (1) **ЖЦ 7→6**: «На проверке» больше не хранимый статус, а **вид** — `uploaded`-док при заявке
  в комиссии показывается бейджем «На проверке» (`_dispSt`/`_atCommission`); `docUpload` пишет
  `uploaded`. (2) **Именованный чип комплектации** (`_complStatus`/`_complChip`, CSS `.compl.cs-*`)
  рядом с `N/M` на секции, залоговом предмете и пакете: Не начато / В работе / Готов к отправке /
  На проверке / Укомплектовано / Есть замечания / Заблокировано. ✓ в счётчике теперь = «Укомплектовано»
  (все приняты), не просто собрано. (3) **Кнопки**: ярус A — +Скачать (`docDownload`), +Вернуть для
  waived (`docUnwaive`); ярус B — панель пакета для спеца·draft: Отправить (гейт+тултип), Запросить
  недостающие (N, `pkgRequestMissing`), Скачать пакет, Опись; ярус C — видимость по роли/режиму без
  изменений. Гейт `sendReady` — логика прежняя. Проверено в браузере (`scripts/inspect/docs-completeness-render.mjs`):
  спец·draft·view/edit + комиссия·review, JS-ошибок нет, чипы во всех состояниях.
- 2026-07-09 — **Залоговые документы — сворачиваемые под-панели по предметам** (`loan-application.html`,
  секция «Залоговые» вкладки «Документы»): при многих предметах плоский список тяжело
  просматривать → каждый предмет теперь **сворачиваемая под-панель** (`doc-item`) со счётчиком
  готовности `N/M` + статус-чипом (готов ✓ / блок / в работе) в шапке. По умолчанию раскрыт
  только **первый** предмет, остальные свёрнуты (как секции-аккордеоны); клик перекрывает
  (`_docCollOpen`). Скан готовности — без раскрытия. Проверено в браузере на 5 предметах
  (первый открыт, остальные закрыты + ручной toggle). Выбран **вариант A** (аккордеон по предметам) — матрица/по-типу отпали:
  комплект доков зависит от вида залога (Недвижимость 4 / ТС 3 / Оборудование 2), колонки нестабильны.
- 2026-07-09 — **Сверка задач заявок (P3-R*) с эталонным мокапом** (`mockups/loan-application/loan-application.html`):
  все 31 задачу фазы 3 проверил против мокапа — функции по цитируемым якорям на месте,
  найдено 3 расхождения, все закрыты. **Гигиена (P3-R31):** удалена мёртвая статик-разметка
  `#tab-0` (~140 строк, фантомные поля «Дата подтверждения кредитных/залоговых документов»)
  + мёртвая JS `renderTab0` — таб теперь пустой контейнер, наполняется `renderGeneral`.
  **P3-R28:** кнопка формы создания `Далее`→**«Сохранить»** (одноэкранная форма, не мастер).
  **P3-R27:** снята устаревшая пометка «мокап — отдельной задачей» (номера `З-2026-000NNN`
  уже во всех демо-заявках; открыто только на серверный sequence). `TODO.md` синхронизирован.
- 2026-07-09 — **Мокап «Заявка» → документы (to-be)** (`mockups/loan-application/loan-application.html`):
  залоговые документы переведены на модель **по предметам залога** (стабильный `id`
  у каждого предмета, ключ состояния `<id предмета>::<docId>`; комплект — из справочника
  **вида** предмета). Полный ЖЦ документа (required→uploaded→review→accepted/rejected/
  waived/expired), роли (спец собирает / комиссия принимает / просмотр — всем), гейт
  «Отправить в комиссию» = обязательный комплект **+** коэффициент покрытия. Секция
  **«Залоговые»** вкладки **«Документы»** рендерит доки подгруппами по каждому предмету
  (вид · оценочная стоимость в подзаголовке) и входит в общую полосу готовности; вкладка
  **«Залог»** — регистр предметов (CRUD) + KPI (Предметов / Покрытие / Документы N/M) +
  сводный гейт со ссылкой на «Документы». `_docRerender` синхронно обновляет обе вкладки.
  Закрывает находку **P3-18**, отвечает **P2-R20 / P2-R21 / P3-R20**. Коммиты `a26307e`
  (влитие вкладки «Документы» to-be) → `0897ef6` (перенос залоговых доков в «Документы»).
- 2026-07-07 — **Мокап «Комиссии по заявкам»** (`mockups/loan-application-commission/commission.html`) —
  повторная сверка с live (`scripts/inspect/commission-sverka*.mjs`, скриншоты
  `.auth/commission-*-live.png`). Список приведён к текущему стенду: 2 строки
  (139 «Одобрено» + 138 «На рассмотрении»), плитка «Одобрено» 0→1, пейджер «2 строки».
  Добавлены 2 новых экрана + диалоги: **Просмотр** теперь data-driven по выбранной
  строке (138/139, разные Итоговое решение/История); **Создать·Изменить** открывает
  экран-редактор `/new`·`/{id}` (шапка + Состав комиссии с «Проголосовать» + Документы
  + Комментарии членов комиссии + Финальное решение с Одобрить/Отклонить/Запросить
  доп. информацию + Прогресс голосования + OK/Отмена). Кнопки голосования вызывают
  живые диалоги: «Ваш отзыв» (Решение/Риск/Комментарий/Файл) и «Подтверждение — Вы
  уверены? Да/Отмена». Решённую 139 «Изменить» не даёт (кнопка disabled, live-правило).
  Сверка mockup↔live `scripts/inspect/commission-mockshot.mjs`.
- 2026-07-06 — **Мокап «Кредиты»** (`mockups/loan-credit/loan-credit.html`) — точная
  копия раздела: список `/loansCredit` (11 колонок, 14 строк, «14 строк»-пейджер,
  Обновить/шестерёнка/Добавить условие поиска, Изменить/Просмотр) + деталь
  «Редактирование кредита» `/loan-credits/{id}` со всеми 11 вкладками (Общая информация,
  Оформление, Заемщик, Условия кредита с под-вкладками, График погашений, Транши,
  Резерв, Платеж, Код оплаты, Детальный расчет, Залог). Данные записи 18 («Номер рег. 56»,
  ОсОО «Бек Кабель»). Переключение вкладок, выбор строки, OK/Отмена работают как в
  оригинале. Инспекция `scripts/inspect/loan-credit-clone.mjs`, сверка скриншотами
  (`loan-credit-mockshot.mjs`) — ветка `mockup/loan-credit-page`.
  **Клики воспроизводят живые диалоги** (`loan-credit-clicks*.mjs`): каждый ••• открывает
  экран-браузер сущности (Фильтр/Обновить/Создать·Изменить·Удалить/«N строк»/грид/Выбрать)
  с реальными данными справочника (Вид кредита, Решение правительства, Кредитная линия,
  Счёт/Вид счёта погашения, Куратор→Субъекты); «Добавить» на Транши→«Создание транша»,
  на Платеж→«Платеж» (с живыми дефектами P7-02 Payment uuid/version + eager-required red);
  поля даты → календарь-попап. Сверка `loan-credit-mockshot2.mjs`.
- 2026-07-04 — **Мокап «Заявка»** — повторная сверка формы «Новая заявка» с живой
  системой (`scripts/inspect/app-create-fields.mjs`, `app-detail-tabs.mjs`). Список,
  9 вкладок детали, наборы документов и диалоги «Выберите комиссию» совпадают.
  Исправлены расхождения формы создания: (1) добавлен отсутствовавший селект «Метод
  расчета дней» (по умолч. «календарный 365»); (2) блок «Ручные параметры льготного
  периода» приведён к живому виду — 3 жирных подзаголовка + чекбоксы, чекбокс №1
  переименован «…по основному долгу» (было «…по основной сумме»), блок раскрыт по
  умолчанию.
- 2026-06-29 — **Мокап «Заявка»** (`mockups/loan-application/loan-application.html`) завершён:
  as-is клон полного потока — список заявок (грид, 8 колонок, 5 плиток-статистики) +
  мастер «Новая заявка» (все поля step 1) + страница детали (все 9 вкладок: Общая
  информация, График, 3 набора документов, Кредитная/Залоговая комиссия, История) +
  диалоги «Выберите комиссию» (кредитная/залоговая) со статус-тостом. Переиспользует
  дизайн-систему loan-program. Воспроизводит живые дефекты (серые поля P3-01, eager
  валидация P3-06, нет «Просмотр» P3-02), без улучшений. Живые находки 2026-06-29
  (по `scripts/inspect/app-capture.mjs`): добавлены 5 цветовых stat-cards (Всего/На
  рассмотрении/Одобрено/Отклонено/Требуется доп. информация); новый статус «Новый»;
  степпер детали с зелёной галочкой + датой под завершёнными шагами; поле диапазона
  «Допустимо: от … до …» в блоке одобренных условий; поле «Кредитная программа» во
  второй колонке Общей информации.
- 2026-06-29 — Loan-program creation form: completed a component-by-component owner
  review of all 8 input tabs (2 Сумма/срок · 3 Ставки · 4 Штрафы · 5 Льготный
  период · 6 Платежи/расчёты · 7 Документы · 8 Залог). Logged defects **P2-12…P2-25**
  in `qa-findings.md` and recommendations **P2-R15…P2-R21** in `TODO.md` (commit
  `e382424`). Cross-cutting rules fixed: admin-curated reference-books + junk-checks
  (feed future reports/analytics), range validation `0<мин≤макс` + soft equal-warning,
  «Маржа = read-only mirror of base rate» (dev duplication defect), textarea 1000+counter,
  explicit units (мес./%/% годовых), cross-grid dedupe, collateral gating on the checkbox. (`mockups/dictionaries/dictionaries.html`) complete: 50 sections, toolbar/grid/modal/picker-demo (P2-R9 select-only dialog, no CRUD). Docs map updated in README.
- 2026-06-23 — Написано ТЗ as-is подсистемы кредитования (`requirements/tz/`): головной index + 7 разделов по этапам жизненного цикла, полная live-ре-инспекция.
- 2026-06-23 — Loan-program mockup (`mockups/loan-program/loan-program.html`): **applied P2-R1
  field visual semantics** — flipped every editable control (input/select/textarea/
  lookup/mscombo) from the live grey "filled" look (defect P2-01) to **white bg +
  full border**; read-only/computed fields (Предпросмотр tab, «Выбранные шаблоны»,
  «Валюта» …) to **grey, no border**; required fields keep the red `*` shown
  immediately on open. Verified across tabs 1/7/9 (`.auth/p2r1-tab{1,7,9}.png`).
- 2026-06-23 — Loan-program mockup (`mockups/loan-program/loan-program.html`): finished the
  add-form wizard clone — tabs 6 «Платежи и расчеты», 7 «Документы», 8
  «Залоговое обеспечение», 9 «Предпросмотр» now match live Jmix 1:1 (inspect
  scripts `scripts/inspect/tab6-9*.mjs`, screenshots in `.auth/`). All 9 tabs
  cloned. Fixes vs prior placeholders: tab 8 fields are NOT conditional on the
  checkbox (live always shows them); tab 7 gained the missing залоговые
  doc-grids; dropped wrong `req:true` flags on tabs 6/8; wired REFBOOKS for the
  `dayCount/queue/interestCalc/checkLevel/templates` ••• pickers (latent break);
  removed orphaned `miniGrid()`.
- 2026-06-20 — Security/access config: inspected Безопасность → Ресурсные роли (`/sec/resourcerolemodels`, 15 roles) + Роли уровня строк (`/sec/rowlevelrolemodels`, 1 role) + Users via Playwright (MCP). Wrote how-to [guides/access-control.md](guides/access-control.md) (control sidebar menu/pages/buttons/fields/rows). Observations to log: `банки` role = complete CRUD template; `СУГС` & `Специалист кредитного отдела` roles have **no policies** (grant nothing); junk roles `123` (resource) + `test` row-level (empty Where) = test pollution; Users list ships a saved filter `test` hiding all rows.
- 2026-06-20 — Phase 7 (Servicing): documented `/payments` (Платеж), `/loan-reserves` (Резерв), `/loan-ledgers` (LoanLedger) via Playwright (`scripts/inspect/p7.mjs`). Logged 5 findings (P7-01 🟠 i18n leak across all 3 grids incl. `EventType.*` enum; P7-02 🟠 `Payment uuid`/`version` columns exposed; P7-03 🟡 duplicate raw «Статус»; P7-04 🟡 reserves have no detail view; P7-05 🟡 ledger «Удалить» on append-only log) and 5 proposals (P7-R1…R5). Cross-check: payment split (ОД+проценты) and reserve math (unused×rate) correct.
- 2026-06-20 — Phase 6 (Disbursement & tranches): documented `/sub-loans` (tranches, 4-tab detail) + `/disbursements` (Освоение) via Playwright (`scripts/inspect/p6-probe.mjs`, `p6.mjs`); model Кредит→Транш→Освоение. Logged 3 findings (P6-01 🟠 raw entity labels `SubLoan.amount`/`Disbursement.subLoan`; P6-02 🟡 empty tranche schedule; P6-03 🔵 «шрафы» typo) and 3 proposals (P6-R1…R3). Cross-check: disbursement-level annuity schedule computes interest correctly (50 000 @10%/24mo → 2 307,25).
- 2026-06-20 — Phase 5 (Loan issuance): documented `/loansCredit` + 11-tab loan detail (`/loan-credits/{id}`) via Playwright (`scripts/inspect/loans*.mjs`); logged 8 findings (P5-01…08, incl. **🔴 P5-01** data-dependent detail crash on rec 20/22) and 5 proposals (P5-R1…R5). Cross-check: loan-level schedule computes interest correctly (contra app-level P3-R11).
- 2026-06-17 — Phase 2 (Loan program): documented `/loan-programs` + 9-tab create wizard via Playwright; logged 6 findings (P2-01…06) and 6 proposals (R8–R13). QA pass (required-field validation). Stack refined to **Jmix on Vaadin**. New Sheet tab «Кред. программы».
- 2026-06-17 — Phase 1 (Gov decision): verified R2/R4/R6 in-app; all 6 proposals confirmed (see findings P1-03/04/05). Stack = Vaadin.
- 2026-06-17 — Phase 1 (Gov decision): documented + logged 6 improvement proposals (R1–R6).
- 2026-06-17 — Logged into test env; reconstructed and documented the full module map.
- 2026-06-17 — Workspace set up.
