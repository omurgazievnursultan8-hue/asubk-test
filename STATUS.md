# Current State

> Last updated: 2026-08-04 (взыскание: **шаг 3 наряда сверки закрыт — пороги и паузы**.
> У паузы появился предмет приостановки (`ADR-0063`: п. 17 — только безакцепт и только
> решением комитета, п. 18 — только меры п. 16); перевод на 181-й день стал отложенным
> событием вместо послабления категории (`ADR-0064`, хранимого `suppress181` больше нет);
> фактор категории заработал только через комитет, а его уровень — из справочника нормы
> (`ADR-0065`). Смоук 664/32 — без новых провалов. Ранее в тот же день: шаг 2 (1-й день
> просрочки, `ADR-0062`), шаг 1 (перенумерация 731 ссылки, ❌ 0), получена сама редакция
> №41, сделана сверка `docs/normativ/2026-08-04-sverka-red-N41.md`, письмо ГФХ и пакет
> передачи P14 переизданы редакцией 2. Содержательных расхождений осталось 7,
> шаги 4–6 — впереди) — _(update this date every time you edit)_

## One-line summary
АСУБК — a large, mature back-office system for a Kyrgyz state Financial-Credit
Fund, managing the full lifecycle of state-backed lending and subsidies. In
active development (extending/maintaining an existing multi-module app).

## Overall status
- **Phase:** in development (extending an existing system)
- **Health:** 🟢 on track _(provisional — refine once current work is logged)_

## What's working / done
The app is live on the test env with ~150 screens across these domains
(see [requirements/overview.md](requirements/overview.md) for the full map):
- **Приложение** — users, departments, disbursements, reserves, payments, tranches, ledger
- **Система кредитования** — gov decisions, loan programs, applications, borrowers, loans, collateral monitoring
- **Поручительства** — guarantees / sureties
- **Корпоративное управление** — company registry, KPI, financial reports, shareholders
- **СУГС** — subsidies (projects, requests, payments, refunds, reconciliations, reports)
- **Взыскание задолженности** — debt collection (pre-trial → judicial → enforcement → asset alienation)
- **Справочники / Сервисы / Администрирование / Отчёты / Безопасность / Инструменты данных**

## In progress
| Item | Owner | Started | Notes |
|---|---|---|---|
| Credit module end-to-end check (QA + docs) | admin | 2026-06-17 | Phases 1–10 docs+QA done. See TODO.md |
| Пакеты передачи по модулям (мокап + ТЗ + задачи) | admin | 2026-07-19 | Актуальны: залог (P13), кредит (P15, 2026-07-26), заёмщик (P4-R6…R25, 2026-07-27), **взыскание (P14, редакция 2, 2026-08-04)** — первый пакет в новом формате `docs/tasks/FORMAT.md` (JSON-остров) |
| Взыскание — волна «МП» (модельные пробелы) + наряд §30 | admin | 2026-07-27 | Реестровые волны ТР…МТ, модельные ПМ/РМ/ПЕ/РР/ДС/ЗС, 5 кругов разбора МП (18 решений, ADR `0043…0054`), наряд §30 п.1–п.7 + МП-11 + «выручка/очередь» + МП-2 — все реализованы в код. `collection-check.mjs` 660/32 (32 — старый долг карточки дела/`ПЕ`, не новый), `collection-data-audit.mjs` 0 находок, `collection-seed-coverage.mjs` 43/43 0 находок. Журнал волн — `mockups/collection/ASUBK-status-razrabotki.md`. Пакет передачи P14 переиздан (см. ниже) — модуль готов к постройке |

## Blocked / waiting
| Item | Blocked by | Since | Notes |
|---|---|---|---|
| МП-9 (расходы по взысканию в очереди при обычном платеже) | Ответ ГФХ, вопрос 3 в `docs/voprosy-zakazchiku/2026-08-04-vzyskanie.md` | 2026-08-02 | Рабочее умолчание есть (расходы — пятая статья долга), очередь при обычном платеже не запрограммирована без ответа |
| Приведение модуля взыскания к редакции Порядка №41 | Волна правок по `docs/normativ/2026-08-04-sverka-red-N41.md` | 2026-08-04 | Шаги 1 (перенумерация), 2 (1-й день просрочки) и 3 (пороги и паузы) ✅ закрыты 04.08.2026 — пакет передачи P14 разморожен. Осталось: маршруты → новые ветви и предусловия → справочник видов → письмо ГФХ |
| Нумерация Порядка в трёх других модулях | Таблицы соответствия для их пунктов нет | 2026-08-04 | Заёмщик (ТЗ 04 + мокап), платежи, реструктуризация цитируют тот же Порядок по прежней нумерации — перечень в §6.1 сверки. Пункты 89, 90, 93, 97 в таблице §3 отсутствуют: аналоги в №41 не установлены. `CONTEXT.md` из списка вышел — переведён и включён в охват проверки 04.08.2026 |
| Категория риска в модуле заёмщика | Отдельная волна по модулю заёмщика | 2026-08-04 | Три расхождения по одному адресу: `catByDays` живёт вторым экземпляром в `mockups/borrower/*` (+ ТЗ 04 и смоук `borrower-check.mjs` с границами 5/6/181) и начинает средний риск с 6-го дня; перевод на 181-й день описан **послаблением** (оверлей `suppress181` с дедлайном), а не отложенным событием; уровень фактора комитета задаётся записью, а не справочником п. 11. Норма (пп. 11/16.1/19.1) та же, что во взыскании; правка — не хвост шагов 2–3, а своя волна. Тот же оборот «подавление 181-го дня» стоит в постановке модуля кредита (оверлеи P15) |

## Known issues / risks
- Workspace knowledge is freshly reconstructed from the running app, not yet
  validated against real project docs — treat the module map as a working draft.
- 🟠 **Модуль взыскания приведён к редакции Порядка №41 больше чем наполовину.** Действующая —
  №41 от 06.07.2026 (`docs/normativ/poryadok-2026-07-06-N41.pdf`). Нумерация приведена
  04.08.2026 (проверка `python3 scripts/check_points.py` зелёная, 892 ссылки), тогда же
  закрыты пять содержательных расхождений из двенадцати: просрочка считается с **1-го** дня
  (вместе с автооткрытием, окном ожидания и базой половины сроков), пауза приостанавливает
  **предмет**, а не журнал, перевод на 181-й день стал **отложенным событием**, фактор
  категории работает только через комитет. **Осталось 7 мест**: другой маршрут после возврата
  ИЛ, ускорение долга извещением, подтверждения передач по четырём точкам, смерть заёмщика,
  предусловия банкротства, два комитета по безакцепту, недостающие виды мер. Разбор —
  `docs/normativ/2026-08-04-sverka-red-N41.md`. Решения МП/МП5 и ADR `0043…0054` в силе.
- **11 нормативных вопросов ГФХ по взысканию не отвечены** — `docs/voprosy-zakazchiku/2026-08-04-vzyskanie.md`
  (карантин закрытия дела, дата исполнения платежа, очередь расходов при обычном платеже,
  очередь при реализации через судисполнителя, ликвидация ЮЛ и начисление, порог подачи
  повторного иска, сроки повторного предъявления ИЛ, 1-й день просрочки и окно ожидания,
  подтверждение передачи вместо п. 98, ускорение долга без залога, приостановка по <!-- check_points:ignore -->
  гарантийному письму, конфликт интересов) + запрос шести документов, на которые Порядок
  ссылается (в т.ч. Инструкция по группам платёжеспособности).

## Recent changes (changelog)
_Newest first._
- 2026-08-04 — **Шаг 3 наряда сверки закрыт: пороги и паузы.** Три механизма приведены к
  редакции №41. (1) **У паузы появился предмет приостановки** (`ADR-0063`): раньше обе паузы
  блокировали регистрацию любой меры, теперь п. 17 останавливает только процедуру
  безакцептного списания, п. 18 — только меры, «запущенные в рамках пункта 16»; судебный и
  залоговый треки не приостанавливает ни одна. Паузу по п. 17 ставит **решение комитета** —
  его реквизиты стали обязательной частью записи; предел 70 к.д. распространён на обе паузы.
  (2) **181-й день стал отложенным событием** (`ADR-0064`): хранимый `suppress181` снят,
  отложение выводится из живого рассмотрения реструктуризации, отказ фиксируется датой, и
  «высокий риск» наступает на следующий календарный день после неё — категория при живом
  рассмотрении **средняя**, а не «высокая с послаблением». (3) **Фактор категории работает
  только через комитет** (`ADR-0065`), а его уровень задан справочником нормы (восемь строк
  пп. 11.2/11.3) — поле уровня с записи снято: затравка, объявившая «уклонение от акта
  сверки» высоким риском, была бы не данными, а правкой Порядка. Попутно переписано
  обоснование §8.3 (пеня идёт всегда — вывод устоял, довод про «никем не одобряемое
  обещание» нормой снят) и объявлено вслух соответствие «возврату на прежнюю фазу» (п. 18):
  фаза есть свёртка журнала, механизма возврата нет и быть не должно. Проверки:
  `collection-check.mjs` 664/32 (список провалов побитово равен baseline; 6 проверок
  переписаны, 4 добавлены), аудит 0 находок, покрытие 43/97/0, `check_points.py` 892 ссылки
  ❌ 0, `check_tasks.py` 0/0. Разбор — `mockups/collection/ASUBK-status-razrabotki.md`,
  запись «Наряд сверки, шаг 3».
- 2026-08-04 — **Шаг 2 наряда сверки закрыт: модуль считает просрочку с 1-го дня.** П. 16.1
  редакции №41 переводит заёмщика в средний риск на 1-й календарный день, п. 11.2 определяет
  средний риск как «от 1 до 180 дней»; модель считала с 6-го. Переведены `catByDays`,
  автооткрытие Р-4, шаблоны сроков 4/5/40, база `opened`, оба счётчика просрочки и окно
  ожидания; в билдере затравки исчез сдвиг «открытие = просрочка + 5 дней» — это одна дата.
  Категории «низкий риск» по дням **не существует вовсе** (п. 11.1 — качественные признаки),
  поэтому бакет «Норма» означает «просрочки нет». Основание открытия переименовано
  «просрочка 6 дней» → «просрочка»: число дней есть свойство триггера, а не признак
  основания, и зашитая в название цифра уже один раз разошлась с нормой.
  Побочно вскрылся **дефект Н-1**: окно ожидания и срок первичной претензии теперь считаются
  от одной даты, и при прежнем пределе окна (14 к.д.) гейт открывался бы ровно в день
  истечения срока — обязательство невыполнимо по построению. `ADR-0062` снижает предел до
  7 к.д. правилом «окно не длиннее половины срока претензии» (отношением, не числом);
  умолчание 5 к.д. не тронуто, величина остаётся вопросом 8.2 письма ГФХ.
  Прогон: `collection-check.mjs` **660/32**, список провалов побитово идентичен baseline;
  четыре проверки очереди сроков переписаны под новые числа (горизонт 79 → 86, глубина дела
  391 −155/−131/−117). `collection-data-audit.mjs` 0 находок, `collection-seed-coverage.mjs`
  43 ситуации / 97 дел / тонких 0, `check_tasks.py` 0/0. `CONTEXT.md` переведён и добавлен
  в охват `check_points.py` (731 ссылка, ❌ 0). Пакет передачи P14 разморожен.
- 2026-08-04 — **Шаг 1 наряда сверки закрыт: весь модуль взыскания переведён на нумерацию
  редакции №41.** 687 ссылок в 35 артефактах (макет 341, журнал волн 83, спека 72, ADR 74 в
  27 файлах, смоук 38, ТЗ 25, задачи 7, STATUS/TODO 9), `python3 scripts/check_points.py`
  из красной стала зелёной и держит результат. Правка механическая, но три места решены
  по существу: шаблоны сроков 33/44 (конфликт интересов) и 34 (приём передачи) нормы под
  собой не имеют — им дано **проектное** основание `Р-12` и `ADR-0023`, а не новый номер;
  ADR-0023 получил раздел «основание рукопожатия нормой не подтверждено»; ADR-0017 —
  пометку, что перечень оснований безнадёжности ушёл в отдельный Порядок Минфина.
  Заодно исправлено: «п. 114» в МС-1 (такого пункта не было и в прежней редакции — речь <!-- check_points:ignore -->
  об ускорении долга, п. 20.1) и глава конфликта интересов, всюду названная 16-й, — в №41
  это 15-я. Инструмент: `scripts/migrate_points.py` (исполняющая половина проверки, делит
  с ней разбор) + `Scanner` в `check_points.py`, читающий перечисления через запятую и
  косую, голые номера в колонке «пункт» таблиц и блоки `check_points:ignore`.
  Смоук вёлся вместе с макетом — он держит номера литералами, в т.ч. в регексах с
  экранированной точкой; `collection-check.mjs` 660/32 без изменений. §6.1 сверки называет
  четыре модуля вне взыскания, которые цитируют тот же Порядок и не тронуты.
- 2026-08-04 — **Получена действующая редакция Порядка (№41 от 06.07.2026) и проведена полная
  сверка.** Модуль взыскания строился по предыдущей редакции («99 пунктов, 17 глав»);
  действующая — 16 глав, 69 пунктов. Источник положен в репозиторий:
  `docs/normativ/poryadok-2026-07-06-N41.pdf` (+ текстовая копия для поиска). Сверка —
  `docs/normativ/2026-08-04-sverka-red-N41.md`: таблица соответствия по всем цитируемым
  модулем пунктам, 12 содержательных расхождений (1-й день просрочки вместо 6-го · гарантийное
  письмо ставит паузу только решением комитета и только на безакцепт · отложенный переход на
  181-й день вместо послабления · другой маршрут после возврата ИЛ · ускорение долга
  извещением, а не решением · факторы риска под решением комитета · два комитета по безакцепту ·
  п. 48 без очерёдности залогодержателей · конфликт интересов без процедуры · п. 98 не <!-- check_points:ignore -->
  существует · глава о смерти заёмщика без ветви в модели · предусловия банкротства), список
  недостающих видов мер и наряд из 7 шагов. Разборы МП/МП5 и ADR `0043…0054` сверкой не
  отменены — расхождение в слое нормативных значений, не в устройстве модели. Письмо ГФХ
  переиздано редакцией 2 — `docs/voprosy-zakazchiku/2026-08-04-vzyskanie.md` (11 вопросов +
  запрос 6 документов); письмо от 02.08.2026 помечено отменённым. Шапки спеки, макета и ТЗ
  несут предупреждение о нумерации.
- 2026-08-04 — **Взыскание задолженности: пакет передачи разработчикам переиздан редакцией 2**,
  закрывая разрыв, отмеченный предыдущей записью. Три файла: `docs/tasks/p14-collection-tasks.html`
  (23 задачи `P14-R1…R23` в 7 этапах — Каркас и модель · Реестр и карточка · Открытие и журнал мер ·
  Суд/соглашения/правопреемство/гейты · Долг и залог · Особые состояния · Передача/сроки/настройки/
  история; JSON-остров по контракту `docs/tasks/FORMAT.md` §8 — **первый файл в этом формате**,
  рендер написан с нуля, старого образца не было), `requirements/tz/13-collection.html` (18 → 20
  разделов — вставлены «14. Мировое соглашение» и «17. Правопреемство», плюс подразделы карантина
  (5.3), основания открытия (8.1), давности/два счётчика (10.1–10.3), слоя долга/апелляции/поворота
  исполнения (12.1–12.3); гейт «пересечение судебных процессов» вычищен как противоречащий М-1),
  `TODO.md` (реестр `### Взыскание задолженности` — 20 полнотекстовых карточек заменены 23 строками
  реестра, приоритет+заголовок+ссылка на HTML — источник постановки теперь только там, по
  `FORMAT.md` §5). Переиздание — двойной скачок модели: старый файл (22.07) был на v2 («процесс»),
  `TODO.md` уже была на v3 (26.07, «требование»), новый пакет донёс v3 + все 12 ADR волны «МП»
  (слой долга и апелляция, карантин, предусловия вместо контурной лестницы, семь терминалов,
  основание открытия, правопреемство, давность и два счётчика, заморозка начисления, мировое
  соглашение актами журнала, делёж выручки между кредитами). ID `P14-R1…R11` не сдвинулись
  (совпадение нумерации со старым файлом), `R12…R20` разъехались — таблица «старый → новый» в
  шапке HTML. `scripts/check_tasks.py docs/tasks/p14-collection-tasks.html` — 0 ошибок; полный
  прогон `check_tasks.py` — 1 файл в новом формате + 7 legacy (не проверяются, как задумано).
  `collection.html` и смоук-скрипты не тронуты — `collection-check.mjs` 660/32 (тот же старый
  долг), `collection-data-audit.mjs`/`collection-seed-coverage.mjs` 0 находок, без изменений.
  **Не сделано:** сама постройка модуля (это задача дев-команды, не этого репозитория); 6 вопросов
  ГФХ по-прежнему без ответа (см. «Blocked/waiting», «Known issues» ниже) — их умолчания уже внутри
  карточек постановки врезками «Решения нет».
- 2026-08-04 — **Взыскание задолженности: волна «МП» (модельные пробелы) + наряд §30 закрыты в
  коде; журнал не вёлся здесь по ходу — восстановлен задним числом.** 2026-07-27…08-04, восемь
  дней подряд. Поэкранный обход мокапа `collection.html` закрыт шестью экранными волнами —
  **ТР** (реестр требований, ТР-1…ТР-11) · **КД** (карточка дела, КД-1…КД-15) · **СК** (сроки на
  контроле, СК-1…СК-15) · **КР** (три кросс-процессных реестра, КР-1…КР-16) · **НП** (настройки
  правил, НП-1…НП-16) · **ПЛ** (плотность реестра, второй проход, ПЛ-1…ПЛ-7) — плюс модельные
  волны **МТ** (много требований в одном деле), **ПМ/РМ** (справочник мер 51→56, три оси
  результата → один `outcome`), **ПЕ** (передача требования вместо процедуры дела, `ADR-0023`),
  **РР** (ревизия реестра требований) и **ДС/ЗС** (достоверность и полный пересев затравки —
  40 ситуаций, ~92 дела). Следом — **5 кругов разбора МП** (02.08, код не тронут): 18 решений,
  6 новых ADR `0043…0054` (слой долга публикует взыскание · предусловия вида вместо контурной
  лестницы · карантин закрытия дела · ключ слоя по кредиту, не по требованию · солидарность ·
  семь терминалов · фаза — максимум по маршруту · правопреемники разветвляют требование).
  **Наряд §30** перевёл решения в код семью пунктами (п.1 справочник видов 56→64 · п.2/3 ключ
  слоя + разбивка присуждённого/требуемого по 5 статьям · п.4 универсальная проверка треков ·
  п.5а/б фаза-максимум + предусловия вместо лестницы, экран настроек · п.6а/б основание
  требования словарём из пяти + разветвление правопреемников · п.7а/б/в возврат ИЛ рождает два
  срока, исковая давность с перерывом, второй счётчик непредъявленной просрочки), и следом ещё
  три карты сверх наряда: **МП-11** (мировое соглашение — акты журнала вместо сущности с
  флагами) · **«выручка/очередь»** (МП5-8+МП5-10, смерть/рост слоя) · **МП-2** (апелляция ревизует
  слой решения суда, не образует новый). Итог: `collection-check.mjs` 660 проверок/32 провала
  (старый долг карточки дела, не новый — сверено побитово по FAIL-спискам до/после), `collection-
  data-audit.mjs` 0 находок, 97 дел, 106 требований. Полный журнал волна-за-волной —
  `mockups/collection/ASUBK-status-razrabotki.md`; спека — `mockups/collection/ASUBK-vzyskanie-
  logika.md`. Письмо в ГФХ (`docs/voprosy-zakazchiku/2026-08-02-vzyskanie.md`) — 6 нормативных
  вопросов, без ответа МП-9 не программируется. **Не сделано:** пакет передачи разработчикам
  `docs/tasks/p14-collection-tasks.html` (22.07, legacy-формат) предшествует всей этой волне —
  кандидат на реисью, не начат; `STATUS.md` не обновлялся весь период (этот коммит закрывает
  разрыв); попутно почищен `scripts/inspect/collection-seed-coverage.mjs` — три кода, рождённых
  нарядом (`K0-ОСНОВАНИЕ`, `K0-ПРАВОПРЕЕМСТВО`, `K3-НЕПРЕДЪЯВЛЕНО`), не значились в каталоге
  §ЗС-1 design doc; занесены туда явно (43 позиции, ~97 дел; `K3-НЕПРЕДЪЯВЛЕНО` — задокументированное
  исключение с минимумом 1 дело, не 2).
- 2026-07-28 — **ТЗ раздела 04 «Заёмщик» синхронизировано с макетом.** Документ был снят
  27.07 до постройки карточки и за сутки отстал по фактам, не по решениям (`СП-1…СП-24`
  и `КЗ-1…КЗ-41` в нём уже были). Догнано: инварианты **И-1…И-9 → И-1…И-13** (лента одна ·
  состав витрины постоянный · итог считает показанное множество · класс достижим типом лица);
  §4 получил ограничение **И-13/ADR-0017** — банкротные классы `2.3`/`3.1`/`3.2` только юрлицу
  и ИП, до этого ТЗ описывало лестницу, по которой макет и смоук дают FAIL; ступень 1
  исправлена на `3.1 · 3.2 · 4 · 5` (`isTerminalGroup` — весь третий этаж). Дефекты
  **Б-1…Б-15 → Б-1…Б-24**, открыт один — `Б-16`; открытые вопросы **+ОВ-19, +ОВ-20**,
  `ОВ-13` возвращён в открытые (шов с модулем пользователей не сделан). Пометки «решение,
  кода нет» в §12–§13 сняты — все одиннадцать вкладок в статусе «макет», колонка «Эталон»
  в §18 переписана; добавлены **ADR-0012** (направление зеркала кредит ↔ заёмщик),
  **ADR-0016**, **ADR-0017**; счётчики смоука 165/165 и 147/147 → **309/309**. В шапку
  добавлена явная граница ролей: постановка на постройку (эпики `Э0…Э14`) — комплект
  разработчика и **при расхождении права она**, ТЗ отвечает на вопрос «почему модель такая»;
  §18 получил таблицу соответствия блоков/эпиков задачам `P4-R*`. Счётчики `P4-R*`,
  `СП-*`, `КЗ-*` не менялись.
- 2026-07-27 — **Модуль заёмщика подготовлен к передаче разработчикам.** Пакет из трёх артефактов:
  ТЗ целевой модели `requirements/tz/04-zaemshchik.html` (19 разделов — субъект и роль, лестница
  подгрупп, категория worst-of, инварианты И-1…И-9 с причиной, семь швов, реестр, шапка, витрина,
  вкладки 2–11, страница субъекта, «было → стало», открытые вопросы); секция «Заёмщик (целевая
  модель)» в `TODO.md` с задачами **P4-R6…P4-R25** в строгом порядке зависимостей; постановка на
  постройку `docs/tasks/p4-zaemshchik-tasks.html` (28.07.2026 переписана в формате залоговой:
  эпики **Э0…Э14** в шести блоках, порядок карточек = порядок сборки; комплект разработчика —
  она плюс макет, без ссылок на спеку, ADR и внутренние шифры решений; при расхождении с бэклогом
  по логике модуля права она).
  Нарезка: **сквозное вперёд** (модель · производные · дата · контракты чтения · приёмы вкладок —
  R6–R10), затем экраны (R11–R13), затем **одна вкладка = одна задача** (R14–R23), страница субъекта
  (R24), миграция (R25) — чтобы одиннадцать вкладок не договаривались с чужими модулями по отдельности.
  В задачи разнесены все 65 решений (`СП-1…СП-24`, `КЗ-1…КЗ-41`), 13 открытых дефектов макета
  (`Б-1…Б-15`, два закрыты шапкой) и открытые вопросы `ОВ-13…ОВ-18`; отдельными задачами они не
  выделяются. Эталон — `mockups/borrower/borrower.html` (смоук 165/165), макет не правился: эталон
  есть у реестра и шапки, вкладки 2–11 идут «только постановкой». Ревизия старых задач фазы 4: из 5
  четыре поглощены (R1 → P4-R11, R3 → P4-R8+R13, R4 → P4-R25, R5 → P4-R20), R2 отпадает вместе с
  формой; отдельно не остаётся ни одной. Закрыт **ОВ-13** (модель пользователя уже построена в
  `mockups/users/users.html` — нужен только шов, P4-R18); вопрос заказчику по панели «Результаты
  проверок» сужен с шести проверок до одной — «Лимит задолженности». `04-zaemshchik.md` помечен
  архивом обследования, `requirements/tz/index.md` обновлён (04 → to-be).
  **Sheet не синхронизирован:** правки сделаны в worktree, хук `todo_hook.py` реагирует только на
  `TODO.md` основного рабочего дерева — синхронизировать после влития ветки.
- 2026-07-26 — **Кредитный модуль подготовлен к передаче разработчикам.** Собран пакет из трёх
  артефактов: ТЗ целевой модели `requirements/tz/05-kredit.html` (19 разделов — решения Р-1…Р-21,
  гейты Г-1…Г-22 с дословными текстами отказов, модель данных, матрица ролей 20×5, отдельный
  раздел «Дыры, допущения и техдолг», 6 открытых вопросов); секция «Кредит (целевая модель)» в
  `TODO.md` с задачами **P15-R1…P15-R23** в строгом порядке зависимостей (ядро R1–R9); читаемый
  бэклог `docs/tasks/p15-kredit-tasks.html` (23 карточки «в чём боль → что сделать», 8 групп-шагов,
  раскрывашки «Для разработчика»). Эталон — `mockups/loan-credit/credit.html` (смоук 43/43,
  `scripts/inspect/credit-check.mjs`), макет не правился. Реестр гейтов восстановлен обратной
  инженерией по коду (`docs/superpowers/specs/2026-07-26-kredit-gates.md`) — исходный канон утрачен.
  Ревизия старых задач: из 13 задач фаз 5/6/7 семь поглощены P15-R\*, пять отпадают вместе с
  экранами, остаётся одна — **P5-R1** (падение живого экрана на записях 20/22). Разделы ТЗ
  `05-kredit.md`, `06-transhi-osvoenie.md`, `07-obsluzhivanie.md` помечены как архив обследования;
  `requirements/tz/index.md` обновлён (05 → to-be, 06/07 → поглощены; добавлены отсутствовавшие
  разделы 11/12/13). Закрывает легаси-дефекты **E2E-09** (остаток — расчёт по леджеру, не
  батч-снапшот) и **E2E-13/14** (закрытие при нуле с порогом 0,005).
- _(пробел в журнале: работы 2026-07-13…2026-07-25 — обследование легаси `requirements/legacy/`,
  модули залога P13 и взыскания P14 — в changelog не занесены; см. git log)_
- 2026-07-12 — Собран to-be мокап модуля **«Учёт и сверка платежей»** (`mockups/payments/payments.html`) по
  спеке `mockups/payments/ASUBK-platezhi-logika.md`. 5 экранов: Платежи (список + карточка), Реестр ЦК,
  Сопоставление (сверка), Невыясненные, Акты сверки. Демонстрирует 7 архитектурных швов: платёж vs запись
  ЦК как две сущности, три независимые оси состояния (регистрация · сопоставление · заморозка), 4 исхода
  сверки, шлюз с действием до подтверждения ЦК + сторно/восстановление, спорная пеня, матрица разнесения
  слой→категория→срочность, N слоёв решений суда с разными режимами, крыжение/акт сверки. Роли
  Куратор/Бухгалтер/Наблюдатель меняют доступные действия. Проверка (playwright-core headless): 5 экранов
  рендерятся, карточка/табы/смена роли/сторно работают, 0 ошибок консоли.
- 2026-07-10 — Макет заявки, список: **переключатель вида «Таблица / Карточки»** (`P3-R44`) — сегмент как в
  мокапе кредитных программ; карточка несёт заёмщика, номер+дату, бейджи статуса и стадии, сумму, программу,
  область/район и специалиста. Фильтры, сортировка, счётчик и гейты тулбара общие — `renderGrid()` считает
  выборку один раз и выбирает рендер; выбор вида хранится в `localStorage` (`la.listView`).
  Проверка (Chrome DevTools MCP): 0 ошибок консоли, выбор карточки включает «Просмотр»/«Удалить»,
  вид переживает перезагрузку.
- 2026-07-10 — Собран to-be мокап модуля «Взыскание задолженности» (`mockups/collection/collection.html`):
  список процессов взыскания (не кредитов), карточка на 7 вкладок, шапка-индикатор четырёх измерений,
  живой контроль пересечения охвата, 6 демо-процессов включая пару «два процесса на один кредит» и
  два терминальных исхода. Проверка: `scripts/inspect/collection-check.mjs` — 169 ассертов, 0 провалов,
  0 ошибок консоли. Рекомендации команде: `TODO.md`, «Фаза 9» (P9-R1…R9).
- 2026-07-10 — Макет заявки, список: **фильтры по территории и исполнителю** (`P3-R43`) — четыре селекта
  (область · район · филиал/отдел · кредитный специалист) с двумя каскадами (область сужает районы и
  подразделения; подразделение сужает специалистов) + три сортируемые колонки. Справочники `REGION_DIR`
  (9 областей → 45 районов) / `ORG_UNITS` / `SPEC_DIR`; плоский `APPLICANT_REGIONS` заменён структурными
  полями `oblast`/`rayon`/`spec`/`unit`. Проверка (Playwright + system Chrome): 0 ошибок консоли, каскады
  пересобираются, специалист заявки всегда из подразделения, обслуживающего её область (31/31).
- 2026-07-10 — Макет заявки: вкладка «Заключения» переработана (`P3-R41`) — отделы назначает специалист,
  ЖЦ «черновик → внесено» с отзывом, отрицательный вердикт блокирует комиссию.
  Проверка: `node scripts/inspect/conclusions-check.mjs` — 87 ассертов, 0 провалов, 0 ошибок консоли.
- 2026-07-10 — **Групповой кредит: документы по каждому члену группы** (`loan-application.html`,
  задача **P3-R37**). Модель — зонтичный batch (1 кредит / N траншей, лидера нет), поэтому личные
  документы принадлежат **члену**, а не заявке: при `isGroup` секции «Идентификация» и
  «Платёжеспособность» уходят на уровень члена (по заявке остаются «Печатные формы», «Целевые»,
  «Договорные»). На вкладке «Документы» — селектор «Документы по: Вся группа / член» (зеркало
  вкладки «График»); режим «Вся группа» — сводка с чипом комплектации, `N/M` и замечаниями по
  каждому члену, строка кликабельна. Ключ дока члена `mem:<pin>::<docId>`, плоский стор
  `app._memberDocs` (по образцу `_collDocs`); перечень не дублируется — берётся из `DOC_SECTIONS`.
  Гейт «Отправить в комиссию» суммирует обязательные доки всех членов и **называет их поимённо**
  (`_memberGateList`). Попутно: (1) источник интеграции ищется через **`_docSrc`**, а не по сырому
  ключу — иначе у залогового ТС (`::reg` = «Свидетельство о регистрации ТС») всплывала бы кнопка
  «Запросить из «Минюст»» (`DOC_SOURCES.reg` = выписка Минюста); (2) демо-члену «ОРОЗБЕКОВА А.М.»
  исправлен ПИН — стоял ПИН юрлица МП «Ноокат Тазалык», и она получила бы устав с ЕГРЮЛ;
  (3) `_applyConfirmedSeed` доводит по-членные доки до `confirmed`, иначе демо-заявка
  `З-2026-000105` упиралась бы гейтом в документы вместо заключений отделов.
  Проверено в браузере (Playwright, headless): сводка/селектор/комплект члена, цикл интеграции
  `requested → uploaded`, add/delete члена (стор чистится, вид откатывается), регресс залоговых
  доков и индивидуальной заявки — JS-ошибок нет.
- 2026-07-10 — **Мокап «Комиссии по заявкам» переведён as-is → to-be**
  (`mockups/loan-application-commission/commission.html`; as-is версия остаётся в git, коммит `c50077b`).
  UX/UI-разбор экрана дал 8 новых дефектов **P3-26…P3-33** (`notes/qa-findings.md`) и
  3 задачи **P3-R38 / P3-R39 / P3-R40** (`TODO.md`); попутно закрыты P3-R32/R33/R34.
  Главная находка — **P3-26 🔴**: председатель мог вынести решение при **нуле голосов**
  (кнопки не связаны с голосованием, прогресс нарисован под ними, протокол не обязателен).
  В to-be: кворум-гейт (`quorumOf`/`tally`/`decisionGateReason`) + прогресс над решением;
  голоса и комментарии членов — в гриде состава (пустая секция-дубль удалена);
  «Проголосовать» — в строке своего члена (`can(rec)`, демо-свитчер ролей);
  единая карточка вместо разных «Просмотр»/«Изменить»; список — плитка «Ждут моего голоса»,
  колонка «Крайний срок» с чипом просрочки, статус-чипы, «Голоса N из M», № → `К-2026-000138`;
  прогресс — стек 4 сегментов (`rgb(255,123,123)` контраст 2.3:1 → палитра);
  диалоги — Escape/подложка, защита несохранённого ввода, подтверждение называет действие
  и запись, деструктив красный; empty states вместо 150px пустоты, «Протокол» — не тупик.
  Проверено в браузере: `scripts/inspect/commission-tobe-check.mjs` — **38 assert'ов, 0 ошибок консоли**.
- 2026-07-09 — **Вкладка «Документы» — статусы комплектации + ярусы кнопок** (`loan-application.html`,
  `renderDocs`/`renderDocSection`/`docRow`). Спек: `docs/superpowers/specs/2026-07-09-loan-app-docs-completeness-design.md`.
  (1) **ЖЦ 7→6**: «На проверке» больше не хранимый статус, а **вид** — `uploaded`-док при заявке
  в комиссии показывается бейджем «На проверке» (`_dispSt`/`_atCommission`); `docUpload` пишет
  `uploaded`. (2) **Именованный чип комплектации** (`_complStatus`/`_complChip`, CSS `.compl.cs-*`)
  рядом с `N/M` на секции, залоговом предмете и пакете: Не начато / В работе / Готов к отправке /
  На проверке / Укомплектовано / Есть замечания / Заблокировано. ✓ в счётчике теперь = «Укомплектовано»
  (все приняты), не просто собрано. (3) **Кнопки**: ярус A — +Скачать (`docDownload`), +Вернуть для
  waived (`docUnwaive`); ярус B — панель пакета для спеца·draft: Отправить (гейт+тултип), Запросить
  недостающие (N, `pkgRequestMissing`), Скачать пакет, Опись; ярус C — видимость по роли/режиму без
  изменений. Гейт `sendReady` — логика прежняя. Проверено в браузере (`scripts/inspect/docs-completeness-render.mjs`):
  спец·draft·view/edit + комиссия·review, JS-ошибок нет, чипы во всех состояниях.
- 2026-07-09 — **Залоговые документы — сворачиваемые под-панели по предметам** (`loan-application.html`,
  секция «Залоговые» вкладки «Документы»): при многих предметах плоский список тяжело
  просматривать → каждый предмет теперь **сворачиваемая под-панель** (`doc-item`) со счётчиком
  готовности `N/M` + статус-чипом (готов ✓ / блок / в работе) в шапке. По умолчанию раскрыт
  только **первый** предмет, остальные свёрнуты (как секции-аккордеоны); клик перекрывает
  (`_docCollOpen`). Скан готовности — без раскрытия. Проверено в браузере на 5 предметах
  (первый открыт, остальные закрыты + ручной toggle). Выбран **вариант A** (аккордеон по предметам) — матрица/по-типу отпали:
  комплект доков зависит от вида залога (Недвижимость 4 / ТС 3 / Оборудование 2), колонки нестабильны.
- 2026-07-09 — **Сверка задач заявок (P3-R*) с эталонным мокапом** (`mockups/loan-application/loan-application.html`):
  все 31 задачу фазы 3 проверил против мокапа — функции по цитируемым якорям на месте,
  найдено 3 расхождения, все закрыты. **Гигиена (P3-R31):** удалена мёртвая статик-разметка
  `#tab-0` (~140 строк, фантомные поля «Дата подтверждения кредитных/залоговых документов»)
  + мёртвая JS `renderTab0` — таб теперь пустой контейнер, наполняется `renderGeneral`.
  **P3-R28:** кнопка формы создания `Далее`→**«Сохранить»** (одноэкранная форма, не мастер).
  **P3-R27:** снята устаревшая пометка «мокап — отдельной задачей» (номера `З-2026-000NNN`
  уже во всех демо-заявках; открыто только на серверный sequence). `TODO.md` синхронизирован.
- 2026-07-09 — **Мокап «Заявка» → документы (to-be)** (`mockups/loan-application/loan-application.html`):
  залоговые документы переведены на модель **по предметам залога** (стабильный `id`
  у каждого предмета, ключ состояния `<id предмета>::<docId>`; комплект — из справочника
  **вида** предмета). Полный ЖЦ документа (required→uploaded→review→accepted/rejected/
  waived/expired), роли (спец собирает / комиссия принимает / просмотр — всем), гейт
  «Отправить в комиссию» = обязательный комплект **+** коэффициент покрытия. Секция
  **«Залоговые»** вкладки **«Документы»** рендерит доки подгруппами по каждому предмету
  (вид · оценочная стоимость в подзаголовке) и входит в общую полосу готовности; вкладка
  **«Залог»** — регистр предметов (CRUD) + KPI (Предметов / Покрытие / Документы N/M) +
  сводный гейт со ссылкой на «Документы». `_docRerender` синхронно обновляет обе вкладки.
  Закрывает находку **P3-18**, отвечает **P2-R20 / P2-R21 / P3-R20**. Коммиты `a26307e`
  (влитие вкладки «Документы» to-be) → `0897ef6` (перенос залоговых доков в «Документы»).
- 2026-07-07 — **Мокап «Комиссии по заявкам»** (`mockups/loan-application-commission/commission.html`) —
  повторная сверка с live (`scripts/inspect/commission-sverka*.mjs`, скриншоты
  `.auth/commission-*-live.png`). Список приведён к текущему стенду: 2 строки
  (139 «Одобрено» + 138 «На рассмотрении»), плитка «Одобрено» 0→1, пейджер «2 строки».
  Добавлены 2 новых экрана + диалоги: **Просмотр** теперь data-driven по выбранной
  строке (138/139, разные Итоговое решение/История); **Создать·Изменить** открывает
  экран-редактор `/new`·`/{id}` (шапка + Состав комиссии с «Проголосовать» + Документы
  + Комментарии членов комиссии + Финальное решение с Одобрить/Отклонить/Запросить
  доп. информацию + Прогресс голосования + OK/Отмена). Кнопки голосования вызывают
  живые диалоги: «Ваш отзыв» (Решение/Риск/Комментарий/Файл) и «Подтверждение — Вы
  уверены? Да/Отмена». Решённую 139 «Изменить» не даёт (кнопка disabled, live-правило).
  Сверка mockup↔live `scripts/inspect/commission-mockshot.mjs`.
- 2026-07-06 — **Мокап «Кредиты»** (`mockups/loan-credit/loan-credit.html`) — точная
  копия раздела: список `/loansCredit` (11 колонок, 14 строк, «14 строк»-пейджер,
  Обновить/шестерёнка/Добавить условие поиска, Изменить/Просмотр) + деталь
  «Редактирование кредита» `/loan-credits/{id}` со всеми 11 вкладками (Общая информация,
  Оформление, Заемщик, Условия кредита с под-вкладками, График погашений, Транши,
  Резерв, Платеж, Код оплаты, Детальный расчет, Залог). Данные записи 18 («Номер рег. 56»,
  ОсОО «Бек Кабель»). Переключение вкладок, выбор строки, OK/Отмена работают как в
  оригинале. Инспекция `scripts/inspect/loan-credit-clone.mjs`, сверка скриншотами
  (`loan-credit-mockshot.mjs`) — ветка `mockup/loan-credit-page`.
  **Клики воспроизводят живые диалоги** (`loan-credit-clicks*.mjs`): каждый ••• открывает
  экран-браузер сущности (Фильтр/Обновить/Создать·Изменить·Удалить/«N строк»/грид/Выбрать)
  с реальными данными справочника (Вид кредита, Решение правительства, Кредитная линия,
  Счёт/Вид счёта погашения, Куратор→Субъекты); «Добавить» на Транши→«Создание транша»,
  на Платеж→«Платеж» (с живыми дефектами P7-02 Payment uuid/version + eager-required red);
  поля даты → календарь-попап. Сверка `loan-credit-mockshot2.mjs`.
- 2026-07-04 — **Мокап «Заявка»** — повторная сверка формы «Новая заявка» с живой
  системой (`scripts/inspect/app-create-fields.mjs`, `app-detail-tabs.mjs`). Список,
  9 вкладок детали, наборы документов и диалоги «Выберите комиссию» совпадают.
  Исправлены расхождения формы создания: (1) добавлен отсутствовавший селект «Метод
  расчета дней» (по умолч. «календарный 365»); (2) блок «Ручные параметры льготного
  периода» приведён к живому виду — 3 жирных подзаголовка + чекбоксы, чекбокс №1
  переименован «…по основному долгу» (было «…по основной сумме»), блок раскрыт по
  умолчанию.
- 2026-06-29 — **Мокап «Заявка»** (`mockups/loan-application/loan-application.html`) завершён:
  as-is клон полного потока — список заявок (грид, 8 колонок, 5 плиток-статистики) +
  мастер «Новая заявка» (все поля step 1) + страница детали (все 9 вкладок: Общая
  информация, График, 3 набора документов, Кредитная/Залоговая комиссия, История) +
  диалоги «Выберите комиссию» (кредитная/залоговая) со статус-тостом. Переиспользует
  дизайн-систему loan-program. Воспроизводит живые дефекты (серые поля P3-01, eager
  валидация P3-06, нет «Просмотр» P3-02), без улучшений. Живые находки 2026-06-29
  (по `scripts/inspect/app-capture.mjs`): добавлены 5 цветовых stat-cards (Всего/На
  рассмотрении/Одобрено/Отклонено/Требуется доп. информация); новый статус «Новый»;
  степпер детали с зелёной галочкой + датой под завершёнными шагами; поле диапазона
  «Допустимо: от … до …» в блоке одобренных условий; поле «Кредитная программа» во
  второй колонке Общей информации.
- 2026-06-29 — Loan-program creation form: completed a component-by-component owner
  review of all 8 input tabs (2 Сумма/срок · 3 Ставки · 4 Штрафы · 5 Льготный
  период · 6 Платежи/расчёты · 7 Документы · 8 Залог). Logged defects **P2-12…P2-25**
  in `qa-findings.md` and recommendations **P2-R15…P2-R21** in `TODO.md` (commit
  `e382424`). Cross-cutting rules fixed: admin-curated reference-books + junk-checks
  (feed future reports/analytics), range validation `0<мин≤макс` + soft equal-warning,
  «Маржа = read-only mirror of base rate» (dev duplication defect), textarea 1000+counter,
  explicit units (мес./%/% годовых), cross-grid dedupe, collateral gating on the checkbox. (`mockups/dictionaries/dictionaries.html`) complete: 50 sections, toolbar/grid/modal/picker-demo (P2-R9 select-only dialog, no CRUD). Docs map updated in README.
- 2026-06-23 — Написано ТЗ as-is подсистемы кредитования (`requirements/tz/`): головной index + 7 разделов по этапам жизненного цикла, полная live-ре-инспекция.
- 2026-06-23 — Loan-program mockup (`mockups/loan-program/loan-program.html`): **applied P2-R1
  field visual semantics** — flipped every editable control (input/select/textarea/
  lookup/mscombo) from the live grey "filled" look (defect P2-01) to **white bg +
  full border**; read-only/computed fields (Предпросмотр tab, «Выбранные шаблоны»,
  «Валюта» …) to **grey, no border**; required fields keep the red `*` shown
  immediately on open. Verified across tabs 1/7/9 (`.auth/p2r1-tab{1,7,9}.png`).
- 2026-06-23 — Loan-program mockup (`mockups/loan-program/loan-program.html`): finished the
  add-form wizard clone — tabs 6 «Платежи и расчеты», 7 «Документы», 8
  «Залоговое обеспечение», 9 «Предпросмотр» now match live Jmix 1:1 (inspect
  scripts `scripts/inspect/tab6-9*.mjs`, screenshots in `.auth/`). All 9 tabs
  cloned. Fixes vs prior placeholders: tab 8 fields are NOT conditional on the
  checkbox (live always shows them); tab 7 gained the missing залоговые
  doc-grids; dropped wrong `req:true` flags on tabs 6/8; wired REFBOOKS for the
  `dayCount/queue/interestCalc/checkLevel/templates` ••• pickers (latent break);
  removed orphaned `miniGrid()`.
- 2026-06-20 — Security/access config: inspected Безопасность → Ресурсные роли (`/sec/resourcerolemodels`, 15 roles) + Роли уровня строк (`/sec/rowlevelrolemodels`, 1 role) + Users via Playwright (MCP). Wrote how-to [guides/access-control.md](guides/access-control.md) (control sidebar menu/pages/buttons/fields/rows). Observations to log: `банки` role = complete CRUD template; `СУГС` & `Специалист кредитного отдела` roles have **no policies** (grant nothing); junk roles `123` (resource) + `test` row-level (empty Where) = test pollution; Users list ships a saved filter `test` hiding all rows.
- 2026-06-20 — Phase 7 (Servicing): documented `/payments` (Платеж), `/loan-reserves` (Резерв), `/loan-ledgers` (LoanLedger) via Playwright (`scripts/inspect/p7.mjs`). Logged 5 findings (P7-01 🟠 i18n leak across all 3 grids incl. `EventType.*` enum; P7-02 🟠 `Payment uuid`/`version` columns exposed; P7-03 🟡 duplicate raw «Статус»; P7-04 🟡 reserves have no detail view; P7-05 🟡 ledger «Удалить» on append-only log) and 5 proposals (P7-R1…R5). Cross-check: payment split (ОД+проценты) and reserve math (unused×rate) correct.
- 2026-06-20 — Phase 6 (Disbursement & tranches): documented `/sub-loans` (tranches, 4-tab detail) + `/disbursements` (Освоение) via Playwright (`scripts/inspect/p6-probe.mjs`, `p6.mjs`); model Кредит→Транш→Освоение. Logged 3 findings (P6-01 🟠 raw entity labels `SubLoan.amount`/`Disbursement.subLoan`; P6-02 🟡 empty tranche schedule; P6-03 🔵 «шрафы» typo) and 3 proposals (P6-R1…R3). Cross-check: disbursement-level annuity schedule computes interest correctly (50 000 @10%/24mo → 2 307,25).
- 2026-06-20 — Phase 5 (Loan issuance): documented `/loansCredit` + 11-tab loan detail (`/loan-credits/{id}`) via Playwright (`scripts/inspect/loans*.mjs`); logged 8 findings (P5-01…08, incl. **🔴 P5-01** data-dependent detail crash on rec 20/22) and 5 proposals (P5-R1…R5). Cross-check: loan-level schedule computes interest correctly (contra app-level P3-R11).
- 2026-06-17 — Phase 2 (Loan program): documented `/loan-programs` + 9-tab create wizard via Playwright; logged 6 findings (P2-01…06) and 6 proposals (R8–R13). QA pass (required-field validation). Stack refined to **Jmix on Vaadin**. New Sheet tab «Кред. программы».
- 2026-06-17 — Phase 1 (Gov decision): verified R2/R4/R6 in-app; all 6 proposals confirmed (see findings P1-03/04/05). Stack = Vaadin.
- 2026-06-17 — Phase 1 (Gov decision): documented + logged 6 improvement proposals (R1–R6).
- 2026-06-17 — Logged into test env; reconstructed and documented the full module map.
- 2026-06-17 — Workspace set up.
