# АСУБК · Заёмщик — статус разработки макета

> Дом-статус макета `mockups/borrower/borrower.html`. Смоук —
> `scripts/inspect/borrower-check.mjs`. Язык предметной области — `CONTEXT.md`,
> правило значений — `docs/adr/0001-proizvodnye-ne-hranyatsya.md`.
> Инварианты И-1…И-9 — в шапке HTML.
>
> **Этот файл — задачи по макету, не бэклог разработки.** Задачи для команды
> разработки новой системы живут в `TODO.md` (`P4-R*`) и туда не дублируются.

## Передача в разработку (27.07.2026)

Все решения этого файла — `СП-1…СП-24`, `КЗ-1…КЗ-41`, дефекты макета `Б-1…Б-15` и
открытые вопросы `ОВ-13…ОВ-18` — разнесены по **20 задачам разработки `P4-R6…P4-R25`**
(порядок сборки строгий: сквозное → экраны → одна вкладка = одна задача → страница
субъекта → миграция). Ничего не потеряно; отдельными задачами `Б-*` и `ОВ-*` не выделяются.

| Что | Где |
|---|---|
| Бэклог (источник истины) | `TODO.md` → «### Заёмщик (целевая модель)» |
| Карта задач для разработчиков | `docs/tasks/p4-zaemshchik-tasks.html` |
| ТЗ целевой модели | `requirements/tz/04-zaemshchik.html` |
| Архив as-is (обследование июня 2026) | `requirements/tz/04-zaemshchik.md` |

Старые задачи фазы 4 (`P4-R1…P4-R5`, живой стенд) поглощены: R1 → P4-R11, R3 → P4-R8+P4-R13,
R4 → P4-R25, R5 → P4-R20 (вопрос заказчику сузился до «Лимита задолженности»), R2 отпадает
вместе с формой. Счётчик `P4-R*` **общий** — целевые задачи продолжают ту же нумерацию.

**Границы разнесения.** `P4-R6/R7/R11/R12` помечены «есть эталон в макете» (код и смоук есть —
переносить); `P4-R8` — эталон только у механизма среза, денежная часть постановка; остальные —
«только постановка». `P4-R24` (страница субъекта) — единственная задача **без разобранного
эталона**: `ОВ-16` остаётся открытым намеренно, разбор идёт после решений о переносе, а не до.

## Нумерация

`СП-*` — решения по **экрану списка заёмщиков** (сессия-гриллинг 26.07.2026).
Отдельный префикс взят потому, что `Р-1…Р-10` в этом модуле заняты сессией
23.07.2026 (`docs/superpowers/specs/2026-07-23-borrower-rework-design.md`), а
`Р-11 / Р-20 / Р-22` внутри `borrower.html` — ссылки на решения **залогового**
модуля в зеркальных комментариях. `ОВ-*` продолжает сквозную нумерацию открытых
вопросов модуля (заняты ОВ-2, ОВ-4, ОВ-6).

Сессия шла в два прохода: СП-1…СП-14 — экран списка покомпонентно, СП-15…СП-21 —
открытые вопросы ОВ-7…ОВ-12, оставшиеся после первого прохода. СП-22…СП-25 — правка
списка 27.07.2026; СП-22 и СП-23 отменяют СП-20 и СП-14.

`КЗ-*` — решения по **карточке заёмщика** (сессия-гриллинг 26.07.2026, второй заход:
шапка и одиннадцать вкладок покомпонентно). Снова новый префикс: `СП-*` закреплён за
списком, и смешивать два экрана в одной последовательности значило бы потерять
адресацию. `Б-*` — дефекты **макета** (ошибки кода, а не решения дизайна); в
`notes/qa-findings.md` они не идут — там `Pn-NN` живого стенда.

---

## Готово — экран списка

| # | Решение | Статус |
|---|---------|--------|
| СП-1 | Список = рабочая очередь + полноценный поиск (назначение экрана) | ✅ |
| СП-2 | Один инпут поиска в тулбаре, ИЛИ по наименованию и ИНН | ✅ |
| СП-3 | Группа+подгруппа — один двухуровневый селект | ✅ |
| СП-4 | +Куратор · +Дни просрочки от—до · +Дефекты · +«не применяется» · бейдж числа условий | ✅ |
| СП-5 | Каскад Область → Район из данных; метки без Jmix-синтаксиса | ✅ |
| СП-6 | «+ Добавить заёмщика» остаётся (предрегистрация) | ✅ форма — СП-17/18 |
| СП-7 | Подгруппа: код · «Кредитов не было» · «Нет действующих» | ✅ |
| СП-8 | «Открыть» убрана, чекбоксов нет, строка открывается кликом и Enter | ✅ |
| СП-9 | Экспорт: видимое + шапка-оттиск (условия, дата среза, дата снимка денег) | ✅ |
| СП-10 | Карточный вид и переключатель удалены | ✅ |
| СП-11 | Настоящая пагинация, размер страницы, «1–20 из 47», дребезг поиска | ✅ |
| СП-12 | ИНН в подписи · куратор разведён с дырой · +«Задолженность» · подгруппа короче | ✅ |
| СП-13 | Дефекты: модальность отдельно от степени, сортировка составным ключом | ✅ |
| СП-14 | Полоса над таблицей с обеими датами | ❌ отменено СП-23 |
| СП-15 | Конструктор условий Jmix убран («+ Условие поиска», шестерёнка) | ✅ |
| СП-16 | Пустое состояние называет условия; сброс чистит всё, включая поиск | ✅ |
| СП-17 | Создание входит через ИНН: лицо найдено → карточка, нет → создание субъекта | ✅ |
| СП-18 | Поля в двух уровнях; орг-форма только у юр. лица; ИНН — 14 цифр | ✅ |
| СП-19 | Удаление в карточке, только субъекта, при нуле ссылок, с подтверждением | ✅ |
| СП-20 | Три плашки рабочих списков без сохранения; «мои» не делаем | ❌ отменено СП-22 |
| СП-21 | Тонкая спека модуля — только невосстановимое знание | ✅ |
| СП-22 | Плашки рабочих списков сняты: разрезы делает панель фильтра (отменяет СП-20) | ✅ |
| СП-23 | Полоса двух дат над таблицей снята: реестр всегда «сегодня», даты остались в оттиске выгрузки СП-9 (отменяет СП-14) | ✅ |
| СП-24 | Ширины колонок реестра подобраны замером содержимого; заголовок переносится, данные — нет; ФИО кураторов приведены к «Фамилия И. О.» | ✅ |
| СП-25 | Размеры страницы — один набор на модуль: 5 / 20 / 50, по умолчанию 20 (реестр и пейджер кредитов в карточке) | ✅ |

### Что именно изменилось в коде

- **Панель фильтра.** Вписанных значений в разметке больше нет — все справочники
  строит `fillFilterDicts()` из `SUBJECTS` и `REGION_OF`. Полей стало 9 (было 7),
  из них `f-name` / `f-inn` / `f-group` / `f-sub` удалены, добавлены
  `f-q` (в тулбар), `f-region`, `f-grp`, `f-curator`, `f-ovFrom`, `f-ovTo`, `f-def`.
- **`REGION_OF`** поднята из локальной области сид-функции на верхний уровень —
  один источник и для сида, и для каскада фильтра.
- **`subgroupOf()`** получила ступень «нет долга — нет класса»: `'none'` решается
  до лестницы, `'closed'` — после неё (иначе терминальные 3.2 и 5, где кредиты тоже
  закрыты, потеряли бы свой класс). `groupOf()` вне лестницы отдаёт `null`,
  `solvencyOfGroup()` не принимает такие коды за «Неплатёжеспособные».
- **`listRow()`** отдаёт `debt`, `mid`, `curatorNames`, `gaps` — фамилия куратора
  больше не подавляется дырой, а сортировка по колонке идёт только по фамилиям.
- **`sortVal('def')`** возвращает составной ключ `[стоп, высокие, всего]` вместо
  `(стоп ? 1000 : 0) + всего`; сравнение — новая `cmpVal()`.
- **Удалено:** `renderCards`, `listView`, `LIST_VIEW_KEY`, `setListView`, `#segTable`,
  `#segCards`, `#cardsWrap`, `#tbOpen`, `gTop`, CSS `.bcard-*` / `.cards-*`.
  CSS `.viewseg` **сохранён** — им пользуются переключатели внутри карточки
  («Взыскание»: по процессам / по кредитам, «Обеспечение»: по кредитам / по предметам).
- **Сид:** добавлен ОсОО «Нарын Агро Плюс» (ИНН 11101199900111) — предрегистрация
  без единого кредита. Без такой записи ветка СП-7 «Кредитов не было» в макете
  недостижима, а прежняя база выдавала ровно ей «1.1 Производят погашение по графику».

### Что изменилось по СП-15…СП-21

- **Панель фильтра.** `#fAddCond` и кнопка-шестерёнка удалены. Это был типовой
  конструктор условий Jmix — он стоит на каждом списочном экране целевого приложения
  (`requirements/tz/04-zaemshchik.md` §4.1 и то же в разделах 05/06/07), но обработчиков
  в макете не имел. Произвольная пара условий возвращает ровно тот дефект, который СП-3
  погасил структурно. Механику отдаём платформе, знание осталось в СП-4/СП-5.
- **Пустое состояние** строит `renderEmpty()`: метки из `FIELD_LABEL`, значения через
  `condValLabel()` — коды (`na`, `none`, `stop`, «2» как вся группа) наружу не выходят.
  Кнопка вызывает `resetFilter()`, который чистит все `FIELDS`, включая `f-q`: поиск лежит
  в тулбаре и в счёт активных условий не идёт, но «Сбросить» должно давать чистый реестр.
- **Плашки-пресеты сняты (СП-22, 27.07.2026).** `PRESETS`, `presetOn()`, `renderChips()`,
  `togglePreset()` и блок `#qchips` удалены целиком. Пресет был набором значений тех же
  полей фильтра, поэтому ничего, кроме одного нажатия, он не давал: те же три разреза
  («есть дефекты», «без куратора», «просрочка от 90») набираются панелью. Сохраняемых
  списков не было и не появилось — вопрос остаётся за ОВ-13.
- **Полоса двух дат снята (СП-23, 27.07.2026).** `#listAsOf` и `renderListAsOf()` удалены.
  Реестр всегда «сегодня» (`VIEW_DATE = TODAY` при входе в список), своей даты среза у него
  нет, а расхождение денег со срезом (И-6) подписано там, где оно воспроизводится, — в
  оттиске выгрузки (СП-9) и в карточке. `DEBT_ASOF` продолжает жить в обоих.
- **Ширины колонок (СП-24).** Подобраны не на глаз, а замером: `.auth/measure.mjs` меряет
  содержимое каждой ячейки span'ом со шрифтом самой ячейки и берёт максимум по колонке
  (мерить ширину `td` бесполезно — в таблице все ячейки колонки равны по определению).
  Итог: 18 / 15,5 / 9,5 / 9 / 11 / 11,5 / 10,5 / 15 %. Единственная колонка, которой
  сознательно не хватает, — «Подгруппа»: полностью её просит только «3.2 Банкротство
  завершено» (3 записи из 47), а код и полная подпись в `title` остаются.
- **Заголовок переносится, данные — нет (СП-24).** Раньше `nowrap` стоял и на шапке:
  «Категория рис…», «Просроч…», «Просроченная ча…» — подписи не читались, а место всё
  равно занимали. Теперь шапка двухстрочная (платим один раз высотой), а стрелка
  сортировки вынута из потока `position:absolute` — в потоке она считалась ещё одним
  словом подписи и уезжала на вторую строку одна.
- **`min-width: 1270px` у таблицы (СП-24).** Ниже этого предела проценты режут уже не хвост
  подписи, а ИНН, сумму и фамилию куратора. Узкий экран получает горизонтальный скролл
  `#gridWrap`, а не восемь колонок многоточий.
- **ФИО кураторов (СП-24).** Генератор сида клал в `EMP` полное «Фамилия Имя Отчество»,
  а вписанные руками записи — «Фамилия И. О.»: одна колонка печатала две формы одного поля
  и просила под это 294 px. Генератор приведён к формату справочника (`initials()`).

- **Размеры страницы (СП-25, 27.07.2026).** В модуле было две разные шкалы: реестр давал
  25/50/100 (по умолчанию 25), пейджер кредитов в карточке — 10/25/50/100 (по умолчанию 10).
  Один экран, два набора, и ни один не давал короткой страницы под разбор очереди по пять
  записей. Набор один на оба места: **5 / 20 / 50, по умолчанию 20**. Меняются `pgSize`
  (`borrower.html:3287`), разметка `#pgSize` (:786), фолбэк обработчика (:5692) и `crSize`
  с генератором опций пейджера карточки (:3706, :3771). Смена размера сбрасывает на первую
  страницу — иначе на pgPage=3 при 5 строках выборка уезжает в пустоту.

### Проверено

Смоук на момент закрытия списка: **147/147 PASS** (`node scripts/inspect/borrower-check.mjs`,
27.07.2026; после шапки карточки — 165/165, после СП-25 — **168/168**, см. ниже).
Прежние тесты V1–V3 (переключатель вида) удалены вместе с функциональностью;
S5, C3, C7, H5 переписаны; 24 теста с префиксом `СП-` за первую половину сессии,
ещё 23 — за СП-15…СП-20; из них 4 снято вместе с СП-14 и СП-20 (27.07.2026).

В браузере (26.07.2026) проверено вживую:

| Проверка | Результат |
|---|---|
| Каскад: область «Чуй» | 4 района, 12 строк, бейдж условий = 1 |
| Фильтр «вся группа 2» | 11 строк, счётчик «1–11 из 11» |
| Фильтр «Нет действующих» | 3 строки — Ак-Суу Майнинг, Глобал Сервис, Осмонов Данияр |
| Фильтр «Кредитов не было» | 1 строка — Нарын Агро Плюс (предрегистрация) |
| Стоп-дефекты + просрочка 90+ | 16 строк, активных условий 2 |
| Сортировка по дефектам ↓ | первая строка «стоп 6в 5с» — модальность впереди степени |
| Карточка предрегистрации | открывается, плитка «Кредитов не было», группа «не применяется», кода `none` в тексте нет |
| Мёртвые контролы (СП-15) | `#fAddCond` отсутствует, `.btn-icon` в панели — 0 |
| Плашка «Просрочка 90+» | 16 строк, `f-ovFrom=90`, плашка подсвечена; повторное нажатие → 47 строк |
| Плашка от ручного ввода | `f-def=any` руками → подсвечена «С дефектами» |
| Пустое состояние | «Область: Нарын · Классификатор: Кредитов не было · Дефекты: есть стоп» + «Сбросить все условия»; кодов наружу нет |
| Сброс из пустого состояния | 1–25 из 47, бейдж скрыт, поиск очищен |
| Форма: существующий ИНН | реквизиты ОАО «АгроТехСервис», роли «Заёмщик · Залогодатель», полей создания нет, кнопка «Открыть карточку» |
| Форма: новый ИНН | 9 обязательных пометок, «Создать» заперта, «Остальные реквизиты» свёрнуты, орг-форма скрыта |
| Орг-форма | видна при «Юр. лицо», скрыта и очищена при «ИП» |
| Создание записи | 47 → 48; `subgroupOf`=`none`, `groupOf`=`null`, категория `null`; карточка печатает «Кредитов не было» без кода; находится и поиском, и фильтром района |
| Удаление с кредитами | кнопка заперта, `title` = «заёмщик (3 действующих из 3); залогодатель (4 предмета в залоге)» |
| Удаление предрегистрации | кнопка активна, подтверждение называет ИНН и наименование |
| Консоль | ошибок нет |

> Число записей в сиде — **47**. В прежней редакции этого файла стояло «46»; проверено
> заново в jsdom и в браузере.

---

## Готово — шапка карточки (КЗ-1…КЗ-7)

Решения приняты по одному, покомпонентно: шапка, затем одиннадцать вкладок по порядку.
Разбор шёл по `borrower.html` в редакции 26.07.2026 (после СП-1…СП-21); номера строк
в таблицах ниже — из неё. **Шапка реализована 27.07.2026**; сквозные приёмы и вкладки
2–11 остаются к работе (см. следующий раздел).

### Шапка — все семь решений в коде ✅

| # | Решение | Почему |
|---|---------|--------|
| КЗ-1 | `id-bar` в теле карточки: наименование · ИНН · тип лица · отрасль · ссылка на субъекта | Топбар — шасси приложения, а не паспорт записи. Сейчас `showDetail` (5382) кладёт туда одно наименование, и **ИНН на карточке не показан нигде** — при том что ИНН первичный ключ всех выводимых значений (см. Б-12) |
| КЗ-2 | Дата среза: в норме — тихая строка; в режиме среза — полоса во всю ширину. Оговорка про деньги стоит **у денег**, не у органа управления | Полоса на каждом открытии обесценивается; в режиме среза она обязана быть заметна, потому что весь экран показывает не сегодня. Оговорка И-6 у переключателя даты объясняет то, чего в этот момент на экране не видно |
| КЗ-3 | Срез уходит в маршрут: `#/b/<ИНН>/<таб>?on=<дата>`; нет параметра → сегодня | Сейчас срез живёт только в памяти вкладки: ссылку на «состояние на 01.03» переслать нечем, а И-9 («одна дата на карточку») этому не мешает — одна она и остаётся |
| КЗ-4 | Плитки шапки — две: **Подгруппа** и **Категория риска**. `flagsRow` поднимается из вкладки 1 в шапку. Состояние субъекта показывается только баннером | Третья плитка сейчас дублирует баннер `ro`. А стоп-факторы (`flagsRow`, 3182 в прежней редакции) отрисованы **внутри вкладки 1** — их не видно с десяти вкладок из одиннадцати, хотя они запрещают работу со всей карточкой |
| КЗ-5 | Плитка подгруппы двухуровневая: группа сверху, `2.3 · работа по исполнительным листам` снизу. Завести `GROUP_LABEL` на пять кодов; `solvencyOfGroup` переименовать в `surveyClassOf` | Группа выводится из подгруппы — показывать её отдельной строкой значит показывать одно значение дважды. Имя `solvencyOfGroup` описывает не то, что функция делает: она отдаёт класс периодичности обследования залога, а не платёжеспособность |
| КЗ-6 | Значок 🔒 read-only переносится внутрь: «как выведено» — цепочка вывода, а не замок | Замок отвечает на вопрос «почему нельзя править», а спрашивают «откуда это число». Ответ на второй вопрос содержит ответ на первый |
| КЗ-7 | Таббар в две осмысленные строки, со счётчиками у вкладок, где счёт есть | Одиннадцать вкладок в одну строку не читаются. Перенос по смыслу (состояние · обеспечение и обязательства · работа · досье) держит `TAB_DEFS` (3345) как есть — меняется только подача |

### Что изменилось в коде по КЗ-1…КЗ-7

- **`renderCard()` переписан.** Порядок шапки: `id-bar` → строка/полоса даты среза →
  две плитки → баннер состояния → лента стоп-факторов → таббар в две строки.
- **`id-bar` в теле карточки (КЗ-1).** Наименование · ИНН · тип лица · отрасль · ссылка
  на субъекта. `showDetail()` больше не кладёт наименование в топбар — там теперь
  «Карточка заёмщика», имя экрана. Этим закрыт **Б-12**: ИНН на карточке не показывался нигде.
- **Дата среза в двух подачах (КЗ-2).** Класс `.asof-bar.past` — полоса во всю ширину
  с кнопкой «Вернуться к сегодня»; в норме та же строка тихая. Оговорка «деньги — снимок
  на `DEBT_ASOF`» из неё убрана: она уже стоит у денег (подпись «Сводки по кредитам»
  и тулбары вкладок «Кредиты» / «Залог» / «Взыскание»).
- **Срез в маршруте (КЗ-3).** `#/b/<ИНН>/<таб>?on=ГГГГ-ММ-ДД`; `writeHash()` дописывает
  параметр только при `VIEW_DATE ≠ TODAY`, `route()` его разбирает, `showDetail(inn, tab, onDate)`
  принимает третьим аргументом. `setAsOf()` рисует сразу и пишет адрес следом — ждать
  `hashchange` нельзя, он приходит отдельной задачей. Этим закрыт **Б-2**: `showDetail`
  теперь всегда задаёт `VIEW_DATE`, и переход карточка → карточка не уносит чужой срез.
- **Плиток две (КЗ-4).** `tile('Состояние субъекта', …)` удалена, сетка `.phead-dims` —
  две колонки. Состояние показывает баннер: `.ro-banner` (красный) при запрете работы,
  `.state-line` (тихая серая строка) при событии без запрета. Из `flagsRow()` убран бейдж
  «Только для чтения» — рядом с баннером он повторял то же слово третий раз.
- **`flagsRow(inn)` переехала** из `generalTab()` в `renderCard()`: стоп-факторы запрещают
  работу со всей карточкой, а были видны только с одной вкладки из одиннадцати.
- **Плитка подгруппы двухуровневая (КЗ-5).** Заведены `GROUP_LABEL` (пять кодов) и
  `groupOfSub()`; в `tile()` добавлен параметр `subHtml` (`.dim-sub`). У групп 4 и 5
  подгрупп нет — вторая ступень честно говорит «подгрупп у этой группы нет».
- **`solvencyOfGroup` → `surveyClassOf` (КЗ-5).** Функция отдаёт ключ `SURVEY_MATRIX` —
  класс периодичности обследования залога, а не платёжеспособность лица.
- **Замок внутри «как выведено» (КЗ-6).** Параметр `lockHtml` у `tile()` удалён; строка
  «🔒 значение выводится · ручной ввод невозможен» печатается последней внутри `<details>`.
- **Таббар в две строки со счётчиками (КЗ-7).** У `TAB_DEFS` появилось поле `row`
  (1 — состояние · обеспечение и обязательства · работа, 2 — досье), у вкладок —
  `.cnt` от новой `tabCount()`. Счётчиков пять: кредиты (действующих), залог (предметов),
  обязательства (открытых), взыскание (живых процессов), проверки (дефектов). Ноль не
  печатается; порядок вкладок и адресация ключом `tabIx()` не менялись.

### Проверено (шапка)

Смоук: **165/165 PASS** (`node scripts/inspect/borrower-check.mjs`, 27.07.2026) — 17 новых
тестов `КЗ-*`; H1, R1 переписаны под две плитки и новую подпись полосы среза.

В браузере (27.07.2026, `file://` + Playwright, 1440×1000):

| Проверка | Результат |
|---|---|
| ОАО «АгроТехСервис», норма | id-bar с ИНН и отраслью; тихая строка даты; плитки «Принудительное взыскание / 2.1» и «Высокий»; счётчики 3 · 4 · 4 · 2 · 12 |
| Он же, `?on=2026-01-01` | оранжевая полоса «Срез 01.01.2026», категория пересчитана в «Средний», счётчики 3 · 4 · 3 · 2 · 11 |
| ОсОО «Нарын Строй» (ликвидирован) | красный баннер, бейдж «Только для чтения» ушёл, стоп-фактор остался |
| АО «Ак-Ордо Строй» (реорганизация) | тихая строка «Состояние субъекта: реорганизация от 01.02.2026 · основание: Преобразование ОсОО → АО», красного баннера нет |

---

## К работе — карточка заёмщика

Сквозные приёмы и вкладки 2–11. **Код по ним ещё не менялся** — статус у всех «к работе».

### Сквозные приёмы

Приняты один раз и действуют на всех вкладках — иначе то же решение пришлось бы
принимать одиннадцать раз.

| # | Решение | Почему |
|---|---------|--------|
| КЗ-14 | Подписи блоков `sect()`: ссылка на пункт Порядка **остаётся**, внутренний номер решения (`Р-N`) уходит в спеку, жаргон переписывается | Пункт Порядка — основание, видимое пользователю. `Р-20` наружу не значит ничего и вдобавок принадлежит **залоговому** модулю (2906, 4151 — зеркальные комментарии) |
| КЗ-16 | 14 мёртвых ссылок помечаются «↗ внешний модуль» и по нажатию дают тост «маршрут не реализован» | Ссылка, которая молча ничего не делает, читается как сломанный экран. Перечень — в Б-9 |
| КЗ-22 | Дефекты в трёх подачах: в шапке — компактно и кликабельно, на своей вкладке — развёрнуто, зелёные полосы «всё в порядке» сворачиваются в тихую строку | Один движок (спека §7), но сейчас зелёная полоса весит столько же, сколько красная, и вытесняет содержание вкладки |
| КЗ-31 | Хронология на вкладке — последние 5 записей + ссылка в «Историю» с преднастроенным фильтром категории | Каждая вкладка ведёт свой обрывок ленты. Пусть ведёт одна, а вкладки в неё указывают |

### Вкладка 1 «Общая информация» → витрина

| # | Решение | Почему |
|---|---------|--------|
| КЗ-8 | Вкладка становится **витриной**: шесть сводок, каждая со ссылкой на свою вкладку; добавляются кураторство и обеспеченность | Сейчас это свалка реквизитов и сводок вперемешку. Витрина отвечает на «что с заёмщиком сейчас», реквизиты отвечают на другой вопрос |
| КЗ-9 | «Основные сведения» расформировываются: отрасль → `id-bar`, юр. и факт. адреса → **страница субъекта**, примечание → условная плашка, аудит создания/изменения → «История» | Ни одно из этих полей не работает на вопрос витрины. Адрес — паспортный атрибут субъекта (CONTEXT: «Субъект»), а деление субъекта на «Реквизиты» и «Основные сведения» ничем не обосновано — см. ОВ-17 |
| КЗ-10 | Контакты: вернуть адрес корреспонденции, завести признак «основной», показывать дату актуальности; пустой набор поднимает дефект `Д-КОНТ` | Заёмщик без единого действующего канала связи — это расхождение с Порядком, то есть дефект по определению (CONTEXT: «Дефект»), а не пустой блок |
| КЗ-11 | «Последние события»: недатированные факты не занимают слоты; лента — журнал и дате среза **не подчиняется** (то же для `categoryEvents`) | Пять слотов сейчас съедают записи без даты, и свежие события до экрана не доходят. Журнал — это набор фактов, а не состояние; И-9 говорит про состояния |
| КЗ-12 | Сводка по кредитам — **один режим дат**: всё на снимке `DEBT_ASOF`, с одной подписью. `totalDebt` / `overdueDebt` / `atRisk` / `coverageOf` принимают дату параметром | Сейчас в семи строках под одной подписью сходятся три режима дат — см. Б-3 |
| КЗ-13 | «Требует внимания»: сортировка по сроку, топ-3, ссылка ведёт в модуль-источник; очередь на комитет отсюда убирается; «просрочено» → «срок вышел» | Слово «просрочено» рядом с «просроченной частью» долга — два разных смысла в одном экране. Очередь на комитет — работа коллегиального органа, у неё свой адрес |
| КЗ-15 | Послабления с витрины убрать; отметка остаётся строкой основания в плитке категории | Послабление — не самостоятельный факт для витрины, а объяснение того, почему категория такая (CONTEXT: «Послабление»; спека §4, подавление 181) |
| КЗ-17 | Кураторство и обеспеченность становятся сводными карточками витрины; `breaches` из `coverageOf` наконец показывается | `coverageOf` (2532) считает `breaches` и `aggregate`, а наружу отдаётся только `worst` — «нарушений 2 из 5» информативнее одной цифры |

### Вкладки 2–11

| # | Вкладка | Решение |
|---|---------|---------|
| КЗ-18 | Кредиты | Тулбар: «+ Добавить кредит» ведёт маршрутом в модуль заявок, а не притворяется формой здесь; пункты меню переименованы по смыслу |
| КЗ-19 | Кредиты | Таблица — рабочий список: строка открывает кредит, пять статей долга раскрытием (а не в `title`, 3521), фильтр по состоянию, сортировка, **валюта из данных** |
| КЗ-20 | Кредиты | Итоги следуют фильтру; погашенные кредиты выносятся отдельной строкой, а не растворяются в сумме |
| КЗ-21 | Залог | Вкладка переименовывается в **«Обеспечение»**; внутри два раздела — залог и поручительство |
| КЗ-23 | Обеспечение | Сводка: worst-of с указанием кредита + «нарушений N из M»; `aggregate` удаляется как невыводимая наружу величина |
| КЗ-24 | Обеспечение | Обеспеченность показывается цепочкой вывода (залоговая ÷ остаток ÷ порог), а не голым индексом рядом с процентами: 1.00 и 120 % на одной строке читаются как разные шкалы, каковыми и являются |
| КЗ-25 | Обеспечение | Вид «по предметам»: поиск, сортировка, ИНН залогодателя вместо строки-имени (см. Б-14) |
| КЗ-26 | Обеспечение | **Поручительство — обеспечительный договор**, а не значение поля `role` в реестре лиц. Оформлено ADR-0005 |
| КЗ-27 | Обязательства | Подача по образцу «Актов сверок»: две секции, ссылка на пункт Порядка вместо внутреннего `О-N`, ссылка «где закрывается» в модуль-источник (И-7) |
| КЗ-28 | Взыскание | **Вкладка переписывается под ADR-0002**: строка списка = требование (процесс × кредит × обязанное лицо), не процесс; `p.pause` убирается |
| КЗ-29 | Взыскание | Один вид с группировкой по кредиту; сводная «стадия» заменяется распределением по фазам |
| КЗ-30 | Взыскание | Закрытые требования — свёрнутый архивный блок; исход берётся из закрытого словаря (CONTEXT: «Терминальный исход») |
| КЗ-32 | Кураторство | Матрица: давность дыры, дата приостановки, фильтр по кредитам |
| КЗ-33 | Кураторство | «Очередь передач» показывает только живые заявки, с давностью; зависшая заявка поднимает дефект |
| КЗ-34 | Кураторство | Колонка «Фаза» в конфликте интересов переименовывается в **«Ход рассмотрения»**; живое отделяется от архива |
| КЗ-35 | Акты сверок | Акт сверяет те же **пять** статей, что и долг (см. Б-8); «Штрафы» → «Пеня» по CONTEXT |
| КЗ-36 | Акты, Взыскание | Слово **«Охват»** возвращается к значению глоссария. В актах — «Состав акта», в архиве взыскания — «Кредиты» |
| КЗ-37 | Проверки | Кнопка «Проверить заново» убирается: производная считается при отображении (ADR-0001), пересчитывать нечего. Справочный подвал сжимается в одну строку — он дублирует плитки шапки |
| КЗ-38 | Документы | Перечень объектов досье строится **из объектов**, а не из уже приложенных документов (см. Б-7); заявки помечаются непроверяемыми |
| КЗ-39 | Реквизиты и связи | Связанные лица и ГЗПМ приводятся к правилам зеркала (И-5): один ярлык идентификатора, ГЗПМ показывается только группам. Сейчас `GROUP_MEMBERS` (1540) фильтруется одним ИНН без проверки вида лица, и блок выводится для ОАО — в коде это отмечено как демо-упрощение, но вида «группа» в модели субъекта нет вовсе |
| КЗ-40 | Реквизиты и связи | **Владение реквизитами — у субъекта.** Каналы связи и счета правятся на странице субъекта; вкладка 10 показывает их только для чтения с маршрутом «править у субъекта». Карточка заёмщика становится целиком выводимой |
| КЗ-41 | История | Достроить состав и навигацию: в ленту входят датированные факты `CHANNELS` / `BANK_REQ` / `RELATED`, добавляются фильтр по периоду, переход по годам и текстовый поиск; категория принимается из маршрута (КЗ-31) |

### Четыре решения, которые тянут за собой модель

**КЗ-26 · Поручительство.** Сейчас поручитель — строка в `RELATED` со значением
`role`. У такой записи нет ссылки на кредит, нет договора, нет объёма ответственности
и нет прекращения. Из этого следуют две потери: теряется легаси-находка, что залог и
поручительство в старой системе — один паттерн «обеспечительный договор»
(`requirements/legacy/20-e2e-cycle-plan.md`, §«ИТОГ ПРОГОНА», находка 3), и становится
непостроимым «требование = процесс × кредит × **обязанное лицо**» из CONTEXT — второй
координаты просто нет. Отсюда ADR-0005.

**КЗ-28 · Взыскание.** Вкладка отражает модель, которую **отменил принятый в тот же
день ADR-0002**: ключ — процесс, у процесса одна `phase`, один `contour` и вложенный
`credits[]`, обязанного лица нет, есть `p.pause` — который спека модуля-источника
отменяет правилом М-11. Сводная «стадия» по заёмщику — ровно тот worst-of, который
ADR-0002 называет причиной дефектов Д-1/Д-2. Модуль-владелец
(`mockups/collection/collection.html`) уже переписан: `{proc, credit, obligor, role}`,
`obligors[]`. Здесь зеркало отстало от владельца — это и есть «хрупкий шов» из спеки §6.

**КЗ-36 · «Охват».** В CONTEXT это **предмет требования** — что именно Холдинг требует
по кредиту (просроченная сумма · полный остаток · залог). В макете тем же словом названы
две таблицы-перечня: состав акта сверки и список кредитов в архиве взыскания. Настоящее
же значение лежит безымянным в `cover[].subject`. КЗ-28 требует это слово освободить:
охват — свойство требования, и на строке требования он должен называться собой.

**КЗ-40 · Карточка без ввода.** Расширяет И-5 с зеркал на **все** реквизиты: у карточки
заёмщика не остаётся ни одной точки ввода. Заёмщик — роль (спека §2), у роли своих
реквизитов нет; всё, что здесь показывалось редактируемым, принадлежит субъекту.
Обратная сторона: правка адреса требует перехода на страницу субъекта — на один шаг
больше. Принято сознательно, потому что альтернатива — два места правки одного факта,
расходящиеся молча.

---

## К исправлению — дефекты макета

Ошибки кода, не зависящие от решений выше: их надо чинить, даже если ни одно КЗ-решение
не будет реализовано. Строки — по редакции `borrower.html` на 26.07.2026.

**Закрыты 27.07.2026 шапкой:** Б-2 (сброс `VIEW_DATE` в `showDetail`) и Б-12 (ИНН
в `id-bar`) — оба попали в состав КЗ-3 и КЗ-1. Остальные тринадцать открыты.

| # | Дефект | Где | Степень |
|---|--------|-----|---------|
| Б-1 | Индекс вкладки в дефектах зашит числом мимо готового хелпера: `wrap('залог', 2, …)` и `wrap('взыскание', 3, …)`. Вкладка 3 — «Обязательства», вкладка «Взыскание» — четвёртая. Ссылка «перейти к дефекту» ведёт не туда. Рядом, в `borrowerChecks`, тот же код адресуется правильно — `tabIx('документы')` | 4887, 4888 (ср. 4775, 4783, 4863) | 🟠 |
| Б-2 ✅ | `showDetail` не сбрасывает `VIEW_DATE`. `showList` сбрасывает (5364), поэтому через реестр всё верно; но переход **карточка → карточка** по прямой ссылке (преемник после перевода долга — 5209, 5222; связанные лица со страницы субъекта — 5271, 5277, 5282) уносит срез предыдущего заёмщика на следующего без подписи | 5371 | 🟠 |
| Б-3 | `totalDebt`, `overdueDebt`, `atRisk`, `coverageOf` фильтруют кредиты по `TODAY`, тогда как сами деньги приходят снимком `DEBT_ASOF`. Множество кредитов и суммы по ним — на разные даты уже сегодня (`DEBT_ASOF` = 10.07.2026, `TODAY` = 26.07.2026). Дата среза в эти функции не передаётся вовсе | 2515, 2521, 2526–2527, 2533 | 🟠 |
| Б-4 | Через `totalDebt` дата `TODAY` протекает **в состояние**: существование обязательства О-2 («анализ фин.-хоз. состояния при долге > 50 млн») решается по сегодняшнему долгу, а срок того же обязательства — по дате среза. Одно обязательство на две даты | 2617 | 🟠 |
| Б-5 | Витрина считает по действующим кредитам, итог вкладки «Кредиты» — по всем (`credList` без фильтра). На демо-данных расходится наблюдаемо: ИНН `09901199990091` — «Сумма выдано 0 сом» на витрине против «Итого 7 000 000» на вкладке; `10001199900101` — 0 против 2 000 000 | 3636, 3678 | 🟠 |
| Б-6 | Два вида вкладки «Залог» считают итоги по разным правилам: «по кредитам» — по `creds`, «по предметам» — по `a` (действующим договорам). Переключение вида меняет итог | 4212, 4262 | 🟡 |
| Б-7 | `docRefs` строит перечень объектов досье **из уже приложенных документов** (`DOCS`), поэтому объект без единого документа в перечень не попадает и в знаменатель не входит. Залог с нулём документов даёт 100 %, а `Д-ДОСЬЕ` — «комплект полный». После КЗ-4 этот вывод попадает в шапку карточки | 4620 → 4863, 4866 | 🟠 |
| Б-8 | Акт сверки складывает три статьи (`cc.osn + cc.proc + cc.pen`), тогда как модель долга ведёт пять (`principal · interest · penalty · fees · costs`). Латентно: `fees` и `costs` во всех кредитах сида — нули. Первый же ненулевой сбор разведёт акт и карточку | 4359 | 🟠 |
| Б-9 | 14 ссылок `<a class="lnk">` без `href` и без `onclick` — нажатие не делает ничего | 3521, 3578, 3958, 4236, 4329, 4392, 4404, 4467, 4567, 4644, 4647 (две), 4648, 4940 | 🟡 |
| Б-10 | Валюта `KGS` вписана в разметку вкладок «Кредиты», «Обеспечение», «Взыскание», хотя у кредита есть поле `currency`. Правильный приём в этом же файле уже есть — `actMoney(n, cur)` во вкладке «Акты» с итогом по каждой валюте на своей строке (4370, 4410) | 3678 и рядом | 🟡 |
| Б-11 | Чекбоксы строк во вкладке «Кредиты» ни к чему не подключены, а выделение стирается пагинацией. На экране списка тот же контрол уже удалён решением СП-8 | 3521 и рядом | 🟡 |
| Б-12 ✅ | Комментарий утверждает: «наименование и ИНН — в топ-хедере (showDetail)». `showDetail` кладёт в заголовок только наименование — **ИНН на карточке не показан нигде** | 5227 vs 5382 | 🟠 |
| Б-13 | Пороги 60 к.д. (уклонение от акта) и 365 дн. (цвет давности) вписаны числами без ссылки на норму. Соседние пороги в файле сопровождены пунктом Порядка | 4329, 4388, 4737 | 🔵 |
| Б-14 | Залогодатель хранится строкой-именем (`pledger: s.name`), а роль субъекта выводится **сопоставлением строк**. Переименование лица рвёт связь молча | 2841 → `subjectRoles` | 🟠 |
| Б-15 | В спеке модуля (§6, шов «Реестр лиц») сказано, что `RELATED` **пуст**. Массив засеян — связанные лица в макете есть и отрисовываются | `ASUBK-zaemshik-logika.md` §6 | 🔵 |

---

## Закрытые вопросы

| # | Вопрос | Чем закрыт |
|---|--------|-----------|
| ОВ-7 | Сохранённые рабочие списки | СП-20 давал три фиксированные плашки — **отменён СП-22** (27.07.2026): плашки сняты, разрезы набираются панелью фильтра. Сохранение, имена и права по-прежнему **не делаем** — они требуют модели пользователя (ОВ-13) |
| ОВ-8 | Правило удаления записи | СП-19 — удаляется субъект, не роль; только при нуле ссылок; завершённый заёмщик закрывается событием |
| ОВ-9 | Пустое состояние | СП-16 — условия названы человеческими метками, сброс одним нажатием и целиком |
| ОВ-10 | Спека модуля | СП-21 — [ASUBK-zaemshik-logika.md](ASUBK-zaemshik-logika.md), тонкая: правила вывода, инварианты с причиной, швы |
| ОВ-11 | Форма создания заёмщика | СП-17/18 — вход через ИНН, ветка лицо/роль, два уровня полей |
| ОВ-12 | Мёртвые контролы панели фильтра | СП-15 — убраны оба |

## Открытые вопросы

- **ОВ-13 · Идентичность пользователя.** `[admin]` — литерал в шапке макета
  (`borrower.html`, топ-хедер) и в оттиске экспорта; среди кураторов сида
  (Сламкулов, Асанова, Токтосунов, Осмонова, Мамытова) его нет. Поэтому пресет
  «мои» в СП-20 не был сделан: вычислить его нечем, а выдуманное сопоставление
  «пользователь → куратор» соврало бы. Сам СП-20 снят СП-22, но вопрос остался:
  сюда же уходят имена и права сохранённых списков из ОВ-7 — им место рядом с моделью
  пользователя, а не на экране списка.
- **ОВ-14 · Контрольный разряд ИНН.** СП-18 проверяет только длину (14) и состав
  (цифры). Алгоритм контрольного разряда ИНН КР в репозитории нигде не описан —
  в макете не выдумывался.
- **ОВ-15 · Редактирование реквизитов субъекта.** Создание есть (СП-17/18),
  удаление есть (СП-19), правки — нет: в целевом приложении все поля карточки
  `readonly=true` (`requirements/tz/04-zaemshchik.md` §3.2–3.3), то есть механизм
  правки там тоже не наблюдается. Опечатка в наименовании сейчас исправляется
  только удалением и повторным созданием, и то лишь пока на лице нет связей.
  **КЗ-40 поднял цену вопроса:** если весь ввод переезжает на страницу субъекта,
  то отсутствующий там механизм правки становится единственным.
- **ОВ-16 · Страница субъекта не разобрана.** `renderSubject` (5240+) — «Реквизиты»,
  «Основные сведения», «Роли субъекта», «Организационная структура», «Жизненный цикл».
  Единственный экран модуля, не прошедший покомпонентный разбор, и при этом на него
  КЗ-9 и КЗ-40 переносят содержание. Разбирать его надо **после** этих двух решений,
  а не до.
- **ОВ-17 · Граница между «Реквизитами» и «Основными сведениями».** Тип лица и
  организационная форма лежат в одном блоке, отрасль и адрес — в другом, хотя это
  одинаково паспортные атрибуты субъекта. Два блока вместо одного, граница ничем не
  обоснована. Решается в ОВ-16.
- **ОВ-18 · Вида лица «группа» в модели нет.** `GROUP_MEMBERS` (1540) существует и
  отрисовывается, но `personKind` знает только `юр` / `физ`, а `legalForm` — обычные
  организационно-правовые формы. Отсюда КЗ-39: блок ГЗПМ показать «только группам»
  сейчас нечем — признака группы не существует. Либо заводить вид лица, либо
  выводить группу из наличия членов — но второе делает блок самоподтверждающимся.

---

## Не-цели (демо)

Экспорт формирует TSV с BOM под расширением `.xls` (Excel открывает как таблицу):
XLSX-сериализатор — задача реализации, состав колонок и оттиск от формата не зависят.

---

*Основание: сессия-гриллинг по экрану списка заёмщиков, 26.07.2026.
Решения СП-1…СП-14 приняты Нурсултаном по одному, с разбором каждого компонента.*

*Основание КЗ-1…КЗ-41 и Б-1…Б-15: сессия-гриллинг по карточке заёмщика, 26.07.2026 —
шапка и одиннадцать вкладок по одной, каждое решение принято Нурсултаном отдельно.
Все находки сверены с кодом `borrower.html` на 26.07.2026. Шапка (КЗ-1…КЗ-7) реализована
27.07.2026 и покрыта смоуком; по сквозным приёмам и вкладкам 2–11 кода и тестов пока нет.
Термины, заведённые этой сессией, — в
[CONTEXT.md](../../CONTEXT.md) («Обеспечение», «Поручительство»); модельное решение
КЗ-26 — в [ADR-0005](../../docs/adr/0005-poruchitelstvo-obespechitelnyy-dogovor.md).*
