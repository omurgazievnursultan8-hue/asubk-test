# АСУБК · Заёмщик — статус разработки макета

> Дом-статус макета `mockups/borrower/borrower.html`. Смоук —
> `scripts/inspect/borrower-check.mjs`. Язык предметной области — `CONTEXT.md`,
> правило значений — `docs/adr/0001-proizvodnye-ne-hranyatsya.md`.
> Инварианты И-1…И-9 — в шапке HTML.
>
> **Этот файл — задачи по макету, не бэклог разработки.** Задачи для команды
> разработки новой системы живут в `TODO.md` (`P4-R*`) и туда не дублируются.

## Нумерация

`СП-*` — решения по **экрану списка заёмщиков** (сессия-гриллинг 26.07.2026).
Отдельный префикс взят потому, что `Р-1…Р-10` в этом модуле заняты сессией
23.07.2026 (`docs/superpowers/specs/2026-07-23-borrower-rework-design.md`), а
`Р-11 / Р-20 / Р-22` внутри `borrower.html` — ссылки на решения **залогового**
модуля в зеркальных комментариях. `ОВ-*` продолжает сквозную нумерацию открытых
вопросов модуля (заняты ОВ-2, ОВ-4, ОВ-6).

---

## Готово — экран списка

| # | Решение | Статус |
|---|---------|--------|
| СП-1 | Список = рабочая очередь + полноценный поиск (назначение экрана) | ✅ |
| СП-2 | Один инпут поиска в тулбаре, ИЛИ по наименованию и ИНН | ✅ |
| СП-3 | Группа+подгруппа — один двухуровневый селект | ✅ |
| СП-4 | +Куратор · +Дни просрочки от—до · +Дефекты · +«не применяется» · бейдж числа условий | ✅ |
| СП-5 | Каскад Область → Район из данных; метки без Jmix-синтаксиса | ✅ |
| СП-6 | «+ Добавить заёмщика» остаётся (предрегистрация) | ⚠️ частично — см. ОВ-11 |
| СП-7 | Подгруппа: код · «Кредитов не было» · «Нет действующих» | ✅ |
| СП-8 | «Открыть» убрана, чекбоксов нет, строка открывается кликом и Enter | ✅ |
| СП-9 | Экспорт: видимое + шапка-оттиск (условия, дата среза, дата снимка денег) | ✅ |
| СП-10 | Карточный вид и переключатель удалены | ✅ |
| СП-11 | Настоящая пагинация, размер страницы, «1–25 из 46», дребезг поиска | ✅ |
| СП-12 | ИНН в подписи · куратор разведён с дырой · +«Задолженность» · подгруппа короче | ✅ |
| СП-13 | Дефекты: модальность отдельно от степени, сортировка составным ключом | ✅ |
| СП-14 | Полоса над таблицей с обеими датами | ✅ |

### Что именно изменилось в коде

- **Панель фильтра.** Вписанных значений в разметке больше нет — все справочники
  строит `fillFilterDicts()` из `SUBJECTS` и `REGION_OF`. Полей стало 9 (было 7),
  из них `f-name` / `f-inn` / `f-group` / `f-sub` удалены, добавлены
  `f-q` (в тулбар), `f-region`, `f-grp`, `f-curator`, `f-ovFrom`, `f-ovTo`, `f-def`.
- **`REGION_OF`** поднята из локальной области сид-функции на верхний уровень —
  один источник и для сида, и для каскада фильтра.
- **`subgroupOf()`** получила ступень «нет долга — нет класса»: `'none'` решается
  до лестницы, `'closed'` — после неё (иначе терминальные 3.2 и 5, где кредиты тоже
  закрыты, потеряли бы свой класс). `groupOf()` вне лестницы отдаёт `null`,
  `solvencyOfGroup()` не принимает такие коды за «Неплатёжеспособные».
- **`listRow()`** отдаёт `debt`, `mid`, `curatorNames`, `gaps` — фамилия куратора
  больше не подавляется дырой, а сортировка по колонке идёт только по фамилиям.
- **`sortVal('def')`** возвращает составной ключ `[стоп, высокие, всего]` вместо
  `(стоп ? 1000 : 0) + всего`; сравнение — новая `cmpVal()`.
- **Удалено:** `renderCards`, `listView`, `LIST_VIEW_KEY`, `setListView`, `#segTable`,
  `#segCards`, `#cardsWrap`, `#tbOpen`, `gTop`, CSS `.bcard-*` / `.cards-*`.
  CSS `.viewseg` **сохранён** — им пользуются переключатели внутри карточки
  («Взыскание»: по процессам / по кредитам, «Обеспечение»: по кредитам / по предметам).
- **Сид:** добавлен ОсОО «Нарын Агро Плюс» (ИНН 11101199900111) — предрегистрация
  без единого кредита. Без такой записи ветка СП-7 «Кредитов не было» в макете
  недостижима, а прежняя база выдавала ровно ей «1.1 Производят погашение по графику».

### Проверено

Смоук: **128/128 PASS** (`node scripts/inspect/borrower-check.mjs`, 26.07.2026).
Прежние тесты V1–V3 (переключатель вида) удалены вместе с функциональностью;
S5, C3, C7, H5 переписаны; добавлены 24 теста с префиксом `СП-`.

В браузере (26.07.2026) проверено вживую:

| Проверка | Результат |
|---|---|
| Каскад: область «Чуй» | 4 района, 12 строк, бейдж условий = 1 |
| Фильтр «вся группа 2» | 11 строк, счётчик «1–11 из 11» |
| Фильтр «Нет действующих» | 3 строки — Ак-Суу Майнинг, Глобал Сервис, Осмонов Данияр |
| Фильтр «Кредитов не было» | 1 строка — Нарын Агро Плюс (предрегистрация) |
| Стоп-дефекты + просрочка 90+ | 16 строк, активных условий 2 |
| Сортировка по дефектам ↓ | первая строка «стоп 6в 5с» — модальность впереди степени |
| Карточка предрегистрации | открывается, плитка «Кредитов не было», группа «не применяется», кода `none` в тексте нет |
| Консоль | ошибок нет |

---

## Открытые вопросы

- **ОВ-7 · Сохранённые рабочие списки.** СП-1 их подразумевает («мои»,
  «с дефектами», «без куратора»), но состав, хранение и права не проектировались.
- **ОВ-8 · Удаление записи.** В as-is кнопка «Удалить» есть
  (`requirements/tz/04-zaemshchik.md` §4.1), в макете её нет. По языку модуля
  заёмщик — роль, и удалять её при живых кредитах нельзя; предрегистрированную
  запись (СП-6) удалять надо. Правило не сформулировано.
- **ОВ-9 · Пустое состояние.** «Записей не найдено — измените условия фильтра»
  не говорит, какие условия стоят, и не даёт сбросить их одним нажатием.
- **ОВ-10 · Спека модуля.** `ASUBK-zaemshik-logika.md` не существует (вынесен в
  отдельную задачу спекой 23.07, §0), а сама спека помечена как отставшая от кода.
  Решения СП-1…СП-14 записаны только здесь.
- **ОВ-11 · Форма создания заёмщика (хвост СП-6).** Решение «кнопка остаётся»
  выполнено, но `#tbAdd` по-прежнему без обработчика: состав полей, правила
  валидации и уникальность ИНН в сессии не обсуждались, поэтому форма не
  придумывалась. Состояние, которое она порождает, уже поддержано и видно
  (СП-7 + запись Нарын Агро Плюс).
- **ОВ-12 · Мёртвые контролы панели фильтра.** «+ Условие поиска» (`#fAddCond`) и
  шестерёнка «Настройки фильтра» обработчиков не имеют — это след конструктора
  условий Jmix. По той же логике, по которой правились «Открыть» (СП-8) и стрелки
  пагинации (СП-11), их надо либо оживить, либо убрать. Решения не было — оставлены
  как есть.

---

## Не-цели (демо)

Экспорт формирует TSV с BOM под расширением `.xls` (Excel открывает как таблицу):
XLSX-сериализатор — задача реализации, состав колонок и оттиск от формата не зависят.

---

*Основание: сессия-гриллинг по экрану списка заёмщиков, 26.07.2026.
Решения СП-1…СП-14 приняты Нурсултаном по одному, с разбором каждого компонента.*
