# АСУБК · Анализ — статус разработки

> Канон логики — [`ASUBK-analiz-logika.md`](ASUBK-analiz-logika.md). Язык предметной области —
> [`CONTEXT.md`](../../CONTEXT.md), раздел «Анализ». Необратимые решения —
> `docs/adr/0153`…`docs/adr/0155` поверх общего `docs/adr/0001`.
>
> **Макет** — [`analysis.html`](analysis.html), раздел финанализа; смоук
> `node scripts/inspect/analysis-check.mjs` (52/52 PASS, штамп в шапке макета).
>
> **Постановка** — [`docs/tasks/p21-analiz-tasks.html`](../../docs/tasks/p21-analiz-tasks.html),
> формат 2 (`docs/tasks/FORMAT.md`): 22 задачи `P21-R1`…`P21-R22`, 7 этапов, редакция 1 =
> раздел финанализа. Пакет передачи — **два файла**, постановка и макет; карта «экран макета →
> задача» и **три** названных расхождения — в преамбуле шапки `analysis.html` (было три, одно
> закрыто волной 6 — `АН-Д6`, одно заведено волной 7 — зеркало категории и задолженности в
> затравке). **Редакция считается от выдачи, а не от написания** (`АН-45`): пакет не выдан,
> поэтому правки любой глубины идут в редакцию 1, и бронь «редакция 2 за разделом обзоров»
> отменена.
>
> **Этот файл — задачи по модулю, не бэклог разработки.** Задачи для команды живут в
> `TODO.md`, раздел «Анализ (целевая модель)» — заведён волной 3, счётчик `P21-R*` открыт
> волной 5. С открытием счётчика **источник постановки — HTML**: модель, правила, приёмка и
> границы живут только там, в `TODO.md` остаётся реестр (номер, заголовок, приоритет, статус).
> Дефектов живого стенда по модулю нет и быть не может: в `fkftest.okmot.kg` анализа как модуля не существует —
> он проектируется с нуля, поэтому в `notes/qa-findings.md` строк по нему тоже нет.

## Нумерация

`АН-*` — **решения волн** по модулю анализа. `АН-Д*` — **дефекты и пробелы**
(волна 3 завела первые пять — `АН-Д1`…`АН-Д5`, волна 4 закрыла все пять; волна 5 завела
шестой — `АН-Д6`, волна 6 закрыла и его; волна 7 завела и закрыла `АН-Д7` и `АН-Д8`: открытых
нет). Начиная с волны 7 префикс шире, чем «дефект макета»: `АН-Д7` — расхождение **постановки**
с каноном, `АН-Д8` — пробел **самого канона**, и ни у того, ни у другого поверхности в макете
нет. Префикс проверен на свободу по всему
репозиторию: `АН-` и `АЗ-` не заняты никем (`СС-` — статистика, `КФ-` — классификация,
`КР-` — кредит, `КВ-` — взыскание, `КЗ-`/`ИР-` — заёмщик).

Инварианты модуля идут отдельным рядом — **`ИА-1`…`ИА-20`**, разобраны с причиной в §11
канона логики (`ИА-15`…`ИА-18` добавила волна 4 — по одному на починенный дефект, `ИА-19` —
волна 6, `ИА-20` — волна 7). Это не решения волны: их нельзя закрыть, их можно только сломать.

## Состояние

**Волна 1 — 21.08.2026, модуль выведен с нуля, на бумаге.** Началось с задачи «обдумать
логику модуля анализа с учётом всех остальных модулей», кончилось тремя решениями и одной
переформулировкой требования ТЗ.

Что вскрылось сверх текста ТЗ:

| # | находка | решение |
|---|---|---|
| АН-1 | Заключение — подписанный документ, а не витрина: пересчёт после подписи меняет то, под чем стоит подпись | снимок основания внутри утверждённого заключения, `ADR-0153` — **исключение** из `ADR-0001`, объяснённое образцами `ADR-0007` и `ADR-0016` |
| АН-2 | Конструктор среза, нужный анализу, ТЗ уже описало в подсистеме отчётности | своего конструктора у анализа нет; обзор ссылается на шаблон и его редакцию, `ADR-0154` |
| АН-3 | «По завершению анализа информация должна быть внесена в соответствующие поля объекта» читается как право писать в чужие карточки | требование исполняется зеркалом на карточке заёмщика; обратной записи нет, `ADR-0155` |
| АН-4 | Финанализ по всем типам лица одной формой даёт неотличимое «пусто» | методика объявляет применимость по типу лица; выбор — по типу **на дату отчётности** (`ИА-4`) |
| АН-5 | Исполнитель финанализа уже назначен глоссарием | ведущий куратор заёмщика; отдел анализа владеет методикой и даёт вердикт на заявке, `CONTEXT.md` не правится |
| АН-6 | Срок анализа мог задаваться в двух местах | расписание анализа — единственный источник срока; сопровождение читает его и ведёт обязательство |
| АН-7 | Обзор со списком объектов соблазняет породить задачи автоматически | задачи ставит руководитель со ссылкой на обзор как основание (`ИА-11`) |
| АН-8 | Доли, отклонения и темпы из ТЗ выглядят показателями | это представление, считается при показе из двух чисел ряда (`ADR-0150` §4) |

**Волна 2 — 21.08.2026, модель доведена до исполняемого эталона.** Макет
`mockups/analysis/analysis.html` (1633 строки, single-file) и смоук
`scripts/inspect/analysis-check.mjs` (38 проверок, все PASS). Логика в макете **исполняется**,
а не нарисована: снимок фиксируется операцией `approve()` — в сиде он тоже получен ею, а не
выписан литералом.

| # | что доказано макетом | чем |
|---|---|---|
| АН-9 | Вычислитель собран из записей: названий коэффициентов в нём нет ни одним словом, пятый коэффициент заводится редакцией методики | проверки #3, #4 |
| АН-10 | Методика и её редакция выбираются на дату отчётности: у снявшегося с учёта ИП 2025 год считается методикой ИП, 1П 2026 — методикой физлица | #5, #6, #7 |
| АН-11 | Утверждённое не пересчитывается, даже когда источник подменили в базе; уточнённая версия даёт пометку и показ «что дал бы пересчёт» рядом | #8, #9, #11 |
| АН-12 | Дефекта в состоянии модуля нет ни одним полем — он считается из расписания и факта, гейт заявки читает результат | #26, #27 |
| АН-13 | Наружу два шва; классификация и задачи получают названный отказ, а не пустой ответ | #28…#32 |
| АН-14 | Раздел обзоров заперт кодом, а не отсутствием кнопки: своего конструктора среза в модуле нет ни одной функцией | #35 |

Пять экранов: **Заёмщик · Финансы** (зеркало вывода, обязательство, чужой счёт и гейт),
**Финанализ (документ)** (основание · коэффициенты с порогами · заключение · снимок ·
корректировка против переиздания), **Реестр методик** (редакции, применимость, ссылки),
**Расписание и швы** (строки расписания, обязательства по всем заёмщикам, вызов шва,
таблица потребителей), **Аналитические обзоры** — экран отказа.

**Волна 3 — 28.08.2026, задачи зарегистрированы, макет проверен на роль эталона.** Раздел
«Анализ (целевая модель)» заведён в `TODO.md`: преамбула модуля, два примечания о границах и
**четырнадцать заделов по разделу финанализа** — у каждого названы операция макета и номера
проверок смоука. Счётчик `Pn-R*` не открыт.

| # | решение волны | почему |
|---|---|---|
| АН-15 | Счётчик задач не открывается до постановки: в `TODO.md` идут заделы без ID | образец статистики и классификации — ID заводятся вместе с постановкой (`docs/tasks/FORMAT.md`); номер пакета берётся по факту свободного, а не столбится вперёд написания |
| АН-16 | Раздел обзоров не расписан ни одной задачей — одна строка «ждёт отчётность» | у него нет предусловия: шаблона отчётности в системе нет (`ADR-0154`), а задача без предусловия читается как забытая и уезжает в план раньше времени |
| АН-17 | Границей задела объявлен макет: у каждой задачи названы операция и номер проверки | постановка потом собирается **сверкой с исполняемым эталоном**, а не пересказом по памяти — у классификации пересказ разошёлся с макетом шестью поимёнными пунктами |
| АН-18 | Десять названных отказов вошли отдельным заделом | иначе разработчик реализует «кнопки нет», и десять решений модуля потеряются молча — отказ обязан назвать решение, на котором стоит |
| АН-19 | Эталоном макет объявлен **вместе** с пятью своими расхождениями, а не после починки | назвать и зарегистрировать их честнее, чем чинить втихую под словом «регистрация»: правка — волна 4, заделы на неё ссылаются |

**Дефекты макета, вскрытые проверкой на эталон.** Все пять воспроизведены прогоном логического
слоя — тот же `node:vm`, что и в смоуке. Настоящего браузера в этой среде нет (`playwright-core`
не установлен, `node_modules` в репозитории отсутствует), поэтому ручной прогон по экранам
остаётся волне 4 — на нём у классификации вылезли девять дефектов, которых смоук не видел.

| # | дефект | как воспроизводится |
|---|---|---|
| АН-Д1 | 🟠 Кнопка «Новый финанализ» стоит у **каждой** версии отчётности и зовёт `newAnalysis()` напрямую, минуя `reissue()`: документ за тот же период заводится с `prev:null` и без проверки «основание то же самое». Один поступок двумя дверями даёт разный результат, а `prev` читается только списком на карточке заёмщика | `reissue('ФА-7')` → `ФА-10`, `prev:'ФА-7'`; `newAnalysis({subj:'b-1', report:'r-103'})` → `ФА-10`, `prev:null`. Смоук держит только дверь `reissue` (#16) |
| АН-Д2 | 🟡 Снимок хранит `verdict` на момент подписи; корректировка вывода меняет `d.verdict` и снимка не трогает — в одном заключении два значения одного поля, шов отдаёт исправленное, а на экране снимка вывода нет вовсе | `correct('ФА-7', {field:'вывод', …})` → `snapshot.verdict` = «удовлетворительное с оговорками», `d.verdict` = «удовлетворительное», `analysisVerdict('b-1')` отдаёт второе |
| АН-Д3 | 🟠 `dueOf` берёт строку расписания первой попавшейся по типу лица (`rows.find`, без сортировки по `since`): вторая, более поздняя строка того же типа молча не действует. Рядом `editionOn` тот же датированный выбор делает правильно — сортировкой и последней. Один модуль, два способа выбрать действующую запись на дату | добавить `sc-org-2` с `since:'2026-08-01'` и другой периодичностью → `dueOf('b-1')` по-прежнему возвращает `row:'sc-org'` и прежний срок |
| АН-Д4 | 🟡 Тип лица берётся на **две разные даты**: методика — на дату отчётности (`ИА-4`), строка расписания — на сегодня (`typeOn(subjId, today)`). У сменившего тип внутри периода срок посчитается по одной строке, а заключение встанет на методику другого типа; правило, какая дата верна для срока, не записано ни в каноне, ни в макете | `b-2` снят с учёта как ИП 10.02.2026: обязательство за 2025 год считалось бы строкой `sc-fl`, тогда как ФА-6 за тот же период стоит на методике ИП (#5) |
| АН-Д5 | 🟠 Справочника строк формы отчётности нет как объекта ведения: `LINES` — константа, операции завести строку нет ни одной, а редакция с новой строкой отказывается («строки … в справочнике форм нет»). §2.2 объявляет методику **набором форм ввода**, но форм в модуле не заводится: отдел анализа не может ввести новый показатель отчётности без разработчика | `addEdition` с неизвестной строкой в числителе → отказ; среди операций `AN.*` нет ни одной над `LINES` |

**Волна 4 — 28.08.2026, пять расхождений починены, канон догнал макет.** Все пять дефектов,
вскрытых волной 3, закрыты правкой макета; четыре из них потребовали **правил, которых в каноне
не было вовсе**, — они дописаны в §2.2, §2.5, §3 и §7, а инварианты `ИА-15`…`ИА-18` названы в
§11. Канон переведён в **v2**. Смоук вырос с 38 до **44/44**: по проверке на каждую починку
плюс экранная (#44).

| # | решение волны | почему |
|---|---|---|
| АН-20 | Переиздание собрано в **одну операцию**: правило живёт в `newAnalysis`, а `reissue` стал ярлыком без собственных проверок | правило, продублированное на второй двери, расходится с первым; связь `prev` теперь **выводится из данных**, а не приносится аргументом — аргумента `spec.prev` в модуле больше нет, и это проверено текстом (#39) |
| АН-21 | Вывод **удалён** из снимка, а не синхронизирован с документом | `ADR-0153` §1 перечисляет состав снимка, и суждения в перечне нет: снимок — основание. Синхронизация двух копий одного поля лечит симптом и оставляет вторую копию |
| АН-22 | Датированный выбор записи сведён к **одной функции** `onDate`, ею пользуются тип лица, редакция методики и строка расписания | в модуле было два способа выбрать действующую на дату запись, и второй молча ошибался. Проверка #41 держит и правило, и его единственность |
| АН-23 | При расхождении периодичностей строк срок **не определяется машиной** — она называет отказ | это тот случай, когда молчаливый выбор одной из двух строк даёт правдоподобный, но чужой срок, а на нём вырастает дефект «на допуск». Отказ вместо молчания — домашний стиль модуля |
| АН-24 | Справочник строк формы заведён как **объект ведения** (`addLine`/`retireLine`, владелец — отдел анализа), а не расширен константой | §2.2 объявляет методику набором форм ввода; без операции ведения новый показатель отчётности ждал бы релиза. Снятие строки закрыто той же причиной, что и снятие редакции (`ИА-5`) |
| АН-25 | Ручной прогон в браузере **снова отложен** и записан как долг, а не как сделанный | среда его не даёт двумя разными путями: пакета `playwright-core` нет (`node_modules` в репозитории отсутствует), а MCP-браузер не стартует — «Chromium distribution 'chrome' is not found at /opt/google/chrome/chrome». Смоук рисует экраны в заглушке DOM и ловит текст, но не верстку и не живой клик |

| # | дефект | как починен |
|---|---|---|
| АН-Д1 | ✅ закрыт | правило переиздания перенесено в `newAnalysis` (обе двери), `prev` выводится из данных; в документе появилась строка «Переиздаёт» и колонка «Связь» в списке заключений за период, на карточке заёмщика сказано, что двери две, а операция одна. Проверка #39 |
| АН-Д2 | ✅ закрыт | `verdict` убран из снимка (`ИА-15`); на карточке снимка отсутствие вывода объяснено словами, «было → стало» остаётся в таблице корректировок. Проверка #40 |
| АН-Д3 | ✅ закрыт | введена `onDate`, `dueOf` выбирает строку через `rowOn` — по дате вступления, последнюю (`ИА-17`). Проверка #41, включая обратный случай: строка, вступившая в силу **после** конца периода, на этот период не действует |
| АН-Д4 | ✅ закрыт | тип лица для срока берётся на конец отчётного периода — на ту же дату, что и методика; при расхождении периодичностей назван отказ, зеркало показывает «срок не определён», а не дефект. Проверка #42 |
| АН-Д5 | ✅ закрыт | `AN.addLine`/`AN.retireLine`, карточка «Справочник строк формы отчётности» в реестре методик, редакция на неизвестную и на снятую строку отклоняется поимённо (`ИА-18`). Проверка #43 |

**Волна 5 — 28.08.2026, постановка написана, счётчик открыт.**
`docs/tasks/p21-analiz-tasks.html` (формат 2, JSON-остров): **22 карточки, 7 этапов**, свод —
12 сущностей карты данных, 7 экранов, 25 терминов и сквозной пример на `b-1` ОсОО «Ак-Жол Агро»
за 1П 2026 в восьми шагах. `scripts/check_tasks.py` — 0 ошибок, 0 предупреждений; номер пакета
**P21** взят по факту свободного. Пятнадцать заделов волны 3 закрыты со ссылкой на карточку,
куда каждый уехал.

Этапы: 1 справочники и датированный выбор (`P21-R1`…`R4`) · 2 отчётность (`R5`…`R7`) ·
3 вычислитель (`R8`, `R9`) · 4 документ и подпись (`R10`…`R12`) · 5 снимок и два механизма
правки (`R13`…`R16`) · 6 срок, обязательство, зеркало (`R17`…`R19`) · 7 швы, роли, отказы
(`R20`…`R22`).

| # | решение волны | почему |
|---|---|---|
| АН-26 | Постановка собрана **сверкой с исполняемым эталоном**: каждое правило перед записью прогонялось в `node:vm` | так и было объявлено заделом (`АН-17`). Сверка окупилась сразу — она вскрыла **три моих собственных расхождения** с макетом (ниже), все три до сдачи. У классификации пересказ по памяти разошёлся с макетом шестью пунктами |
| АН-27 | Числа всех примеров — **прогон затравки**, а не выписанные значения: 1,2203 · 0,3315 · 7,62 % · 4,2202 · 1,2857 у ФА-7, 1,1183 / 4,3853 / 0,312 у «что дал бы пересчёт», срок 14.08.2026 | пример с выдуманным числом разработчик проверить не может, а сойтись с ним обязан: приёмка на нём и стоит |
| АН-28 | Расхождения макета и постановки названы **поимённо в преамбуле пакета**, а не вычищены задним числом | у пакета из двух файлов правило старшинства бесполезно без списка мест, где файлы расходятся: иначе разработчик угадывает. Названо три (расписание+швы одним экраном, адресация экрана обзоров, `АН-Д6`) |
| АН-29 | Раздел обзоров в редакцию 1 **не вошёл** и назван первым пунктом справки модуля | ровно причина `АН-16`: у него нет предусловия (`ADR-0154`, модуля отчётности нет). Ненаписанный раздел, о котором сказано «не пишется и почему», читается решением; просто отсутствующий — забытым |
| АН-30 | Три открытых вопроса перенесены в постановку **с ответом по умолчанию**, а не оставлены вопросами | формат запрещает `default` вида «уточняется»: у отсрочек расписания (`P21-R17`), группового заёмщика (`P21-R18`) и видимости чужих заключений (`P21-R21`) есть работающее умолчание, и разработка не встаёт, пока идёт согласование |
| АН-31 | Номер пакета при столкновении уступлен: анализ писался в своём worktree и взял `P20`, тот же номер в `main` уже держало **расчётное ядро** (32 карточки) — перенумерован **анализ**, `P21-R1`…`P21-R22` | у ядра номер старше по `main`, и перенумеровать влитое дороже, чем невлитое. Правило записано в `docs/ochered-modulei.md`: свободный номер берётся `git ls-tree` по **всем ветвям**, а не по рабочему дереву — ветка чужого worktree держит номер, которого в `main` ещё нет (тот же урок, что и с номерами ADR) |

**Три расхождения постановки с эталоном, найденные сверкой и исправленные до сдачи.** Ни одно
не дошло бы до дефекта макета — они были в моём тексте, и сверка их сняла:

| что я написал | как на самом деле | где исправлено |
|---|---|---|
| подпись при непосчитанном коэффициенте проходит с предупреждением | `AN.approve` **отказывает**: подпись стоит под полным основанием (смоук #14) | `P21-R9` переписана целиком, `P21-R12` — шаг и приёмка |
| версия отчётности вносится с любым составом строк | `AN.addReport` требует **полной формы** и называет пустые строки поимённо | `P21-R6` — шаг, блок правил «Полнота, пустое и ноль», пункт приёмки |
| справочник строк формы открыт любой роли | `AN.addLine` закрыт `AN.isAnalyst()` — ведёт отдел анализа | снято из преамбулы пакета |

| # | дефект | как воспроизводится |
|---|---|---|
| АН-Д6 | 🟡 Нулевой знаменатель макет отдаёт **общей причиной** «не заполнены строки», и список строк при этом пуст — отказ читается оборванным: «не посчитан коэффициент «X»: в отчётности не заполнены строки ». `OPS.ratio`/`OPS.share` возвращают `null` при нулевом знаменателе, а `missingOf` не находит ни одной пустой строки. Причины разные и лечатся по-разному: пустая строка — внесением отчётности, нулевой знаменатель — разбором самой отчётности | завести версию, где знаменатель коэффициента заполнен нулём, → `calcCoef` даёт `{v:null, missing:[]}`, `AN.approve` отказывает с пустым перечнем. Постановка требует **двух** причин (`P21-R9`), макет даёт одну |

**Волна 6 — 29.08.2026, шестой дефект закрыт, эталон и постановка сошлись.** `АН-Д6` —
единственный дефект, вскрытый не проверкой макета, а **сверкой с постановкой**: правило
писалось от того, как беда лечится, и оказалось, что бед две, а причина в макете одна.
Починен макет, постановка не тронута по существу. Смоук вырос с 44 до **46/46**, канон
переведён в **v3** (правило — §3, инвариант — `ИА-19`).

| # | решение волны | почему |
|---|---|---|
| АН-32 | Вычислитель возвращает причину **кодом и списком строк**, а слова собираются один раз снаружи — их читают и экран, и отказ в подписи | это тот же урок, что и `АН-20` про две двери одного правила: второй текст той же причины расходится с первым, и расходится молча. Проверка `#45` держит не только два кода, но и единственность текста |
| АН-33 | Нулевой знаменатель объявлен **операцией** (`nonZero:'den'` в записи `OPS`), а не веткой в вычислителе | операции — закрытый список записей (`ИА-3`), и «чего операция не терпит» — такое же её свойство, как знак и способ счёта. Ветка `if(op==='ratio')` вернула бы в вычислитель то, что из него убрано волной 2 |
| АН-34 | `apply` перестала возвращать `null`: guard стоит **до** неё | безымянный `null` от операции неотличим от «данных нет» — назвать причину по нему нечем, и именно из этого дефект и вырос |
| АН-35 | Постановка **исправлена в двух местах, а не переиздана**: редакция 1 остаётся редакцией 1 | по существу не изменилось ничего — правило `P21-R9` было верным с самого начала, чинился макет. Устарело описание макета: ловушка утверждала «в макете обе беды приходят одним отказом», и после починки это стало неправдой о файле, который лежит в том же пакете. Переиздание (`revision: 2`) зарезервировано за разделом обзоров |
| АН-36 | Пример постановки приведён к **дословному** тексту эталона: «знаменатель равен нулю: “Выручка за период” = 0» | пример, который цитирует отказ приблизительно, разработчик сверить не может: он обязан сойтись с ним посимвольно, и приёмка стоит на этом. Числа примера (0,3315 · 4,2202 · четыре посчитанных из пяти) смоук `#45` теперь воспроизводит прогоном, а не подтверждает на слово |

| # | дефект | как починен |
|---|---|---|
| АН-Д6 | ✅ закрыт | `calcCoef` возвращает `nocalc:{code, lines}` — `empty` со списком незаполненных строк или `zeroden` со строками знаменателя; текст собирает `nocalcWhy` (`NOCALC`), и его читают оба потребителя: таблица коэффициентов ставит причину под пометкой «не посчитан», отказ в подписи — после названия коэффициента. Проверки `#45` (две причины, ноль остаётся данными, текст один) и `#46` (то же видно на экране и в отказе) |

**Волна 7 — 29.08.2026, сверка с Порядком №41 и с канонами соседей; таблица требований
заведена, пункт `A3` очереди закрыт.** Первая волна модуля, шедшая **не по своему макету**:
два внешних угла названы заранее — норма и соседи. Норма переписала ключ расписания и дала
обзору основание без обязательства; соседи дали форму шва к заявке и вычистили модель обзора.
Работа шла картой из шести тикетов — [`.scratch/analysis-suhost/map.md`](../../.scratch/analysis-suhost/map.md).
Смоук вырос с 46 до **52/52**, канон переведён в **v4** (`ИА-20`), постановка `P21` правлена
по существу внутри редакции 1.

| # | решение волны | почему |
|---|---|---|
| АН-37 | Обязательность планового финанализа ставится **парой «тип лица × категория риска»** плюс **порогом суммы задолженности**, а не одним типом лица: охват и порог — ведомые поля строки расписания, ключ строки — пара, датирование `since` сохраняется | так ставит норма: п. 11.1 — по низкому риску плановый анализ **не проводится**, п. 11.2 — по среднему обязателен при задолженности свыше 50 000 000 сомов, п. 6.5 — отслеживаются «Средний» и «Высокий» наравне. Число, движущееся вместе с редакцией Порядка, обязано быть **записью, а не константой кода**: иначе следующую редакцию отрабатывает разработчик релизом, а не отдел анализа записью (`ИА-5`, `АН-24`) |
| АН-38 | Категория риска и сумма задолженности читаются **на конец отчётного периода** — той же датой, что тип лица (`ИА-4`) и редакция методики (`ИА-17`) | третья дата воспроизводит `АН-Д4`, закрытый волной 4 ровно за это: величина, взятая на дату, отличную от периода, о котором документ судит. Оба шва датированы и читать на эту дату умеют. Затравка сделана **несущей**: у `b-1` долг падает ниже порога после 30.06, и чтение «сегодня» дало бы обратный ответ (#49) |
| АН-39 | Порог — величина **строки**, а не общая константа: у среднего риска 50 000 000 сомов, у высокого порога нет (обязателен безусловно), для низкого строки расписания нет вовсе | п. 11.3 фразы про финанализ не содержит вовсе, а п. 6.5 называет высокий риск наравне со средним — значит для высокого условие не смягчено, а **отсутствует**. п. 11.1 исполнен **отсутствием строки**, а не полем «не обязателен»: пустое поле порога означает «порога нет», а не ноль (#47, #50) |
| АН-40 | Состав «полного пакета финансовых документов» объявляет отдел анализа **справочником строк формы** (`ИА-18`), а не отдельный перечень | фраза встречается в Порядке **единственный раз** (п. 6.5), состава не называет и к другому документу не отсылает. Перечень, если появится, войдёт **редакцией справочника**, а не вторым механизмом. Строки требования нет: норма — не сосед |
| АН-41 | «Департамент анализа и отчетности» Порядка и роль «Сотрудник отдела анализа» канона — **разные вещи**; орг-единица в `CONTEXT.md` не заводится | норма называет орг-единицу, объединяющую анализ и отчётность, и внутри неё сектор ИТ, вносящий первичные данные в АСУБК (п. 13, п. 14); канон называет **владельца методики и справочника строк**. Глоссарий ведёт предметные термины, не оргструктуру. Группа платёжеспособности и подгруппа анализа не трогают ни как условие, ни как срез — они целиком классификационные |
| АН-42 | «Анализ качества и структуры общего объема отслеживаемой задолженности по портфелю бюджетных кредитов» (п. 6.7) — **это аналитический обзор**; посылка §15 «б» («у обзора нормы нет») ложна и снята | определение нормы совпадает с предметом §2.4 — суждение над срезом; портфель бюджетных кредитов есть **значение разрезов** («все кредиты»), а не новый объект. Значит модуль за границу к статистике не выводится, граница с дашбордом не двигается, и модель обзора шире не меняется |
| АН-43 | Норма обязательства **не порождает**: основание есть, срока нет — обзор делается **по поводу**, расписание остаётся календарём одного финанализа | две независимые причины. (1) «На постоянной основе» — режим, а не периодичность: там, где норма хотела назвать периодичность, она назвала её явно — п. 6.1, «не реже одного раза в месяц»; молчание рядом с говорящим соседом есть молчание. (2) Расписание объявлено **единственным** источником срока (`АН-6`) — обязательство без строки расписания было бы вторым источником |
| АН-44 | Канон цитирует норму **по месту**: основание обзора — абзацем в §4, граница «расписание ведёт только финанализ» — строкой в §7. Раздела «Нормативное основание» не заводится | отдельный список — второе место, где написано то же самое (`АН-20`/`АН-32`); сводку собирает `check_points.py --map`. С этой волны прогон чекера — **обязательный шаг**: до неё канон анализа не ссылался на Порядок ни разу, а пять файлов модуля заведены в его область (97/97 → **102/102**) |
| АН-45 | **Редакция постановки считается от выдачи, а не от написания.** `P21` не выдан — правки любой глубины идут в редакцию 1; бронь «редакция 2 зарезервирована за разделом обзоров» отменена | редакция — обещание читателю, а читателя ещё не было. Прецедент `АН-35` разрешал точечную правку потому, что правило не менялось; здесь `P21-R17` меняется **по существу** — разработчик, читавший прежний текст, построил бы справочник из четырёх строк по типу лица. Без этого решения пришлось бы открывать редакцию 2 на невыданный пакет |
| АН-46 | §15 «а» (групповой заёмщик) и «в» (видимость финанализа) остаются с умолчанием — но **с основанием**, а не с молчанием | проверено нулевыми вхождениями: «групповой заемщик» — 0, «филиал» / «доступ к» / «конфиденциальн» — 0. Записанное основание отличает «норма этого не решает» от «мы не смотрели». Вопросом заказчику остаётся формулировка §15, умолчанием для разработки — формулировка постановки; «не нужна ли отдельная периодичность для крупных заёмщиков» из `P21-R17` **закрыто нормой** (`АН-39`) и снято |
| АН-47 | **Критерий одностороннего шва.** Односторонне объявленный шов — требование соседу только там, где исполнение требует работы **на чужой стороне**; шов, объявленный соседом публичным, требованием не является | так устроены сами соседи: ядро — `ИЯ-1` («потребитель, которому нужно своё, заводит своё хранилище, а не ещё одну колонку в проекции»), классификация §9 («чужой модуль отдаёт вход и **читает выход**; ни один из них не пишет значение и не правит правило»). Требовать от них строки о себе значит требовать работы, которой нет. По этому критерию девять строк разложились на четыре «выполнено», одну «открыто» и четыре «не тронуто» |
| АН-48 | Вердикт на заявке подставляется **ссылкой-основанием**, а не заключением: шов `analysisVerdict` даёт вывод, номер, дату и редакцию методики, текст заключения отдела аналитики по-прежнему пишет человек | решено не нами, а глоссарием: финанализ — «**материал, а не решение** — на него ссылается вердикт отдела на заявке, но сам он ничего не разрешает и не запрещает». Требование к заявке от этого становится маленьким и точным, а `ADR-0155` не нарушается: ссылку показывает заявка у себя, анализ не пишет ничего |
| АН-49 | **Сопровождение — пропущенный модуль, а не пропущенная строка**: §7 канона не переписывается, в очередь выносится **заявка на модуль** | в отсутствующего соседа независимо упёрлись **два** модуля: §12 анализа (`analysisDone`, обязательство, дефект) и §16 отчётности (бланки, `reportIssues`). Дыра, найденная двумя независимыми модулями, — пропущенный модуль, и очередь ведётся ровно для этого: с подсистемой заданий она поступила так же. Переписать §7 под существующего соседа значило бы назначить владельца обязательства из своей ветки — перенос решения через границу модуля (прецедент `B3`) |
| АН-50 | Список объектов среза — **рабочий список отчётности** по ссылке; своего экземпляра состава обзор не заводит | `ADR-0157`, «Границы»: «Снимок хранит числа, а не объекты. Замороженный состав объектов — это **рабочий список**, отдельная вещь с другой судьбой». Механизм у соседа уже есть. Свой завёл бы ту же ошибку, ради которой писался ADR, только на объектах вместо чисел; а спрашивать `statRows` заново при показе нельзя — состав поедет под ногами, и «утверждение фиксирует состав» станет неправдой |
| АН-51 | `ИА-8` **остаётся** — с переписанной причиной: «исполняется паспортом выпуска, своего механизма у обзора нет» | инвариант — утверждение о том, чего в системе **не бывает**; кто его держит — второй вопрос, и смоук с приёмкой ищут инварианты, а не механизмы. Прецедент в той же таблице — `ИА-13`: «дефект анализ не хранит: отдаёт факт, дефект считает сопровождение» |
| АН-52 | У обзора нет ни корректировки, ни переиздания: утверждённый обзор, чьи числа поехали, **метку носит и не пересчитывается**; исправление — корректирующий выпуск у отчётности плюс **новый** обзор | править в обзоре нечего — внутри суждение и две ссылки; `ИА-1` держится неизменяемостью выпуска. `ИА-16` (переиздание за период) на обзор **не распространяется** — прямое следствие `АН-43`: обзор делается по поводу и обязательства не закрывает, поэтому два обзора одного среза — обычное дело, а не коллизия, требующая правила |
| АН-53 | В `CONTEXT.md` заведён **третий вид получателя выпуска — обзор** | не правка чужого канона, а запись уже принятого совместного решения: `ADR-0157` §3 подписан обоими модулями. Без неё пометка «пригоден для обзора» повисает — она разрешает называть обзор получателем, а глоссарий такого получателя не знал |
| АН-54 | Признак «только при действующих кредитах» из строки расписания **убран** как вторая дверь одного правила | категория риска живёт на кредите: у заёмщика без действующих кредитов её нет вовсе, и отказ приходит от классификационного шва раньше, чем дошло бы до признака. Два условия об одном и том же расходятся молча (`АН-20`, `АН-32`). Принято **при исполнении** тикета 05, а не решено заранее, и потому названо отдельной строкой: проверка #47 держит теперь причину «категории риска нет», а не «кредитов нет» |

**Два дефекта, заведённые и закрытые внутри волны.** Ни один не в макете — впервые за шесть
волн: `АН-Д7` живёт в постановке, `АН-Д8` — в самом каноне. Заводятся всё равно, по прецеденту
волны 4 (пять дефектов заведены и закрыты одной волной): дефект, найденный и закрытый молча,
будет найден следующей сверкой заново, и та не узнает, что его уже видели.

| # | дефект | как закрыт |
|---|---|---|
| АН-Д7 | 🟡 Постановка расходится с каноном тремя открытыми вопросами: `АН-30` утверждает, что все три вопроса §15 перенесены в `P21` с ответом по умолчанию, — на деле вопрос «б» (есть ли расписание у обзора) в постановку **не перенесён вовсе**, а на его месте стоит вопрос о справочнике расписания, которого в §15 нет. Два других сужены: «на группе или на членах» → «как считается срок», «видимость чужих заключений» → «видят ли сотрудники других филиалов» | §15 приведён в соответствие: вопросов стало два, посылка «б» снята как ложная (`АН-42`), у обоих оставшихся записано основание умолчания (`АН-46`). `АН-30` в этой части признан неверным — здесь и в разделе «Открытые вопросы» ниже. Поверхности в макете у дефекта нет: он между двумя текстами |
| АН-Д8 | 🟡 §4 канона называет **два** шва статистики («система спрашивает статистику швами `statSlice` и `statSeries`»), тогда как §5 берёт **три**, и пропущен именно тот, которым приходит **список объектов среза**, — а §2.4 требует хранить его в обзоре. Так шов и терялся по дороге в постановку | §4 назвал третий шов. Строка требования к статистике от этого стала точной: её §12 объявляет анализу только `statSeries`, а нужны три — это единственная **открытая** строка таблицы ниже. Поверхности в макете нет: раздела обзоров в нём не заводится (`ADR-0154`, `АН-29`) |

**Чего волна не сделала, и это названо.** Домашнее правило «по проверке смоука на каждый
закрытый дефект» здесь не исполнено: у `АН-Д7` и `АН-Д8` в макете **нет поверхности** — оба
про раздел обзоров и про соседство текстов, а раздела обзоров макет сознательно не строит.
Шесть новых проверок (#47…#52) закрывают решения `АН-37`…`АН-39` и `АН-54`, но не эти два
дефекта. Изобретать под них проверку значило бы проверять не то.

### Вердикт о сухости

**Волна 7 сухой не была.** Критерий очереди (`docs/ochered-modulei.md`, решение 7): волна
считается сухой, если не добавила ни строки в «Что модуль требует от соседей». Эта волна
завела **саму таблицу** и все её десять строк, и шесть из них — прямая работа двух углов:

- **от Порядка №41** — две: категория риска у классификации и сумма задолженности у ядра.
  Обе соседями исполнены заранее, и обе всё равно строки: неполна была **наша** сторона —
  до этой волны расписание о них не знало (`АН-37`…`АН-39`). Молчать о них значит скрыть
  зависимость, которая уже появилась в макете и смоуке;
- **от канонов соседей** — четыре: `statSlice`/`statRows` у статистики (единственная
  открытая), ссылка-основание у заявки, место под зеркало у карточки заёмщика, расписание с
  обязательством у сопровождения.

**Угол назван, и назван честно.** Сухость меряется исчерпанностью не модуля, а того угла, с
которого на модуль смотрят, — урок отчётности, объявившей себя сухой по трём волнам, шедшим
**по своему макету**, после чего каждая сверка с внешним источником добавляла строку. Здесь
углов было два, оба внешние, и оба дали строки. Третий угол проверен и **пуст**: анализа как
модуля в старой системе нет — в `requirements/legacy/` «финанс…» встречается трижды и всё не о
том. ТЗ углом не считается: модуль из него и выведен волной 1. **Новая сверка с новым
источником даст новую строку — это нормально, и от этого не защищаются.**

## Что модуль требует от соседей

Раздел заведён **волной 7, 29.08.2026** — до неё его у модуля не было вовсе, и именно это
держало пункт `A3` очереди: §5 канона перечисляет только то, что модуль **берёт** через уже
объявленные швы, а таблицы, в которую волна либо добавляет строку, либо нет, не существовало.

Форма — по образцу отчётности. Значения столбца «состояние»: **выполнено** · **выполнено
иначе** (и переписана наша сторона, а не их) · **условное** (ждёт ответа заказчика) ·
**открыто** · **не тронуто**. У «не тронуто» в скобках сказано главное — **жив ли адресат**:
есть ли у модуля рабочая ветка и владелец, которому требование вообще можно предъявить
(проверено перечнем рабочих деревьев 29.08.2026). Требование без адресата **не снимается**:
требование без адресата — это адресат, которого ещё не завели, а не требование, которого не
было.

Строка заводится по критерию `АН-47`: односторонне объявленный шов есть требование соседу
только там, где исполнение требует работы **на чужой стороне**. Публичный шов соседа
требованием не является — но в таблице стоит, со состоянием «выполнено»: строка со
состоянием «выполнено» — всё равно строка, и зависимость она называет.

| от кого | что | где записано | состояние на 29.08.2026 |
|---|---|---|---|
| Отчётность | шаблон с пометкой **«пригоден для обзора»** и его редакция; окончательный выпуск как снимок основания, получателем которого назван обзор | канон отчётности §16, `ADR-0154`, `ADR-0157` §3 | **выполнено** (объявлено со стороны соседа). Наша сторона дописана: в `CONTEXT.md` заведён третий вид получателя выпуска — обзор (`АН-53`) |
| Отчётность | убрать из модели обзора хранение чисел, оставить ссылку на выпуск | `ADR-0157` §4 | **выполнено** — исполнено буквально, и заодно ушёл **состав**: список объектов среза стал рабочим списком отчётности по ссылке (`АН-50`). Канон v4, §2.4, §2.5, §4, §6, `ИА-20`; `ИА-8` остался с переписанной причиной (`АН-51`) |
| Классификация | **категория риска заёмщика на дату** швом `riskCategory` — и как условие строки расписания (охват), и как величина среза; свёртка «худшее по кредитам» — её | `ADR-0124` (шов датирован), `ADR-0121` §2 (свёртка); канон анализа §5, §7 | **выполнено** (публичный шов, `АН-47`). Строка заведена, хотя уже исполнена: неполна была **наша** сторона — `АН-37`, `АН-38`. Обратного направления нет и не будет: `ИА-10` |
| Ядро | **сумма задолженности заёмщика на дату** швом `calcPortfolio` — с ней сравнивается порог строки расписания; величины по одному объекту — швами `calc*` | `ADR-0135` §4; канон анализа §5, §7 | **выполнено** (публичный шов, `АН-47`). То же самое: швы объявлены и датированы до того, как мы их спросили (`АН-37`, `АН-39`) |
| Кураторство | ведущий куратор заёмщика — **вычислением**, а не полем: он ведёт и утверждает финанализ своих заёмщиков, чужому отказ приходит с именем куратора | `CONTEXT.md`, ТЗ 16 §3.2; канон анализа §9 | **выполнено** (публичный шов, `АН-47`) |
| Статистика | открыть анализу **`statSlice` и `statRows`**: её §12 объявляет анализу только `statSeries`, а обзору нужны три шва — срез, ряд и список объектов среза | канон анализа §4, §5; канон статистики §12 | **открыто** (адресат жив: ветка статистики заведена). Вскрыто вместе с `АН-Д8` — §4 канона называл два шва из трёх, и третий терялся по дороге в постановку |
| Заявка и комиссия | показать в блоке заключения отдела `analytics` **ссылку-основание** на последний утверждённый финанализ швом `analysisVerdict` — вывод, номер, дата, редакция методики; текст заключения по-прежнему пишет человек | канон анализа §8, §12; `АН-48` | **не тронуто** (адресата нет: ветки у заявки не заведено). **Место за столом есть** — отдел `analytics` в `DEPT_DIR` заявки заведён (`mockups/loan-application/loan-application.html:5962`); нет провода, а не места |
| Субъекты / Заёмщики | место в шапке карточки под **зеркало вывода** (вывод · номер · дата) и вкладка «Финансы» со списком заключений; своих значений анализ не пишет ни одного | канон анализа §12; `ADR-0155` | **не тронуто** (адресата нет: ветки у заёмщиков не заведено; канон заёмщика анализ не упоминает ни разу) |
| Сопровождение | читать расписание анализа, вести **обязательство** по нему и считать **дефект**; читать `analysisDone` | канон анализа §7, §12; `ADR-0155` §6 | **не тронуто** (адресата нет вовсе: модуль сопровождения не прорабатывался — `АН-49`, заявка заведена в `docs/ochered-modulei.md`) |
| Задачи | обзор как **основание** задачи: задачу ставит руководитель ссылкой на обзор, своей мини-задачи модуль не заводит | канон анализа §11 (`ИА-11`), `ADR-0155` §4, `ADR-0201` | **не тронуто** (адресата нет вовсе: подсистема управления заданиями ТЗ не прорабатывалась — та же дыра, из-за которой у отчётности помечена `ФО-20`) |

## Что дальше

1. **Ручной прогон по экранам в браузере** (`АН-25`) — единственный оставшийся долг по
   разделу финанализа, и он не «сделано»: среда его по-прежнему не даёт ни одним путём
   (`playwright-core` не установлен, `node_modules` в репозитории нет, Chrome по
   `/opt/google/chrome/chrome` отсутствует — перепроверено 29.08.2026). Смоук рисует экраны в
   заглушке DOM и ловит текст, но не вёрстку и не живой клик. На классификации браузерный
   прогон дал девять дефектов, которых смоук не видел, — ждать от него того же и здесь.
2. **Раздел ТЗ** — свободный номер `requirements/tz/20`, пишется последним и не раньше раздела
   по отчётности (очередь, «Выдача», пункт 5).
3. **Раздел обзоров** — когда появится шаблон отчётности с пометкой «пригоден для обзора»:
   тогда же снимаются `ИА-6`…`ИА-8` из «в ожидании» на экране обзоров. В постановку он войдёт
   **той же редакцией 1**, пока пакет не выдан (`АН-45`), — бронь «редакция 2» отменена.
4. **Одна открытая строка требований** — `statSlice` и `statRows` у статистики. Адресат жив,
   предъявляется его следующей волной; нашу работу не блокирует.
5. **Четыре строки без адресата** — заявка, карточка заёмщика, сопровождение, задачи. Не
   снимаются и не ждут: заявка на модуль сопровождения заведена в очереди (`АН-49`), у
   подсистемы заданий та же судьба, что у отчётности с `ФО-20`.

**Формулировка «своей работы у модуля больше нет» волной 7 опровергнута и переписана.** Она
стояла здесь после волны 6 и была правдой ровно в одном смысле: работы не было **по своему
макету**. Два внешних угла дали восемнадцать решений, два дефекта, шесть проверок смоука,
канон v4 и десять строк требований. Точная формулировка другая: **работа по разделу
финанализа исчерпана изнутри**, и возобновляет её только внешнее — новый источник сверки,
выданный канон соседа или ответ заказчика на два оставшихся вопроса §15.

## Открытые вопросы

В §15 канона их **два**, а не три: групповой заёмщик (документ на группе или на членах) и
видимость финанализа сотрудниками других подразделений. У обоих умолчание оставлено **с
основанием**, а не с молчанием (`АН-46`): Порядок №41 не решает ни того, ни другого —
«групповой заемщик», «филиал», «доступ к», «конфиденциальн» не встречаются в нём ни разу.
Вопросом заказчику остаётся формулировка §15, умолчанием для разработки — формулировка
постановки (`P21-R18`, `P21-R21`): согласование идёт, разработка не ждёт.

Третий вопрос — «есть ли расписание у обзора» — **снят как ложная посылка** (`АН-42`, `АН-43`):
основание у обзора есть (п. 6.7), но срока норма ему не даёт, и обязательства не возникает.
Это записано в §4 канона **фактом, а не вопросом**. Видимость **обзора** снята отдельно:
после `ADR-0157` §4 она наследуется через паспорт выпуска — закрыта механизмом, а не
согласованием.

Открытый вопрос `P21-R17` сузился до одного: «не нужна ли отдельная периодичность для крупных
заёмщиков» **закрыто нормой** (порог 50 000 000 сомов, `АН-39`); осталось «одинаковы ли
отсрочки по типам лица» — умолчание 45 дней для лиц и 60 для группового заёмщика, нормой не
решается.

**`АН-30` в части «три вопроса §15 перенесены в постановку» неверен** — это `АН-Д7`: вопрос
«б» в постановку не переносился вовсе, на его месте стоял вопрос о справочнике расписания,
которого в §15 нет. Утверждение оставлено на месте с этой поправкой, а не вычищено задним
числом: читатель, проверивший его, должен найти здесь ответ, а не второе такое же
утверждение.
