# АСУБК · Анализ — статус разработки

> Канон логики — [`ASUBK-analiz-logika.md`](ASUBK-analiz-logika.md). Язык предметной области —
> [`CONTEXT.md`](../../CONTEXT.md), раздел «Анализ». Необратимые решения —
> `docs/adr/0153`…`docs/adr/0155` поверх общего `docs/adr/0001`.
>
> **Макета ещё нет.** До него единственный источник модели — канон логики.
>
> **Этот файл — задачи по модулю, не бэклог разработки.** Задачи для команды живут в
> `TODO.md`. Дефектов живого стенда по модулю нет и быть не может: в `fkftest.okmot.kg`
> анализа как модуля не существует — он проектируется с нуля, поэтому в
> `notes/qa-findings.md` строк по нему тоже нет.

## Нумерация

`АН-*` — **решения волн** по модулю анализа. `АН-Д*` — **дефекты и пробелы макета**, когда
макет появится. Префикс проверен на свободу по всему репозиторию: `АН-` и `АЗ-` не заняты
никем (`СС-` — статистика, `КФ-` — классификация, `КР-` — кредит, `КВ-` — взыскание,
`КЗ-`/`ИР-` — заёмщик).

Инварианты модуля идут отдельным рядом — **`ИА-1`…`ИА-14`**, разобраны с причиной в §11
канона логики. Это не решения волны: их нельзя закрыть, их можно только сломать.

## Состояние

**Волна 1 — 21.08.2026, модуль выведен с нуля, на бумаге.** Началось с задачи «обдумать
логику модуля анализа с учётом всех остальных модулей», кончилось тремя решениями и одной
переформулировкой требования ТЗ.

Что вскрылось сверх текста ТЗ:

| # | находка | решение |
|---|---|---|
| АН-1 | Заключение — подписанный документ, а не витрина: пересчёт после подписи меняет то, под чем стоит подпись | снимок основания внутри утверждённого заключения, `ADR-0153` — **исключение** из `ADR-0001`, объяснённое образцами `ADR-0007` и `ADR-0016` |
| АН-2 | Конструктор среза, нужный анализу, ТЗ уже описало в подсистеме отчётности | своего конструктора у анализа нет; обзор ссылается на шаблон и его редакцию, `ADR-0154` |
| АН-3 | «По завершению анализа информация должна быть внесена в соответствующие поля объекта» читается как право писать в чужие карточки | требование исполняется зеркалом на карточке заёмщика; обратной записи нет, `ADR-0155` |
| АН-4 | Финанализ по всем типам лица одной формой даёт неотличимое «пусто» | методика объявляет применимость по типу лица; выбор — по типу **на дату отчётности** (`ИА-4`) |
| АН-5 | Исполнитель финанализа уже назначен глоссарием | ведущий куратор заёмщика; отдел анализа владеет методикой и даёт вердикт на заявке, `CONTEXT.md` не правится |
| АН-6 | Срок анализа мог задаваться в двух местах | расписание анализа — единственный источник срока; сопровождение читает его и ведёт обязательство |
| АН-7 | Обзор со списком объектов соблазняет породить задачи автоматически | задачи ставит руководитель со ссылкой на обзор как основание (`ИА-11`) |
| АН-8 | Доли, отклонения и темпы из ТЗ выглядят показателями | это представление, считается при показе из двух чисел ряда (`ADR-0150` §4) |

## Что дальше

1. **Макет** `mockups/analysis/analysis.html` — раздел финанализа: реестр методик, документ
   финанализа со снимком, зеркало в шапке заёмщика. Раздел обзоров в макет не идёт, пока нет
   шаблона отчётности (`ADR-0154`, границы).
2. **Смоук** `scripts/inspect/analysis-check.mjs` вместе с макетом; держит `ИА-1`…`ИА-14`.
3. **Регистрация задач** в `TODO.md` — после макета.
4. **Раздел ТЗ** — последним и не раньше раздела по отчётности.

## Открытые вопросы

Перечислены в §15 канона логики с рекомендацией по каждому: групповой заёмщик (документ на
группе или на членах), расписание у обзора (нужно ли), видимость чужих заключений.
