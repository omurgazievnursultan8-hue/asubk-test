# `kuratorstvo.html` — записка: прототип старше модели

> **Исполнено 16.08.2026: прототип переписан целиком.** По адресу `kuratorstvo.html` теперь
> лежат волны 1–2 нового макета — под принятую модель, с исполняемой логикой и смоуком
> `scripts/inspect/kuratorstvo-check.mjs` (70/70). Всё, что ниже, — **археология**: разбор
> уничтоженного прототипа, объясняющий, почему макет переписан, а не поправлен. Номера строк
> относятся к коммиту `0ddd575`. Живое состояние и план волн —
> [`ASUBK-status-razrabotki.md`](ASUBK-status-razrabotki.md).

Мокап модуля «Кураторство», один самодостаточный файл (954 строки, vanilla JS, без сети и без
хранилища), заведён 12.07.2026 коммитом `0ddd575`. Четыре вкладки: **Политика распределения · Пересчёт ·
Карточка кредита · Очередь передач**.

**Он старше принятой модели.** 13.08.2026 модуль прошёл разбор и получил фиксацию; прототип под
неё не переписывался. Читать его как постановку нельзя — расхождения перечислены ниже.

## Спека, на которую он ссылается, утрачена

Каждый второй комментарий в файле ссылается на `ASUBK-kuratorstvo-logika.md` («каркас залочен
11.07.2026», строка 12) и на её параграфы — §2.1, §4.3, §4.4, §6.1, §7, §9.2, §10.2, §13.1.
**Этого файла нет ни в одном коммите репозитория.** Все §-ссылки в прототипе висят в пустоту:
проверить по ним ничего нельзя, и восстановить их источник — тоже.

## Где теперь живёт модель

| что | где |
|---|---|
| постановка разработчикам, редакция 1 | `docs/tasks/p18-kuratorstvo-tasks.html` — 5 этапов, 20 карточек |
| термины | `CONTEXT.md`, раздел «Кураторство» (12 статей) |
| необратимые развилки | `docs/adr/0116-kuratorstvo-izmerenie-a-ne-pole.md` · `0117-dve-stupeni-pravila-i-folbek.md` · `0118-otstranenie-otdelnoe-izmerenie.md` |
| реестр задач | `TODO.md`, секция «Кураторство» (`P18-R1`…`P18-R20`) |

Постановка — источник истины по логике. Прототип входит в пакет передачи как **иллюстрация
экранов**, а не как описание поведения.

## Что в нём смотреть можно

- **Форму экранов.** Таблица правила с объявленным ключом, предпросмотр пересчёта списком,
  карточка объекта с журналом закреплений, очередь передач — раскладка попала в постановку
  почти без правок.
- **Справочник признаков** `FEATURES` (строки 506–516): девять признаков с пометкой `inTZ`, из
  них восемь совпали с принятой моделью. Значения демонстрационные.
- **Журнал назначений как append-only** (`ASSIGN`, строки 555+): записи закрываются `to`, а не
  переписываются — это ровно то, что закрепил
  [ADR-0116](../../docs/adr/0116-kuratorstvo-izmerenie-a-ne-pole.md).
- **Демо-пробел в правиле** (строка 532: `'Таласская': null`) — незакрытая ячейка, отданная
  заведующей. Механизм в модели остался, только лестница стала длиннее.
- **Куратор жив после погашения** (`a6`, строка 564): закрепление не закрывается судьбой
  объекта. Принято без изменений.

## Что читать как логику нельзя — пять расхождений

1. **Верхней ступени нет.** `POLICIES` (строки 528–543) ключует пару «подразделение × роль»
   сразу на работника, и это правильная **нижняя** ступень: владеет ею заведующий отделом
   (строка 63), и в модель она переезжает как есть. Не хватает того, что прототип объявил
   ненастраиваемым (шапка, §3): подразделение приходит полем `LOANS.unit` (строки 580–585) —
   ни правила, ни рождения, ни даты вступления. В модели ступени две: верхняя
   (`(объект, роль) → подразделение`) принадлежит администратору кураторства и **родильная**,
   нижняя (`(объект, роль) → работник`) принадлежит заведующему отделом и живая
   ([ADR-0117](../../docs/adr/0117-dve-stupeni-pravila-i-folbek.md)). Роль в ключе прототип, к
   слову, угадал — она осталась на обеих ступенях: у предмета залога куратор залога и
   региональный исполнитель раздаются разными правилами и приезжают в разные подразделения
   (`ozo` строка 538 против `reg_osh`/`reg_nar` строки 541–542), поэтому правил холдинга
   пять — по числу
   назначаемых ролей. Не угадал прототип только верхнюю ступень: он её не отверг, а вынес
   за скобки как ненастраиваемую.
2. **Лестницы фолбэка выше заведующего нет.** Пустая ячейка удерживается заведующим (строка
   527) — и на этом всё. В модели пустое или отсутствующее правило подразделения отдаёт объект
   администратору кураторства, а состояние «объект без куратора» невозможно в принципе.
3. **Роль на несуществующем объекте.** Запись `a8` (строка 567) закрепляет
   `collection_owner` за объектом типа `процесс` (`C-2025-017`); в словаре `ROLES` такой роли
   нет вовсе — имя подставляется заплаткой в `roleName` (строка 842). Объекта «процесс
   взыскания» больше не существует: процедура упразднена
   [ADR-0023](../../docs/adr/0023-vedenie-trebovaniya-zhurnal-peredach.md), закрепление по
   взысканию живёт на **требовании**, и требований у кредита может быть несколько.
4. **Пересчёт молча сметает ручные назначения.** `computeRecalcDemo` (строка 708) раскладывает
   объекты на «тихо», «через рукопожатие» и «заблокировано конфликтом»; четвёртой корзины —
   «сделано рукой, не трогаем» — нет, и переопределение возвращается к правилу без следа. В
   модели корзин четыре, и ручное закрепление пересчёт не трогает. Хозяина операции прототип
   назвал верно — «инициирует заведующий, отдельным действием» (шапка, §7); модель добавляет
   к этому, что у верхней ступени пересчёта нет вовсе, потому что она родильная.
5. **Конфликт интересов — фильтр внутри пересчёта.** `COI` + `coiActive` (строки 547–551)
   работают как проверка при раздаче. В модели это своё датированное измерение по паре
   «работник × заёмщик» со своим реестром: закрепление живёт, меняется только действующий
   исполнитель, и отрезок вычитается из периода ответственности
   ([ADR-0118](../../docs/adr/0118-otstranenie-otdelnoe-izmerenie.md)).

## Мелочи, которые тоже устарели

- Комментарий к `COI` (строка 548) ссылается на пункт Порядка, которого в действующей редакции
  №41 от 06.07.2026 больше нет: глава о конфликте интересов оставила только декларации.
  Механизм нормой не описан и принят проектным решением 13.08.2026: отстранение вводит
  администратор кураторства по служебной записке, инициировать может любой (включая самоотвод),
  дата окончания открытая ([ADR-0118](../../docs/adr/0118-otstranenie-otdelnoe-izmerenie.md)).
- Рукопожатие в прототипе включается **статусом объекта**: `computeRecalcDemo` шлёт через
  handoff кредиты со статусом «взыскание» (строка 723). В модели критерий другой — незакрытая
  работа на объекте (горящий срок, открытое задание, незавершённая мера, нерешённая заявка,
  неоконченная проверка залога), и читается она живьём из четырёх модулей.
- Ролей в `ROLES` четыре; в модели их пять назначаемых (добавлен куратор требования) плюс одна
  производная — ведущий куратор заёмщика, который не назначается, а вычисляется.
- Сайдбар: прототип вешает «Кураторство» корнем дерева (строка 352). В каноне модуль стоит
  **подгруппой в «Системе кредитования»**, следом за «Кредиты и транши» (решено 13.08.2026).
  Имя первого листа прототипа — «Политика распределения» — принято как есть.
- Заявка в прототипе есть как объект роли `spec_app`, но перехода «заявка → кредит» он не
  показывает. В модели преемства нет: кредит получает куратора своим правилом при подписании
  договора.

## Как это кончилось

Решение «переписывать, а не править» исполнено **16.08.2026**: расхождения 1 и 5 задевали саму
раскладку данных (вторая ступень правила и отдельное измерение отстранения), и от `POLICIES`,
`COI` и `computeRecalcDemo` не осталось ничего. Волна 1 нового макета закрывает этапы 1–2
постановки (`P18-R1`…`R8`): каркас, журнал закреплений, обе ступени, фолбэк-лестница с
порогами, редакции с датой вступления, шов `curatorOn` с двумя ответами; рабочий экран один —
«Политика распределения», остальные четыре стоят заглушками. **Волна 2 тем же днём** закрыла
этап 3 (`P18-R9`…`R14`): рождение со снимком признаков, пересчёт четырьмя корзинами, очередь
передач с приёмом, закрепление рукой и ретро-правка с запертым периодом — то есть расхождения
4 и 6 воспроизведены уже исполняемо, а расхождение 5 закрыто в расчёте (реестр отстранений
остаётся этапу 4). Заглушек осталось две. Порядок дальнейших работ и
приёмка — по этапам постановки `docs/tasks/p18-kuratorstvo-tasks.html`, журнал волн — в
[`ASUBK-status-razrabotki.md`](ASUBK-status-razrabotki.md).
