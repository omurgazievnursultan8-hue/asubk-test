# АСУБК — логика модуля отчётности (спецификация)

> **Статус: v1, 23.08.2026.** Standalone-спека модуля «Отчётность».
> Формат — по образцу `ASUBK-statistika-logika.md` и `ASUBK-analiz-logika.md`.
>
> **История.** Модуль выведен разбором 23.08.2026 (39 вопросов, начиная с «что вообще такое
> отчётность»). Разбор вскрыл четыре вещи, которых в тексте ТЗ не видно: отчёт определяется
> **объявленностью состава**, а не тем, что его распечатали; **выпуск с названным получателем**
> — вторая и последняя законная заморозка чисел, и через неё же исполняется `ADR-0153`;
> режиму «сейчас» **не хватало шва** — три шва `ADR-0152` обслуживают только «на дату»;
> ТЗ описывает **два модуля** (5.1 отчётность и 5.2 шаблоны документов) двумя дословно
> совпадающими рецептурами конструктора — значит, модель одна, а разделов интерфейса два.
>
> **Нормативная база.** **Макета** конкретной формы норма не задаёт: и Порядок №41, и ПКМ
> №554 говорят об отчётности через обязанность предоставить сведения, а не через сетку листа.
> Норма приходит в модуль **основанием шаблона** (ссылка на пункт и редакцию) и **сроком**
> (расписание сдачи). **Перечень** документов при этом она называет вполне определённо —
> одиннадцать обязательных бланков со сроками и адресатами; они сведены в
> [`ASUBK-otchetnost-katalog.md`](ASUBK-otchetnost-katalog.md) (`ФО-01`…`ФО-41`) вместе с
> формами, которых требует спрос, а не норма. Канон описывает механику, каталог — предмет:
> какие формы должны существовать в реестре шаблонов на приёмке.
>
> Источник функциональных требований — **ТЗ АСУБК-3**, подсистема
> предоставления информации: 5.1 модуль отчетности, 5.2 модуль шаблонов (формирование
> документов), 5.3 модуль дашбордов; «формирование отчетности по ранее подготовленным
> шаблонам», «по параметрам отчета», «экспорт в xls и pdf», «настройка конструктора»,
> «создание списка объектов по результатам формирования отчетности для дальнейшего
> использования пользователями». Копия ТЗ — `requirements/tz/original/TZ-ASUBK-3.md`.
>
> **Сверка с легаси, 24.08.2026.** Отчётность старой системы снята целиком
> (`requirements/legacy/10-reports.md`): 928 параметров содержания, 165 фильтрации, 276 вывода,
> 70 видов групп, шесть уровней группировки, два реальных прогона. Сверка добавила в модель
> девять пунктов — `ОЧ-25`…`ОЧ-33` в `ASUBK-status-razrabotki.md`: итоги уровней разреза,
> макет выгрузки, язык операторов отбора, список-правило против списка-состава, выпуск фоновым
> заданием, разрядность денег, порядок полос, перечень форматов, вторая дата (§19 п. 6).
>
> Решения модуля — `docs/adr/0156`…`docs/adr/0161`. Смежные, на которые модуль опирается и
> которые не отменяет: `ADR-0001` (производные не хранятся; исключение — §5), `ADR-0089` и
> `ADR-0112` (числа за закрытый период законно меняются), `ADR-0123`/`ADR-0148` (учётный период
> и строка на дату), `ADR-0125` §2 (черновик → публикация), `ADR-0145`…`ADR-0152` (слои,
> реестр показателей, швы, паспорт ответа), `ADR-0153`…`ADR-0155` (анализ). Глоссарий —
> `CONTEXT.md`, раздел «Отчётность».

## 1. Назначение и границы

Модуль **объявляет и печатает** — пятый глагол в разделении `ADR-0145`: ядро выводит,
классификация присваивает, статистика пересчитывает и датирует, анализ объясняет, задачи
поручают.

**Делает:**

- держит **шаблоны отчётности** — объявленный состав вопроса вместе со способом его напечатать;
- **предъявляет** ответ экраном, файлом или бумагой и отвечает за предъявленное;
- ведёт **журнал выпусков** — что, кем, когда и кому предъявлено;
- держит **календарь сдачи** и состояние обязательств;
- держит **рабочие списки** — замороженные составы объектов как единицы работы;
- собирает **дашборды** из объявленных отчётов.

**Не делает:**

- **не считает.** Показатели — реестр статистики, поля — публичный перечень объекта; своей
  формулы нет ни одной (`ADR-0150`);
- **не поручает.** Задачу по просроченному обязательству ставит человек (`ADR-0160`);
- **не пишет в чужие карточки.** В карточке объекта его выпуски видны зеркалом;
- **не ходит в базу.** Четыре шва — единственный вход в данные (`ADR-0158`);
- **не ведёт документооборот.** Модуля документооборота нет (`ADR-0149` §4); файл принадлежит
  выпуску.

## 2. Модель: восемь сущностей

### 2.1 Шаблон отчётности

Род. Объявленный состав вопроса и способ его напечатать. Живёт **редакциями**. Сам ничего не
считает. Шаблоном становится **публикацией** (`ADR-0156` §2); до неё собранный состав — личный
черновик.

Вне редакции живут: круг подразделений · расписание сдачи · «пригоден для обзора» · «полный
охват обязателен» · серия исходящих. Они меняются, не порождая новой редакции, потому что не
меняют вопроса.

**Типа шаблона «информационный / отчётный» не бывает.** Сдаваемость — это **расписание сдачи**:
включено — форма порождает обязательства, выключено — её просто смотрят. Один и тот же вопрос
обычно и сдают по сроку, и весь месяц смотрят своими разрезами; двумя типами он заводился бы
дважды, и две настройки одного вопроса разошлись бы. Статичность сдаваемого держится не типом,
а вторым замком: окончательно выпускается только состав по умолчанию (§5, `ОЧ-54`).

### 2.2 Отчёт и бланк — два вида

| | **отчёт** | **бланк** |
|---|---|---|
| о чём | о множестве объектов | об одном объекте |
| адресат | круг подразделений | названное лицо |
| форма | таблица, разрезы, порядок | макет docx с подстановками |
| состояние | «сейчас» или «на дату» — объявляет редакция | **то же**: уведомление — «сейчас», акт сверки — на 1 января (`ADR-0165` §5) |
| форматы | xlsx · csv · pdf | docx (черновик) · pdf |
| юр. значимость | у сданного выпуска | всегда: реквизиты, место подписи |
| номер исходящего | нет | серия шаблона |

### 2.3 Редакция

Версионирует **вопрос и форму**: основной объект, показатели и реквизиты, разрезы, параметры,
порядок, режим времени, макет и подстановки. Проходит черновик → публикацию с основанием,
автором и датой (`ADR-0125` §2). Опубликованная неизменна.

### 2.4 Выпуск

Предъявление, за которое система отвечает: редакция + значения параметров + дата состояния +
паспорт. Просмотр на экране выпуском не является.

### 2.5 Обязательство

Ожидаемый окончательный выпуск к сроку: за кем, что, к какому числу. Состояния: ожидается ·
сдано · сдано с опозданием · просрочено.

### 2.6 Рабочий список

Замороженный состав объектов, отобранный выпуском и названный. Единица работы, а не запрос.

### 2.7 Дашборд

Раскладка плиток. Мебель: своей даты и своего паспорта не имеет.

### 2.8 Вид

Сохранённая под именем накрутка показа: «как я привык смотреть этот отчёт». Личный — передаётся
не вид, а выпуск. Публикации не требует и черновиком не является: живёт поверх чужой
опубликованной редакции. Привязан к **шаблону**, при каждом показе сверяется с границами
действующей редакции; ставшая недопустимой часть отваливается с пометкой. Чисел не хранит
(`ADR-0162`).

## 3. Что объявляет редакция

1. **Основной объект** — всякий, кто объявил публичный перечень полей. Отдельного реестра
   объектов отчётности нет (`ADR-0156` §6). Открытый список ТЗ («кредит, залог, погашение,
   сотрудник, задание, документ и т.д.») закрывается сам собой: появится модуль — объявит
   поля — станет доступен. **В v1 объявленный владелец ровно один:** ядро отвечает швом
   `objectRows` по объектам ядра, и в макете это кредит (`ADR-0181` §4). Отсюда `ИО-30`:
   режим «сейчас» в v1 доступен только формам с основным объектом «кредит», остальные живут
   режимом «на дату», где строки даёт статистика снимком. Формы, у которых нет ни того ни
   другого владельца, в каталоге **помечены**, а не выброшены (столбец «данные»).
2. **Показатели** — из реестра статистики. Показателя вне реестра не бывает (`ADR-0150`).
   Здесь же объявлены **разрядность и правило округления**: величина уходит в файл уже
   округлённой. Иначе сданное и пересчитанное расходятся в копейках, и метка расхождения (§6)
   начинает срабатывать на шуме — легаси печатает в лист `378917795.43852067` (`ОЧ-32`).
3. **Реквизиты** — поля из публичного перечня основного объекта.
4. **Разрезы, порядок вывода и итоги уровней.** Разрез объявляет и то, печатается ли по нему
   итоговая строка. Итог **не складывается модулем**: доля и средняя не аддитивны, поэтому
   число уровня **приходит узлом дерева**. Шов `statSlice` отвечает не одним значением, а
   деревом объявленных уровней; значение каждого узла статистика считает по строкам этого
   узла, а не из детей (§11, `ОЧ-25`, `ОЧ-34`, `ИС-24`, `ADR-0176` §6). Отдельного вопроса
   рядом со строками больше нет: уровни и их числа — один ответ одного шва.
5. **Параметры** — с ролью: параметр **вопроса** (входит в смысл: период, валюта) или параметр
   **отбора** (сужает состав: подразделение, куратор). Значения даёт выпуск, не шаблон.
   У параметра отбора объявлен **оператор сравнения** из перечня, положенного типу величины
   (число · дата · текст · ссылка на словарь). Перечень объявлен на систему, а не набирается
   в каждом шаблоне заново — в легаси он набран руками и разъехался: 15 операторов и 33 фильтра
   с именами колонок БД в поле «имя поля» (`ОЧ-29`).
6. **Режим времени** — «на дату» или «сейчас». При выпуске не переключается: это часть
   объявленного вопроса, а не пожелание спросившего. Дата состояния у ответа **одна**: своей
   даты у отдельной колонки или ячейки не бывает, иначе паспорт (§5) перестаёт описывать ответ.
   Сравнение с другой датой решается второй датой **на весь отчёт** — §19 п. 6 (`ОЧ-27`).
7. **Макет и подстановки** [бланк] — подстановка только из публичного перечня или реестра.
8. **Макет выгрузки** — печатное представление ответа: ориентация и поля листа, повторение
   шапки на каждой странице, колонтитул с паспортом, ширины колонок, **порядок полос**
   (заголовок · шапка · уровни разрезов · итог · подвал) и **перечень форматов**. Принадлежит
   редакции, а не экрану и не браузеру спросившего: сданное печатается одинаково у всех, а «как
   было напечатано» — часть того, за что модуль отвечает (`ОЧ-26`, `ОЧ-31`, `ОЧ-33`).

9. **Допустимые отклонения** — второй список рядом с составом по умолчанию: какие разрезы
   разрешено включать и переставлять, какие показатели скрывать и добавлять, разрешено ли
   падать до строк объектов. Перечень **разрешённого**, а не признак «жёсткий/свободный» на
   весь отчёт и не флаг на каждой ячейке; берётся из того же публичного перечня и реестра
   показателей, поэтому крутилка не выводит за область видимости физически (`ADR-0158` §2).
   Что в строках, а что в колонках, — **раскладка**, объявления не требует вовсе: клетки те
   же. Падение до строк объектов требует, потому что это другой шов, другой объём и другая
   видимость полей (`ADR-0162`).

Порядок полос **объявлен**, а не выведен из положения ячеек, и итог по умолчанию печатается
**после** уровней. В легаси он нигде не объявлен и собирается из `position` каждой из 928
ячеек — оттуда лист, где `ИТОГО` стоит перед группами. Перечень форматов тоже объявлен, а не
зашит: легаси отдаёт единственный двоичный `.xls` и потому не может ни отдать выгрузку наружу,
ни подписать её (`requirements/legacy/10-reports.md` §12).

## 4. Публикация: где ловятся ошибки

Всё проверяемое заранее проверяется **на публикации редакции**, а не на выпуске (`ADR-0159` §4).
К моменту выпуска за экраном стоит человек с задачей, а не с настройкой.

| проверка | отказ |
|---|---|
| подстановка не из публичного перечня | «в макете {{остаток_осн}}, объект „кредит“ такого поля не объявляет» |
| показатель выведен из реестра | «показатель X выведен 12.08, шаблону нужна новая редакция» |
| показатель неисчислим налету при режиме «сейчас» | «показатель „средняя просрочка“ налету не берётся» |
| нормативное основание в личном черновике | «сослаться на пункт нормы может только публикующий» |
| поле ядра переименовано или снято | ломает публикацию и называет виновника |
| допустимое отклонение, которого не может быть (§3 п. 9) | «показателя X в реестре нет» · «показатель Y выведен 12.08: разрешать экрану выведенное нечего» · «показатель Z налету не берётся — разрешить его экрану значит пообещать невыполнимое» |

Проверок **шесть**: последняя добавлена вместе с перечнем допустимых отклонений (`ADR-0162`
§1). Перечень отклонений — подмножество доступного, а не список пожеланий: обещать экрану
то, чего реестр не даст, дешевле всего запретить там же, где объявляется состав.

Что проверить заранее нельзя — усечённый видимостью охват и подставленная соседняя дата
(`ADR-0152` §5) — делает выпуск **предварительным**: он не запрещён, но обязательства не
закрывает и снимка не даёт.

## 5. Выпуск: две ступени

**Предварительный.** Обязательства не закрывает, чисел не морозит, номера не тратит. Форматы
любые из положенных виду. Файл живёт объявленный срок (в v1 — 90 дней, единый на систему),
после чего удаляется; строка журнала остаётся.

**Окончательный с названным получателем.** Морозит числа и файл, наружу уходит **только pdf**,
тратит номер серии, попадает под метку расхождения. Это второе и последнее исключение из
`ADR-0001` (`ADR-0157` §2).

**Получатель — ссылка** (`ADR-0157` §3): субъект ядра [бланк] · адресат сдачи из справочника
[отчёт] · сам обзор [анализ].

**Паспорт** печатается колонтитулом и неудаляем: дата состояния · признак фиксации · область
видимости · редакция шаблона · значения параметров · **показанный состав** (разрезы в порядке
уровней и показатели — как было показано, целиком, а не дельтой от умолчания).

**Выпуск — заказ, а не нажатие.** Ответ по портфелю в десятки тысяч объектов считается минутами:
на легаси-стенде 90–107 с при 51 268 субъектах, синхронно, без индикатора и без отмены
(`requirements/legacy/10-reports.md` §12). Поэтому выпуск исполняется **фоновым заданием** с
состояниями: заказан → считается → готов → ошибка. Паспорт заводится **в момент заказа** — дата
состояния, редакция, значения параметров и область видимости известны уже тогда, — а не в момент
готовности. Строка журнала появляется вместе с заказом, поэтому неудачный выпуск виден
неудачным, а не отсутствующим (`ОЧ-30`).

**Показ — не выпуск, но и не пустое место.** Ответ на экране считается по редакции и значениям
параметров, снимка не морозит, номера не тратит, следа в журнале не оставляет. При этом рядом
с числами стоит **шапка показа**: отчёт, редакция, значения параметров, область видимости,
дата состояния, имя вида и явная метка «не выпуск». Причина та же, что у `ИО-6`: скриншот
уходит в переписку и обязан отвечать за себя сам.

**Накрученный показ выпускается, но не окончательным.** Состав, собранный на экране в границах
редакции (§3 п. 9), можно выпустить предварительно — паспорт запишет показанный состав целиком.
Окончательный выпуск с названным получателем идёт **только составом по умолчанию**:
обязательство берётся на объявленной форме, а не на той, что человек сложил себе под задачу
(`ADR-0162` §6). Выпуск по расписанию — всегда состав по умолчанию. Метки расхождения (§6) у
накрученного выпуска не бывает по устройству: метится то, где есть снимок, а снимок морозит
названный получатель (`ИО-4`). Накрученное нельзя сдать — в этом и цена свободы.

## 6. Расхождение со сданным

Открыли период (`ADR-0089`) или поправили запись (`ADR-0112`) — выпуски со снимком за этот
период получают метку «числа изменились после сдачи» и предложение корректирующего выпуска.

- Метка **не отзывает** выпуск: сданное остаётся сданным, но перестаёт молча считаться верным.
- Корректирующий выпуск уточняет прежний; прежний не гасится, связь видна с обеих сторон.
- Метится только то, где есть снимок.
- Обратный ход: «что мы уже отдали наружу по затронутому периоду и кому» — поимённо.

## 7. Календарь и обязательства

Расписание объявлено на шаблоне вне редакции. Из него порождаются обязательства.

Закрывает обязательство **только окончательный выпуск с получателем** — «сдано» руками не
проставляется (`ADR-0160` §2). Отчётность **показывает, но не поручает**: задачу ставит
человек, задача несёт ссылку на обязательство как на основание. Исполнитель назван у
обязательства и из круга подразделений не выводится: круг — доступ, а не ответственность.

**И здесь у v1 тупик, названный вслух.** Задача, на которую ссылается абзац выше, ставится в
**подсистеме управления заданиями** — она объявлена ТЗ (раздел «подсистема управления
заданиями», там же раздел расчётных показателей по заданиям), но в рабочей области её **никто
не прорабатывал**: своего канона у неё нет, ветки нет, и ни статистика, ни кураторство её на
себя не берут. Пока это так, **продолжения у просроченного обязательства внутри системы
нет**: модуль показывает просрочку в календаре сдачи и на дашборде, а задача ставится **вне
системы**. Свою мини-задачу отчётность не заводит нарочно — это чужая сущность, и заведённая
здесь она разъедется с настоящей, когда та появится (`ADR-0160` §2 не меняется). Строка долга
занесена в требования к соседям и в очередь модулей; блокирующей она не является — календарь,
обязательство и метка просрочки работают без неё.

**Полнота бывает двух родов, и род объявляется на шаблоне** (`ADR-0165`). `fullCoverage`
отвечает «полон ли ответ», `coverBy` — «по чему полнота считается». У отчёта усечение области
видимости делает выпуск предварительным (§13.4). У бланка с `coverBy: 'объект'` — иначе:
норма требует акт сверки **со всеми действующими заёмщиками**, и обязательство закрывает не
бумага, а **все бумаги**. Пока не по каждому объекту — обязательство несёт поимённый остаток:
кого не досверили. Знаменатель берётся **из снимка на дату состояния**, а не из живого
перечня: кредит, закрытый после 1 января, из живого выпал бы — а акт с ним подписывать
обязаны; выданный в июне в живом стоит — а сверяться с ним на 1 января не о чем (`ИО-27`).

**Знаменатель нормы и знаменатель охвата — разные величины, и это говорится вслух.** Пункт 9
считает **лиц** («со всеми действующими заёмщиками»), охват считает **договоры**, потому что
акт печатается по договору. У одного заёмщика договоров бывает несколько — тогда договоров
больше, чем лиц. Счёт по договорам оставлен: он строже и раньше нормы не закроется. Но в
клетке охвата печатаются обе величины («2 из 3 договоров · лиц: 0 из 2»), а вопрос «акт на
лицо или на договор» задан заказчику (`ОЧ-61`, `ADR-0167` §4).

**Отметки «подписан» нет — по той же причине, что и отметки «сдано».** Подпись заёмщика —
событие контрагента, а не выпуск; модуль меряет обязательство тем, что производит сам. Ход
подписания ведёт сопровождение на объекте, и нужен он там классификации: уклонение свыше
60 дней — фактор среднего кредитного риска (Порядок №41, п. 11 п.п. 2). Открытый вопрос
заказчику остаётся (`ОЧ-59`, §19 п. 9).

**Расписание календарное, и только календарное.** Правило — `freq`, `dueDay`, `dueMonth` (у годового),
`since` (§19 п. 2): «ежемесячно до 15-го», «ежегодно до 1 сентября». Срок, отсчитываемый **от события
объекта** — «десять рабочих дней со дня неисполнения требований первичной претензии», «за тридцать дней
до платежа по графику», — расписанием не выражается и в календарь сдачи не попадает. Такой
срок принадлежит модулю, которому принадлежит событие: взысканию, сопровождению. Отчётность
даёт бланку макет, номер и выпуск, а факт предъявления отдаёт швом `reportIssues` (§12) — по
нему сосед и закрывает свою фазу (`ADR-0164`).

Различается при этом **не вид формы, а природа срока**: акт сверки — бланк, но срок у него
годовой и календарный, и обязательство он порождает на общих основаниях. Календарь сдачи
честно неполон: он отвечает «что за подразделением к сроку **по расписанию**», а не «какие
сроки на нас висят».

**Одной календарности мало** (`ADR-0166` §2, сверка с каноном взыскания 26.08.2026). У соседа
нашёлся календарный срок, который остаётся **его**: «вынести просроченную задолженность на
комитет не позднее 15-го числа» (п. 12). Форма `ФО-01` к этому заседанию — материал, а норма
требует **вынести вопрос**, и меряется тем, состоялось ли вынесение. У акта сверки наоборот:
требуемое действие и **есть** выпуск документа заёмщику. Значит разграничивает то, **что
меряется — выпуск формы или действие процесса**; календарность срока лишь необходимое
условие. На один и тот же п. 12 к 15-му числу поэтому висят два обязательства в двух модулях —
форма сдана и вопрос вынесен, — и это цепь, а не дубль.

## 8. Бланки: макет, серия, номер

- Макет верстается в docx; подстановки — только объявленные, проверка на публикации.
- Подстановки **не ведутся отдельным списком**: они *есть* текст макета, а список выводится
  разбором `{{…}}` (уточнено макетом 24.08.2026). Список рядом с текстом со временем разошёлся
  бы с ним — и проверка на публикации проверяла бы не то, что напечатается.
- **Показателя в бланке не бывает:** показатель — величина о множестве объектов, а бланк
  говорит об одном (§2.2). Такой вопрос задаётся отчётом.
- **Бланк бывает «на дату»** и тогда читает шов статистики, а не ядра: акт сверки говорит о
  состоянии на 1 января, и взять его из живого ядра значило бы напечатать сегодняшние числа
  под чужой датой. Дата состояния у такого бланка — параметр вопроса, без неё печатать нечего;
  подставленный соседний снимок валит выпуск в предварительный — бумага с чужой датой
  состояния на подпись не уходит (`ADR-0165` §5–§6).
- **Получатель бланка — ссылка, а не строка.** В выпуске стоит ссылка на того, кому бумага
  ушла, а не имя, скопированное из поля объекта. Объект — **дорога** к получателю, а не сам
  получатель, и дорога эта названа при любом виде: служебная записка тоже уходит по конкретному
  кредиту (`ADR-0167` §1, `ADR-0168` §2).
- **Получатель объявляется парой «вид + значение», и видов четыре:** лицо **в роли** (заёмщик ·
  поручитель · залогодатель), **подразделение**, **подразделение своего объекта** (то, которое
  ведёт этот кредит), **должность**. Вид объявляет редакция — это свойство формы, а не выбор
  человека на выпуске; значение приходит от соседа, и **ни одного из перечней отчётность не
  ведёт** (`ИО-28`, `ИО-29`).
- **Отказ называется, пустая строка в бумагу не попадает.** Публикация отклоняется, если вид не
  объявлен или такого вида не бывает, а также если значения нет у соседа («подразделения такого
  в оргструктуре нет»). Выпуск отклоняется, если у объекта нет лица в объявленной роли или
  подразделение объекта не названо.
- **Записка внутрь организации — такой же выпуск.** Служебная записка ДПО или Председателю
  Правления проходит ступени, тратит номер серии и морозит числа снимком (`ADR-0157` §1). Одно
  отличие: ключом «лицо» она не отбирается — этому лицу она не предъявлялась (`ADR-0168` §4).
- **Должность — не человек.** Записка адресована Председателю Правления как должности; кто её
  занимал в день выпуска — вопрос оргструктуры, отчётность имени не хранит (`ADR-0168` §3).
- **Подстановка — не получатель.** В теле требования поручителю печатается `{{borrower}}` —
  о ком речь; уходит бумага поручителю — кому речь. Совпадают они только у части форм.
- Серия исходящих объявлена на шаблоне; у уведомлений своя, у справок своя.
- Номер выдаётся **вместе со снимком**: шесть перепечаток черновика не сжигают шесть номеров.
- Серия бывает **ручной**: если журнал ведёт канцелярия, система номер не выдаёт, а ловит дубль.
- docx — формат черновика; другой текст насовсем делается правкой макета и новой редакцией.

## 9. Рабочие списки

Отбирается выпуском, называется, замораживается. Чисел не хранит — только объекты; их величины
всегда текущие.

- Передаётся подразделению: тогда его видят все, кто в круге.
- Выбывший объект показан **выбывшим** («кредит закрыт 14.08»), а не исчезает: иначе
  «обзвонили всех» перестаёт быть проверяемым.
- Скрытое видимостью названо числом: «3 объекта скрыты областью видимости».
- Законный параметр отбора для следующего выпуска. Пятого шва это не требует: перечень
  объектов уходит в **разрез**, и строки спрашиваются тем же `objectRows`.
- Кончается **закрытием** («работа окончена»), не стиранием. Закрытый читается, не пополняется.
- Сам никому ничего не поручает.

**Список-состав ≠ список-правило.** Рабочий список — замороженный перечень объектов. Правило
отбора, которое хочется переиспользовать в вопросе («по Баткенской области», «с остатком больше
миллиона»), живёт **параметром отбора редакции**, а не рабочим списком: правило пересчитывается
на каждом выпуске, состав по определению не пересчитывается никогда. Смешать их — потерять
`ИО-14`: в легаси «список объектов» есть правило, и в одном реестре на 201 запись лежат рядом
территориальная сетка и «татьяна и дети» (`ОЧ-28`).

## 10. Дашборды

- Плитка = способ показать **объявленный отчёт**; своего состава не имеет, клик раскрывает
  отчёт целиком. Внешние источники запрещены.
- **Паспорт у каждой плитки**, не у экрана: на одном дашборде рядом стоят «на дату» и «сейчас».
  Общий паспорт сверху создавал бы вид единой даты, которого нет.
- Раскладка **объявлена на роль** (чтобы первый день не начинался с пустого экрана), поверх
  живёт личная перекладка со «вернуть как объявлено».
- Личная раскладка — не личный отчёт: добавить можно только доступное.
- Личная перекладка хранится **отдельно** от объявленной на роль, а не поверх неё правкой:
  иначе «вернуть как объявлено» нечему было бы возвращать (уточнено макетом 24.08.2026).

## 11. Что модуль берёт у соседей — четыре шва

| шов | владелец | вопрос |
|---|---|---|
| `statSlice` | статистика | значение показателя в разрезе на дату; ответ **всегда дерево уровней** |
| `statSeries` | статистика | ряд значений по датам |
| `statRows` | статистика | строки объектов на дату: **полный список или отказ** |
| `objectRows` | **ядро** | перечисление живых объектов: поля + исчислимые налету показатели; **в v1 — по объектам ядра** |

Своих запросов к базе у отчётности нет вовсе (`ADR-0158` §2 — его «Последствия» читаются по
§4 и §5, а не по заголовку). Отсюда: обойти область видимости она не может физически,
показать необъявленное — тоже.

**Три контракта соседи выдали не такими, какими мы их просили, и переписана наша сторона, а не
их** (сверка 29.08.2026, волна 13).

1. **`statSlice` отвечает деревом, а не пакетом.** Мы просили принимать список значений
   разреза и отвечать таблицей; статистика сделала один вопрос с объявленными уровнями и
   ответом-деревом, где плоский разрез вырождается в один уровень (`ADR-0176` §6). Это
   **сильнее** нашей просьбы: пакет пришлось бы задавать по каждому уровню отдельно, а два
   уровня разреза — это уже два пакета и ручная сшивка. Число уровня приходит **узлом** и
   посчитано статистикой по строкам этого узла (`ИС-24`). Складывать детей по-прежнему нельзя —
   доля и средняя не аддитивны (`ИО-17`, `ОЧ-25`); проверено макетом (`RP.foldLevelsSelf`
   остался **сторожем**: доли по трём подразделениям дают в сумме 83,3 % против 25 % на всём
   охвате). «Количество уникальных» из этого довода убрано: такого показателя реестр больше не
   держит (`ADR-0186`, `ИС-30`). Двух форм шва нет — вход остался четырёхшовным.
2. **`statRows` отвечает бинарно: полный список или отказ** (`ADR-0178`). Порога «отдам
   сколько влезет» нет; выше порога статистика **отказывает и предлагает асинхронную
   выгрузку**. Выгрузку мы себе не берём: она принадлежит владельцу строк.
3. **`objectRows` принят, но сужен**: в v1 ядро отвечает по своим объектам, и в макете это
   кредит (`ADR-0181` §4). Следствие — `ИО-30` и столбец «данные» в каталоге.

**Два фоновых механизма — не дубль, и вот граница.** Асинхронная выгрузка статистики (п. 2)
отдаёт **строки**: вопрос чужой, ответ чужой, судьба файла — у владельца строк. Фоновый
**выпуск** отчётности (§5) отдаёт **выпуск**: номер серии, паспорт, замороженные числа, строка
журнала, получатель, обязательство. Совпадает у них ровно одно — что ждать приходится не на
кнопке. Слить их значило бы либо отдать чужие строки под нашим номером серии (выпуск без
паспорта чисел), либо завести у себя очередь на чужие данные. Поэтому: выше порога `statRows`
отказ **проговаривается на экране** как отказ соседа со ссылкой на его выгрузку, а не
превращается молча в наш фоновый выпуск.

**Углубление до строк объектов — это переход на `statRows`**, а не подробность показа. Поэтому
оно объявляется редакцией отдельно (§3 п. 9) и разрешено не везде: у него другой объём ответа
и другая видимость полей объекта. Раскладка (строки↔колонки) шва не меняет вовсе.

## 12. Швы наружу

| шов | вопрос | отдаёт |
|---|---|---|
| `reportIssues` | что предъявлялось по этому **объекту** или этому **лицу** | список выпусков: вид, дата, получатель со ссылкой и ролью, номер, договор |
| `reportObligations` | что за подразделением к сроку | обязательства и их состояния |
| `reportTemplate` | пригоден ли шаблон для обзора, какая редакция текущая | признак, редакция, основание |

Первый читает карточка заёмщика (зеркалом), второй — модуль задач и руководитель, третий —
анализ.

**У `reportIssues` два ключа, а не два шва.** По объекту спрашивает карточка — «что уходило по
этому договору». По лицу спрашивают классификация и взыскание — «что предъявлялось этому
человеку»: у заёмщика договоров несколько, а уклоняется от подписания акта он один (п. 11
п.п. 2). Отвечает в обоих случаях тот же выпуск теми же полями, поэтому ключ второй, а пятого
шва не заводится (`ADR-0167` §5).

## 13. Видимость

1. **Шаблон** виден тем, кто в круге **подразделений**. Поимённого доступа нет — ни списком
   людей вместо круга, ни исключениями сверх него, ни по должности: привязка к человеку ставит
   сдачу на отпуске (п. 6), а кадровое движение заставляет править каждый шаблон. Точность
   даёт не список людей, а область видимости (п. 2): двое из одного круга открывают один отчёт
   и видят разные строки (`ОЧ-53`).
2. **Строки ответа** — по области видимости `ADR-0152` §3; агрегат считается только по видимым.
3. **Бланк** печатает тот, у кого шаблон в круге **и** объект видим — два замка, оба нужны.
4. **Усечение произносится вслух.** На шаблоне с признаком «полный охват обязателен» неполный
   охват делает выпуск предварительным и запрещает называть получателя.
5. **Выпуск** виден автору и кругу шаблона; выпуск с получателем виден и тем, кто отвечает за
   обязательство.
6. **Окончательно выпускает тот, за кем обязательство**; где обязательства нет — всякий в
   круге. «За кем» бывает подразделением, а не человеком, — иначе сдача встанет на отпуске.
   Отдельного права не заводится: крутилка ничего не тратит (`ADR-0162` §9), а окончательный
   выпуск тратит номер серии и закрывает срок, и спрашивать за него надо с того, с кого и так
   спросят.
7. **Незаведённая обязательная форма видна всем** (`ADR-0163`): круга у неё нет, потому что
   нет шаблона, а её основание публично — оно в норме.

## 14. Экраны

1. **Отчётность** — витрина: формы, доступные смотрящему. Две группы («обязательные» /
   «остальные»), внутри обязательных — по сроку; вид и объект чипами; поиск по названию.
   Строка: название · о чём · основание · срок и состояние обязательства · последний
   окончательный выпуск. Недоступные по кругу — приглушённо в конце группы, с причиной и с
   тем, у кого доступ. Незаведённые обязательные — как неисполняемая норма, без кнопки
   (`ADR-0163` §5). Сверху свёрнутая группа «мои виды». Бланк ведёт не на формирование, а
   на «где печатается» — карточка объекта или рабочий список.
2. **Реестр шаблонов** — вид, основание, редакция, круг, расписание, состояние
   (опубликован · черновик · неисполним). Стол автора и уполномоченного; витрина (1) — стол
   потребителя. Списки перечисляют одно и то же, различаются отбором и адресатом.
3. **Конструктор** — состав, параметры, режим, предпросмотр; кнопка «опубликовать» с проверками §4.
4. **Показ и выпуск отчёта** — значения параметров, дата, шапка показа, крутилка в границах
   редакции, личные виды, «предварительно» / «окончательно» (окончательно — только составом
   по умолчанию). Открывается на **последнем периоде, у которого есть снимок**: период без
   снимка выбирается вручную и предупреждает в момент выбора. Режим «на дату» считается сразу,
   «сейчас» — по кнопке: живой запрос к ядру не гоняют оттого, что кто-то промахнулся мимо
   строки списка. Действий четыре — выгрузить (предварительный выпуск со строкой в журнале),
   выпустить окончательно, сохранить вид, завести рабочий список (через предварительный
   выпуск: морозить нечего, пока у ответа нет паспорта).
5. **Журнал выпусков** — фильтр по шаблону, получателю, периоду, метке расхождения.
6. **Календарь сдачи** — обязательства по срокам и состояниям. Отвечает «что горит на этой
   неделе» поперёк форм; витрина (1) отвечает «а с этой формой у меня что». Обе читают одно
   обязательство, ни одна не хранит.
7. **Бланки** — верстальщик макета, серии, печать из карточки объекта.
8. **Рабочие списки** — мои, подразделения, закрытые.
9. **Дашборд** — объявленная раскладка + личная.

В макете к этим девяти добавлены два служебных: **швы и соседи** (§11, §12, §16 — что модуль
берёт и отдаёт) и **границы макета** (чего в нём сознательно нет). В приложении они не нужны:
их содержание — этот документ.

**Меню разложено на три стола** — теми же словами, какими модуль описал заказчик (`ОЧ-52`…`ОЧ-55`):

| стол | экраны | чей |
|---|---|---|
| Смотреть и сдавать | Отчётность · Показ и выпуск · Календарь сдачи · Рабочие списки · Дашборд | всякий в круге |
| Настраивать | Реестр шаблонов · Конструктор · Верстальщик бланка | уполномоченный и делопроизводитель; личные черновики собирает здесь же всякий |
| След | Журнал выпусков | что, кем, когда и кому предъявлено |

Стол — **раскладка, а не право**. Ни один раздел меню по роли не прячется: спрятать
«Настраивать» от пользователя значило бы завести право, которого §17 не объявлял, и отнять
у него личный черновик. Журнал вынесен третьим столом потому, что он ни настройка, ни
потребление: он отвечает на вопрос «что мы отдали наружу» и живёт дольше обоих (`ИО-11`).

## 15. Инварианты — с причиной

| # | инвариант | причина |
|---|---|---|
| ИО-1 | Шаблоном состав становится публикацией, не сборкой | иначе через полгода 300 «отчётов» и слово теряет смысл (`ADR-0156`) |
| ИО-2 | Отчётность не считает: показатель только из реестра, поле только из перечня | вторая формула той же величины разойдётся с первой (`ADR-0150`) |
| ИО-3 | Своих запросов к базе нет — только четыре шва | видимость должна быть свойством системы, а не аккуратности (`ADR-0158`) |
| ИО-4 | Числа морозит не выпуск, а названный получатель | иначе морозится склад черновиков (`ADR-0157`) |
| ИО-5 | Получатель — ссылка, не строка | «Минфин» и «МФ КР» — один адресат, а в журнале два (`ADR-0157` §3) |
| ИО-6 | Ответ без паспорта не показывается и не хранится | дата, фиксация и видимость — часть смысла числа (`ADR-0152` §2) |
| ИО-7 | Проверяемое заранее валит публикацию, а не выпуск | у экрана в момент выпуска человек с задачей (`ADR-0159` §4) |
| ИО-8 | Непроверяемое заранее делает выпуск предварительным | тихая неточность хуже громкого отказа |
| ИО-9 | Номер серии тратится вместе со снимком | иначе дыры в исходящих от перепечаток (`ADR-0161` §5) |
| ИО-10 | Наружу с получателем уходит только pdf | снимок в редактируемом формате — не снимок (`ADR-0161` §2) |
| ИО-11 | Факт выпуска неудалим, файл предварительного — расходник | журнал заведён ради «что мы отдали наружу» |
| ИО-12 | Обязательство закрывает выпуск, а не отметка | сдано без выпуска = несданное с отметкой |
| ИО-13 | Отчётность задач не порождает | у поручения должен быть автор, отвечающий за нагрузку (`ADR-0160`) |
| ИО-14 | Состав рабочего списка заморожен; выбывшие видны выбывшими | иначе «обзвонили всех» непроверяемо |
| ИО-15 | Паспорт у плитки, не у дашборда | общий паспорт создаёт вид единой даты, которого нет |
| ИО-16 | Обзор ссылается на выпуск, своего снимка не заводит | два механизма заморозки разойдутся (`ADR-0157` §4) |
| ИО-17 | Число уровня разреза приходит узлом дерева `statSlice`, а не складывается модулем | доля и средняя не аддитивны — сложенный итог врал бы молча; значение узла статистика считает по строкам этого узла, а не из детей (`ОЧ-25`, `ИС-24`, `ADR-0176` §6) |
| ИО-18 | Печатное представление объявлено редакцией, а не выбрано на экране | «как было напечатано» — часть предъявленного, и у всех оно должно быть одинаковым (`ОЧ-26`) |
| ИО-19 | Дата состояния у ответа одна | дата на колонку превращает паспорт в описание неизвестно чего (`ОЧ-27`) |
| ИО-20 | Экран крутится только в границах, объявлённых редакцией | иначе у каждого свой «отчёт по субсидиям» и спор решает тот, у кого экран больше (`ADR-0162`) |
| ИО-21 | Окончательный выпуск — только составом по умолчанию | получатель сравнивает присланное с прошлым разом, а не с паспортом (`ADR-0162` §6) |
| ИО-22 | Паспорт хранит показанный состав целиком | состав перестал быть однозначным следствием редакции; дельта и флаг снимок не восстанавливают (`ADR-0162` §7) |
| ИО-23 | Форма без шаблона законна; обязательная без шаблона видна | пустой экран читается как «такой формы у нас нет» и разрешает сделать её в Word (`ADR-0163`) |
| ИО-24 | Одна форма — не более одного шаблона | иначе разрез, который некуда положить, ложится в название — 12 дублей легаси (`ОЧ-39`, `ADR-0163` §3) |
| ИО-25 | Окончательно выпускает тот, за кем обязательство | крутилка ничего не тратит, окончательный выпуск тратит номер и закрывает срок (§13.6) |
| ИО-26 | Экран открывается на периоде, у которого есть снимок | иначе по умолчанию показываются числа, которыми нельзя отчитаться, а разница «посмотрел / сдал» размывается |
| ИО-27 | Полнота у бланка меряется объектами, а знаменатель берётся из снимка на дату состояния | живой перечень молча теряет закрытые после даты и молча требует заведённые позже — обе ошибки всплывают через год (`ADR-0165`) |
| ИО-28 | Получатель бланка — лицо в объявленной редакцией роли, а не имя из поля объекта | «контрагент объекта» не адрес: у кредита их несколько, и требование поручителю ушло бы заёмщику; строка вместо ссылки рвёт связь с субъектом, по которой сосед собирает «что предъявлялось лицу» (`ADR-0167`) |
| ИО-29 | Внутренний получатель — ссылка в оргструктуру, а не строка: подразделение, подразделение своего объекта, должность | три обязательные записки уходят не лицу вовсе, и лица у них нет; строкой они разошлись бы с оргструктурой при первом переименовании, а «подразделение объекта» строкой не записывается в принципе — оно свойство кредита, не формы (`ADR-0168`) |
| ИО-30 | Режим «сейчас» в v1 доступен только формам с основным объектом «кредит» | живые строки отдаёт `objectRows`, а в v1 на него отвечает только ядро и только по своим объектам; форме без владельца строк режим «сейчас» дать нечем, и молчаливая подмена снимком выдала бы вчерашние числа за сегодняшние (`ADR-0181` §4, каталог, столбец «данные») |

## 16. Стыки с модулями

| модуль | берёт у него | отдаёт ему |
|---|---|---|
| Статистика | три шва, реестр показателей, признак исчислимости | список шаблонов, ломающихся при выводе показателя |
| Ядро (кредиты, заёмщики, залог, платежи) | `objectRows`, публичные перечни полей, **роли лиц у договора ссылкой** (заёмщик · поручитель · залогодатель) | зеркало «какие бумаги уходили» в карточке; перечень бланков, применимых к объекту этого вида и видимых смотрящему (карточка своего перечня не держит) |
| Оргструктура | **подразделения и должности ссылкой** (с признаком «ведёт кредиты» — иначе ДПО попадёт в разрезы полноты) и ответ «какое подразделение ведёт этот объект» | — |
| Анализ | — | шаблон «пригоден для обзора», выпуск как снимок основания |
| Задачи | — | обязательство как второй вид основания задачи |
| Справочники | адресаты сдачи, оргструктура | — |
| Взыскание | — | бланки: уведомление, претензия, справка о задолженности; `reportIssues` — ушёл ли бланк и когда |
| Сопровождение | — | бланки: график на подпись, акт сверки; `reportIssues` |
| Классификация | — | `reportIssues` **ключом «лицо»**: что предъявлялось заёмщику по всем его договорам — материал к фактору «уклонение от подписания акта сверки свыше 60 дней» (п. 11 п.п. 2) |

**Сроки бланков держат соседи, а не отчётность** (`ADR-0164`). Фаза взыскания несёт срок и
признак «печатается такой-то бланк»; отчётность печатает, нумерует и отдаёт факт швом. Обратно
это значит требование к соседу, а не к нам: без срока на фазе одиннадцать обязательных бланков
Порядка №41 не стережёт никто.

## 17. Роли

| роль | что делает |
|---|---|
| Пользователь | собирает личные черновики и личные виды, крутит показ в границах редакции, делает выпуски, ведёт рабочие списки |
| Уполномоченный по отчётности | публикует шаблоны, называет основание, ведёт круг и расписание |
| Делопроизводитель | верстает макеты бланков, ведёт серии, печатает |
| Руководитель подразделения | видит календарь, ставит задачи по обязательствам, правит раскладку роли |
| Главный бухгалтер | видит расхождения по открытому периоду и что ушло наружу |

## 18. Состояние

- **Модель** — принята разбором 23.08.2026, ADR `0156`–`0162`; уточнена сверкой с легаси
  24.08.2026 на девять пунктов (`ОЧ-25`…`ОЧ-33`): §3 пп. 2, 4, 5, 6, 8 · §5 (выпуск фоновым
  заданием) · §9 (список-состав ≠ список-правило) · §11 (итоги уровней — пакетным
  `statSlice`) · `ИО-17`…`ИО-19` · §19 п. 6. Новых ADR правка не потребовала — всё внутри
  `0156`…`0161`, кроме требования к шву `statSlice` (пакетная форма), которое ложится
  в `ADR-0159`. **Пакетная форма отменена волной 13:** статистика выдала шов сильнее — ответ
  деревом уровней (`ADR-0176` §6), и переписана наша сторона (§3 п. 4, §11, `ИО-17`). Разбор 24.08.2026 «как формируется отчёт» добавил **`ADR-0162`**: экран
  крутится в границах, объявленных редакцией; сохранённая накрутка — **«Вид», восьмая
  сущность**; накрученное выпускается, но никогда окончательным. Отсюда §2.8, §3 п. 9, §5
  (шапка показа и показанный состав в паспорте), §11 (углубление — другой шов), §14 п. 3,
  `ИО-20`…`ИО-22` и шестая проверка публикации.
- **Глоссарий** — `CONTEXT.md`, раздел «Отчётность».
- **Макет** — `reports.html`, волны 4–7: все девять экранов §14 работают плюс два служебных —
  витрина «Отчётность», реестр шаблонов, конструктор, выпуск, журнал, календарь, верстальщик
  макета бланка, рабочие списки, дашборд, швы и границы макета. Запертых экранов не осталось.
  Волна 4
  внесла исполняемую часть сверки с легаси: итоги по уровням пакетным `statSlice` (`ИО-17`),
  лист выгрузки в составе редакции — порядок полос, ориентация, разрядность, перечень
  форматов (`ИО-18`), выпуск фоновым заданием с состояниями `заказан → считается → готов →
  ошибка`, снимок в показанной разрядности (`ОЧ-32`), отказы по второй дате столбца (`ИО-19`)
  и по правилу вместо рабочего списка (`ИО-14`). **Волна 5** от 24.08.2026 внесла `ADR-0162`:
  границы крутилки в составе редакции, показ с шапкой вместо предпросмотра, накрутка разрезов,
  показателей и углубления внутри границ, личные виды со сверкой при показе, показанный состав
  в паспорте и запрет окончательного выпуска накрученного. **Волна 6** от 26.08.2026 внесла
  вход потребителя и `ADR-0163`: витрина §14 п. 1 первым экраном, справочник форм рядом с
  восемью сущностями, обязательная форма без шаблона строкой без кнопки (`ИО-23`), одна форма —
  не более одного шаблона с отказом, называющим первый (`ИО-24`), право окончательного выпуска
  от обязательства (`ИО-25`), умолчания показа по режиму и снимку (`ИО-26`) и четыре действия
  с показанным, из которых три — выпуск. **Волна 7** от 26.08.2026 разложила меню на три стола
  §14 — раскладка, а не право: скрытых по роли разделов нет ни одного (`ОЧ-55`). **Волна 8**
  от 26.08.2026 сверила каталог форм `ФО-01`…`ФО-41` с Порядком №41 и внесла `ADR-0164`:
  расписание сдачи календарное и только календарное, срок бланка от события объекта
  принадлежит взысканию и сопровождению (`ОЧ-56`); новыми открытыми вопросами стали язык
  бланка (`ОЧ-57`) и доказательство доставки со следом СЭД (`ОЧ-58`). Тем же днём добор с
  легаси-стенда закрыл два вопроса каталога: «ОИП» — орган исполнительного производства, форма
  оказалась **Видом** формы по взысканию, а «Расчет» и «Детальный расчет» разведены на бланк
  для иска и отчёт о том, как задолженность сложилась. **Волна 9** от 26.08.2026 завела акт
  сверки (`ФО-34`) и внесла `ADR-0165`: расписание бывает и у бланка — по природе срока, а не
  по виду формы; полнота у такого бланка меряется **объектами**, и знаменатель берётся из
  снимка на дату состояния, а не из живого перечня (`ИО-27`); бланк бывает «на дату» и тогда
  читает `statRows`, а не `objectRows`; подставленный снимок валит акт в предварительный;
  отметки «подписан» в модуле нет — подпись контрагента не выпуск (`ОЧ-59`). Смоук —
  `scripts/inspect/reports-check.mjs`, **141/141 PASS**, результат вшит в шапку макета.
  **Волна 10** от 26.08.2026 — документная, макет не менялся: перед постановкой требования
  взысканию его канон прочитан целиком, и оба прежних требования пришлось переписать
  (`ADR-0166`). Обязательство по п. 9 оказалось заведено **дважды** — у соседа строка 38
  справочника сроков, — и снимается там, а не здесь: срок принадлежит требованию, требования
  заводят только на проблемного заёмщика, а норма требует акт со всеми действующими. Правило
  `ADR-0164` §3 уточнено: календарность необходима, но не достаточна — разграничивает то, что
  меряется, выпуск формы или действие процесса (контрпример — строка 37, п. 12, вынос на
  комитет). Слово «фаза» в требовании заменено на **вид меры** и **меру**: фаза во взыскании —
  свёртка журнала (М-2) и нести не может ничего. Новый открытый вопрос — владелец показателя
  «погашено в период по листу» (`ОЧ-60`). **Волна 11** от 26.08.2026 закрыла дефект макета
  `ОЧ-Д1` и внесла `ADR-0167`: получатель бланка — **ссылка на субъект ядра**, а роль получателя
  объявляет **редакция** (`ИО-28`); заведена `ФО-32` «Требование поручителю» — форма, на которой
  это видно: в теле печатается `{{borrower}}`, а бумага уходит поручителю. У шва `reportIssues`
  появился **второй ключ — лицо** (пятого шва не заводится), знаменатель охвата разошёлся со
  знаменателем нормы и печатается обеими величинами (`ОЧ-61`). **Волна 12** от 26.08.2026
  внесла **пять последних незаведённых обязательных бланков** (`ФО-31`, `ФО-33`, `ФО-36`,
  `ФО-37`, `ФО-39`) и вместе с ними `ADR-0168`: получатель объявляется **видом и значением**,
  видов четыре — роль, подразделение, подразделение своего объекта, должность, — и все перечни
  значений чужие (`ИО-29`). Три записки уходят внутрь организации и остаются полноценным
  выпуском: тратят номер, морозят числа, но ключом «лицо» не отбираются. Заданы `ОЧ-62`
  (`ФО-33` по норме — трём адресатам), `ОЧ-63` (кто печатает записку ДПО), `ОЧ-64` (объект
  `ФО-33` — предмет залога). Смоук — **154/154 PASS**. **Волна 13** от 29.08.2026 —
  сверка с выданными соседями: ядро и статистика вышли из очереди, и три из шестнадцати
  требований выполнены **иначе**, чем просились. `statSlice` отвечает деревом, а не пакетом
  (сильнее просьбы — два уровня разреза больше не два вопроса); `statRows` отвечает бинарно,
  полный список или отказ, а выгрузка выше порога принадлежит статистике (`ADR-0178`);
  `objectRows` принят, но в v1 сужен до объектов ядра (`ADR-0181` §4) — отсюда `ИО-30` и
  столбец «данные» в каталоге. Переписана наша сторона, ни одного встречного требования
  соседям не выставлено. Сторож `RP.foldLevelsSelf` в макете **остался**, но довод у него
  другой: складывать нечего — узел уже посчитан статистикой по своим строкам (`ИС-24`).
  Названы вслух два тупика v1: восемь форм без владельца данных (`ОЧ-68`) и непроработанная
  подсистема заданий, из-за которой у просроченного обязательства нет продолжения внутри
  системы (`ОЧ-69`); плюс `ОЧ-70` — у правки взыскания нет адресата. Все три — не блокирующие.
  Смоук — **154/154 PASS**.
- **Легаси** — отчётность старой системы снята целиком: `requirements/legacy/10-reports.md`
  (метамодель, все перечни, форма прогона, два прогона с числами). Оттуда же взяты
  доказательства цены ошибки в §3, §5, §9.
- **Раздел ТЗ** — `requirements/tz/20-otchetnost.html`, **редакция 2** от 29.08.2026
  (редакция 1 — волна 14 того же дня, редакция 2 — волна 15: добавлен раздел 20 «Карта задач
  постановки», обратная карта «этап → карточка → пункт раздела»), 20 разделов. Канон и каталог сведены в один передаваемый документ; сверх них там три
  раздела, которых по отдельности не было ни в одном источнике: «долг соседей» шестнадцатью
  строками со «состоянием», «чего в модуле нет» с разведёнными причинами восьмёрки без
  владельца данных и открытые вопросы с **ценой другого ответа**. Одна находка сборки:
  у `ФО-38` («Исковое заявление») адресат — судебные органы, а это ни лицо в роли, ни
  подразделение, ни должность; четырёх видов получателя ей не хватает. Шаблона у формы нет,
  поэтому в макете это не всплыло; записано к закрытию в момент заведения шаблона и, вероятно,
  закрывается тем же ответом, что `ОЧ-62`, — пятым видом «внешняя организация». Отдельного
  решения не заведено: это следствие уже открытого вопроса, а не новое.
- **Задачи разработки** — `docs/tasks/p23-otchetnost-tasks.html`, редакция 1 от 29.08.2026
  (волна 15), формат 2 по `docs/tasks/FORMAT.md`: **28 карточек `P23-R1`…`P23-R28` в восьми
  этапах** (шаблон, редакция, публикация · четыре шва и паспорт · показ · выпуск · бланки ·
  календарь, обязательства, полнота · рабочие списки и дашборды · витрина, видимость, швы
  наружу). Свод — 14 сущностей карты данных, 10 экранов, 29 терминов и сквозной пример на
  `ФО-01` и `ФО-34` эталона. Реестровые строки — `TODO.md`, раздел «Отчётность (целевая
  модель)»; модель, правила, приёмка и границы живут только в HTML. Два места переписаны
  после повторного чтения раздела ТЗ 13: окончательно выпускает **тот, за кем обязательство**
  (где обязательства нет — всякий в круге; «за кем» бывает подразделением, а не человеком), и
  неполный охват роняет выпуск в предварительный с запретом называть получателя — усечение
  произносится вслух.
- **Блокирует и разблокирует:** закрывает `mockups/analysis/ASUBK-analiz-logika.md` §14
  («раздел обзоров ждёт модуль отчётности»); требует от ядра публичных перечней полей
  (`ADR-0158`), от статистики — признака исчислимости налету (`ADR-0159`), от оргструктуры —
  подразделений и должностей ссылкой с признаком «ведёт кредиты» (`ADR-0168`); ждёт модуля задач
  для §7. **На 29.08.2026 ничем не заблокирован:** ядро и статистика выданы, оставшиеся
  требования (`ОЧ-68`…`ОЧ-70`) сужают область, а не останавливают модуль.

## 19. Открытые вопросы

1. **Состав прав за словом «уполномоченный».** Одна роль на систему или по подразделениям.
   *Рекомендация:* одна на систему в v1; дробить, когда появится второй спор о шаблоне.
2. ~~**Формат записи расписания.** «Ежемесячно до 5 числа» — правилом или календарём дат.~~
   **Закрыт макетом 24.08.2026:** расписание записано правилом (`freq`, `dueDay`, `since`,
   `asOfRule`), обязательства порождаются из него и нигде не хранятся; состояние обязательства
   считается из выпусков. Смена срока переписывает будущее целиком, а не по одной строке.
3. **Срок жизни файлов предварительных выпусков.** 90 дней — оценка, не измерение.
   В макете это единая на систему константа `FILE_DAYS`, а не поле шаблона.
4. **ЭЦП на бланке.** `ADR-0161` его не решает; вопрос откладывается до появления требования.
   В макете кнопки подписи нет намеренно: нарисовать её раньше решения — значит решить вопрос
   молча (волна 3, 24.08.2026).
5. **Отчёты по объектам несуществующих модулей** (задание, документ из ТЗ) — появятся сами,
   но в приёмке ТЗ их отсутствие придётся объяснить. **Уточнено волной 13** (`ОЧ-68`): вопрос
   перестал быть отдалённым — восемь форм каталога уже сегодня не имеют владельца данных ни в
   одном режиме.
6. **Вторая дата для сравнения.** Легаси держит дату состояния **на каждой ячейке** и печатает
   рядом остаток на сегодня и на конец года; так делать нельзя — паспорт перестаёт описывать
   ответ (`ИО-19`). Но графа сравнения нужна. *Рекомендация:* одна вторая дата на весь отчёт,
   паспорт печатает обе, разрешена только режиму «на дату» (`ОЧ-27`).
7. **Язык бланка.** Модель его не знает: ни поля, ни правила выбора, ни второй редакции макета.
   Между тем каждый бланк — официальный документ, уходящий наружу заёмщику, поручителю,
   госоргану, суду. *Рекомендация:* спросить практику раньше, чем решать (`ОЧ-57`, вопрос
   заказчику от 26.08.2026): переделка задевает все тринадцать бланков разом.
8. **Доказательство доставки и СЭД.** Порядок №41 п. 16 требует штампа почтовой связи,
   уведомления о вручении, отметки заёмщика либо квитанции из СЭД — и от этого дня считаются
   14 дней. Выпуск несёт получателя, дату и номер; доказательства у него нет, а СЭД не
   упомянута ни в §12, ни в §16. *Рекомендация:* по `ADR-0164` срок чужой, значит и
   доказательство скорее всего чужое — но подтвердить фактом (`ОЧ-58`).
9. **Что считается выполненной нормой по акту сверки** (`ОЧ-59`). Порядок №41 п. 9 говорит
   «составляют и **подписывают**» — а модуль наблюдает только то, что производит сам: выпуск
   акта каждому действующему заёмщику. Отметку «подписан» он не заводит намеренно
   (`ADR-0165` §7): она превратила бы обязательство в опрос исполнителей. *Рекомендация:*
   спросить заказчика (вопрос от 26.08.2026); если нормой считается подписание, факт ведёт
   **сопровождение** на объекте — он же нужен классификации, чтобы считать 60 дней уклонения
   (п. 11 п.п. 2), — а календарь отчётности на него ссылается, но своего не заводит.
10. **Владелец показателя «погашено в период по листу»** (`ОЧ-60`). Без него Вид «ОИП» формы
   `ФО-17` собирается с пустой колонкой: разрезы есть, величины нет. Платежи разносят
   поступление **по ключу слоя** (`ADR-0043`), а слой — это присуждённое решением суда, не
   исполнительный лист; листов на один слой бывает несколько, по лицу. *Рекомендация:*
   спросить владельца у платежей и взыскания вместе, одним разговором — по отдельности каждый
   ответит «не моё» справедливо (`ADR-0166` §6).
11. **Акт сверки — на лицо или на договор** (`ОЧ-61`). Пункт 9 требует акт «со всеми
   действующими заёмщиками» — считает **лиц**; макет печатает акт по договору и охват считает
   **договорами**. У заёмщика с двумя кредитами это два акта вместо одного, и знаменатели
   расходятся. Счёт по договорам оставлен намеренно: он строже и раньше нормы не закроется, а
   расхождение печатается в клетке охвата обеими величинами. *Рекомендация:* спросить
   заказчика, как акт подписывается на практике (вопрос от 26.08.2026). Ответ «на лицо» меняет
   не только знаменатель, но и состав бумаги — в ней окажется таблица договоров; ответ «на
   договор» закрывает вопрос без правок (`ADR-0167` §4).
12. **`ФО-33` по норме уходит трём адресатам** (`ОЧ-62`). Пункт 20 п.п. 1 называет заёмщика,
   залогодателя и орган регистрации прав, а выпуск объявляет **одного** получателя. Макет
   заводит залогодателя — ту роль, по которой бумага адресна, — но норма требует ещё двух, и
   третий (орган регистрации) вообще не лицо ядра и не подразделение: это внешняя организация,
   перечня которых модуль не держит. *Рекомендация:* спросить, три ли это бумаги с разными
   номерами или одна с тремя адресатами; от ответа зависит, появится ли пятый вид получателя —
   «внешняя организация» — и что считать исполнением нормы (`ADR-0168`, «Отвергнуто»).
13. **Кто печатает записку о возврате материалов** (`ОЧ-63`). `ФО-39` пишет департамент
   правового обеспечения, а он кредитов не ведёт: в области видимости его нет, роли ДПО в
   макете нет, и строка в витрине видна приглушённой — выпустить некому. *Рекомендация:*
   спросить, кто в ДПО работает с модулем; ответ заводит роль с областью видимости «своё
   подразделение, кредитов не ведущее» — случай, которого модель пока не знает.
14. **Объект `ФО-33` — кредит или предмет залога** (`ОЧ-64`). Норма говорит об обращении
   взыскания **на предмет залога**; в макете объектом бланка остаётся кредит, потому что
   публичного перечня предметов залога ядро не объявляет (`ADR-0158`). *Рекомендация:*
   спросить, бывает ли по одному кредиту несколько предметов с раздельным обращением; если да —
   ядру нужен перечень, а бланк станет говорить о предмете, а не о договоре.
15. **Восемь форм каталога без владельца данных** (`ОЧ-68`). `ФО-05`, `ФО-08`, `ФО-15`,
   `ФО-18`, `ФО-19`, `ФО-20`, `ФО-21`, `ФО-25` не собираются ни «сейчас» (нет объявленного
   публичного перечня полей и `objectRows` — в v1 на него отвечает только ядро, `ADR-0181` §4),
   ни «на дату» (нужного показателя в реестре статистики нет). В каталоге они **помечены, а не
   выброшены**: снятая форма читается как «такой отчётности у нас нет» и разрешает сделать её в
   Word — ровно то, против чего стоит `ИО-23`. *Рекомендация:* не решать это за соседей и не
   заводить у себя ни полей, ни расчёта. Строка долга — владельцам объектов: объявить публичный
   перечень полей и ответить `objectRows`. Блокирующей она не является: без неё модуль
   работоспособен, недоступны ровно эти восемь форм.
16. **Подсистема управления заданиями не прорабатывалась** (`ОЧ-69`). ТЗ объявляет её отдельно
   («подсистема управления заданиями» плюс раздел расчётных показателей по заданиям), но канона,
   ветки и владельца у неё в рабочей области нет; статистика и кураторство её на себя не берут.
   Отсюда два следствия у нас: `ФО-20` помечена в каталоге как «модуль ТЗ не прорабатывался», а
   у просроченного обязательства **нет продолжения внутри системы** — задача ставится вне её
   (§7). Своей мини-задачи отчётность не заводит намеренно: заведённая здесь, она разъедется с
   настоящей, когда та появится (`ИО-13`, `ADR-0160` §2). *Рекомендация:* поднять подсистему
   отдельной строкой очереди модулей, а не растащить её по потребителям.
17. **Календарный срок взыскания продублирован, а адресата у правки нет** (`ОЧ-70`). `ADR-0166`
   §1 предложил снять строку 38 `DEADLINE_TEMPLATES` как дубль строки 37 и остался в состоянии
   «предложено»: у взыскания нет ни живой ветки, ни владельца, кому предложение адресовать.
   Из своей ветки канон соседа мы не правим — это перенос решения через границу модуля.
   *Рекомендация:* расхождение записано в ТЗ и в очередь модулей входящим ящиком взыскания;
   `ADR-0166` оставлен «предложено» до появления владельца. Блокирующим не является: на нашей
   стороне цепь «форма сдана → вопрос вынесен» уже описана §7.
