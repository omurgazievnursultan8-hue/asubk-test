# АСУБК · Отчётность — каталог форм `ФО-01`…`ФО-41`

> Составлен 25.08.2026. Канон логики — [`ASUBK-otchetnost-logika.md`](ASUBK-otchetnost-logika.md),
> журнал волн — [`ASUBK-status-razrabotki.md`](ASUBK-status-razrabotki.md), макет —
> [`reports.html`](reports.html).
>
> Каталог закрывает п. 10 раздела «Что дальше» журнала волн: **модуль родовой, а приёмка
> потребует конкретных форм.**

## 1. Что это и чем оно не является

Канон описывает **механику**: кто заводит шаблон, что объявляет редакция, как выпуск
превращается в факт. Про то, **какие именно формы** организация ждёт от системы, канон
не говорит ни слова — и правильно делает: перечень форм меняется чаще, чем механика.

Каталог — **предметный перечень**: что должно существовать в реестре шаблонов на приёмке.

Чем он **не** является:

- **Не реестр шаблонов.** Реестр — живой объект приложения, его наполняет уполномоченный
  по отчётности. Каталог — намерение и обязательство перед заказчиком: договорённость,
  какие формы v1 обязана уметь. Строка каталога становится шаблоном ровно одним способом —
  публикацией редакции (`ADR-0156`).
- **Не список экранов.** Форма — вопрос, а не страница. Восемь экранов макета обслуживают
  все 41 форму и любые будущие.
- **Не постановка.** Состав полей, разрезы, показатели и лист выгрузки каждой формы
  собираются в конструкторе, а не описываются здесь.

## 2. Нумерация

Один счётчик **`ФО-NN`** на обе разновидности — и на отчёты, и на бланки. Разновидность
(«вид» в терминах §2.2 канона) — столбец таблицы, а не отдельный ряд номеров: у шаблона это
свойство, а не сорт, и форма при переосмыслении может сменить вид, не сменив номера.

Префикс выбран не с первой попытки, и причина стоит того, чтобы её записать. Очевидные
`Р-` и `ОТ-` заняты (`Р-1`…`Р-21` в `docs/superpowers/plans/2026-07-26-kredit-handoff.md`,
`ОТ-317` в `mockups/collection/collection.html`). Короткий `Ф-` формально свободен, но
**ломает поиск**: `python3 scripts/find.py Ф-31` находит `КФ-31` из классификации — а рядом
живут ещё `БФ-*` (реструктуризация), `СФ-*` (залог), `МФ-*`. Тот же случай, из-за которого
модуль отказался от `ОТ-` для решений волн. `ФО-` проверен и по свободе, и по вхождению.

`ФО-*` — префикс **модуля**, как `ОЧ-*` и `ИО-*`: в сквозной `INDEX.md` он не индексируется
(`scripts/find.py` держит только межмодульные `P*-*`, `E2E-*`, `ADR-*`, `Г-*`).

## 3. Откуда взяты строки

| источник | что даёт | где |
|---|---|---|
| **Порядок №41** (ред. 06.07.2026) | нормативно обязательные формы со сроками и адресатами | `docs/normativ/poryadok-2026-07-06-N41.txt` |
| **Легаси, съёмка P10** | что организация реально заказывает сегодня: 16 отчётов + 5 распечаток | `requirements/legacy/10-reports.md:13-26` |
| **Легаси, `reportType`** | 22 базовых объекта — где объект есть, а формы нет, там пробел | `requirements/legacy/10-reports.md:61-65` |
| **Реестр шаблонов макета** | 8 засеянных форм | `mockups/reports/reports.html:1028-1130` |
| **Разделы ТЗ** | объекты новых модулей, которых в легаси не было | `requirements/tz/` |

## 4. Столбец «статус» — что он значит

| статус | значит |
|---|---|
| **обязательна** | названа Порядком №41 со сроком и адресатом. Без неё приёмка не проходит |
| **перенести** | есть в легаси, спрос подтверждён, вопрос тот же — меняется только механика |
| **переосмыслить** | есть в легаси, но вопрос сформулирован через устройство старой базы; переносить как есть нельзя |
| **новая** | объект в новой системе есть, формы никогда не было |
| **вне области** | объект принадлежит модулю за границей проекта |
| **отпадает** | §7 — форма была способом обойти отсутствие параметра |

## 5. Отчёты — `ФО-01`…`ФО-28`

| ID | форма | объект | источник | статус |
|---|---|---|---|---|
| **ФО-01** | Просроченная задолженность по состоянию на 1-е число | кредит | Порядок №41 п. 12 · легаси «задолженность» · макет `t-overdue` | **обязательна** |
| **ФО-02** | Задолженность — свод по портфелю | кредит | легаси отчёт 3 (`LOAN_SUMMARY`, 109 ячеек, 21 разрез) | перенести |
| **ФО-03** | Детальная задолженность по кредиту | кредит | легаси `LOAN_DETAILED_SUMMARY` — тип есть, отчёта нет; распечатки «Расчет» и «Детальный расчет» | переосмыслить |
| **ФО-04** | Погашения за период | платёж | легаси «погашения» (`LOAN_PAYMENT`) | перенести |
| **ФО-05** | Графики погашения | график | легаси «графики» (`LOAN_SCHEDULE`) | перенести |
| **ФО-06** | План поступлений | кредит | легаси «план» (`LOAN_PLAN`) | переосмыслить |
| **ФО-07** | Начисления процентов и штрафов | кредит | легаси `LOAN_ACCRUE` — тип есть, отчёта нет | новая |
| **ФО-08** | Освоение и транши | транш | ТЗ `06-transhi-osvoenie.md`; в легаси только разрез «Группа по освоению» | новая |
| **ФО-09** | Условия кредита и их версии | кредит | легаси `LOAN_TERMS` — тип есть, отчёта нет | новая |
| **ФО-10** | Реструктуризация — заявки и решения | кредит | легаси `LOAN_RECONSTRUCTION` — тип есть, отчёта нет; ТЗ `15-restrukturizatsiya.html` | новая |
| **ФО-11** | Списание задолженности | кредит | легаси «списание» (`LOAN_WRITE_OFF`) | перенести |
| **ФО-12** | Перевод долга | кредит | легаси «перевод долга» (`LOAN_DEBT_TRANSFER`) | перенести |
| **ФО-13** | Целевое использование средств | кредит | легаси `LOAN_TARGETED_USE` — тип есть, отчёта нет | новая |
| **ФО-14** | Залоговое обеспечение — предметы | предмет залога | легаси «залог» (`COLLATERAL_ITEM`) · Порядок №41 п. 10 | перенести |
| **ФО-15** | Акты обследования залога | обследование | легаси «акты обследования» (`COLLATERAL_INSPECTION`) | перенести |
| **ФО-16** | Снятие с ареста | предмет залога | легаси «снятие с ареста» (`COLLATERAL_ARREST_FREE`) | перенести |
| **ФО-17** | Взыскание — процедуры, фазы, события | процедура взыскания | легаси «взыскание» (`COLLECTION_PHASE`, `COLLECTION_EVENT`) | перенести |
| **ФО-18** | Оформление — комплектность кредитно-залогового дела | документ объекта | легаси «оформление» (`ENTITY_DOCUMENT`) · Порядок №41 п. 19 п.п. 2 | перенести |
| **ФО-19** | Документооборот — входящие и исходящие | документ | легаси «документооборот» (`DOCUMENT`) | переосмыслить |
| **ФО-20** | Задания и их исполнение | задание | легаси `TASK` — тип есть, отчёта нет | новая |
| **ФО-21** | Кадры — штат и события сотрудников | сотрудник | легаси «кадры» (`STAFF`) | переосмыслить |
| **ФО-22** | ОИП | **не определён** | легаси «Отчет ОИП» — расшифровка аббревиатуры со стенда не снята | **уточнить** |
| **ФО-23** | Субсидии — выплаты | субсидия | легаси «субсидии» (`SUBSIDY_PAYMENT`) · СУГС (~7 отчётов, `requirements/overview.md:32`) | вне области |
| **ФО-24** | Классификация — движение по группам платежеспособности | заёмщик | ТЗ `17-klassifikatsiya.html`; в легаси только разрезы «Группа заемщика», «Подгруппа заемщика» | новая |
| **ФО-25** | Кураторство — нагрузка и закрепление | куратор | ТЗ `16-kuratorstvo.html`; в легаси только разрез «Куратор» | новая |
| **ФО-26** | Портфель — оперативный срез | кредит | макет `t-portfolio`, режим «сейчас» | новая |
| **ФО-27** | Средняя просрочка по департаментам | кредит | макет `t-avg` | кандидат — см. §6 |
| **ФО-28** | Доля проблемных кредитов | кредит | макет `t-npl` | кандидат — см. §6 |

## 6. Форма или «Вид» — граница, о которую каталог спотыкается первым

Восьмая форма макета — **«Просрочка свыше 60 дней — свод для комитета»** (`t-osh`) —
в каталог **не попала намеренно**, и это главное решение при его составлении.

Она задаёт **тот же вопрос**, что `ФО-01`, с одним отличием: отбор сужен порогом дней.
По `ADR-0162` это не форма, а **накрутка в границах редакции**, сохранённая как **«Вид»** —
восьмая сущность модуля. Заводить её отдельной строкой каталога значит повторить родовой
порок легаси: там «Отчет по задолженности **(Баткенская область)**» — отдельный шаблон
именно потому, что разрез некуда было положить, кроме названия (§7).

Правило, по которому строка попадает в каталог:

> Отдельная форма — там, где **другой вопрос**: другой объект, другой режим, другой
> адресат или другое нормативное основание. Тот же вопрос под другим отбором, разрезом
> или раскладкой — **Вид**, а не форма.

По этому правилу `ФО-27` и `ФО-28` помечены **кандидатами**: обе — один показатель в одном
разрезе, то есть скорее плитка дашборда (§10 канона), чем форма. Решение — за владельцем
статистики: если показатель исчислим (`ADR-0159`), место ему на дашборде.

## 7. Что отпадает — и почему это находка, а не сокращение

Семь шаблонов отчётов и пять шаблонов распечаток легаси **не дают ни одной строки каталога**:

| легаси | почему отпадает |
|---|---|
| Отчет по задолженности **(Баткенская область)** | область — значение параметра отбора `ФО-01`/`ФО-02` |
| Отчет ОИП **копия** | дубль, следствие ручного копирования |
| Акт сверки **Пром** | отдел — значение параметра, не форма |
| Претензия **Пром** | то же |

Причина одна и названа в съёмке легаси: **разрез не параметризуется, а копируется**
(`requirements/legacy/10-reports.md:84-88`). Одна форма размножается по регионам и отделам,
и каждая копия дальше живёт своей жизнью — расходится в составе, в шапке, в опечатках.

В новой модели у этого места есть имя: параметр **роли «отбора»** в составе редакции. Поэтому
12 легаси-шаблонов сворачиваются в **ноль** новых форм — они уже учтены как параметры тех
форм, копиями которых были.

## 8. Бланки — `ФО-29`…`ФО-41`

Бланк говорит **об одном объекте** и имеет адресата (§2.2 канона). Почти все обязательные
формы Порядка №41 — бланки, и у каждой есть срок, то есть **обязательство** (§7 канона).

| ID | бланк | адресат | основание | статус |
|---|---|---|---|---|
| **ФО-29** | Уведомление о наступающем платеже | заёмщик | Порядок №41 п. 15 п.п. 2 · макет `t-notice` | **обязательна** |
| **ФО-30** | Первичная претензия | заёмщик | Порядок №41 п. 16 п.п. 2 · легаси «Претензия» · макет `t-claim` | **обязательна** |
| **ФО-31** | Повторная претензия | заёмщик | Порядок №41 п. 16 п.п. 3 | **обязательна** · в макете **нет** |
| **ФО-32** | Требование поручителю, гаранту, госоргану | поручитель · гарант · госорган | Порядок №41 п. 16 п.п. 4 | **обязательна** · в макете **нет** |
| **ФО-33** | Извещение о начале обращения взыскания на предмет залога | заёмщик · залогодатель · орган регистрации прав | Порядок №41 п. 20 п.п. 1 | **обязательна** · в макете **нет** |
| **ФО-34** | Акт сверки задолженности | заёмщик | Порядок №41 п. 9 · легаси «Акт сверки» | **обязательна** |
| **ФО-35** | Расчёт задолженности, подписанный отраслевым отделом | департамент правового обеспечения | Порядок №41 п. 19 п.п. 2 · легаси «Расчет», «Детальный расчет» | **обязательна** |
| **ФО-36** | Служебная записка об инициировании принудительного взыскания | ДПО | Порядок №41 п. 19 п.п. 2 | **обязательна** · в макете **нет** |
| **ФО-37** | Служебная записка о судебном взыскании | Председатель Правления | Порядок №41 п. 21 | **обязательна** · в макете **нет** |
| **ФО-38** | Исковое заявление | судебные органы | Порядок №41 п. 21 · легаси шаблон «На исковое заявление» | **обязательна** |
| **ФО-39** | Служебная записка о возврате материалов на доработку | отраслевое подразделение | Порядок №41 п. 20 п.п. 1 | **обязательна** · в макете **нет** |

Плюс две формы легаси без нормативного основания, но с подтверждённым спросом:

| ID | бланк | источник | статус |
|---|---|---|---|
| **ФО-40** | Справка о задолженности | макет `t-ref` | перенести |
| **ФО-41** | Реестр погашений | легаси распечатка «Реестр погашений» | перенести |

## 9. Что каталог вскрыл

**1. Обязательных бланков вчетверо больше, чем в макете.** Макет несёт три (`t-notice`,
`t-claim`, `t-ref`), Порядок №41 требует **одиннадцать** (`ФО-29`…`ФО-39`). Шесть не заведены
нигде: повторная претензия, требование поручителю, извещение об обращении взыскания и три
служебные записки — в ДПО, Председателю Правления и на возврат материалов.

Сроки у них есть, но **обязательств отчётности они не порождают**: сроки эти отсчитываются
не от календаря, а **от события объекта** — «десять рабочих дней со дня повторного
направления требований», «за тридцать дней до платежа по графику». Расписание сдачи записано
календарным правилом (`freq`, `dueDay`, `since`), и выразить в нём такой срок нечем. По
`ADR-0164` он принадлежит модулю, которому принадлежит событие: фаза взыскания держит срок,
отчётность печатает бланк, даёт номер и отдаёт факт швом `reportIssues`. Календарь сдачи
таких бланков не содержит — и это его свойство, а не пробел (`ОЧ-56`).

**2. У акта сверки самый жёсткий срок в системе — и он годовой.** Порядок №41 п. 9: акт
по состоянию **на 1 января**, подписанный **до 1 сентября**, **со всеми действующими
заёмщиками**. Срок здесь календарный, поэтому обязательство он порождает — вместе с `ФО-01`
это **два** календарных обязательства на все двенадцать обязательных форм.

Форма при этом единственная, где обязательство считается не по одному объекту, а по полноте
охвата: не подписан хотя бы с одним действующим заёмщиком — обязательство не закрыто.
Проверить, что правило `fullCoverage` (§13.4 канона) это выражает. Открытым остаётся и то,
чем закрывается год: норма требует акт **подписанный**, а подписывает внешнее лицо — выпуск
этого не знает, а ручное «сдано» канон запретил (`ADR-0160` §2).

**3. «Расчёт задолженности» — бланк, а не отчёт.** В легаси это распечатка, но живёт она
как часть пакета в ДПО (п. 19 п.п. 2) и **подписывается отраслевым отделом**. То есть у неё
есть адресат и подпись — признаки бланка, а не таблицы о множестве. Легаси держит два
похожих («Расчет» и «Детальный расчет»), и разница между ними со стенда не снята.

**4. Шесть типов объектов легаси не имеют ни одного отчёта.** `LOAN_ACCRUE`, `LOAN_TERMS`,
`LOAN_RECONSTRUCTION`, `LOAN_TARGETED_USE`, `LOAN_DETAILED_SUMMARY`, `TASK` — объявлены в
`reportType`, но ни один из 16 отчётов на них не построен. Это `ФО-03`, `ФО-07`, `ФО-09`,
`ФО-10`, `ФО-13`, `ФО-20`: спроса на них не было **не потому, что он не нужен**, а потому,
что заводить отчёт в легаси стоило 109 ячеек ручного набора.

**5. Кадры в кредитном модуле — чужая форма.** `ФО-21` («кадры», `STAFF`) отвечает на вопрос
модуля сотрудников, а не кредитования. Отчётность — родовой модуль и умеет её собрать, но
владелец перечня полей здесь другой. Отдать вместе с модулем сотрудников.

**6. Одна форма не расшифрована.** `ФО-22` «ОИП» — аббревиатура встречается в трёх местах
легаси (классификатор `24 исп.лист ОИП`, `05.1.2 безнадёжные ОИП`, вид примечания), но что
она значит, со стенда не снято. Пока не расшифрована — в перечень v1 не входит.

## 10. Что дальше

Разделено на **вопросы заказчику** и **нашу домашку** — по правилу: спрашивать человека о том,
что можно посмотреть самим, значит тратить его ответ.

**Заказчику** — письмо `docs/voprosy-zakazchiku/2026-08-26-otchetnost.md`, семь вопросов:

1. **Отчётность наружу.** Каталог собран из Порядка №41 и легаси — оба источника внутренние.
   Форм, сдаваемых Минфину, Кабмину, Нацстаткому, банку-агенту, в нём **нет ни одной**. Это
   единственная белая зона каталога, и она же — единственный настоящий случай, на котором
   проверяется механика сдачи. Туда же строкой: кадры (`ФО-21`) и субсидии (`ФО-23`) — не наши?
2. **Спрос на девять «новых»** (`ФО-07`…`ФО-10`, `ФО-13`, `ФО-20`, `ФО-24`…`ФО-26`). Спрашивать
   надо не «нужна ли форма» (ответ всегда «да»), а что собирают руками и как часто: частота
   даёт расписание, а готовый Excel — прототип состава.
3. **Образцы шести незаведённых бланков.** Без действующего образца шаблон не сверстать; ответ
   заодно вскроет, документ ли это вообще (служебная записка о возврате материалов вполне
   может оказаться резолюцией).
4. **Акт сверки `ФО-34`:** кто входит в охват на 1 января · что считается подписью · чем
   закрывается год, если заёмщик отказался или недоступен.
5. **Кто отвечает за сдачу `ФО-01` и `ФО-34`.** Только за этими двумя стоит календарное
   обязательство (`ADR-0164`); по остальным исполнителя называет норма.
6. **Как бланк уходит и чем это доказывается** — почта, нарочно, СЭД; где лежит квитанция; от
   какого дня идут 14 дней (`ОЧ-58`).
7. **Язык бланков** (`ОЧ-57`).

**Наша домашка:**

8. **Расшифровать `ФО-22`** — прогнать «Отчет ОИП» на легаси-стенде и посмотреть, о чём он.
9. **Развести `ФО-03` / `ФО-35`** — «Расчет» против «Детальный расчет» на стенде.
10. **Решить `ФО-27` / `ФО-28`** с владельцем статистики: форма или плитка дашборда.
11. **Внести недостающие бланки в макет** (`ФО-31`…`ФО-33`, `ФО-36`, `ФО-37`, `ФО-39`) — или явно
    записать, что макет их не показывает намеренно.
12. **Проверить `fullCoverage` на `ФО-34`** — годовой акт сверки как случай полного охвата.
13. **Поставить требование взысканию** — фаза несёт срок и признак «печатается бланк такой-то»,
    читает `reportIssues` (`ADR-0164`, раздел ТЗ 18).

Пункт 1 остаётся **блокирующим** по `ADR-0163`: до согласования перечня справочнику форм нечем
наполниться, кроме двенадцати обязательных.
