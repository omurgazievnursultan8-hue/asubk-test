# АСУБК · Отчётность — каталог форм `ФО-01`…`ФО-43`

> Составлен 25.08.2026. Канон логики — [`ASUBK-otchetnost-logika.md`](ASUBK-otchetnost-logika.md),
> журнал волн — [`ASUBK-status-razrabotki.md`](ASUBK-status-razrabotki.md), макет —
> [`reports.html`](reports.html).
>
> Каталог закрывает п. 10 раздела «Что дальше» журнала волн: **модуль родовой, а приёмка
> потребует конкретных форм.**
>
> **Дополнен 26.08.2026** сверкой с легаси-стендом (`claude-in-chrome`, `85.113.29.29:8080`):
> расшифрован `ФО-22`, разведены `ФО-03` и `ФО-35` — §9 пп. 3, 6, 7, 8.
>
> **Обновлён 26.08.2026 волной 12 макета:** пять последних обязательных бланков заведены
> (`ФО-31`, `ФО-33`, `ФО-36`, `ФО-37`, `ФО-39`); незаведённых обязательных не осталось, кроме
> `ФО-35` и `ФО-38` — обе переносятся из легаси. Получатель бланка стал парой «вид + значение»
> (`ADR-0168`) — §8, §9 п. 1.
>
> **Обновлён 29.08.2026 волной 13:** у таблицы §5 появился столбец **«данные»** — кто в v1
> отдаёт строки формы. Восемь форм не собираются **ни в одном** режиме: владельца у них нет.
> Они **помечены, а не сняты** — §5, §9 п. 9 (`ОЧ-68`, `ОЧ-69`).
>
> **Дополнен 31.08.2026 волной 17** (разбор логики распечатки): периметр бланка расширен по
> **месту забора** — всё, что куратор берёт из карточки, делается одним механизмом макетов
> (`ADR-0223`). Отсюда третья таблица §8 — **рабочие бумаги** `ФО-42` и `ФО-43`, у которых нет
> ни серии, ни номера, ни обязательства, но есть получатель и заморозка. Обе помечены по
> данным: строк для них в v1 никто не отдаёт — §8, §9 п. 10.

## 1. Что это и чем оно не является

Канон описывает **механику**: кто заводит шаблон, что объявляет редакция, как выпуск
превращается в факт. Про то, **какие именно формы** организация ждёт от системы, канон
не говорит ни слова — и правильно делает: перечень форм меняется чаще, чем механика.

Каталог — **предметный перечень**: что должно существовать в реестре шаблонов на приёмке.

Чем он **не** является:

- **Не реестр шаблонов.** Реестр — живой объект приложения, его наполняет уполномоченный
  по отчётности. Каталог — намерение и обязательство перед заказчиком: договорённость,
  какие формы v1 обязана уметь. Строка каталога становится шаблоном ровно одним способом —
  публикацией редакции (`ADR-0156`).
- **Не список экранов.** Форма — вопрос, а не страница. Восемь экранов макета обслуживают
  весь каталог и любые будущие формы.
- **Не постановка.** Состав полей, разрезы, показатели и лист выгрузки каждой формы
  собираются в конструкторе, а не описываются здесь.

## 2. Нумерация

Один счётчик **`ФО-NN`** на обе разновидности — и на отчёты, и на бланки. Разновидность
(«вид» в терминах §2.2 канона) — столбец таблицы, а не отдельный ряд номеров: у шаблона это
свойство, а не сорт, и форма при переосмыслении может сменить вид, не сменив номера.

Префикс выбран не с первой попытки, и причина стоит того, чтобы её записать. Очевидные
`Р-` и `ОТ-` заняты (`Р-1`…`Р-21` в `docs/superpowers/plans/2026-07-26-kredit-handoff.md`,
`ОТ-317` в `mockups/collection/collection.html`). Короткий `Ф-` формально свободен, но
**ломает поиск**: `python3 scripts/find.py Ф-31` находит `КФ-31` из классификации — а рядом
живут ещё `БФ-*` (реструктуризация), `СФ-*` (залог), `МФ-*`. Тот же случай, из-за которого
модуль отказался от `ОТ-` для решений волн. `ФО-` проверен и по свободе, и по вхождению.

Номер, однажды выданный, **не переиспользуется и не освобождается**. `ФО-22` после сверки со
стендом 26.08.2026 отпал в Вид формы `ФО-17` (§9 п. 6), но строка осталась на месте со статусом
«отпадает»: сорок живых строк при сорока одном номере. Иначе ссылка из письма заказчику или из
ADR через месяц указывает на другую форму.

`ФО-*` — префикс **модуля**, как `ОЧ-*` и `ИО-*`: в сквозной `INDEX.md` он не индексируется
(`scripts/find.py` держит только межмодульные `P*-*`, `E2E-*`, `ADR-*`, `Г-*`).

## 3. Откуда взяты строки

| источник | что даёт | где |
|---|---|---|
| **Порядок №41** (ред. 06.07.2026) | нормативно обязательные формы со сроками и адресатами | `docs/normativ/poryadok-2026-07-06-N41.txt` |
| **Легаси, съёмка P10** | что организация реально заказывает сегодня: 16 отчётов + 5 распечаток | `requirements/legacy/10-reports.md:13-26` |
| **Легаси, `reportType`** | 22 базовых объекта — где объект есть, а формы нет, там пробел | `requirements/legacy/10-reports.md:61-65` |
| **Реестр шаблонов макета** | 9 засеянных форм | `mockups/reports/reports.html:1120-1266` |
| **Разделы ТЗ** | объекты новых модулей, которых в легаси не было | `requirements/tz/` |

## 4. Столбец «статус» — что он значит

| статус | значит |
|---|---|
| **обязательна** | названа Порядком №41 со сроком и адресатом. Без неё приёмка не проходит |
| **перенести** | есть в легаси, спрос подтверждён, вопрос тот же — меняется только механика |
| **переосмыслить** | есть в легаси, но вопрос сформулирован через устройство старой базы; переносить как есть нельзя |
| **новая** | объект в новой системе есть, формы никогда не было |
| **вне области** | объект принадлежит модулю за границей проекта |
| **отпадает** | §7 — форма была способом обойти отсутствие параметра |

## 5. Отчёты — `ФО-01`…`ФО-28`

Столбцов о происхождении теперь два, и они про разное. **«Откуда строка»** — откуда взято
требование: норма, легаси, ТЗ, макет. **«Данные»** — кто в v1 отдаёт **числа и строки**:

- **ядро · «сейчас»** — живые строки шва `objectRows`. В v1 на него отвечает только ядро и
  только по своим объектам, а в макете это **кредит** (`ADR-0181` §4, `ИО-30`).
- **статистика · «на дату»** — снимок швами `statSlice` / `statRows`; доступно там, где объект
  заведён в реестре статистики (девять объектов: кредит · заёмщик · залог · дело взыскания ·
  требование взыскания · мера взыскания · платёж · поступление · кредитная программа).
- **владельца нет** — форма не собирается **ни в одном** режиме: публичного перечня полей
  никто не объявил, показателя в реестре нет. Строка остаётся в каталоге: снятая форма
  читается как «такой отчётности у нас нет» и разрешает сделать её в Word — то самое, против
  чего стоит `ИО-23`.

| ID | форма | объект | откуда строка | **данные** | статус |
|---|---|---|---|---|---|
| **ФО-01** | Просроченная задолженность по состоянию на 1-е число | кредит | Порядок №41 п. 12 · легаси «задолженность» · макет `t-overdue` | ядро · «сейчас»<br>статистика · «на дату» | **обязательна** |
| **ФО-02** | Задолженность — свод по портфелю | кредит | легаси отчёт 3 (`LOAN_SUMMARY`, 109 ячеек, 21 разрез) | ядро · «сейчас»<br>статистика · «на дату» | перенести |
| **ФО-03** | Детальная задолженность по кредиту — построчно по датам пересчёта | кредит | легаси распечатка «Детальный расчет» (`LOAN_DETAILED_SUMMARY`, шаблон 2) + вкладка «Дет. расчет» карточки кредита, 35 колонок; **отчёта нет** | ядро · «сейчас»<br>статистика · «на дату» | переосмыслить |
| **ФО-04** | Погашения за период | платёж | легаси «погашения» (`LOAN_PAYMENT`) | статистика · «на дату» | перенести |
| **ФО-05** | Графики погашения | график | легаси «графики» (`LOAN_SCHEDULE`) | **владельца нет** | перенести |
| **ФО-06** | План поступлений | кредит | легаси «план» (`LOAN_PLAN`) | ядро · «сейчас»<br>статистика · «на дату» | переосмыслить |
| **ФО-07** | Начисления процентов и штрафов | кредит | легаси `LOAN_ACCRUE` — тип есть, отчёта нет | ядро · «сейчас»<br>статистика · «на дату» | новая |
| **ФО-08** | Освоение и транши | транш | ТЗ `06-transhi-osvoenie.md`; в легаси только разрез «Группа по освоению» | **владельца нет** | новая |
| **ФО-09** | Условия кредита и их версии | кредит | легаси `LOAN_TERMS` — тип есть, отчёта нет | ядро · «сейчас»<br>статистика · «на дату» | новая |
| **ФО-10** | Реструктуризация — заявки и решения | кредит | легаси `LOAN_RECONSTRUCTION` — тип есть, отчёта нет; ТЗ `15-restrukturizatsiya.html` | ядро · «сейчас»<br>статистика · «на дату» | новая |
| **ФО-11** | Списание задолженности | кредит | легаси «списание» (`LOAN_WRITE_OFF`) | ядро · «сейчас»<br>статистика · «на дату» | перенести |
| **ФО-12** | Перевод долга | кредит | легаси «перевод долга» (`LOAN_DEBT_TRANSFER`) | ядро · «сейчас»<br>статистика · «на дату» | перенести |
| **ФО-13** | Целевое использование средств | кредит | легаси `LOAN_TARGETED_USE` — тип есть, отчёта нет | ядро · «сейчас»<br>статистика · «на дату» | новая |
| **ФО-14** | Залоговое обеспечение — предметы | предмет залога | легаси «залог» (`COLLATERAL_ITEM`) · Порядок №41 п. 10 | статистика · «на дату» | перенести |
| **ФО-15** | Акты обследования залога | обследование | легаси «акты обследования» (`COLLATERAL_INSPECTION`) | **владельца нет** | перенести |
| **ФО-16** | Снятие с ареста | предмет залога | легаси «снятие с ареста» (`COLLATERAL_ARREST_FREE`) | статистика · «на дату» | перенести |
| **ФО-17** | Взыскание — процедуры, фазы, события | процедура взыскания | легаси «взыскание» (`COLLECTION_PHASE`, `COLLECTION_EVENT`) | статистика · «на дату» | перенести |
| **ФО-18** | Оформление — комплектность кредитно-залогового дела | документ объекта | легаси «оформление» (`ENTITY_DOCUMENT`) · Порядок №41 п. 19 п.п. 2 | **владельца нет** | перенести |
| **ФО-19** | Документооборот — входящие и исходящие | документ | легаси «документооборот» (`DOCUMENT`) | **владельца нет** | переосмыслить |
| **ФО-20** | Задания и их исполнение | задание | легаси `TASK` — тип есть, отчёта нет | **владельца нет** — **модуль ТЗ не прорабатывался** | новая |
| **ФО-21** | Кадры — штат и события сотрудников | сотрудник | легаси «кадры» (`STAFF`) | **владельца нет** | переосмыслить |
| **ФО-22** | Исполнительные листы, переданные в ОИП — ход взыскания | фаза взыскания | легаси «Отчет ОИП» (отчёт 14, `COLLECTION_PHASE`) + дубль «Отчет ОИП копия» (16) | — | отпадает — **Вид** формы `ФО-17`, см. §9 п. 6 |
| **ФО-23** | Субсидии — выплаты | субсидия | легаси «субсидии» (`SUBSIDY_PAYMENT`) · СУГС (~7 отчётов, `requirements/overview.md:32`) | — | вне области |
| **ФО-24** | Классификация — движение по группам платежеспособности | заёмщик | ТЗ `17-klassifikatsiya.html`; в легаси только разрезы «Группа заемщика», «Подгруппа заемщика» | статистика · «на дату» | новая |
| **ФО-25** | Кураторство — нагрузка и закрепление | куратор | ТЗ `16-kuratorstvo.html`; в легаси только разрез «Куратор» | **владельца нет** | новая |
| **ФО-26** | Портфель — оперативный срез | кредит | макет `t-portfolio`, режим «сейчас» | ядро · «сейчас» | новая |
| **ФО-27** | Средняя просрочка по департаментам | кредит | макет `t-avg` | ядро · «сейчас»<br>статистика · «на дату» | кандидат — см. §6 |
| **ФО-28** | Доля проблемных кредитов | кредит | макет `t-npl` | ядро · «сейчас»<br>статистика · «на дату» | кандидат — см. §6 |

## 6. Форма или «Вид» — граница, о которую каталог спотыкается первым

Восьмая форма макета — **«Просрочка свыше 60 дней — свод для комитета»** (`t-osh`) —
в каталог **не попала намеренно**, и это главное решение при его составлении.

Она задаёт **тот же вопрос**, что `ФО-01`, с одним отличием: отбор сужен порогом дней.
По `ADR-0162` это не форма, а **накрутка в границах редакции**, сохранённая как **«Вид»** —
восьмая сущность модуля. Заводить её отдельной строкой каталога значит повторить родовой
порок легаси: там «Отчет по задолженности **(Баткенская область)**» — отдельный шаблон
именно потому, что разрез некуда было положить, кроме названия (§7).

Правило, по которому строка попадает в каталог:

> Отдельная форма — там, где **другой вопрос**: другой объект, другой режим, другой
> адресат или другое нормативное основание. Тот же вопрос под другим отбором, разрезом
> или раскладкой — **Вид**, а не форма.

По этому правилу `ФО-27` и `ФО-28` помечены **кандидатами**: обе — один показатель в одном
разрезе, то есть скорее плитка дашборда (§10 канона), чем форма. Решение — за владельцем
статистики: если показатель исчислим (`ADR-0159`), место ему на дашборде.

## 7. Что отпадает — и почему это находка, а не сокращение

Семь шаблонов отчётов и пять шаблонов распечаток легаси **не дают ни одной строки каталога**:

| легаси | почему отпадает |
|---|---|
| Отчет по задолженности **(Баткенская область)** | область — значение параметра отбора `ФО-01`/`ФО-02` |
| Отчет ОИП **копия** | дубль ради **одного пользователя** — см. ниже |
| Акт сверки **Пром** | отдел — значение параметра, не форма |
| Претензия **Пром** | то же |

«Отчет ОИП копия» (отчёт 16) сверен с оригиналом (14) поштучно на стенде 26.08.2026: совпадают
базовый объект, все 16 фильтров, **все 81 параметр содержания**, все 15 параметров вывода и все
12 разрезов. Отличие ровно одно — **список пользователей**: у 14 их 26, у 16 — 25, без учётной
записи `oip04`. Дать ею доступ или отобрать — со стенда не видно; видно, что **81 ячейка
размножена ради разницы в одного человека**, потому что видимость ведётся поимённым списком,
а не ролью.

Причина одна и названа в съёмке легаси: **разрез не параметризуется, а копируется**
(`requirements/legacy/10-reports.md:84-88`). Одна форма размножается по регионам и отделам,
и каждая копия дальше живёт своей жизнью — расходится в составе, в шапке, в опечатках.

В новой модели у этого места есть имя: параметр **роли «отбора»** в составе редакции. Поэтому
12 легаси-шаблонов сворачиваются в **ноль** новых форм — они уже учтены как параметры тех
форм, копиями которых были.

## 8. Бланки — `ФО-29`…`ФО-43`

Бланк говорит **об одном объекте** и имеет адресата (§2.2 канона). Почти все обязательные
формы Порядка №41 — бланки, и у каждой есть срок, то есть **обязательство** (§7 канона).

| ID | бланк | адресат | основание | статус |
|---|---|---|---|---|
| **ФО-29** | Уведомление о наступающем платеже | заёмщик | Порядок №41 п. 15 п.п. 2 · макет `t-notice` | **обязательна** |
| **ФО-30** | Первичная претензия | заёмщик | Порядок №41 п. 16 п.п. 2 · легаси «Претензия» · макет `t-claim` | **обязательна** |
| **ФО-31** | Повторная претензия | заёмщик — лицо в роли у кредита | Порядок №41 п. 16 п.п. 3 · макет `t-claim2` (волна 12) | **обязательна** |
| **ФО-32** | Требование поручителю, гаранту, госоргану | **поручитель** — лицо в роли у кредита, не отдельный объект | Порядок №41 п. 16 п.п. 4 · макет `t-guar` (волна 11, `ADR-0167`) | **обязательна** |
| **ФО-33** | Извещение о начале обращения взыскания на предмет залога | по норме трое: заёмщик · залогодатель · орган регистрации прав; в макете — **залогодатель** (`ОЧ-62`) | Порядок №41 п. 20 п.п. 1 · макет `t-pledge` (волна 12) | **обязательна** |
| **ФО-34** | Акт сверки задолженности | заёмщик | Порядок №41 п. 9 · легаси «Акт сверки» · макет `t-recon` (волна 9, `ADR-0165`) | **обязательна** |
| **ФО-35** | Расчёт задолженности, подписанный отраслевым отделом | департамент правового обеспечения | Порядок №41 п. 19 п.п. 2 · легаси распечатка «Расчет» (`LOAN_SUMMARY`), единственный шаблон — **«На исковое заявление»** | **обязательна** |
| **ФО-36** | Служебная записка об инициировании принудительного взыскания | **подразделение** — ДПО (вид «подразделение», `ADR-0168`) | Порядок №41 п. 19 п.п. 2 · макет `t-memo-dpo` (волна 12) | **обязательна** |
| **ФО-37** | Служебная записка о судебном взыскании | **должность** — Председатель Правления, не человек | Порядок №41 п. 21 · макет `t-memo-chair` (волна 12) | **обязательна** |
| **ФО-38** | Исковое заявление | судебные органы | Порядок №41 п. 21 · легаси шаблон «На исковое заявление» | **обязательна** |
| **ФО-39** | Служебная записка о возврате материалов на доработку | **подразделение своего объекта** — то, которое ведёт этот кредит; пишет ДПО (`ОЧ-63`) | Порядок №41 п. 20 п.п. 1 · макет `t-memo-back` (волна 12) | **обязательна** |

Столбца «данные» у бланков нет за ненадобностью: объект у всех тринадцати — **кредит**
(у `ФО-41` — платёж), то есть владелец строк есть в обоих режимах. Единственная оговорка —
`ФО-33`: норма говорит о **предмете залога**, и если ответ заказчика по `ОЧ-64` окажется
«предмет», у бланка не станет владельца в режиме «сейчас» — перечня предметов ядро не
объявляет (`ADR-0158`).

Плюс две формы легаси без нормативного основания, но с подтверждённым спросом:

| ID | бланк | источник | статус |
|---|---|---|---|
| **ФО-40** | Справка о задолженности | макет `t-ref` | перенести |
| **ФО-41** | Реестр погашений | легаси распечатка «Реестр погашений» | перенести |

И две **рабочие бумаги** — то, что куратор забирает из карточки кредита, чтобы ею работать.
Признак «рабочая бумага» снимает серию, номер и обязательство, и только их: получатель у обеих
есть, и названный получатель по-прежнему морозит числа и файл (`ADR-0223`, `ИО-36`). В графе
основания стоит **практика**, а не пункт Порядка: нормой они не требуются, меню «Печатные
формы» карточки кредита их перечисляет (ТЗ 05, `P15-R31`).

| ID | бумага | адресат | основание | данные | статус |
|---|---|---|---|---|---|
| **ФО-42** | График погашения | заёмщик — лицо в роли у кредита | практика · меню карточки кредита | **нет владельца**: тело — строки графика, `objectRows` по ним в v1 не отвечает (`ADR-0181` §4, `ИО-30`) | **помечена** |
| **ФО-43** | Акт обследования залога | подразделение своего объекта | практика · меню карточки кредита | **нет владельца**: залог публичного перечня полей не объявляет | **помечена** |

Обе требуют **повторяющегося блока** в макете (`ADR-0226`, `ИО-39`) — как и `ФО-34`, и
`ФО-41`: плоскими подстановками их тело невыразимо.

## 9. Что каталог вскрыл

**1. Обязательных бланков было вдвое больше, чем в макете — теперь незаведённых не осталось.**
Порядок №41 требует **одиннадцать** бланков (`ФО-29`…`ФО-39`); на 26.08.2026 макет нёс из них
четыре, а пять не были заведены нигде. Волны 9 и 11 добавили акт сверки `ФО-34` и требование
поручителю `ФО-32`, **волна 12 внесла оставшиеся пять**: повторную претензию (`t-claim2`),
извещение об обращении взыскания (`t-pledge`) и три служебные записки — в ДПО (`t-memo-dpo`),
Председателю Правления (`t-memo-chair`), на возврат материалов (`t-memo-back`). Без шаблона
остаются `ФО-35` и `ФО-38`: обе есть в легаси и переносятся оттуда, а не пишутся заново.

Заводились они не ради полноты каталога: каждая **проверяла модель получателя**, и модель
дважды не выдержала.

`ФО-32` показала, что «контрагент объекта» получателя не определяет: у кредита их несколько, и
роль объявляет редакция бланка, а не человек на выпуске — в теле требования печатается
`{{borrower}}`, а бумага уходит поручителю (`ADR-0167`, `ИО-28`).

Три служебные записки показали, что **получателем бывает не лицо вовсе**: департамент, должность
и «то подразделение, которое ведёт этот кредит» — не субъекты ядра, и ИНН у них нет. Получатель
стал парой **вид + значение**, видов четыре, и ни одного перечня значений отчётность не ведёт
(`ADR-0168`, `ИО-29`). Записка внутрь организации осталась полноценным выпуском — тратит номер и
морозит числа, — но ключом «лицо» не отбирается: этому лицу она не предъявлялась.

Три вопроса заказчику отсюда же: `ФО-33` по норме уходит **троим** (заёмщику, залогодателю,
органу регистрации прав), а выпуск объявляет одного (`ОЧ-62`); `ФО-39` пишет ДПО, который
кредитов не ведёт и в область видимости не входит — печатать её в макете некому (`ОЧ-63`);
объект `ФО-33` по норме — **предмет залога**, а публичного перечня предметов ядро не объявляет
(`ОЧ-64`).

Сроки у них есть, но **обязательств отчётности они не порождают**: сроки эти отсчитываются
не от календаря, а **от события объекта** — «десять рабочих дней со дня повторного
направления требований», «за тридцать дней до платежа по графику». Расписание сдачи записано
календарным правилом (`freq`, `dueDay`, `since`), и выразить в нём такой срок нечем. По
`ADR-0164` он принадлежит модулю, которому принадлежит событие: срок держит **мера** взыскания
(экземпляр шаблона срока — не «фаза», `ADR-0166` §5), признак «печатается такой-то бланк» —
свойство **вида меры**;
отчётность печатает бланк, даёт номер и отдаёт факт швом `reportIssues`. Календарь сдачи
таких бланков не содержит — и это его свойство, а не пробел (`ОЧ-56`).

**2. У акта сверки самый жёсткий срок в системе — и он годовой.** Порядок №41 п. 9: акт
по состоянию **на 1 января**, подписанный **до 1 сентября**, **со всеми действующими
заёмщиками**. Срок здесь календарный, поэтому обязательство он порождает — вместе с `ФО-01`
это **два** календарных обязательства на все двенадцать обязательных форм.

Форма при этом единственная, где обязательство считается не по одному объекту, а по полноте
охвата: не сверено хотя бы с одним действующим заёмщиком — обязательство не закрыто.
**Проверено волной 9 макета — и признака `fullCoverage` не хватило** (`ADR-0165`): он отвечал
на другой вопрос, «полон ли ответ» (не усекла ли строки область видимости, §13.4), а тут
спрашивается «по чему считается полнота». Заведён второй признак `coverBy: 'объект'`:
обязательство закрывают **все выпуски разом**, а пока не все — несёт поимённый остаток
(«Пром — 1 из 2, нет: КД-2024/203»). Знаменатель берётся из **снимка на 1 января**, а не из
живого перечня: кредит, закрытый в августе, из живого перечня выпал бы, хотя акт с ним
подписывать обязаны, а выданный в июне — стоял бы, хотя на 1 января его не было (`ИО-27`).

**Волна 11 нашла у этого знаменателя расхождение с нормой.** Пункт 9 говорит «со всеми
действующими **заёмщиками**» — считает **лиц**; охват считает **договоры**, потому что акт
печатается по договору. У заёмщика с двумя кредитами это два акта вместо одного. Счёт по
договорам оставлен (он строже и раньше нормы не закроется), но в клетке охвата печатаются обе
величины, а вопрос «акт на лицо или на договор» ушёл заказчику: ответ «на лицо» меняет и
знаменатель, и состав бумаги — в ней окажется таблица договоров (`ОЧ-61`, `ADR-0167` §4).

Чем закрывается год, остаётся открытым (`ОЧ-59`): норма требует акт **подписанный**, а
подписывает внешнее лицо. Отметки «подписан» в отчётности нет и не заводится — модуль меряет
обязательство тем, что производит сам (`ADR-0160` §2, `ADR-0165` §7); факт подписания и
уклонение свыше 60 дней ведёт сопровождение, и спрашивает их классификация (п. 11 п.п. 2).

**3. «Расчёт задолженности» — бланк, а не отчёт; «Детальный расчет» — не он.** В легаси это
распечатка, но живёт она как часть пакета в ДПО (п. 19 п.п. 2) и **подписывается отраслевым
отделом**. То есть у неё есть адресат и подпись — признаки бланка, а не таблицы о множестве.
Два похожих названия разведены на стенде 26.08.2026:

| легаси | тип | шаблон | что это на самом деле | наша строка |
|---|---|---|---|---|
| «Расчет» | `LOAN_SUMMARY` | единственный — **«На исковое заявление»** | расчёт задолженности для иска: один лист, один адресат, подпись | `ФО-35` (бланк) |
| «Детальный расчет» | `LOAN_DETAILED_SUMMARY` | «Детальный расчет» (шаблон 2) | **35 колонок построчно по датам пересчёта** — освоение, погашение, по графику, списание, просрочка, и так по основной сумме, процентам, накопленным процентам и штрафам | `ФО-03` (отчёт) |

Разводит их не объём, а **вопрос**: первый отвечает «сколько должен на дату и чем это
подтверждено» и уходит наружу, второй — «как задолженность сложилась» и остаётся внутри. Второй
и в интерфейсе стоит не там: он **вкладка «Дет. расчет» внутри «ДОПОЛНИТЕЛЬНО»** карточки
кредита, рядом с товаром, банкротством и списанием, — а не на вкладке «РАСЧЕТЫ», где живут все
остальные расчётные кнопки.

Печатается он **построчно**: у каждой строки таблицы своя кнопка со ссылкой
`/printoutTemplate/2/objectId/<id строки>/generate`, где `<id строки>` — идентификатор снимка на
дату, а не кредита. То же на вкладке «РАСЧЕТЫ»: там построчно печатается «Реестр погашений»
(шаблон 1). Единица печати в легаси — **строка пакетного пересчёта**, и это прямое следствие
`E2E-09`: остаток там снимок, а не величина налету.

**4. Пять типов объектов легаси не имеют ни одного отчёта.** `LOAN_ACCRUE`, `LOAN_TERMS`,
`LOAN_RECONSTRUCTION`, `LOAN_TARGETED_USE`, `TASK` — объявлены в `reportType`, но ни один из 16
отчётов на них не построен. Это `ФО-07`, `ФО-09`, `ФО-10`, `ФО-13`, `ФО-20`: спроса на них не
было **не потому, что он не нужен**, а потому, что заводить отчёт в легаси стоило 109 ячеек
ручного набора.

Шестой тип, `LOAN_DETAILED_SUMMARY`, из этого списка **вычеркнут**: отчёта на нём действительно
нет, но спрос закрыт распечаткой и экранной вкладкой (п. 3). Отсутствие отчёта здесь означает не
«не спрашивали», а «спрашивают, но без единого параметра»: 35 колонок зашиты намертво, отбора
нет, разрезов нет.

**5. Кадры в кредитном модуле — чужая форма.** `ФО-21` («кадры», `STAFF`) отвечает на вопрос
модуля сотрудников, а не кредитования. Отчётность — родовой модуль и умеет её собрать, но
владелец перечня полей здесь другой. Отдать вместе с модулем сотрудников.

**6. `ФО-22` «ОИП» расшифрован — и оказался Видом, а не формой.** Снято со стенда 26.08.2026,
`/report/14/edit`. Базовый объект — **`COLLECTION_PHASE`**, фаза взыскания. Отбор построен
вокруг исполнительного листа: «по исполнительным листам», «Вид результата (на исполнении)»,
«Статус процедуры (пер. в ОИП)», «Индекс ИЛ», «Индекс2 ИЛ». Своя группа ячеек содержания —
«Отчет ОИП. Шапка таблицы. Вид ИЛ / Индекс ИЛ / Индекс2 ИЛ / Погашение в период / Примечание»
и «Отчет ОИП. ИТОГО. Погашение в период». Двенадцать разрезов (область, район, отрасль, вид
кредита, заёмщик, процедура, фаза, вид стадии, вид результата, статус процедуры, группа и
подгруппа заёмщика). Пользователи — учётные записи `oip001`, `oip002`, `oip003`, `oip04`.

Прямой расшифровки аббревиатуры в интерфейсе нет нигде, но набор параметров её не оставляет
вариантов: **ОИП — орган исполнительного производства**, куда уходит исполнительный лист.
Форма отвечает на вопрос «что у судебных исполнителей и сколько по этому взыскано за период».
Карточка кредита это подтверждает с другой стороны — там есть кнопка «Примечание ( ОИП )».

Отдельной строкой каталога она **не остаётся**. По правилу §6 разные формы — там, где разный
вопрос: объект тот же (фаза взыскания), режим тот же, нормативного основания своего нет,
отличаются отбор, состав и разрезы — то есть ровно то, что в новой модели крутится **Видом**
в границах редакции (`ADR-0162` п. 1, 3). Единственный довод за отдельную форму — свой круг
пользователей `oip*`; он снимается тем, что область видимости у нас свойство редакции
(`ADR-0152` §3), а не причина заводить вторую форму. Отсюда `ФО-22` = **Вид формы `ФО-17`**.

Требование, которое из этого следует, адресовано не отчётности: чтобы Вид собрался, нужны
**реквизиты исполнительного листа** — вид, индекс, второй индекс — и показатель «погашено в
период по листу». Иначе разрез есть, а колонки пустые.

**Адресат поправлен 26.08.2026** (`ADR-0166` §5). Написано было «фаза взыскания обязана
нести»; во взыскании фазы как хранимой сущности нет — решением **М-2** она свёртка журнала
мер. Реквизиты листа лежат на **мере вида «Исполнительный лист»** (`ADR-0030`), она же
самостоятельная. У показателя «погашено в период по листу» владельца не нашлось вовсе:
платежи разносят по ключу слоя, а слой — присуждённое решением суда, не лист (`ОЧ-60`).

**7. Распечатка в легаси не параметризуется вообще.** У всех пяти шаблонов распечаток вкладки
«параметры формирования», «фильтрации», «содержания» и две непереведённые («3», «4») —
**пустые**; у шаблона отчёта на тех же местах сотни ячеек. То есть в старой системе документ
об одном объекте и таблица о множестве собраны из одних кирпичей формально, а на деле бланк —
жёстко свёрстанный файл, который нельзя ни настроить, ни версионировать. Наша модель разводит
это иначе (`ADR-0156`: редакция объявляет состав и у бланка тоже), и легаси показывает цену
обратного: пять бланков — пять неизменяемых вёрсток, редакция «Пром» делается копией.

**8. Бланк и фаза взыскания в легаси запускаются одной кнопкой.** На вкладке «РАСЧЕТЫ»
карточки кредита кнопка «Претензия» имеет идентификатор `bnt_init_phase` — она **создаёт фазу
взыскания**, а не печатает документ. Рядом «Акт сверки» = `bnt_init_manualCalculator` (ручной
пересчёт), «Обновить данные» = `bnt_init_dailyCalculator` (пакетный). Старая система, не имея
модели отчётности, склеила бумагу с процессом в одно нажатие — и это независимое подтверждение
`ADR-0164`: срок и повод у такого бланка живут в фазе взыскания, отчётности принадлежат макет,
номер и факт предъявления. Разница в том, что у нас это **шов**, а не общая кнопка.

**9. Восьми формам данные отдать некому — и выяснилось это только на сверке с выданными
соседями** (29.08.2026, волна 13). Пока `objectRows` и реестр показателей существовали как
требования, каталог был полон: у любой формы предполагался владелец, который «объявит поля,
когда появится». Ядро и статистика вышли из очереди — и предположение проверилось. `ФО-05`
(графики), `ФО-08` (транши), `ФО-15` (акты обследования), `ФО-18` (комплектность дела), `ФО-19`
(документооборот), `ФО-20` (задания), `ФО-21` (кадры), `ФО-25` (кураторство) не собираются
**ни в одном** режиме: в режиме «сейчас» на `objectRows` отвечает только ядро и только по
своим объектам (`ADR-0181` §4), в режиме «на дату» нужного объекта нет в реестре статистики
(там их девять, и это её осознанная граница, а не недобор — `ADR-0183`).

Причины у восьмёрки разные, и мешать их нельзя. У `ФО-05`, `ФО-08`, `ФО-15`, `ФО-18` владелец
**есть**, но публичного перечня полей он не объявлял — это домашка соседа (§10 п. 14). У
`ФО-19` и `ФО-21` владельца нет в кредитном модуле вовсе: документооборот и кадры — чужие
подсистемы, и это уже спрошено у заказчика (§10 п. 1). У `ФО-25` объект «задание кураторства»
статистика **сняла** волной 12, а кураторство отказывается от заданий дословно. У `ФО-20`
случай отдельный: подсистема ТЗ **не прорабатывалась** (§10 п. 15).

Ни одна из восьми не снята и не помечена «вне области»: снятая строка читается как «такой
отчётности у нас нет» и разрешает сделать форму в Word — то самое, против чего заведён
`ИО-23`. Помеченная строка, наоборот, называет, чего не хватает и с кого спрашивать. Модуль от
этого не блокирован: недоступны ровно эти восемь форм из сорока двух живых (плюс две рабочие
бумаги волны 17 — по своей, отдельной строке долга).

## 10. Что дальше

Разделено на **вопросы заказчику** и **нашу домашку** — по правилу: спрашивать человека о том,
что можно посмотреть самим, значит тратить его ответ.

**Заказчику** — письмо `docs/voprosy-zakazchiku/2026-08-26-otchetnost.md`, семь вопросов:

1. **Отчётность наружу.** Каталог собран из Порядка №41 и легаси — оба источника внутренние.
   Форм, сдаваемых Минфину, Кабмину, Нацстаткому, банку-агенту, в нём **нет ни одной**. Это
   единственная белая зона каталога, и она же — единственный настоящий случай, на котором
   проверяется механика сдачи. Туда же строкой: кадры (`ФО-21`) и субсидии (`ФО-23`) — не наши?
2. **Спрос на девять «новых»** (`ФО-07`…`ФО-10`, `ФО-13`, `ФО-20`, `ФО-24`…`ФО-26`). Спрашивать
   надо не «нужна ли форма» (ответ всегда «да»), а что собирают руками и как часто: частота
   даёт расписание, а готовый Excel — прототип состава.
3. **Образцы пяти внесённых бланков** (`ФО-31`, `ФО-33`, `ФО-36`, `ФО-37`, `ФО-39`). Волна 12
   свёрстала их **по тексту нормы** — действующего образца ни у одного нет, и вопрос остаётся:
   образец заодно вскроет, документ ли это вообще (служебная записка о возврате материалов
   вполне может оказаться резолюцией). Тем же разговором — три вопроса, которые волна вскрыла:
   `ФО-33` уходит троим или одному (`ОЧ-62`), кто печатает записку ДПО (`ОЧ-63`), объект
   `ФО-33` — кредит или предмет залога (`ОЧ-64`).
4. **Акт сверки `ФО-34`:** считается ли норма выполненной **выпуском** акта или его
   **подписанием** (`ОЧ-59`) · чем закрывается год, если заёмщик отказался или недоступен ·
   кто ведёт факт подписания, раз отчётность его не наблюдает · **акт составляется на лицо или
   на договор** (`ОЧ-61`): норма считает заёмщиков, макет печатает по договору, и у заёмщика с
   двумя кредитами это два акта. Про источник знаменателя спрашивать нечего — снимок на
   1 января (`ADR-0165` §4); спрашивается единица счёта.
5. **Кто отвечает за сдачу `ФО-01` и `ФО-34`.** Только за этими двумя стоит календарное
   обязательство (`ADR-0164`); по остальным исполнителя называет норма.
6. **Как бланк уходит и чем это доказывается** — почта, нарочно, СЭД; где лежит квитанция; от
   какого дня идут 14 дней (`ОЧ-58`).
7. **Язык бланков** (`ОЧ-57`).

**Наша домашка:**

8. ~~**Расшифровать `ФО-22`**~~ — сделано 26.08.2026 на стенде: ОИП = орган исполнительного
   производства, форма оказалась **Видом `ФО-17`** (§9 п. 6).
9. ~~**Развести `ФО-03` / `ФО-35`**~~ — сделано 26.08.2026: «Расчет» → `ФО-35` (бланк для иска),
   «Детальный расчет» → `ФО-03` (35 колонок построчно по датам, §9 п. 3).
10. **Решить `ФО-27` / `ФО-28`** с владельцем статистики: форма или плитка дашборда.
11. ~~**Внести недостающие бланки в макет**~~ — сделано 26.08.2026 волной 12: `ФО-31`, `ФО-33`,
    `ФО-36`, `ФО-37`, `ФО-39` заведены шаблонами, получатель стал парой «вид + значение»
    (`ADR-0168`, `ИО-29`, смоук #148–#154). `ФО-32` внесена той же датой волной 11: она
    проверяла модель получателя, а не пополняла каталог (`ADR-0167`).
12. ~~**Проверить `fullCoverage` на `ФО-34`**~~ — сделано 26.08.2026 волной 9 макета: признака
    не хватило, заведён `coverBy: 'объект'` и снимок как знаменатель (`ADR-0165`, `ИО-27`,
    смоук #135–#141). `ФО-34` ушла из незаведённых, следом `ФО-32` (волна 11) и пять последних
    (волна 12): **обязательных без шаблона не осталось**, кроме `ФО-35` и `ФО-38` — обе есть
    в легаси и переносятся оттуда.
13. **Поставить требование взысканию** — **вид меры** несёт признак «печатается бланк такой-то»,
    срок остаётся экземпляром шаблона, шов `reportIssues` читает **мера** (`ADR-0164`,
    переписано `ADR-0166` §5; раздел ТЗ 18). Тем же письмом — **реквизиты исполнительного
    листа** (вид, индекс, второй индекс) и показатель «погашено в период по листу», без которых
    не собирается Вид «ОИП» (§9 п. 6), и **шов `reportIssues` ключом «лицо»** — требование
    поручителю адресовано лицу, а не договору (`ADR-0167` §5).

14. **Строка долга владельцам объектов** (`ОЧ-68`, волна 13). Восемь форм — `ФО-05`, `ФО-08`,
    `ФО-15`, `ФО-18`, `ФО-19`, `ФО-20`, `ФО-21`, `ФО-25` — не собираются ни в одном режиме.
    Требуется от соседей ровно одно: **объявить публичный перечень полей своего объекта и
    ответить `objectRows`**; всё остальное отчётность делает сама. Ни полей, ни расчёта у себя
    она не заводит — это было бы второй формулой той же величины (`ИО-2`). Долг
    **не блокирующий**: без него модуль работоспособен, недоступны ровно эти восемь форм.
15. **`ФО-20` — отдельный случай, и он не наш** (`ОЧ-69`). ТЗ объявляет **подсистему управления
    заданиями** вместе с разделом расчётных показателей по заданиям, но канона, ветки и
    владельца у неё в рабочей области нет; статистика объект «задание кураторства» **сняла**
    (`ADR-0201`), кураторство отказывается от заданий дословно. Форма помечена «модуль ТЗ не
    прорабатывался» — не «вне области»: вне области значило бы, что заказчик её не заказывал,
    а он заказывал. Поднято строкой очереди модулей.

**10. Каталог не знал двух форм, которые куратор печатает каждый день** (волна 17, 31.08.2026).
Меню «Печатные формы» карточки кредита перечисляет семь форм, и двух из них — **графика
погашения** и **акта обследования залога** — в каталоге не было вовсе. Не по недосмотру: обе не
исходящие в делопроизводственном смысле, номера серии им никто не выдаёт, срока сдачи у них нет.
При этом график **уходит заёмщику**, то есть получатель у него самый настоящий.

Развилку до сих пор обходили молчанием: `ИО-4` морозит числа названным получателем, `ИО-9`
тратит номер вместе со снимком, и пока бумаги были одной породы, из этого тихо следовало
«окончательный ⇒ исходящий». График связку разрывает. Разбор ввёл **признак «рабочая бумага»**
(`ADR-0223`): он снимает серию, номер и обязательство и не трогает заморозку. Обе формы заведены
как `ФО-42` и `ФО-43` — с признаком, с основанием «практика» и **помеченными по данным**: строк
для них в v1 никто не отдаёт.

**11. Три формы каталога были невыразимы макетом, и никто этого не проверял** (волна 17). У
`ФО-34` (акт сверки), `ФО-41` (реестр погашений) и `ФО-42` (график погашения) тело — строки, а
проверка макета 31.08.2026 (`grep` по `reports.html`) нашла в нём **только скаляры**:
`{{borrower}}`, `{{days}}`, `{{debt_main}}`, `{{dept}}`, `{{no}}`. Конструкции повторения не
существовало. Три обещанные формы — две из них обязательные по Порядку №41 — делались бы в
Word, против `ИО-23`. Введён **повторяющийся блок** (`ADR-0226`): объявляет свой объект с
отбором «принадлежит объекту бланка», отвечает `objectRows`, языка шаблонов при этом не
заводится.

**Долг соседям от волны 17** — три строки, к `ОЧ-68` не сводимые и потому названные отдельно:
ядро обязано ответить `objectRows` по **платежу** (тело `ФО-41`) и по **строке графика** (тело
`ФО-42`), залог — объявить **публичный перечень полей** (`ФО-43`). Долг **не блокирующий**, как
и `ОЧ-68`: без него недоступны ровно эти формы.

Пункт 1 остаётся **блокирующим** по `ADR-0163`: до согласования перечня справочнику форм нечем
наполниться, кроме двенадцати обязательных.
