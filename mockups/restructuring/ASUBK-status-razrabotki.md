# АСУБК · Реструктуризация — статус разработки

> Дом-статус макета `mockups/restructuring/restructuring.html`. Канон логики —
> `ASUBK-restrukturizatsiya-logika.md`. Смоук — `scripts/inspect/restructuring-check.mjs`.

## Главное на 10.08.2026: норма найдена, открытых вопросов к заказчику нет

Найдено **Положение о работе с бюджетными кредитами** — приложение к постановлению Кабинета
Министров КР от 19.01.2026 № 14, редакция 19.01.2026, глава 9 (пп. 81–96) и п. 34;
извлечение — `docs/normativ/polozhenie-2026-01-19-N14-gl9.txt`. Восемь вопросов письма от
05.08.2026 закрыты: семь — собственными решениями (**РС-26…РС-32**), восьмой — самой нормой.
Из текста нормы пришли ещё четыре решения (**РС-33…РС-37**), которых до этого некому было
задать. Тем же вечером вторым заходом закрыта последняя собственная дыра — распределение
начисленного по периодам (**РС-38…РС-41**, `ADR-0109`). Макет и смоук доведены; матрица
приёмки — §14 спеки, **54 сценария**.

Главное следствие для маршрута: решение принимает **Кабинет Министров по согласованию с
профильным комитетом Жогорку Кенеша** (п. 82), а не уполномоченный орган — конвейер
развёрнут с пяти стадий до девяти (`ADR-0107`).

## Витрина демо-данных (11.08.2026): 22 заявки вместо 7

Макет вырос с 7 демо-заявок до **22** (`RS-1008…RS-1022`, 14 новых кредитов, 3 новых
субъекта). Набор — по осям «вид × финансовое ядро»: девять заявок с зарегистрированным ДС,
четыре в работе (комитет · письмо · заключение органа · проект решения КМ), две негативные
(отказ постановлением КМ и возврат без рассмотрения). Заняты **все девять стадий**, оба
отказных исхода и **все шесть видов** справочника, включая `origin: 'иное'`. Правила не
менялись — заявки собраны существующими дверями: ДС строится через `AppSide.run` →
`CreditSide.restructureApplied` и падает на `!ok`, поэтому регрессия ядра валит загрузку
макета. Состав и разбор — §14 спеки.

Четыре жёстких упора теперь видно на данных, а не только в тексте: предел срока п. 90
(`RS-1017`), пол ставки п. 92/п. 34 (`RS-1018`), прощение без названной в акте суммы
(`RS-1019`), интервал периодов шире графика (`RS-1020`); покрытие ниже требуемого закрыто
waiver'ом (`RS-1016`). Субъектный охват — юрлица, физлицо, групповой заёмщик и кредит на
слитом ключе; на последнем и вскрылся `КЛ-1`.

## Охват статей сужен (11.08.2026): пять вместо семи

Расходы по обращению взыскания и сборы выведены из охвата реструктуризации (**РС-13а**,
`ADR-0110`). В базе переноса их нет **ни строкой** — не обнулены, а не заведены: п. 89
называет формы изменения условий поимённо, и ни одна из них этих статей не касается;
расходами владеет «Взыскание» (`ADR-0043`), сбор — цена отдельной услуги, а не долг по
договору. Канон долга (`ADR-0093`, `CONTEXT.md`, платежи, кредит, взыскание) не тронут:
у кредита статей по-прежнему семь, сужен охват одного модуля.

Следствия в севе: прощение `RS-1001` переехало со «сборов» на накопленную пеню (вид X1
прощает **санкцию** — сбор ею никогда не был), накопленная пеня стала сеяться у кредитов с
историей ДС, перенос `RS-1001` сжался 133 360 → 118 000, факт на дату ДС — 4 082 000.
Сторож ИР-2′ переставлен на `RS-1011`: у демо-заявки безставочных остатков больше нет.

## Снят шум (11.08.2026): карточка заявки

Карточка объясняла сама себя абзацами над таблицами — пересказывала колонки, излагала
устройство модели и роняла наружу внутренние адреса (`issues/02 реш. 6`, `тикет 07`,
`map.md`, `transfer_out/transfer_in`, `app.version.plan`, `restructureApplied`,
`conclusionGate`, `cr.odDays/odSum`). Правило волны то же, что в кредите: **правило живёт
там, где его нарушают** — в форме ввода, а не абзацем над таблицей, которую читают после.

**Убрано.**

- **«Охват»** — абзац про якорь-субъект, слияние ключей и то, что кредит не может быть в
  двух заявках. Первое видно строкой ИНН в шапке, второе — правилом в модалке
  «Добавить кредит», где оно и применяется. Колонка «Счётчик» переименована в
  «Реструктуризаций», из ячейки ушла подпись `реструкт.:` — заголовок её уже сказал.
- **«Заключения»** — абзац про состав заключантов и `conclusionGate`: состав виден
  таблицей, гейт — красной строкой, когда он закрыт, и пиллом `гейт «в Комитет» открыт`,
  когда открыт.
- **«База и распоряжения»** — три абзаца: «база начисления» (значение стоит в самой
  строке) · «Итог прогона» (пересказ трёх плиток под ними) · «деление — не сценарий, а
  производный транш» (устройство модели, живёт в `ADR-0096`).
- **«График и дифф»** — подпись под таблицей-черновиком (`app.version.plan`,
  `tranche.schedule`, «что сохранится при регистрации ДС») и ярлык с адресом полей
  просрочки в кредите.
- **«Оформление»** — три абзаца: пределы (правило стоит в самой строке-пределе и в
  красной строке гейта) · прощение (правило применяется при вводе суммы) · сроки
  распоряжений (то же — в модалке распоряжения).
- **«История»** — «Append-only: записи не редактируются…»: термин хранения наружу не
  нужен, а неизменяемость журнала пользователь не проверяет.
- **Хвост про залоговую комиссию и `КД-10`** в красной строке гейта обеспечения —
  адрес демо-кредита в тексте правила.

**Сокращено** (правило осталось, устройство ушло).

- Полнота пакета: абзац → красная строка с перечнем недостающего (как было) плюс пилл
  `пакет полон` у заголовка вместо зелёной плашки на всю полосу.
- Срез vs факт: → **«В учёт идёт только факт на дату вступления ДС; срез —
  согласовательная величина»**.
- Охват статей: → **«Расходов взыскания и сборов в базе нет вовсе: формы п. 89 их не
  касаются (`ADR-0110`)»** — без пересказа семи статей канона.
- Цепочка трёх актов: → **«Своих регламентных сроков у трёх актов нет — все они внутри
  70 к. д. от письма (`ADR-0107` §3)»**.
- Порог расхождения: → **«Обе ступени — настройка с историей значений, а не норма
  (РС-26)»**. Покрытие: → формула, возраст техники и страховка на новый срок (Р-15).
- Пустое состояние графика: «график транша в демо-данных не оцифрован — не факт
  отсутствия платежей» (экран не знает причины и не выдаёт её за факт).

**Оставлено сознательно.** Все красные строки гейтов — они блокирующие и называют
причину · плашки состояний (пауза мер, группа и риск-категория при вступлении:
последствия, из данных не видные) · «Ответ Обеспечения (`ADR-0098`) — зеркало, не
собственный расчёт» (замок, а не пояснение) · правила в модалках ввода — там их место.

**Проверено** 2026-08-11: `node scripts/inspect/restructuring-check.mjs` — **57/57 PASS**;
прогон в браузере `node scripts/inspect/restructuring-cleanup-shot.mjs` (8 вкладок ×
`RS-1001` · `RS-1004` · `RS-1017` · `RS-1020`): подсказок **19 против 47**, текста
подсказок и плашек **3 729 знаков против 12 590** (−70 %), ошибок консоли нет. Все семь
модалок (новая заявка, добавить кредит, заключант, waiver покрытия, регистрация ДС, новый
вид, зеркало взыскания) перечитаны с новыми формулировками.

## Переборка вкладки «База и распоряжения» (12.08.2026): стол ввода

Вкладка держала четыре работы разом — ввод (галки и распоряжения), сверку двух дат,
разбор гейтов и показ производного транша, — и с приходом многорасчётности (РС-42) росла
стопкой: по секции на расчёт, и в каждой секции ещё раз печатались **гейты заявки** и
итоги. Диагноз волны: вкладка — **стол ввода одного расчёта**, всё остальное с неё уходит.

**Что уехало.**

- **Разбор гейтов** (`gateDetails`) — из тела вкладки под шапку карточки, в раскрытие
  чипов (`gatesPanel`, `RS.toggleGates`). Гейты считаются **по заявке**, а печатались по
  разу на расчёт плюс копия на соседней вкладке «График и дифф» — на трёх расчётах один и
  тот же разбор шёл трижды на самой вкладке и ещё раз рядом. Плитка «Гейты» в шапке теперь
  раскрывает разбор на месте, а не уводит на вкладку.
- **Производный транш** (коробки «источник → производный») — на «График и дифф»: это
  **следствие** регистрации, а не поле ввода, и читается вместе с графиком.
- **«Итог прогона»** — снят целиком: три плитки повторяли числа, которые теперь стоят
  подытогом таблицы и полосой итога под ней.

**Что осталось и как встало.**

- **Полоса выбора расчёта** вместо стопки секций: на экране один расчёт, адрес суммы
  (транш · кредит) виден кнопкой, а не только в аргументах сеттеров. При одном расчёте
  полоса не рисуется. Она же — готовая форма для `P17-R2` (регистрация ДС по расчёту).
- **Срез и факт — колонки одной строки**, а не две плитки-итога над таблицей: это одна и
  та же разбивка по статьям на двух датах, и вопрос «чем набежало между письмом и ДС»
  получает ответ построчно, без клика. Клетка, которой на дате нет, несёт прочерк с
  причиной, а не ноль: ноль читается как «было и погасили». Строки, дожившие до среза и
  погашенные к факту, показаны отдельным хвостом — иначе колонка «Срез» тихо не сходилась
  бы со своим подытогом.
- **Пять колонок**: статья · срочность (с подписью «начисляется: …») · Срез · Факт ·
  Включена · Распоряжение. Начисление на статью — свойство справочника, одинаковое во всех
  заявках, поэтому оно подпись у названия, а не столбец на десять строк.
- **Итог — в `<tfoot>` и полосой сразу под таблицей**: подытоги колонок стоят под своими
  колонками, а следствие правки («Прощено · Кап. процентов · Кап. пени · Тело переноса ·
  Перенос, всего») — вплотную к ним, той же полосой в потоке (липкой к низу экрана она была
  до 14.08.2026 и накрывала форму условий с кнопкой расчёта — см. журнал того дня).
  Нулевые распоряжения не печатаются.
  Суммы **через расчёты** складываются ровно в одном месте — свёрнутый «Итог по заявке»,
  и он подписан: такой суммы нет ни в одном акте, у каждого расчёта своё ДС.
- **Расхождение (РС-26) стало четвёртым чипом** в шапке и красной/жёлтой строкой над
  таблицей: причина расхождения лежит в колонках «Срез» и «Факт», гнать за ней в раскрытие
  чипа значит прятать число, стоящее перед глазами. У расчёта с **зарегистрированным** ДС
  ступень 1 не печатается: виза требовалась до регистрации, требовать её задним числом —
  выдавать прошлое за незакрытое дело.

**Таблица помещается в экран.** Общий для таблиц `nowrap` распирал долг расчёта до 1145 px —
на 1440 и уже карточка резала колонку «Распоряжение», то самое поле ввода, ради которого на
вкладку заходят, и его приходилось выкручивать горизонтальной прокруткой. Правило: числа не
переносятся никогда, подписи переносятся всегда; раскладка фиксированная, ширины долями
(29 · 14 · 18 · 9 · 30 %; последняя доля выросла с 26 % под кнопку «×» делимой позиции,
см. ниже) — иначе список распоряжений держал ширину по самому длинному пункту
и съедал колонку статьи. Заодно полоса вкладок переносится на вторую строку вместо прокрутки:
на 1280 «Оформление» и «История» уезжали за край. Обход 8 вкладок × 4 заявок на 1280 не даёт
ни одного режущегося узла (на 1152 остаётся только многоточие в самом списке распоряжений).

**Подписи срезаны до тех, что несут новое.** Ушли: «без изменений с <дата>» (стояло на
большинстве строк и повторяло два равных числа по соседству), «строка снята — распоряжаться
нечем» (пустая клетка говорит это сама), «позиция покрыта распоряжением целиком» (нечего
делать — нечего и подписывать), правило про охват статей (ADR-0110: рассказывало про то,
чего на экране нет вовсе — ему место в спеке), длинные хвосты у «сроков нет» и у списка
начислений. Осталось то, что объясняет **пустое место** («срез не зафиксирован», «появилась
после среза», «погашена к <дата>») и то, что несёт число («без распоряжения остаётся X»).
Мерка обходом 8 вкладок × 4 заявок (`scripts/inspect/restructuring-cleanup-shot.mjs`):
подсказок 15 → 13, знаков 3979 → 3429; на самой вкладке ярлыков 7 → 4 (`RS-1001`).

**Позиция делится на несколько распоряжений** (`РС-43`, 12.08.2026). Разбор экрана упёрся в
модель: п. 89 перечисляет формы через «и/или», заявка вправе нести несколько видов, и акт
«200 000 простить, 250 000 капитализировать, 100 000 распределить по периодам» — обычный,
а не выдуманный случай. Одна строка выбора на позицию вынуждала бы потерять две части акта
либо завести три заявки на один кредит, и остаток при этом молча уезжал в «без распоряжения».
Теперь на паре (статья, срочность) живёт список линий: у каждой свой вид, сумма и сроки, у
каждой своя дверь с потолком «позиция минус прочие линии». Свободный остаток называется числом
один раз, под последней дверью. Границы держат **отказы, а не подрезка**: сумма сверх
свободного остатка отбивается с его названием, повтор вида без собственных сроков (второе
«прощение» на строке) — тоже, а повтор капитализации пени принимается: у неё сроки свои.
Кнопка «+ распоряжение на X» появляется только там, где деление уже началось — на нетронутой
позиции вид выбирается в стоящем селекте, звать делить нераспределённое значит предлагать
лишний шаг. Порядок линий на экране исхода не меняет: очерёдность держит конвейер, не список.
Витрина получила `RS-1026` (три вида, неустойка 600 000 на три части и свободные 50 000).

**Термины разведены** (`CONTEXT.md`). Слово «база» несло четыре смысла разом; теперь:
**База переноса** — сумма включённых строк (и только она) · **Долг расчёта** — весь
список строк · **начисляется** — свойство статьи · **Тело переноса** — часть переноса под
ставкой. У «Срез долга» в `_Avoid_` дописано «база».

**Дефект КЛ-2 стал виден на экране.** Колонка «Факт» несёт долг расчёта **до** переноса, а
«Оформление» печатает под тем же словом остаток транша **после** переноса (`fixFact`
зовётся после `restructureApplied`). Пока КЛ-2 не починен, колонка подписывает, какое из
двух чисел показывает; чинить его — работа модели, не раскладки.

**Проверено** 2026-08-12: `node scripts/inspect/restructuring-check.mjs` — **71/71 PASS**;
прогон в браузере (8 вкладок × `RS-1001` · `RS-1004` · `RS-1017` · `RS-1020`, отдельно
`RS-1026`) без ошибок консоли и без режущихся узлов на 1280 и 1440. Точечно: на `RS-1026`
клетка «Распоряжение» несёт три селекта и кнопку остатка, под строкой три двери с потолками
250 000 / 300 000 / 150 000, оговорка «черновой график ещё не рассчитан» печатается один раз
на позицию, а не по разу на дверь. На `RS-1020` полоса переключает подытог «База переноса» 1 245 760 →
398 076 (не залипает на первом расчёте), «Итог по заявке» сходится с суммой расчётов;
на `RS-1001` коробки производного транша на столе ввода нет, а на «Графике и диффе» их
две; разбор гейтов не печатается ни на одной из двух вкладок расчёта и держит все четыре
`<details>` под шапкой.

## Охват перестал быть вкладкой (13.08.2026): полоса расчётов

Запрос был мягкий — «найти другое название вкладке “Охват”»: слово в модуле занято дважды
(охват статей `ADR-0110`, субъектный охват сева), и заголовок спорил сам с собой. Разбор
показал, что переименование лечит не то.

**Почему вкладка ушла, а не переименовалась.**

- **Дубль списка.** Вкладка печатала транш · кредит · базу переноса · судьбу ДС — ровно то,
  что уже несла полоса выбора расчёта на «Базе и распоряжениях». Два списка одного и того же
  расходятся по определению: править приходилось оба.
- **Расхождение с моделью.** Вкладка читала `app.creditIds` — производное свойство с откатом
  на демо-сид, тогда как охват по РС-2 живёт в `app.calcs` (один расчёт на транш, ИР-16).
  Написанные по модели `scopeTranches` и `removeTrancheFromScope` не звались ниоткуда:
  снять транш с экрана было нельзя вовсе.
- **Дыра гейта.** «Анализ → комитет» мерил `creditIds.length`, а тот из-за сид-отката непуст
  и на заявке с нулём расчётов — комитету показывали кредит, для которого ничего не считали.
  Теперь гейт меряет `calcs`.

**Как встало.** Вкладок семь вместо восьми. Полоса охвата стоит над панелью на обеих вкладках
расчёта — «База и распоряжения» и «График и дифф», — и рисуется **всегда**, в том числе при
одном расчёте: иначе у однотраншевой заявки нет входа ни в «+ Добавить транш», ни в снятие.
На строку переехал скрининг, ради которого вкладка и жила: просрочка (дни · сумма), чип
«другая активная заявка» (ИР-1), ссылка в зеркало взыскания, счётчик прежних реструктуризаций;
остаток и сводка по заявке не дублируются — они в плитках шапки. Снятие транша уносит расчёт
с руками введёнными суммами, поэтому идёт **через подтверждение**, называющее транш, кредит и
базу переноса; у расчёта с зарегистрированным ДС крестика нет вовсе — запрет показан
отсутствием двери, а не дверью, которая всегда отвечает «нельзя». На закрытой стадии полоса
только читается. Пустой охват открытой заявки печатает «Охват пуст — добавьте транш, и расчёт
заведётся автоматически» и держит кнопку добавления.

**Проверено** 2026-08-13: `node scripts/inspect/restructuring-check.mjs` — **72/72 PASS**
(новый **#72** и переписанные **#66**/**#68**). В браузере: вкладок семь, «Охвата» среди них
нет; на `RS-1020` полоса несёт два транша на обеих вкладках расчёта и переключает подытог
«База переноса» 1 245 760 → 398 076; на `RS-1001` (ДС-60540/2) крестика нет, добавление есть;
на закрытых `RS-1002`/`RS-1014` полоса только читается; снятие второго транша `RS-1020` даёт
модалку «Вывести транш из охвата», расчётов 2 → 1, `curCalc` сбрасывается в `null`, а
`creditIds` сжимаются до одного кредита следом. Обход 7 вкладок × 4 заявок
(`restructuring-cleanup-shot.mjs`, `TABS` 8 → 7): подсказок 13, знаков 3 429 — как и до волны,
вкладка подсказок не несла; ошибок консоли нет, на 1280 не режется ни один узел.
Из `REAL_TABS` прототипа `prototype-05-vkladka-rascheta.html` вкладка снята тем же заходом.

## Вкладки расчёта названы по сделке (13.08.2026): «Параметры» и «Было и стало»

> Имя второй вкладки прожило один день: тем же числом она стала **«Результатом расчёта»**, а
> две половины — тремя блоками. Раздел оставлен как запись хода; действующая раскладка — ниже,
> «Было и стало» → «Результат расчёта» (РС-45). Всё, что тут сказано про **«Параметры»**,
> в силе.

Следом за охватом пересмотрены подписи самой пары. Реструктуризация — изменение структуры
кредита: куратор задаёт **параметры изменения** и получает **старую и новую версию**. Прежние
«База и распоряжения» / «График и дифф» называли куски экрана, а не предмет, и спорили с
собственным словарём: «база» в модуле занята одним смыслом («База переноса» — только включённая
часть долга, `CONTEXT.md`), а «дифф» — латинский обрубок в русском ряду вкладок. Пара названа
по канонической статье «Расчёт реструктуризации» — предлагаемая сделка целиком: срез долга,
распоряжения начисленным, параметры новых условий, черновой график и разница «было / стало».

**Ввод собран в одном месте.** Форма условий (срок · ставка · периодичность · метод) и кнопка
предварительного расчёта переехали с графика на **«Параметры»** — под таблицу долга расчёта.
Порядок чтения повторяет порядок сделки: что переносим → как распоряжаемся → на каких условиях
→ пересчитать. Прежде ввод жил на двух вкладках, причём форма стояла рядом со своим же исходом,
а ставку и срок норма меряет **от базы переноса** (пределы пп. 34, 90, 92) — от таблицы, которая
теперь тут же перед глазами. Кнопка расчёта переводит на «Было и стало»: исход нажатия не на том
экране, где нажимают, и без перевода нажатие читалось бы как отказ.

**«Было и стало» стало только показом** — ни одного поля ввода. Две половины вместо трёх
разнородных секций: **«Было»** — исходный транш (остаток, условия, статус) и его действующий
график; **«Стало»** — производный транш, черновой график и строка РС-15 о непокрытой просрочке.
Сетка `split-wrap` со стрелкой распущена: сопоставление несут сами половины, а прежде оно
печаталось дважды — сначала парой графиков, потом парой коробок под стрелкой, — и «действующий»
уходил третьей таблицей под форму условий, ниже своего же результата.

Переименованы и слаги состояния (`calcBase`/`calcSched` → `params`/`diff`), и функции
(`pCalcBase`/`calcBaseSection` → `pParams`/`paramsSection`, `pCalcSched`/`calcSchedSection` →
`pDiff`/`diffSection`). Дверь модели `calcBaseOf` имени не меняла: она про долг расчёта, а не
про вкладку.

**Проверено** 2026-08-13: `node scripts/inspect/restructuring-check.mjs` — **72/72 PASS**
(**#66** сторожит, что `versionParamsBlock` зовётся из `paramsSection` и не зовётся из
`diffSection`; **#68** добавил «ввод только на Параметрах» — на «Было и стало» нет ни одного
`input`/`select`, и берёт график производного из половины «Стало»). В браузере (1280):
вкладки «Пакет документов · Заключения · **Параметры** · **Было и стало** · Комитет и решение ·
Оформление · История»; на «Параметрах» два заголовка — «Долг расчёта — статьи и распоряжения»
и «Условия чернового графика», 12 полей ввода; нажатие «Предварительный расчёт» переводит
`curTab` в `diff`, где полей ввода 0; на `RS-1001` порядок читается «Было → исходный
КД-60540-Т1 → его график → Стало → производный КД-60540-Т2 → черновой график»; полоса охвата
на месте на обеих вкладках; ошибок консоли нет, на 1280 не режется ни один узел.

## Условия ведутся зоной, черновик считается движком кредита (13.08.2026)

Форма условий давала править **четыре** параметра — срок · ставка · периодичность · метод —
из двенадцати, закреплённых за конвейером `ADR-0111` (зона «формы п. 89»). Пробел оказался не
в удобстве:

- **два вида из пяти нечем оформить.** N3 «Предоставление или изменение сроков льготного
  периода» и N5 «Отсрочка исполнения» определены **льготой**, а поля льготы в форме не было
  вовсе. Справочник видов их объявлял, конвейер их не считал.
- **черновик считался другим движком, чем контракт.** Демо-`amortize` мерил месяц ровно в
  30 дней, знал две льготы из трёх и свой словарь значений («дифференцированный», «раз в
  полугодие»); кредитный модуль строит график `buildSchedule` — календарные даты, день платежа,
  привязка последнего платежа, три льготы, дневное начисление по `dayMethod`. Приложение к ДС
  есть контракт (РС-9): комитет утверждал график, который кредит после регистрации ДС не
  воспроизводил.
- **вид не фильтровал форму.** `allowedParams` был посчитан и экспортирован, но форма его не
  спрашивала: у вида N2 (разрешена одна ставка) правились срок, периодичность и метод.
- **словарь разошёлся со швом.** ДС уезжало плоским `ds.params` с ключами `schedule`/`repayMethod`,
  а `restructureApplied` кредита ждёт массив `conditions:[{param,value}]` в вокабуляре реестра.

**Как встало (РС-44).** Механика `buildSchedule` перенесена в расчёт целиком — тогда все
двенадцать ключей падают в форму почти даром, а черновик становится тем же расчётом, что
построит кредит. Форма строится **циклом по реестру** и фильтруется `allowedParams`:
неразрешённые видом поля печатаются read-only с «вид не разрешает», и запрет держится в модели
(`setVersionParam` молча отвергает чужой ключ), а не в разметке. Подполя условны, как в кредите:
месяцы платежей — только при «по конкретным месяцам», окно раскладки — только при льготе по %.
Зона «вне п. 89» (`penaltyMain · penaltyInt · reserveRate · dayMethod · earlyRepayMoratorium ·
earlyRepayFee`) в форму **не пущена** — у неё свой маршрут, прямое ДС «прочее изменение условий»
(гейт Г-23). Распоряжения (`capInterest`/`capPenalty`/`forgive`) из словаря условий убраны:
вторая дверь к ним спорила бы с РС-28. `payMonths` **хранится и не считается** — при неравных
промежутках график не формула, а ручной ввод; на экране честная плашка, а не молчание.

**Три дефекта, которые прежняя механика прятала.** `validateDS` мерил конец срока как
`term × 30` дней — отверг бы любой график длиннее года; `newMaturity` тем же шагом считал дату
погашения для гейта обеспечения; запись условий производного транша **датировалась днём выдачи
источника** — свёртка `conditionsAt` несла свой `effectiveFrom` и, стоя последней в спреде,
затирала дату соглашения. Капитализация процентов в льготный период (`morCap`) снята вместе с
блоками льгот — она спорила с ИР-11/РС-27 и обязана идти явным распоряжением `cap`.

**Проверено** 2026-08-13: `node scripts/inspect/restructuring-check.mjs` — **74/74 PASS**
(новые **#73**/**#74**, движковые **#24–#29** переписаны). Новый
`node scripts/inspect/cross-engine-check.mjs` гоняет оба макета в `node:vm` и сверяет
`RS.amortize` с `CR.buildSchedule` строка в строку на 13 наборах условий — **13/13 совпало**.
В браузере: на заявке вида N2 полей условий девять, редактируется одна ставка, восемь несут
«вид не разрешает»; на `RS-1017` три льготы дают график 96 строк с фазами «льгота по %» →
«льгота по ОД» → обычные периоды, Σ тела = база 701 306,83; `payDay=15` даёт все даты 15-го
числа (2026-08-15 … 2034-07-15); «по конкретным месяцам» поднимает поле и плашку; на `RS-1001`
(ДС зарегистрировано) форма закрыта целиком; ошибок консоли нет, на 1280 не режется.

## Сайдбар — канон дерева модулей (13.08.2026)

Реструктуризация была единственным макетом со своим сайдбаром: плоский список из двух пунктов
с иконками, без дерева, без бургера. Восемь макетов (`credit.html`, `collection.html`,
`zalog.html`, `org-structure.html`, `loan-program.html`, `loan-application.html`,
`decisions.html`, `commission.html`) несут один канон — `TREE` → `renderNav()`, три уровня
`.nav-group` → `.nav-sub` → `.nav-item`, чеврон 14/13 px, подвал «[admin] + выход».

Расхождение было не косметическим: **модуль не показывал, что вокруг него есть система**.
`credit.html` уже объявляет «Проблемная задолженность → Реструктуризация · Взыскание» — соседи
о модуле знают, а он о них нет; с экрана не видно ни куда вернуться, ни где стоит взыскание,
с которым у реструктуризации швы (РС-16, РС-21, РС-22).

**Как встало.** Дерево из семи групп; свои экраны — листья «Проблемной задолженности»
(«Реструктуризация — реестр заявок», «Справочник видов»), рядом «Взыскание». Чужой пункт даёт
тост «вне объёма макета» (`credit.html`), подвал канонический. Сохранена **динамическая
подсветка**: активен лист текущего экрана, на карточке заявки горит её реестр — статичный флаг
канона это терял. Добавлены бургер в шапке (клавиатурный вход, `aria-expanded`) и персист
свёрнутости в `localStorage` (`asubk.restructuring.navCollapsed`) — как в `collection.html`.
Раскрытость узлов держится **вне DOM**, в `NAV_OPEN`: `render()` перерисовывает `#nav` на
каждое действие, и DOM-переключатель канона схлопывал бы то, что раскрыл пользователь
(приём `payments.html`).

**Проверено** 2026-08-13: `node scripts/inspect/restructuring-check.mjs` — **75/75 PASS**
(**#75**: дерево несёт группу `pz` и «Взыскание», листья со `view` покрывают ровно `list`/`dict`,
свой лист переводит экран, чужой — нет, раскрытость переживает перерисовку, бургер без DOM не
падает; плюс сторож на разметку — `.nav-item` блочный с отступом 52/40, `margin-left:-26px`
в файле нет). В браузере (1280): групп 7, раскрыта «Проблемная задолженность», активен «…реестр
заявок»; переход в «Справочник видов» переносит подсветку, открытие карточки — нет; клик
«Кредиты» даёт тост и вида не меняет; раскрытая «Система кредитования» переживает фильтр
реестра; бургер прячет сайдбар и держит `aria-expanded=false` после `reload()`; ошибок консоли
нет, не режется.

## «Было и стало» → «Результат расчёта» (13.08.2026): сравнение стало таблицей

Имя, данное вкладке утром того же дня, продержалось один день. Оно ломалось трижды, и все три
раза — по одной причине: **называло форму показа, а не предмет** — ровно та ошибка, которую
эта же волна чинила у «Базы и распоряжений» и «Графика и диффа».

- **Половины сравнивали разные объекты.** «Было» — транш-источник, который переносом
  закрывается (или остаётся срочным остатком); «Стало» — производный транш, рождающийся датой
  вступления ДС. Их графики построчно **несопоставимы**, а стопка из двух таблиц предлагала
  сопоставление, которого в данных нет.
- **Настоящего «было/стало» на вкладке не было вовсе.** П. 89 меняет **условия** — двенадцать
  ключей зоны `ADR-0111`. Действующее значение печаталось одной строкой «Условия 12 %, 60 мес.»
  в коробке источника, форма на «Параметрах» показывала только новое. Сравнить срок, льготу,
  метод, отсрочку, день платежа куратору было **негде** — при том что имя обещало сравнение.
- **Два числа половин не сходились ничем.** «Остаток» у источника и «Перенесённая база» у
  производного стояли в соседних коробках, и чем первое превращается во второе, экран молчал.

Плюс дефект дат, который прежняя раскладка прятала: источник читался **на `TODAY`**
(`balanceAt`/`conditionsAt`/`scheduleAt`), а прогон конвейера в той же функции — **на дате
вступления ДС**. Деньги и условия стояли на разных датах и назывались одним словом «было».

**Как встало (РС-45).** Вкладка зовётся **«Результат расчёта»**: предмет её — то, что расчёт
даёт, а сравнение живёт **внутри, колонками**, а не раскладкой экрана. Три блока в порядке
самой сделки:

1. **Условия — действует и по расчёту.** Таблица по реестру `PARAMS`: `Параметр · Действует ·
   По расчёту · Δ`. «Действует» — `conditionsAt(src, D)` (запись условий транша несёт все
   двенадцать ключей); «По расчёту» — до регистрации ДС черновик расчёта с пиллом «Черновик»,
   после — `conditionsAt(derived, D)` с номером ДС, то есть **то, что реально доехало** через
   `restructureApplied`. Видимость подполей — тем же `showIf`, что в форме, чтобы таблица и
   форма не расходились. Движение стоит наверху, неизменное — за раскрытием: двенадцать ключей
   целиком рассказывали бы в основном про то, что не менялось. Пустое «По расчёту» — **не
   обнуление**: молчание соглашения переносит условие как есть (`toConditions` отсеивает
   пустые), и такая строка стоит среди неизменных.
2. **Деньги — что уходит в перенос.** Одна лента вместо пары коробок:
   `остаток источника на D → база переноса → (− прощено) → Перенос, всего → остаток после
   переноса`. Держит обе арифметики разом: `база − прощено = перенос` (капитализация сумму
   **не меняет** — она перекладывает статью, ИР-2/РС-39; состав переноса подписан под числом:
   «тело … · безставочные …») и `остаток до − перенос = остаток после` (`transfer_out`, ИР-3).
3. **Графики.** Производный со своим итогом «Перенос, всего»; действующий график источника
   **остался открытым** (решение пользователя), но получил итог по всему графику, фильтр строк
   по `D` вместо `TODAY` и подпись судьбы («закрывается переносом» / «переносом не тронут»).
   Под ним — прямая оговорка, что построчно графики не сопоставляются. Строка РС-15 о непокрытой
   просрочке осталась при производном: это свойство результата.

**Одна дата на весь показ.** Дверь `calcEffectiveDate(app, calc)`: факт расчёта → дата
соглашения расчёта → ДС заявки → сегодня. Дата **печатается** — в подписи секции («на дату
вступления ДС-60540/2: 2026-07-19» либо «ДС не зарегистрировано — предпросмотр на …») и в шапке
колонки «Действует — 2026-07-19», а не подразумевается.

Слаг вкладки `diff` → `result`, функции `pDiff`/`diffSection` → `pResult`/`resultSection`. Полоса
на «Параметрах» переименована из «Результат распоряжений» в **«Итог распоряжений»**
(`calcResultBar` → `dispositionsBar`): два «результата» на соседних вкладках — тот же дубль,
что чинили у охвата. CSS `.split-box` снят целиком.

> Через несколько часов того же дня показ разобран по мерке и почищен — **РС-46**, следующий
> раздел. Оттуда: дата осталась только в шапке колонки, подписи секций и оговорка под графиками
> сняты, лента зовёт роли величин. Читать этот блок как «что показывается», следующий — «сколько
> раз и какого размера».

**Проверено** 2026-08-13: `node scripts/inspect/restructuring-check.mjs` — **78/78 PASS**
(новые **#76** одна дата на показ — в коде `resultSection` нет ни одного `TODAY`, и сдвиг даты
соглашения на 2026-09-01 двигает весь показ; **#77** таблица условий сходится и с формой
(9 строк = видимые по `showIf` поля), и с моделью — до ДС графа равна черновику, после равна
условиям производного транша; **#78** денежная лента сходится обеими арифметиками и равна
`run().transferSum`; перецелены **#66**/**#68**). Обход 7 вкладок × 4 заявок
(`restructuring-cleanup-shot.mjs`) — ошибок консоли нет. В браузере (1280): на `RS-1001`
таблица печатает «36 → 60, +24 мес.» и «8 → 6,5, −1,5 п.п.», семь неизменных под раскрытием,
лента `4 200 000 → 133 360 − 15 360 = 118 000 → 4 082 000`; на `RS-1004` (ДС нет) пилл
«Черновик» и подпись «предпросмотр»; на `RS-1020` полоса охвата переключает расчёт; полей ввода
на вкладке **0**, горизонтальной прокрутки нет, не режется ни один узел.

**Найден дефект макета КЛ-3 (не исправлен этой волной).** Черновик расчёта сеется
`draftVersion(cr, overrides)` — **из условий кредита**, а не из условий транша-источника. На
исходном транше это одно и то же, на **производном** — нет: расчёт, заведённый на
`КД-60540-Т2` (условия ДС: 60 мес., 6,5 %), получает черновик `36 мес., 8 %` — терму кредита
(проверено 2026-08-13 прогоном в `node:vm`: `addTrancheToScope` на Т2 даёт
`{term:36, rate:8}` при `conditionsAt(Т2) = {term:60, rate:6.5}`). Прежде это было незаметно —
новое значение печаталось в одиночку; теперь таблица сравнения показала бы **выдуманное
движение** «60 → 36, −24 мес.», которого никто не задавал. Ни одна демо-заявка сюда не попадает
(все черновики стоят на исходных траншах), поэтому смоук молчит. Починка — сеять черновик из
`conditionsAt(src, D)`; трогает восемь мест вызова и семантику модели, поэтому вынесена из
волны показа.

## Чистка «Результата расчёта» (13.08.2026): мерка вместо глазомера

Вкладка, собранная тем же днём, разобрана заново — но не на глаз, а **по мерке**. Скрипт снял с
неё на четырёх демо-заявках весь текст, все подсказки, плашки и ярлыки, счёт таблиц, строк и
высоту. Картина: `RS-1001` — 1 672 знака, 1 142 px, четыре таблицы, 16 строк; `RS-1020` —
2 123 знака, 1 351 px. Треть текста — **рассказ вкладки о самой себе**, а три факта печатались
по три раза каждый.

- **Дата показа — трижды.** Подписью секции («Сравнение и суммы — на дату вступления
  ДС-60540/2: 2026-07-19»), шапкой колонки «Действует» и подписью денежной ленты. Три
  экземпляра одного числа расходятся молча, и куратор поверит любому.
- **Имя транша и его судьба — трижды.** Заголовком секции, шапкой внутри таблицы и чипом полосы
  охвата.
- **Колонка Δ держала три разных языка.** Дельту (`+36 мес.`), слово («изменено») и целую фразу
  («не названо — остаётся прежним»). Колонка с заголовком «Δ» не может быть то числом, то
  рассказом.
- **Пустые колонки утверждали ноль.** График переноса накопленного состоит из безставочных
  статей: «Тело» и «%» стояли в нём сорока восемью нулями подряд. Колонка нулей говорит «эта
  величина есть и равна нулю», тогда как её просто **нет**.
- **Растянутые таблицы.** График на трёх колонках занимал всю ширину карточки, оставляя между
  «Датой» и числами пустую полосу в пол-экрана; при этом длинные статейные заголовки
  («Накопленные проценты») печатались **поверх** соседа.
- **Усечение сообщалось, а не раскрывалось.** Ярлык «показаны первые 6 из 60» под таблицей
  ничего не открывал.
- **Полоса охвата на вкладке ввода не имела**, но несла кнопки ввода — «+ Добавить транш» и
  крестики снятия, при том что вкладка объявлена смотровой.

**Как встало (РС-46).** Двадцать два решения одной волной, все — об одном: **каждый факт
печатается ровно один раз и занимает столько места, сколько в нём содержания**.

| Было | Стало |
|------|-------|
| Дата в трёх местах | Только шапкой колонки «Действует» — вплотную к значениям, снятым на неё |
| Имя транша и судьба в трёх местах | Имя — заголовком своего графика, судьба — пиллом рядом с именем |
| Полоса охвата с чипами и кнопками | Переключатель расчёта: имя, кредит, база переноса, состояние ДС. Ввод — на «Параметрах» |
| Δ: число, слово или фраза | Δ: только число. Характер значения — подписью под самим значением в графе «По расчёту» |
| Две таблицы условий (вторая за раскрытием, с повтором заголовков) | Одна таблица; неизменные добиваются строкой раскрытия |
| Колонки «Тело»/«%» из одних нулей | Пустая колонка не рисуется — ни статейная, ни «Тело»/«%» |
| Таблица во всю ширину карточки | 130 px на колонку, потолок 920, прокрутка внутри рамки; с условиями роднится левым краем, а не сеткой |
| Ярлык «показаны первые 6 из 60» | Строка таблицы «Показать все 60» / «Свернуть»; раскрытость — в `state.expand`, не в DOM |
| «Сравнение и суммы», «Графики» | «Условия» · «Деньги» · «График производного КД-…-Т2 · Действующий» / «График по расчёту · Черновик» |
| Лента денег звала объекты («у источника») и печатала id прогона | Лента зовёт роли: «Остаток до переноса» → «База переноса» → «Перенос по этому расчёту» → «Остаток после переноса»; id снят |
| Подписи-инструкции, плашки «ОК», коды `ADR`/`РС`/`ИР` на экране | Вкладка молчит о себе. Единственная оговорка — о непогашенной после ДС просрочке (РС-15): она про деньги, а не про показ |

**Мерка после.** `RS-1001` — 776 знаков (−54 %), 940 px, две таблицы, 10 строк; `RS-1004` —
876 (−49 %); `RS-1017` — 930 (−48 %); `RS-1020` — 1 272 (−40 %), 1 252 px, три таблицы. Ошибок
консоли нет ни на одной.

**Проверено** 2026-08-13: `node scripts/inspect/restructuring-check.mjs` — **79/79 PASS**. Новый
**#79** держит геометрию и тишину разом: ни одной мёртвой колонки · ширина таблицы ровно
`130 × колонок` (пустой график источника засчитывается схлопнутым в строку) · усечение
раскрывается и сворачивается · имя каждого транша встречается в теле вкладки ровно один раз, а
пиллы стоят только у заголовков графиков · в теле нет ни `section-note`, ни позитивных плашек,
ни внутренних кодов. Перецелены **#66** (полоса read-only, ввод только на «Параметрах»),
**#68** (лента денег и раскрытие графика), **#76** (дата ровно в одном месте — счётчиком
вхождений, а не просто наличием), **#77** (единственный заголовок таблицы условий, Δ только
число, молчание соглашения ≠ обнуление), **#78** (ленту читает по ролям величин).

## Финансовое ядро чинено (14.08.2026): перенос гасит вошедшее, факт снимается до сделки

Началось с жалобы «на `RS-1005` не сходится калькуляция: в базе 107 700, а переносим
1 575 000». Разбор показал не описку раскладки, а **шесть сцепленных дефектов**, из которых
пять — модель и один — необъявленный пробел. Чинены все.

| Что было | Что стало |
|---|---|
| `restructureApplied` списывала со счёта тела **сумму переноса целиком**, чем бы та ни была собрана: на `RS-1001` перенос из одних начисленных процентов и пени двигал тело 4 200 000 → 4 082 000 — основной долг гасился за чужие проценты | Дверь списывает **взятые строки** долга источника; из остатка уходит `transfer_out` только на обеспеченную остатком часть базы, прощённое — своей операцией `forgive_out` (**РС-7б**, **ИР-17**) |
| Транш закрывался по одному нулю остатка — при живых строках начисленного | Закрытие требует и нулевого остатка, и отсутствия строк долга |
| `fixFact` звался **после** двери во всех четырёх местах регистрации ДС (`КЛ-2`); дрейф РС-26 на цепочке `RS-1005…1007` показывал 50–100 % и мерил сам перенос | Порядок обращён (**ИР-18**), `appDrift` читает зафиксированный снимок; дрейф цепочки — 0 %, `КЛ-2` закрыт |
| Лента денег склеивала **живой пересчёт** с зафиксированным фактом: «база 107 700 = перенос 1 575 000» — числа двух эпох через знак равенства; подпись «тело 0 · безставочные 1 575 000» стояла под графиком, где всё было телом | Состав ДС хранится реквизитом соглашения (`taken`, `forgivenRows`, `principalPart`, `baseSum`) и читается оттуда; лента разведена на две арифметики — **состав** (база − прощено = перенос) и **источник** (остаток до − ушло = остаток после) |
| Цепочка `RS-1005…1007` сеялась мимо базы и распоряжений — `transferSum` клался числом `balanceAt/2`, поэтому «Параметры» и «Результат расчёта» показывали разные сделки | Оба сева — цепочка и вторая волна — идут теми же дверями, что живой куратор: база → `run()` → `restructureApplied` |
| Частичный перенос был **необъявленной подменой**: §14 п. 8 требовал половину тела, а записать её было нечем — строка базы значила «да/нет» | Взятая часть живёт на строке (`take`/`takeOf`), поле стоит в клетке «Включена», подытог «База переноса» считает взятое; отказы с обеих сторон — выше остатка позиции и ниже занятого распоряжениями (**РС-7а**) |

**Проверено** 2026-08-14: `node scripts/inspect/restructuring-check.mjs` — **81/81 PASS**.
Новый **#81** — сводка по всей витрине: 24 расчёта на 26 заявках, на каждом сходятся подытог
базы, обе арифметики ленты и число сделки (реквизит соглашения либо прогон конвейера). Именно
её отсутствие и стоило жалобы: места проверялись порознь и расходились между собой.
Новый **#80** держит частичный перенос четырьмя глазами разом (модель — перенос падает ровно
на невзятое; источник — невзятая половина остаётся на `T1` и не даёт его закрыть; экран —
поле, названный остаток и подытог по взятому; оба потолка). Перецелены **#13** (факт `RS-1001`
= 4 200 000 вместо пост-переносных 4 082 000), **#14** (фикстура переехала на заявку без ДС),
**#18** (сторожит **место** записи операций, а не их число — дверь пишет две), **#55** (база
читается сохранённая, а не пересчитанная по опустевшему траншу), **#68** и **#78** (две ленты
вместо одной, состав из реквизитов соглашения).

## Деньги результата — матрица изменений (14.08.2026)

Секция «Деньги» показывала итоги: сколько взято в сделку и сколько перенесено. На вопрос «что
стало с процентами» такие итоги не отвечают — статьи в них свёрнуты, а движение между статьями
(капитализация, распределение) не видно вообще. Секция переименована в **«Матрица изменений»** и
переписана: строка — статья долга, четыре числа — «было · взято в сделку · осталось» на
источнике и «стало» на производном (`РС-47`).

Что пришлось назвать вслух, чтобы формат не врал:

| Соблазн | Почему нельзя | Что вместо |
|---|---|---|
| Показать «было → стало» по каждой статье как равенство | Статья на входе ≠ статья на выходе: капитализация процентов вливает их в тело, капитализация пени — в накопленную пеню, распределение переводит текущее в накопленное | Два других равенства: по строке `было = взято + осталось`, по итогу `взято − прощено = стало` |
| Пересчитывать состав живым конвейером | После регистрации ДС источник опустошён переносом — «было» вышло бы нулём | Позиции базы и капитализированные проценты заморожены в реквизитах соглашения (`baseRows`, `capitalizedInterest`); живой пересчёт остался предпросмотру |
| Печатать в «стало» прочерк там, где статья отдала деньги | Прочерк читается как пропажа денег | Подпись словами «переехало в другую статью»; само число стоит на строке-получателе (правило «каждый факт один раз») |
| Оставить прочерк у статьи, ушедшей целиком | Величина есть и равна нулю — это не отсутствие | Ноль печатается, прочерк остаётся только за «на этой стороне такой статьи нет» |
| Дать в шапке групп имена траншей | Имя транша уже стоит заголовком его графика | Группы называются «Источник» и «Производный» |
| Оставить под матрицей денежную ленту | База переноса и сам перенос — это итоги граф «Взято» и «Стало» вторыми экземплярами | Ленты нет: словарь сделки, итог прощённого и состояние состава стоят подписями итоговой строки матрицы |

Ленты сняты обе, и по одной причине — `РС-46`, каждый факт один раз. Первая (движение остатка
источника, заведена этим же днём) ушла сразу: остаток печатает подписью график источника. Вторая
(состав сделки) — вопросом заказчика «зачем показывать базу переноса и перенос по этому расчёту»:
двух её чисел из трёх матрица уже не повторяет, а уникальные три (словарь сделки, прощённое,
состояние состава) переехали подписями в `tfoot` матрицы — «Итого · состав зафиксирован
соглашением ДС-60540/2 │ 133 360 база переноса · из них прощено 15 360 │ 118 000 перенос по этому
расчёту». В пакете передачи карточка `P17-R30` разделена: показ вкладки остался в ней, матрица
ушла в новую `P17-R43` (потолок формата — 400 слов на карточку).

Следом — вопрос «почему не показываем график источника». Показываем: секция стоит всегда, у 19
расчётов из 24 таблицей с итогом по остатку. Пусто у пяти (`КД-60540-Т1`, `КД-60541-Т1`,
`КД-58120-Т1`, `КД-59003-Т1`, `КД-61200-Т1`) — у них `schedules[0].rows` пуст, `seedPlan` применён
только ко второй волне кредитов. Засеять их **не стали**, и это проверено, а не решено на глаз:
прогон с планом на всех пяти дал **81/84** — падают `#13` (срез/факт `5 000 000 / 4 200 000` →
`1 271 155 / 869 266`), `#14` (в базе шесть строк вместо пяти) и `#45` (раскладка капитализации).
План даёт `principalAt` просроченное тело, тело входит в базу переноса, и все числа `RS-1001`
едут — а они напечатаны в спеке, ТЗ, статусе и карточках. Заодно у `RS-1001` из этого и вырос её
сценарий: сделка целиком из накопленных статей, тела в переносе нет.

Заглушка при этом **не врёт** — транш без графика возможен и в проде: график набивают руками,
автогенерации в предшествующей системе нет (находка прогона legacy). Молчала она о другом: у
таблицы был итог, у строки его нет, и «остаток по графику» выглядел нулём вместо непосчитанного.
Теперь строка договаривает последствие — «График не заведён — остаток по графику не рассчитан»
(и симметрично у производного: «График пуст — перенос по этому расчёту не разложен по датам»).
Сторож `#79` требует именно этой формулировки.

## «Итог распоряжений» снят с липкости (14.08.2026)

Полоса «Итог распоряжений» на «Параметрах» держалась `position:sticky; bottom:64px` — над липким
футером карточки. Заказчик: «итог распоряжений на футере мешает, закрывает нужные мне кнопки и
данные». Так и было: две липкие полосы забирали у окна около 130 px нижней кромки постоянно, а под
ними на этой вкладке лежит ровно то, к чему куратор идёт СРАЗУ ПОСЛЕ распоряжений — форма «Условия
чернового графика» и кнопка «Предварительный расчёт — пересчитать черновой график».

Липкость снята, вызов `dispositionsBar` переехал из `pParams` внутрь `paramsSection`: полоса стоит
в потоке сразу за таблицей долга, перед секцией условий. Близость следствия к причине держится
теперь МЕСТОМ, а не наложением — итог печатается вплотную к `<tfoot>`, чьи галки его и меняют, а
живость числа даёт полная перерисовка вкладки на каждой правке, а не `sticky`. Порядок чтения:
таблица долга → её подытог → итог распоряжений → условия → кнопка расчёта (проверено на `RS-1001`,
`RS-1004`, `RS-1020`). Смоук — **84/84 PASS**: сторож `#68` читает `<details class="rb-app">` из
разметки `pParams`, и она полосу по-прежнему содержит.

## Готово (по РС-номерам)

| Блок | Решения | Статус |
|------|---------|--------|
| Заявка: якорь субъекта, охват траншами, объединение видов | РС-1, РС-2, РС-3, РС-24 | ✅ |
| Стадия как свёртка журнала, гейты пакета/заключений/ДС, сроки из нормы | РС-7 (гейты), РС-23 | ✅ |
| Финансовое ядро: срез vs факт, база по 5 статьям, распоряжение начисленным | РС-7, РС-8, РС-11…РС-14 | ✅ |
| **Расходы взыскания и сборы вне охвата** (`ADR-0110`) — в базе переноса не заводятся | РС-13а | ✅ |
| Производный транш и перенос на цепочке ДС | РС-4, РС-6 | ✅ |
| Расчёт долга на любую дату при многих реструктуризациях | РС-5 | ✅ |
| Одна дверь в кредит (`restructureApplied`), движок у кредита | РС-9, РС-10 | ✅ |
| Швы со взысканием: пауза, отложение 181-го дня, терминал по условию | РС-16, РС-21, РС-22 | ✅ |
| Гейт согласий обеспечителей + гейт покрытия (мягкий, waiver) | РС-17, РС-18 | ✅ |
| Регуляторные пределы из нормы-справочника | РС-19 | ✅ |
| Деньги при вступлении: адресный резерв → безадресный аванс | РС-20 | ✅ |
| Роли (6) через `canX`, журнал append-only | РС-25 | ✅ |
| **Порог расхождения «срез ↔ факт» в две ступени** (10 %/500 тыс. · 25 %/5 млн) | РС-26 | ✅ |
| **Пеня — терминальная статья** (`ADR-0106`); капитализация разводит проценты и пеню | РС-27 | ✅ *(до 10.08.2026 стояло ✅ авансом: справочник режимов правило объявлял, а график начислял ставку на капитализированную пеню. Исполнено `ADR-0109`)* |
| **Прощение только по сумме, названной в акте** (ИР-13) | РС-28 | ✅ |
| **Снятие гейта покрытия — комитет по администрированию бюджетных кредитов**, раскрывается в пакете к письму | РС-29 | ✅ |
| **Производственный календарь** — справочник дат, общий с платежами; пустой год виден | РС-30 | ✅ |
| **Пять нормативных видов п. 89 замкнуты**, прочее — «иное» с обоснованием (ИР-14) | РС-31 | ✅ |
| **Повторность — сигнал с третьей сделки, не гейт** | РС-32 | ✅ |
| **Реквизиты нормы реальные**, метка «пункт не подтверждён» снята | РС-33 | ✅ |
| **Девять стадий: заключение органа · согласование ЖК · постановление КМ** (`ADR-0107`, ИР-12) | РС-34 | ✅ |
| **Два пола ставки, применяется больший**; вид проекта не указан → флаг, не блокировка | РС-35 | ✅ |
| **Нормативный пакет пп. 85–86 с условными строками пп. 91/95** | РС-36 | ✅ |
| **Предел срока от остатка задолженности**, граница ступени — вверх | РС-37 | ✅ |
| **Распределение начисленного «по периодам»** — дверь суммы и сроков, `disposition.periods` пишется и читается | РС-11, РС-38 | ✅ |
| **Строка графика несёт статьи, ставка только по колонке ОД** (`ADR-0109`, ИР-2′) | РС-41 | ✅ |
| **Распоряжение меняет статью и снимает просрочку по покрытой сумме** | РС-39 | ✅ |
| **Интервал сроков за длиной плана не подрезается молча** — жёсткий гейт (ИР-15) | РС-40 | ✅ |
| **Позиция делится на несколько распоряжений** — Σ линий ≤ суммы позиции, границы держат отказы | РС-43 | ✅ |
| **Зона «формы п. 89» ведётся целиком** (12 ключей, `ADR-0111`), форма фильтруется видом, черновик считается движком кредита | РС-44 | ✅ |
| **Результат расчёта: условия сравниваются таблицей по 12 ключам, деньги — матрицей изменений, показ стоит на одной дате** | РС-45 | ✅ |
| **Геометрия и тишина вкладки результата: каждый факт один раз, пустых колонок нет, ширина по колонкам, усечение раскрывается** | РС-46 | ✅ |
| **Позиция берётся целиком или частью**: взятая сумма на строке базы, подытог и распоряжения считают взятое, невзятое остаётся у источника | РС-7а | ✅ |
| **Перенос гасит вошедшие статьи, а не тело**: из остатка уходит только его балансовая часть, прощённое — операцией прощения (ИР-17), факт снимается до сделки (ИР-18) | РС-7б | ✅ |
| **Матрица изменений**: строка на статью, «было · взято · осталось» на источнике и «стало» на производном, движение между статьями объяснено подписями, итог сделки — итоговой строкой той же таблицы, позиции базы заморожены в реквизитах ДС | РС-47 | ✅ |

## Смоук

Прогон 2026-08-11: **57/57 PASS** (`scripts/inspect/restructuring-check.mjs`), 0 ошибок
консоли — headless-харнесс исполняет `<script>` в `node:vm`, DOM недоступен; сценарии читают
производные функции и мутируют состояние напрямую по образцу прежних тестов #1/#2/#9/#10.
С 10.08.2026 `render()` без `document` — no-op (тем же приёмом, что и `toast`), поэтому
экспортированные двери (`toggleBaseRow`, `setDisposition`, `setDispAmount`, `setDispPeriods`)
зовутся смоуком напрямую, а не обходятся guard-возвратом. Стамп — в шапке
`restructuring.html`.

Сценарии 30–42 добавлены 10.08.2026 под РС-26…РС-37, сценарии 43–51 — тем же вечером под
РС-38…РС-41. Три проверяют не результат, а устройство: #39 сторожит по тексту файла, что
признак повторности нигде не читается как условие гейта; #42 — что «залоговой комиссии» в
макете не осталось; #16 сторожит закрытую дыру `disposition.periods` — прежде он падал, если
`periods` начинали писать, теперь падает, если перестанут (и заодно ловит счётчик
`capitalizedPenalty`, который был признаком неисполненной РС-27).

Сценарии 52–54 добавлены 11.08.2026 под витрину: сев поднимается без исключений и даёт
уникальные id (22 тогда, 26 после хвостов 11–12.08) · все шесть видов задействованы ·
заняты все девять стадий плюс оба отказа и
возврат. #52 — сторож на сам сев: девять ДС в нём строятся настоящими дверями, поэтому
поломка ядра или гейтов закрытия видна сразу и адресно.

Сценарии 55–57 добавлены 11.08.2026 под РС-13б (ненаступившее накопленное, `accTail`):
строка есть, начислений не порождает и по умолчанию снята · распоряжение переназначает
сроки, не статьи, а шаг просрочки её не видит · капитализация хвоста — единственный путь,
возвращающий безставочную сумму под ставку.

Под `ADR-0110` переделаны четыре сценария: #14 сторожит **отсутствие** `collectionCost`/`fees`
в наборе статей (а не их ноль), #13 несёт новый факт 4 082 000, #16 и #51 читают прощение на
`accPenalty`, #48 переставлен на `RS-1011`.

Прогон 2026-08-12: **68/68 PASS**. Сценарии 58–67 добавлены 12.08.2026 под многорасчётность
(РС-42): адрес расчёта в дверях, пределы по каждому кредиту охвата, две ступени расхождения
(1 — по расчёту, 2 — по заявке), второе ДС и закрытие заявки. Под переборку вкладки
переписаны два: **#66** сторожит раскладку (разбор гейтов — под шапкой, коробка производного
транша — на «Графике и диффе», ни того ни другого в теле вкладки), **#68** щёлкает полосу
выбора и проверяет, что подытог «База переноса» следует за выбором, а не залипает на первом
расчёте, и что суммы через расчёты складываются ровно в свёрнутом «Итоге по заявке».

Прогон 2026-08-12, вторым заходом: **71/71 PASS**. Сценарии 69–71 добавлены под РС-43
(деление позиции): три вида на одной позиции дают три исхода и сходятся в копейку со
свободным остатком · границы держат отказы (сумма сверх остатка, повтор вида без своих
сроков), а снятие линии перенумеровывает оставшиеся · порядок линий исхода не меняет —
прогон на перевёрнутом списке даёт то же самое до последней статейной колонки. #66 дополнен:
он сверяет, что хвост адреса `'${calc.id}',${seq}` стоит у каждого сеттера распоряжения —
без него правка второй линии уехала бы в первую и читалась бы как «выбор не сохраняется».

Прогон 2026-08-13: **72/72 PASS**. Сценарий **#72** добавлен под полосу охвата: полоса
строится и при одном расчёте · у расчёта с зарегистрированным ДС крестика нет, а сама
`removeTrancheFromScope` отказывает · крестиков ровно столько, сколько расчётов · снятие
уносит расчёт и сбрасывает `curCalc` · гейт «анализ → комитет» **закрыт** у заявки с нулём
расчётов при непустом сид-`creditIds` — та самая дыра, ради которой гейт переставлен с
`creditIds` на `calcs`. #66 и #68 переписаны с исчезнувших `calcSectionHead`/`.cpick` на
`scopeStrip`/`.spick` и сторожат заодно **отсутствие** вкладки охвата (`pScope`, `'scope'`).

Прогон 2026-08-13, вторым заходом: **74/74 PASS**. Сценарии **#73**/**#74** добавлены под
РС-44: форма отдаёт ровно `allowedParams` (девять полей, редактируемое одно, восемь read-only;
чужой ключ отвергается **моделью**, число приводится к числу, подполя всплывают по условию), и
шов ДС уносит условия **массивом** — `ds.params` больше нет, вокабуляр совпадает с реестром,
`toConditions(condObj(x)) === x`, записи ДС перекрывают унаследованные условия источника и
наследуют неназванные, а `draftSchedule` даёт те же строки, что легли в контракт. Движковые
**#24–#29** переписаны под перенесённую механику: аннуитет с постоянным платежом · «равными
долями» и синоним «дифференцированный» дают одно · квартальная периодичность и «в конце срока» ·
нулевая ставка · три льготы (фазы, отложенные % равны сумме окна раскладки) · календарные
даты с зажимом конца месяца (31-е → 30 апреля) и привязкой последнего платежа. Отдельный
`scripts/inspect/cross-engine-check.mjs` — сторож на главное: черновик и контракт считает одна
механика (**13/13** наборов условий, расхождение валит выход).

Прогон 2026-08-13, третьим заходом: **78/78 PASS**. Сценарии **#76**/**#77**/**#78** добавлены
под РС-45 — одна дата на весь показ (в коде `resultSection` нет ни одного `TODAY`, сдвиг даты
соглашения двигает показ целиком) · таблица условий сходится и с формой, и с моделью (до ДС
графа равна черновику, после — условиям производного транша) · денежная лента сходится обеими
арифметиками и равна `run().transferSum`.

Прогон 2026-08-13, четвёртым заходом: **79/79 PASS**. Сценарий **#79** добавлен под РС-46 —
пять правил одним сторожем, потому что порознь каждое чинится и снова разъезжается: мёртвых
колонок нет · ширина равна `130 × колонок` · усечение раскрывается строкой · каждый факт
печатается один раз · вкладка молчит о себе. Перецелены **#66**, **#68**, **#76**, **#77**,
**#78**: они читали снесённые подписи секций, ярлык усечения и адресные подписи ленты.

Прогон 2026-08-14: **81/81 PASS**. Сценарий **#80** — частичный перенос (`РС-7а`) сразу тремя
глазами: модель (перенос падает ровно на невзятое, состав ДС называет взятое) · источник
(невзятая половина тела остаётся на `T1`, транш жив) · экран (поле в клетке «Включена»,
названный остаток, подытог по взятому) плюс оба потолка — распоряжение выше взятого и взятое
ниже занятого отбиваются. Сценарий **#81** — сводка по всей витрине (24 расчёта на 26 заявках):
подытог базы = Σ взятого · база − прощено = перенос · остаток до − ушло = остаток после ·
ушло ≤ базы · перенос = числу сделки, а база зарегистрированного ДС = реквизиту соглашения.
Перецелены **#13**, **#14**, **#18**, **#55**, **#68**, **#78** —
подробности в разделе о починке ядра выше. Сторож **#18** сменил природу: считать вхождения
`.ops.push(` больше нельзя, дверь пишет две операции разной природы, и проверяется **место**
записи, а не число.

Прогон 2026-08-14, вторым заходом: **83/83 PASS**. Сценарии **#82**/**#83** добавлены под
закрытие `КЛ-1` и `КЛ-3` — то есть под сборку пакета передачи: макет уходит эталоном, и
починка без сторожа разъедется первой же правкой. **#82** читает обе половины якоря субъекта:
кредит на слитом ключе (`КД-62015`, ключ `02508199700099`) стоит в кандидатах заявки
`RS-1004` и принимается дверью охвата, а кредит правопреемника (`02508199700321`) не
предлагается и дверью **отбивается**. **#83** цепляет к заявке все свободные траншы её
субъекта и сверяет каждый свежий черновик с независимым оракулом — свёрткой
`conditionRecords` транша на сегодня; сторож заодно требует, чтобы хотя бы один транш
расходился с термой кредита (иначе проверка холостая) и чтобы черновик приходил без графика,
который считает движок.

Прогон 2026-08-14, третьим заходом: **84/84 PASS**. Сценарий **#84** добавлен под `РС-47` —
матрица изменений по всей витрине: замороженные позиции соглашения совпадают с итогом «было»
(и «было» не обнуляется опустевшим после переноса источником) · подпись срочности в сумме равна
«было», подпись происхождения — «стало» · прощённое не больше взятого · капитализация процентов
приходит только в тело · статья, ушедшая целиком, печатает ноль, а не прочерк, и прочерк нигде
не стоит рядом с числом · строка, отдавшая деньги и не появившаяся на производном, называет
переезд словами. Сторож считает случаи каждого рода (заморозки, прощения, капитализации,
остатки, срочности, прочерки, переезды) и валится с пометкой «СТОРОЖ ХОЛОСТОЙ», если какой-то
род пропал из витрины. Перецелены **#68**, **#76** (вкладка больше не читает остаток транша
вовсе), **#78** и **#81** — обе денежные ленты сняты, и оба сторожа теперь читают итоговую
строку матрицы: имена сделки стоят подписями, `взято − прощено = стало` сходится, построчное
«было = взято + осталось» держится, Σ построчных «из них прощено» равна итогу прощённого, а
разметка ленты (`.flowline` и её подписи) не имеет права вернуться.

## Дефекты макета

Дефекты **макета**, не нормы и не решений спеки; в `TODO.md` и `notes/qa-findings.md` не
заводятся (там живёт живое приложение). `КЛ-1` и `КЛ-2` найдены при сборке витрины 11.08.2026,
`КЛ-3` — при переборке «Результата расчёта» 13.08.2026. **Открытых нет: все три закрыты
14.08.2026** — макет уходит разработчику эталоном, а дефект в эталоне копируется.

**`КЛ-1` ✅ ЗАКРЫТ 14.08.2026. Охват не резолвил слияние ключей.** `naFillCredits` подбирал
кредиты строгим `c.inn === inn`, поэтому кредит, заведённый на слитом (старом) ключе субъекта,
в охват заявки не попадал — при том что зеркало субъектов слияние знает и `keysOf`/`resolveKey`
для этого написаны, но не вызывались ниоткуда. Расходилось с РС-1 (якорь — субъект, а не ИНН)
и `ADR-0019`. Видно было на `RS-1014`: кредит `КД-62015` сидит на ключе `02508199700099`,
заявка подана на `02508199700123`, охват проставляли руками. Починено: заведена единственная
дверь сравнения `sameSubject(a,b)` = `resolveKey(a)===resolveKey(b)`, и через неё пропущены все
четыре места, где субъекты сравнивались, — подбор кредитов новой заявки, список кандидатов
охвата, дверь `addTrancheToScope` и заголовок заявки; список ИНН новой заявки схлопывает слитые
ключи в корень, иначе один заёмщик стоял бы в нём дважды. Реорганизация (`successorOf`) не
резолвится намеренно: у правопреемника свой живой ключ, его кредиты чужие.

**`КЛ-2` ✅ ЗАКРЫТ 14.08.2026. Факт для дрейфа снимался после переноса.** `AppSide.fixFact`
вызывался **после** `CreditSide.restructureApplied` — и в живой двери `regDS`, и в трёх местах
сева, повторявших её порядок. Факт читается как `debtAt(app.calcTranche, date)`, а исходный
транш к тому моменту был уже опустошён переносом, поэтому дельта РС-26 равнялась ≈ −сумме
переноса (на цепочке `RS-1005…RS-1007` — 50–100 %): вторая ступень порога срабатывала не на
расхождении «срез ↔ факт», а на самом факте переноса. Починено: порядок обращён во всех
четырёх дверях (**ИР-18**), `appDrift` читает зафиксированный снимок вместо пересчёта по
опустевшему траншу, дрейф цепочки стал 0 %. Соседние вкладки теперь печатают под словом «факт»
одно число (`RS-1001` — 4 406 360 в обеих), и оговорка «до переноса» с колонки снята.

**`КЛ-3` ✅ ЗАКРЫТ 14.08.2026. Черновик расчёта сеялся из условий кредита, а не транша.**
`draftVersion(cr, overrides)` брал терму кредита; на исходном транше это одно и то же, на
**производном** — нет: расчёт на `КД-60540-Т2` (условия ДС: 60 мес., 6,5 %) получал черновик
`36 мес., 8 %`, и таблица сравнения (`РС-45`) показала бы выдуманное движение «60 → 36, −24 мес.».
Ни одна демо-заявка сюда не попадала — все черновики стояли на исходных траншах, — поэтому смоук
молчал. Починено: подпись сменена на `draftVersion(src, overrides, at)`, черновик сеется свёрткой
`conditionsAt(src, at)` того **транша**, на котором стоит расчёт; переставлены все десять мест
вызова. На витрине правка сразу видна: `КД-60540-Т2` даёт `60 мес./6,5 %`, `КД-60541-Т2` —
`36 мес./6 %`, `КД-62015-Т2` — ставку `4,5 %`, то есть условия своих ДС, а не терму кредита.

## Открытые вопросы

**Вопросов к заказчику нет.** Восемь вопросов письма от 05.08.2026
(`docs/voprosy-zakazchiku/2026-08-05-restrukturizatsiya.md`) закрыты 10.08.2026; письмо
переиздано **редакцией 2** — не как вопросы, а как принятые умолчания с просьбой возразить,
если они расходятся с практикой Холдинга. Прежние ОВ-1 и ОВ-2 закрыты решениями РС-37 и
РС-15 соответственно.

Собственных пробелов не осталось. Последний — отсутствие постановки задачи по модулю для
разработчиков — закрыт 14.08.2026 сборкой пакета передачи (раздел ниже). Прежний второй пробел —
распределение начисленного «по периодам» без UI-двери (§14 п. 16) — закрыт 10.08.2026 решениями
РС-38…РС-41 и `ADR-0109`.

## Пакет передачи разработчику (14.08.2026)

Три плоских файла, отдаются поштучно, ссылки внутри — по голому имени файла:

| Файл | Что в нём | Проверка |
|---|---|---|
| `docs/tasks/p17-restrukturizatsiya-tasks.html` | постановка: редакция 1, 8 этапов, 43 карточки, справка модуля, 3 открытых вопроса с названными умолчаниями | `python3 scripts/check_tasks.py docs/tasks/p17-restrukturizatsiya-tasks.html` |
| `requirements/tz/15-restrukturizatsiya.html` | ТЗ, разделы `s1`…`s11` повторяют разделы спеки — карточки цитируются якорем | глазами: навигация строится, якоря из карточек ведут в свой раздел |
| `mockups/restructuring/restructuring.html` | эталон интерфейса и поведения | `node scripts/inspect/restructuring-check.mjs` — 84/84 PASS |

Модель, правила, приёмка и границы живут **только в постановке**; `TODO.md` держит реестр
`P17-R1…P17-R42` (ID, заголовок, приоритет, статус), совпадающий с карточками один в один —
это сверяет тот же `check_tasks.py`. Внутренние опоры в пакет не входят и в нём не упоминаются
путями: спека `ASUBK-restrukturizatsiya-logika.md`, решения `docs/adr/0092`…`0114`, этот файл.

**Переехавшие строки реестра.** Прежние `P17-R1` и `P17-R2` были работой по макету, а не
постановкой разработчику, и при переписывании реестра редакцией 1 из `TODO.md` убраны:

- `P17-R1` (сделана): многокредитная заявка — расчёт живёт на транше. Охват называет траншы,
  расчёт заводится один на транш охвата, ключ строки базы и распоряжения — тройка
  (транш, статья, срочность). Спека — РС-42 (§6.2), решение — `ADR-0114`.
- `P17-R2` (сделана 13.08.2026 переборкой «Охват перестал быть вкладкой»): экраны
  многокредитной заявки — полоса расчётов вместо перечня кредитов, регистрация и снятие ДС
  адресуют расчёт. На момент записи строки логика была готова и покрыта смоуком, экран — нет;
  теперь есть и он.

## Не-цели (демо)

Не прод-движок начислений, но с 13.08.2026 — **тот же движок, что у кредитного макета**:
механика `buildSchedule` перенесена целиком (календарные даты, день платежа, дневное начисление
по `dayMethod`, три льготы), совпадение стережёт `cross-engine-check.mjs`. Осталось за рамками:
производственный календарь (`nonworking`, РС-30 — общий с кредитом открытый вопрос), график «по
конкретным месяцам» (`payMonths` хранится, не считается), мультивалютность. Реальный движок
подключается со стороны **кредита** (РС-9, `ADR-0096`), а не реструктуризации.
