# АСУБК — статистика: поля и логика хранения

> **Статус: v3, 22.09.2026 — все 16 таблиц строк и 18 служебных таблиц** (v2 — 18.09.2026, 11 таблиц строк). Для разработчика:
> как модуль «Статистика» хранит данные и зачем нужно каждое поле. Состав колонок, типы и
> источники — в [`ASUBK-statistika-fizschema.md`](ASUBK-statistika-fizschema.md), он главный.
> Этот файл добавляет к каждой колонке «что это» и «зачем». Решения — `ADR-0237`…`ADR-0245`.
>
> **Сборка.** `python3 scripts/build_stat_fields.py` собирает из двух файлов страницу
> `statistics-fields.html`. Сборщик сверяет: у каждой таблицы схемы есть раздел здесь, каждая
> колонка схемы описана ровно в одном поле, и каждая колонка поля есть в схеме. Не сошлось —
> страница не пишется.
>
> **Как записано поле.** Строка таблицы — одно поле: одна величина или один разрез со всеми
> своими колонками (`_id` + `_lbl`, `_v` + `_som`, …). «Зачем» называет потребителя:
> - параметр ТЗ — `ТЗ #N`, список 22 параметров в `ASUBK-statistika-logika.md` §10.1;
> - форму отчётности — `ФО-NN`, каталог `mockups/reports/ASUBK-otchetnost-katalog.md`;
> - механизм модуля: ключ, охват, сверку, прогон.
>
> Если потребитель нигде не назван, так и написано: «потребитель не назван».
>
> **Пометка ⚑** — у соседа поля пока нет (схема, графа «Источник»). Поле, забравшее строку
> схемы целиком, получает её ⚑ само. Если строка схемы разделена на несколько полей, ⚑ стоит
> в тексте тех полей, которых у соседа нет: у кредита «Область» и «Район» уже есть, а
> «Айылный округ» и «Село» — с ⚑.

---

## 0. Как хранятся данные

Статистика хранит **строки объектов на дату** и ничего не выводит сама. Каждая величина в
строке пришла от соседа: от ядра, классификации, кураторства или от модуля — владельца
объекта. Сводов в базе нет: итог по подразделению, области или портфелю считается при чтении
из строк. Ниже — правила, по которым строки пишутся и читаются. Поля каждой таблицы — в
разделах объектов и служебных таблиц.

### 0.1. Коротко

1. У каждого объекта своя таблица `stat_row_<объект>`; колонки заводит релиз (`ADR-0237`).
2. Строка — это объект на дату среза; срез — на **начало** дня (`ADR-0238`).
3. У объекта-состояния строка есть на каждый день открытого месяца; от закрытого месяца
   остаётся только строка первого числа следующего (`ADR-0238`).
4. Платёж, поступление и мера — события: строка есть только в день события, исправление
   после закрытия месяца — новая строка-поправка (`ADR-0239`).
5. У каждой суммы есть колонка в сомах; колонка в валюте — только там, где сумма возникла в
   валюте; валюта и курс лежат в строке один раз (`ADR-0240`).
6. Разрез лежит в строке значением на дату: id и подпись, или код, подпись и порядок.
   Внешних ключей нет (`ADR-0241`).
7. `NULL` значит «сосед не ответил», а не ноль. `now_cols` называет величины, которые сосед
   отдал неточно (`ADR-0208`, `ADR-0242`).
8. Строки закрытого месяца не меняются: запись в них запрещает триггер (`ADR-0245` §8).

### 0.2. Таблицы и откуда берётся колонка

{{tables}}

- **Своя таблица на объект.** У объектов разные поля. В общей таблице у строки платежа были
  бы пустыми 150 колонок кредита, а колонка, заведённая заёмщику, появилась бы у всех.
- **Колонку заводит миграция Liquibase в релизе.** Имя колонки выбирает разработчик по
  смыслу. Сущность Jmix называется по таблице: `StatRowCredit`, `StatRowBorrower`, …
- **Реестр `stat_registry` — каталог колонок.** Запись называет колонку (`col_name`), даёт ей
  человеческое имя и вид значения. При старте приложение сверяет реестр со схемой:
  - запись без колонки попадает в список «ждёт колонку»;
  - колонка без записи — предупреждение в журнал.
- **Колонку не удаляют.** Выведенная запись прекращает действие с даты, колонка с историей
  остаётся.

**Новый показатель или разрез — задача из трёх частей:**
1. changeset Liquibase с колонкой;
2. поле сущности Jmix;
3. запись реестра с `col_name`.

Без релиза администратор может:
- завести агрегат — количество, сумму, среднее, минимум, максимум; он считается при чтении;
- объявить корзины над существующей колонкой;
- переименовать запись, включить и выключить её;
- занять слот классификатора (§0.7).

### 0.3. Строка на дату

Строка — объект на дату среза `slice_date`. Срез — на **начало** дня: строка `01.07` — это
состояние на 00:00 1 июля, то есть итог июня. Отсюда главное правило:

> **Период строки — месяц даты `slice_date − 1`.** Строка 01.07 принадлежит июню. По этому
> правилу работают закрытие месяца, триггер запрета записи и события.

**Плотность.** У объекта-состояния (кредит, заёмщик, залог, дело, требование, программа, …)
строка есть на каждый день открытого месяца. Ночной прогон пересчитывает объекты, у которых
соседи сообщили изменение, а остальным копирует вчерашнюю строку с новой датой. Когда месяц
закрывают, дни удаляются. Остаётся только итог — строка первого числа следующего месяца.

Пример: кредит КД-117, июнь закрыли 05.07.

```
slice_date   до закрытия июня   после закрытия
2026-06-01   итог мая           итог мая    ← остался от закрытия мая
2026-06-02   день               удалена
…            день               удалена
2026-06-30   день               удалена
2026-07-01   итог июня          итог июня
2026-07-02   день               день        ← июль открыт
…
2026-07-05   день               день
```

- **Чтение на дату** одно для всех объектов-состояний: `WHERE slice_date = :d`.
- **Дата внутри закрытого месяца не хранится.** Вопрос на 15.06 после закрытия июня получает
  ответ «дневные срезы закрытого месяца не хранятся» и итоги соседних первых чисел.
- **Дата видна только целиком** — когда прогон за неё закончился со статусом `done` или
  `partial`. Пропущенную ночь следующая ночь догоняет по порядку, дыр в открытом месяце нет.
- **Поток за период — разность двух строк.** «Выдано за июнь» = `i_issued` на 01.07 минус
  `i_issued` на 01.06.
- **Строки нет до рождения объекта.** У кредита — до даты договора, у заёмщика — до первого
  кредитного договора, у предмета залога — до регистрации первого залогового договора.
  Закрытый кредит копируется дальше, пока он есть в списке владельца.

### 0.4. События и поправки

Платёж, поступление и мера не живут, а случаются. Платёж от 20.05 и через десять лет тот же
платёж, и копировать его каждый день — сотни миллионов одинаковых строк. Поэтому у объекта
есть **способ хранения** — `stat_registry.storage`:

| способ | объекты | строка есть | чтение на дату D |
|---|---|---|---|
| `state` | кредит, заёмщик, член группы, залог, залоговый договор, дело, требование, программа | каждый день открытого месяца и первые числа | строка `slice_date = D` |
| `event_delta` | платёж, поступление | в дни событий | деньги — сумма строк `slice_date ≤ D`; состояния — последняя строка `≤ D` |
| `event_full` | мера (строка «мера × цель») | в дни событий | последняя строка `≤ D` |

- **Дата строки события — день события плюс один** (срез на начало дня). У платежа —
  `max(дата поступления, дата привязки) + 1`: платёж, опознанный задним числом, попадает в
  месяц опознания, и закрытый месяц не меняется.
- **В открытом месяце** событие переписывается на месте.
- **В закрытом месяце** исходная строка не трогается. В месяц исправления пишется
  строка-поправка. Вид строки `d_corr_kind` входит в первичный ключ.

Пример: платёж ПЛ-9300 записали на КД-117, в июле нашли ошибку — это деньги КД-205. Май
закрыт.

```
stat_row_repay
object_id  slice_date  d_credit_id  i_amount_som  d_corr_kind  d_corr_date
ПЛ-9300    2026-05-21  КД-117             50 000  original     —           ← май, как был
ПЛ-9300    2026-07-16  КД-117            −50 000  reversal     2026-07-15  ← снять
ПЛ-9300    2026-07-16  КД-205             50 000  rebind       2026-07-15  ← перепривязать
```

- У `event_delta` поправка несёт **приращения** (снятие — с минусом), и сумма строк даёт
  текущий итог.
- У `event_full` поправка несёт **полное новое состояние**, и за всё отвечает последняя
  строка.
- Исправленная строка мая получает маркер в `stat_fixed_divergence` со способом
  `corrected_later` и ссылкой на поправку `corr_slice_date`. Паспорт закрытого отчёта за май
  предупреждает: платёж перепривязан 15.07, поправка учтена в июле.

**Читать события только функциями** `stat_repay_at(D)`, `stat_receipt_at(D)`,
`stat_measure_at(D)`. Они возвращают по строке на событие в состоянии на D. Запрос
`WHERE slice_date = D` по таблице событий вернёт только события этого дня. Функции охват не
применяют — его добавляет вызывающий (§0.9). Из JPQL они не вызываются, только нативным SQL.

**Поток событий за период** — строки в интервале дат: «погашено за июнь» — `slice_date` от
02.06 до 01.07 включительно, вместе с поправками, попавшими в июнь.

### 0.5. Имена колонок

{{legend}}

### 0.6. Деньги

- **Сомы — всегда.** У каждой денежной величины есть колонка `_som numeric(20,2)`. По ней
  складывается любой свод.
- **Валюта — где возникла.** Колонка в валюте `_v` есть у кредита, требования, платежа,
  поступления и меры. Валюта `cur`, курс НБКР `rate` и дата курса `rate_date` лежат в строке
  один раз.
- **Курс строки — только к остаткам:** `_som = _v × rate`. Потоки (выдано, погашено,
  начислено, списано) копились по курсу дня каждой операции. Обе их колонки приходят от ядра,
  и курс строки к ним не применяется.
- **Итоги — только в сомах**, одной колонкой: портфель заёмщика, итоги дела, оценка залога,
  доли залогового договора.

Пример: КД-117 в долларах на 12.06, курс 87,45.

```
cur  rate   rate_date   i_od_v  i_od_som   i_issued_v  i_issued_som
USD  87.45  2026-06-12  12 000  1 049 400      20 000     1 702 000
                                ↑ 12 000 × 87,45           ↑ от ядра: 10 000 × 84,00 + 10 000 × 86,20
```

Пересчитать `i_issued_som` по курсу строки — ошибка: 20 000 × 87,45 = 1 749 000 ≠ 1 702 000.

**Курсовая разница за период** — разность `i_fx_som` двух строк, а не разность сомов остатка:
в ней смешаны курс и движение долга. У кредита в сомах `cur = KGS`, `rate = 1`, `_v = _som`.

### 0.7. Разрезы

| что | как хранится | пример |
|---|---|---|
| ссылка на справочник или объект | `_id uuid` + `_lbl text`; подпись — какой была на дату; внешнего ключа нет | `d_curator_id`, `d_curator_lbl` |
| закрытый словарь модели | `text` + `CHECK`; значения — как у владельца; новое значение — релиз | `d_status` |
| значение классификатора | `_code` + `_lbl` + `_ord`, без `CHECK` | `d_risk_code`, `d_risk_lbl`, `d_risk_ord` |
| уровень иерархии | пара колонок на уровень; подразделение — 2 уровня, территория — 4 | `d_unit_parent_id`, `d_terr_aokrug_lbl` |
| признак | `boolean`; `NULL` — сосед молчал, а не «нет» | `d_f_restr` |
| дата | `date`; дни от неё — вычисляемая колонка `GENERATED … STORED` | `d_next_survey` → `i_next_survey` |
| набор значений (только программа) | `uuid[]` + `text[]` с `CHECK` на равную длину | `d_kind_ids`, `d_kind_lbls` |

**Почему у классификатора код, подпись и порядок.** Значения заводит администратор внутри
версии классификатора, и версии меняются:

```
кредит  slice_date  версия  d_risk_code  d_risk_lbl       d_risk_ord
КД-205  2026-08-15  v1      B            Под наблюдением  2
КД-205  2026-09-15  v2      B            Субстандартный   2     ← B переименована
КД-310  2026-09-15  v2      D            Безнадёжный      4     ← D появилась в v2
```

- `GROUP BY d_risk_code` даёт одну категорию B через смену версии.
- `ORDER BY d_risk_ord` сортирует от лучшей к худшей и находит худшую.
- `CHECK` нет: иначе новое значение D уронило бы ночной прогон до релиза.

**Слоты классификаторов администратора.** Администратор публикует классификаторы без
разработчиков. Чтобы новый классификатор попадал в статистику без релиза, у кредита, заёмщика
и залога заранее заведены 10 слотов `d_cls1…d_cls10` по три колонки. Какой классификатор в
каком слоте — таблица `stat_cls_slot`. Слот занимается навсегда: в нём история. До даты
начала действия в слоте `NULL`.

**Признак чужого объекта — join, а не копия.** Отрасль и вид заёмщика в отчёте по кредитам
берутся из строки заёмщика на ту же дату:

```sql
SELECT b.d_industry_lbl, sum(r.i_total_som)
FROM stat_row_credit r
LEFT JOIN stat_row_borrower b
       ON b.object_id = r.d_borrower_id AND b.slice_date = r.slice_date
WHERE r.slice_date = :d
GROUP BY 1;
```

Если отрасль заёмщика исправили задним числом, переписываются только строки заёмщика, и отчёт
по кредитам подхватывает правку сам. Копия делается только там, где у соседа нет строки на ту
же дату: платёж несёт параметры кредита на дату платежа.

### 0.8. Пустое, неполное, неточное

- **`NULL` у величины — сосед не ответил.** Это не ноль и не вчерашнее значение.
- **Ответ соседа — своя колонка.** На каждого опрашиваемого соседа есть колонка `src_core`,
  `src_class`, `src_curation`, … — перечисление `stat_source_state`: `ok` · `unavailable` ·
  `error` · `timeout` · `denied` · `legacy`. Текст ошибки — в `src_detail`.
- **`is_partial`** — вычисляемая колонка: хоть один `src_*` не `ok` и не `legacy`.
  - Неполные строки дозаполняют следующие прогоны. Частичный индекс по ним и есть очередь
    дозаполнения.
  - Пока в месяце есть неполные строки, закрыть его нельзя.
- **У залогового договора, программы и поступления** всё приходит от владельца вместе со
  списком объектов. Колонок `src_*` у них нет: если владелец молчит, строк нет.
- **`now_cols` — неточные величины.** Некоторые соседи умеют отдать только «текущее»
  значение, а не на дату среза: например, ядро берёт график в сегодняшней редакции. Прогон
  записывает имена таких величин в `now_cols`, и паспорт отчёта печатает под ними сноску.

```
object_id  slice_date  now_cols
КД-117     2026-06-12  {i_npay, i_npay_days, i_full_days}              ← график сегодняшний
КД-117     2026-06-05  {i_npay, i_npay_days, i_full_days, d_industry}  ← догон задним числом
КД-205     2026-06-12  {}                                              ← всё на дату
```

В массиве — имя величины, а не колонки: `i_npay` закрывает и `i_npay_v`, и `i_npay_som`.

### 0.9. Охват: кто видит строку

Охват задают правила в `stat_registry_scope`. Правила одного объекта соединяются ИЛИ и
применяются к строкам до группировки.

| вид | как режет | объекты |
|---|---|---|
| `own` | по своим колонкам куратора и подразделения | кредит, заёмщик, залог, требование, платёж; у меры — вторым правилом, по автору |
| `via` | через строки другой таблицы на ту же дату: строка видна, если видна хоть одна связанная | дело — через требования, член группы — через заёмщика, мера — через требование-цель |
| `open` | роль не сужает; паспорт печатает причину | программа, залоговый договор, поступление |

К пути признаков охват не применяется: признаки заёмщика у кредита берутся из строки
заёмщика, даже если саму строку заёмщика роль не видит.

### 0.10. Прогон, закрытие месяца, запрет записи

- **Ночной прогон** за дату спрашивает соседей, что изменилось, пересчитывает эти объекты и
  копирует остальным вчерашнюю строку. Прогоны идут строго по одному. В конце счётчики
  сверяются: не сошлось — прогон `failed`, дата не публикуется.
- **Изменение задним числом.** Сосед отвечает ключом объекта и самой ранней датой действия
  изменения.
  - Дата в открытом месяце — задача `retro` в `stat_queue`: пересчёт с этой даты по сегодня.
  - Дата в закрытом месяце — маркер `stat_fixed_divergence` на каждое первое число, где
    пересчёт действительно отличается. Закрытые строки не переписываются.
- **Журнал перезаписи** `stat_rewrite_log` хранит, какая строка перезаписана, какие колонки
  изменились (имена, не значения) и почему.
- **Закрытие месяца** — две фазы:
  1. переспрос соседей по строкам первого числа и проверка, что неполных строк нет;
  2. одна транзакция: удалить дни закрываемого месяца, их записи журнала и выполненные
     задачи, поставить `period_calendar.stat_closed_at`.
- **Триггер запрета записи** `stat_row_guard` стоит на всех 11 таблицах строк. Если период
  строки закрыт, `INSERT`, `UPDATE` и `DELETE` падают. Поправка события пишется в открытый
  месяц, и триггер её не задевает.
- **Легаси** грузится только строками первых чисел: период заведён закрытым, колонки
  источника — `legacy`. Исправить легаси можно только новой версией миграции под флагом
  сессии `stat.legacy_fix`.

### 0.11. Ключи, индексы, секции

- **Первичный ключ** — (`object_id`, `slice_date`). Исключения:
  - член группы — (`group_id`, `member_id`, `slice_date`);
  - мера — (`object_id`, `target_id`, `slice_date`);
  - платёж и поступление — (`object_id`, `slice_date`, `d_corr_kind`).
- **Внешних ключей** из таблиц строк нет: ни к справочникам, ни к соседям, ни к `stat_run`.
- **Индексы каждой таблицы строк:** первичный ключ, `(slice_date)` и частичный
  `(slice_date) WHERE is_partial` — там, где колонка есть.
- **Индексы для join и охвата:** `stat_row_claim (d_case_id, slice_date)`,
  `stat_row_claim (d_credit_id, slice_date)`, `stat_row_credit (d_program_id, slice_date)`,
  `stat_row_group_member (member_id, slice_date)`, `stat_row_measure (target_id, slice_date)`,
  `stat_row_repay (d_credit_id, slice_date)`. У программы — GIN по массивам для фильтра `@>`.
- **Секции** — `PARTITION BY RANGE (slice_date)`, секция на год плюс `DEFAULT`. Дни закрытого
  месяца удаляются `DELETE`.

### 0.12. Чего в схеме нет

- сводов и хранимых агрегатов — они считаются при чтении;
- `jsonb` — ни одного;
- журнала DDL — колонки ведёт Liquibase;
- строк событий на каждый день;
- колонки на каждый тип узла оргструктуры — подразделение хранится двумя уровнями.

---

## 1. `stat_row_credit` — кредит

Строка кредита — состояние кредита на начало дня. Владелец множества — модуль «Кредиты»: он
отдаёт список кредитов, реквизиты, историю куратора и статуса. Если он молчит, строк нет.
Суммы, дни и обеспеченность приходят от ядра (`src_core`), категория риска и слоты — от
классификации (`src_class`).

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Кредит | `object_id` | id кредита в модуле «Кредиты»; одинаков во всех строках кредита за все даты | первая половина первичного ключа; по нему к кредиту приходят строки требований и платежей (`d_credit_id`) и детализация отчёта открывает карточку кредита | `ADR-0237` · fizschema §0.4 |
| Дата среза | `slice_date` | дата, на **начало** которой снято состояние: строка 01.07 — итог июня | вторая половина ключа; чтение на дату — `WHERE slice_date = :d`; по месяцу `slice_date − 1` триггер решает, закрыт ли период; по ней нарезаны секции | `ADR-0238` §2 · `ADR-0245` §8 |
| Прогон | `run_id` | прогон, который последним записал строку (`stat_run.id`); внешнего ключа нет | объяснить строку: журнал срезов показывает вид прогона, ответы соседей, счётчики и исключения этого прогона | `ADR-0245` §2 |
| Ответ ядра | `src_core` | как ответило расчётное ядро за эту строку: `ok` · `unavailable` · `error` · `timeout` · `denied` · `legacy` | отличить «ядро не ответило» от нуля: при ответе не `ok` все величины ядра — `NULL`; входит в `is_partial` | `ADR-0208` · `ADR-0216` |
| Ответ классификации | `src_class` | как ответила классификация: категория риска и слоты классификаторов | то же для категории риска и слотов; входит в `is_partial` | `ADR-0208` · `ADR-0216` |
| Текст ошибки | `src_detail` | текст ошибки соседа, если ответ не `ok` | показать причину неполной строки в журнале срезов и паспорте ответа | `ADR-0245` §12 |
| Строка неполна | `is_partial` | вычисляемая: `src_core` или `src_class` не `ok` и не `legacy` | частичный индекс по ней — очередь дозаполнения; пока такие строки есть, месяц не закрыть; паспорт предупреждает о неполноте | `ADR-0208` §3 · `ADR-0245` §7 |
| Неточные величины | `now_cols` | имена величин, которые сосед отдал «текущими», а не на дату среза; обычно `{}` | паспорт печатает под такими величинами сноску «на дату / текущее / смешанно»; без неё два отчёта за один день расходились бы без следа | `ADR-0242` · `ADR-0205` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Подразделение | `d_unit_id` `d_unit_lbl` | конечный узел оргструктуры, где сидит куратор кредита на дату; подпись — название на эту дату. ⚑ «Кредиты» пока не отдают подразделение на дату | ТЗ #5 «по подразделению»: строки и итоги отчётов, в ФО-01 — уровень «подразделение»; охват `own`: пользователь подразделения видит свои кредиты | ТЗ #5 · `ADR-0241` §4 · `ADR-0243` §2 · ФО-01 |
| Вышестоящее подразделение | `d_unit_parent_id` `d_unit_parent_lbl` | узел над конечным на дату: департамент, региональное представительство или ГО, если служба стоит прямо под ГО. ⚑ «Кредиты» пока не отдают его на дату | свод на уровень выше, например средняя просрочка по департаментам (ФО-27); охват: начальник вышестоящего видит кредиты всех своих подразделений. Перевод сектора в другой департамент старые строки не переписывает. Выше двух уровней отчётов нет | ТЗ #5 · `ADR-0241` §4 · ФО-27 |
| Куратор | `d_curator_id` `d_curator_lbl` | сотрудник — куратор кредита на дату | ТЗ #6 «по сотруднику»; охват `own`: куратор видит свои кредиты | ТЗ #6 · `ADR-0243` §2 · fizschema §15 п. 9 |
| Категория риска | `d_risk_code` `d_risk_lbl` `d_risk_ord` | категория риска кредита по Порядку №41 п. 11 на дату: код, подпись в действующей версии классификатора, порядок тяжести | отчёты по категориям риска; `GROUP BY` по коду переживает смену версии, `ORDER BY` по порядку сортирует от лучшей к худшей и даёт худшую категорию; платёж берёт её через `v_repay_credit` | `ADR-0241` §3 · `ADR-0125` · Порядок №41 п. 11 |
| Классификаторы администратора | `d_cls{1..10}_{code,lbl,ord}` | 10 резервных слотов по три колонки: значение классификатора, который администратор опубликовал для кредита. Какой классификатор в каком слоте — `stat_cls_slot` | разрез по новому классификатору без релиза: отчёт находит колонку слота через реестр. До даты начала действия в слоте `NULL` — «классификатора ещё не было», а не «без значения» | `ADR-0241` §5 · `ADR-0245` §1 |
| Отрасль кредита | `d_industry_id` `d_industry_lbl` | отрасль, указанная в кредитном договоре | разрез «по отрасли кредита». Отрасль заёмщика (ТЗ #8) — другое поле: оно берётся join-ом из строки заёмщика | макет статистики `d-industry` · `ADR-0206` |
| Статус | `d_status` | статус кредита на дату: 'Проект' · 'Зарегистрирован' · 'Действует' · 'Закрыт' | ТЗ #10 «по статусу кредита»; разрез и отбор «действующие» в портфельных отчётах (в ФО-01 — разрез по состоянию) | ТЗ #10 · ФО-01 |
| Область | `d_terr_region_id` `d_terr_region_lbl` | верхний уровень территории выдачи кредита; город областного значения (Бишкек) стоит на этом уровне | ТЗ #7 «по областям и районам» — первый уровень | ТЗ #7 · `ADR-0241` §4 |
| Район или город | `d_terr_district_id` `d_terr_district_lbl` | второй уровень территории выдачи | ТЗ #7 — второй уровень | ТЗ #7 · `ADR-0241` §4 |
| Айылный округ | `d_terr_aokrug_id` `d_terr_aokrug_lbl` | третий уровень территории выдачи; у города пусто. ⚑ У «Кредитов» этого уровня пока нет | разрез глубже ТЗ #7 — решение пользователя 17.09.2026 | `ADR-0241` §4 · `СС-140` |
| Село | `d_terr_village_id` `d_terr_village_lbl` | нижний уровень территории выдачи; не указано — пусто, кредит из отчёта не пропадает. ⚑ У «Кредитов» этого уровня пока нет | то же, самый детальный уровень | `ADR-0241` §4 · `СС-140` |
| Дата договора | `d_cdate` | дата кредитного договора; с неё у кредита есть строки | ТЗ #3 «по дате кредитного договора» — разрез с корзинами год · квартал · месяц; дата рождения строки | ТЗ #3 · `ADR-0176` §2 · `ADR-0197` |
| Дата окончания | `d_maturity` | дата окончания кредитного договора | разрез с корзинами: «кредиты со сроком погашения в 2027 году» | `СС-140` |
| Линия кредитования | `d_line_id` `d_line_lbl` | линия кредитования, по которой выдан кредит | ТЗ #12 «по видам и линиям кредита» | ТЗ #12 |
| Программа | `d_program_id` `d_program_lbl` | кредитная программа кредита; у кредита она одна | разрез «по программе»: «выдано по программе» — это `i_issued` кредитов в разрезе программы, а не поле программы; ключ join к `stat_row_program`: условие программы против факта (ставка программы рядом со средневзвешенной ставкой её кредитов) | `ADR-0201` §2 · fizschema §0.7 |
| Вид кредита | `d_kind_id` `d_kind_lbl` | вид кредита из справочника, который ведёт администратор | ТЗ #12 «по видам и линиям кредита» | ТЗ #12 |
| Основание выдачи | `d_decision_id` `d_decision_lbl` | решение правительства, на основании которого выдан кредит, например «ПКМКР №554 от 11.09.2024» | ТЗ #13 «по основанию на выдачу кредита» | ТЗ #13 |
| Заёмщик | `d_borrower_id` `d_borrower_lbl` | заёмщик кредита; подпись — наименование и ИНН | ключ join к `stat_row_borrower` на ту же дату: признаки заёмщика (ТЗ #8 отрасль, #9 вид, классы, слоты) в отчёте по кредитам без копии; сверка «у каждого кредита на дату есть строка заёмщика» | `ADR-0241` §8 · `ADR-0206` §5 · ТЗ #8 · ТЗ #9 |
| Состояние обеспеченности | `d_cov_state` | состояние залогового покрытия на дату: 'обеспечен' · 'ниже порога' · 'доля ликвида ниже 80 %' · 'без обеспечения' | разрез и отбор «кредиты ниже порога обеспечения» для контроля залога. ⚠ у ядра перечня нет; вопрос о пятом значении — fizschema §15 п. 5 | `ADR-0011` · Положение №22 Прил. 1 |
| Требуемый порог обеспечения | `d_cov_req` | порог, который кредит должен покрыть залогом, например 1,2500 | рядом с `i_secured_som` и `i_risk_som` показывает, насколько кредит не дотягивает до порога; отбор по величине порога | `ADR-0011` · Положение №22 Прил. 1 §2.3–2.4 |
| Реструктуризирован | `d_f_restr` | на дату действует реструктуризация кредита. ⚑ Шва признаков на дату у «Кредитов» пока нет | ТЗ требует расчётов «с учётом реструктуризации»; портфель реструктурированных на дату (`WHERE d_f_restr`) и разрез да/нет, который складывается в портфель | ТЗ (модуль статистики) · КВ-13 · ФО-10 |
| Во взыскании | `d_f_coll` | по кредиту на дату есть требование во взыскании. ⚑ Шва признаков на дату у «Кредитов» пока нет | портфель во взыскании на дату; разрез да/нет | КВ-13 · `СС-140` |
| Банкротство | `d_f_bankr` | взыскание по кредиту дошло до стадии банкротства. Банкротство самого предприятия — признак заёмщика. ⚑ Шва признаков на дату у «Кредитов» пока нет | ТЗ требует расчётов «с учётом банкротства»; отбор «банкроты с просрочкой 181+» | ТЗ (модуль статистики) · КВ-13 |
| Начисление приостановлено | `d_f_hold` | на дату действует приостановка начисления процентов и пени. ⚑ Шва признаков на дату у «Кредитов» пока нет | объясняет нулевые начисления за период; отбор приостановленных | КВ-13 · `СС-140` |
| Решение суда | `d_f_court` | на дату часть долга живёт по решению суда (судебный слой). ⚑ Шва признаков на дату у «Кредитов» пока нет | ТЗ требует расчётов «с учётом наличия решений суда»; отбор кредитов с решениями суда | ТЗ (модуль статистики) · КВ-13 |
| Спорная пеня | `d_f_dispute` | на дату есть спорная пеня на решении комиссии. ⚑ Шва признаков на дату у «Кредитов» пока нет | отбор спорных кредитов; пара к `i_disputed_days` и `i_pen_disp` | КВ-13 · `СС-140` |
| Долг переведён | `d_f_transfer` | долг по кредиту переведён на другое лицо. ⚑ Признака перевода долга у «Кредитов» пока нет | ТЗ требует расчётов «с учётом перевода долга»; ФО-12 «Перевод долга» | ТЗ (модуль статистики) · ФО-12 |
| Фактор: уклонение от сверки | `d_rf_evade` | засчитанный фактор п. 11: уклонение от подписания акта сверки или непредоставление документов свыше 60 дней. `true` — фактор действует на дату и есть решение комитета | факторы объясняют категорию риска и дают отборы «кредиты с фактором X на дату»; незасчитанный фактор в отчёт не попадает | Порядок №41 п. 11 · `ADR-0065` |
| Фактор: нарушение договора залога | `d_rf_pledge` | засчитанный фактор п. 11: частичное нарушение условий договора залога | то же | Порядок №41 п. 11 · `ADR-0065` |
| Фактор: нет контакта | `d_rf_contact` | засчитанный фактор п. 11: трудности с сохранением контакта с заёмщиком | то же | Порядок №41 п. 11 · `ADR-0065` |
| Фактор: нет обеспечения | `d_rf_nocoll` | засчитанный фактор п. 11: непредоставление надлежащего обеспечения | то же | Порядок №41 п. 11 · `ADR-0065` |
| Фактор: нарушение залогодателем | `d_rf_pledger` | засчитанный фактор п. 11: залогодатель нарушил условия — порча, утрата, распоряжение предметом | то же | Порядок №41 п. 11 · `ADR-0065` |
| Фактор: препятствие мониторингу | `d_rf_nomon` | засчитанный фактор п. 11: воспрепятствование мониторингу свыше 90 дней | то же | Порядок №41 п. 11 · `ADR-0065` |
| Фактор: нецелевое использование | `d_rf_misuse` | засчитанный фактор п. 11: нецелевое использование средств | то же; «кредиты с нецелевым использованием на 01.06»; ФО-13 «Целевое использование средств» | Порядок №41 п. 11 · `ADR-0065` · ФО-13 |
| Фактор: иные обстоятельства | `d_rf_other` | засчитанный фактор п. 11: иные обстоятельства риска невозврата | то же | Порядок №41 п. 11 · `ADR-0065` |

### Деньги

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Валюта договора | `cur` | код валюты кредитного договора: `USD`, `KGS`, … | ТЗ #11 «по валюте кредита» — отдельной колонки разреза нет, разрез берёт `cur`; говорит, в какой валюте все `_v` строки | ТЗ #11 · `ADR-0240` §2 |
| Курс строки | `rate` `rate_date` | курс НБКР и его дата, по которым ядро пересчитало остатки в сомы; у кредита в сомах — 1 | проверка остатков `_som = _v × rate`. К потокам курс строки **не** применяется | `ADR-0240` §3 · `ADR-0214` §5 |
| Задолженность всего | `i_total_v` `i_total_som` | весь долг по кредиту на дату: срочный плюс просроченный | главный итог портфеля: ФО-02 «свод по портфелю»; итог по заёмщику, подразделению, области | ФО-02 · ядро `calcDebt` |
| Просрочено всего | `i_over_v` `i_over_som` | вся просроченная задолженность на дату | ФО-01 «Просроченная задолженность на 1-е число» — обязательная форма, Порядок №41 п. 12; ФО-02 | ФО-01 · ФО-02 · Порядок №41 п. 12 |
| Срочно всего | `i_curr_v` `i_curr_som` | непросроченная часть долга | ФО-02: разбивка долга на срочный и просроченный | ФО-02 |
| Остаток: расходы взыскания | `i_coll_v` `i_coll_som` | остаток расходов по обращению взыскания — первая статья очереди погашения | структура долга по семи статьям очереди; ФО-02 | ТЗ 18 §5.1 · ФО-02 |
| Остаток: комиссии | `i_fee_v` `i_fee_som` | остаток сборов и комиссий | структура долга по статьям; ФО-02 | ТЗ 18 §5.1 · ФО-02 |
| Остаток ОД | `i_od_v` `i_od_som` | остаток основного долга | ФО-01 (графа «остаток основного долга»); ФО-02; вес средней ставки портфеля: Σ(`i_rate_w` × `i_od_som`) / Σ(`i_od_som`) | ФО-01 · ФО-02 · ТЗ 18 §5.1 |
| Остаток: накопленные проценты | `i_int_acc_v` `i_int_acc_som` | проценты, начисленные, но ещё не выставленные к оплате | структура долга по статьям; ФО-02 | ТЗ 18 §5.1 · ФО-02 |
| Остаток процентов | `i_int_v` `i_int_som` | проценты, выставленные к оплате и не погашенные | структура долга по статьям; ФО-02 | ТЗ 18 §5.1 · ФО-02 |
| Остаток: накопленная пеня | `i_pen_acc_v` `i_pen_acc_som` | пеня, начисленная, но ещё не выставленная | структура долга по статьям; ФО-02 | ТЗ 18 §5.1 · ФО-02 |
| Остаток пени | `i_pen_v` `i_pen_som` | выставленная и не погашенная пеня | структура долга по статьям; ФО-02 | ТЗ 18 §5.1 · ФО-02 |
| Просрочено: расходы взыскания | `i_over_coll_v` `i_over_coll_som` | просроченная часть расходов взыскания | ФО-01 раскладывает просрочку по тем же семи статьям | ФО-01 |
| Просрочено: комиссии | `i_over_fee_v` `i_over_fee_som` | просроченная часть сборов и комиссий | ФО-01 — просрочка по статьям | ФО-01 |
| Просрочено: ОД | `i_over_od_v` `i_over_od_som` | просроченный основной долг | ФО-01 — просрочка по статьям | ФО-01 |
| Просрочено: накопленные проценты | `i_over_int_acc_v` `i_over_int_acc_som` | просроченная часть накопленных процентов | ФО-01 — просрочка по статьям | ФО-01 |
| Просрочено: проценты | `i_over_int_v` `i_over_int_som` | просроченные проценты | ФО-01 — просрочка по статьям | ФО-01 |
| Просрочено: накопленная пеня | `i_over_pen_acc_v` `i_over_pen_acc_som` | просроченная часть накопленной пени | ФО-01 — просрочка по статьям | ФО-01 |
| Просрочено: пеня | `i_over_pen_v` `i_over_pen_som` | просроченная пеня | ФО-01 — просрочка по статьям | ФО-01 |
| Переплата | `i_overpaid_v` `i_overpaid_som` | деньги заёмщика сверх долга, ещё не разнесённые | остаток, который надо вернуть или зачесть; потребитель в каталоге отчётности не назван | ядро `calcDebt` |
| Ближайший платёж | `i_npay_v` `i_npay_som` | сумма ближайшего платежа по графику | ФО-06 «План поступлений»: сколько придёт в ближайшие дни. Обычно в `now_cols`: ядро берёт график в сегодняшней редакции | ФО-06 · `ADR-0242` |
| Сумма под риском | `i_risk_v` `i_risk_som` | остаток ОД плюс неосвоенная часть, а не сумма договора | знаменатель обеспеченности: `i_secured_som / i_risk_som` сравнивают с `d_cov_req` | `ADR-0011` |
| Сумма договора | `i_amount_v` `i_amount_som` | сумма по кредитному договору | ФО-09 «Условия кредита»; объём выданного по договорам за период | ФО-09 |
| Неосвоенный остаток | `i_undisb_v` `i_undisb_som` | сколько по договору обещано, но ещё не выдано. Это не «договор минус выдано»: бывают переносы и отмены траншей | «сколько обещано, но не выдано»; на неосвоенный остаток начисляется плата; у ядра клетки пока нет | `ADR-0115` · `СС-140` |
| Остаток спорной пени | `i_pen_disp_v` `i_pen_disp_som` | пеня, которую заёмщик оспорил и по которой ждут решения комиссии | спорная сумма по портфелю; пара к `d_f_dispute` и `i_disputed_days`. Лежит у кредита, а не у платежа; у ядра клетки пока нет | `СС-148` |
| Выдано всего | `i_issued_v` `i_issued_som` | выдано по кредиту нарастающим итогом; сомы — по курсу дня каждого транша, от ядра | «выдано за период» — разность двух строк; «выдано по программе» — в разрезе `d_program_id` | `ADR-0151` §5 · `ADR-0201` §2 |
| Погашено всего | `i_repaid_v` `i_repaid_som` | погашено нарастающим итогом; сомы — по курсу дня каждого платежа, от ядра | «погашено за период» — разность двух строк; сходится с платежами: погашено в сомах = сумма сомов платежей кредита (ФО-04 «Погашения за период») | `ADR-0151` §5 · `ADR-0239` · ФО-04 |
| Начислено процентов | `i_accr_int_v` `i_accr_int_som` | начислено процентов нарастающим итогом | ФО-07 «Начисления процентов и штрафов»: начислено за период — разность двух строк | ФО-07 |
| Начислено пени | `i_accr_pen_v` `i_accr_pen_som` | начислено пени нарастающим итогом | ФО-07; «сколько пени начислили за квартал» — иначе это есть только в журнале ядра; у ядра клетки пока нет | ФО-07 · `СС-140` |
| Списано | `i_woff_v` `i_woff_som` | списано нарастающим итогом | ФО-11 «Списание задолженности» — списано за период | ФО-11 |
| Обеспечено | `i_secured_som` | сумма отнесённых к кредиту залоговых долей — не оценочная стоимость предметов; только в сомах | числитель обеспеченности; итог обеспечения по портфелю | `ADR-0011` · `ADR-0240` §4 |
| Обеспечено ликвидными | `i_secliq_som` | часть обеспечения предметами ликвидных видов; только в сомах | числитель доли ликвида: `i_secliq_som / i_risk_som`; основание состояния 'доля ликвида ниже 80 %' | `ADR-0011` · Положение №22 Прил. 1 |
| Курсовая разница | `i_fx_som` | курсовая разница по кредиту нарастающим итогом, только в сомах; у кредита в сомах всегда 0 | ТЗ требует «расчёт курсовой разницы на заданный период»: за период — разность двух строк. Пример: 01.05 — 50 000, 01.06 — 66 200 → за май 16 200; ядро пока не считает | ТЗ (расчётное ядро) · `ADR-0240` §6 |

### Прочие показатели

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Дней просрочки | `i_overdue_days` | сколько дней кредит в просрочке на дату | корзины срока неплатежа 1 · 31 · 91 · 181 — над этой колонкой, своей колонки у корзины нет; ФО-01 (графа «дней просрочки»); ФО-27 «Средняя просрочка по департаментам»; по дням считается категория риска (п. 11) | ФО-01 · ФО-27 · Порядок №41 п. 11 |
| Дней спорной просрочки | `i_disputed_days` | дни просрочки, которые оспорены и ждут решения комиссии | эти дни вычитаются из просрочки до расчёта категории риска; средний и наибольший спорный срок по портфелю | Порядок №41 п. 11 · макет «Кредиты» `riskBasisOf` |
| Дней до ближайшего платежа | `i_npay_days` | через сколько дней ближайший платёж по графику | ФО-06 «План поступлений»: платежи на ближайшие N дней; обычно в `now_cols` | ФО-06 · `ADR-0242` |
| Дней до полного погашения | `i_full_days` | через сколько дней кредит будет погашен по прогнозу ядра | средний и минимальный срок до погашения по портфелю — потребитель в каталоге отчётности не назван | макет статистики `m-fdays` |
| Средневзвешенная ставка | `i_rate_w` | ставка процентов кредита, средневзвешенная по ОД траншей: транш 8 000 $ под 5 % и 4 000 $ под 8 % → 6,0000 | средняя ставка портфеля Σ(`i_rate_w` × `i_od_som`) / Σ(`i_od_som`); условие программы против факта; ядро пока не считает | `СС-140` · `ADR-0201` |

---

## 2. `stat_row_borrower` — заёмщик

Строка заёмщика — его портфель и признаки на начало дня. Строка есть на дату, если на неё
есть строка хотя бы одного кредита заёмщика: субъект без кредитов строк не имеет. Владельцы
множества — «Заёмщик» и «Субъекты»: они отдают список лиц и их поля. Суммы и дни портфеля
приходят от ядра (`src_core`, шов `calcPortfolio`), худшая категория, подгруппа и слоты — от
классификации (`src_class`), ведущий куратор — от кураторства (`src_curation`). Суммы только в
сомах: колонок `_v` у заёмщика нет. Факторы риска лежат у кредита, группа совместного риска —
в таблице членства (§3).

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Заёмщик | `object_id` | id лица в «Субъектах»; одинаков во всех строках заёмщика | первая половина ключа. С ним сходятся `d_borrower_id` кредита, залогового договора, дела и платежа и `member_id` члена группы: признаки заёмщика попадают в их отчёты join-ом на ту же дату, без копии | `ADR-0241` §8 · `СС-141` |
| Дата среза | `slice_date` | дата, на **начало** которой снято состояние | вторая половина ключа; чтение на дату — `WHERE slice_date = :d`; join с кредитом — по равенству дат | `ADR-0238` §2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ ядра | `src_core` | как ответило ядро по портфелю заёмщика (`calcPortfolio`) | при ответе не `ok` суммы и дни портфеля — `NULL`, а не ноль; входит в `is_partial` | `ADR-0208` · `ADR-0216` |
| Ответ классификации | `src_class` | как ответила классификация: худшая категория, подгруппа, слоты | то же для классов заёмщика; входит в `is_partial` | `ADR-0208` |
| Ответ кураторства | `src_curation` | как ответило кураторство: ведущий куратор и его подразделение | без ответа заёмщик выпадает из охвата `own` и из разрезов ТЗ #5 и #6; входит в `is_partial` | `ADR-0208` · `ADR-0243` |
| Текст ошибки | `src_detail` | текст ошибки соседа, если ответ не `ok` | причина неполной строки в журнале срезов и паспорте ответа | `ADR-0245` §12 |
| Строка неполна | `is_partial` | вычисляемая: хоть один из трёх `src_*` не `ok` и не `legacy` | очередь дозаполнения; пока такие строки есть, месяц не закрыть | `ADR-0208` §3 · `ADR-0245` §7 |
| Неточные величины | `now_cols` | имена величин, которые сосед отдал «текущими», а не на дату | сноска паспорта под такими величинами | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Подразделение | `d_unit_id` `d_unit_lbl` | конечный узел оргструктуры, где сидит ведущий куратор на дату | ТЗ #5 в отчётах по заёмщикам; охват `own`: подразделение видит своих заёмщиков | ТЗ #5 · `ADR-0241` §4 · `ADR-0243` §2 |
| Вышестоящее подразделение | `d_unit_parent_id` `d_unit_parent_lbl` | узел над конечным на дату | свод на уровень выше; начальник вышестоящего видит заёмщиков всех своих подразделений | ТЗ #5 · `ADR-0241` §4 |
| Ведущий куратор | `d_curator_id` `d_curator_lbl` | куратор того кредита заёмщика, у которого больше остаток ОД. Своего куратора у заёмщика нет — кураторство вычисляет ведущего (`leadCurator`) | ТЗ #6 «по сотруднику» в отчётах по заёмщикам; охват `own` | ТЗ #6 · `ADR-0184` §3 · ТЗ 16 §11 |
| Худшая категория | `d_worst_code` `d_worst_lbl` `d_worst_ord` | худшая категория риска среди кредитов заёмщика на дату. Заёмщику категория не присваивается — она свёрнута по худшему кредиту | «заёмщики по худшей категории», отбор проблемных; `ORDER BY d_worst_ord` сортирует от лучшей к худшей. Потребитель в каталоге отчётности не назван | `ADR-0121` §2 · `ADR-0185` §2 · `ADR-0241` §3 |
| Подгруппа | `d_subgroup_code` `d_subgroup_lbl` `d_subgroup_ord` | подгруппа платёжеспособности на дату: 1.1, 1.2, 2.1, … Группа — первая цифра кода, своей колонки у неё нет. Банкротство — подгруппы 2.3 · 3.1 · 3.2 | ФО-24 «Классификация — движение по группам платежеспособности»: движение за период — сравнение подгрупп в двух строках. ТЗ требует расчётов «с учётом банкротства» — у заёмщика это подгруппы банкротства | ФО-24 · Порядок №41 п. 14 · `ADR-0185` §1 |
| Классификаторы администратора | `d_cls{1..10}_{code,lbl,ord}` | 10 резервных слотов по три колонки, как у кредита. Какой классификатор в каком слоте — `stat_cls_slot` с объектом `borrower` | разрез по новому классификатору заёмщика без релиза. До даты начала действия в слоте `NULL` | `ADR-0241` §5 · `ADR-0245` §1 |
| Вид лица | `d_ptype` | 'юр' · 'ИП' · 'физ'; перечень зашит в код | ТЗ #9 «по видам заёмщика». Кредит берёт вид лица join-ом, платёж — копией на дату платежа | ТЗ #9 · `ADR-0241` §8 |
| Организационно-правовая форма | `d_form_id` `d_form_lbl` | ОПФ субъекта: ОсОО, АО, КФХ, … | вид заёмщика подробнее, чем юр · ИП · физ; заведён рядом с ТЗ #9 | ТЗ #9 · logika §10.1 |
| Форма собственности | `d_own_id` `d_own_lbl` | частная, государственная, муниципальная, … | «долг государственных предприятий». Поле есть в макете «Заёмщика», в официальном ТЗ его нет. Потребитель в каталоге отчётности не назван | `СС-141` · макет «Заёмщика» |
| Отрасль заёмщика | `d_industry_id` `d_industry_lbl` | отрасль деятельности лица | ТЗ #8 «по отрасли заёмщика». Отчёт по кредитам берёт её join-ом по `d_borrower_id`: исправили отрасль задним числом — переписываются только строки заёмщика | ТЗ #8 · `ADR-0206` · `ADR-0241` §8 |
| Область | `d_terr_region_id` `d_terr_region_lbl` | верхний уровень территории субъекта. ⚑ У «Субъектов» пока есть только район | ТЗ #7 в отчётах по заёмщикам — первый уровень. Дело взыскания берёт территорию отсюда: своей у дела нет | ТЗ #7 · `ADR-0241` §4 |
| Район или город | `d_terr_district_id` `d_terr_district_lbl` | второй уровень территории субъекта; этот уровень у «Субъектов» уже есть | ТЗ #7 — второй уровень | ТЗ #7 · `ADR-0241` §4 |
| Айылный округ | `d_terr_aokrug_id` `d_terr_aokrug_lbl` | третий уровень территории субъекта. ⚑ У «Субъектов» его нет | разрез глубже ТЗ #7 — четыре уровня, как у кредита | `ADR-0241` §4 |
| Село | `d_terr_village_id` `d_terr_village_lbl` | нижний уровень территории субъекта. ⚑ У «Субъектов» его нет | то же, самый детальный уровень | `ADR-0241` §4 |
| Дата регистрации | `d_regdate` | дата государственной регистрации субъекта | корзины по годам: «долг компаний моложе трёх лет». Потребитель в каталоге отчётности не назван | `СС-141` · `ADR-0176` §2 |
| Состояние субъекта | `d_sstate` | 'действует' · 'смерть' · 'ликвидирован' · 'долг переведён' на дату — по датированным событиям субъекта; реорганизация — 'действует' | «долг умерших заёмщиков», «ликвидированные с ненулевым долгом». Банкротство здесь не отмечается: это подгруппы 2.3 · 3.1 · 3.2. Заменило «статус заёмщика» макета: там было одно значение и не было источника | `СС-141` |
| Чёрный список | `d_black` | заёмщик на дату внесён в реестр чёрного списка | «заёмщики из чёрного списка с непогашенным долгом». Потребитель в каталоге отчётности не назван | `СС-141` |
| Состояние портфеля | `d_pstate` | 'действующий' · 'весь портфель погашен' · 'без договоров'. Перечень есть только у заглушки ядра в макете статистики | ТЗ: «количество заёмщиков (действующие, погасившие задолженность)» — число строк по значению | ТЗ (расчёты по кредиту) · fizschema §15.1 |
| Валюта портфеля | `d_pcur` | код валюты, если все кредиты заёмщика в одной валюте, иначе `multi` — «разновалютный». Это метка, а не состав | отбор «заёмщики с валютными кредитами». Суммы заёмщика всё равно в сомах. Потребитель в каталоге отчётности не назван | `ADR-0184` §6 · `ADR-0240` §4 |
| Дата первого договора | `d_first_cdate` | дата первого кредитного договора заёмщика; с неё у заёмщика есть строки | корзины «новые заёмщики за год». Потребитель в каталоге отчётности не назван | `ADR-0244` §1 · `СС-141` |

### Показатели

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Задолженность всего | `i_total_som` | весь долг заёмщика по всем кредитам на дату, в сомах | долг заёмщика одним числом, без сборки из строк кредитов: отборы «долг свыше N», сортировка. Долг группы совместного риска — сумма этой колонки по строкам членов (§3) | ядро `calcPortfolio` · `ADR-0184` · `ADR-0199` |
| Просрочено всего | `i_over_som` | вся просроченная задолженность заёмщика | «заёмщики с просрочкой» и их свод по признакам заёмщика | ядро `calcPortfolio` |
| Срочно всего | `i_curr_som` | непросроченная часть долга заёмщика | разбивка долга заёмщика на срочный и просроченный | ядро `calcPortfolio` |
| Остаток ОД | `i_od_som` | остаток основного долга по всем кредитам заёмщика | вес при сравнении заёмщиков по основному долгу; ведущий куратор назначается по ОД кредитов | ядро `calcPortfolio` · `ADR-0184` §3 |
| Неосвоенный остаток | `i_undisb_som` | сколько обещано заёмщику по договорам, но ещё не выдано | «сколько обещано, но не выдано» по заёмщикам | ядро `calcPortfolio` · `СС-141` |
| Выдано всего | `i_issued_som` | выдано заёмщику нарастающим итогом; сомы — по курсу дня каждого транша | «выдано за период» — разность двух строк | ядро `calcPortfolio` · `ADR-0240` §3 |
| Погашено всего | `i_repaid_som` | погашено заёмщиком нарастающим итогом; сомы — по курсу дня каждого платежа | «погашено за период» — разность двух строк | ядро `calcPortfolio` · `ADR-0240` §3 |
| Начислено процентов | `i_accr_int_som` | начислено процентов нарастающим итогом | начислено за период — разность двух строк | ядро `calcPortfolio` |
| Начислено пени | `i_accr_pen_som` | начислено пени нарастающим итогом | «сколько пени начислено заёмщикам за квартал»; пара к такому же показателю кредита | ядро `calcPortfolio` · `СС-141` |
| Списано | `i_woff_som` | списано нарастающим итогом | списано за период по заёмщикам — разность двух строк | ядро `calcPortfolio` |
| Курсовая разница | `i_fx_som` | курсовая разница по портфелю нарастающим итогом, в сомах | «на сколько вырос долг заёмщиков только из-за курса»: за период — разность двух строк | ядро `calcPortfolio` · `ADR-0240` §6 |
| Кредитов | `i_credits` | число кредитных договоров заёмщика на дату | «заёмщики с двумя и более кредитами». Число договоров по портфелю считается по строкам кредитов | ядро `calcPortfolio` |
| Кредитов с просрочкой | `i_over_credits` | сколько кредитов заёмщика в просрочке | «заёмщики, у которых просрочен хотя бы один кредит» | ядро `calcPortfolio` |
| Закрытых кредитов | `i_closed_credits` | сколько кредитов заёмщика закрыто | история заёмщика: сколько кредитов он уже погасил. Потребитель в каталоге отчётности не назван | ядро `calcPortfolio` |
| Худшая просрочка | `i_worst_days` | наибольшее число дней просрочки среди кредитов заёмщика на дату | ступени худшей просрочки — корзина над этой колонкой, своей колонки у ступеней нет | ядро `calcPortfolio` · `ADR-0176` |
| Дней до ближайшего платежа | `i_npay_days` | через сколько дней ближайший платёж по любому кредиту заёмщика | «заёмщики с платежом в ближайшие N дней». Потребитель в каталоге отчётности не назван | ядро `calcPortfolio` |
| Срок отношений | `i_tenure_days` | дней от первого кредитного договора до даты среза | средний срок отношений, корзины по стажу. Потребитель в каталоге отчётности не назван | ядро `calcPortfolio` |
| Худшая просрочка за историю | `i_hist_worst_days` | наибольшая просрочка заёмщика за всю историю, включая погашенные кредиты | проверка «кредитная история: просрочка свыше 90 дней — отказ»; «сколько заёмщиков с плохой историей». Кто владеет величиной — «Заёмщик» или ядро — не решено | `СС-141` |

---

## 3. `stat_row_group_member` — член группы совместного риска

Строка — «группа × член × дата»: член состоит в группе на эту дату. Группа — не лицо: она
не берёт кредитов, и одно лицо бывает в нескольких группах. Владелец групп и их состава —
«Субъекты» (`ADR-0213`). Сумм у группы нет: долг группы на дату — join к строкам
заёмщиков-членов на ту же дату. В итоге по всем группам заёмщик считается один раз
(`ADR-0199`). Член без кредитов строки заёмщика не имеет и даёт ноль.

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Группа | `group_id` | id группы совместного риска в «Субъектах» | первая часть ключа; «долг группы» — `GROUP BY group_id` по строкам членов | `ADR-0213` · `ADR-0244` §2 |
| Член группы | `member_id` | субъект, входящий в группу на дату | вторая часть ключа; ключ join к `stat_row_borrower` (`object_id = member_id` на ту же дату). По нему же охват `via`: строка видна, если видна строка заёмщика-члена | `ADR-0243` §3 · fizschema §0.11 |
| Дата среза | `slice_date` | дата, на начало которой член состоит в группе | третья часть ключа; «долг группы на 01.06» видит состав на 01.06: вошедший позже член не попадает | `ADR-0238` §2 · `СС-142` |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ «Субъектов» | `src_subj` | как ответили «Субъекты» за строку; у истории из старой системы — `legacy` | по нему триггер запрета записи узнаёт строку легаси. `src_detail` и `is_partial` у таблицы нет: источник один, и если он молчит, строк нет | fizschema §14 · `ADR-0245` §9 |
| Неточные величины | `now_cols` | имена величин, которые «Субъекты» отдали текущими, а не на дату | сноска паспорта, если состав группы известен только на сегодня | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Название группы | `d_group_lbl` | название группы на дату | подпись группы в отчёте «долг групп совместного риска» | `СС-142` |
| Состояние группы | `d_group_state` | 'действует' · 'распущена'; роспуск — событие в «Субъектах» | «долг действующих групп»: распущенная группа не смешивается с действующими | `СС-142` |

---

## 4. `stat_row_collateral` — предмет залога

Строка предмета — его стоимость, отнесения и контроль на начало дня. Строки начинаются с
регистрации первого залогового договора на предмет: незаложенные и аннулированные предметы в
статистику не попадают, а после освобождения строки продолжаются. Владелец множества —
«Залог». Отнесения, обеспеченные требования и даты контроля считает ядро (`src_core`, шов
`calcPledge`), слоты — классификация (`src_class`), куратора залога назначает кураторство
(`src_curation`). Все суммы только в сомах: оценка залога всегда в сомах.

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Предмет залога | `object_id` | id предмета в модуле «Залог»; одинаков во всех строках предмета | первая половина ключа; детализация отчёта открывает карточку предмета | fizschema §0.4 |
| Дата среза | `slice_date` | дата, на начало которой снято состояние | вторая половина ключа; чтение на дату — `WHERE slice_date = :d` | `ADR-0238` §2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ ядра | `src_core` | как ответило ядро по предмету (`calcPledge`) | при ответе не `ok` отнесения, обеспеченные требования и даты контроля — `NULL`; входит в `is_partial` | `ADR-0208` · `ADR-0216` |
| Ответ классификации | `src_class` | как ответила классификация по слотам предмета | то же для слотов; входит в `is_partial` | `ADR-0208` |
| Ответ кураторства | `src_curation` | как ответило кураторство: куратор залога и его подразделение | без ответа предмет выпадает из охвата `own`; входит в `is_partial` | `ADR-0208` · `ADR-0243` |
| Текст ошибки | `src_detail` | текст ошибки соседа, если ответ не `ok` | причина неполной строки в журнале срезов и паспорте ответа | `ADR-0245` §12 |
| Строка неполна | `is_partial` | вычисляемая: хоть один из трёх `src_*` не `ok` и не `legacy` | очередь дозаполнения; пока такие строки есть, месяц не закрыть | `ADR-0208` §3 · `ADR-0245` §7 |
| Неточные величины | `now_cols` | имена величин, которые сосед отдал «текущими», а не на дату | сноска паспорта — например, если «Залог» знает только текущее место предмета | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Подразделение | `d_unit_id` `d_unit_lbl` | конечный узел оргструктуры, где сидит куратор залога на дату | ТЗ #5 в отчётах по залогу; охват `own` | ТЗ #5 · `ADR-0241` §4 · `ADR-0243` §2 |
| Вышестоящее подразделение | `d_unit_parent_id` `d_unit_parent_lbl` | узел над конечным на дату | свод на уровень выше; охват начальника вышестоящего | ТЗ #5 · `ADR-0241` §4 |
| Куратор залога | `d_curator_id` `d_curator_lbl` | сотрудник в роли «Куратор залога» на дату | ТЗ #6 «по сотруднику»; охват `own`: куратор видит свои предметы | ТЗ #6 · `ADR-0243` §2 |
| Вид залога | `d_kind_id` `d_kind_lbl` | вид предмета из справочника «Залога»: недвижимость, транспорт, оборудование, … | ТЗ #16 «по видам залога»; ФО-14 «Залоговое обеспечение — предметы» | ТЗ #16 · ФО-14 |
| Классификаторы администратора | `d_cls{1..10}_{code,lbl,ord}` | 10 резервных слотов, как у кредита; слоты — `stat_cls_slot` с объектом `collateral` | укрупнённые группы видов — «ликвидный / неликвидный», «движимое / недвижимое» — администратор задаёт классификатором. От групп зависят нормы обеспеченности: ликвидный залог — не меньше 120 %, движимый неликвидный — не меньше 150 %. Уровень «класс ликвидности» снят: такого справочника нет | `ADR-0241` §5 · Положение №22 Прил. 1 · `СС-143` |
| Область | `d_terr_region_id` `d_terr_region_lbl` | верхний уровень места, где предмет находится на дату; не место залогодателя и не место выдачи кредита. ⚑ Сейчас у «Залога» адрес только текстом | ТЗ #7 «по областям и районам» для залога; поле «месторасположение» предмета из ТЗ. Переезд техники виден по датам | ТЗ #7 · ТЗ (модуль «Залог») · `СС-143` |
| Район или город | `d_terr_district_id` `d_terr_district_lbl` | второй уровень места предмета. ⚑ У «Залога» уровней пока нет | ТЗ #7 — второй уровень | ТЗ #7 · `ADR-0241` §4 |
| Айылный округ | `d_terr_aokrug_id` `d_terr_aokrug_lbl` | третий уровень места предмета. ⚑ У «Залога» уровней пока нет | разрез глубже ТЗ #7 — четыре уровня, как у кредита | `ADR-0241` §4 |
| Село | `d_terr_village_id` `d_terr_village_lbl` | нижний уровень места предмета. ⚑ У «Залога» уровней пока нет | то же, самый детальный уровень | `ADR-0241` §4 |
| Принят в залог | `d_adm` | дата регистрации первого залогового договора на предмет; с неё у предмета есть строки | корзины «принято в залог за период»; дата рождения строки. В «Залоге» такого поля нет — его надо вывести из договоров или хранить | `СС-143` |
| Залоговый статус | `d_zstate` | 'свободен' · 'резервируется' · 'в залоге' · 'реализуется' · 'выбыл' · 'освобождён' — как `lienStatus` «Залога» | «сколько предметов в залоге и на какую сумму»; отбор реализуемых и освобождённых; ФО-14 | ФО-14 · fizschema §15.1 п. 1 |
| Запрет на отчуждение | `d_ban` | 'не требуется' · 'не наложен' · 'просрочен' · 'наложен' · 'подлежит снятию' — как `banStatus` «Залога» | контроль запретов: предметы с неналоженным или просроченным запретом, очередь «подлежит снятию» | ТЗ (модуль «Залог») |
| Залогодатель | `d_pledger_id` `d_pledger_lbl` | лицо, чьё имущество заложено: сам заёмщик или третье лицо | ТЗ «количество залогодателей» — `count(distinct d_pledger_id)`. Признаков залогодателя через заёмщика не взять: у третьего лица без кредитов строк заёмщика нет | ТЗ (расчёты по залогу) · `СС-143` |
| Тип залогодателя | `d_pledger_type` | 'Физлицо' · 'Физлицо (имущество в браке)' · 'Муниципальное' · 'Госпредприятие' · 'АО / хоз. общество' · 'Прочее ЮЛ' — как `PLEDGER_TYPES` «Залога» | «сколько залога дали физлица и сколько юрлица». Потребитель в каталоге отчётности не назван | `СС-143` |
| Третье лицо | `d_third` | залогодатель не совпадает с заёмщиком обеспечиваемых кредитов; у свободного предмета — `NULL` | «сколько залога дали третьи лица» | ядро `calcPledge` · `СС-143` |
| Состояние контроля | `d_ctl` | 'в срок' · 'срок подошёл' (до обследования 30 дней и меньше) · 'просрочен' · 'обследований не было' · 'отнесений нет' · 'по договору со спецадминистратором'. Перечень есть только у заглушки ядра в макете статистики | мониторинг залога по Положению: «просроченные обследования», «обследований не было» | Порядок №41 п. 10 · fizschema §15.1 |
| Худшая подгруппа | `d_worst_sub_code` `d_worst_sub_lbl` `d_worst_sub_ord` | строжайшая подгруппа среди заёмщиков обеспечиваемых кредитов на дату | по ней ядро выбирает периодичность обследования: интервал зависит от группы заёмщика и от того, движимое имущество или нет. У банкрота обследование идёт по договору со спецадминистратором. В отчётах — «залог под проблемными кредитами»; потребитель в каталоге не назван | ядро `calcPledge` · макет статистики `calcPledge` |
| Состояние предмета | `d_cond_id` `d_cond_lbl` | состояние по последнему акту обследования: хорошее, удовлетворительное, повреждённое, требует ремонта, аварийное, утраченное. Актов не было — `NULL` | ТЗ требует поле «состояние» предмета и справочник видов состояний; «предметы в неудовлетворительном состоянии» | ТЗ (модуль «Залог») · `СС-143` |
| Утрачен | `d_lost` | предмет утрачен — по акту обследования или по внешнему документу | «залог, которого фактически нет» | `СС-143` |
| Нужна досрочная переоценка | `d_need_reval` | «Залог» поставил отметку: предмет надо переоценить раньше срока, например после акта «требует ремонта» | очередь на переоценку | `СС-143` |
| Отчёт оценщика устарел | `d_appr_stale` | срок действия отчёта оценщика истёк | оценке такого предмета нельзя верить | `СС-143` |
| Документы полные | `d_docs_full` | комплект документов предмета полный | дефекты оформления залога. Потребитель в каталоге отчётности не назван | `СС-143` |
| Стоимость понижена комиссией | `d_override` | залоговая стоимость понижена решением комиссии | «сколько залога оценено вручную» | `СС-143` |
| Страховка | `d_ins_state` | 'не требуется' · 'есть' · 'нет' · 'истекла' — выводится из признаков «Залога» | «техника без действующей страховки». Заменила признак «застрахован»: тот не отличал «страховка не нужна» от «должна быть, но её нет» | `СС-143` |
| Последнее обследование | `d_last_survey` | дата последнего акта обследования. ⚑ Ядро пока отдаёт дни, а не даты | «когда было обследование»; основа для `i_since_survey` | ядро `calcPledge` · `СС-143` |
| Следующее обследование | `d_next_survey` | плановая дата следующего обследования; у банкрота — `NULL`. ⚑ Ядро пока отдаёт дни | план обследований по месяцам — корзины по этой дате; основа для `i_next_survey` | ядро `calcPledge` · `СС-143` |
| Последняя переоценка | `d_last_reval` | дата последней переоценки. ⚑ Ядро пока отдаёт дни | основа для `i_since_reval` | ядро `calcPledge` · `СС-143` |

### Показатели

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Оценочная стоимость | `i_val_som` | стоимость предмета по оценке, в сомах | ТЗ «оценочная и залоговая стоимость заложенного имущества»; ФО-14 | ТЗ (расчёты по залогу) · ФО-14 |
| Залоговая стоимость | `i_pledge_som` | оценочная, умноженная на коэффициент вида; правится только вниз | то же; из неё ядро считает отнесения и доступное | ТЗ (расчёты по залогу) · ФО-14 |
| Отнесено под кредиты | `i_alloc_som` | сумма долей предмета, отнесённых к кредитам; не больше залоговой стоимости | сколько стоимости предмета уже работает на обеспечение | ядро `calcPledge` |
| Доступно | `i_free_som` | залоговая стоимость минус отнесённое по действующим договорам | база перезалога: сколько ещё можно отнести к новому кредиту | ядро `calcPledge` |
| Обеспеченные требования | `i_secured_som` | сумма под риском обеспечиваемых кредитов, взвешенная долей предмета в их обеспечении | сложенная по предметам, сходится с суммой по кредитам, а не удваивает её; процент обеспеченности считается по строкам кредита | ядро `calcPledge` · `ADR-0011` |
| Кредитов | `i_credits` | сколько кредитов обеспечивает предмет | «предметы под несколькими кредитами»; клетка владельца, а не подсчёт здесь | ядро `calcPledge` |
| Залоговых договоров | `i_deals` | сколько действующих залоговых договоров на предмет; больше одного — перезалог | «перезаложенные предметы» | ядро `calcPledge` |
| Дней с обследования | `i_since_survey` | вычисляемая: `slice_date − d_last_survey` | «средний срок с последнего обследования»; число и дата не расходятся: их связывает база | `СС-143` |
| Дней до обследования | `i_next_survey` | вычисляемая: `d_next_survey − slice_date`; меньше нуля — обследование просрочено | «просроченные обследования» — `i_next_survey < 0` | `СС-143` |
| Дней с переоценки | `i_since_reval` | вычисляемая: `slice_date − d_last_reval` | «давно не переоценённый залог» | `СС-143` |
| Актов обследования | `i_surveys` | число актов обследования нарастающим итогом | ТЗ «количество актов обследования»: за период — разность двух строк | ТЗ (расчёты по залогу) |
| Полных снятий | `i_rel_full` | число полных снятий запрета нарастающим итогом | ТЗ «количество снятий залога» и «вид снятия (полное, частичное)»; ФО-16 «Снятие с ареста» | ТЗ (расчёты по залогу) · ФО-16 |
| Частичных снятий | `i_rel_part` | число частичных снятий запрета нарастающим итогом | то же | ТЗ (расчёты по залогу) · ФО-16 |
| Переоценок | `i_revals` | число переоценок нарастающим итогом | из «мер по залогу» ТЗ источник есть только у переоценки; остальные меры (замена, восстановление, доп. соглашение) — находка «Залогу» | ТЗ (расчёты по залогу) · `СС-143` |
| Стоимость снятого | `i_released_som` | стоимость снятого залога нарастающим итогом; при полном снятии — оценочная на дату снятия | ТЗ «сумма по снятому залогу»; ФО-16 | ТЗ (расчёты по залогу) · ФО-16 |

---

## 5. `stat_row_zdeal` — залоговый договор

Строка договора — его стороны, регистрация и доли на начало дня. Строки начинаются с
регистрации договора, после закрытия продолжаются. Всё приходит от «Залога» вместе со
списком договоров, поэтому колонок `src_*` нет. Охват общий: своего куратора у договора нет,
и пользователь решил, что договоры видят все (`СС-144`).

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Залоговый договор | `object_id` | id договора в модуле «Залог» | первая половина ключа; ТЗ «количество залоговых договоров» — число строк на дату | ТЗ (расчёты по залогу) |
| Дата среза | `slice_date` | дата, на начало которой снято состояние | вторая половина ключа; чтение на дату | `ADR-0238` §2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Неточные величины | `now_cols` | имена величин, которые «Залог» отдал текущими, а не на дату | сноска паспорта | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Дата договора | `d_zdate` | дата залогового договора | ТЗ #4 «по дате залогового договора» — корзины год · квартал · месяц: «сколько договоров заключено в 2025 году и на какую сумму» | ТЗ #4 · `ADR-0176` §2 |
| Статус договора | `d_zstatus` | 'Зарегистрирован' · 'Закрыт'; до регистрации строк нет | отбор действующих договоров. ⚠ У «Залога» есть ещё 'Аннулирован' — бывает ли оно после регистрации, не выяснено | fizschema §15.1 п. 7 |
| Залогодатель | `d_pledger_id` `d_pledger_lbl` | лицо, заложившее имущество по договору | «договоры по залогодателям» | `СС-144` |
| Заёмщик | `d_borrower_id` `d_borrower_lbl` | заёмщик по обеспечиваемым кредитам | ключ join к `stat_row_borrower`: по нему договоры раскладываются по подразделению, территории, отрасли заёмщика | `ADR-0241` §8 |
| Нотариальное удостоверение | `d_notary_date` | дата удостоверения договора у нотариуса; `NULL` — ещё не удостоверен | ТЗ требует у договора регистрацию в нотариальной конторе: «договоры без нотариального удостоверения» | ТЗ (модуль «Залог») |
| Свидетельство о регистрации | `d_cert_date` | дата свидетельства о регистрации залога; `NULL` — ещё не зарегистрирован | ТЗ требует регистрацию в залоговой и регистрирующих органах | ТЗ (модуль «Залог») |

### Показатели

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Сумма долей | `i_share_som` | сумма долей предметов по договору, в сомах | сумма залога по дате договора без двойного счёта: предмет в двух договорах даёт свои доли, а не оценку дважды | `СС-144` |
| Предметов | `i_items` | сколько предметов в договоре | «договоры с несколькими предметами» | `СС-144` |
| Кредитов | `i_credits` | сколько кредитов обеспечивает договор | «договоры под несколько кредитов» | `СС-144` |
| Дополнительных соглашений | `i_addenda` | число доп. соглашений нарастающим итогом | доп. соглашение — одна из «мер по залогу» ТЗ; за период — разность двух строк | ТЗ (расчёты по залогу) |

---

## 6. `stat_row_case` — дело взыскания

Дело — папка по одному заёмщику на время непрерывной работы с его просрочкой. Работа идёт в
требованиях дела (§7), мерах (§11) и платежах. Строки дела начинаются с даты открытия и
после закрытия продолжаются. Своего куратора и подразделения у дела нет: охват и разрез «по
подразделениям» идут через строки требований, итог по холдингу — `count(distinct)` по делам
(`СС-145`). Территория и признаки заёмщика — join по `d_borrower_id`. Долг приходит от ядра
(`src_core`), платежи — от «Погашений» (`src_pay`).

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Дело | `object_id` | id дела в модуле «Взыскание» | первая половина ключа; с ним сходятся `d_case_id` требования и меры: охват дела, меры по делу | `ADR-0243` §3 · `СС-145` |
| Дата среза | `slice_date` | дата, на начало которой снято состояние | вторая половина ключа; чтение на дату | `ADR-0238` §2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ ядра | `src_core` | как ответило ядро по долгу дела | при ответе не `ok` долг и база возвратности — `NULL`; входит в `is_partial` | `ADR-0208` |
| Ответ «Погашений» | `src_pay` | как ответили «Погашения» по платежам дела | при ответе не `ok` платежи дела — `NULL`; входит в `is_partial` | `ADR-0208` |
| Текст ошибки | `src_detail` | текст ошибки соседа, если ответ не `ok` | причина неполной строки | `ADR-0245` §12 |
| Строка неполна | `is_partial` | вычисляемая: `src_core` или `src_pay` не `ok` и не `legacy` | очередь дозаполнения; пока такие строки есть, месяц не закрыть | `ADR-0208` §3 |
| Неточные величины | `now_cols` | имена величин, которые сосед отдал текущими | сноска паспорта | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Заёмщик | `d_borrower_id` `d_borrower_lbl` | заёмщик, по которому открыто дело | ключ join к `stat_row_borrower`: территория, отрасль, вид лица дела берутся оттуда | `ADR-0241` §8 · `СС-145` |
| Правопреемник | `d_successor_id` `d_successor_lbl` | лицо, к которому перешёл долг после реорганизации заёмщика; нет — `NULL` | «дела с правопреемством» | `СС-145` |
| Открыто | `d_odate` | дата открытия дела; с неё у дела есть строки | «дел открыто за период»; основа срока дела `i_age_days` | `СС-145` |
| Закрыто | `d_cdate` | дата закрытия дела — конец карантина; у открытого `NULL`. ⚑ «Взыскание» пока не отдаёт дату закрытия | «дел закрыто за июнь»; срок закрытого дела перестаёт расти | `СС-145` |
| Состояние дела | `d_ostate` | 'открыто' · 'карантин' · 'закрыто'. Карантин: последнее требование закрыто, 14 дней ждём новой просрочки | «дел в работе» без дел в карантине | `ADR-0044` · `СС-145` |
| Основание открытия | `d_obasis` | 'просрочка' · 'сведения о банкротстве обязанного лица' · 'ликвидация · смерть · безвестное отсутствие' · 'привлечение обязанного лица по обеспечительному договору' — основание первого требования; после открытия не меняется | «дела, открытые за полугодие, по основаниям» | `СС-145` |
| Приостановка назначения | `d_suspended` | назначение по делу приостановлено: куратор отстранён из-за конфликта интересов | «дела с приостановкой назначения» | `СС-145` |

### Показатели

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Текущий долг | `i_claim_som` | долг по требованиям дела на дату; каждый кредит считается один раз | долг во взыскании по делам | `casePortfolio` · ядро |
| База возвратности | `i_base_som` | долг по каждому кредиту дела на дату его первого требования, сложенный по кредитам. От платежей и пени не меняется, растёт только с новым кредитом в деле | знаменатель процента возвратности: `i_paid_som / i_base_som` считается при чтении. От текущего долга процент «плыл» бы в обе стороны | ТЗ (расчёты по взысканию) · `СС-145` |
| Сумма платежей | `i_paid_som` | платежи по кредитам дела с его открытия, нарастающим итогом; сомы — по курсу дня платежа | ТЗ «сумма платежей в ходе принятия мер» и числитель возвратности. Толкование «платежи по кредитам дела, пока оно открыто» — вопрос заказчику | ТЗ (расчёты по взысканию) · `СС-145` |
| Число платежей | `i_paid_cnt` | число этих платежей нарастающим итогом | ТЗ «количество платежей в ходе принятия мер»: за период — разность двух строк | ТЗ (расчёты по взысканию) |
| Остаток по решениям суда | `i_court_som` | присуждено судом минус погашено по решениям, на дату | ТЗ «остаток суммы по решению суда» | ТЗ (расчёты по взысканию) |
| Расходы взыскания | `i_exp_som` | расходы по взысканию нарастающим итогом | «сколько стоило взыскание»; за период — разность двух строк | `casePortfolio` |
| Требований | `i_claims` | требований в деле всего | «дела со многими требованиями»; число от «Взыскания», без join | `СС-145` |
| Открытых требований | `i_claims_open` | требований в деле, которые ещё не закрыты. ⚑ У `casePortfolio` этой клетки нет | «дела без открытых требований» — `i_claims_open = 0` | `СС-145` |
| Кредитов | `i_credits` | кредитов в деле | «дела по нескольким кредитам» | `СС-145` |
| Срок дела | `i_age_days` | вычисляемая: `coalesce(d_cdate, slice_date) − d_odate`; у закрытого дела не растёт | «средний срок дела» | `СС-145` |

---

## 7. `stat_row_claim` — требование взыскания

Требование — дело × кредит × обязанное лицо; это рабочая единица взыскания. По одному кредиту
бывает несколько требований: к заёмщику и к поручителю. Строки начинаются с даты открытия и
после закрытия продолжаются; продолжение после ветвления (несколько правопреемников) — новое
требование. Владелец — «Взыскание»: фазу, признаки и сроки оно выводит из журнала мер. Долг
приходит от ядра (`src_core`), куратор требования — от кураторства (`src_curation`). Через
строки требований режутся дела и меры. Просрочка, категория, программа и территория кредита
берутся join-ом по `d_credit_id`, а не копией.

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Требование | `object_id` | id требования в модуле «Взыскание» | первая половина ключа; с ним сходится `target_id` меры: охват меры и её цель | `ADR-0243` §3 |
| Дата среза | `slice_date` | дата, на начало которой снято состояние | вторая половина ключа; чтение на дату | `ADR-0238` §2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ ядра | `src_core` | как ответило ядро по долгу требования (`claimDebt`) | при ответе не `ok` сумма требования — `NULL`; входит в `is_partial` | `ADR-0208` |
| Ответ кураторства | `src_curation` | как ответило кураторство: куратор в роли «Куратор требования» | без ответа требование выпадает из охвата `own`, а вместе с ним его дело и меры; входит в `is_partial` | `ADR-0208` · `ADR-0243` |
| Текст ошибки | `src_detail` | текст ошибки соседа, если ответ не `ok` | причина неполной строки | `ADR-0245` §12 |
| Строка неполна | `is_partial` | вычисляемая: `src_core` или `src_curation` не `ok` и не `legacy` | очередь дозаполнения; пока такие строки есть, месяц не закрыть | `ADR-0208` §3 |
| Неточные величины | `now_cols` | имена величин, которые сосед отдал текущими | сноска паспорта | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Ведущее подразделение | `d_unit_id` `d_unit_lbl` | подразделение, которое ведёт требование на дату, — по журналу передач «Взыскания»: отраслевой отдел, ДПО, САК | ТЗ #5 для взыскания; охват `own`. Дела режутся через него: дело числится в каждом подразделении, где у него есть требование | ТЗ #5 · `ADR-0243` §3 · `СС-145` |
| Вышестоящее подразделение | `d_unit_parent_id` `d_unit_parent_lbl` | узел над ведущим на дату | свод на уровень выше; охват начальника вышестоящего | ТЗ #5 · `ADR-0241` §4 |
| Куратор требования | `d_curator_id` `d_curator_lbl` | сотрудник в роли «Куратор требования» на дату | ТЗ #6 «по сотруднику»; «мои дела» — дела, где куратор ведёт хоть одно требование | ТЗ #6 · `ADR-0023` · `СС-145` |
| Дело | `d_case_id` `d_case_lbl` | дело, в котором лежит требование; у строки требования дело всегда одно | ключ join и охвата дела: дело видит тот, кто на ту же дату курирует хоть одно его требование | `ADR-0243` §3 · fizschema §0.11 |
| Кредит | `d_credit_id` `d_credit_lbl` | кредит, долг по которому взыскивается | ключ join к `stat_row_credit`: дни просрочки, категория, программа, территория берутся оттуда | `ADR-0241` §8 · `СС-146` |
| Роль обязанного лица | `d_role` | 'заёмщик' · 'поручитель' · 'гарант' и четвёртая роль | «требования к поручителям», «присуждено поручителям» у мер. ⚠ Четвёртая роль не выяснена: 'госорган' в макете взыскания или 'залогодатель' в его спецификации | fizschema §15.1 п. 2 |
| Обязанное лицо | `d_obligor_id` `d_obligor_lbl` | лицо, к которому требование предъявлено на дату; после правопреемства — новое лицо | «сколько лиц под взысканием» — `count(distinct d_obligor_id)` | `СС-146` |
| Лицо — правопреемник | `d_obl_succ` | на дату обязанное лицо — правопреемник исходного | «требования, перешедшие к правопреемникам» — для любой роли | `СС-146` |
| Объём требования | `d_scope_vol` | 'просроченная сумма' · 'полный остаток' | «что требуем»: досрочное взыскание всего остатка против просрочки | `СС-146` |
| Способ взыскания | `d_scope_way` | 'деньгами' · 'обращением на предмет залога' | «требования с обращением на залог» | `СС-146` |
| Основание открытия | `d_obasis` | те же четыре значения, что у дела (§6) | «требования по основаниям»; основание дела — основание его первого требования | `СС-145` |
| Представитель кредита | `d_primary` | на дату у каждого кредита ровно одно требование помечено: с наибольшей суммой, при равенстве — самое раннее | итог сумм по холдингу — только по этим строкам: заёмщик и поручитель отвечают солидарно, и без признака долг посчитался бы дважды. Итог подразделения — по всем его требованиям | `СС-146` |
| Фаза | `d_phase` | шаг маршрута взыскания: 'Досудебное урегулирование' · 'Претензия' · 'Повторная претензия' · 'Безакцептное списание' · 'Иск' · 'Решение суда' · 'Исполнительный лист' · 'На исполнении' | «справочник видов статуса процесса принудительного взыскания» из ТЗ — это фаза; ФО-17 «Взыскание — процедуры, фазы, события», его Вид — исполнительные листы в ОИП (прежняя ФО-22) | ТЗ (модуль взыскания) · ФО-17 · `СС-146` |
| Порядок фазы | `d_phase_ord` | вычисляемая: номер фазы 1…8 по маршруту | «по фазам в порядке маршрута», а не по алфавиту | `СС-146` |
| Стадия | `d_stage` | вычисляемая из фазы: 'Наблюдение' · 'Досудебный порядок' · 'Судебный порядок' · 'Исполнительное производство' | «требования по стадиям» без join; стадия не может разойтись с фазой — её вычисляет база | `СС-146` |
| Дата входа в фазу | `d_phase_date` | когда требование вошло в текущую фазу | основа `i_phase_days`: «сколько дней в среднем стоят в фазе „Иск“» | `СС-146` |
| Открыто | `d_qdate` | дата открытия требования; с неё у требования есть строки | «новых требований за месяц»; основа срока `i_age_days` | `СС-146` |
| Закрыто | `d_qcdate` | дата закрытия; у открытого — `NULL`. Заполняется вместе с исходом | «закрыто за месяц»; срок закрытого требования перестаёт расти | `СС-146` |
| Исход | `d_outcome` | исход закрытого требования: 'Полное погашение' · 'Признана безнадёжной' · 'Списана' · 'Завершена процедура банкротства' · 'Реструктурирована' · 'Обеспечение прекращено' · 'Правопреемство'; у открытого — `NULL` | «закрыто в июне по исходам»; «средний срок до полного погашения» | `СС-146` |
| Пауза: гарантийное письмо | `d_p_guar` | действует пауза по гарантийному письму: безакцептное списание отложено до 70 дней | «требования на паузе» по видам пауз; паузы бывают одновременно. ⚑ У «Взыскания» признака пока нет | `СС-146` |
| Пауза: риск программы | `d_p_risk` | действует пауза из-за риска срыва программы | то же. ⚑ У «Взыскания» признака пока нет | `СС-146` |
| Пауза: реструктуризация | `d_p_restr` | действует пауза на время рассмотрения реструктуризации | то же; ТЗ требует расчётов «с учётом реструктуризации». ⚑ У «Взыскания» признака пока нет | ТЗ (модуль статистики) · `СС-146` |
| Мировое соглашение | `d_settlement` | по требованию утверждено мировое соглашение, у долга свой график | «дела с мировым соглашением» — через строки требований. ⚑ У «Взыскания» признака пока нет | `СС-145` · `СС-146` |
| Добровольное исполнение | `d_voluntary` | заключено соглашение о добровольном исполнении: должник признал долг и платит сам | «требования на добровольном исполнении». ⚑ У «Взыскания» признака пока нет | `СС-146` |
| Особое состояние лица | `d_person_state` | 'банкротство инициировано' · 'признан банкротом' · 'правопреемство'; нет — `NULL` | ТЗ требует расчётов «с учётом банкротства» — здесь банкротство обязанного лица, в том числе поручителя | ТЗ (модуль статистики) · `СС-146` |
| Вне учёта | `d_acct_off` | 'Признана безнадёжной' · 'Списана': долг снят с баланса, но исполнительный лист ещё у пристава, поэтому требование открыто; на учёте — `NULL` | «списанные, но взыскиваемые требования» | `СС-146` |
| Трек: суд | `d_trk_court` | подан иск, решения ещё нет | «требования в суде». ⚑ У «Взыскания» признака пока нет | `СС-146` |
| Трек: исполнение | `d_trk_exec` | исполнительный лист у пристава | «листы на исполнении». ⚑ У «Взыскания» признака пока нет | `СС-146` |
| Трек: залог | `d_trk_pledge` | идёт обращение взыскания на предмет залога | фаза этот трек не показывает; «требования с обращением на залог». ⚑ У «Взыскания» признака пока нет | `СС-146` |
| Непредъявленная просрочка с | `d_unpres_from` | первый взнос, не покрытый иском или другой мерой; всё предъявлено — `NULL`. ⚑ У «Взыскания» даты пока нет | основа `i_unpres_days`: «пора подавать новый иск». Общий счётчик просрочки после решения суда уже ничего не сообщает | `СС-146` |
| Конец срока давности | `d_limit_due` | дата, после которой долг через суд не взыскать; перерывается признанием долга, частичной оплатой, иском. ⚑ У «Взыскания» даты пока нет | основа `i_limit_left`: «давность истекает за полгода» | `СС-146` |
| Срок предъявления листа | `d_writ_due` | крайний срок повторного предъявления исполнительного листа после возврата приставом; не возвращался — `NULL`. ⚑ У «Взыскания» даты пока нет | основа `i_writ_left`: «риск потерять исполнительный лист» | `СС-146` |
| Ближайший внутренний срок | `d_next_due` | ближайший срок по шаблонам Порядка (претензия за 10 рабочих дней и т. п.). ⚑ У «Взыскания» даты пока нет | исполнительская дисциплина; в ТЗ по взысканию её нет — потребитель не назван | `СС-146` |

### Деньги

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Валюта | `cur` | валюта кредита требования | в какой валюте `i_amount_v` | `ADR-0240` §2 |
| Курс строки | `rate` `rate_date` | курс НБКР и его дата, по которым ядро пересчитало сумму в сомы | проверка `i_amount_som = i_amount_v × rate`: сумма требования — остаток | `ADR-0240` §3 |
| Сумма требования | `i_amount_v` `i_amount_som` | полная сумма требования к этому лицу на дату | «сумма требований по холдингу» — по строкам с `d_primary`; по подразделениям — по всем их требованиям | ядро `claimDebt` · `СС-146` |

### Прочие показатели

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Дней в фазе | `i_phase_days` | вычисляемая: `slice_date − d_phase_date` | «средний срок в фазе „Иск“» | `СС-146` |
| Срок требования | `i_age_days` | вычисляемая: `coalesce(d_qcdate, slice_date) − d_qdate` | «средний срок требования до погашения» | `СС-146` |
| Дней непредъявленной просрочки | `i_unpres_days` | вычисляемая: `slice_date − d_unpres_from` | «пора подавать новый иск» — например, 90 дней и больше | `СС-146` |
| Дней до конца давности | `i_limit_left` | вычисляемая: `d_limit_due − slice_date`; меньше нуля — давность истекла | «давность истекает за полгода» — `i_limit_left < 180` | `СС-146` |
| Дней до срока предъявления листа | `i_writ_left` | вычисляемая: `d_writ_due − slice_date` | риск потерять исполнительный лист | `СС-146` |
| Просроченных внутренних сроков | `i_due_overdue` | сколько внутренних сроков по требованию просрочено | «просрочены внутренние сроки» — `i_due_overdue > 0`; потребитель в ТЗ не назван | `СС-146` |

---

## 8. `stat_row_program` — кредитная программа

Строка программы — её условия на начало дня. Строки начинаются с утверждения: черновики,
отклонённые и удалённые программы в статистику не попадают, после закрытия строки
продолжаются. Владелец — «Программы»; колонок `src_*` нет. Охват общий: состав программ —
общее знание. Освоение программы — выдано, портфель, число кредитов, фактическая ставка —
join к строкам кредитов по `d_program_id`. В официальном ТЗ кредитных программ нет: объект —
развитие новой модели, это вопрос заказчику (`СС-147`).

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Программа | `object_id` | id программы в модуле «Программы» | первая половина ключа; с ним сходится `d_program_id` кредита и платежа: освоение программы — join к строкам кредитов | `ADR-0201` §2 · fizschema §0.11 |
| Дата среза | `slice_date` | дата, на начало которой сняты условия | вторая половина ключа; «условия программы на дату выдачи» | `ADR-0238` §2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Неточные величины | `now_cols` | имена величин, которые «Программы» отдали текущими | сноска паспорта | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Статус | `d_pstate` | `draft` · `review` · `rework` · `approved` · `rejected` · `closed` — как `STATUS` «Программ». Строки пишутся с `approved`, но утверждённую программу могут вернуть на доработку | «сколько программ действует на 01.06» | `СС-147` |
| Дата утверждения | `d_approved` | дата утверждения программы; с неё у программы есть строки | корзины «утверждено за год»; дата рождения строки | `СС-147` |
| Действует с | `d_pstart` | дата начала действия | «действующих на дату» — утверждена и `d_pstart ≤ slice_date`: утверждённая, но не начавшаяся программа не считается | `СС-147` |
| Действует по | `d_pend` | дата окончания; `NULL` — бессрочная | «программы, заканчивающиеся в этом году» | `СС-147` |
| Валюта программы | `d_pcur` | валюта, в которой заданы условия программы | «программы в долларах»; в ней заданы `i_amt_min` и `i_amt_max` | `СС-147` |
| Решение правительства | `d_decision_id` `d_decision_lbl` | решение, которым утверждена программа, например «ПКМКР №554 от 11.09.2024» | «программы по решениям»; у кредита — ТЗ #13 | ТЗ #13 · `СС-147` |
| Отрасль | `d_industry_id` `d_industry_lbl` | отрасль, на которую направлена программа | «программы по отраслям» | `СС-147` |
| Источники финансирования | `d_source_ids` `d_source_lbls` | набор источников программы: id и подписи в одном порядке, `CHECK` на равную длину | «программы по источникам»: программа попадает в каждую свою группу через `unnest`, итог считается по строкам программ | `ADR-0241` §9 · `СС-147` |
| Виды кредита | `d_kind_ids` `d_kind_lbls` | набор видов кредита программы | «программы по видам кредита», в том числе без выданных кредитов; у кредита вид один — ТЗ #12 | ТЗ #12 · `ADR-0241` §9 |
| Назначения | `d_purp_ids` `d_purp_lbls` | набор назначений кредита программы | «программы по назначению: оборотные средства, основные средства, …» | `ADR-0241` §9 |
| Линии кредитования | `d_line_ids` `d_line_lbls` | набор линий программы | «программы АБР» — `d_line_ids @> ARRAY[:id]`, GIN-индекс; у кредита линия одна — ТЗ #12 | ТЗ #12 · `ADR-0241` §9 · fizschema §0.11 |
| Тип ставки | `d_rate_type` | 'фиксированная' · 'плавающая' | «программы с фиксированной ставкой». ⚠ У «Программ» `rateType` = 'Фиксированная' · 'Диапазон', а плавающая — отдельный признак `floatRate`: одна колонка или две — не решено | fizschema §15.1 п. 3 |
| Групповое кредитование | `d_group` | программа выдаёт групповые кредиты | «программы группового кредитования» | `СС-147` |
| Обеспечение обязательно | `d_coll_req` | залог по кредитам программы обязателен | «программы с обязательным залогом» | `СС-147` |
| Льготный период | `d_grace` | у программы есть льготный период | «программы с льготой» | `СС-147` |

### Показатели

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Ставка | `i_rate` | ставка программы; при плавающей — `NULL` | условие против факта: ставка программы рядом со средневзвешенной ставкой её кредитов (`i_rate_w`, join); «программы со ставкой не выше 6 %» | `ADR-0201` · `СС-147` |
| Диапазон суммы | `i_amt_min` `i_amt_max` | наименьшая и наибольшая сумма кредита в валюте программы. Это условие, а не деньги: в сомы не пересчитывается и не складывается | фильтры по суммам; «кредиты сверх максимума программы» — сравнение с суммой договора | `СС-147` · `ADR-0240` |
| Диапазон срока | `i_term_min` `i_term_max` | наименьший и наибольший срок кредита, месяцев | фильтры по срокам | `СС-147` |

---

## 9. `stat_row_repay` — платёж

Платёж — часть поступления, привязанная к одному кредиту. Это событие: строка есть только в
день признания платежа плюс один — `max(дата поступления, дата привязки) + 1`. Платёж,
опознанный задним числом, попадает в месяц опознания, и закрытый месяц не меняется. Исправление
после закрытия месяца — строки-поправки в месяце исправления. Читать на дату — функцией
`stat_repay_at(D)`. Параметры ТЗ кредита копируются в платёж на дату платежа; категория риска,
слоты и признаки заёмщика — через представление `v_repay_credit`. Владелец — «Погашения»;
соседа один — кураторство.

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Платёж | `object_id` | id платежа в «Погашениях»; одинаков в исходной строке и во всех поправках | часть ключа; «чистый» итог по платежу — сумма его строк; ФО-41 «Реестр погашений» — строки платежей | ФО-41 · `ADR-0239` |
| Дата строки | `slice_date` | день признания плюс один: `max(d_rdate, d_bdate) + 1`; у поправки — день исправления плюс один | часть ключа; «погашено за июнь» — строки с `slice_date` от 02.06 по 01.07 включительно, вместе с поправками | `ADR-0239` §2 · `СС-148` |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ кураторства | `src_curation` | как ответило кураторство: куратор кредита на дату платежа | без ответа платёж выпадает из охвата `own` и из разрезов ТЗ #5 и #6; входит в `is_partial` | `ADR-0208` · `ADR-0243` |
| Текст ошибки | `src_detail` | текст ошибки кураторства, если ответ не `ok` | причина неполной строки | `ADR-0245` §12 |
| Строка неполна | `is_partial` | вычисляемая: `src_curation` не `ok` и не `legacy` | очередь дозаполнения; пока такие строки есть, месяц не закрыть | `ADR-0208` §3 |
| Неточные величины | `now_cols` | имена величин, которые сосед отдал текущими | сноска паспорта | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Вид строки | `d_corr_kind` | `original` исходная · `reversal` снятие · `rebind` перепривязка; входит в ключ | поправка после закрытия месяца: `reversal` снимает сумму со старого кредита, `rebind` отдаёт новому. Строки одной даты читаются в порядке `rebind` › `reversal` › `original` | `ADR-0239` §4 · `СС-148` |
| Дата исправления | `d_corr_date` | когда исправили платёж; у `original` — `NULL` | паспорт закрытого месяца: «платёж перепривязан 15.07, поправка учтена в июле» | `ADR-0239` §5 |
| Приход денег | `d_rdate` | когда деньги пришли | «когда реально пришли деньги» — у опознанного задним числом она раньше месяца строки | `СС-148` |
| Привязка | `d_bdate` | когда платёж привязали к кредиту | вместе с `d_rdate` задаёт дату строки; срок опознания | `СС-148` |
| Состояние платежа | `d_pay_state` | `pending` ожидает подтверждения казначейством · `confirmed` подтверждён · `reversed` сторнирован. У «Погашений» сейчас — признак `void` и сопоставление поступления | «погашено» — все строки, кроме исходной, сторнированной в открытом месяце; «в т. ч. не подтверждено казначейством» — `pending`. Месяц не закрывается, пока в нём есть неподтверждённые | `СС-148` |
| Подразделение | `d_unit_id` `d_unit_lbl` | подразделение куратора кредита на дату платежа | ТЗ #5 для погашений; охват `own`. На дату платежа, а не сегодня: строки дней кредита к тому времени удалены закрытием | ТЗ #5 · `ADR-0243` §2 |
| Вышестоящее подразделение | `d_unit_parent_id` `d_unit_parent_lbl` | узел над ним на дату платежа | свод на уровень выше | ТЗ #5 · `ADR-0241` §4 |
| Куратор | `d_curator_id` `d_curator_lbl` | куратор кредита на дату платежа | ТЗ #6 для погашений; охват `own` | ТЗ #6 · `ADR-0243` §2 |
| Кредит | `d_credit_id` `d_credit_lbl` | кредит, который гасит платёж; у `rebind` — новый кредит | ключ `v_repay_credit`; сверка: погашено по кредиту за период = сумма его платежей в сомах | `ADR-0239` · fizschema §13 |
| Заёмщик | `d_borrower_id` `d_borrower_lbl` | заёмщик кредита на дату платежа | «погашения по заёмщикам» | `СС-148` |
| Поступление | `d_receipt_id` | поступление, из которого разнесён платёж | связь с `stat_row_receipt`: одно сводное поступление гасит несколько кредитов | `СС-148` |
| Вид погашения | `d_kind_id` `d_kind_lbl` | вид погашения: денежные средства, имущество в счёт долга, по решению суда, … | ТЗ #14 «по видам погашения»; ФО-04 «Погашения за период» | ТЗ #14 · ФО-04 |
| Статус кредита | `d_status` | статус кредита на дату платежа — значения как у `stat_row_credit.d_status` | ТЗ #10 для погашений: платёж по кредиту, закрытому в конце месяца, показан как по действующему | ТЗ #10 · `ADR-0241` §8 |
| Область | `d_terr_region_id` `d_terr_region_lbl` | верхний уровень территории кредита на дату платежа | ТЗ #7 для погашений | ТЗ #7 · `ADR-0241` §8 |
| Район или город | `d_terr_district_id` `d_terr_district_lbl` | второй уровень территории кредита на дату платежа | ТЗ #7 — второй уровень | ТЗ #7 |
| Айылный округ | `d_terr_aokrug_id` `d_terr_aokrug_lbl` | третий уровень территории кредита на дату платежа | разрез глубже ТЗ #7 | `ADR-0241` §4 |
| Село | `d_terr_village_id` `d_terr_village_lbl` | нижний уровень территории кредита на дату платежа | то же, самый детальный уровень | `ADR-0241` §4 |
| Отрасль кредита | `d_industry_id` `d_industry_lbl` | отрасль кредита на дату платежа | «погашения по отраслям». Отрасль заёмщика (ТЗ #8) — через `v_repay_credit` и строку заёмщика | `ADR-0241` §8 · `СС-148` |
| Программа | `d_program_id` `d_program_lbl` | кредитная программа кредита | «погашено по программе» | `ADR-0241` §8 |
| Вид кредита | `d_ckind_id` `d_ckind_lbl` | вид кредита на дату платежа | ТЗ #12 для погашений | ТЗ #12 |
| Линия кредитования | `d_line_id` `d_line_lbl` | линия кредита на дату платежа | ТЗ #12 для погашений | ТЗ #12 |
| Основание выдачи | `d_decision_id` `d_decision_lbl` | решение правительства, по которому выдан кредит | ТЗ #13 для погашений | ТЗ #13 |
| Вид заёмщика | `d_ptype` | вид лица заёмщика на дату платежа — значения как у `stat_row_borrower.d_ptype` | ТЗ #9 для погашений | ТЗ #9 |

### Деньги

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Валюта | `cur` | валюта кредита | в какой валюте все `_v` платежа | `ADR-0240` §2 |
| Курс | `rate` `rate_date` | курс НБКР на дату поступления | снимок пересчёта: сомы платежа — те, что пришли; курсом строки их не пересчитывают | `ADR-0240` §3 · `СС-148` |
| Сумма платежа | `i_amount_v` `i_amount_som` | сумма платежа; у поправки — со знаком: снятие с минусом | ФО-04 «Погашения за период»; ТЗ «погашенная сумма» и «количество погашений». Сходится с `i_repaid` кредита | ФО-04 · ТЗ (расчёты по кредиту) · `ADR-0239` |
| Разнесено: расходы взыскания | `i_al_coll_v` `i_al_coll_som` | часть платежа, ушедшая на расходы взыскания — первая статья очереди | погашено по статьям; `CHECK`: сумма пяти статей = `i_amount` | ТЗ 18 §5.1 · «Погашения» `payAlloc` |
| Разнесено: комиссии | `i_al_fee_v` `i_al_fee_som` | часть на сборы и комиссии | то же | ТЗ 18 §5.1 |
| Разнесено: ОД | `i_al_od_v` `i_al_od_som` | часть на основной долг. У «Погашений» это разность — сумма минус четыре статьи, поэтому тождество сходится до копейки | ТЗ «погашенная сумма … по основной сумме» | ТЗ (расчёты по кредиту) · ТЗ 18 §5.1 |
| Разнесено: проценты | `i_al_int_v` `i_al_int_som` | часть на проценты | ТЗ «погашенная сумма … по процентам» | ТЗ (расчёты по кредиту) |
| Разнесено: пеня | `i_al_pen_v` `i_al_pen_som` | часть на пеню | ТЗ «погашенная сумма … по штрафам» | ТЗ (расчёты по кредиту) |
| В т. ч. по решениям суда | `i_al_court_v` `i_al_court_som` | часть платежа, погасившая долг по решениям суда (судебный слой) | «погашено по решениям суда»; свободный слой — `i_amount − i_al_court` при чтении | «Погашения» `payAlloc` · `СС-148` |
| В т. ч. просроченного | `i_al_over_v` `i_al_over_som` | часть платежа, погасившая просроченную задолженность, по всем статьям | «погашено просроченной задолженности» — главный показатель работы с долгом | `СС-148` |

---

## 10. `stat_row_receipt` — поступление

Поступление — факт прихода денег, нашёлся кредит или нет. Из одного поступления получается
ноль, один или несколько платежей. Тождество «Погашений»: сумма = разнесено платежами +
возвращено + нераспределённый остаток; невыясненное — поступление без платежей. Хранится как
событие с приращениями: исходная строка — на дату поступления плюс один, изменения после
закрытия месяца — строки-поправки. Читать на дату — функцией `stat_receipt_at(D)`: суммы
складываются, состояния берутся из последней строки. Всё приходит от «Погашений», колонок
`src_*` нет. Охват общий — решение пользователя (`СС-144`).

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Поступление | `object_id` | id поступления в «Погашениях»; одинаков во всех строках | часть ключа; с ним сходится `d_receipt_id` платежа | `ADR-0239` |
| Дата строки | `slice_date` | дата поступления плюс один; у поправки — день изменения плюс один | часть ключа; «невыясненные на 01.06» — сумма строк `slice_date ≤ 01.06` | `ADR-0239` §2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Неточные величины | `now_cols` | имена величин, которые «Погашения» отдали текущими | сноска паспорта | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Вид строки | `d_corr_kind` | `original` исходная · `bind` привязка · `refund` возврат · `match` подтверждение или отзыв · `freeze` заморозка · `amount` исправление суммы; входит в ключ | поправка после закрытия месяца несёт приращение сумм и новое состояние; порядок строк одной даты — по этому списку | `ADR-0239` §4 · fizschema §13 |
| Дата исправления | `d_corr_date` | когда изменили поступление; у исходной — `NULL` | паспорт закрытого месяца: где учтено изменение | `ADR-0239` §5 |
| Приход денег | `d_rdate` | когда деньги пришли | «дней без опознания» у невыясненных — `D − d_rdate` при чтении | `СС-144` |
| Первая привязка | `d_bdate` | когда поступление впервые привязали к кредиту; `NULL` — невыясненное | «средний срок опознания» — `d_bdate − d_rdate` | `СС-144` |
| Канал | `d_chan` | `gateway` шлюз · `bank` банк · `cbk` импорт казначейства · `manual` ручной ввод | «поступления по каналам»; ручной ввод — зона контроля. Потребитель в каталоге отчётности не назван | «Погашения» `CHAN` |
| Сопоставление | `d_match` | сверка с реестром казначейства: `pending` · `confirmed` · `reversed` · `restored` · `mismatch` · `none` — шесть значений, как `MATCH` «Погашений» | «не подтверждено казначейством», «расхождения с реестром» на дату | fizschema §15.1 п. 4 |
| Заморожено | `d_frozen` | деньги заморожены арестом счёта | «замороженные поступления на дату» | `СС-144` |

### Деньги и счётчики

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Валюта | `cur` | валюта денег; обычно сом | в какой валюте все `_v` поступления | `ADR-0240` §2 |
| Курс | `rate` `rate_date` | курс НБКР на дату поступления; для сомов — 1 | снимок пересчёта в сомы на дату прихода | `ADR-0240` §3 |
| Сумма поступления | `i_amount_v` `i_amount_som` | сумма, которая пришла; у поправки `amount` — приращение | «поступило за период»; первое слагаемое невыясненного остатка | «Погашения» `receiptSplit` |
| Разнесено платежами | `i_paid_v` `i_paid_som` | сколько разнесено платежами по кредитам; у поправки — приращение | нераспределённый остаток `i_amount − i_paid − i_ret` при чтении: «сколько невыясненных денег на дату» | «Погашения» `receiptSplit` |
| Возвращено | `i_ret_v` `i_ret_som` | сколько возвращено плательщику | то же — третье слагаемое тождества | «Погашения» `receiptSplit` |
| Платежей | `i_pays` | число платежей из поступления; у поправки — приращение | «сводные поступления на несколько кредитов» | «Погашения» `receiptSplit` |

---

## 11. `stat_row_measure` — мера взыскания × цель

Мера — зарегистрированный документ взыскания: претензия, иск, решение суда, исполнительный
лист, … Один документ — одна мера, а целей у неё несколько: требования к разным лицам и по
разным кредитам. Строка — «мера × цель». Мера — событие с полным состоянием: строка пишется
при регистрации, каждое изменение после закрытия месяца — новая строка с полным состоянием
цели. Читать на дату — функцией `stat_measure_at(D)`: последняя строка пары не позже D.
Итоги сумм — по `d_primary` и `d_mstate = 'действует'`, число мер — `count(distinct
object_id)`. Признаки кредита и заёмщика — join требование → кредит → заёмщик на дату.

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Мера | `object_id` | id меры в модуле «Взыскание» | часть ключа; ТЗ «количество принятых мер» — `count(distinct object_id)`: иск к двум ответчикам — одна мера | ТЗ (расчёты по взысканию) · `СС-149` |
| Цель | `target_id` | требование, к которому относится мера | часть ключа; ключ join и охвата к `stat_row_claim`: меру видит текущий владелец требования-цели | `ADR-0243` §3 · fizschema §0.11 |
| Дата строки | `slice_date` | день события плюс один: регистрации или изменения | часть ключа; состояние на D — последняя строка пары `≤ D` | `ADR-0239` §3 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ кураторства | `src_curation` | как ответило кураторство: автор меры и его подразделение | без ответа мера выпадает из второго правила охвата и из разреза «кто сделал»; входит в `is_partial` | `ADR-0208` · `ADR-0243` |
| Текст ошибки | `src_detail` | текст ошибки кураторства, если ответ не `ok` | причина неполной строки | `ADR-0245` §12 |
| Строка неполна | `is_partial` | вычисляемая: `src_curation` не `ok` и не `legacy` | очередь дозаполнения; пока такие строки есть, месяц не закрыть | `ADR-0208` §3 |
| Неточные величины | `now_cols` | имена величин, которые сосед отдал текущими | сноска паспорта | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Вид меры | `d_kind_id` `d_kind_lbl` | вид документа: претензия, иск, решение суда, исполнительный лист, … | ТЗ #17 «по видам принимаемых мер»; ФО-17 «Взыскание — процедуры, фазы, события» | ТЗ #17 · ФО-17 |
| Состояние меры | `d_mstate` | 'действует' · 'сторнирована' — сторно ставится по каждой цели отдельно | сторнированная цель не входит в суммы и счётчики | `СС-149` |
| Представитель кредита | `d_primary` | цель-заёмщик, иначе единственная цель по кредиту, иначе старшая | суммы мер без задвоения: иск к заёмщику и поручителю по одному кредиту — одна сумма | `СС-149` |
| Роль лица цели | `d_role` | роль лица цели — значения как у `stat_row_claim.d_role` | «присуждено поручителям»: у поручителя присуждённое — его предел ответственности, а не второй долг | `СС-149` |
| Исход | `d_outcome_id` `d_outcome_lbl` | значение из справочника исходов своего вида меры, по каждой цели; у видов без исхода (иск, жалоба) — `NULL`, результат — следующая мера | ТЗ #18 «по видам результата по принятым мерам»; ТЗ «количество результатов» | ТЗ #18 · ТЗ (расчёты по взысканию) |
| Знак исхода | `d_outcome_sign` | 'положительный' · 'отрицательный' · 'нейтральный' — копия знака, который справочник исходов даёт значению, на дату | ТЗ «количество и сумма положительных и отрицательных результатов». Копия на дату: правка справочника не переписывает закрытые месяцы | ТЗ (расчёты по взысканию) · `СС-149` |
| Источник документа | `d_source` | 'наш документ' · 'внешний акт' (решение суда, письмо должника) | отделить наши меры от входящих актов | `СС-149` |
| Дата события | `d_evdate` | когда произошло событие документа: вынесено решение, составлена претензия | нормативные сроки отсчитываются от события, а не от ввода | `СС-149` |
| Дата получения | `d_recvdate` | когда входящий документ получен | «решения, полученные позже чем через 5 дней» — `d_recvdate − d_evdate` | `СС-149` |
| Дата регистрации | `d_regdate` | когда мера внесена в систему | отчёты за период строятся по ней: «исков за март» | `СС-149` |
| Направлено | `d_sentdate` | когда документ направлен должнику | срок должника идёт от направления; «дней с направления» — `D − d_sentdate` при чтении | `СС-149` |
| Канал направления | `d_channel` | 'СЭД' · 'Почта' · 'Нарочно' · 'публикация'. «Публикация» есть в спецификации взыскания, в макете её нет | «направлено по каналам» | `СС-149` |
| Вручено | `d_servdate` | когда документ вручён | «претензии, направленные более 14 дней назад и не врученные»; пакет в суд без вручения вернут | `СС-149` |
| Вступление в силу | `d_forcedate` | когда решение вступило в силу | «решения, не вступившие в силу на 01.06» | `СС-149` |
| Инстанция | `d_instance` | судебная инстанция акта, четыре значения | «решения по инстанциям». ⚠ Перечня нет: у «Взыскания» инстанция — вид меры (апелляция, кассация) | fizschema §15.1 п. 6 |
| Мера-основание | `d_parent_id` | мера, на которой основана эта: лист → решение, решение → иск, повторная претензия → первичная | «чем кончились иски»: иск ← решение → знак исхода | `СС-149` |
| Подразделение автора | `d_reg_unit_id` `d_reg_unit_lbl` | подразделение сотрудника, зарегистрировавшего меру, на дату регистрации | ТЗ #5 для мер: «меры по подразделениям» — кто работал, а не текущий владелец требования. Второе правило охвата: автор видит свою меру после передачи требования | ТЗ #5 · `ADR-0243` §3 |
| Вышестоящее подразделение автора | `d_reg_unit_parent_id` `d_reg_unit_parent_lbl` | узел над ним на дату регистрации | свод мер на уровень выше | ТЗ #5 · `ADR-0241` §4 |
| Автор меры | `d_reg_curator_id` `d_reg_curator_lbl` | сотрудник, зарегистрировавший меру | ТЗ #6 для мер; второе правило охвата `own` | ТЗ #6 · `ADR-0243` §3 |
| Дело | `d_case_id` | дело требования-цели | меры по делу: «дела, где больше 5 мер», результаты по делам | `СС-145` · `СС-149` |
| Кредит | `d_credit_id` | кредит требования-цели | путь к признакам кредита и заёмщика на дату; `d_primary` выбирается внутри кредита | `СС-149` |

### Деньги

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Валюта | `cur` | валюта кредита цели | в какой валюте `i_claim_v` и `i_award_v` | `ADR-0240` §2 |
| Курс | `rate` `rate_date` | курс НБКР на дату документа меры; дальше не меняется | снимок суммы в документе, а не остаток | `ADR-0240` §3 · `СС-149` |
| Требуемое | `i_claim_v` `i_claim_som` | сколько требуется по цели; `NULL` — не заявлялось | ТЗ «сумма по принятым мерам» — по `d_primary` и действующим | ТЗ (расчёты по взысканию) |
| Присуждённое | `i_award_v` `i_award_som` | сколько присуждено по цели; `NULL` — не присуждалось, это не ноль: после отмены решения сумма пуста | ТЗ «сумма по результатам принятых мер», в том числе по положительным и отрицательным | ТЗ (расчёты по взысканию) · `СС-149` |
| Расходы меры | `i_exp_som` | госпошлина, услуги представителя; возникают в сомах | «сколько стоили меры» | `СС-149` |

---

## 12. Служебные таблицы

Семнадцать таблиц модуля и четыре колонки статистики в общем календаре периодов
(`ADR-0245`). Они описывают, что лежит в строках (реестр), как строки появились (прогон,
очередь, журнал), что в закрытом месяце разошлось с соседями (маркер), кто закрыл месяц
(календарь), кто читает записи реестра (индекс ссылок) и что выгружено (выгрузки). Чисел
объектов в них нет: величины лежат только в строках.

### `stat_registry` — реестр

Запись реестра называет объект, разрез, показатель или агрегат. Для разреза и показателя
запись указывает колонку в таблице строк. Ни одна величина не существует вне реестра, а
реестр не хранит чисел (`ADR-0209`).

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Запись | `id` | id записи реестра | на неё ссылаются уровни, корзины, периоды действия, индекс ссылок и фильтры выгрузок; основание агрегата — тоже запись | `ADR-0209` |
| Вид записи | `kind` | `object` · `dimension` · `indicator` · `aggregate` | от вида зависит набор обязательных полей: у разреза — уровни и корзины, у показателя — источник, у агрегата — функция, у объекта — способ хранения | `ADR-0209` §1–§3 |
| Объект | `object` | код объекта: `credit` · `borrower` · `group_member` · `collateral` · `zdeal` · `case` · `claim` · `program` · `repay` · `receipt` · `measure` | к какой таблице `stat_row_<объект>` относится запись | `ADR-0237` §3 |
| Название | `title` | имя записи для людей | подпись в конструкторе отчётов, в паспорте ответа и на экране вывода записи | `ADR-0209` §1 |
| Колонка | `col_name` | основа имени колонки в таблице строк; у объекта и агрегата — `NULL`. Не `column`: это слово зарезервировано | при старте приложение сверяет реестр со схемой: запись без колонки попадает в список «ждёт колонку», колонка без записи — предупреждение в журнал | `ADR-0237` §3 · `ИС-53` |
| Вид значения | `value_type` | `ref` · `ref_arr` · `code` · `cls` · `hier` · `date` · `int` · `num` · `bool` · `money_cur` · `money_som` · `cur` | из него выводятся суффиксы колонок: `ref` — `_id` + `_lbl`, `cls` — три колонки, `money_cur` — `_v` + `_som`. Сверка со схемой проверяет все колонки пары и слота | `ADR-0237` §3 · `ИС-57` |
| Агрегат | `agg_fn` `agg_of` | функция агрегата — `count` · `sum` · `avg` · `min` · `max` — и запись, над которой он считается | администратор заводит агрегат без релиза; считается при чтении из строк, в базе не хранится | `ADR-0209` §2 · `ADR-0147` §2 |
| Исчислим налету | `live` | показатель можно отдать живым запросом, без ночного прогона | режим «сейчас» берёт только такие показатели. Смена признака проходит экраном индекса ссылок: она ломает тех же читателей, что и вывод записи | `ADR-0159` · `ADR-0177` §5 |
| Способ хранения | `storage` | у записи-объекта: `state` · `event_delta` · `event_full` | как читать строки объекта на дату (§0.4) и какие счётчики прогона у него заполнены (`stat_run_count`) | `ADR-0239` |
| Путь к чужому значению | `via_table` `via_key` | у разреза: таблица строк другого объекта и ключ связи | признак чужого объекта берётся join-ом, а не копией: отрасль заёмщика в отчёте по кредитам — через `stat_row_borrower` по `d_borrower_id` | `ADR-0241` §8 |

### `stat_registry_level` — уровни разреза

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Разрез | `record_id` | запись реестра — разрез с уровнями | к какой иерархии относится уровень | `ADR-0176` §4 |
| Уровень | `ord` `code` `title` | номер, код и название уровня: у подразделения два — конечное и вышестоящее, у территории четыре — область, район, айылный округ, село | отчёт предлагает уровни по порядку и сводит на выбранный | `ADR-0241` §4 |
| Колонка уровня | `col_name` | основа имени колонки уровня: `d_unit`, `d_unit_parent`, `d_terr_region`, … | по ней отчёт находит колонки уровня в таблице строк | `ADR-0241` §4 |

### `stat_registry_bucket` — корзины разреза

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Разрез | `record_id` | запись реестра, над колонкой которой объявлены корзины | к какой колонке относится корзина | `ADR-0176` §2 |
| Корзина | `code` `title` | корзина из списка, допустимого для вида значения: год · квартал · месяц у даты, 1 · 31 · 91 · 181 у дней просрочки | корзины живут в реестре, а не в настройке отчёта, поэтому во всех отчётах одни и те же. Своей колонки у корзины нет — она раскладывает существующую | `ADR-0176` · `ADR-0209` §5 |

### `stat_registry_period` — периоды действия записи

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Запись | `record_id` | запись реестра | чей период действия | `ADR-0209` §6 |
| Период | `date_from` `date_to` | с какой даты по какую запись действует; `date_to` `NULL` — действует | запись не удаляют, а прекращают с даты: колонка с историей остаётся, а конструктор не предлагает выведенную запись для новых дат | `ADR-0209` §6 · `ADR-0237` |
| Кто изменил | `by_id` `by_lbl` | кто включил или выключил запись | след решения администратора | `ADR-0245` §1 |
| Причина | `reason` | почему запись включили или выключили | то же | `ADR-0245` §1 |

### `stat_registry_scope` — правила охвата

Кто видит строки объекта. Правила одного объекта соединяются ИЛИ и режут строки до
группировки. Объект без правила валит загрузку реестра (`ADR-0243`).

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Объект | `object` | код объекта | к какой таблице строк применяется правило | `ADR-0243` |
| Номер правила | `rule_no` | номер правила внутри объекта | у меры правил два: через требование-цель и по автору | `ADR-0243` §3 |
| Вид правила | `kind` | `own` · `via` · `open` | `own` режет по своим колонкам, `via` — через строки другой таблицы на ту же дату, `open` — роль не сужает | `ADR-0243` §1 |
| Путь через таблицу | `via_table` `via_key` | у `via`: таблица и ключ связи — например, `stat_row_claim` по `d_case_id` для дела | дело видно, если видно хоть одно его требование; член группы — если видна строка заёмщика | `ADR-0243` §3 |
| Свои колонки охвата | `cols` | у `own`: колонки куратора и подразделения — например, `d_curator_id`, `d_unit_id`, `d_unit_parent_id` | по ним куратор видит свои строки, подразделение и вышестоящее — строки своих людей | `ADR-0243` §2 |
| Причина общего охвата | `reason` | у `open`: почему объект видят все | паспорт ответа печатает причину: программы — общее знание, договоры и поступления — решение пользователя 17.09.2026 | `ADR-0243` §4 · `СС-144` |

### `stat_cls_slot` — слоты классификаторов

Какой классификатор администратора лежит в каком слоте `d_cls1…d_cls10` у кредита, заёмщика и
залога (`ADR-0241` §5).

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Слот | `object` `slot` | объект (`credit` · `borrower` · `collateral`) и номер слота 1…10 | по ним отчёт находит колонки `d_clsN_*` нужного классификатора | `ADR-0241` §5 |
| Классификатор | `classifier_id` | классификатор, который занял слот; название — у классификации | один классификатор — один слот объекта (`UNIQUE`) | `ADR-0241` §5 |
| Срок в слоте | `since` `until` | с какой даты слот заполняется и по какую; `until` — классификатор выведен | до `since` в слоте `NULL` — «классификатора ещё не было», а не «без значения». Слот не переиспользуется: в нём история | `ADR-0245` §1 |
| Кто занял | `taken_by_id` `taken_by_lbl` `taken_at` | кто и когда занял слот | след решения администратора; при восьми занятых слотах из десяти — предупреждение | `ADR-0245` §1 |

### `stat_run` — прогон

Прогон пишет строки за одну или несколько дат. Прогоны идут строго по одному.

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Прогон | `id` | номер прогона, `bigint` | на него ссылаются `run_id` каждой строки и журналы прогона; внешних ключей из строк нет | `ADR-0245` §2 |
| Вид прогона | `kind` | `nightly` ночной · `catchup` догон пропущенных ночей · `retro` пересчёт задним числом · `manual` ручной · `closing` закрытие месяца · `reopen` повторное открытие | журнал срезов объясняет, откуда строка. Только ночной двигает окно вопроса соседям | `ADR-0245` §2 |
| Даты среза | `date_from` `date_to` | за какие даты прогон писал строки | какой прогон отвечает за дату | `ADR-0245` §2 |
| Статус | `status` | `running` · `done` · `partial` · `failed` · `skipped` | дата видна, только если за неё есть прогон `done` или `partial`. `running` одновременно один — `UNIQUE ((true)) WHERE status = 'running'` | `ADR-0238` · `ADR-0245` §2 |
| Время | `started_at` `finished_at` | когда прогон начался и кончился | длительность прогона, поиск зависших | `ADR-0245` §2 |
| Кто и почему | `actor_id` `actor_lbl` `reason` | кто запустил и почему; обязательны у ручного, закрытия и повторного открытия, причина — ещё у `skipped` | за каждым ручным действием есть человек и основание | `ADR-0245` §2 |

### `stat_run_source` — ответ соседа в прогоне

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Прогон | `run_id` | прогон | к какому прогону относится ответ | `ADR-0245` §2 |
| Сосед | `source` | `core` · `class` · `curation` · `pay` · `subj` | чей ответ | `ADR-0245` §2 |
| Режим | `mode` | `delta` — «что изменилось» · `full` — полный обход | догон, закрытие и ручной пересчёт идут полным обходом | `ADR-0245` §2 |
| Окно вопроса | `t_from` `t_to` | за какой отрезок времени прогон спросил соседа об изменениях; `t_to` — момент перед вопросом | текущее `T` соседа отдельно не хранится: это последний `t_to` с `ok` в режиме `delta`. Следующий вопрос — с перекрытием `t_from = T − запас`, чтобы изменение на границе не потерялось | `ADR-0245` §2 · `ADR-0221` |
| Ответ | `state` | `ok` · `unavailable` · `error` · `timeout` · `denied` · `legacy` | почему строки за дату неполны: сосед не ответил в этом прогоне | `ADR-0208` · `ADR-0245` §2 |
| Счётчики | `changed` `failed` | сколько объектов сосед назвал изменившимися и сколько не ответил | масштаб изменений и сбоев | `ADR-0245` §2 |
| Текст | `detail` | текст ошибки или пояснение | причина сбоя в журнале срезов | `ADR-0245` §2 |

### `stat_run_count` — счётчики прогона по объекту

В конце прогона счётчики сверяются: охват = пересчитано + скопировано + упало +
ненаступившие + безродные. Не сошлось — прогон `failed`, дата не публикуется.

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Прогон и объект | `run_id` `object` | прогон и код объекта | ключ; одна строка на объект прогона | `ADR-0245` §2 |
| Способ хранения | `storage` | способ хранения объекта на момент прогона — копия из реестра | для `CHECK`: у состояний нет счётчиков событий, у событий нет «скопировано» | `ADR-0245` §2 |
| Отдал владелец | `owner_total` | сколько объектов в списке владельца | охват — левая часть сверки | `ADR-0245` §2 |
| Кандидаты | `candidates` | сколько отобрано к пересчёту по ответам соседей | масштаб изменений за ночь | `ADR-0245` §2 |
| Пересчитано | `recomputed` | сколько строк пересчитано | слагаемое сверки | `ADR-0245` §2 |
| Изменилось | `changed` | сколько пересчитанных строк действительно изменилось | отличить «пересчитали» от «поменялось» | `ADR-0245` §2 |
| Скопировано | `copied` | сколько строк скопировано со вчерашнего дня; только у состояний | слагаемое сверки | `ADR-0238` · `ADR-0245` §2 |
| Упало | `failed` | сколько объектов не удалось посчитать | слагаемое сверки; поимённо — в `stat_run_item` | `ADR-0245` §2 |
| Ненаступившие | `not_born` | объект есть у владельца, но дата раньше его рождения | слагаемое сверки: «строк 0» отличается от «владелец не отдал список» | logika §2.5 |
| Безродные | `no_birth` | рождение объявлено, а даты рождения у записи нет | слагаемое сверки; поимённо — в `stat_run_item` | logika §2.5 |
| Неполные | `partial` | сколько строк записано неполными | от него статус прогона `partial` | `ADR-0208` |
| Закрытие: дописано и переписано | `appended` `rewritten` | при закрытии месяца: сколько строк первого числа дописано и сколько переписано после переспроса соседей | что изменило закрытие | `ADR-0245` §7 |
| События: строки и поправки | `new_rows` `corr_rows` | только у событий: новых строк и строк-поправок | у событий нет «скопировано»; поправки видны отдельным счётчиком | `ADR-0239` · `ADR-0245` §2 |
| Маркеры | `markers` | сколько маркеров расхождения поставлено | у всех объектов | `ADR-0245` §5 |

### `stat_run_item` — исключения прогона поимённо

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Прогон | `run_id` | прогон | к какому прогону относится исключение | `ADR-0245` §2 |
| Строка | `object` `object_id` `target_id` | объект, его id и у меры — цель | какой объект не записан | `ADR-0245` §2 |
| Вид исключения | `kind` | `not_born` · `no_birth` · `failed` | те же счётчики, что в `stat_run_count`, но поимённо: по ним разбирают сбой | `ADR-0245` §2 |
| Пояснение | `detail` | текст ошибки или причина | то же | `ADR-0245` §2 |

### `stat_queue` — очередь пересчёта

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Задача | `id` | id задачи | ключ | `ADR-0245` §4 |
| Вид задачи | `kind` | `retro` пересчёт задним числом · `manual` ручной · `reopen` после повторного открытия | сосед сообщил изменение с датой действия в открытом месяце — задача `retro` с этой даты по сегодня | `ADR-0245` §3–§4 |
| Что пересчитать | `obj_table` `object_id` | таблица строк и объект; у «всех объектов» `object_id` — `NULL`, у `reopen` таблица — `NULL` | у объекта не больше одной открытой задачи: новая сливается с ней, дата берётся более ранняя | `ADR-0245` §4 |
| Даты | `date_from` `date_to` | с какой даты и по какую пересчитать; `date_to` `NULL` — по последнюю опубликованную | задача закрывается, когда пересчитаны все её даты | `ADR-0245` §4 |
| Откуда задача | `source` | `poll` опрос соседей · `admin` администратор · `calendar` календарь | кто поставил задачу | `ADR-0245` §4 |
| Кто и почему | `actor_id` `actor_lbl` `reason` | кто поставил задачу вручную и почему | след ручного пересчёта | `ADR-0245` §4 |
| Статус | `status` | `new` · `done` | выполненные задачи удаляются при закрытии месяца | `ADR-0245` §4 |
| Выполнил | `run_id` | прогон, который выполнил задачу | связь задачи с журналами прогона | `ADR-0245` §4 |

### `stat_rewrite_log` — журнал перезаписи

Какая строка перезаписана и почему. Значения не хранятся — только имена изменившихся колонок.
Строка-поправка события сюда не пишется: её след — сама строка, маркер и счётчик.

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Запись | `id` | id записи журнала | ключ | `ADR-0245` §4 |
| Прогон | `run_id` | прогон, который перезаписал строку | подробности — в журналах прогона | `ADR-0245` §4 |
| Строка | `row_table` `object_id` `slice_date` `target_id` | таблица, объект и дата перезаписанной строки; `target_id` — только у меры | «почему строка 01.06 кредита КД-117 не такая, как вчера»; индекс `(row_table, slice_date)` | `ADR-0215` §6 |
| Изменившиеся колонки | `changed` | имена колонок, которые изменились | что именно поменялось, без хранения значений | `ADR-0215` §6 |
| Причина | `reason` | `retro` · `backfill` дозаполнение · `reopen` · `manual` · `closing` · `legacy` | дозаполнение неполной строки тоже оставляет след | `ADR-0245` §4 |
| Кто | `actor_id` `actor_lbl` | человек; обязателен у `manual` и `legacy` | за ручной правкой и правкой легаси есть человек | `ADR-0245` §9 |
| Когда | `at` | время перезаписи | порядок перезаписей одной строки | `ADR-0245` §4 |

### `stat_fixed_divergence` — маркер расхождения

Запись о строке закрытого месяца, которая разошлась с соседями. Закрытая строка не
переписывается — маркер говорит, что и почему теперь было бы иначе. Маркеры хранятся всегда.

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Маркер | `id` | id маркера | ключ | `ADR-0245` §5 |
| Строка | `row_table` `object_id` `slice_date` | таблица, объект и дата строки закрытого месяца | паспорт закрытого отчёта предупреждает о расхождении в этой строке | `ADR-0148` §5 · `ADR-0245` §5 |
| Цель | `target_id` | только у меры | строка меры — пара «мера × цель» | `ADR-0239` §5 |
| Вид поправки | `corr_kind` | только у платежа и поступления | строка события — с видом строки в ключе | `ADR-0239` §5 |
| Изменившиеся колонки | `changed` | имена колонок, которые разошлись; значения не хранятся | число устареет к следующему прогону — его даёт живой расчёт | `ADR-0245` §5 |
| Основание | `basis` | какое действие соседа вызвало расхождение | «платёж перепривязан 15.07» в паспорте | `ADR-0245` §5 |
| Нашёл и закрыл | `found_run` `closed_run` | прогон, нашедший расхождение, и прогон, закрывший маркер | открытый маркер один на строку: `closed_run IS NULL` | `ADR-0245` §5 |
| Как закрыт | `closed_how` | `converged` пересчёт сошёлся · `reopen` период открыли · `corrected_later` учтено поправкой | у событий маркер ставит поправка и сразу закрывает: `closed_run = found_run` | `ADR-0239` §5 · `ADR-0245` §5 |
| Где поправка | `corr_slice_date` | дата строки-поправки; есть тогда и только тогда, когда `corrected_later` | паспорт мая: «поправка учтена в июле» | `ADR-0239` §5 |

### `period_calendar` — календарь периодов

Общий календарь учётных периодов (`ADR-0204`). Статистика пишет в него только свои четыре
колонки; признак легаси — общая колонка периода `origin`.

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Закрыт статистикой | `stat_closed_at` | когда статистика закрыла период; `NULL` — открыт | по ней триггер `stat_row_guard` запрещает запись в строки периода | `ADR-0245` §6 · §8 |
| Кто закрыл | `stat_by_id` `stat_by_lbl` | человек, закрывший период; календарь сам не актор | за закрытием есть человек | `ADR-0245` §6 · `ИС-9` |
| Распоряжение | `stat_order` | распоряжение, по которому закрыт период | основание закрытия | `ADR-0245` §6 |
| Прогон закрытия | `stat_run` | прогон `closing` | что изменило закрытие — в журналах этого прогона, а не в календаре | `ADR-0245` §6 |

### `stat_ref_index` — индекс ссылок

Кто читает записи реестра. Ссылку публикует потребитель — шаблон отчёта, плитка, правило
классификатора; статистика своих потребителей не знает (`ADR-0177`).

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Запись | `record_id` | запись реестра: показатель, агрегат или разрез | на что ссылаются | `ADR-0245` §10 |
| Потребитель | `consumer` | `report` · `dashboard` · `classify` | кто ссылается | `ADR-0177` §1 |
| Документ | `doc_id` | шаблон, а не редакция | публикация новой редакции заменяет все строки документа одной транзакцией | `ADR-0177` §6 |
| Место | `place` | графа, плитка, правило | где именно в документе запись | `ADR-0177` |
| Название документа | `doc_title` | название с номером редакции | экран вывода записи перечисляет, что сломается | `ADR-0177` §4 |
| Владелец документа | `owner_unit_id` `owner_unit_lbl` | подразделение-владелец | экран вывода называет, чьё сломается | `ADR-0177` §4 |
| Опубликовано | `published_at` | когда опубликована редакция | с какой редакции действует ссылка | `ADR-0177` §6 |

### `stat_export_job` — задание выгрузки

Ответ выше порога детализации выдаётся не списком, а выгрузкой-заданием (`ADR-0178`).

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Задание | `id` | id задания | на него ссылаются фильтр и скачивания | `ADR-0245` §11 |
| Вопрос | `obj_table` `slice_date` | таблица строк и дата среза | что выгружать | `ADR-0178` §5 |
| Заказчик | `ordered_by_id` `ordered_by_lbl` `ordered_at` | кто и когда заказал | выгрузка читает строки в охвате заказчика | `ADR-0178` §6 |
| Статус | `status` | `queued` · `ready` · `error` · `expired` | файл удаляется через N дней, задание остаётся с `expired` | `ADR-0245` §11 |
| Строк в файле | `row_count` | сколько строк выгружено | полнота: список сходится с реестром один в один | `ADR-0178` §3 |
| Файл | `file_ref` | ссылка в файловом хранилище Jmix | откуда скачивать | `ADR-0245` §11 |
| Область видимости | `scope_lbl` | охват заказчика словами | паспорт файла: чья это выборка | `ADR-0178` §6 |
| Ошибка | `error` | причина `error` — например, «15.07 больше не хранится: июль закрыт» | дату могло удалить закрытие месяца, пока задание ждало | `ADR-0245` §11 |

### `stat_export_filter` — фильтр выгрузки

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Задание | `job_id` | задание выгрузки | чей фильтр | `ADR-0245` §11 |
| Группа | `grp` | номер группы условий | внутри группы условия соединяются И, группы между собой — ИЛИ | `ADR-0245` §11 |
| Условие | `record_id` `op` `value` | запись реестра, операция и значение | условие на колонку записи; фильтр по записи, а не по имени колонки | `ADR-0245` §11 |

### `stat_export_download` — скачивания выгрузки

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Задание | `job_id` | задание выгрузки | что скачано | `ADR-0245` §11 |
| Кто | `user_id` `user_lbl` | кто скачал файл | след выдачи данных из системы | `ADR-0245` §11 |
| Когда | `at` | когда скачал | то же | `ADR-0245` §11 |

---

## 16. `stat_row_app` — заявка на получение кредита

Строка заявки начинается с ПОДАЧИ, а не с черновика: номер `З-2026-000123` и дата
присваиваются при подаче (ТЗ 03 §4.9), и до неё записи у владельца нет. Статус и стадия —
две независимые оси: статус есть юридическая веха, стадия — где физически стоит
комплектация (ТЗ 03 §3.3), и «Новый» бывает и на комплектации, и на заключениях отделов.
Территория берётся у ЗАЯВИТЕЛЯ, подразделение — у специалиста: заявка из Ошской области
ведётся отделом ГО, и это два разных среза, а не один.

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Заявка | `object_id` | id заявки в модуле «Заявки» | первая половина ключа; «сколько заявок подано» — число строк на дату | ТЗ 03 §4.7 |
| Дата среза | `slice_date` | дата, на начало которой снято состояние | вторая половина ключа; чтение на дату | `ADR-0238` §2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ «Заявок» | `src_app` | ответил ли владелец за эту строку | молчание соседа не притворяется нулём | `ADR-0208` §2 |
| Что молчало | `src_detail` | какие величины не пришли и почему | сноска паспорта называет причину поимённо | `ADR-0208` §2 |
| Строка неполна | `is_partial` | вычисляется из ответа владельца | отбор полных строк в отчёт | `ADR-0208` §2 |
| Неточные величины | `now_cols` | имена величин, отданных «текущими», а не на дату | сноска паспорта | `ADR-0242` |
| Валюта | `cur` | валюта запрашиваемой суммы | суммы разных валют не складываются молча | `ADR-0214` §1 |
| Курс строки | `rate` | курс валюты заявки к сому, замороженный в строке | сомовая сторона считается по курсу СВОЕЙ строки | `ADR-0214` §2 |
| Дата курса | `rate_date` | на какой день взят курс | ответ показывает, каким курсом пересчитано | `ADR-0214` §2 |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Подразделение специалиста | `d_unit_id` `d_unit_lbl` | подразделение кредитного специалиста на дату | «заявки по филиалам»; подразделение специалиста, а не адрес заявителя | ТЗ 03 §4.4 |
| Блок | `d_unit_parent_id` `d_unit_parent_lbl` | вышестоящий узел оргструктуры на дату | свод по блокам без сегодняшнего справочника | `ADR-0241` §2 |
| Кредитный специалист | `d_spec_id` `d_spec_lbl` | кто ведёт заявку | колонка охвата: специалист видит свои заявки; «нагрузка по специалистам» | ТЗ 03 §4.4 · `ИС-58` |
| Кредитная программа | `d_program_id` `d_program_lbl` | программа, под которую просят кредит | «заявок по программе ФРР-2025 — сколько и на какую сумму» | ТЗ 03 §4.2 |
| Область заявителя | `d_terr_region_id` `d_terr_region_lbl` | область в адресе субъекта-заявителя | спрос по территориям; объект определения — заявка, а не кредит | `ADR-0206` §3 |
| Район заявителя | `d_terr_district_id` `d_terr_district_lbl` | второй уровень той же иерархии | разбор области по районам | `ADR-0176` §4 |
| Заявитель | `d_borrower_id` `d_borrower_lbl` | субъект, подавший заявку | ключ join к `stat_row_borrower`: заявки раскладываются по отрасли и типу лица заявителя | `ADR-0241` §8 |
| Статус заявки | `d_status` | девять статусов одной дорожки | статкарточки над списком: «сколько на рассмотрении, сколько отклонено» | ТЗ 03 §3.1 |
| Стадия | `d_stage` | узел пайплайна: комплектация → решение | где физически стоит работа; ортогонален статусу | ТЗ 03 §3.3 |
| Поток | `d_flow` | 'A' — от заёмщика, 'B' — по решению | два потока происхождения идут разными дорогами, и мерить их вместе нечестно | ТЗ 03 §1.2 |
| Дата подачи | `d_subdate` | день, когда заявка подана и получила номер | корзины год · квартал · месяц: «заявок за июль» | ТЗ 03 §4.9 |

### Показатели

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Запрашиваемая сумма | `i_amount_v` `i_amount_som` | сколько просят, в валюте заявки и в сомах | единственная денежная колонка списка; спрос «запрошено за период» | ТЗ 03 §4.2 |
| Одобренная сумма | `i_approved_v` `i_approved_som` | сколько одобрила комиссия; `NULL` до решения | разница «запрошено ↔ одобрено» и есть вопрос к комиссии; пустое не есть ноль | ТЗ 03 §5.2 · `ADR-0151` |
| Дней с подачи | `i_days` | срок рассмотрения; после решения ЗАМИРАЕТ | «средний срок рассмотрения» не растёт у решённых заявок вечно | `ИС-1` |

---

## 17. `stat_row_rs_app` — заявка на реструктуризацию

Единица работы модуля — обращение, а не кредит и не транш (РС-2). Стадия у владельца полем
не лежит: это свёртка журнала актов (РС-23), и витрина читает её историей. Разрезов «Кредит»,
«Транш» и «Вид» у заявки нет: кредитов в охвате несколько, а видов на одной заявке бывает не
один — многозначное поднимает уровень, а не сплющивается в клетку (`ИС-21`). Кредит и транш
живут у расчёта (§18); вид ждёт уровня «применённое изменение условия» — заявка владельцу.

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Обращение | `object_id` | id заявки в модуле «Реструктуризация» | первая половина ключа; «сколько обращений подано» — число строк | ТЗ 15 §2 |
| Дата среза | `slice_date` | дата, на начало которой снято состояние | вторая половина ключа | `ADR-0238` §2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ «Реструктуризации» | `src_rs` | ответил ли владелец за эту строку | молчание соседа не притворяется нулём | `ADR-0208` §2 |
| Что молчало | `src_detail` | какие величины не пришли и почему | сноска паспорта | `ADR-0208` §2 |
| Строка неполна | `is_partial` | вычисляется из ответа владельца | отбор полных строк в отчёт | `ADR-0208` §2 |
| Неточные величины | `now_cols` | имена величин, отданных «текущими» | сноска паспорта | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Подразделение куратора | `d_unit_id` `d_unit_lbl` | подразделение куратора ОД на дату | «обращения по филиалам» | ТЗ 15 §9 |
| Блок | `d_unit_parent_id` `d_unit_parent_lbl` | вышестоящий узел оргструктуры на дату | свод по блокам | `ADR-0241` §2 |
| Куратор | `d_curator_id` `d_curator_lbl` | куратор ОД, ведущий заявку | колонка охвата; «нагрузка по кураторам» | ТЗ 15 §9 · `ИС-58` |
| Заёмщик | `d_borrower_id` `d_borrower_lbl` | якорь заявки — ключ субъекта | у заявки заёмщик ОДИН, в отличие от кредита; join к `stat_row_borrower` | ТЗ 15 §2.1 |
| Стадия | `d_stage` | девять стадий конвейера: регистрация → … → закрыта | «где стоят обращения»: сколько ждёт заключения органа, сколько на оформлении | ТЗ 15 §3 · РС-23 |
| Исход | `d_outcome` | одобрено · одобрено с изменёнными условиями · отказ · отпала; `NULL` до решения | доля отказов и «отпавших»; пустое не есть отказ | ТЗ 15 §3, §9 |
| Многократная | `d_multi` | признак с ТРЕТЬЕЙ реструктуризации кредита | кредит, не просроченный ни дня, но реструктурированный трижды, обязан читаться как проблемный | РС-32 |
| Дата регистрации | `d_regdate` | день официальной регистрации обращения | якорь регламентного срока; корзины год · квартал · месяц | ТЗ 15 §3 |

### Показатели

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| База среза по заявке | `i_base_som` | сумма баз среза всех расчётов заявки, в сомах | по ней срабатывает ступень 2 порога — комитет высказывался по обращению целиком; охват разновалютен, поэтому только сомовая колонка | РС-26 · `ADR-0240` §4 |
| Дней с регистрации | `i_days` | срок рассмотрения обращения; у закрытой замирает | регламентный срок мягкий: число показывает просрочку регламента, а не запрещает её | ТЗ 15 §3 |
| Расчётов в заявке | `i_calcs` | сколько траншей в охвате | «обращения по одному траншу и по многим»: у многотраншевых порог ведёт себя иначе | РС-26 |

---

## 18. `stat_row_rs_calc` — расчёт реструктуризации

Расчёт — рабочая единица: заявка × транш. Та же пара уровней, что «дело → требование» и
«поступление → платёж». Ступень 1 порога расхождения считается по КАЖДОМУ расчёту отдельно —
виза куратора блокирует регистрацию конкретного ДС; ступень 2 — по сумме баз всей заявки
(§17). Расхождение считает владелец: вывести его вычитанием витрина не вправе, иначе
арбитров порога станет двое (`ИС-1`).

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Расчёт | `object_id` | id расчёта в модуле «Реструктуризация» | первая половина ключа | ТЗ 15 §2.3 |
| Дата среза | `slice_date` | дата, на начало которой снято состояние | вторая половина ключа | `ADR-0238` §2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ «Реструктуризации» | `src_rs` | ответил ли владелец за эту строку | молчание не притворяется нулём | `ADR-0208` §2 |
| Что молчало | `src_detail` | какие величины не пришли и почему | сноска паспорта | `ADR-0208` §2 |
| Строка неполна | `is_partial` | вычисляется из ответа владельца | отбор полных строк | `ADR-0208` §2 |
| Неточные величины | `now_cols` | имена величин, отданных «текущими» | сноска паспорта | `ADR-0242` |
| Валюта | `cur` | валюта транша-источника | базы разных валют не складываются молча | `ADR-0214` §1 |
| Курс строки | `rate` | курс к сому, замороженный в строке | сомовая сторона по курсу своей строки | `ADR-0214` §2 |
| Дата курса | `rate_date` | на какой день взят курс | ответ показывает, каким курсом пересчитано | `ADR-0214` §2 |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Кредит | `d_credit_id` `d_credit_lbl` | кредит, по траншу которого идёт расчёт | «реструктуризации по программе, отрасли, филиалу» — всё это приходит join-ом к кредиту | `ADR-0241` §8 |
| Транш | `d_tranche_id` `d_tranche_lbl` | транш-источник переноса | охват заявки суть траншы, а не кредиты целиком | ТЗ 15 §2.2 |
| Заявка | `d_app_id` | ключ вверх, к обращению | свод расчётов по заявке; подпись не нужна — она у заявки | ТЗ 15 §2 |
| Ступень расхождения | `d_step` | нет · ступень 1 · ступень 2 | «сколько расчётов ушло на визу куратора и сколько вернулось на комитет» | РС-26 |
| Состояние ДС | `d_ds_state` | не зарегистрировано · зарегистрировано · снят с основанием | хвост оформления: что зарегистрировано, что снято | ТЗ 15 §3 |

### Деньги

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| База среза | `i_base_v` `i_base_som` | согласовательная величина: долг на дату среза | то, что согласовывали заключения, комитет и письмо | ТЗ 15 §6.1 |
| Факт на дату ДС | `i_fact_v` `i_fact_som` | та же база, пересчитанная на дату подписания | между срезом и ДС лежит до 105 дней, и всё это время идёт начисление | ТЗ 15 §6.1 |
| Расхождение | `i_diff_v` `i_diff_som` | разница «срез ↔ факт» по базе целиком | вход порога РС-26; считает владелец, витрина только хранит | ИР-18 |
| Сумма переноса | `i_transfer_v` `i_transfer_som` | что ушло в производный транш | реквизит ДС; «сколько перенесено за период» | ТЗ 15 §6.4 |

---

## 19. `stat_row_staff` — сотрудник

Строка сотрудника начинается с приёма — начала первого основного назначения; «Оформляется»
строки не имеет. Три оси статуса ТЗ 11 §5 — три разные колонки, а не одна с оговоркой:
уволенный с активной учётной записью и работающий в отпуске суть разные факты. Денежных
колонок нет: нагрузка куратора отвечается срезом `stat_row_credit` по `d_curator_id`, и
второй адрес у той же величины был бы формой `E2E-09`.

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Сотрудник | `object_id` | id сотрудника в модуле «Сотрудники» | первая половина ключа; «численность на дату» — число строк | ТЗ 11 §4.1 |
| Дата среза | `slice_date` | дата, на начало которой снято состояние | вторая половина ключа; все колонки — на дату среза | ТЗ 11 §3.2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ «Сотрудников» | `src_hr` | ответил ли владелец за эту строку | молчание не притворяется нулём | `ADR-0208` §2 |
| Что молчало | `src_detail` | какие величины не пришли и почему | сноска паспорта | `ADR-0208` §2 |
| Строка неполна | `is_partial` | вычисляется из ответа владельца | отбор полных строк | `ADR-0208` §2 |
| Неточные величины | `now_cols` | имена величин, отданных «текущими» | сноска паспорта | `ADR-0242` |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Подразделение | `d_unit_id` `d_unit_lbl` | подразделение на дату из основного назначения | «штат по подразделениям на дату»; при переименовании узла подпись берётся из строки | ТЗ 11 §5 · `ADR-0241` §2 |
| Блок | `d_unit_parent_id` `d_unit_parent_lbl` | вышестоящий узел оргструктуры на дату | свод по блокам | `ADR-0241` §2 |
| Должность | `d_post_id` `d_post_lbl` | должность на дату из основного назначения | «сколько ведущих специалистов», «вакансии по должностям» | ТЗ 11 §5 |
| Кадровый статус | `d_hr_state` | ось 1: Работает · Уволен · Оформляется | считается из назначений-периодов, а не из поля | ТЗ 11 §5 |
| Доступность | `d_avail` | ось 2: На месте · В отпуске · На больничном · В командировке | «кого нет на месте и кто замещает» | ТЗ 11 §5 |
| Учётная запись | `d_acct` | ось 3: активна · заблокирована · не заведена | кураторство висит на ЗАПИСИ, а не на сотруднике: блокировка и увольнение по-разному влияют на маршрутизацию | ТЗ 11 §5, §11 |
| Вид назначения | `d_assign_kind` | основное · совмещение · и.о. | «сколько человек работает по совмещению и сколько и.о.» | ТЗ 11 §5 |
| Дата приёма | `d_hired` | день начала первого основного назначения | корзины год · квартал: «принято за 2026 год» | ТЗ 11 §9 |

### Показатели

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Стаж | `i_tenure_days` | дней в Фонде; у уволенного замирает на дате увольнения | «средний стаж по подразделениям», «кто работает дольше всех» | ТЗ 11 §5 |
| Ставок | `i_rate` | сумма действующих ставок на дату | лимит 1.5 держит владелец; витрина показывает, у кого сколько | ТЗ 11 §7 |

---

## 20. `stat_row_overdue_spell` — эпизод просрочки

Эпизод — кредит × слой × непрерывный период просрочки, от выхода до лечения. Таблица
заведена, а строк в ней нет: `calcDebt` отдаёт просрочку НА ДАТУ (с какой даты · дней ·
спорных дней) по каждому слою, но перечня эпизодов не отдаёт никто, а вывести его самой
статистике запрещено (`ИС-1`). Объект объявлен целиком — реестром и таблицей (`ИС-53`), —
и все четыре двери отвечают отказом, называющим заявку владельцу: **`P20-R33`, шов
`overdueSpells`**. Пустой ответ был бы неотличим от «ни одного эпизода не было» (`ИС-24`).
Вопросы, ради которых эпизод и заводится: сколько РАЗ выходили в просрочку, какова средняя
длительность, что вылечено, а что тянется, и есть ли рецидив.

### Служебные

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Эпизод | `object_id` | id эпизода у ядра: кредит × слой × дата выхода | первая половина ключа; «сколько эпизодов было» — число строк | `P20-R33` |
| Дата среза | `slice_date` | дата, на начало которой снято состояние | вторая половина ключа | `ADR-0238` §2 |
| Прогон | `run_id` | прогон, который последним записал строку | объяснить строку в журнале срезов | `ADR-0245` §2 |
| Ответ ядра | `src_core` | ответило ли ядро за эту строку | пока шва нет, отвечает отказ на двери, а не пустая строка | `ADR-0208` §2 |
| Что молчало | `src_detail` | какие величины не пришли и почему | сноска паспорта | `ADR-0208` §2 |
| Строка неполна | `is_partial` | вычисляется из ответа ядра | отбор полных строк | `ADR-0208` §2 |
| Неточные величины | `now_cols` | имена величин, отданных «текущими» | сноска паспорта | `ADR-0242` |
| Валюта | `cur` | валюта кредита эпизода | пики разных валют не складываются молча | `ADR-0214` §1 |
| Курс строки | `rate` | курс к сому, замороженный в строке | сомовая сторона по курсу своей строки | `ADR-0214` §2 |
| Дата курса | `rate_date` | на какой день взят курс | ответ показывает, каким курсом пересчитано | `ADR-0214` §2 |

### Разрезы

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Кредит | `d_credit_id` `d_credit_lbl` | кредит, по которому идёт эпизод | join к `stat_row_credit`: эпизоды раскладываются по филиалу, куратору, программе | `ADR-0241` §8 |
| Слой долга | `d_layer` | свободный · по решению суда | дни просрочки считаются ВНУТРИ слоя от своей даты: иначе кредит с новым решением суда мгновенно выглядит просроченным на весь остаток | ТЗ 18 §5 |
| Состояние | `d_state` | тянется · вылечен | «сколько эпизодов вылечено за период, сколько тянется» | `P20-R33` |
| Дата выхода | `d_from` | день выхода в просрочку по этому слою | корзины год · квартал · месяц: «эпизодов началось в июле» | ТЗ 18 §5 |

### Показатели

| Поле | Колонки | Что это | Зачем | Опора |
|---|---|---|---|---|
| Пик просрочки | `i_peak_v` `i_peak_som` | наибольшая просроченная сумма за эпизод | эпизод на 300 сомов и эпизод на 3 млн по числу дней неразличимы, по пику — нет | `P20-R33` |
| Длительность | `i_days` | у тянущегося — дней на дату среза, у вылеченного — дней до лечения | «средняя длительность эпизода», «рецидив: второй эпизод по тому же кредиту» | `P20-R33` |
